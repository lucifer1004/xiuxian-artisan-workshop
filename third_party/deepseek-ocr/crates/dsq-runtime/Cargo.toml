[package]
name = "deepseek-ocr-dsq-runtime"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1.0"
candle-core = { version = "0.9", default-features = false }
deepseek-ocr-dsq = { path = "../dsq" }
half = "2.4"
rayon = "1.10"
tracing = "0.1"

[features]
default = []
qtensor-bytes = []
