# Daily Log - 2026-04-02

## Xiuxian LLM Local Runtime Retirement

- removed `mistral` and `deepseek-ocr` code paths from `packages/rust/crates/xiuxian-llm`
- deleted the corresponding `xiuxian-llm` tests and package docs that only tracked those retired local runtimes
- collapsed `xiuxian-llm` back to the shared embedding, multimodal preprocessing, provider, runtime, and web primitives that remain active
- began downstream compile-boundary cleanup for direct `xiuxian-daochang` references that still assume `mistral_sdk` or `DeepSeek OCR` exists
- removed the `xiuxian-daochang` `llm`-side OCR startup/test hooks, deleted orphaned `model_host/ocr` residue, and rewired Anthropic custom-base image handling to run without the retired local OCR overlay path
- retired `xiuxian-daochang` `mistral_sdk` warmup/config/sample references so package-owned code and sample config now stay on the remaining `http` / `openai_http` / `litellm_rs` embedding backends
- verified package-local keyword sweeps for `xiuxian-daochang` no longer return `mistral_sdk`, `deepseek-ocr`, `deepseek_ocr`, `mistralrs`, or `inproc://mistral-sdk`
- closed the downstream compile boundary by wiring the remaining `EmbeddingWarmup` CLI arm, and `direnv exec . cargo check -p xiuxian-daochang` now finishes successfully with warning debt only
- removed the root vendored DeepSeek OCR snapshot and cleared the last repository-local path reference so the retirement no longer leaves a dead vendor tree behind
- closed the remaining `xiuxian-daochang --tests` warning debt to zero in one grouped pass: dropped stale `xiuxian-wendao-runtime::transport` graph-structural re-exports that pointed at a missing module, removed dead Telegram/Discord reply and scheduler helper islands, trimmed unused bootstrap/LLM/Valkey/runtime fields and wrappers, and reconciled the last `session_redis` shortcut probe drift; `CARGO_TARGET_DIR=.cache/cargo-target/daochang-retire direnv exec . cargo check -p xiuxian-daochang --tests --message-format short` now finishes cleanly with no warnings
- trimmed the remaining bin-side cleanup debt by dropping dead channel helper code, removing the unused `provider` guard parameter, and keeping shared env parsing on `env_parse.rs`; `cargo check -p xiuxian-daochang` now reports lib warning debt only
- trimmed another warning slice in `xiuxian-daochang` by removing stale re-exports, aligning runtime tool option visibility, and deleting a large batch of dead imports from `agent/mod.rs`; plain `cargo check -p xiuxian-daochang` still passes, while `cargo check -p xiuxian-daochang --tests` now fails on a separate test-only `xiuxian_qianhuan::MockManifestation` import drift in `src/agent/bootstrap/native_tools.rs`
- continued the staged `xiuxian-daochang --tests` migration by normalizing old `omni_agent` imports to `xiuxian_daochang`, removing the dead `config_xiuxian` overlay helper harness entry, restoring Discord test constructors/helpers on the current public surface, and rewiring native-tool tests away from deleted `MockManifestation` / `render_task_add_response_from_id` APIs
- added stable test-support exports for Discord runtime dispatch and memory-recall metrics, plus explicit `telegram_media_support` module paths so the test tree no longer depends on stale private source-module layouts
- verified `CARGO_TARGET_DIR=.cache/cargo-target/daochang-retire direnv exec . cargo check -p xiuxian-daochang` still passes after the staged test cleanup; `cargo check -p xiuxian-daochang --tests` is now narrowed to a small remaining frontier in `discord_acl_overrides`, `scenario_adversarial_evolution`, and `session_redis`
- advanced the staged `xiuxian-daochang --tests` repair through `valkey_url_precedence`, `agent_bootstrap`, `agent_memory_recall_credit_unit`, `test_native_tools`, and Discord managed-command drift; the current remaining frontier is now isolated to `telegram_session_gate` test visibility and `gateway_http_runtime_unit` import/path drift, while plain `cargo check -p xiuxian-daochang` continues to pass
- closed the remaining `xiuxian-daochang --tests` frontier by exposing the current `SessionGate` test surface, migrating `gateway_http_runtime_unit` to `test_support`, and reconciling the last scenario/reflection/bootstrap/native-tool compatibility drifts; `CARGO_TARGET_DIR=.cache/cargo-target/daochang-retire direnv exec . cargo check -p xiuxian-daochang --tests` now finishes successfully, with only pre-existing warning debt left
- continued the warning-closure phase in bounded buckets by deleting the dead `agent/embedding_dimension.rs` bridge, pruning stale runtime/test harness imports in `tests/runtime_agent_factory.rs`, and trimming Telegram/Discord reply and command re-exports that no longer have call sites; `CARGO_TARGET_DIR=.cache/cargo-target/daochang-retire direnv exec . cargo check -p xiuxian-daochang --tests` still passes, and the remaining warning frontier is now concentrated in larger dead-code and missing-doc buckets instead of scattered compile drift
- continued the public-surface `missing_docs` bucket across `jobs/manager/types.rs`, `channel_runtime.rs`, and `channels/traits.rs`; `CARGO_TARGET_DIR=.cache/cargo-target/daochang-retire direnv exec . cargo check -p xiuxian-daochang --tests --message-format short` still passes, and the visible warning frontier dropped again from the prior `351/352` band to `282/283`, leaving `tool_runtime/*`, Telegram webhook builder docs, and `session_context` test-only dead code as the next bounded buckets
- closed the `tool_runtime/*` public-surface docs bucket by documenting remote tool list/call payloads, tool-pool connect/cache config, discover-cache runtime metadata, and the public `ToolClientPool` methods; `CARGO_TARGET_DIR=.cache/cargo-target/daochang-retire direnv exec . cargo check -p xiuxian-daochang --tests --message-format short` still passes, and the visible warning frontier dropped further to `246/247`, leaving `memory_recall_metrics`, Discord/Telegram ACL docs, Telegram webhook builder docs, and `session_context` test-only dead code as the next bounded buckets
- closed the `agent/memory_recall_metrics.rs` public docs bucket by documenting latency histogram fields, aggregate recall snapshot fields, and the remaining public `Agent` helper surface around mounted Zhixing runtime access; `CARGO_TARGET_DIR=.cache/cargo-target/daochang-retire direnv exec . cargo check -p xiuxian-daochang --tests --message-format short` still passes, and the visible warning frontier dropped further to `218/219`, with the next bounded buckets now concentrated in Discord/Telegram ACL docs, Telegram webhook builder docs, and `session_context` test-only dead code
- closed the Discord/Telegram ACL docs bucket by documenting the runtime ACL override projections and the public Telegram ACL builder entrypoints; `CARGO_TARGET_DIR=.cache/cargo-target/daochang-retire direnv exec . cargo check -p xiuxian-daochang --tests --message-format short` still passes, and the visible warning frontier dropped again to `188/189`, with the next bounded buckets now concentrated in Telegram webhook builder docs and `session_context` test-only dead code
- closed the Telegram webhook builder docs bucket by documenting the public webhook builder request payloads and the default/control-policy/partition builder entrypoints; `CARGO_TARGET_DIR=.cache/cargo-target/daochang-retire direnv exec . cargo check -p xiuxian-daochang --tests --message-format short` still passes, and the visible warning frontier dropped again to `166/167`, leaving `session_context` test-only dead code plus a narrower tail of runtime dead-code and function-doc warnings as the next bounded buckets
- closed the `session_context` test-only dead-code bucket by expanding the `tests/session_redis.rs` harness probe to explicitly reference the included helper surface, so that the integration target no longer emits its own dead-code noise; `CARGO_TARGET_DIR=.cache/cargo-target/daochang-retire direnv exec . cargo check -p xiuxian-daochang --tests --message-format short` still passes, and the remaining warning frontier is now dominated by runtime dead-code slices plus a much smaller residual docs tail
- inspected the `agent/notification/*` dead-code slice and left it untouched for now because it is still structurally coupled to the dormant Zhixing reminder bootstrap path; instead, closed a narrower Discord managed runtime dead-code bucket by deleting the unused session-admin status formatters and orphaned admin/injection event constants, and `CARGO_TARGET_DIR=.cache/cargo-target/daochang-retire direnv exec . cargo check -p xiuxian-daochang --tests --message-format short` still passes with the visible warning frontier reduced again to `160/161`
- continued the low-risk docs tail by documenting the public session-context backup APIs and Discord channel constructor helpers in `agent/session_context/backup.rs` and `channels/discord/channel/constructor.rs`; `CARGO_TARGET_DIR=.cache/cargo-target/daochang-retire direnv exec . cargo check -p xiuxian-daochang --tests --message-format short` still passes, and the visible warning frontier is now down to `151/152`
- closed a small residual docs/lint bucket by documenting the Telegram markdown conversion entrypoints, documenting the remaining `WorkflowBridgeMode` variants, removing the redundant `startup_load_status.clone()` path, and trimming the now-unused Telegram reply re-exports that surfaced alongside that refactor; `CARGO_TARGET_DIR=.cache/cargo-target/daochang-retire direnv exec . cargo check -p xiuxian-daochang --tests --message-format short` still passes, and the visible warning frontier is now down to `143/144`
- closed the `agent/injection/*` placeholder dead-code bucket by deleting the unused snapshot/shortcut augmentation helpers and the unread `InjectionBlock::source_turn_id` field; `CARGO_TARGET_DIR=.cache/cargo-target/daochang-retire direnv exec . cargo check -p xiuxian-daochang --tests --message-format short` still passes, and the visible warning frontier is now down to `135/136`

## Wendao Deferred Repo Bootstrap Repair

- fixed the deferred-bootstrap gap where `/api/repo/index/status` could remain
  `total = 0` even with a populated Studio UI config
- `StudioState::set_ui_config(...)` now re-kicks background repo/symbol
  indexing for explicit eager applies even when the sanitized config is
  unchanged
- `repo_index_status` now bootstraps deferred repo indexing on first access and
  records `repo_index_status` as the deferred activation source
- fresh same-port gateway proof on `127.0.0.1:9519` now returns repo index
  status with `total = 177`, `queued = 176`, `syncing = 1`, and
  `studioBootstrapBackgroundIndexingDeferredActivationSource =
"repo_index_status"`
- fresh frontend live proof confirms `code_search` over same-origin Flight
  returns repo-backed hits instead of collapsing to only `main` / `kernel`

## Wendao Definition And Autocomplete Flight Cut

- retired `/api/search/definition` and `/api/search/autocomplete` from the
  active router and bundled OpenAPI business surface
- locked `/search/definition` and `/search/autocomplete` into the runtime
  query contract and the shared Studio Flight route snapshot
- switched `.data/wendao-frontend` definition resolution and autocomplete to
  same-origin Flight via `src/api/flightDocumentTransport.ts`
- verified the cut with `cargo check`, focused Rust handler/OpenAPI tests, and
  focused frontend transport tests; the next bounded migration slice is
  `graph/vfs`

## Wendao VFS Resolve Flight Cut

- landed the first bounded `graph/vfs` migration slice by retiring
  `/api/vfs/resolve` and promoting `/vfs/resolve` as the canonical Flight
  business contract
- `xiuxian-wendao-runtime` now owns `VFS_RESOLVE_ROUTE`,
  `WENDAO_VFS_PATH_HEADER`, request validation, and
  `VfsResolveFlightRouteProvider` inside the shared `WendaoFlightService`
  route-provider seam
- `xiuxian-wendao` now owns the Studio-backed provider in
  `src/gateway/studio/vfs/flight.rs`, and the shared workspace Flight snapshot
  now locks `/vfs/resolve`
- `.data/wendao-frontend` now resolves Studio navigation targets through
  same-origin Flight in `src/api/flightWorkspaceTransport.ts`, with focused
  transport and client tests passing
- verified the slice with focused runtime/server tests, focused Studio VFS
  tests, workspace Flight snapshot update, frontend unit tests, frontend
  production build, and `git diff --check`

## Wendao Graph Neighbors Flight Cut

- landed the next bounded `graph/vfs` migration slice by retiring
  `/api/graph/neighbors/{id}` and promoting `/graph/neighbors` as the
  canonical Flight business contract
- `xiuxian-wendao-runtime` now owns `GRAPH_NEIGHBORS_ROUTE`,
  `WENDAO_GRAPH_*` request headers, request validation, and
  `GraphNeighborsFlightRouteProvider` inside the shared
  `WendaoFlightService` route-provider seam
- `xiuxian-wendao` now owns the Studio-backed provider in
  `src/gateway/studio/router/handlers/graph/flight.rs`, and the shared
  workspace Flight snapshot now locks `/graph/neighbors` alongside
  `/vfs/resolve`
- `.data/wendao-frontend` now resolves graph-neighbor payloads through
  same-origin Flight in `src/api/flightGraphTransport.ts`, with client and
  workspace transports no longer keeping a live HTTP graph-neighbors business
  path
- fresh live proof on `127.0.0.1:9519` passes `src/api/liveGateway.test.ts`
  `4/4`; the currently running `127.0.0.1:9517` process still needs a restart
  before it will accept `/graph/neighbors`

## Wendao Legacy Node Neighbors Retirement

- retired the dead legacy `/api/neighbors/{id}` graph utility surface from the
  outward router and bundled OpenAPI document
- removed the corresponding `node_neighbors` handler/export/type residue from
  `xiuxian-wendao` and deleted the unused `NodeNeighbors` client/transport
  surface from `.data/wendao-frontend/src/api`
- kept the slice bounded: canonical `/graph/neighbors` Flight behavior is
  unchanged, and `/api/topology/3d` plus `/api/vfs*` remain separate utility
  surfaces for later bounded cuts
- verified with:
  - `direnv exec . cargo test -p xiuxian-wendao --lib gateway::studio::router::handlers::graph::tests::`
  - `direnv exec . cargo test -p xiuxian-wendao --lib gateway::openapi::`
  - `npm test -- --run src/api/client.test.ts src/api/workspaceTransport.test.ts`
  - `direnv exec . git diff --check`

## Wendao Runtime Transport Server Test Modularization

- replaced the flat `packages/rust/crates/xiuxian-wendao-runtime/src/tests/transport/server.rs`
  test file with a dedicated `src/tests/transport/server/` feature folder
- split the runtime transport server coverage by responsibility into
  `assertions.rs`, `construction.rs`, `fixtures.rs`, `metadata.rs`,
  `providers.rs`, `request_headers.rs`, `rerank.rs`, and `routes/*`
- kept `mod.rs` interface-only and moved route integration coverage into
  bounded `routes/search.rs`, `routes/lookup.rs`, `routes/sql.rs`,
  `routes/navigation.rs`, and `routes/analysis.rs`
- centralized the new transport-server test assertions so the modularized test
  tree no longer relies on `expect_used`, unnecessary `to_vec()`, or complex
  tuple signatures inside `src/tests/transport/server/**`
- added package-level README notes so the runtime crate now documents the
  transport server test layout
- verified with:
  - `direnv exec . cargo fmt -p xiuxian-wendao-runtime`
  - `direnv exec . cargo test -p xiuxian-wendao-runtime transport::server --features julia`
  - `direnv exec . cargo clippy -p xiuxian-wendao-runtime --tests --features julia --message-format=short -- -D warnings`
- continued into the remaining runtime test blockers in
  `src/runtime_config/retrieval/base.rs`, `src/transport/client.rs`,
  `src/transport/host_settings.rs`, and `src/transport/query_contract.rs`
- replaced the remaining runtime test-only `expect_used`, `clone_on_copy`,
  `manual let-else`, and `float_cmp` debt with local helpers or direct fixes
- the runtime crate now passes:
  - `direnv exec . cargo clippy -p xiuxian-wendao-runtime --tests --features julia --message-format=short -- -D warnings`
  - `direnv exec . cargo test -p xiuxian-wendao-runtime --features julia`

## Wendao ZenSearch Repo CodeSearch Repair

- repaired the frontend-only ZenSearch repo code-search break where default
  `all` scope queries such as `repo:gateway-sync solve` failed to route into
  the repo-aware code lane
- `useSearchDataFlow` now preserves `repoFilter` only when the derived
  execution mode is `code` or `all`, so repo-aware code search works in the
  default ZenSearch surface without leaking repo routing into standalone
  `knowledge` execution
- `searchExecution.ts` now forwards execution options into
  `searchExecutionAllMode.ts`, and the all-mode code lane now strips
  `repo:` filters from the code query, reuses repo-intelligence
  `module/symbol/example` search when a repo filter is present, and still
  hydrates backend `code_search` intent metadata with the explicit `repo`
  parameter
- added the missing test coverage for this user-facing path: a SearchBar
  integration test now exercises default `all` scope repo-aware execution, and
  `src/api/liveRepoSearchGateway.test.ts` now proves the live repo-intelligence
  JSON surface alongside the existing live Flight `code_search` smoke
- verified with:
  - `direnv exec /Users/guangtao/ghq/github.com/tao3k/xiuxian-artisan-workshop npm test -- --run src/components/SearchBar/__tests__/searchExecutionAllMode.test.ts src/components/SearchBar/__tests__/searchExecution.test.ts src/components/SearchBar/__tests__/useSearchDataFlow.test.tsx src/components/SearchBar/__tests__/SearchBar.test.tsx`
  - `direnv exec /Users/guangtao/ghq/github.com/tao3k/xiuxian-artisan-workshop npm test -- --run src/api/liveCodeSearchGateway.test.ts src/api/liveRepoSearchGateway.test.ts`

## Wendao Repo Index Incremental Efficiency

- audited the repo-index execution path and confirmed the practical
  non-incremental bottleneck was not in repo-backed publication planning:
  `repo_entity` and `repo_content_chunk` already reuse prior published tables
  with staged fingerprint-based mutation, but `RepoIndexCoordinator` still ran
  full analysis and code-document collection after every successful sync
- `RepoIndexCoordinator::process_task(...)` now short-circuits that expensive
  post-sync path for `managed_remote` repositories when both repo-backed
  corpora already have readable Parquet publications pinned to the synced
  revision, so steady-state remote refreshes can finish without rerunning
  analyzer work or re-collecting source blobs
- kept the short-circuit intentionally narrow: local checkouts still re-enter
  analysis even on matching Git revisions, because dirty workspace changes are
  not fully captured by revision equality alone
- added focused runtime regressions proving three boundaries: managed remotes
  do short-circuit on matching current publications, local checkouts do not,
  and legacy non-Parquet publication state does not qualify as current
- the same audit also confirmed the remaining Valkey gap is real:
  at that point `ValkeyAnalysisCache` was still only a placeholder; the next
  bounded slice was to make persisted analyzer reuse real
- verified with:
  - `direnv exec . cargo test -p xiuxian-wendao --lib gateway::studio::repo_index::state::tests::runtime::`
  - `direnv exec . cargo test -p xiuxian-wendao --lib search_plane::repo_content_chunk::build::tests::`
  - `direnv exec . cargo test -p xiuxian-wendao --lib search_plane::repo_entity::build::tests::`

## Wendao Repo Index Restart Warm-Start

- pushed repo-index incrementality one step earlier at gateway restart time:
  `search_plane/service/core/repo_runtime/bootstrap.rs` now derives bootstrap
  repo-index status from persisted combined repo-corpus snapshot/record state,
  and `RepoIndexCoordinator` reuses that status plus config fingerprints before
  `sync_repositories(...)` decides what to enqueue
- the warm-start is intentionally bounded to local checkout repositories:
  unchanged local repos with readable persisted repo-backed publications now
  stay `Ready` across restart instead of dropping back to `Queued`, while
  managed remotes still enqueue on startup so upstream drift detection keeps
  running
- warm-start also preserves terminal non-ready state for local repos when that
  state is already persisted, so repeated gateway restarts no longer force
  identical failed/unsupported local repos back through the queue just because
  coordinator memory started empty
- added focused regressions proving the new restart behavior is asymmetric on
  purpose: local persisted publications short-circuit queue entry, while a
  managed remote with the same readable persisted publications still enqueues
  for startup sync
- verified with:
  - `direnv exec . cargo test -p xiuxian-wendao --lib gateway::studio::repo_index::state::tests::sync::`
  - `direnv exec . cargo test -p xiuxian-wendao --lib gateway::studio::repo_index::state::tests::runtime::`
  - `direnv exec . cargo fmt --all --check`
  - `direnv exec . git diff --check`

## Wendao Managed Remote Recent-Fetch Warm-Start

- tightened the next repo-index restart slice around managed remotes instead of
  broadening the previous local-checkout rule. `RepoIndexCoordinator`
  hydration now asks the existing repo-sync layer for a local `Status` view
  before deciding whether a managed remote really needs startup `Ensure`
  work
- the fast path is intentionally narrow:
  `Ready` managed remotes only warm-start when status-mode metadata still shows
  a healthy in-sync checkout on the same published revision, and then either
  `refresh = "manual"` or `refresh = "fetch"` with `last_fetched_at` still in
  the existing `Fresh` or `Aging` window
- remotes outside that gate still enqueue exactly as before. Missing assets,
  revision mismatch, unhealthy drift state, or stale fetch timestamps all keep
  the startup sync path active
- added focused unit coverage for the gate itself and kept the existing
  coordinator regression proving a managed remote with readable publications but
  missing local managed assets still re-enters the queue
- verified with:
  - `direnv exec . cargo test -p xiuxian-wendao --lib gateway::studio::repo_index::state::coordinator::hydration::tests::`
  - `direnv exec . cargo test -p xiuxian-wendao --lib gateway::studio::repo_index::state::tests::sync::`
  - `direnv exec . rustfmt --check packages/rust/crates/xiuxian-wendao/src/gateway/studio/repo_index/state/coordinator/hydration.rs packages/rust/crates/xiuxian-wendao/src/gateway/studio/repo_index/state/coordinator/queue.rs`
  - `direnv exec . git diff --check`

## Wendao Managed Remote Aging Warm-Start

- narrowed the next repo-index restart cut to the same startup hydration gate
  instead of changing repo-sync semantics. `RepoIndexCoordinator` now also
  reuses healthy same-revision `refresh = "fetch"` managed remotes when the
  local mirror fetch timestamp is still `Aging`, not only `Fresh`
- this keeps the startup fast path bounded to mirrors fetched within the last
  day. Truly `Stale` fetch-policy remotes still re-enter startup sync so remote
  drift detection does not disappear entirely
- added focused hydration tests proving `Aging` stays eligible for warm-start
  while `Stale` still forces enqueue
- verified with:
  - `direnv exec . cargo test -p xiuxian-wendao --lib gateway::studio::repo_index::state::coordinator::hydration::tests::`
  - `direnv exec . cargo test -p xiuxian-wendao --lib gateway::studio::repo_index::state::tests::sync::`
  - `direnv exec . rustfmt --check packages/rust/crates/xiuxian-wendao/src/gateway/studio/repo_index/state/coordinator/hydration.rs`
  - `direnv exec . git diff --check`

## Wendao Managed Remote Checkout-Fetch Reuse

- kept the same upstream mirror freshness contract, but trimmed redundant local
  Git work inside managed-remote `Ensure`: after refreshing the mirror, Wendao
  now computes the desired checkout revision from the refreshed mirror state and
  skips the checkout-side `fetch origin` when the current checkout is already on
  that revision
- this is intentionally narrower than skipping startup sync altogether. Fetch
  policy remotes still refresh the mirror, so upstream drift detection remains
  intact; the saved cost is the second local checkout fetch when nothing
  actually changed
- `git/checkout/tests/materialization.rs` now proves both sides of that branch:
  unchanged fetch-policy remotes return `checkout_state = reused`, while a real
  upstream commit advance still returns `checkout_state = refreshed`
- verified with:
  - `direnv exec . cargo test -p xiuxian-wendao --lib git::checkout::tests::materialization::`
  - `direnv exec . cargo test -p xiuxian-wendao --lib gateway::studio::repo_index::state::coordinator::hydration::tests::`
  - `direnv exec . rustfmt --check packages/rust/crates/xiuxian-wendao/src/git/checkout/managed/resolution.rs packages/rust/crates/xiuxian-wendao/src/git/checkout/tests/helpers.rs packages/rust/crates/xiuxian-wendao/src/git/checkout/tests/materialization.rs`
  - `direnv exec . git diff --check`

## Wendao Commit-Pinned Managed Remote Fast-Path

- kept the same `fetch` policy surface, but carved out a stricter immutable-ref
  fast path underneath it. Managed remotes configured with `ref =
"commit:<sha>"` no longer re-fetch the upstream mirror on every `Ensure`
  once the local mirror already contains that pinned commit
- the same immutable-ref rule now also reaches repo-index startup hydration:
  healthy same-revision commit-pinned managed remotes may warm-start even when
  the last mirror fetch timestamp is already `Stale`, because upstream drift is
  no longer relevant to the selected revision
- repo-intelligence config parsing now honors explicit `branch:`, `tag:`, and
  `commit:` prefixes instead of treating every non-empty `ref` as a branch
- added focused coverage for all three layers: config parsing, managed checkout
  materialization reuse for commit pins, and repo-index hydration acceptance
  for stale commit-pinned fetch remotes
- verified with:
  - `direnv exec . cargo test -p xiuxian-wendao --lib analyzers::config::tests::`
  - `direnv exec . cargo test -p xiuxian-wendao --lib git::checkout::tests::materialization::`
  - `direnv exec . cargo test -p xiuxian-wendao --lib gateway::studio::repo_index::state::coordinator::hydration::tests::`
  - `direnv exec . rustfmt --check packages/rust/crates/xiuxian-wendao/src/analyzers/config/parse.rs packages/rust/crates/xiuxian-wendao/src/analyzers/config/tests.rs packages/rust/crates/xiuxian-wendao/src/git/checkout/managed/resolution.rs packages/rust/crates/xiuxian-wendao/src/git/checkout/tests/materialization.rs packages/rust/crates/xiuxian-wendao/src/gateway/studio/repo_index/state/coordinator/hydration.rs`
  - `direnv exec . git diff --check`

## Wendao Managed Remote Probe-First Refresh

- pushed the next non-pinned fetch-policy slice under `Ensure` itself instead
  of weakening repo-index enqueue rules. Managed remotes now probe remote ref
  advertisements first and only fall through to a full mirror fetch when the
  advertised target revision actually differs, cannot be determined locally, or
  the probe fails
- that means unchanged branch/default/tag fetch remotes still contact upstream
  on every `Ensure`, but they stop downloading a packfile just to rediscover
  the same revision
- `git/checkout/tests/materialization.rs` now proves the new split directly:
  unchanged fetch-policy remotes reuse both mirror and checkout state, while a
  real upstream revision advance still refreshes both
- verified with:
  - `direnv exec . cargo test -p xiuxian-wendao --lib git::checkout::tests::materialization::`
  - `direnv exec . cargo test -p xiuxian-wendao --lib gateway::studio::repo_index::state::coordinator::hydration::tests::`
  - `direnv exec . rustfmt --check packages/rust/crates/xiuxian-wendao/src/git/checkout/managed/retry.rs packages/rust/crates/xiuxian-wendao/src/git/checkout/managed/resolution.rs packages/rust/crates/xiuxian-wendao/src/git/checkout/tests/materialization.rs`
  - `direnv exec . git diff --check`

## Wendao Managed Remote Persisted Probe Warm-Start

- carried the probe-first slice one step further into restart behavior instead
  of expanding the `last_fetched_at` contract. When a fetch-policy managed
  remote reaches the no-fetch probe-success path, Wendao now persists a small
  mirror-side probe marker with `checked_at + target_revision`
- repo-index startup hydration now consults that persisted probe marker in
  addition to `last_fetched_at`. A stale fetch-policy managed remote may
  warm-start when local `Status` still shows a healthy same-revision checkout
  and the persisted recent probe marker proves upstream was checked recently
  for that exact revision
- the same startup gate now also has a bounded transient-outage path: probe
  metadata persists success vs retryable failure, `Ensure` records a recent
  retryable probe failure before falling back to fetch, and startup hydration
  may keep a healthy same-revision readable repo `Ready` when that retryable
  failure is still `Fresh`. Non-retryable failures and older probe failures
  still force the normal startup sync path
- tightened that outage path one step further: retryable probe failure no
  longer destroys the last successful probe proof. The mirror-side sidecar now
  keeps the most recent successful `checked_at + target_revision` marker even
  after a transient failure, so startup hydration may still warm-start a
  healthy same-revision stale fetch-policy remote when the failure itself is no
  longer `Fresh` but the preserved success proof is still within the accepted
  `Fresh` or `Aging` window
- this keeps the public repo-sync freshness surface unchanged: `last_fetched_at`
  still only means a real mirror fetch. The new sidecar is an internal
  bootstrap optimization for the subset of remotes that were recently probed
  and confirmed unchanged without downloading a packfile
- added coverage at all three feature boundaries:
  `git/checkout/metadata.rs` now proves probe success / retryable-failure /
  sidecar-clear round trips plus preserved last-success proof, `repo_index/state/coordinator/hydration.rs`
  now proves retryable failure is reusable but non-retryable failure is not
  and that probe freshness can still derive from the carried success marker,
  and `repo_index/state/tests/sync.rs` now proves startup hydration reuses a
  stale fetch-policy managed remote when a retryable probe failure has already
  aged out of the outage-only window but the preserved last-success proof still
  matches the published revision
- verified with:
  - `direnv exec . cargo test -p xiuxian-wendao --lib git::checkout::metadata::tests:: -- --nocapture`
  - `direnv exec . cargo test -p xiuxian-wendao --lib gateway::studio::repo_index::state::coordinator::hydration::tests:: -- --nocapture`
  - `direnv exec . cargo test -p xiuxian-wendao --lib gateway::studio::repo_index::state::tests::sync::sync_repositories_warm_starts_stale_fetch_policy_remote_when_retryable_probe_failure_preserves_aging_success_proof -- --exact --nocapture`
  - `direnv exec . cargo check -p xiuxian-wendao-runtime --lib`
  - `direnv exec . cargo check -p xiuxian-wendao --lib`
  - `direnv exec . cargo clippy -p xiuxian-wendao-runtime --lib -- -D warnings`
  - `direnv exec . git diff --check`
- full `direnv exec . cargo clippy -p xiuxian-wendao --lib -- -D warnings`
  still hits pre-existing unrelated lint debt across broader `link_graph`,
  `query_core`, `gateway`, and `xiuxian-wendao-julia` surfaces; this slice now
  compiles cleanly and passes focused tests, but crate-wide zero-warning is not
  yet a repo-owned baseline for the whole package

## Wendao Valkey Analysis Cache

- replaced the placeholder repo-intelligence `ValkeyAnalysisCache` with a real
  Wendao-owned cache implementation under
  `src/analyzers/cache/valkey/` feature-folder modules for runtime resolution,
  payload/key storage, and tests
- analyzer output now persists to Valkey as versioned JSON payloads keyed by a
  stable hash of `repo_id + checkout_root + stable revision + plugin_ids`, so
  ready analyzer snapshots can survive process restarts instead of living only
  in-process
- the canonical runtime is now `XIUXIAN_WENDAO_ANALYZER_VALKEY_URL` with
  `VALKEY_URL` / `REDIS_URL` fallback, plus optional
  `XIUXIAN_WENDAO_ANALYZER_VALKEY_KEY_PREFIX` and
  `XIUXIAN_WENDAO_ANALYZER_VALKEY_TTL_SECS`
- load/store stays intentionally best-effort at runtime: invalid Valkey URL
  configuration still fails fast during cache initialization, but connection or
  payload misses no longer block analyzer execution
- `analyzers/service/analysis.rs` and `analyzers/service/cached.rs` now query
  Valkey by full `RepositoryAnalysisCacheKey` identity instead of the old
  revision-only placeholder API, and the in-memory analysis cache is still
  warmed on Valkey hit
- focused tests now cover env/runtime parsing, stable revision rules, payload
  roundtrip, shadow-backed cache roundtrip, and repo-analysis search handler
  regressions
- verified with:
  - `direnv exec . cargo test -p xiuxian-wendao --lib analyzers::cache::`
  - `direnv exec . cargo test -p xiuxian-wendao --lib gateway::studio::router::handlers::repo::analysis::search::tests::`

## Wendao Julia Clippy Frontier Advance

- closed the active `xiuxian-wendao-julia` warning bucket that had been
  blocking full `direnv exec . cargo clippy -p xiuxian-wendao --lib -- -D warnings`
  before the wider `xiuxian-wendao` debt even started reporting
- fixed the current Julia-specific doc-markdown, lazy-evaluation, needless
  range-loop, needless-lifetime, and projection-helper argument-count issues;
  the graph-structural convenience helper now takes dedicated query and pair
  input bundles instead of one 13-argument public function
- verified the repaired Julia slice with:
  - `direnv exec . cargo check -p xiuxian-wendao-julia --lib`
  - `direnv exec . cargo test -p xiuxian-wendao-julia --lib plugin::graph_structural_projection::tests:: -- --nocapture`
  - `direnv exec . rustfmt --check packages/rust/crates/xiuxian-wendao-julia/src/compatibility/link_graph/artifact.rs packages/rust/crates/xiuxian-wendao-julia/src/compatibility/link_graph/paths.rs packages/rust/crates/xiuxian-wendao-julia/src/compatibility/link_graph/runtime.rs packages/rust/crates/xiuxian-wendao-julia/src/plugin/graph_structural_exchange.rs packages/rust/crates/xiuxian-wendao-julia/src/plugin/graph_structural_projection.rs packages/rust/crates/xiuxian-wendao-julia/src/plugin/transport.rs packages/rust/crates/xiuxian-wendao-julia/src/plugin/mod.rs packages/rust/crates/xiuxian-wendao-julia/src/lib.rs`
  - `direnv exec . cargo clippy -p xiuxian-wendao --lib -- -D warnings`
- the full clippy gate now advances into broader pre-existing `xiuxian-wendao`
  debt instead of stopping in `xiuxian-wendao-julia`; the next visible frontier
  is concentrated in `link_graph/*`, `query_core/*`, `search_plane/*`,
  analyzer Julia transport docs, and OpenAPI path inventory wildcard imports

## Wendao Live Flight Route-Payload Reuse

- profiled the live same-origin Flight search harness on the default `9517`
  gateway and confirmed the remaining warm-state request-path work had narrowed
  to `GetFlightInfo` plus `DoGet`, while the harness also exposed two restart
  artifacts: transient `EADDRNOTAVAIL` during `/api/health` and `/api/ui/config`
  preflight, and false perf samples captured before search/repo status had
  settled after restart
- `xiuxian-wendao-runtime` now keeps a bounded reusable route-payload cache in
  `transport/server/service.rs`, so repeated identical `GetFlightInfo`
  requests can reuse the already materialized batch/app-metadata payload
  instead of re-entering the provider after the first request
- that same runtime payload cache now also keeps lazily encoded `DoGet`
  `FlightData` frames, so repeated identical `DoGet` requests no longer rerun
  Arrow Flight record-batch encoding after the first request
- added a focused runtime regression proving repeated identical
  `GetFlightInfo` calls reuse the cached search-family payload, while the
  existing `GetFlightInfo -> DoGet` reuse regression still passes under the
  `julia` feature gate; a new focused regression also proves repeated
  `DoGet` calls reuse the cached encoded frames
- hardened `.data/wendao-frontend/src/api/liveFlightSearchPerf.test.ts` so the
  control-plane preflight now normalizes loopback origins, retries transient
  gateway-startup network failures, avoids unnecessary `/api/ui/config`
  rewrites, and waits for search/repo status to reach steady state before
  measured Flight requests begin
- the latest steady-state live proof against the checked-in 179-repository
  config is now `2.15ms` overall average, `3.31ms` P95, with
  `GetFlightInfo` averaging `0.86ms` and `DoGet` averaging `0.79ms`
- the production-line restart audit also exposed a `process.nix` operator
  issue outside the Flight request path: `wendao-gateway` had been writing the
  wrapper-shell PID into `wendao.pid`, while readiness compared that value
  against the gateway's `x-wendao-process-id` header. `process.nix` now writes
  the actual gateway child PID instead
- a fresh post-restart live perf run then exposed the remaining production-line
  blocker more precisely: the steady-state gate timed out because restart-time
  repo-backed corpus records could transiently exist in memory with runtime but
  no publication, which surfaced `published_manifest_missing` on
  `repo_entity`/`repo_content_chunk` even though persisted repo-corpus records
  already existed on disk/cache
- fixed that restart recovery path in
  `search_plane/service/core/repo_runtime/reads.rs` so read-time hydration now
  restores publication/maintenance from persisted repo-corpus record
  cache/local state even when a runtime-only in-memory record already exists;
  manifest-only fallback remains disabled
- a second restart audit showed one more producer-side bug beside that
  recovery path: repo-index status changes fan out into asynchronous
  `refresh_repo_runtime_cache(...)` tasks, and older tasks could finish after
  newer snapshots during restart churn. That let stale runtime snapshots
  overwrite fresher repo-backed status synthesis even when the combined
  repo-corpus record on disk was already correct
- fixed that producer-side race by adding a repo-runtime generation fence in
  `search_plane/service/core/repo_runtime/sync.rs`, so only the newest async
  runtime-refresh task may persist repo-corpus cache/snapshot state or
  recompute repo-backed search status
- added focused runtime regression coverage proving stale repo-runtime refresh
  tasks no longer override a newer generation, and revalidated the earlier
  restart-time publication recovery regression after the fence landed
- the final live blocker was then in the frontend perf gate rather than in the
  gateway: the harness was still waiting for the full repo/background queue to
  drain after restart, even when `repo_entity` / `repo_content_chunk` were
  already readable and `blockingCorpusCount` was zero
- `.data/wendao-frontend/src/api/liveFlightSearchPerf.test.ts` now treats that
  readable recovery window as stable enough for measurement and has focused
  helper coverage for both accepted and rejected gateway-state shapes
- fresh post-restart live proof on the restarted `9517` gateway is now green:
  `2.93ms` overall average, `4.52ms` P95, `GetFlightInfo` average `1.24ms`,
  and `DoGet` average `0.98ms`; after convergence the same gateway reported
  `ready=152`, `failed=9`, `unsupported=16`, `queued=0`, `active=0`
- architectural boundary remains unchanged: Arrow Flight is the canonical
  business surface, minimal JSON remains the control surface, and Arrow IPC is
  retained only as the payload codec used inside Flight frames rather than as
  a third business transport
- verified with:
  - `direnv exec . cargo test -p xiuxian-wendao-runtime --features julia --lib transport::server::tests::wendao_flight_service_get_flight_info_reuses_cached_search_family_payload -- --exact`
  - `direnv exec . cargo test -p xiuxian-wendao-runtime --features julia --lib transport::server::tests::wendao_flight_service_do_get_reuses_cached_search_family_payload_after_get_flight_info -- --exact`
  - `direnv exec . cargo test -p xiuxian-wendao-runtime --features julia --lib transport::server::tests::wendao_flight_service_do_get_reuses_cached_search_family_encoded_frames -- --exact`
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm run test:live-flight-perf'`
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm test -- --run src/api/liveFlightSearchPerf.test.ts'`
  - `direnv exec . cargo test -p xiuxian-wendao --lib search_plane::service::tests::publication::state::repo_search_publication_state_recovers_publication_after_runtime_only_restart_hydration -- --exact`
  - `direnv exec . cargo test -p xiuxian-wendao --lib gateway::studio::repo_index::state::tests::runtime:: -- --nocapture`

## WendaoSearch Mixed-Graph Bootstrap

- created the active blueprint
  `.data/blueprints/wendao_search_mixed_graph_retrieval.md` to define
  `WendaoSearch.jl` as the Julia package boundary for mixed-relation graph
  retrieval and bounded structural solver lanes
- created the active ExecPlan
  `.cache/codex/execplans/wendao_search_bootstrap.md` so blueprint, docs, GTD,
  and package code advance under one tracked bootstrap slice
- extended `.data/WendaoSearch.jl/docs/` with package-local mixed-graph query,
  layered retrieval, service-boundary, and roadmap documents
- bootstrapped `.data/WendaoSearch.jl` as a real Julia package with a minimal
  code surface for `QueryGraph`, layered retrieval plans, candidate subgraphs,
  and structural evaluation normalization
- planned this slice as architecture and scaffold only; live Arrow Flight
  routes and direct `GadgetSearch.jl` solver integration remain deferred to the
  next bounded implementation step
- verified the bootstrap slice with:
  - `direnv exec . julia --project=.data/WendaoSearch.jl -e 'using Pkg; Pkg.test()'`

## Wendao Julia Graph-Structural Plugin Dispatch

- corrected the mixed-graph structural plugin boundary so Julia-specific route
  names, staged schema-version defaults, request or response column
  inventories, and Arrow batch validation now live in
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/graph_structural.rs`
  instead of expanding `xiuxian-wendao-runtime`
- kept `xiuxian-wendao-runtime` on the generic transport seam only; the moved
  module now imports runtime-owned route normalization but the Julia plugin
  crate owns the graph-structural semantic contract
- re-exported the new Julia-owned surface from `xiuxian-wendao-julia`
  `src/plugin/mod.rs` and `src/lib.rs`, including
  `GraphStructuralRouteKind`, `JULIA_GRAPH_STRUCTURAL_SCHEMA_VERSION`, the
  draft route constants, column inventories, and request or response
  validation helpers
- synchronized the package and design docs so ownership prose now points
  graph-structural route promotion at `xiuxian-wendao-julia`: updated
  `packages/rust/crates/xiuxian-wendao-julia/README.md`,
  `packages/rust/crates/xiuxian-wendao/docs/06_roadmap/415_m4_julia_externalization_package_list.md`,
  `.data/WendaoSearch.jl/docs/01_core/wendao_search/architecture/service-boundary.md`,
  `.data/WendaoSearch.jl/docs/contracts/route_draft.md`, and
  `.data/WendaoSearch.jl/docs/contracts/arrow_schema_contract.md`
- verified the boundary correction with:
  - `direnv exec . cargo test -p xiuxian-wendao-julia plugin::graph_structural:: --lib`
  - `direnv exec . cargo check -p xiuxian-wendao-julia --lib`
  - `direnv exec . cargo check -p xiuxian-wendao-runtime --features julia --lib`
  - `git diff --check -- packages/rust/crates/xiuxian-wendao-julia packages/rust/crates/xiuxian-wendao-runtime/src/transport/mod.rs packages/rust/crates/xiuxian-wendao/docs/06_roadmap/415_m4_julia_externalization_package_list.md .data/WendaoSearch.jl/docs/contracts/route_draft.md .data/WendaoSearch.jl/docs/contracts/arrow_schema_contract.md .data/WendaoSearch.jl/docs/01_core/wendao_search/architecture/service-boundary.md docs/GTD/DAILY_2026_04_02.md`
- attempted `direnv exec . cargo test -p xiuxian-wendao-julia --lib`; the new
  `plugin::graph_structural` tests passed, while six pre-existing real-loopback
  transport tests still fail in this checkout because
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/test_support/common.rs`
  cannot resolve local `.data/WendaoArrow` or `.data/WendaoAnalyzer` package
  directories
- corrected those local package assumptions to the real checkout names
  `.data/WendaoArrow.jl` and `.data/WendaoAnalyzer.jl`, exported
  `DEFAULT_JULIA_ARROW_PACKAGE_DIR`, and aligned the Julia compatibility
  defaults plus Rust test-support path resolution with those `.jl` package
  directories
- fixed the Julia launcher parse bug in
  `.data/WendaoAnalyzer.jl/scripts/run_analyzer_example.jl` and taught the
  analyzer launcher to prefer the sibling local `.data/WendaoArrow.jl`
  checkout before falling back to the pinned GitHub source
- reran script-level smoke checks for
  `.data/WendaoArrow.jl/scripts/run_stream_scoring_flight_server.sh` and
  `.data/WendaoAnalyzer.jl/scripts/run_stream_linear_blend_server.sh`; both now
  fail fast on the same remaining environment prerequisite instead of stale
  path or syntax errors: no vendored `gRPCServer.jl` checkout exists under
  `.cache/vendor/`, and no `WENDAO_FLIGHT_GRPCSERVER_PATH` override is set
- restored the vendored `gRPCServer.jl` prerequisite, reran the real Julia
  loopback lane, and `direnv exec . cargo test -p xiuxian-wendao-julia --lib`
  now passes end to end against the local `.data/WendaoArrow.jl` and
  `.data/WendaoAnalyzer.jl` package checkouts
- added a second Julia-owned dispatch slice in
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/graph_structural_transport.rs`:
  `graph_structural_transport` repository config parsing, graph-structural
  capability selection, staged request or response validation, schema metadata
  stamping, and route-kind-specific Flight dispatch now live in the Julia
  plugin crate instead of leaking back into `xiuxian-wendao-runtime`
- updated `packages/rust/crates/xiuxian-wendao-julia/src/plugin/entry.rs` so
  repository preflight now validates both `structural_rerank` and
  `constraint_filter` transport blocks through the Julia-owned helper layer
- synchronized the ownership docs again across
  `packages/rust/crates/xiuxian-wendao-julia/README.md`,
  `packages/rust/crates/xiuxian-wendao/docs/06_roadmap/415_m4_julia_externalization_package_list.md`,
  `.data/WendaoSearch.jl/docs/01_core/wendao_search/architecture/service-boundary.md`,
  `.data/WendaoSearch.jl/docs/contracts/route_draft.md`, and
  `.cache/codex/execplans/wendao_julia_graph_structural_plugin_dispatch.md`
- verified this transport-ownership slice with:
  - `direnv exec . cargo test -p xiuxian-wendao-julia --lib`
  - `git diff --check -- .data/WendaoSearch.jl .data/blueprints/wendao_search_mixed_graph_retrieval.md .cache/codex/execplans/wendao_search_bootstrap.md docs/GTD/DAILY_2026_04_02.md`
- corrected the next structural-search integration slice after review: the
  temporary `xiuxian-wendao/src/analyzers/service/julia_graph_structural_transport/`
  host adapter was removed, and the typed graph-structural request rows,
  response rows, Arrow batch builders, Arrow batch decoders, and
  repository-scoped fetch helpers now live in
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/graph_structural_exchange.rs`
- kept `xiuxian-wendao` thin on that seam: it now only re-exports Julia-owned
  graph-structural helpers through `src/analyzers/languages/mod.rs`, while
  the service-layer duplicate module was deleted
- fixed a small unrelated but blocking Arrow Flight API drift in
  `packages/rust/crates/xiuxian-wendao-runtime/src/transport/server/flight/payload.rs`
  by switching the encoder path to `map_err(...).try_collect()`, which
  restored compile compatibility for the touched Julia/plugin verification lane
- synchronized the boundary docs and plan with the plugin-owned shape:
  updated `packages/rust/crates/xiuxian-wendao-julia/README.md`,
  `packages/rust/crates/xiuxian-wendao/docs/06_roadmap/415_m4_julia_externalization_package_list.md`,
  `.data/WendaoSearch.jl/docs/01_core/wendao_search/architecture/service-boundary.md`,
  `.data/WendaoSearch.jl/docs/contracts/route_draft.md`, and
  `.cache/codex/execplans/wendao_julia_graph_structural_host_adapter.md`
- verified the corrected plugin-owned seam with:
  - `direnv exec . cargo check -p xiuxian-wendao-runtime --lib`
  - `direnv exec . cargo test -p xiuxian-wendao-julia graph_structural_exchange --lib`
  - `direnv exec . cargo check -p xiuxian-wendao --features julia --lib`
- added a second plugin-owned layer above the low-level request rows in
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/graph_structural_projection.rs`:
  normalized query-anchor DTOs, query-context DTOs, candidate-subgraph DTOs,
  rerank-signal DTOs, and filter-constraint DTOs now build staged
  graph-structural request rows without making the host manually align
  `anchor_planes` / `anchor_values`
- kept the host seam thin again: `packages/rust/crates/xiuxian-wendao/src/analyzers/languages/mod.rs`
  only re-exports the new Julia-owned projection helpers for future
  consumption, while all normalization logic stays in `xiuxian-wendao-julia`
- synchronized docs and plan for this projection-helper slice across
  `packages/rust/crates/xiuxian-wendao-julia/README.md`,
  `packages/rust/crates/xiuxian-wendao/docs/06_roadmap/415_m4_julia_externalization_package_list.md`,
  and `.cache/codex/execplans/wendao_julia_graph_structural_projection_helpers.md`
- verified the projection-helper slice with:
  - `direnv exec . cargo test -p xiuxian-wendao-julia graph_structural_projection --lib`
  - `direnv exec . cargo check -p xiuxian-wendao-julia --lib`
  - `direnv exec . cargo check -p xiuxian-wendao --features julia --lib`
- added a bounded host-consumption proof in
  `packages/rust/crates/xiuxian-wendao/tests/unit/link_graph_agentic_expansion.rs`
  plus the integration harness
  `packages/rust/crates/xiuxian-wendao/tests/link_graph_agentic_expansion.rs`:
  a real `LinkGraphIndex` agentic-expansion pair now projects into
  `xiuxian-wendao-julia` query-anchor, query-context, candidate-subgraph, and
  rerank-signal DTOs before building a validated structural-rerank request
  batch
- kept the production boundary unchanged in that proof: `xiuxian-wendao`
  consumes Julia-owned projection and request-batch builders, but does not
  grow a second graph-structural adapter layer
- verified the host-consumption proof with:
  - `direnv exec . cargo test -p xiuxian-wendao --test link_graph_agentic_expansion`
  - `direnv exec . cargo check -p xiuxian-wendao --features julia --test link_graph_agentic_expansion`
- confirmed the broader `direnv exec . cargo check -p xiuxian-wendao --features julia --tests`
  lane is still blocked separately by a pre-existing Arrow test compile issue
  at `packages/rust/crates/xiuxian-wendao/src/gateway/studio/search/handlers/sql.rs:303`;
  it was not introduced by this host-consumption slice
- moved the next thin consumption seam into
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/graph_structural_projection.rs`:
  stable two-node pair candidate id normalization, pair candidate-subgraph
  projection, and pair-to-request-row helpers now live in the Julia plugin
  crate instead of being rebuilt inside host tests or future host consumers
- updated the same host proof in
  `packages/rust/crates/xiuxian-wendao/tests/unit/link_graph_agentic_expansion.rs`
  to consume `build_graph_structural_pair_rerank_request_row(...)`, so the
  host-side test no longer owns its own pair candidate id helper or manual
  candidate-subgraph assembly
- synchronized the package docs and plan for that pair-projection slice across
  `packages/rust/crates/xiuxian-wendao-julia/README.md`,
  `packages/rust/crates/xiuxian-wendao/docs/06_roadmap/415_m4_julia_externalization_package_list.md`,
  and `.cache/codex/execplans/wendao_julia_graph_structural_pair_projection_helper.md`
- verified the pair-projection slice with:
  - `direnv exec . cargo test -p xiuxian-wendao-julia graph_structural_projection --lib`
  - `direnv exec . cargo check -p xiuxian-wendao-julia --lib`
  - `direnv exec . cargo test -p xiuxian-wendao --test link_graph_agentic_expansion`
  - `direnv exec . cargo check -p xiuxian-wendao --features julia --test link_graph_agentic_expansion`
- moved the next simple semantic-assembly seam into
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/graph_structural_projection.rs`:
  keyword-or-tag query-context helpers and binary keyword-or-tag rerank-signal
  helpers now live in the Julia plugin crate instead of being rebuilt inside
  the host proof
- updated the same host proof in
  `packages/rust/crates/xiuxian-wendao/tests/unit/link_graph_agentic_expansion.rs`
  to consume `build_graph_structural_keyword_tag_query_context(...)` and
  `build_graph_structural_keyword_tag_rerank_signals(...)`, so the host-side
  test no longer manually constructs keyword or tag anchors or maps boolean
  tag matches into staged plane scores
- synchronized the package docs and plan for that query-and-signal slice across
  `packages/rust/crates/xiuxian-wendao-julia/README.md`,
  `packages/rust/crates/xiuxian-wendao/docs/06_roadmap/415_m4_julia_externalization_package_list.md`,
  and `.cache/codex/execplans/wendao_julia_graph_structural_query_signal_helper.md`
- attempted the focused verification lane for this slice with:
  - `direnv exec . cargo test -p xiuxian-wendao-julia graph_structural_projection --lib`
  - `direnv exec . cargo check -p xiuxian-wendao-julia --lib`
  - `direnv exec . cargo test -p xiuxian-wendao --test link_graph_agentic_expansion`
- after the local `xiuxian-vector` blocker was repaired, reran the same focused
  lane and it now passes end to end:
  - `direnv exec . cargo test -p xiuxian-wendao-julia graph_structural_projection --lib`
  - `direnv exec . cargo check -p xiuxian-wendao-julia --lib`
  - `direnv exec . cargo test -p xiuxian-wendao --test link_graph_agentic_expansion`
  - `direnv exec . cargo check -p xiuxian-wendao --features julia --test link_graph_agentic_expansion`
- that rerun also exposed a small pre-existing test drift in
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/graph_structural_transport.rs`
  and `packages/rust/crates/xiuxian-wendao-julia/src/plugin/transport.rs`:
  several assertions still expected `Option<&str>` from `flight_base_url()` and
  `flight_route()`, while the current runtime surface returns `&str`
- aligned those stale assertions to the current `&str` API so the focused Julia
  plugin test lane compiles and the graph-structural verification slice stays
  green
- moved the next convenience seam into
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/graph_structural_projection.rs`:
  a combined keyword-or-tag pair-rerank request-row helper now composes the
  Julia-owned query-context builder, binary rerank-signal builder, and
  pair-rerank row projection in one call
- updated the same host proof in
  `packages/rust/crates/xiuxian-wendao/tests/unit/link_graph_agentic_expansion.rs`
  to consume that combined helper, so the host-side test now only extracts the
  real pair metadata and shared tag before handing the rest of the staged row
  construction to `xiuxian-wendao-julia`
- synchronized the package docs and plan for that combined-helper slice across
  `packages/rust/crates/xiuxian-wendao-julia/README.md`,
  `packages/rust/crates/xiuxian-wendao/docs/06_roadmap/415_m4_julia_externalization_package_list.md`,
  and `.cache/codex/execplans/wendao_julia_graph_structural_combined_pair_helper.md`
- verified the combined-helper slice with:
  - `direnv exec . cargo test -p xiuxian-wendao-julia graph_structural_projection --lib`
  - `direnv exec . cargo check -p xiuxian-wendao-julia --lib`
  - `direnv exec . cargo test -p xiuxian-wendao --test link_graph_agentic_expansion`
  - `direnv exec . cargo check -p xiuxian-wendao --features julia --test link_graph_agentic_expansion`
- moved the remaining shared-tag overlap seam into
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/graph_structural_projection.rs`:
  normalized shared-tag anchor extraction and a tag-overlap-aware combined
  pair-rerank helper now live in the Julia plugin crate
- updated the same host proof in
  `packages/rust/crates/xiuxian-wendao/tests/unit/link_graph_agentic_expansion.rs`
  to consume `build_graph_structural_keyword_overlap_pair_rerank_request_row(...)`,
  so the host-side test now only loads the two metadata records and forwards
  raw tag lists instead of discovering the shared tag itself
- synchronized the package docs and plan for that tag-overlap slice across
  `packages/rust/crates/xiuxian-wendao-julia/README.md`,
  `packages/rust/crates/xiuxian-wendao/docs/06_roadmap/415_m4_julia_externalization_package_list.md`,
  and `.cache/codex/execplans/wendao_julia_graph_structural_tag_overlap_helper.md`
- verified the tag-overlap slice with:
  - `direnv exec . cargo test -p xiuxian-wendao-julia graph_structural_projection --lib`
  - `direnv exec . cargo check -p xiuxian-wendao-julia --lib`
  - `direnv exec . cargo test -p xiuxian-wendao --test link_graph_agentic_expansion`
  - `direnv exec . cargo check -p xiuxian-wendao --features julia --test link_graph_agentic_expansion`

## WendaoSearch Structural Contract Sketch

- created the next active ExecPlan
  `.cache/codex/execplans/wendao_search_contract_sketch.md` for the
  package-local contract slice
- added package-local contract docs for `structural_rerank` and
  `constraint_filter` under `.data/WendaoSearch.jl/docs/contracts/`
- extended `.data/WendaoSearch.jl` with a `contracts` module so the package now
  has explicit request and response types for the first two structural-search
  integration modes
- kept this slice bounded to package-local contract vocabulary and tests; no
  live Flight route or Rust-host contract was promoted yet
- verified the contract slice with:
  - `direnv exec . julia --project=.data/WendaoSearch.jl -e 'using Pkg; Pkg.test()'`
  - `git diff --check -- .data/WendaoSearch.jl .cache/codex/execplans/wendao_search_contract_sketch.md docs/GTD/DAILY_2026_04_02.md`

## WendaoSearch Arrow Schema And Route Draft

- created the next active ExecPlan
  `.cache/codex/execplans/wendao_search_arrow_route_draft.md` for the package-local
  Arrow schema and route-draft slice
- added package-local docs for the first Arrow schema draft and route draft
  under `.data/WendaoSearch.jl/docs/contracts/`
- extended `.data/WendaoSearch.jl` with package-local Arrow column descriptors,
  route draft descriptors, schema version `v0-draft`, and helper accessors for
  the first two structural-search modes
- kept this slice bounded to package-local schema and route vocabulary; no live
  Flight service or Rust-host route constant was promoted yet
- verified the schema and route draft slice with:
  - `direnv exec . julia --project=.data/WendaoSearch.jl -e 'using Pkg; Pkg.test()'`
  - `git diff --check -- .data/WendaoSearch.jl .cache/codex/execplans/wendao_search_arrow_route_draft.md docs/GTD/DAILY_2026_04_02.md`

## WendaoSearch WendaoArrow Hard Dependency

- created the next active ExecPlan
  `.cache/codex/execplans/wendao_search_wendaoarrow_dependency.md` for the
  dependency-promotion slice
- promoted `WendaoArrow.jl` into `.data/WendaoSearch.jl/Project.toml` as a
  direct hard dependency with local sibling source path
- aligned `WendaoSearch.jl` Julia compat to `1.12` so the package metadata
  matches the current `WendaoArrow.jl` substrate requirement
- added a narrow dependency bridge in
  `.data/WendaoSearch.jl/src/integration/wendao_arrow.jl` so the package now
  imports and exposes required `WendaoArrow` schema-version state plus Flight
  availability through the local `WendaoArrow` substrate
- synchronized the dependency rule into blueprint, package docs, roadmap, and
  service-boundary notes
- corrected `.data/WendaoArrow.jl/Project.toml` back to the current
  `arrow-julia` main-head source rev
  `d133dcbe7445fc5a2b3d72c5ebd258cb47ce10a4` after verifying
  `refs/heads/main` on 2026-04-02
- verified that this rev change alone does not clear package resolution yet:
  the shared depot still materializes `Arrow v2.8.0` without `Arrow.Flight`,
  while a clean project-local depot fails earlier with
  `Did not find subdirectory 'src/ArrowTypes'`
- fixed the actual resolver break by keeping `Arrow` pinned to upstream main
  while resolving `ArrowTypes` from the sibling checkout
  `.data/arrow-julia/src/ArrowTypes`, then regenerating both package manifests
- verified the final dependency graph and test lanes with:
  - `direnv exec . julia --project=.data/WendaoSearch.jl -e 'using Pkg; Pkg.resolve(); Pkg.instantiate(); Pkg.test()'`
  - `direnv exec . julia --project=.data/WendaoArrow.jl -e 'using Pkg; Pkg.resolve(); Pkg.instantiate(); Pkg.test()'`
  - `git diff --check -- .data/WendaoSearch.jl .data/WendaoArrow.jl .cache/codex/execplans/wendao_search_wendaoarrow_dependency.md docs/GTD/DAILY_2026_04_02.md`

## WendaoSearch Row Normalization

- created the next active ExecPlan
  `.cache/codex/execplans/wendao_search_row_normalization.md` for the
  package-local row-normalization slice
- added `.data/WendaoSearch.jl/src/contracts/rows.jl` so the package now
  flattens draft request and response contracts into explicit row-shaped named
  tuples and schema-ordered columntables
- kept the slice draft-only and package-local: no live Flight service or
  Rust-owned route contract was promoted
- updated package docs and README so the schema draft now records the new
  normalization surface and its current omissions
- verified with:
  - `direnv exec . julia --project=.data/WendaoSearch.jl -e 'using Pkg; Pkg.resolve(); Pkg.instantiate(); Pkg.test()'`
  - `git diff --check -- .data/WendaoSearch.jl .cache/codex/execplans/wendao_search_row_normalization.md docs/GTD/DAILY_2026_04_02.md`

## WendaoArrow Generic Schema Builder For WendaoSearch

- corrected the initial payload direction so `WendaoSearch.jl` would not own a
  parallel Arrow transport boundary
- created `.cache/codex/execplans/wendao_arrow_generic_schema_builder.md` for
  the upstream schema-builder slice and updated
  `.cache/codex/execplans/wendao_search_arrow_payload_builder.md` to record the
  thin-adapter outcome
- added `merge_schema_metadata(...)` and `schema_table(...)` to
  `.data/WendaoArrow.jl`, with focused tests proving schema-version overlay and
  transport-ready Arrow table materialization
- refit `.data/WendaoSearch.jl` so draft `structural_rerank` and
  `constraint_filter` payload helpers now delegate to
  `WendaoArrow.schema_table(...)` and only keep package-local
  `wendao_search.*` route metadata
- fixed one exposed row-normalization edge case where empty edge-constraint
  batches produced `Vector{Any}` and broke draft payload construction
- synchronized package docs and READMEs so the new upstream/downstream boundary
  is explicit in both Julia packages
- verified with:
  - `direnv exec . julia --project=.data/WendaoArrow.jl -e 'using Pkg; Pkg.test()'`
  - `direnv exec . julia --project=.data/WendaoSearch.jl -e 'using Pkg; Pkg.test()'`

## WendaoSearch Draft Flight Route Headers

- created `.cache/codex/execplans/wendao_search_flight_route_headers.md` for
  the next bounded Flight-route seam
- added shared generic `flight_route_descriptor(...)` and
  `flight_schema_headers(...)` helpers to `.data/WendaoArrow.jl` so downstream
  packages do not rebuild schema-version Flight headers locally
- added `.data/WendaoSearch.jl/src/contracts/flight.jl` so the package now
  exposes draft descriptor and request-header adapters for
  `structural_rerank` and `constraint_filter`
- kept the layering explicit: `WendaoArrow.jl` owns shared
  `x-wendao-schema-version`, while `WendaoSearch.jl` keeps additive
  `x-wendao-search-*` draft headers package-local
- synchronized package docs and READMEs so the new Flight-request boundary is
  explicit in both Julia packages
- verified with:
  - `direnv exec . julia --project=.data/WendaoArrow.jl -e 'using Pkg; Pkg.test()'`
  - `direnv exec . julia --project=.data/WendaoSearch.jl -e 'using Pkg; Pkg.test()'`

## WendaoSearch DoExchange Request Wrapper

- created `.cache/codex/execplans/wendao_search_doexchange_request_wrapper.md`
  for the next request-assembly slice
- added `FlightExchangeRequest`, `flight_exchange_request(...)`, and
  `flight_exchange_table(...)` to `.data/WendaoArrow.jl` so request assembly
  now stays in the upstream transport substrate
- extended the packaged `build_flight_service(...)` and
  `build_stream_flight_service(...)` surfaces with
  `expected_schema_version = ...`, then corrected the response writer so draft
  services echo the same schema version instead of collapsing back to `v1`
- added package-local draft exchange-request adapters to
  `.data/WendaoSearch.jl` for `structural_rerank` and `constraint_filter`
- verified a real local roundtrip where a `WendaoSearch v0-draft` request goes
  through `WendaoArrow.build_flight_service(expected_schema_version =
WENDAOSEARCH_SCHEMA_VERSION)` and decodes back with the same schema version
- synchronized package docs and READMEs so the new request-wrapper boundary is
  explicit in both Julia packages
- verified with:
  - `direnv exec . julia --project=.data/WendaoArrow.jl -e 'using Pkg; Pkg.test()'`
  - `direnv exec . julia --project=.data/WendaoSearch.jl -e 'using Pkg; Pkg.test()'`

## WendaoSearch Typed Exchange Helpers

- created `.cache/codex/execplans/wendao_search_typed_exchange_helpers.md`
  for the next thin-client closure slice
- added `.data/WendaoSearch.jl/src/contracts/exchange.jl` so draft response
  tables now decode back into `StructuralRerankResult` and
  `ConstraintFilterResult`
- added route-shaped local-service or Flight-client helpers that call the
  upstream `WendaoArrow` send surface and return typed Search-owned results
- kept the transport boundary unchanged: `WendaoArrow.jl` still owns request
  wrappers and send helpers, while `WendaoSearch.jl` only owns response
  semantics and route-shaped helper composition
- verified with:
  - `direnv exec . julia --project=.data/WendaoSearch.jl -e 'using Pkg; Pkg.test()'`

## WendaoSearch Result Validation And Summary

- created `.cache/codex/execplans/wendao_search_result_validation_summary.md`
  for the next host-facing result slice
- added `.data/WendaoSearch.jl/src/contracts/results.jl` so typed structural
  results now have package-local validation and deterministic summary helpers
- kept the boundary explicit: transport and request wrappers remain in
  `WendaoArrow.jl`, while Search-specific result semantics stay in
  `WendaoSearch.jl`
- verified with:
  - `direnv exec . julia --project=.data/WendaoSearch.jl -e 'using Pkg; Pkg.test()'`

## WendaoSearch Host Ingest Contract

- created `.cache/codex/execplans/wendao_search_host_ingest_contract.md` for
  the next host-facing envelope slice
- added `.data/WendaoSearch.jl/src/contracts/host_ingest.jl` so typed
  structural-search results now collapse into deterministic
  `structural_rerank` and `constraint_filter` host-ingest envelopes
- added exchange-to-ingest helpers on top of the existing typed exchange path,
  so Wendao host integration can consume one normalized result contract per
  route without rebuilding summary logic
- synchronized package docs and README so the host-ingest layer is recorded as
  a Search-owned semantic boundary and not a new Arrow transport seam
- verified with:
  - `direnv exec . julia --project=.data/WendaoSearch.jl -e 'using Pkg; Pkg.test()'`
  - `git diff --check -- .data/WendaoSearch.jl .cache/codex/execplans/wendao_search_host_ingest_contract.md docs/GTD/DAILY_2026_04_02.md`

## Xiuxian Daochang Warning Closure

- switched from fragmented warning cleanup to one grouped runtime pass
- removed the unused Telegram reply islands under
  `src/channels/telegram/runtime/jobs/replies/`:
  `session_budget.rs`, `session_context.rs`, and the whole
  `session_memory/*` tree
- removed the unused Discord session-admin helper island under
  `src/channels/discord/runtime/managed/handlers/command_dispatch/session/helpers.rs`
  and `src/channels/discord/runtime/managed/replies/session_admin.rs`
- trimmed the now-dead Telegram/Discord module wiring in the corresponding
  `mod.rs` files and pruned stale formatting helpers from
  `src/channels/telegram/runtime/jobs/replies/shared.rs`
- verified with:
  - `CARGO_TARGET_DIR=.cache/cargo-target/daochang-retire direnv exec . cargo check -p xiuxian-daochang --tests --message-format short`
- warning frontier moved from `135/136` down to `93/94`; the remaining large
  grouped buckets are `agent/bootstrap/zhixing.rs` plus `agent/notification/*`,
  `agent/bootstrap/zhenfa.rs`, and a smaller set of runtime dead-code tails

- removed the dormant Zhixing reminder-notification line from
  `src/agent/bootstrap/zhixing.rs` and `src/agent/bootstrap/builder.rs`:
  no reminder queue bootstrap, no timer watcher mount, and no notification
  worker/provider registration
- reduced `src/agent/notification/` to the still-used dispatcher/provider
  abstraction by deleting the retired transport implementations and recipient
  parsing helpers
- verified again with:
  - `CARGO_TARGET_DIR=.cache/cargo-target/daochang-retire direnv exec . cargo check -p xiuxian-daochang --tests --message-format short`
- warning frontier moved again from `93/94` down to `73/74`; the next large
  grouped buckets are `agent/bootstrap/zhenfa.rs` and the remaining
  `agent/bootstrap/zhixing.rs` dead-code tail

- confirmed `src/agent/bootstrap/builder.rs` was an orphaned bootstrap path:
  `Agent::from_config` is actually implemented in `src/agent/bootstrap/memory.rs`
- removed the orphan builder file and detached the equally orphaned
  `src/agent/bootstrap/zhenfa.rs` module from `src/agent/bootstrap.rs`
- trimmed the stale `ZhenfaRuntimeDeps` re-export from
  `src/agent/zhenfa/mod.rs`
- verified again with:
  - `CARGO_TARGET_DIR=.cache/cargo-target/daochang-retire direnv exec . cargo check -p xiuxian-daochang --tests --message-format short`
- warning frontier moved from `73/74` down to `66/67`; the remaining thick
  bucket is now mostly the live-but-unmounted `src/agent/bootstrap/zhixing.rs`
  support surface, followed by smaller runtime dead-code tails

- retired the orphan `src/agent/bootstrap/hot_reload/*` chain and removed the
  stale `tests/agent/bootstrap_hot_reload.rs` entry that asserted mount records
  for a bootstrap path no longer wired into `Agent::from_config`
- kept the still-live zhixing/native-tool tests in place; only the detached
  hot-reload bootstrap surface was removed
- verified again with:
  - `CARGO_TARGET_DIR=.cache/cargo-target/daochang-retire direnv exec . cargo check -p xiuxian-daochang --tests --message-format short`
- warning frontier remained stable at `65/66`; the next concentrated cleanup
  target is the remaining `src/agent/bootstrap/zhixing.rs` support surface

- reduced `src/agent/bootstrap/zhixing.rs` to the still-used test-support
  helpers only: path resolution, notebook/template resolution, and embedded
  skill-template bridge loading
- removed the remaining unmounted runtime-init chain from that file, including
  `ZhixingRuntimeBundle`, runtime persona selection, and manifestation/heyi
  bootstrap helpers that were no longer reachable from the live agent path
- verified again with:
  - `CARGO_TARGET_DIR=.cache/cargo-target/daochang-retire direnv exec . cargo check -p xiuxian-daochang --tests --message-format short`
- warning frontier moved from `65/66` down to `50/51`; the next grouped
  buckets are `resolve.rs`, Telegram session command event constants, and
  `channels/telegram/session_gate/core.rs`

- removed the retired Telegram session command event constants for
  `status`, `budget`, and `memory`, keeping only the event ids still used by
  feedback, injection, admin, partition, and control-admin flows
- deleted the orphan legacy in-memory gate implementation at
  `src/channels/telegram/session_gate/core.rs`; the live `SessionGate`
  continues to come from `src/channels/telegram/session_gate/types.rs`
- verified again with:
  - `CARGO_TARGET_DIR=.cache/cargo-target/daochang-retire direnv exec . cargo check -p xiuxian-daochang --tests --message-format short`
- warning frontier moved from `50/51` down to `39/40`; the next thick bucket
  is the still-orphaned `src/resolve.rs`, followed by smaller runtime/helper
  tails

## Wendao Julia Metadata Projection Helper

- moved the next metadata projection seam into
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/graph_structural_projection.rs`:
  added `GraphStructuralNodeMetadataInputs`,
  `GraphStructuralKeywordOverlapPairInputs`, and
  `build_graph_structural_keyword_overlap_pair_rerank_request_row_from_metadata(...)`
  so metadata-aware overlap projection remains plugin-owned
- updated
  `packages/rust/crates/xiuxian-wendao/tests/unit/link_graph_agentic_expansion.rs`
  so the host proof now forwards raw metadata through plugin-owned metadata
  bundles instead of passing raw tag vectors directly into the overlap helper
- synchronized the package docs and plan for this metadata-projection slice
  across `packages/rust/crates/xiuxian-wendao-julia/README.md`,
  `packages/rust/crates/xiuxian-wendao/docs/06_roadmap/415_m4_julia_externalization_package_list.md`,
  and `.cache/codex/execplans/wendao_julia_graph_structural_metadata_projection_helper.md`
- verified the metadata-projection slice with:
  - `direnv exec . cargo test -p xiuxian-wendao-julia graph_structural_projection --lib`
  - `direnv exec . cargo test -p xiuxian-wendao --test link_graph_agentic_expansion`
  - `direnv exec . cargo check -p xiuxian-wendao-julia --lib`
  - `direnv exec . cargo check -p xiuxian-wendao --features julia --test link_graph_agentic_expansion`

## Wendao Julia Metadata Batch Helper

- moved the next row-to-batch seam into
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/graph_structural_projection.rs`:
  added `GraphStructuralKeywordOverlapPairRerankInputs` and
  `build_graph_structural_keyword_overlap_pair_rerank_request_batch_from_metadata(...)`
  so metadata-aware rerank batch assembly remains plugin-owned
- updated
  `packages/rust/crates/xiuxian-wendao/tests/unit/link_graph_agentic_expansion.rs`
  so the host proof now consumes the metadata-aware batch helper directly and
  asserts request-batch columns instead of assembling a staged row vector first
- synchronized the package docs and plan for this metadata-batch slice across
  `packages/rust/crates/xiuxian-wendao-julia/README.md`,
  `packages/rust/crates/xiuxian-wendao/docs/06_roadmap/415_m4_julia_externalization_package_list.md`,
  and `.cache/codex/execplans/wendao_julia_graph_structural_metadata_batch_helper.md`
- verified the metadata-batch slice with:
  - `direnv exec . cargo test -p xiuxian-wendao-julia graph_structural_projection --lib`
  - `direnv exec . cargo test -p xiuxian-wendao --test link_graph_agentic_expansion`
  - `direnv exec . cargo check -p xiuxian-wendao-julia --lib`
  - `direnv exec . cargo check -p xiuxian-wendao --features julia --test link_graph_agentic_expansion`

## Wendao Julia Candidate Input Helper

- moved the next candidate-collection seam into
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/graph_structural_projection.rs`:
  added `GraphStructuralKeywordOverlapPairRequestInputs` and
  `build_graph_structural_keyword_overlap_pair_rerank_request_batch_from_inputs(...)`
  so host consumers can hand one higher-level plugin-owned bundle per pair into
  staged rerank batch assembly
- updated
  `packages/rust/crates/xiuxian-wendao/tests/unit/link_graph_agentic_expansion.rs`
  so the host proof now consumes the higher-level candidate-input helper
  directly instead of composing query-input, metadata-input, pair-input, and
  scored-rerank-input bundles by hand
- synchronized the package docs and plan for this candidate-input slice across
  `packages/rust/crates/xiuxian-wendao-julia/README.md`,
  `packages/rust/crates/xiuxian-wendao/docs/06_roadmap/415_m4_julia_externalization_package_list.md`,
  and `.cache/codex/execplans/wendao_julia_graph_structural_candidate_input_helper.md`
- verified the candidate-input slice with:
  - `direnv exec . cargo test -p xiuxian-wendao-julia graph_structural_projection --lib`
  - `direnv exec . cargo test -p xiuxian-wendao --test link_graph_agentic_expansion`
  - `direnv exec . cargo check -p xiuxian-wendao-julia --lib`
  - `direnv exec . cargo check -p xiuxian-wendao --features julia --test link_graph_agentic_expansion`

## Wendao Julia Query Candidate Helper

- moved the next shared-query and raw-candidate seam into
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/graph_structural_projection.rs`:
  added `GraphStructuralKeywordOverlapQueryInputs`,
  `GraphStructuralKeywordOverlapCandidateInputs`,
  `build_graph_structural_keyword_overlap_pair_request_input(...)`, and
  `build_graph_structural_keyword_overlap_pair_rerank_request_batch(...)` so
  the plugin crate now owns the query-plus-candidate staging layer above the
  higher-level request-input helper
- updated
  `packages/rust/crates/xiuxian-wendao/tests/unit/link_graph_agentic_expansion.rs`
  so the host proof now forwards one shared query bundle plus one raw
  per-candidate bundle per pair instead of manually constructing
  `GraphStructuralKeywordOverlapPairRequestInputs`
- repaired the small borrow drift in
  `packages/rust/crates/xiuxian-wendao/src/search_plane/attachment/build/orchestration.rs`
  and
  `packages/rust/crates/xiuxian-wendao/src/search_plane/reference_occurrence/build/orchestration.rs`
  so the host verification lane compiles again under the current
  `previous_fingerprints: &BTreeMap<...>` signatures
- synchronized the package docs and plan for this query-candidate slice across
  `packages/rust/crates/xiuxian-wendao-julia/README.md`,
  `packages/rust/crates/xiuxian-wendao/docs/06_roadmap/415_m4_julia_externalization_package_list.md`,
  and `.cache/codex/execplans/wendao_julia_graph_structural_query_candidate_helper.md`
- verified the query-candidate slice with:
  - `direnv exec . cargo test -p xiuxian-wendao-julia graph_structural_projection --lib`
  - `direnv exec . cargo test -p xiuxian-wendao --test link_graph_agentic_expansion`
  - `direnv exec . cargo check -p xiuxian-wendao --features julia --test link_graph_agentic_expansion`

## Wendao Julia Query Candidate Exchange Helper

- promoted the repository-fetch convenience helper
  `fetch_graph_structural_keyword_overlap_pair_rerank_rows_for_repository(...)`
  into the public `xiuxian-wendao-julia` surface so the query-plus-candidate
  structural-rerank dispatch seam stays Julia-plugin-owned instead of growing
  a host-local exchange wrapper
- synchronized the thin re-export surface through
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/mod.rs`,
  `packages/rust/crates/xiuxian-wendao-julia/src/lib.rs`, and
  `packages/rust/crates/xiuxian-wendao/src/analyzers/languages/mod.rs`
  without adding a host-local exchange wrapper
- kept the current plugin-owned candidate-bundle shape in
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/graph_structural_projection.rs`
  and left the host proof on that DTO shape, so this slice only moved the
  repository-fetch seam instead of reopening candidate-shape design
- synchronized the package docs and plan for this exchange-helper slice across
  `packages/rust/crates/xiuxian-wendao-julia/README.md`,
  `packages/rust/crates/xiuxian-wendao/docs/06_roadmap/415_m4_julia_externalization_package_list.md`,
  and `.cache/codex/execplans/wendao_julia_graph_structural_query_candidate_exchange_helper.md`
- verified the exchange-helper slice with:
  - `direnv exec . cargo test -p xiuxian-wendao-julia graph_structural_exchange --lib`
  - `direnv exec . cargo check -p xiuxian-wendao-julia --lib`
  - `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate test_agentic_expansion_pair_projects_into_julia_graph_structural_request`
  - `direnv exec . cargo check -p xiuxian-wendao --features julia --test xiuxian-testing-gate`

## Wendao Julia Host Fetch Proof

- added a bounded host-side proof in
  `packages/rust/crates/xiuxian-wendao/tests/unit/link_graph_agentic/expansion.rs`
  that calls the public
  `fetch_graph_structural_keyword_overlap_pair_rerank_rows_for_repository(...)`
  helper directly, so the new repository-fetch seam is exercised from host
  code without introducing a host-local graph-structural exchange layer
- kept the proof on the current plugin-owned candidate-bundle DTO shape, so
  this slice only extends consumption proof depth and does not reopen the
  candidate-shape design
- synchronized the package docs and plan for this host-fetch-proof slice
  across `packages/rust/crates/xiuxian-wendao-julia/README.md`,
  `packages/rust/crates/xiuxian-wendao/docs/06_roadmap/415_m4_julia_externalization_package_list.md`,
  and `.cache/codex/execplans/wendao_julia_graph_structural_host_fetch_proof.md`
- verified the host-fetch-proof slice with:
  - `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate test_agentic_expansion_pair_uses_julia_graph_structural_fetch_helper`
  - `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate test_agentic_expansion_pair_projects_into_julia_graph_structural_request`
  - `direnv exec . cargo check -p xiuxian-wendao --features julia --test xiuxian-testing-gate`
  - `direnv exec . cargo check -p xiuxian-wendao-julia --lib`

## Wendao Host Language Seam Graph-Structural Proof

- tightened the bounded host proof in
  `packages/rust/crates/xiuxian-wendao/tests/unit/link_graph_agentic/expansion.rs`
  so it now imports graph-structural constants, DTOs, batch builders, and the
  public query-plus-candidate fetch helper through
  `xiuxian_wendao::analyzers::languages` instead of importing
  `xiuxian-wendao-julia` directly
- widened the thin host re-export seam in
  `packages/rust/crates/xiuxian-wendao/src/analyzers/languages/mod.rs` with
  the remaining graph-structural constants and `GraphStructuralPairCandidateInputs`
  needed by that proof, without adding any host-local graph-structural logic
- synchronized the package docs and plan for this language-seam slice across
  `packages/rust/crates/xiuxian-wendao-julia/README.md`,
  `packages/rust/crates/xiuxian-wendao/docs/06_roadmap/415_m4_julia_externalization_package_list.md`,
  and `.cache/codex/execplans/wendao_host_language_seam_graph_structural_proof.md`
- verified the language-seam slice with:
  - `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate test_agentic_expansion_pair_uses_julia_graph_structural_fetch_helper`
  - `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate test_agentic_expansion_pair_projects_into_julia_graph_structural_request`
  - `direnv exec . cargo check -p xiuxian-wendao --features julia --test xiuxian-testing-gate`
  - `direnv exec . cargo check -p xiuxian-wendao-julia --lib`

## Wendao Julia Candidate Bundle Helper

- added
  `build_graph_structural_keyword_overlap_candidate_inputs(...)` in
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/graph_structural_projection.rs`
  so the raw `pair-input DTO + left/right tags + scores` to
  `GraphStructuralKeywordOverlapCandidateInputs` staging seam now stays
  Julia-plugin-owned instead of being manually rebuilt in host proofs
- re-exported that helper through
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/mod.rs`,
  `packages/rust/crates/xiuxian-wendao-julia/src/lib.rs`, and
  `packages/rust/crates/xiuxian-wendao/src/analyzers/languages/mod.rs`
  without adding any host-local graph-structural logic
- updated the bounded host proof in
  `packages/rust/crates/xiuxian-wendao/tests/unit/link_graph_agentic/expansion.rs`
  so both the request-batch proof and the repository-fetch proof now consume
  that helper through `xiuxian_wendao::analyzers::languages` instead of
  manually constructing `GraphStructuralNodeMetadataInputs` and
  `GraphStructuralKeywordOverlapCandidateInputs`
- synchronized the package docs and plan for this candidate-bundle-helper
  slice across `packages/rust/crates/xiuxian-wendao-julia/README.md`,
  `packages/rust/crates/xiuxian-wendao/docs/06_roadmap/415_m4_julia_externalization_package_list.md`,
  and `.cache/codex/execplans/wendao_julia_graph_structural_candidate_bundle_helper.md`
- verified the candidate-bundle-helper slice with:
  - `direnv exec . cargo test -p xiuxian-wendao-julia graph_structural_projection --lib`
  - `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate test_agentic_expansion_pair_projects_into_julia_graph_structural_request`
  - `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate test_agentic_expansion_pair_uses_julia_graph_structural_fetch_helper`
  - `direnv exec . cargo check -p xiuxian-wendao-julia --lib`
  - `direnv exec . cargo check -p xiuxian-wendao --features julia --test xiuxian-testing-gate`
- `direnv exec . cargo fmt --all` still remains blocked by the unrelated parse
  error at
  `packages/rust/crates/xiuxian-wendao/src/search_plane/repo_entity/query/execution.rs:226`,
  so this slice used `direnv exec . rustfmt ...` on the touched files only

## Wendao Julia Query Bundle Helper

- added
  `build_graph_structural_keyword_overlap_query_inputs(...)` in
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/graph_structural_projection.rs`
  so the raw `query_id + retrieval layer + max layers + keyword anchors +
edge constraints` to `GraphStructuralKeywordOverlapQueryInputs` staging seam
  now stays Julia-plugin-owned instead of being manually rebuilt in host
  proofs or exchange tests
- re-exported that helper through
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/mod.rs`,
  `packages/rust/crates/xiuxian-wendao-julia/src/lib.rs`, and
  `packages/rust/crates/xiuxian-wendao/src/analyzers/languages/mod.rs`
  without adding any host-local graph-structural logic
- updated the bounded host proof in
  `packages/rust/crates/xiuxian-wendao/tests/unit/link_graph_agentic/expansion.rs`
  and the missing-transport exchange test in
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/graph_structural_exchange.rs`
  so both now consume that helper instead of manually constructing
  `GraphStructuralKeywordOverlapQueryInputs`
- synchronized the package docs and plan for this query-bundle-helper slice
  across `packages/rust/crates/xiuxian-wendao-julia/README.md`,
  `packages/rust/crates/xiuxian-wendao/docs/06_roadmap/415_m4_julia_externalization_package_list.md`,
  and `.cache/codex/execplans/wendao_julia_graph_structural_query_bundle_helper.md`
- verified the query-bundle-helper slice with:
  - `direnv exec . cargo test -p xiuxian-wendao-julia graph_structural_projection --lib`
  - `direnv exec . cargo test -p xiuxian-wendao-julia graph_structural_exchange --lib`
  - `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate test_agentic_expansion_pair_projects_into_julia_graph_structural_request`
  - `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate test_agentic_expansion_pair_uses_julia_graph_structural_fetch_helper`
  - `direnv exec . cargo check -p xiuxian-wendao-julia --lib`
  - `direnv exec . cargo check -p xiuxian-wendao --features julia --test xiuxian-testing-gate`

## Wendao Julia Pair Input Helper

- added
  `build_graph_structural_pair_candidate_inputs(...)` in
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/graph_structural_projection.rs`
  so the raw `left_id + right_id + edge kinds` to
  `GraphStructuralPairCandidateInputs` staging seam now stays
  Julia-plugin-owned instead of being manually rebuilt in host proofs or
  exchange tests
- re-exported that helper through
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/mod.rs`,
  `packages/rust/crates/xiuxian-wendao-julia/src/lib.rs`, and
  `packages/rust/crates/xiuxian-wendao/src/analyzers/languages/mod.rs`
  without adding any host-local graph-structural logic
- updated the bounded host proof in
  `packages/rust/crates/xiuxian-wendao/tests/unit/link_graph_agentic/expansion.rs`
  and the missing-transport exchange test in
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/graph_structural_exchange.rs`
  so both now consume that helper instead of manually constructing
  `GraphStructuralPairCandidateInputs`
- synchronized the package docs and plan for this pair-input-helper slice
  across `packages/rust/crates/xiuxian-wendao-julia/README.md`,
  `packages/rust/crates/xiuxian-wendao/docs/06_roadmap/415_m4_julia_externalization_package_list.md`,
  and `.cache/codex/execplans/wendao_julia_graph_structural_pair_input_helper.md`
- verified the pair-input-helper slice with:
  - `direnv exec . cargo test -p xiuxian-wendao-julia graph_structural_projection --lib`
  - `direnv exec . cargo test -p xiuxian-wendao-julia graph_structural_exchange --lib`
  - `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate test_agentic_expansion_pair_projects_into_julia_graph_structural_request`
  - `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate test_agentic_expansion_pair_uses_julia_graph_structural_fetch_helper`
  - `direnv exec . cargo check -p xiuxian-wendao-julia --lib`
  - `direnv exec . cargo check -p xiuxian-wendao --features julia --test xiuxian-testing-gate`

## Wendao Julia Raw Candidate Helper

- added
  `build_graph_structural_keyword_overlap_pair_candidate_inputs_from_raw(...)`
  in
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/graph_structural_projection.rs`
  so the raw `left_id + right_id + edge kinds + left/right tags + scores` to
  `GraphStructuralKeywordOverlapCandidateInputs` staging seam now stays
  Julia-plugin-owned instead of being manually rebuilt in host proofs or
  exchange tests
- re-exported that helper through
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/mod.rs`,
  `packages/rust/crates/xiuxian-wendao-julia/src/lib.rs`, and
  `packages/rust/crates/xiuxian-wendao/src/analyzers/languages/mod.rs`
  without adding any host-local graph-structural logic
- updated the bounded host proof in
  `packages/rust/crates/xiuxian-wendao/tests/unit/link_graph_agentic/expansion.rs`
  and the missing-transport exchange test in
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/graph_structural_exchange.rs`
  so both now consume that helper instead of manually composing
  `build_graph_structural_keyword_overlap_pair_candidate_metadata_inputs(...)`
  and `build_graph_structural_keyword_overlap_candidate_inputs(...)`
- synchronized the package docs and plan for this raw-candidate-helper slice
  across `packages/rust/crates/xiuxian-wendao-julia/README.md`,
  `packages/rust/crates/xiuxian-wendao/docs/06_roadmap/415_m4_julia_externalization_package_list.md`,
  and `.cache/codex/execplans/wendao_julia_graph_structural_raw_candidate_helper.md`
- verified the raw-candidate-helper slice with:
  - `direnv exec . rustfmt packages/rust/crates/xiuxian-wendao-julia/src/plugin/graph_structural_projection.rs packages/rust/crates/xiuxian-wendao-julia/src/plugin/graph_structural_exchange.rs packages/rust/crates/xiuxian-wendao-julia/src/plugin/mod.rs packages/rust/crates/xiuxian-wendao-julia/src/lib.rs packages/rust/crates/xiuxian-wendao/src/analyzers/languages/mod.rs packages/rust/crates/xiuxian-wendao/tests/unit/link_graph_agentic/expansion.rs`
  - `direnv exec . cargo test -p xiuxian-wendao-julia graph_structural_projection --lib`
  - `direnv exec . cargo test -p xiuxian-wendao-julia graph_structural_exchange --lib`
  - `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate test_agentic_expansion_pair_projects_into_julia_graph_structural_request`
  - `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate test_agentic_expansion_pair_uses_julia_graph_structural_fetch_helper`
  - `direnv exec . cargo check -p xiuxian-wendao-julia --lib`
  - `direnv exec . cargo check -p xiuxian-wendao --features julia --test xiuxian-testing-gate`

## Wendao Julia Raw Candidate Batch Helper

- added `GraphStructuralKeywordOverlapRawCandidateInputs`,
  `build_graph_structural_keyword_overlap_raw_candidate_inputs(...)`, and
  `build_graph_structural_keyword_overlap_pair_rerank_request_batch_from_raw_candidates(...)`
  in
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/graph_structural_projection.rs`,
  plus
  `fetch_graph_structural_keyword_overlap_pair_rerank_rows_for_repository_from_raw_candidates(...)`
  in
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/graph_structural_exchange.rs`,
  so the shared-query plus raw-candidate-collection seam now stays
  Julia-plugin-owned instead of being manually normalized in host proofs
- re-exported that raw-candidate batch or fetch surface through
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/mod.rs`,
  `packages/rust/crates/xiuxian-wendao-julia/src/lib.rs`, and
  `packages/rust/crates/xiuxian-wendao/src/analyzers/languages/mod.rs`
  without adding any host-local graph-structural logic
- updated the bounded host proof in
  `packages/rust/crates/xiuxian-wendao/tests/unit/link_graph_agentic/expansion.rs`
  so both the request-batch proof and the repository-fetch proof now consume
  one shared query plus plugin-owned raw candidate bundles instead of
  normalizing each candidate before batch or fetch dispatch
- updated the missing-transport exchange test in
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/graph_structural_exchange.rs`
  so it now proves the new raw-candidate fetch helper resolves through the
  Julia-owned structural-rerank route
- synchronized the package docs and plan for this raw-candidate-batch-helper
  slice across `packages/rust/crates/xiuxian-wendao-julia/README.md`,
  `packages/rust/crates/xiuxian-wendao/docs/06_roadmap/415_m4_julia_externalization_package_list.md`,
  and `.cache/codex/execplans/wendao_julia_graph_structural_raw_candidate_batch_helper.md`
- verified the raw-candidate-batch-helper slice with:
  - `direnv exec . rustfmt packages/rust/crates/xiuxian-wendao-julia/src/plugin/graph_structural_projection.rs packages/rust/crates/xiuxian-wendao-julia/src/plugin/graph_structural_exchange.rs packages/rust/crates/xiuxian-wendao-julia/src/plugin/mod.rs packages/rust/crates/xiuxian-wendao-julia/src/lib.rs packages/rust/crates/xiuxian-wendao/src/analyzers/languages/mod.rs packages/rust/crates/xiuxian-wendao/tests/unit/link_graph_agentic/expansion.rs`
  - `direnv exec . cargo test -p xiuxian-wendao-julia graph_structural_projection --lib`
  - `direnv exec . cargo test -p xiuxian-wendao-julia graph_structural_exchange --lib`
  - `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate test_agentic_expansion_pair_projects_into_julia_graph_structural_request`
  - `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate test_agentic_expansion_pair_uses_julia_graph_structural_fetch_helper`
  - `direnv exec . cargo check -p xiuxian-wendao-julia --lib`
  - `direnv exec . cargo check -p xiuxian-wendao --features julia --test xiuxian-testing-gate`
