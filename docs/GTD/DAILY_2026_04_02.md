# Daily Log - 2026-04-02

## Wendao Deferred Repo Bootstrap Repair

- fixed the deferred-bootstrap gap where `/api/repo/index/status` could remain
  `total = 0` even with a populated Studio UI config
- `StudioState::set_ui_config(...)` now re-kicks background repo/symbol
  indexing for explicit eager applies even when the sanitized config is
  unchanged
- `repo_index_status` now bootstraps deferred repo indexing on first access and
  records `repo_index_status` as the deferred activation source
- fresh same-port gateway proof on `127.0.0.1:9519` now returns repo index
  status with `total = 177`, `queued = 176`, `syncing = 1`, and
  `studioBootstrapBackgroundIndexingDeferredActivationSource =
  "repo_index_status"`
- fresh frontend live proof confirms `code_search` over same-origin Flight
  returns repo-backed hits instead of collapsing to only `main` / `kernel`

## Wendao Definition And Autocomplete Flight Cut

- retired `/api/search/definition` and `/api/search/autocomplete` from the
  active router and bundled OpenAPI business surface
- locked `/search/definition` and `/search/autocomplete` into the runtime
  query contract and the shared Studio Flight route snapshot
- switched `.data/wendao-frontend` definition resolution and autocomplete to
  same-origin Flight via `src/api/flightDocumentTransport.ts`
- verified the cut with `cargo check`, focused Rust handler/OpenAPI tests, and
  focused frontend transport tests; the next bounded migration slice is
  `graph/vfs`

## Wendao VFS Resolve Flight Cut

- landed the first bounded `graph/vfs` migration slice by retiring
  `/api/vfs/resolve` and promoting `/vfs/resolve` as the canonical Flight
  business contract
- `xiuxian-wendao-runtime` now owns `VFS_RESOLVE_ROUTE`,
  `WENDAO_VFS_PATH_HEADER`, request validation, and
  `VfsResolveFlightRouteProvider` inside the shared `WendaoFlightService`
  route-provider seam
- `xiuxian-wendao` now owns the Studio-backed provider in
  `src/gateway/studio/vfs/flight.rs`, and the shared workspace Flight snapshot
  now locks `/vfs/resolve`
- `.data/wendao-frontend` now resolves Studio navigation targets through
  same-origin Flight in `src/api/flightWorkspaceTransport.ts`, with focused
  transport and client tests passing
- verified the slice with focused runtime/server tests, focused Studio VFS
  tests, workspace Flight snapshot update, frontend unit tests, frontend
  production build, and `git diff --check`

## Wendao Graph Neighbors Flight Cut

- landed the next bounded `graph/vfs` migration slice by retiring
  `/api/graph/neighbors/{id}` and promoting `/graph/neighbors` as the
  canonical Flight business contract
- `xiuxian-wendao-runtime` now owns `GRAPH_NEIGHBORS_ROUTE`,
  `WENDAO_GRAPH_*` request headers, request validation, and
  `GraphNeighborsFlightRouteProvider` inside the shared
  `WendaoFlightService` route-provider seam
- `xiuxian-wendao` now owns the Studio-backed provider in
  `src/gateway/studio/router/handlers/graph/flight.rs`, and the shared
  workspace Flight snapshot now locks `/graph/neighbors` alongside
  `/vfs/resolve`
- `.data/wendao-frontend` now resolves graph-neighbor payloads through
  same-origin Flight in `src/api/flightGraphTransport.ts`, with client and
  workspace transports no longer keeping a live HTTP graph-neighbors business
  path
- fresh live proof on `127.0.0.1:9519` passes `src/api/liveGateway.test.ts`
  `4/4`; the currently running `127.0.0.1:9517` process still needs a restart
  before it will accept `/graph/neighbors`
