# Daily GTD Log - 2026-03-29

### [DONE] Feature: clear the stale `xiuxian-llm` direct dependencies from the rmcp exit follow-up

- **Objective**: complete the next bounded remediation after the
  `xiuxian-wendao` cleanup by removing the stale `xiuxian-llm` direct
  dependencies that no longer own any physical runtime usage, while keeping
  the macro-owned config dependencies intact.
- **Update**:
  - verified the physical `xiuxian-llm` manifest and current source usage
    before editing
  - removed stale direct dependencies from
    `packages/rust/crates/xiuxian-llm/Cargo.toml`:
    `rmcp`, `fast_image_resize`, and `spider_agent`
  - restored `toml` and `xiuxian-config-core` after compile verification
    proved they are macro-owned dependencies required by
    `#[xiuxian_macros::xiuxian_config(...)]`
  - recorded those two macro-owned dependencies in
    `[package.metadata.cargo-machete].ignored` so the bounded remediation is
    aligned with physical compile reality rather than tool false positives
  - verified that `cargo-machete` is now clean for `xiuxian-llm`
  - verified that `xiuxian-llm` compiles on the bounded `--lib` baseline and
    on the explicit `vision-dots` test-compile lane
  - confirmed separately that `--all-features` is still blocked by the
    existing missing-CUDA-toolchain environment (`nvcc` / `nvidia-smi`), not
    by this manifest change
- **Validation Executed**:
  - `direnv exec . cargo machete packages/rust/crates/xiuxian-llm`
  - `direnv exec . cargo check -p xiuxian-llm --lib`
  - `direnv exec . cargo check -p xiuxian-llm --tests --features vision-dots`

### [DONE] Feature: clear the `xiuxian-wendao` cargo-machete advisory findings

- **Objective**: finish the first post-`Phase 8` remediation bundle by
  removing the stale direct dependencies that the new governance lane surfaced
  in `xiuxian-wendao`, while confirming that `xiuxian-mcp` itself had already
  been physically removed in the earlier 2026-03-27 retirement cut.
- **Update**:
  - verified the physical repo state: `packages/rust/crates/xiuxian-mcp/`
    no longer exists and is not a live workspace member anymore
  - removed stale direct dependencies from
    `packages/rust/crates/xiuxian-wendao/Cargo.toml`:
    `arrow-ipc`, `lazy_static`, `once_cell`, `rmcp`, and `sha2`
  - verified at that point that `rmcp` was still a live dependency elsewhere
    (`xiuxian-daochang`, `xiuxian-llm`, and `xiuxian-zhenfa` test support), so
    global `rmcp` retirement remained a separate bounded change rather than a
    follow-on from `xiuxian-mcp` package removal
  - reran the governance bundle and confirmed the advisory
    `cargo-machete` lane now passes cleanly on the Wendao migration cluster
  - updated the RFC, Wendao program note, outward inventory, and active
    ExecPlan so they no longer report a live `xiuxian-wendao` machete finding
- **Validation Executed**:
  - `direnv exec . cargo check -p xiuxian-wendao --all-targets --all-features`
  - `direnv exec . just rust-contract-dependency-governance`
  - `direnv exec . git diff --check`

### [DONE] Feature: complete Phase-8 Stage C lane integration and gate bundle

- **Objective**: land the first bounded contract/dependency governance lanes
  physically in local tasks, `devenv`, and CI, then record the explicit
  `Phase 8` gate decision.
- **Update**:
  - added `scripts/rust/wendao_contract_dependency_governance.sh` as the
    bounded owner for the new semver and dependency-hygiene lane behavior
  - added local `just` tasks:
    `rust-contract-semver-core`,
    `rust-dependency-hygiene-machete-wendao`,
    `rust-dependency-hygiene-udeps-wendao`, and
    `rust-contract-dependency-governance`
  - extended `nix/modules/rust.nix` and `nix/modules/tasks.nix` so
    `cargo-semver-checks`, `cargo-machete`, and `cargo-udeps` are provisioned
    and exposed through `ci:rust-contract-dependency-governance`
  - wired the new governance bundle into the main Rust CI workflows
  - verified that the blocking semver lane for `xiuxian-wendao-core` passes
    on the current tree
  - verified that the advisory `cargo-machete` lane is live in the initial
    bounded rollout
  - wired `cargo-udeps` with bounded skip semantics when `rustup` nightly is
    unavailable in the current Nix-managed environment
  - updated the RFC, Wendao program note, outward inventory, and active
    ExecPlan so `Phase-8 Stage C` is recorded as complete and the explicit
    `Phase 8` gate decision is now `go`
- **Validation Executed**:
  - `direnv exec . bash -lc 'command -v cargo-semver-checks; command -v cargo-machete; command -v cargo-udeps'`
  - `direnv exec . just rust-contract-dependency-governance`
  - `direnv exec . devenv tasks run ci:rust-contract-dependency-governance`
  - `direnv exec . git diff --check`

### [DONE] Research: broaden the `offset_map` fill-strategy matrix and reject a global iterator swap

- **Objective**: verify whether the remaining `offset_map` flatten hotspot can
  be improved by changing only the `Dict` fill-loop spelling, using broader
  map sizes and missing densities instead of trusting the existing dense
  2-entry microbench alone.
- **Update**:
  - added a cache-local benchmark harness at
    `.cache/agent/benchmarks/arrow_julia_offset_map_fill_matrix.jl`
  - measured three fill-loop shapes across
    `entries in (1, 2, 8, 32)` and `missing_period in (0, 2)`:
    current `for (k, v) in pairs(y)`, `for kv in pairs(y)`, and `for kv in y`
  - confirmed there is no globally dominant replacement:
    the current destructuring loop remains best on the small-map cases that
    most closely match the live `offset_map` production benchmark, while the
    alternative pair-object/direct-iter forms only become competitive on some
    larger dense-map cases and then lose again once missing rows appear
  - rejected a package-level change in `src/arraytypes/map.jl`; the next
    worthwhile `offset_map` move must be more structural than a loop spelling
    swap
- **Validation Executed**:
  - `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_offset_map_fill_matrix.jl`
  - `direnv exec . git diff --check -- docs/GTD/DAILY_2026_03_29.md .cache/agent/execplans/arrow_julia_offset_map_breakdown_profile.md .cache/agent/benchmarks/arrow_julia_offset_map_fill_matrix.jl`

### [DONE] Research: reject a narrow non-missing split for `offset_map`

- **Objective**: verify whether a Julia-native specialization for non-missing
  map columns can remove the `missing` branches from
  `mapoffsetsandvalues(...)` and produce enough production upside to justify a
  new code path.
- **Update**:
  - added a cache-local A/B harness at
    `.cache/agent/benchmarks/arrow_julia_map_nomissing_split_ab.jl`
  - compared the current `mapoffsetsandvalues(...)` against a branch-free
    non-missing candidate on `entries=2`, `entries=2` with missing rows, and
    `entries=32`
  - found only marginal wins on dense non-missing maps and a slight loss on
    the missing case
  - rejected the candidate; it does not clear the production-value bar for a
    new path in `src/arraytypes/map.jl`
- **Validation Executed**:
  - `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_map_nomissing_split_ab.jl`
  - `direnv exec . git diff --check -- docs/GTD/DAILY_2026_03_29.md .cache/agent/execplans/arrow_julia_offset_map_breakdown_profile.md .cache/agent/benchmarks/arrow_julia_map_nomissing_split_ab.jl`

### [DONE] Feature: widen direct single-partition `tobuffer` to non-missing top-level binary columns

- **Objective**: revisit the previously rejected top-level binary direct path
  with a narrower production rule and only land it if binary-only interleaved
  A/B still shows a stable upside.
- **Update**:
  - added `.cache/agent/benchmarks/arrow_julia_singlecol_binary_exact_interleaved.jl`
    to compare legacy writer, plain direct path, and direct path with exact
    `IOBuffer(sizehint=...)` for single-column binary inputs
  - concluded that exact sizehint remains too variable for production, while
    the plain direct path is good enough for non-missing top-level binary
    columns
  - updated `.data/arrow-julia/src/utils.jl` so only single-column,
    non-missing `Base.CodeUnits` inputs join the existing direct single-
    partition path
  - kept `Union{Missing,Base.CodeUnits}` on the legacy route and added a
    missing-binary byte-equivalence regression to
    `.data/arrow-julia/test/runtests.jl`
- **Validation Executed**:
  - `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_singlecol_binary_exact_interleaved.jl`
  - `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_writepaths_bench.jl`
  - `direnv exec . julia --project=.data/arrow-julia/test -e 'using Pkg; Pkg.instantiate(); Pkg.test("Arrow")'`
  - `direnv exec . git diff --check -- docs/GTD/DAILY_2026_03_29.md .cache/agent/execplans/arrow_julia_direct_tobuffer_fastpath.md .cache/agent/benchmarks/arrow_julia_singlecol_binary_exact_interleaved.jl .data/arrow-julia/src/utils.jl .data/arrow-julia/test/runtests.jl`

### [DONE] Research: reject exact-sizehint widening for single-column string/binary direct paths

- **Objective**: verify whether the current direct single-partition path should
  also adopt exact top-level `IOBuffer(sizehint=...)` for single-column string
  and binary inputs.
- **Update**:
  - added
    `.cache/agent/benchmarks/arrow_julia_singlecol_string_binary_exact_interleaved.jl`
    to compare legacy writer, plain direct path, and direct path with exact
    top-level sizehint across string/binary and missing/non-missing cases
  - found that `direct_exact` is still too unstable across reruns to justify a
    production rule in `src/utils.jl`
  - kept package code unchanged after the binary direct-path landing:
    plain direct remains the chosen route for single-column top-level strings
    and non-missing binaries
- **Validation Executed**:
  - `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_singlecol_string_binary_exact_interleaved.jl`
  - `direnv exec . git diff --check -- docs/GTD/DAILY_2026_03_29.md .cache/agent/execplans/arrow_julia_direct_tobuffer_fastpath.md .cache/agent/benchmarks/arrow_julia_singlecol_string_binary_exact_interleaved.jl`

### [DONE] Research: add a throughput matrix for the current Arrow write-path baseline

- **Objective**: produce a production-oriented throughput snapshot for the
  current `8779723` Arrow baseline across representative batch sizes instead of
  relying only on one `100k` microbench point.
- **Update**:
  - added `.cache/agent/benchmarks/arrow_julia_throughput_matrix.jl`
  - measured `int_native`, `string`, `binary`, and `map` at
    `10k`, `100k`, and `500k` rows
  - confirmed the current scaling picture:
    primitive/native paths are no longer the main concern, string/binary remain
    write-side dominated, and map conversion now scales reasonably with larger
    batches
- **Validation Executed**:
  - `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_throughput_matrix.jl`
  - `direnv exec . git diff --check -- docs/GTD/DAILY_2026_03_29.md .cache/agent/execplans/arrow_julia_direct_tobuffer_fastpath.md .cache/agent/benchmarks/arrow_julia_throughput_matrix.jl`

### [DONE] Research: quantify multi-partition throughput for the current Arrow baseline

- **Objective**: move the production discussion from single-batch microbench
  numbers to a more realistic continuous-batch model by measuring the current
  baseline under multi-partition streaming and repeated per-partition
  serialization.
- **Update**:
  - added `.cache/agent/benchmarks/arrow_julia_partition_throughput_matrix.jl`
  - measured `string`, `binary`, and `map` with the same total row counts
    under:
    - one single large batch
    - one multi-record-batch stream via `Tables.partitioner(...)`
    - repeated independent `Arrow.tobuffer(part)` calls
  - confirmed that the current multi-partition stream path is acceptable, not
    catastrophic
  - confirmed that `map` stays close to single-batch throughput even with more
    partitions
  - confirmed that string-heavy streamed partitions show the clearest penalty,
    while repeated independent per-partition serialization can outperform a
    single large batch for current string/binary shapes
  - left package code unchanged; the next production-relevant question is
    whether multi-partition streaming should gain a more direct per-batch path
- **Validation Executed**:
  - `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_partition_throughput_matrix.jl`
  - `direnv exec . git diff --check -- docs/GTD/DAILY_2026_03_29.md .cache/agent/execplans/arrow_julia_direct_tobuffer_fastpath.md .cache/agent/benchmarks/arrow_julia_partition_throughput_matrix.jl`

### [DONE] Feature: complete Phase-8 Stage B contract and dependency policy bundle

- **Objective**: stop `Phase 8` at one explicit policy boundary before wiring
  any new lanes, so `Stage C` inherits fixed owner crates and gate semantics
  instead of reopening scope.
- **Update**:
  - fixed workspace security policy as the continuing blocking baseline
    through existing `cargo-audit` + `cargo-deny` lanes
  - fixed the current `cargo-deny` contract as blocking for advisories, bans,
    and sources, while keeping duplicate-version findings warn-only and
    leaving license gating out of the initial bounded rollout
  - fixed `cargo-semver-checks` as the new blocking contract-governance lane
    for `xiuxian-wendao-core` only
  - fixed the initial dependency-hygiene rollout as advisory-first:
    `cargo-machete` for the Wendao migration cluster and `cargo-udeps`
    starting at `xiuxian-wendao-core` plus `xiuxian-wendao-runtime`
  - updated the RFC, Wendao program note, outward inventory, and active
    ExecPlan so `Phase-8 Stage B` is recorded as complete and
    `Phase-8 Stage C: Lane Integration and Gate Bundle` is now the governed
    next move
- **Validation Executed**:
  - `direnv exec . git diff --check`

### [DONE] Feature: open the Phase-8 contract and dependency governance proposal

- **Objective**: stop after the `Phase 7` gate and open the next macro-phase
  as one RFC-governed proposal bundle instead of drifting back into ad hoc
  implementation.
- **Update**:
  - reconciled the RFC with the physical repo state: `Phase 7` is now closed
    with a `go` decision rather than still treated as open
  - recorded the next macro-phase as
    `Phase 8: Contract and Dependency Governance`
  - grounded that proposal in current repo reality:
    `deny.toml` exists, local `just rust-security-audit` /
    `just rust-security-deny` lanes already exist, and the missing rollout is
    now clearly `cargo-semver-checks` plus dependency-hygiene lanes such as
    `cargo-machete` / `cargo-udeps`
  - added a bounded three-stage plan to the RFC, Wendao program note, active
    surface inventory, and ExecPlan so the next governed move is
    `Phase-8 Stage A: Tooling Reality Inventory Bundle`
- **Validation Executed**:
  - `direnv exec . git diff --check`

### [DONE] Feature: complete Phase-8 Stage A tooling reality inventory

- **Objective**: verify the actual contract/dependency governance tooling that
  already exists in-tree before defining new policy work.
- **Update**:
  - verified that a live repo-root `deny.toml` baseline already exists
  - verified that the live `justfile` already exposes
    `rust-security-audit`, `rust-security-deny`, and
    `rust-security-gate`
  - verified that Nix already provisions `cargo-audit` and `cargo-deny`
  - verified that no live `justfile`, Nix, or CI lane currently references
    `cargo-semver-checks`, `cargo-machete`, or `cargo-udeps`
  - updated the RFC, Wendao program note, outward inventory, and active
    ExecPlan so `Phase-8 Stage A` is recorded as complete and
    `Phase-8 Stage B: Contract and Dependency Policy Bundle` is now the only
    governed next move
- **Validation Executed**:
  - `direnv exec . rg --files $PRJ_ROOT | rg '(^|/)(deny\\.toml|\\.cargo/config\\.toml|justfile|flake\\.nix|devenv\\.nix|\\.github/workflows/.+)'`
  - `direnv exec . rg -n "cargo-deny|cargo-semver-checks|cargo-machete|cargo-udeps|cargo-audit" $PRJ_ROOT -g '!*target*'`
  - `direnv exec . git diff --check`

### [DONE] Feature: complete Phase-7 Stage C and record the transport gate decision

- **Objective**: close `Phase-7 Stage C` by exposing negotiated transport
  diagnostics through both outward inspection payloads and the live rerank
  telemetry path, then record the explicit `Phase 7` gate decision.
- **Update**:
  - extended
    `packages/rust/crates/xiuxian-wendao-core/src/artifacts/payload.rs`
    with `selected_transport`, `fallback_from`, and `fallback_reason`
  - wired those diagnostics through
    `packages/rust/crates/xiuxian-wendao/src/link_graph/plugin_runtime/artifacts/resolve.rs`
    and
    `packages/rust/crates/xiuxian-wendao/src/gateway/studio/types/config.rs`
    so the outward Studio inspection surface now reports negotiated transport
    and fallback decisions
  - extended
    `packages/rust/crates/xiuxian-wendao/src/link_graph/models/records/payload.rs`
    and
    `packages/rust/crates/xiuxian-wendao/src/link_graph/index/search/plan/payload/quantum/rerank.rs`
    so live Julia rerank telemetry now carries the same transport decision
    fields
  - updated the RFC, Wendao program note, surface inventory, and active
    ExecPlan so `Phase-7 Stage C` is recorded as complete and the explicit
    `Phase 7` gate decision is `go`
- **Validation Executed**:
  - `direnv exec . cargo test -p xiuxian-wendao-core artifacts::payload::tests:: --lib`
  - `direnv exec . cargo test -p xiuxian-wendao-runtime artifacts:: --lib`
  - `direnv exec . cargo test -p xiuxian-wendao-julia compatibility::link_graph::tests:: --lib`
  - `direnv exec . cargo test -p xiuxian-wendao gateway::studio::types::config::tests:: --lib`
  - `direnv exec . cargo test -p xiuxian-wendao link_graph::plugin_runtime::tests:: --lib`
  - `direnv exec . cargo test -p xiuxian-wendao gateway::studio::router::tests::config:: --lib`
  - `direnv exec . cargo test -p xiuxian-wendao gateway::studio::router::handlers::capabilities::deployment::tests:: --lib`
  - `direnv exec . cargo test -p xiuxian-wendao link_graph::index::search::plan::payload::quantum::tests:: --lib`
  - `direnv exec . cargo test -p xiuxian-wendao zhenfa_router::rpc::tests:: --lib`
  - `direnv exec . cargo test -p xiuxian-wendao --lib --no-run`
  - `direnv exec . git diff --check`

### [DONE] Feature: complete Phase-7 Stage B with real Flight materialization on the Lance Arrow line

- **Objective**: finish `Phase-7 Stage B` by replacing the inventory-only
  Flight preference with a real runtime-owned Flight client, while respecting
  the repository's existing LanceDB Arrow `57.3` line instead of adding a new
  transport-only Arrow line.
- **Update**:
  - added
    `packages/rust/crates/xiuxian-wendao-runtime/src/transport/flight.rs`
    as the runtime-owned real Flight client seam
  - pinned the Flight transport stack to `arrow-flight = 57.3.0`, aligned to
    the Arrow line already used by `lance-arrow`
  - reused `xiuxian-vector`'s existing
    `engine_batches_to_lance_batches(...)` and
    `lance_batches_to_engine_batches(...)` helpers so the host rerank path
    stays on the current engine Arrow line while the Flight transport stays on
    the Lance Arrow line
  - widened
    `packages/rust/crates/xiuxian-wendao-runtime/src/transport/negotiation.rs`
    so the negotiated client can materialize both `ArrowFlight` and
    `ArrowIpcHttp`, and can process batches through either path
  - rewired the host rerank seam to consume the negotiated runtime client
    directly instead of collapsing immediately back into an Arrow IPC client
  - added runtime preferred-path, fallback-path, and mock Flight roundtrip
    coverage, plus touched host rerank tests for both Arrow IPC and Arrow
    Flight bindings
  - updated the RFC, surface inventory, Wendao program note, and active
    ExecPlan so `Phase-7 Stage B` is now recorded as complete and `Stage C`
    is the governed next move
- **Validation Executed**:
  - `direnv exec . cargo test -p xiuxian-wendao-runtime transport:: --lib --features julia`
  - `direnv exec . cargo test -p xiuxian-wendao link_graph::index::search::plan::payload::quantum:: --lib`
  - `direnv exec . cargo test -p xiuxian-wendao-runtime --lib --features julia --no-run`
  - `direnv exec . cargo test -p xiuxian-wendao --lib --no-run`
  - `direnv exec . git diff --check`

### [DONE] Feature: land the first Phase-7 Stage B runtime negotiation policy bundle

- **Objective**: move `Phase 7` out of inventory-only mode by landing a real
  runtime-owned negotiation seam without pretending the workspace already has
  a Flight client implementation.
- **Update**:
  - added
    `packages/rust/crates/xiuxian-wendao-runtime/src/transport/negotiation.rs`
    as the runtime-owned owner of the canonical transport preference order and
    deterministic fallback selection
  - hardened
    `packages/rust/crates/xiuxian-wendao-runtime/src/transport/client.rs`
    so configured non-IPC transports are rejected explicitly at the Arrow IPC
    client-construction seam instead of being treated as generic HTTP
  - rewired the rerank transport path to delegate through the new runtime
    negotiation helper before materializing the current `ArrowIpcHttp` client
  - added runtime and host-side tests for the canonical preference order and
    deterministic fallback from unsupported `ArrowFlight` to
    `ArrowIpcHttp`
  - updated the RFC, active surface inventory, Wendao program note, and
    active ExecPlan so `Stage B` is now recorded as in progress rather than
    still pending
- **Validation Executed**:
  - `direnv exec . cargo test -p xiuxian-wendao-runtime transport:: --lib --features julia`
  - `direnv exec . cargo test -p xiuxian-wendao link_graph::plugin_runtime::tests:: --lib`
  - `direnv exec . cargo test -p xiuxian-wendao link_graph::index::search::plan::payload::quantum:: --lib`

### [DONE] Feature: complete Phase-7 Stage A transport surface inventory

- **Objective**: turn `Phase 7` from a named plan into one concrete,
  physically grounded transport inventory before any negotiation-policy code
  moves.
- **Update**:
  - identified the live generic transport contract surface in
    `xiuxian-wendao-core` through `PluginCapabilityBinding`,
    `PluginTransportEndpoint`, and `PluginTransportKind`
  - identified the only live runtime-owned transport-construction seam in
    `xiuxian-wendao-runtime/src/transport/client.rs`, where Arrow IPC clients
    are currently built from generic bindings
  - recorded that the host `arrow_transport.rs` helper is a response encoder,
    not a negotiation owner, and that `UiPluginArtifact` is the current
    outward inspection seam for endpoint metadata
  - fixed the canonical `Phase 7` transport preference order as
    `ArrowFlight -> ArrowIpcHttp -> LocalProcessArrowIpc`
  - updated the RFC, active surface inventory, Wendao program note, and
    active ExecPlan so `Phase-7 Stage B: Negotiation Policy Bundle` is now
    the only active next move
- **Validation Executed**:
  - `direnv exec . git diff --check`

### [DONE] Feature: open the staged Phase-7 transport-hardening plan

- **Objective**: turn the post-`M6` transport work into a governed `Phase 7`
  rollout before resuming code exploration.
- **Update**:
  - added a three-stage `Phase 7: Flight-First Runtime Negotiation` push plan
    to the active RFC
  - updated the Wendao program note and active ExecPlan so `Phase-7 Stage A:
Transport Surface Inventory Bundle` is now the only active next move
  - kept the kickoff documentation-only so no transport behavior changed
- **Validation Executed**:
  - `direnv exec . git diff --check`

### [DONE] Feature: complete Stage C and formally close Wendao M6

- **Objective**: run the explicit `M6` exit review and record a governed
  go/no-go decision instead of leaving the migration in open-ended late-`M6`
  additive drift.
- **Update**:
  - recorded the `M6` exit review as a `go` in the active RFC and Wendao
    program note
  - updated the outward inventory, Modelica package note, and active ExecPlan
    so all active docs now agree that the additive-proof track is complete
  - opened `Phase 7: Flight-First Runtime Negotiation` as the next
    macro-phase target
- **Validation Executed**:
  - `direnv exec . git diff --check`

### [DONE] Research: land Julia 1.12 diagnostics for Arrow write-path hotspots

- **Objective**: stop guessing about `offset_map` and `offset_binary` by
  using Julia `1.12.5` diagnostics to determine whether the remaining cost is
  runtime dispatch, compile churn, or downstream allocation and write
  behavior.
- **Update**:
  - added a cache-local diagnostics harness at
    `.cache/agent/benchmarks/arrow_julia_julia12_diagnostics.jl`
    that can run `dispatch`, `compile`, and `allocs` modes against the live
    write-path benchmark cases
  - fixed the harness to capture trace output through a temporary file
    because Julia `1.12.5` does not support `redirect_stdout(::IOBuffer)`
  - confirmed after warmup that both `offset_map` and `offset_binary`
    report `precompile_count = 0` in `@trace_dispatch` and `@trace_compile`
  - confirmed via `Profile.Allocs` that the remaining bytes are dominated by
    downstream `unsafe_write`, `GenericMemory`, and `sizehint!`, while the
    first package-local frames in `src/arraytypes/map.jl` and
    `src/arraytypes/list.jl` are comparatively small
  - tested and rejected a first writer-level candidate that pre-sized
    `IOBuffer` per message from exact `8 + metalen + bodylen`; it matched the
    intended buffer-first direction but regressed the official harness and was
    reverted immediately
  - concluded that the next production slice should focus on writer and
    buffer sizing strategy in `src/utils.jl` / `src/write.jl`, not more
    speculative wrapper-local dispatch tuning
- **Validation Executed**:
  - `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_julia12_diagnostics.jl dispatch offset_map`
  - `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_julia12_diagnostics.jl dispatch offset_binary`
  - `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_julia12_diagnostics.jl compile offset_map`
  - `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_julia12_diagnostics.jl compile offset_binary`
  - `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_julia12_diagnostics.jl allocs offset_map`
  - `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_julia12_diagnostics.jl allocs offset_binary`

### [DONE] Feature: add a guarded direct single-partition `Arrow.tobuffer` fast path

- **Objective**: capture the builder-first/buffer-first upside from the
  off-tree direct-stream prototype without regressing the top-level
  string/binary write paths that still benchmark best on the legacy writer
  route.
- **Update**:
  - added a narrow direct `Arrow.tobuffer` path in `src/utils.jl` that
    bypasses `Writer` task/channel orchestration only when
    `Tables.partitions(data)` is a single-element tuple and the top-level
    partition columns are not `AbstractString` / `Base.CodeUnits`
  - reused the existing schema, dictionary-batch, and record-batch builders
    so the emitted streaming bytes stay identical to `Arrow.write(io, tbl)`
  - added byte-equivalence tests in `test/runtests.jl` for plain
    single-partition input, dictionary-encoded input, metadata-bearing input,
    and multi-partition fallback behavior
  - prototyped and rejected two broader variants before landing:
    unguarded direct mode and exact `IOBuffer` pre-sizing
- **Validation Executed**:
  - `direnv exec . julia --project=.data/arrow-julia/test -e 'using Pkg; Pkg.instantiate(); include(joinpath(pwd(), ".cache/agent/benchmarks/arrow_julia_writepaths_bench.jl"))'`
  - `direnv exec . julia --project=.data/arrow-julia/test -e 'using Pkg; Pkg.instantiate(); Pkg.test("Arrow")'`

### [DONE] Feature: add a Map-only sizehint refinement to direct `Arrow.tobuffer`

- **Objective**: recover the allocation win from exact `IOBuffer` pre-sizing
  without reopening the broader `List`/`Map` candidate that still showed
  mixed nested-list behavior.
- **Update**:
  - added a narrow helper in `src/utils.jl` that computes the exact stream
    message bytes only when the converted top-level direct-path columns
    contain `Arrow.Map`
  - kept every other direct-path case on the existing plain `IOBuffer()`
    route, so top-level nested lists do not inherit the broader exact-sizehint
    experiment
  - added a byte-equivalence regression in `test/runtests.jl` for an
    offset-backed top-level map column
  - added a same-process A/B harness at
    `.cache/agent/benchmarks/arrow_julia_direct_tobuffer_map_exact_ab.jl`
    to compare `plain` vs `map_exact` without inter-run noise
  - kept the refinement because the A/B run showed `offset_map` essentially
    flat-to-positive on wall time (`1.890 -> 1.884 ms`) and materially lower
    allocations (`13.341 -> 8.827 MiB`), while `offset_nested_list` only paid
    a negligible non-map branch cost (`0.752 -> 0.771 ms`)
- **Validation Executed**:
  - `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_direct_tobuffer_map_exact_ab.jl`
  - `direnv exec . julia --project=.data/arrow-julia/test -e 'using Pkg; Pkg.instantiate(); include(joinpath(pwd(), ".cache/agent/benchmarks/arrow_julia_writepaths_bench.jl"))'`
  - `direnv exec . julia --project=.data/arrow-julia/test -e 'using Pkg; Pkg.instantiate(); Pkg.test("Arrow")'`

### [DONE] Research: phase-profile the remaining single-partition `Arrow.tobuffer` hotspots

- **Objective**: stop guessing whether the next production slice belongs in
  writer orchestration, direct-path message handling, or the remaining
  `toarrowtable` conversion work.
- **Update**:
  - added a cache-local phase harness at
    `.cache/agent/benchmarks/arrow_julia_tobuffer_phase_profile.jl`
  - measured `current_tobuffer`, `legacy_write`, `manual_direct_total`,
    `direct_convert`, `direct_build`, and `direct_write` for
    `offset_binary` and `offset_map`
  - confirmed that `offset_binary` is overwhelmingly dominated by the shared
    write phase, not by `toarrowtable` conversion or message build
  - confirmed that `offset_map` is now overwhelmingly dominated by
    `toarrowtable` conversion (`1.920 ms / 4.222 MiB`) while direct write is
    a small tail (`0.241 ms / 9.112 MiB`)
  - concluded that the next production slice should target the map
    conversion path in `toarrowtable` / `toarrowvector`, not generic writer
    orchestration
- **Validation Executed**:
  - `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_tobuffer_phase_profile.jl`

### [DONE] Feature: bypass `ToArrow` wrapping for offset-axis concrete map columns

- **Objective**: target the real remaining `offset_map` hotspot in
  `toarrowtable` conversion instead of touching writer orchestration again.
- **Update**:
  - added a cache-local component harness at
    `.cache/agent/benchmarks/arrow_julia_map_conversion_components.jl` and
    confirmed the dominant conversion cost is the flatten step
    `mapoffsetsandvalues(...)`, not validity or child `arrowvector`
  - tested and rejected a source-based `mapoffsetsandvalues(::ToArrow)`
    rewrite because it improved flatten in isolation but did not stabilize
    the full `MapKind` conversion
  - added an interleaved A/B harness at
    `.cache/agent/benchmarks/arrow_julia_map_raw_vs_toarrow_interleaved.jl`
    and confirmed that raw offset-axis map inputs consistently beat the
    wrapped `ToArrow` path for both concrete and `Union{Missing,Dict}` cases
  - landed a narrow guard in
    `.data/arrow-julia/src/arraytypes/arraytypes.jl` so
    `MapKind + offset axes + concrete/concreteunion element types` bypass the
    `ToArrow` wrapper
  - added a byte-equivalence regression in
    `.data/arrow-julia/test/runtests.jl` for an offset-backed missing-map
    `tobuffer` path
- **Validation Executed**:
  - `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_map_conversion_components.jl`
  - `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_map_raw_vs_toarrow_interleaved.jl`
  - `direnv exec . julia --project=.data/arrow-julia/test -e 'using Pkg; Pkg.instantiate(); include(joinpath(pwd(), ".cache/agent/benchmarks/arrow_julia_writepaths_bench.jl"))'`
  - `direnv exec . julia --project=.data/arrow-julia/test -e 'using Pkg; Pkg.instantiate(); Pkg.test("Arrow")'`

### [DONE] Feature: exact-fill `ToList` offsets construction

- **Objective**: attack the remaining `offset_binary` / `offset_string`
  shared hotspot from the builder side instead of retrying rejected buffered
  write candidates.
- **Update**:
  - added a cache-local candidate harness at
    `.cache/agent/benchmarks/arrow_julia_tolist_exactfill_candidate.jl`
    and confirmed the `ToList` builder itself improves when offsets are
    filled into a preallocated `Vector{I}(undef, len + 1)` instead of
    `push!`-ing into `I[0]`
  - landed the exact-fill builder in
    `.data/arrow-julia/src/arraytypes/list.jl`, while keeping the Int32 ->
    Int64 promotion path intact
  - added an offset binary byte-equivalence regression in
    `.data/arrow-julia/test/runtests.jl`
  - treated the first official harness run as noisy because `offset_binary`
    bounced the wrong way, then confirmed the keep/reject decision from a
    warm rerun
- **Validation Executed**:
  - `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_tolist_exactfill_candidate.jl`
  - `direnv exec . julia --project=.data/arrow-julia/test -e 'using Pkg; Pkg.instantiate(); include(joinpath(pwd(), ".cache/agent/benchmarks/arrow_julia_writepaths_bench.jl"))'`
  - `direnv exec . julia --project=.data/arrow-julia/test -e 'using Pkg; Pkg.instantiate(); Pkg.test("Arrow")'`

### [DONE] Research: reject single-column top-level string/binary direct `tobuffer` widening

- **Objective**: verify whether the guarded direct single-partition
  `Arrow.tobuffer` path could be safely widened to include single-column
  top-level `AbstractString` / `Base.CodeUnits` inputs.
- **Update**:
  - added cache-local A/B probes at
    `.cache/agent/benchmarks/arrow_julia_singlecol_string_binary_direct_ab.jl`
    and
    `.cache/agent/benchmarks/arrow_julia_singlecol_string_binary_paths_ab.jl`
  - compared three shapes on the same 100k-row top-level cases:
    current guarded `Arrow.tobuffer`, explicit legacy
    `Arrow.write(io, table)`, and explicit `_directtobuffer(...)`
  - initial one-off runs looked positive, but the path-isolated rerun did
    not stabilize and showed legacy still ahead on both `offset_string` and
    `offset_binary`
  - reverted the candidate guard widening immediately and kept the landed
    production rule unchanged: top-level string/binary columns still use the
    legacy writer path
- **Validation Executed**:
  - `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_singlecol_string_binary_direct_ab.jl`
  - `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_singlecol_string_binary_paths_ab.jl`

### [DONE] Research: reject the narrow raw-`KeyValue` struct-child bypass for `MapKind`

- **Objective**: check whether `offset_map` still had a cheap remaining win by
  skipping the generic `ToArrow` wrapper only for the `Vector{KeyValue}`
  struct-child conversion, without attempting another full `MapEntries`
  rewrite.
- **Update**:
  - added a cache-local probe at
    `.cache/agent/benchmarks/arrow_julia_map_keyvalue_raw_struct_ab.jl`
  - measured both the child-only path and a full `MapKind` conversion A/B
    between the current route and a raw struct-child bypass
  - observed only a tiny, unstable child-only signal, followed by a slight
    regression on the full conversion path:
    `current_full_map 1.746 ms / 4.221 MiB` vs
    `raw_struct_full_map 1.782 ms / 4.221 MiB`
  - rejected the candidate and kept package source unchanged
- **Validation Executed**:
  - `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_map_keyvalue_raw_struct_ab.jl`

### [DONE] Research: reject Julia-1.12 binary write-side alternatives beyond the current chunk loop

- **Objective**: verify whether the remaining `offset_binary` write hotspot can
  be reduced through Julia-1.12-specific write-side choices instead of another
  broad writer rewrite.
- **Update**:
  - extended
    `.cache/agent/benchmarks/arrow_julia_offset_binary_write_candidates.jl`
    with two new candidates:
    - `base_write_loop`: replace per-chunk `unsafe_write(pointer(...))` with
      `Base.write(io, chunk)`
    - `prealloc_copy_loop`: preallocate an external `Vector{UInt8}`, fill it
      by chunk, then expose it through `IOBuffer(buf; ...)`
  - confirmed a Julia `1.12.5` runtime detail that matters for future write
    work: `IOBuffer.data` is now `Memory{UInt8}`, not a resizable
    `Vector{UInt8}`
  - rejected both candidates on the same 100k-row offset binary case:
    `base_write_loop 1.659 ms / 1.050 MiB`,
    `prealloc_copy_loop 1.941 ms / 0.672 MiB`,
    while the current path stayed at
    `1.623 ms / 1.050 MiB`
  - retained the earlier conclusion that `IOBuffer(sizehint=n)` is the only
    still-positive signal in this microbench family, but it does not yet map
    cleanly onto the production outer-stream write shape
- **Validation Executed**:
  - `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_offset_binary_write_candidates.jl`

### [DONE] Feature: widen direct single-partition `tobuffer` only for top-level single-column strings

- **Objective**: revisit the previously rejected single-column
  string/binary direct-path widening with a narrower, production-safe scope:
  top-level single-column `AbstractString` only.
- **Update**:
  - updated `.data/arrow-julia/src/utils.jl` so direct single-partition
    `Arrow.tobuffer` now admits single-column top-level `AbstractString`
    inputs, while still rejecting `Base.CodeUnits`
  - added a byte-equivalence regression in
    `.data/arrow-julia/test/runtests.jl` for offset-backed
    `Union{Missing,String}` input
  - used the new interleaved harness
    `.cache/agent/benchmarks/arrow_julia_string_binary_direct_interleaved.jl`
    to verify same-process legacy vs direct behavior after the change
  - kept binary excluded because the direct-vs-legacy signal on
    `Base.CodeUnits` stayed too small and noisy to justify widening it too
- **Validation Executed**:
  - `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_string_binary_direct_interleaved.jl`
  - `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_writepaths_bench.jl`
  - `direnv exec . julia --project=.data/arrow-julia/test -e 'using Pkg; Pkg.instantiate(); Pkg.test("Arrow")'`

### [DONE] Research: reject a broad mixed-table relaxation of the direct `Arrow.tobuffer` guard

- **Objective**: verify whether the new direct single-partition `tobuffer`
  fast path should also be enabled for mixed tables that include top-level
  string/binary columns, instead of keeping the current conservative guard.
- **Update**:
  - added cache-local mixed-table probes for
    `mixed_int_string`, `mixed_map_string`, `mixed_int_binary`,
    `mixed_map_binary`, `mixed_string_string`, and `mixed_string_binary`
  - measured each case with the current writer path against an off-tree direct
    path candidate
  - found inconsistent results: some mixed cases improved, but
    `mixed_map_binary`, `mixed_string_string`, and `mixed_string_binary`
    did not
  - kept the repository on the existing conservative guard because the next
    relaxation needs a more structural eligibility rule than "multi-column is
    safe"
- **Validation Executed**:
  - `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_direct_tobuffer_mixed_candidates.jl`

### [DONE] Research: reject string-leaf-count as a standalone direct `Arrow.tobuffer` guard signal

- **Objective**: determine whether the current conservative direct `tobuffer`
  guard can be replaced with a more structural rule based on the number of
  string-like Arrow leaves produced by `toarrowtable(...)`.
- **Update**:
  - added a cache-local structural probe that recursively counts string-like
    leaves across `Arrow.List`, `Arrow.Map`, `Arrow.Struct`, and
    `Arrow.FixedSizeList` outputs
  - benchmarked mixed cases across ints, UUIDs, nested lists, maps, strings,
    and binaries while recording both writer/direct timings and the computed
    leaf count
  - the initial run made `stringy_leaves <= 1` look plausible, but targeted
    reruns did not stabilize into a safe rule
  - examples: `mixed_uuid_string` flipped sign between reruns, while
    `mixed_map_string` and `mixed_map_binary` also moved enough that a
    leaf-count-only guard would still be too speculative
  - kept the repository on the existing conservative guard; no package code
    change was landed from this research slice
- **Validation Executed**:
  - `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_direct_tobuffer_leaf_profile.jl`
  - `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_direct_tobuffer_leaf_profile.jl mixed_uuid_string mixed_map_string mixed_map_binary mixed_string_string`

### [DONE] Feature: complete Stage B of the late-M6 Wendao migration push

- **Objective**: align the active outward docs around one late-`M6` story so
  the program stops mixing early extraction language with the current external
  Modelica proof state.
- **Update**:
  - rewrote
    `packages/rust/crates/xiuxian-wendao/docs/06_roadmap/409_core_runtime_plugin_surface_inventory.md`
    from an early `P0` mapping-only framing into a late-`M6` outward-surface
    inventory and compatibility ledger
  - updated the active RFC, Wendao program note, Modelica package note, and
    ExecPlan so `Stage B` is recorded as complete and `Stage C: M6 Exit Review`
    is now the governed next move
- **Validation Executed**:
  - `direnv exec . git diff --check`

### [DONE] Feature: close Stage A of the late-M6 Wendao gateway push

- **Objective**: finish the remaining Studio repo service-state bundle as one
  bounded late-`M6` batch instead of continuing endpoint-by-endpoint drift.
- **Update**:
  - added deterministic route coverage for `POST /api/repo/index` with the
    repo-index runtime held in queued state so the service-state payload stays
    stable under test
  - added external Modelica proofs for both `POST /api/repo/index` and
    `GET /api/repo/index/status`
  - added a narrow repo-index payload redaction helper for `updatedAt`, with
    no new dead-code suppressions
  - synced the active RFC, Wendao program note, Modelica package note, and
    ExecPlan so `Stage A` is now recorded as complete and `Stage B` is the
    next governed move
- **Validation Executed**:
  - `INSTA_UPDATE=always direnv exec . cargo test -p xiuxian-wendao gateway::studio::studio_repo_sync_api_tests::repo_index_ --lib`

### [DONE] Feature: size-hint `IOBuffer` for `ToList{UInt8}` write paths

- **Objective**: reduce the new top production hotspot `offset_binary`
  without changing Arrow's external semantics or broadening writer behavior
  beyond the existing `ToList{UInt8}` binary and UTF-8 paths.
- **Update**:
  - added a tiny helper in `src/arraytypes/list.jl` that only size-hints the
    backing `Vector{UInt8}` when the sink is an `IOBuffer`
  - wired that helper into both `ToList{UInt8}` write paths so the outgoing
    byte span `length(col)` is reserved before chunked `unsafe_write`
  - measured and rejected two adjacent binary-only candidates in cache:
    direct `CodeUnits` loop and buffered one-shot materialization
  - confirmed on the official harness that this narrow size-hint path
    improves both `offset_binary` and `offset_string`
- **Validation Executed**:
  - `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_offset_binary_breakdown_bench.jl`
  - `direnv exec . julia --project=.data/arrow-julia/test -e 'using Pkg; Pkg.instantiate(); include(joinpath(pwd(), ".cache/agent/benchmarks/arrow_julia_writepaths_bench.jl"))'`
  - `direnv exec . julia --project=.data/arrow-julia/test -e 'using Test, Arrow, OffsetArrays; include(".data/arrow-julia/test/runtests.jl")'`

### [DONE] Feature: re-center the Wendao migration around a staged late-M6 RFC push

- **Objective**: stop ad hoc route selection and turn the remaining Wendao
  migration work into an RFC-backed staged push for late `M6`.
- **Update**:
  - updated
    `docs/rfcs/2026-03-27-wendao-core-runtime-plugin-migration-rfc.md` so the
    current position is recorded as late `M6` rather than early `M1`
  - aligned the RFC phase plan with the live macro-phase ordering and added a
    three-stage late-`M6` push plan:
    `Gateway Completion Bundle`, `Outward Contract Alignment Bundle`, and
    `M6 Exit Review`
  - updated
    `packages/rust/crates/xiuxian-wendao/docs/06_roadmap/412_core_runtime_plugin_program.md`
    and
    `.cache/codex/execplans/wendao-core-runtime-plugin-migration.md`
    so future work has one authoritative staged entrypoint instead of more
    route-by-route drift
- **Validation Executed**:
  - `direnv exec . git diff --check`

### [DONE] Research: reject a parent-storage fast path for offset-axis `MapKind` flattening

- **Objective**: test whether reusing `parent(x)` for offset-axis map columns
  would beat the current indexed offset-axis flattening without adding a
  runtime dependency on `OffsetArrays`.
- **Update**:
  - prototyped a narrow `parent(x)` fast path in `src/arraytypes/map.jl`
    only when the parent had `Base.OneTo(length(x))` axes
  - local standalone microbench on `OffsetArray{Dict}` improved from
    `1.461 ms` to `1.318 ms`
  - the production gates did not confirm the win, so the candidate was
    reverted and the repository stayed on the earlier indexed-only offset
    path
  - a clean post-revert replay confirmed the repository state is better than
    the candidate, with `flatten_only 1.333 ms`, `offset_map_full_write
1.828 ms`, and official `offset_map 2.082 ms`
- **Validation Executed**:
  - `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_offset_map_breakdown_bench.jl`
  - `direnv exec . julia --project=.data/arrow-julia/test -e 'using Pkg; Pkg.instantiate(); include(joinpath(pwd(), ".cache/agent/benchmarks/arrow_julia_writepaths_bench.jl"))'`
  - `direnv exec . git -C .data/arrow-julia diff --check -- src/arraytypes/map.jl`
  - `direnv exec . git diff --check -- .cache/agent/execplans/arrow_julia_map_parent_storage_fastpath.md docs/GTD/DAILY_2026_03_29.md`

### [DONE] Research: split the remaining `offset_map` flatten hotspot by phase

- **Objective**: stop guessing inside `mapoffsetsandvalues(...)` and measure
  whether the remaining production cost sits in the offsets pass or in the
  `KeyValue` fill pass.
- **Update**:
  - added `.cache/agent/benchmarks/arrow_julia_offset_map_phase_bench.jl`
    alongside the existing breakdown harness
  - confirmed on a clean replay that `flatten_only` is still the dominant
    stage of `offset_map`
  - then split flattening itself and measured
    `offsets_pass = 0.239 ms / 0.391 MiB`,
    `fill_pass = 1.217 ms / 3.063 MiB`,
    `flatten_full = 1.424 ms / 3.453 MiB`
  - concluded that the next production-facing optimization should target
    `pairs(y)` iteration and `KeyValue` filling, not offsets counting
- **Validation Executed**:
  - `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_offset_map_breakdown_bench.jl`
  - `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_offset_map_phase_bench.jl`

### [DONE] Feature: extend the M6 external Modelica proof through the Studio repo projected query-and-navigation family

- **Objective**: close the remaining `repo_projected_*` Studio gateway peers
  as one additive batch instead of continuing route-by-route drift.
- **Update**:
  - added bundled external Modelica route regressions in
    `packages/rust/crates/xiuxian-wendao/tests/unit/studio_repo_sync_api.rs`
    for `/api/repo/projected-page-index-tree-search`,
    `/api/repo/projected-page-search`, `/api/repo/projected-retrieval`,
    `/api/repo/projected-page-family-context`,
    `/api/repo/projected-page-family-search`,
    `/api/repo/projected-page-family-cluster`,
    `/api/repo/projected-page-navigation`,
    `/api/repo/projected-page-navigation-search`, and
    `/api/repo/projected-page-index-trees`
  - reused the same external `Projectionica` repo fixture plus stable
    projected selectors (`Projectionica.Controllers`,
    `Projectionica.Controllers.PI`, `Anchors`, and the `Step` how-to page)
    so the batch proves query expansion, family reopening, navigation
    reopening, mixed retrieval, and tree listing at the Studio repo layer
  - added a small local helper seam in `studio_repo_sync_api.rs` to keep the
    Modelica route setup/config selectors shared without introducing new
    dead-code suppressions
- **Validation Executed**:
  - `INSTA_UPDATE=always direnv exec . cargo test -p xiuxian-wendao gateway::studio::studio_repo_sync_api_tests::repo_projected_page_index_tree_search_endpoint_executes_over_external_modelica_plugin_path --lib`
  - `direnv exec . cargo test -p xiuxian-wendao gateway::studio::studio_repo_sync_api_tests::repo_projected_ --lib`
  - `direnv exec . cargo test -p xiuxian-wendao --lib --no-run`
  - `direnv exec . git diff --check`

### [DONE] Feature: specialize `MapKind` flattening for offset-axis arrays

- **Objective**: improve the production `offset_map` write path without
  changing the current Julia-native `Vector{KeyValue}` representation or
  broadening beyond the existing two-pass flatten algorithm.
- **Update**:
  - added a narrow indexed specialization in `src/arraytypes/map.jl` so
    offset-axis map columns use `x[i + off]` instead of generic iteration
    while one-based/generic inputs keep the old path
  - added append roundtrip coverage for the `xame` offset-map case in
    `test/runtests.jl`
  - replayed both a focused `mapoffsetsandvalues(...)` microbench and the
    official write-path harness
  - confirmed a real `offset_map` gain while leaving allocation shape
    unchanged
- **Validation Executed**:
  - `direnv exec . julia --project=.data/arrow-julia/src/ArrowTypes -e 'using Pkg; Pkg.test()'`
  - `direnv exec . julia --project=.data/arrow-julia/test -e 'using Test, Arrow, OffsetArrays; include(".data/arrow-julia/test/runtests.jl")'`
  - `direnv exec . julia --project=.data/arrow-julia/test -e 'using Pkg; Pkg.instantiate(); include(joinpath(pwd(), ".cache/agent/benchmarks/arrow_julia_writepaths_bench.jl"))'`
  - `direnv exec . git -C .data/arrow-julia diff --check -- src/arraytypes/map.jl test/runtests.jl`
  - `direnv exec . git diff --check -- .cache/agent/execplans/arrow_julia_map_offset_axes_indexed.md docs/GTD/DAILY_2026_03_29.md`

### [DONE] Feature: realign WendaoArrow around Flight-first transport profiles

- **Objective**: update `.data/WendaoArrow` so its package docs stop
  describing the Julia boundary as HTTP-only and instead align with the
  stronger Arrow Flight surface now available in `.data/arrow-julia`.
- **Update**:
  - added
    `.data/WendaoArrow/docs/01_core/wendao_arrow/architecture/transport-profiles.md`
    to record the new package direction: Flight-first architecture with
    Arrow IPC over HTTP as the current stable fallback profile
  - rewrote the WendaoArrow `README`, `SPEC`, HTTP interface doc, schema
    contract doc, vector subsystem note, and roadmap so they consistently
    position transport selection as a host/runtime concern and preserve
    stream-first processors as the future Flight integration seam
  - kept the current WendaoArrow runtime behavior unchanged; this slice was
    a design and documentation adaptation rather than a package transport
    implementation change
  - synced the active ExecPlan with the landed scope, validation, and
    blueprint alignment
- **Validation Executed**:
  - `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/runtests.jl`
  - `direnv exec . git diff --check -- .data/WendaoArrow docs/GTD/DAILY_2026_03_29.md .cache/codex/execplans/wendaoarrow_arrow_flight_adaptation.md`

### [DONE] Feature: land a package-local WendaoArrow Flight `DoExchange` adapter

- **Objective**: move beyond design-only alignment and give
  `.data/WendaoArrow` a real package-local Flight service seam that reuses the
  existing table-first and stream-first analyzer contracts without waiting for
  the gRPC listener layer.
- **Update**:
  - added `src/flight.jl` plus exports in `src/WendaoArrow.jl` for
    `flight_descriptor`, `build_flight_service`, and
    `build_stream_flight_service`
  - implemented a local `Arrow.Flight.Service` adapter around `DoExchange`
    that decodes incoming `FlightData`, reuses the existing processor
    contracts, re-encodes Arrow output, and preserves
    `wendao.schema_version = v1` in Arrow schema metadata
  - updated `.data/WendaoArrow/Project.toml` so the environment resolves the
    local `arrow-julia` and matching vendored `ArrowTypes` source tree rather
    than mixing the local Arrow runtime with a stale registry copy
  - added WendaoArrow tests that exercise the new Flight surface through local
    `Arrow.Flight.dispatch` / `Arrow.Flight.doexchange` paths
  - synchronized package docs so they now distinguish between the landed local
    Flight adapter and the still-pending network/gRPC binding
- **Validation Executed**:
  - `direnv exec . julia --project=.data/WendaoArrow -e 'using Pkg; Pkg.resolve(); deps = Pkg.dependencies(); for (_, dep) in deps; if dep.name == "ArrowTypes" || dep.name == "Arrow"; println("name=" * dep.name); println("version=" * string(dep.version)); println("path=" * string(dep.source)); println("---"); end; end'`
  - `direnv exec . julia --project=.data/WendaoArrow -e 'using Arrow; println(isdefined(Arrow, :Flight)); println(hasproperty(Arrow, :Flight))'`
  - `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/runtests.jl`

### [DONE] Feature: land an optional WendaoArrow gRPC Flight server binding

- **Objective**: close the remaining network-listener gap in
  `.data/WendaoArrow` without making `gRPCServer` a mandatory dependency of
  the default HTTP fallback runtime.
- **Update**:
  - added `gRPCServer` weak-dependency wiring in
    `.data/WendaoArrow/Project.toml` and a new
    `.data/WendaoArrow/ext/WendaoArrowgRPCServerExt.jl` package extension
  - exported `DEFAULT_FLIGHT_PORT`, `flight_server`, `serve_flight`, and
    `serve_stream_flight` from `src/WendaoArrow.jl`, with explicit error
    surfaces when the optional weak dependency is absent
  - kept the analyzer-facing contract unchanged by routing the network listener
    surface through the existing `build_flight_service` and
    `build_stream_flight_service` seams
  - added `.data/WendaoArrow/test/flight_grpcserver.jl`, which instantiates an
    isolated temporary Julia environment, develops the vendored Arrow and
    `gRPCServer` sources into it, and verifies non-blocking Flight server
    startup and shutdown
  - synchronized the WendaoArrow package docs and active ExecPlan so they now
    describe the landed optional gRPC Flight binding and the remaining
    interoperability / operations work accurately
- **Validation Executed**:
  - `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/runtests.jl`
  - `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/flight_grpcserver.jl`
  - `direnv exec . git diff --check -- .data/WendaoArrow docs/GTD/DAILY_2026_03_29.md .cache/codex/execplans/wendaoarrow_arrow_flight_adaptation.md`

### [DONE] Feature: add a runnable WendaoArrow Flight startup path and cross-process startup smoke test

- **Objective**: move the optional Flight server surface from extension-only
  plumbing to a runnable package operation path with one real cross-process
  startup proof.
- **Update**:
  - added `.data/WendaoArrow/examples/stream_scoring_flight_server.jl` as the
    first dedicated Flight example using the existing stream-first scoring
    processor shape
  - added `.data/WendaoArrow/scripts/run_flight_example.jl` and
    `.data/WendaoArrow/scripts/run_stream_scoring_flight_server.sh` so the repo
    can bootstrap a temporary opt-in Flight environment with vendored
    `Arrow`, `ArrowTypes`, and `gRPCServer` instead of requiring the base
    package environment to carry those dependencies by default
  - extended `.data/WendaoArrow/test/flight_grpcserver.jl` so it now spawns the
    new Flight startup script in a separate process, waits for the optional
    listener to bind, and proves the runnable startup path works across process
    boundaries
  - added `.data/WendaoArrow/scripts/test_wendao_arrow_flight.sh` and updated
    the package docs to point at the runnable Flight startup and verification
    entrypoints
- **Validation Executed**:
  - `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/flight_grpcserver.jl`

### [DONE] Feature: tighten immediate Flight failure semantics around the optional WendaoArrow server

- **Objective**: remove the server-side ambiguity around `DoExchange`
  failures that happen before the first outbound `FlightData` message and
  describe the remaining remote-client limits precisely instead of treating
  them as solved interoperability.
- **Update**:
  - patched `.cache/vendor/gRPCServer.jl/src/server.jl` so server-streaming
    and bidi-streaming handlers delay response headers until the first actual
    outbound message and use trailers-only error responses when a failure
    happens earlier
  - added `.cache/vendor/gRPCServer.jl/test/unit/test_streaming_response_timing.jl`
    to lock the new wire-level behavior for immediate server-streaming and
    bidi-streaming failures, and kept the HTTP/2 conformance regression in
    place alongside it
  - extended `.data/WendaoArrow/test/flight_grpcserver.jl` with bad-schema
    remote probes so the packaged Flight startup path now records the current
    client boundary explicitly: `pyarrow.flight` still reports a generic
    `Server never sent a data message`, while native Julia client diagnostics
    are still inconsistent before the first response batch
  - synchronized WendaoArrow package docs and the active ExecPlan so the
    current boundary now distinguishes "server immediate-error wire semantics
    fixed" from "all remote clients surface those failures cleanly"
- **Validation Executed**:
  - `direnv exec . julia --project=.cache/vendor/gRPCServer.jl -e 'using Pkg; Pkg.instantiate(); using Test, gRPCServer; include(".cache/vendor/gRPCServer.jl/test/TestUtils.jl"); using .TestUtils; include(".cache/vendor/gRPCServer.jl/test/unit/test_streaming_response_timing.jl"); include(".cache/vendor/gRPCServer.jl/test/unit/test_http2_conformance.jl")'`
  - `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/flight_grpcserver.jl`

### [DONE] Feature: stabilize native Julia Flight failure diagnostics at the Arrow client surface

- **Objective**: stop leaving native Julia `DoExchange` callers with a silent
  empty response when the server fails before emitting the first schema-bearing
  `FlightData` message, even if `gRPCClient` still does not surface a non-OK
  gRPC status consistently on that boundary.
- **Update**:
  - patched `.data/arrow-julia/src/flight/convert/streaming.jl` so
    `Arrow.Flight.table(...)` and `Arrow.Flight.stream(...)` now throw
    `ArgumentError` when they are asked to derive a schema from an empty
    response stream with no schema-bearing `FlightData`
  - refined that Arrow-side `ArgumentError` so it now points to the likely
    pre-first-message server-failure boundary and explicitly notes that the
    underlying transport may not have surfaced the corresponding non-OK gRPC
    status
  - extended `.data/arrow-julia/test/flight/ipc_conversion.jl` with empty
    response regressions and an explicit "schema provided, empty table is
    still valid" check to keep the new guard narrow
  - updated `.data/WendaoArrow/test/flight_grpcserver.jl` so the native Julia
    remote failure regression now asserts a deterministic client-visible error:
    either an explicit non-OK gRPC exception or the new Arrow conversion
    failure when the response stream ends before a schema message arrives
  - synchronized package docs and the active ExecPlan so the current boundary
    reads as "native Julia empty-response diagnostics stabilized at the Arrow
    client layer" rather than "native Julia diagnostics remain wholly
    inconsistent"
- **Validation Executed**:
  - `direnv exec . julia --project=.data/arrow-julia -e 'using Pkg; Pkg.instantiate(); using Test, Arrow, Tables; include(".data/arrow-julia/test/flight/ipc_conversion.jl")'`
  - `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/flight_grpcserver.jl`
  - `direnv exec . git diff --check -- .data/WendaoArrow docs/GTD/DAILY_2026_03_29.md .cache/codex/execplans/wendaoarrow_arrow_flight_adaptation.md`

### [DONE] Feature: tighten WendaoArrow Flight input-contract errors to invalid-argument diagnostics

- **Objective**: replace the current bad-schema remote failure surface with a
  domain-level missing-column diagnostic so Flight clients receive actionable
  `INVALID_ARGUMENT` responses instead of generic transport-level internals.
- **Update**:
  - changed `.cache/vendor/gRPCServer.jl/src/errors.jl` to map `FieldError`
    into `StatusCode.INVALID_ARGUMENT`
  - changed `.cache/vendor/gRPCServer.jl/src/server.jl` so server-streaming and
    bidirectional streaming exception handling reuses the shared
    `handle_exception_with_logging` mapping path instead of forcing
    `INTERNAL`
  - changed `.data/arrow-julia/ext/arrowgrpcserverext/streams.jl` so the
    streaming bridge rethrows the original handler exception instead of
    wrapping it in `TaskFailedException`
  - extended `.cache/vendor/gRPCServer.jl/test/unit/test_error_mapping.jl` and
    `.data/arrow-julia/test/flight/grpcserver_extension/server_streaming_tests.jl`
    plus `.data/arrow-julia/test/flight/grpcserver_extension/bidi_streaming_tests.jl`
    with regressions for the new root-cause propagation path
  - added `.data/WendaoArrow/src/contracts.jl` with `require_columns(...)` and
    applied it to the packaged stream scoring / metadata examples so they fail
    with explicit missing-column diagnostics before touching unavailable fields
  - extended `.data/WendaoArrow/test/runtests.jl` with unit coverage for the
    column-contract helper and updated `.data/WendaoArrow/test/flight_grpcserver.jl`
    so cross-process `pyarrow.flight` and native Julia failures now assert
    `grpc-status: 3` plus the missing-column message
  - synchronized package docs and the active ExecPlan so the current Flight
    boundary reads as "contract failures surface as invalid argument" rather
    than "transport restores internal errors"
- **Validation Executed**:
  - `direnv exec . julia --project=.cache/vendor/gRPCServer.jl -e 'using Pkg; Pkg.instantiate(); using Test, gRPCServer; include(".cache/vendor/gRPCServer.jl/test/unit/test_error_mapping.jl")'`
  - `direnv exec . julia --project=.data/arrow-julia -e 'using Pkg; Pkg.instantiate(); using Test, Arrow, gRPCClient, Tables; include(".data/arrow-julia/test/flight/support.jl"); include(".data/arrow-julia/test/flight/grpcserver_extension.jl")'`
  - `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/runtests.jl`
  - `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/flight_grpcserver.jl`
  - `direnv exec . .data/WendaoArrow/scripts/test_wendao_arrow_flight.sh`
  - `direnv exec . git diff --check -- .data/WendaoArrow .data/arrow-julia .cache/vendor/gRPCServer.jl docs/GTD/DAILY_2026_03_29.md .cache/codex/execplans/wendaoarrow_arrow_flight_adaptation.md`

### [DONE] Feature: strip Julia exception wrappers from remote invalid-argument Flight errors

- **Objective**: make remote Flight clients receive clean domain diagnostics for
  invalid-argument failures instead of Julia exception wrapper text such as
  `ArgumentError("...")` or `ArgumentError: ...`.
- **Update**:
  - changed `.cache/vendor/gRPCServer.jl/src/dispatch.jl` to centralize
    client-visible exception formatting in `exception_message_for_client(...)`
  - changed that formatter so non-debug `INTERNAL` failures still collapse to
    `"Internal server error"`, while non-`INTERNAL` `ArgumentError` paths now
    emit the bare `err.msg`
  - extended `.cache/vendor/gRPCServer.jl/test/unit/test_error_mapping.jl`
    with regressions for `handle_exception(...)` and
    `handle_exception_with_logging(...)` so `INVALID_ARGUMENT` responses keep
    the bare message text
  - updated `.data/WendaoArrow/test/flight_grpcserver.jl` so cross-process
    `pyarrow.flight` and native Julia failures now reject leaked
    `ArgumentError` wrappers and assert the exact bare missing-column message
  - synchronized package docs and the active ExecPlan so the current Flight
    boundary reads as "clean missing-column diagnostics" rather than merely
    "invalid argument with Julia exception text"
- **Validation Executed**:
  - `direnv exec . julia --project=.cache/vendor/gRPCServer.jl -e 'using Pkg; Pkg.instantiate(); using Test, gRPCServer; include(".cache/vendor/gRPCServer.jl/test/unit/test_error_mapping.jl")'`
  - `direnv exec . julia --project=.data/arrow-julia -e 'using Pkg; Pkg.instantiate(); using Test, Arrow, gRPCClient, Tables; include(".data/arrow-julia/test/flight/support.jl"); include(".data/arrow-julia/test/flight/grpcserver_extension.jl")'`
  - `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/flight_grpcserver.jl`
  - `direnv exec . .data/WendaoArrow/scripts/test_wendao_arrow_flight.sh`
  - `direnv exec . git diff --check -- .data/WendaoArrow .data/arrow-julia .cache/vendor/gRPCServer.jl docs/GTD/DAILY_2026_03_29.md .cache/codex/execplans/wendaoarrow_arrow_flight_adaptation.md`

### [DONE] Feature: surface invalid `vector_score` values as row-level Flight diagnostics

- **Objective**: prevent score coercion failures in the packaged scoring example
  from falling back to generic conversion exceptions by turning bad
  `vector_score` values into explicit row-level contract errors.
- **Update**:
  - changed `.data/WendaoArrow/src/contracts.jl` to add
    `coerce_float64(...)`, which converts valid values to `Float64` and throws
    `ArgumentError` with row/column context for invalid or missing values
  - exported that helper from `.data/WendaoArrow/src/WendaoArrow.jl`
  - updated `.data/WendaoArrow/examples/stream_scoring_flight_server.jl` and
    `.data/WendaoArrow/examples/stream_scoring_server.jl` so score processing
    now uses `coerce_float64(...)` instead of raw `Float64(...)`
  - extended `.data/WendaoArrow/test/runtests.jl` with helper coverage for
    valid numeric conversion, string rejection, and `missing` rejection
  - extended `.data/WendaoArrow/test/flight_grpcserver.jl` with cross-process
    `pyarrow.flight` and native Julia regressions that send string
    `vector_score` data and now assert `grpc-status: 3` plus the row-level
    numeric-value message
  - synchronized package docs and the active ExecPlan so the current Flight
    boundary reads as "column/value diagnostics" rather than only
    "missing-column diagnostics"
- **Validation Executed**:
  - `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/runtests.jl`
  - `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/flight_grpcserver.jl`
  - `direnv exec . .data/WendaoArrow/scripts/test_wendao_arrow_flight.sh`
  - `direnv exec . git diff --check -- .data/WendaoArrow docs/GTD/DAILY_2026_03_29.md .cache/codex/execplans/wendaoarrow_arrow_flight_adaptation.md`

### [DONE] Feature: surface invalid `doc_id` values as row-level Flight diagnostics

- **Objective**: prevent packaged scoring examples from relying on `String[]`
  container conversion failures by turning bad `doc_id` values into explicit
  row-level contract errors.
- **Update**:
  - changed `.data/WendaoArrow/src/contracts.jl` to add
    `coerce_string(...)`, which accepts only string values and throws
    `ArgumentError` with row/column context for invalid or missing values
  - exported that helper from `.data/WendaoArrow/src/WendaoArrow.jl`
  - updated `.data/WendaoArrow/examples/stream_scoring_flight_server.jl` and
    `.data/WendaoArrow/examples/stream_scoring_server.jl` so `doc_id`
    normalization now uses `coerce_string(...)` before pushing into the result
    vectors
  - extended `.data/WendaoArrow/test/runtests.jl` with helper coverage for
    valid string conversion, integer rejection, and `missing` rejection
  - extended `.data/WendaoArrow/test/flight_grpcserver.jl` with cross-process
    `pyarrow.flight` and native Julia regressions that send integer `doc_id`
    data and now assert `grpc-status: 3` plus the row-level string-value
    message
  - synchronized package docs and the active ExecPlan so the current Flight
    boundary now names both `doc_id` and `vector_score` diagnostics explicitly
- **Validation Executed**:
  - `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/runtests.jl`
  - `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/flight_grpcserver.jl`
  - `direnv exec . git diff --check -- .data/WendaoArrow docs/GTD/DAILY_2026_03_29.md .cache/codex/execplans/wendaoarrow_arrow_flight_adaptation.md`

### [DONE] Feature: reject mismatched Wendao schema versions before analyzer execution

- **Objective**: stop request-side schema drift from reaching analyzer logic by
  enforcing `wendao.schema_version == v1` at the IPC/Flight decoder boundary.
- **Update**:
  - changed `.data/WendaoArrow/src/contracts.jl` to add
    `require_schema_version(...)`
  - changed `.data/WendaoArrow/src/codec.jl` so `decode_ipc(...)` enforces
    schema-version metadata and `decode_stream(...)` now returns a lightweight
    validated stream wrapper that peeks the first batch, checks metadata, then
    preserves streaming iteration for processors
  - changed `.data/WendaoArrow/src/flight.jl` so both table and stream Flight
    decoders reject mismatched schema-version metadata before calling the
    processor
  - extended `.data/WendaoArrow/test/runtests.jl` with direct IPC/stream
    decoder regressions plus helper coverage for missing and mismatched schema
    versions
  - updated `.data/WendaoArrow/test/flight_grpcserver.jl` so all happy-path
    request helpers now send explicit `v1` metadata and the cross-process
    `pyarrow.flight` / native Julia regressions now assert `grpc-status: 3`
    for mismatched schema-version requests
  - synchronized package docs and the active ExecPlan so the current Flight
    boundary explicitly includes schema-version diagnostics alongside
    missing-column and bad-value diagnostics
- **Validation Executed**:
  - `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/runtests.jl`
  - `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/flight_grpcserver.jl`
  - `direnv exec . .data/WendaoArrow/scripts/test_wendao_arrow_flight.sh`
  - `direnv exec . git diff --check -- .data/WendaoArrow docs/GTD/DAILY_2026_03_29.md .cache/codex/execplans/wendaoarrow_arrow_flight_adaptation.md`

### [DONE] Feature: reject empty `doc_id` and non-finite `vector_score` values as explicit Flight contract errors

- **Objective**: tighten the scoring request contract so semantically bad
  values fail before analyzer execution with row-level invalid-argument
  diagnostics instead of leaking downstream conversion behavior.
- **Update**:
  - changed `.data/WendaoArrow/src/contracts.jl` so `coerce_string(...)`
    rejects empty strings and `coerce_float64(...)` rejects non-finite numeric
    values after conversion
  - extended `.data/WendaoArrow/test/runtests.jl` with helper coverage for
    empty-string `doc_id` values plus `NaN` and `Inf` `vector_score` values
  - updated `.data/WendaoArrow/test/flight_grpcserver.jl` with cross-process
    `pyarrow.flight` and native Julia regressions that send empty `doc_id`
    values and non-finite `vector_score` values, then assert `grpc-status: 3`
    plus the row-level contract messages
  - synchronized package docs and the active ExecPlan so the current Flight
    boundary now states non-empty `doc_id` and finite `vector_score`
    diagnostics explicitly
- **Validation Executed**:
  - `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/runtests.jl`
  - `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/flight_grpcserver.jl`
  - `direnv exec . .data/WendaoArrow/scripts/test_wendao_arrow_flight.sh`
  - `direnv exec . git diff --check -- .data/WendaoArrow docs/GTD/DAILY_2026_03_29.md .cache/codex/execplans/wendaoarrow_arrow_flight_adaptation.md`

### [DONE] Feature: restore non-OK Flight streaming errors before the first response batch

- **Objective**: stop the Flight `gRPCServer` bridge from closing server-streaming
  and bidirectional exchanges with an implicit OK before background handler
  failures have a chance to propagate into gRPC trailers.
- **Update**:
  - changed `.data/arrow-julia/ext/arrowgrpcserverext/streams.jl` so response
    draining no longer closes the stream on its own
  - changed `.data/arrow-julia/ext/arrowgrpcserverext/handlers.jl` so
    server-streaming and bidi handlers wait for background task completion
    before calling `gRPCServer.close!`, allowing task failures to bubble back
    into the server's non-OK streaming completion path
  - extended `.data/arrow-julia/test/flight/grpcserver_extension/server_streaming_tests.jl`
    and `.data/arrow-julia/test/flight/grpcserver_extension/bidi_streaming_tests.jl`
    with regressions that assert pre-first-response failures do not close the
    stream as success
  - updated `.data/WendaoArrow/test/flight_grpcserver.jl` so the cross-process
    bad-schema regressions now assert explicit `Internal server error` /
    `grpc-status: 13` behavior for both `pyarrow.flight` and native Julia
  - synchronized package docs and the active ExecPlan to reflect that this
    boundary now propagates real non-OK gRPC failure instead of generic empty
    response semantics
- **Validation Executed**:
  - `direnv exec . julia --project=.data/arrow-julia -e 'using Pkg; Pkg.instantiate(); using Test, Arrow, gRPCClient, Tables; include(".data/arrow-julia/test/flight/support.jl"); include(".data/arrow-julia/test/flight/grpcserver_extension.jl")'`
  - `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/flight_grpcserver.jl`
  - `direnv exec . git diff --check -- .data/WendaoArrow .data/arrow-julia docs/GTD/DAILY_2026_03_29.md .cache/codex/execplans/wendaoarrow_arrow_flight_adaptation.md`

### [DONE] Feature: align WendaoArrow Flight response envelopes with standard server semantics

- **Objective**: tighten the Flight response path so WendaoArrow emits
  descriptor-free server responses while keeping the request-side descriptor
  available for routing and diagnostics.
- **Update**:
  - changed `.data/WendaoArrow/src/flight.jl` so package-local Flight
    responses preserve schema metadata but no longer echo request descriptors
    into outbound `FlightData`
  - updated `.data/WendaoArrow/test/runtests.jl` to assert the response-side
    descriptor is absent on both table-first and stream-first `DoExchange`
    paths
  - replaced the unstable `.data/arrow-julia` native network regression
    experiment with a local `ServerStream{Any}` extension test so the
    gRPCServer bridge still has concrete coverage for the generic stream type
    fallback exercised by real server registration
  - synchronized the WendaoArrow package docs and ExecPlan to record the
    descriptor-free response boundary as part of the current Flight profile
- **Validation Executed**:
  - `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/runtests.jl`
  - `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/flight_grpcserver.jl`
  - `direnv exec . .data/WendaoArrow/scripts/test_wendao_arrow_flight.sh`
  - `direnv exec . julia /dev/stdin <<'EOF' ... include("test/flight/grpcserver_extension.jl") ... EOF`
  - `direnv exec . git diff --check -- .data/WendaoArrow .data/arrow-julia docs/GTD/DAILY_2026_03_29.md .cache/codex/execplans/wendaoarrow_arrow_flight_adaptation.md`

### [DONE] Feature: prove cross-process pyarrow Flight interop against WendaoArrow

- **Objective**: replace the optional Flight startup path's bind-only smoke
  check with one real external Flight client exchange against the runnable
  WendaoArrow server.
- **Update**:
  - extended `.data/WendaoArrow/test/flight_grpcserver.jl` so the existing
    startup-script test now discovers the project `pyarrow.flight` client
    environment, starts the optional Flight server in a separate process, and
    executes a real remote `DoExchange` roundtrip against
    `.data/WendaoArrow/examples/stream_scoring_flight_server.jl`
  - verified the returned `doc_id`, `analyzer_score`, and `final_score`
    columns over a true external `pyarrow.flight` client instead of stopping
    at listener readiness
  - tightened package docs and the active ExecPlan to describe the new
    validation boundary as "`pyarrow` `DoExchange` proven" rather than generic
    remote-client parity
- **Validation Executed**:
  - `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/flight_grpcserver.jl`
  - `direnv exec . .data/WendaoArrow/scripts/test_wendao_arrow_flight.sh`
  - `direnv exec . .cache/arrow-julia-flight-pyenv/bin/python -c '...'`

### [DONE] Feature: restore native Julia Flight client interop against the optional WendaoArrow server

- **Objective**: close the remaining native Julia client gap so the optional
  Flight startup path is proven against both external `pyarrow` and
  `Arrow.Flight.Client`.
- **Update**:
  - traced the native Julia `DoExchange` failure below WendaoArrow itself to
    the vendored `gRPCServer.jl` HTTP/2 server preface: the server was
    advertising `SETTINGS_ENABLE_PUSH`, which RFC 7540 forbids for servers and
    which `libcurl`/`nghttp2` rejects before any RPC `HEADERS` are sent
  - patched `.cache/vendor/gRPCServer.jl/src/http2/connection.jl` so the
    server preface omits `ENABLE_PUSH`, while still accepting the peer setting
    on inbound client `SETTINGS`
  - added a gRPCServer HTTP/2 conformance assertion to lock out
    `ENABLE_PUSH` in outbound server `SETTINGS`
  - extended `.data/WendaoArrow/test/flight_grpcserver.jl` with a
    cross-process native Julia `Arrow.Flight.Client` `DoExchange` roundtrip
    against the packaged scoring server and verified the expected scoring
    columns
  - synchronized WendaoArrow package docs and the active ExecPlan so the
    current boundary reads as "`pyarrow` plus native Julia client proven"
- **Validation Executed**:
  - `direnv exec . julia --project=.cache/vendor/gRPCServer.jl -e 'using Pkg; Pkg.instantiate(); using Test, gRPCServer; include(".cache/vendor/gRPCServer.jl/test/unit/test_http2_conformance.jl")'`
  - `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/flight_grpcserver.jl`

### [DONE] Feature: extend the optional WendaoArrow Flight proof to multi-batch remote exchanges

- **Objective**: prove that the runnable Flight startup path still behaves
  correctly once a remote client sends more than one record batch in the same
  `DoExchange` stream.
- **Update**:
  - extended `.data/WendaoArrow/test/flight_grpcserver.jl` with shared scoring
    server startup helpers so the remote coverage can add more exchange shapes
    without duplicating process bootstrap code
  - added one cross-process `pyarrow.flight` multi-batch `DoExchange` roundtrip
    that writes two request batches and verifies the server returns the merged
    `doc_id`, `analyzer_score`, and `final_score` columns in order
  - added one cross-process native Julia `Arrow.Flight.Client` multi-batch
    `DoExchange` roundtrip using `Tables.partitioner(...)` and the same output
    assertions, so both currently proven client families now cover more than
    the single-batch happy path
  - synchronized the WendaoArrow package docs and active ExecPlan so the
    current boundary reads as "single-batch and multi-batch proven for
    `pyarrow` and native Julia" rather than just one-batch interoperability
- **Validation Executed**:
  - `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/flight_grpcserver.jl`

### [DONE] Feature: extend the `M6` external Modelica proof through the Studio repo projected reopen family

- **Objective**: stop landing single projected reopen peers and push one
  bounded `M6` slice through the deterministic Studio repo projected lookup
  family so stable reopen flows survive the external plugin path together.
- **Update**:
  - added builtin-registry external Modelica regressions in
    `packages/rust/crates/xiuxian-wendao/tests/unit/studio_repo_sync_api.rs`
    for `/api/repo/projected-page`,
    `/api/repo/projected-page-index-tree`,
    `/api/repo/projected-page-index-node`,
    `/api/repo/projected-retrieval-hit`, and
    `/api/repo/projected-retrieval-context`
  - reused the stable symbol-backed projected reference page
    `Projectionica.Controllers.PI` and the stable `Anchors` page-index node
    so all five routes stay anchored to one deterministic external Modelica
    selector family
  - proved that the batch reopens the symbol page itself, its page-index
    tree, its `Anchors` node, the deterministic retrieval hit for that
    node, and the corresponding node-context bundle
  - synced the Wendao program note, surface inventory, Modelica package
    roadmap, and the active ExecPlan to the landed `M6` state
- **Validation Executed**:
  - `INSTA_UPDATE=always direnv exec . cargo test -p xiuxian-wendao gateway::studio::studio_repo_sync_api_tests::repo_projected_ --lib`
  - `direnv exec . cargo test -p xiuxian-wendao gateway::studio::studio_repo_sync_api_tests::repo_projected_ --lib`
  - `direnv exec . cargo test -p xiuxian-wendao --lib --no-run`
  - `direnv exec . git diff --check`

### [DONE] Feature: extend the `M6` external Modelica proof through the Studio repo lifecycle-and-projection routes

- **Objective**: stop landing single-endpoint crumbs and push one bundled
  `M6` slice through the sibling Studio repo gateway layer so that repo
  status and top-level projection routes survive the external plugin path
  together.
- **Update**:
  - added builtin-registry external Modelica regressions in
    `packages/rust/crates/xiuxian-wendao/tests/unit/studio_repo_sync_api.rs`
    for `/api/repo/sync`, `/api/repo/projected-pages`, and
    `/api/repo/projected-gap-report`
  - proved that `/api/repo/sync` reopens a stable local-checkout status for
    the external Modelica repo and preserves the validated revision summary
  - proved that `/api/repo/projected-pages` enumerates non-empty projected
    pages over the external Modelica path, including controller, example,
    tutorial, and users-guide pages
  - proved that `/api/repo/projected-gap-report` stays anchored to the
    requested external Modelica repo and keeps `summary.gap_count` aligned
    with the materialized gap list while preserving a non-zero projected
    page count
  - synced the Wendao program note, surface inventory, Modelica package
    roadmap, and the active ExecPlan to the landed `M6` state
- **Validation Executed**:
  - `INSTA_UPDATE=always direnv exec . cargo test -p xiuxian-wendao gateway::studio::studio_repo_sync_api_tests::repo_ --lib`
  - `direnv exec . cargo test -p xiuxian-wendao gateway::studio::studio_repo_sync_api_tests::repo_ --lib`
  - `direnv exec . cargo test -p xiuxian-wendao --lib --no-run`
  - `direnv exec . git diff --check`

### [DONE] Feature: extend the `M6` external Modelica proof into the Studio repo doc-coverage route

- **Objective**: keep pushing the same `M6` additive external-plugin proof
  across the sibling Studio repo route family and prove that module-scoped
  doc coverage also survives the external plugin path at the same gateway
  layer.
- **Update**:
  - reused the existing Studio repo doc-coverage route at
    `/api/repo/doc-coverage` instead of adding a new gateway surface
  - added a builtin-registry external Modelica regression in
    `packages/rust/crates/xiuxian-wendao/tests/unit/studio_repo_sync_api.rs`
  - reused the stable external Modelica module selector
    `Projectionica.Controllers`
  - proved that the route stays anchored to the requested external Modelica
    module and reopens non-empty documentation coverage, including
    annotation, tutorial, and users-guide records
  - synced the Wendao program note, surface inventory, Modelica package
    roadmap, and the active ExecPlan to the landed `M6` state
- **Validation Executed**:
  - `INSTA_UPDATE=always direnv exec . cargo test -p xiuxian-wendao gateway::studio::studio_repo_sync_api_tests::repo_doc_coverage_endpoint_executes_over_external_modelica_plugin_path --lib`
  - `direnv exec . cargo test -p xiuxian-wendao gateway::studio::studio_repo_sync_api_tests::repo_doc_coverage_endpoint_executes_over_external_modelica_plugin_path --lib`
  - `direnv exec . cargo test -p xiuxian-wendao --lib --no-run`
  - `direnv exec . git diff --check`

### [DONE] Feature: extend the `M6` external Modelica proof into the Studio repo example-search route

- **Objective**: keep pushing the same `M6` additive external-plugin proof
  across the sibling Studio repo route family and prove that deterministic
  example search also survives the external plugin path at the same gateway
  layer.
- **Update**:
  - reused the existing Studio repo example-search route at
    `/api/repo/example-search` instead of adding a new gateway surface
  - added a builtin-registry external Modelica regression in
    `packages/rust/crates/xiuxian-wendao/tests/unit/studio_repo_sync_api.rs`
  - reused the stable external Modelica example query `Step` with `limit=3`
  - proved that the route stays anchored to the requested external Modelica
    repo and reopens deterministic example hits for
    `Controllers/Examples/Step.mo`
  - synced the Wendao program note, surface inventory, Modelica package
    roadmap, and the active ExecPlan to the landed `M6` state
- **Validation Executed**:
  - `INSTA_UPDATE=always direnv exec . cargo test -p xiuxian-wendao gateway::studio::studio_repo_sync_api_tests::repo_example_search_endpoint_executes_over_external_modelica_plugin_path --lib`
  - `direnv exec . cargo test -p xiuxian-wendao gateway::studio::studio_repo_sync_api_tests::repo_example_search_endpoint_executes_over_external_modelica_plugin_path --lib`
  - `direnv exec . cargo test -p xiuxian-wendao --lib --no-run`
  - `direnv exec . git diff --check`

### [DONE] Feature: extend the `M6` external Modelica proof into the Studio repo symbol-search route

- **Objective**: keep pushing the same `M6` additive external-plugin proof
  across the sibling Studio repo route family and prove that deterministic
  symbol search also survives the external plugin path at the same gateway
  layer.
- **Update**:
  - reused the existing Studio repo symbol-search route at
    `/api/repo/symbol-search` instead of adding a new gateway surface
  - added a builtin-registry external Modelica regression in
    `packages/rust/crates/xiuxian-wendao/tests/unit/studio_repo_sync_api.rs`
  - reused the stable external Modelica symbol query `PI` with `limit=3`
  - proved that the route stays anchored to the requested external Modelica
    repo and reopens deterministic symbol hits for
    `Projectionica.Controllers.PI`
  - synced the Wendao program note, surface inventory, Modelica package
    roadmap, and the active ExecPlan to the landed `M6` state
- **Validation Executed**:
  - `INSTA_UPDATE=always direnv exec . cargo test -p xiuxian-wendao gateway::studio::studio_repo_sync_api_tests::repo_symbol_search_endpoint_executes_over_external_modelica_plugin_path --lib`
  - `direnv exec . cargo test -p xiuxian-wendao gateway::studio::studio_repo_sync_api_tests::repo_symbol_search_endpoint_executes_over_external_modelica_plugin_path --lib`
  - `direnv exec . cargo test -p xiuxian-wendao --lib --no-run`
  - `direnv exec . git diff --check`

### [DONE] Feature: extend the `M6` external Modelica proof into the Studio repo module-search route

- **Objective**: keep pushing the same `M6` additive external-plugin proof
  across the sibling Studio repo route family and prove that deterministic
  module search also survives the external plugin path at the same gateway
  layer.
- **Update**:
  - reused the existing Studio repo module-search route at
    `/api/repo/module-search` instead of adding a new gateway surface
  - added a builtin-registry external Modelica regression in
    `packages/rust/crates/xiuxian-wendao/tests/unit/studio_repo_sync_api.rs`
  - reused the stable external Modelica namespace query
    `Projectionica.Controllers` with `limit=3`
  - proved that the route stays anchored to the requested external Modelica
    repo and reopens deterministic module hits for the requested controller
    namespace
  - synced the Wendao program note, surface inventory, Modelica package
    roadmap, and the active ExecPlan to the landed `M6` state
- **Validation Executed**:
  - `INSTA_UPDATE=always direnv exec . cargo test -p xiuxian-wendao gateway::studio::studio_repo_sync_api_tests::repo_module_search_endpoint_executes_over_external_modelica_plugin_path --lib`
  - `direnv exec . cargo test -p xiuxian-wendao gateway::studio::studio_repo_sync_api_tests::repo_module_search_endpoint_executes_over_external_modelica_plugin_path --lib`
  - `direnv exec . cargo test -p xiuxian-wendao --lib --no-run`
  - `direnv exec . git diff --check`

### [DONE] Feature: extend the `M6` external Modelica proof into the Studio repo overview route

- **Objective**: keep pushing the same `M6` additive external-plugin proof
  past the Studio docs route family and prove that repo-summary reopening
  also survives the external plugin path at the same gateway layer.
- **Update**:
  - reused the existing Studio repo overview route at `/api/repo/overview`
    instead of adding a new gateway surface
  - added a builtin-registry external Modelica regression in
    `packages/rust/crates/xiuxian-wendao/tests/unit/studio_repo_sync_api.rs`
  - proved that the route stays anchored to the requested external Modelica
    repo, reopens the `Projectionica` summary, and preserves a non-zero
    module count over the external plugin path
  - synced the Wendao program note, surface inventory, Modelica package
    roadmap, and the active ExecPlan to the landed `M6` state
- **Validation Executed**:
  - `INSTA_UPDATE=always direnv exec . cargo test -p xiuxian-wendao gateway::studio::studio_repo_sync_api_tests::repo_overview_endpoint_executes_over_external_modelica_plugin_path --lib`
  - `direnv exec . cargo test -p xiuxian-wendao gateway::studio::studio_repo_sync_api_tests::repo_overview_endpoint_executes_over_external_modelica_plugin_path --lib`
  - `direnv exec . cargo test -p xiuxian-wendao --lib --no-run`
  - `direnv exec . git diff --check`

### [DONE] Feature: extend the `M6` external Modelica proof into the Studio docs projected-gap-report route

- **Objective**: keep pushing the same `M6` additive external-plugin proof
  across the non-planner Studio docs route family and prove that
  docs-facing projected gap reporting survives the external plugin path.
- **Update**:
  - reused the existing Studio docs projected-gap-report route at
    `/api/docs/projected-gap-report` instead of adding a new gateway surface
  - added a builtin-registry external Modelica regression in
    `packages/rust/crates/xiuxian-wendao/tests/unit/studio_repo_sync_api.rs`
  - proved that the route stays anchored to the requested external Modelica
    repo and keeps `summary.gap_count` aligned with the materialized gap
    list
  - synced the Wendao program note, surface inventory, Modelica package
    roadmap, and the active ExecPlan to the landed `M6` state
- **Validation Executed**:
  - `INSTA_UPDATE=always direnv exec . cargo test -p xiuxian-wendao gateway::studio::studio_repo_sync_api_tests::docs_projected_gap_report_endpoint_executes_over_external_modelica_plugin_path --lib`
  - `direnv exec . cargo test -p xiuxian-wendao --lib --no-run`
  - `direnv exec . git diff --check`

### [DONE] Feature: extend the `M6` external Modelica proof into the Studio docs navigation-search route

- **Objective**: keep pushing the same `M6` additive external-plugin proof
  across the non-planner Studio docs route family and prove that grouped
  navigation-bundle expansion survives the external plugin path.
- **Update**:
  - reused the existing Studio docs navigation-search route at
    `/api/docs/navigation-search` instead of adding a new gateway surface
  - added a builtin-registry external Modelica regression in
    `packages/rust/crates/xiuxian-wendao/tests/unit/studio_repo_sync_api.rs`
  - reused the stable external Modelica query `Projectionica.Controllers`
    with `kind=reference`, `family_kind=how_to`, `limit=2`,
    `related_limit=3`, and `family_limit=2`
  - proved that the route returns non-empty, limit-bounded navigation hits
    whose center pages stay anchored to the requested Modelica controller
    path
  - synced the Wendao program note, surface inventory, Modelica package
    roadmap, and the active ExecPlan to the landed `M6` state
- **Validation Executed**:
  - `INSTA_UPDATE=always direnv exec . cargo test -p xiuxian-wendao gateway::studio::studio_repo_sync_api_tests::docs_navigation_search_endpoint_executes_over_external_modelica_plugin_path --lib`
  - `direnv exec . cargo test -p xiuxian-wendao --lib --no-run`
  - `direnv exec . git diff --check`

### [DONE] Feature: align ArrowTypes with Julia 1.12 and make offset-axis handling explicit

- **Objective**: raise the last stale Julia compat floor inside Arrow.jl's
  in-tree `ArrowTypes` package, then replace implicit wrapper-entrypoint
  indexing assumptions with explicit Julia offset-axis APIs.
- **Update**:
  - raised `src/ArrowTypes/Project.toml` from `julia = "1.0"` to
    `julia = "1.12"` so it matches the main package's already-landed Julia
    floor
  - added internal `ArrowTypes` helpers based on `Base.has_offset_axes`
    to centralize offset-shift calculation and one-based passthrough checks
  - updated `ArrowTypes.ToArrow` to use those helpers instead of relying on
    implicit `firstindex(x) == 1` logic
  - added internal `ToArrow` source helpers and propagated them into the
    `list` and `map` wrapper entrypoints so those paths no longer
    duplicate direct `:data` / `:offset` field access
  - propagated the same source-helper path into contiguous write checks,
    materialization, validity scanning, and bool packing so the main
    write-side `ToArrow` consumers now share one internal access model
  - added matching `ToList` / `ToStruct` source helpers so the remaining
    wrapper-heavy string and struct write paths no longer reach into
    wrapper fields directly
  - added `ArrowTypes._needsconvert` and matching `ToFixedSizeList`
    source helpers so the remaining write-side wrapper consumers now
    avoid direct field reads there as well
  - centralized the common `ToArrow` validity/source decision into
    shared arraytypes helpers so `primitive` and `bool` no longer repeat
    the same materialize-vs-wrapper branching
  - replayed the production write-path harness against both current
    worktree and `HEAD (d03cad0)` baseline; current state remains ahead
    on the core offset-heavy cases, including `offset_map`,
    `offset_nested_list`, `offset_string`, and the previously failing
    `offset_*_any` / `offset_uuid*` paths
  - updated `src/arraytypes/list.jl` to reuse the same offset-shift helper
    for generic list flattening entrypoints
  - added targeted `ArrowTypes` coverage for one-based views, offset vectors,
    and cached offset values
  - updated the package README to state the Julia `1.12+` requirement
    explicitly
- **Validation Executed**:
  - `direnv exec . julia --project=.data/arrow-julia/src/ArrowTypes -e 'using Pkg; Pkg.test()'`
  - `direnv exec . julia --project=.data/arrow-julia/test -e 'using Test, Arrow, OffsetArrays; include(".data/arrow-julia/test/runtests.jl")'`
  - `direnv exec . git -C .data/arrow-julia diff --check -- README.md src/ArrowTypes/Project.toml src/ArrowTypes/src/ArrowTypes.jl src/ArrowTypes/test/tests.jl src/arraytypes/list.jl test/runtests.jl`
  - `direnv exec . git diff --check -- .cache/agent/execplans/arrow_julia_compat_1_12_offset_api.md docs/GTD/DAILY_2026_03_29.md`

### [DONE] Feature: extend the `M6` external Modelica proof into the Studio docs navigation route

- **Objective**: keep pushing the same `M6` additive external-plugin proof
  across the non-planner Studio docs route family and prove that
  deterministic tree-context plus family-cluster reopening survives the
  external plugin path.
- **Update**:
  - reused the existing Studio docs navigation route at
    `/api/docs/navigation` instead of adding a new gateway surface
  - added a builtin-registry external Modelica regression in
    `packages/rust/crates/xiuxian-wendao/tests/unit/studio_repo_sync_api.rs`
  - reused the stable external Modelica reference page
    `Projectionica.Controllers.PI` plus its `Anchors` page-index node
  - proved that the route keeps `center.page` anchored to the requested
    Modelica projected page, returns `node_context`, and reopens the
    `how_to` family cluster
  - synced the Wendao program note, surface inventory, Modelica package
    roadmap, and the active ExecPlan to the landed `M6` state
- **Validation Executed**:
  - `INSTA_UPDATE=always direnv exec . cargo test -p xiuxian-wendao gateway::studio::studio_repo_sync_api_tests::docs_navigation_endpoint_executes_over_external_modelica_plugin_path --lib`
  - `direnv exec . cargo test -p xiuxian-wendao --lib --no-run`
  - `direnv exec . git diff --check`

### [DONE] Feature: extend the `M6` external Modelica proof into the Studio docs family-cluster route

- **Objective**: keep pushing the same `M6` additive external-plugin proof
  across the non-planner Studio docs route family and prove that
  deterministic single-family reopening survives the external plugin path.
- **Update**:
  - reused the existing Studio docs family-cluster route at
    `/api/docs/family-cluster` instead of adding a new gateway surface
  - added a builtin-registry external Modelica regression in
    `packages/rust/crates/xiuxian-wendao/tests/unit/studio_repo_sync_api.rs`
  - reused the stable external Modelica reference page
    `Projectionica.Controllers` and requested the `how_to` family cluster
  - proved that the route keeps `center_page` anchored to the requested
    Modelica reference page and reopens the `how_to` family cluster
  - synced the Wendao program note, surface inventory, Modelica package
    roadmap, and the active ExecPlan to the landed `M6` state
- **Validation Executed**:
  - `INSTA_UPDATE=always direnv exec . cargo test -p xiuxian-wendao gateway::studio::studio_repo_sync_api_tests::docs_family_cluster_endpoint_executes_over_external_modelica_plugin_path --lib`
  - `direnv exec . cargo test -p xiuxian-wendao --lib --no-run`
  - `direnv exec . git diff --check`

### [DONE] Feature: extend the `M6` external Modelica proof into the Studio docs family-search route

- **Objective**: keep pushing the same `M6` additive external-plugin proof
  across the non-planner Studio docs route family and prove that grouped
  family-search expansion survives the external plugin path.
- **Update**:
  - reused the existing Studio docs family-search route at
    `/api/docs/family-search` instead of adding a new gateway surface
  - added a builtin-registry external Modelica regression in
    `packages/rust/crates/xiuxian-wendao/tests/unit/studio_repo_sync_api.rs`
  - reused the stable external Modelica query `Projectionica.Controllers`
    with `kind=reference`, `limit=3`, and `per_kind_limit=2`
  - proved that the route returns non-empty, limit-bounded grouped family
    hits whose center pages stay anchored to the requested Modelica
    controller path
  - synced the Wendao program note, surface inventory, Modelica package
    roadmap, and the active ExecPlan to the landed `M6` state
- **Validation Executed**:
  - `INSTA_UPDATE=always direnv exec . cargo test -p xiuxian-wendao gateway::studio::studio_repo_sync_api_tests::docs_family_search_endpoint_executes_over_external_modelica_plugin_path --lib`
  - `direnv exec . cargo test -p xiuxian-wendao --lib --no-run`
  - `direnv exec . git diff --check`

### [DONE] Feature: extend the `M6` external Modelica proof into the Studio docs family-context route

- **Objective**: keep pushing the same `M6` additive external-plugin proof
  across the non-planner Studio docs route family and prove that grouped
  family-context reopening survives the external plugin path.
- **Update**:
  - reused the existing Studio docs family-context route at
    `/api/docs/family-context` instead of adding a new gateway surface
  - added a builtin-registry external Modelica regression in
    `packages/rust/crates/xiuxian-wendao/tests/unit/studio_repo_sync_api.rs`
  - reused the stable external Modelica projected how-to page selected from
    the builtin-registry analysis result
  - proved that the route keeps `center_page` anchored to the requested
    Modelica how-to page and returns non-empty grouped families
  - synced the Wendao program note, surface inventory, Modelica package
    roadmap, and the active ExecPlan to the landed `M6` state
- **Validation Executed**:
  - `INSTA_UPDATE=always direnv exec . cargo test -p xiuxian-wendao gateway::studio::studio_repo_sync_api_tests::docs_family_context_endpoint_executes_over_external_modelica_plugin_path --lib`
  - `direnv exec . cargo test -p xiuxian-wendao --lib --no-run`
  - `direnv exec . git diff --check`

### [DONE] Research: align Arrow.jl map-path planning with local `arrow-rs 57.3.0` and Julia 1.12.5 realities

- **Objective**: stop treating Rust/C++ Arrow builder structure as something
  that can be copied mechanically into Julia, and ground the next write-path
  decisions in the actual local Rust compat version plus the current Julia
  runtime characteristics.
- **Update**:
  - verified that the workspace still carries a real `arrow 57.3.0` compat
    path in `packages/rust/crates/xiuxian-vector/Cargo.toml`, even though the
    mainline workspace resolves `arrow 58.1.0`
  - generated a local scratch `cargo doc` for `arrow@57.3.0` and read the
    local `MapBuilder`, `GenericListBuilder`, and `MutableBuffer` sources
  - confirmed that Rust's map builder shape is offsets + nulls + key child +
    value child
  - prototyped a literal Julia rewrite that replaced `Vector{KeyValue}` with
    direct `keys::Vector` and `values::Vector`
  - measured a severe end-to-end regression in the existing Julia harness:
    `offset_map` moved from roughly `2.251 ms / 13.342 MiB` to
    `14.416 ms / 11.607 MiB`
  - localized the regression to the direct child-buffer construction step,
    reverted the code, and kept only a useful new `Any` offset-map regression
    (`xame`) in `test/runtests.jl`
- **Validation Executed**:
  - `direnv exec . julia -e 'println(VERSION)'`
  - local scratch `cargo doc -p arrow@57.3.0 --no-deps`
  - `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_writepaths_bench.jl`
  - `direnv exec . julia --project=.data/arrow-julia/test -e 'using Arrow, OffsetArrays; ... println("xame_roundtrip_ok")'`
  - `direnv exec . git -C .data/arrow-julia diff --check -- src/arraytypes/map.jl test/runtests.jl`
  - `direnv exec . git diff --check -- .cache/agent/execplans/arrow_julia_map_builder_alignment_57_3.md`

### [DONE] Feature: extend the `M6` external Modelica proof into the Studio docs page route

- **Objective**: keep pushing the same `M6` additive external-plugin proof
  across the non-planner Studio docs route family and prove that
  deterministic docs page lookup survives the external plugin path.
- **Update**:
  - reused the existing Studio docs page route at `/api/docs/page` instead
    of adding a new gateway surface
  - added a builtin-registry external Modelica regression in
    `packages/rust/crates/xiuxian-wendao/tests/unit/studio_repo_sync_api.rs`
  - reused the stable external Modelica projected reference page
    `Projectionica.Controllers.PI`
  - proved that the route reopens the requested Modelica projected page and
    keeps both `page_id` and `title` anchored to that stable target
  - synced the Wendao program note, surface inventory, Modelica package
    roadmap, and the active ExecPlan to the landed `M6` state
- **Validation Executed**:
  - `INSTA_UPDATE=always direnv exec . cargo test -p xiuxian-wendao gateway::studio::studio_repo_sync_api_tests::docs_page_endpoint_executes_over_external_modelica_plugin_path --lib`
  - `direnv exec . cargo test -p xiuxian-wendao --lib --no-run`
  - `direnv exec . git diff --check`

### [DONE] Feature: extend the `M6` external Modelica proof into the Studio docs retrieval-hit route

- **Objective**: keep pushing the same `M6` additive external-plugin proof
  across the non-planner Studio docs route family and prove that
  deterministic docs hit reopening survives the external plugin path.
- **Update**:
  - reused the existing Studio docs retrieval-hit route at
    `/api/docs/retrieval-hit` instead of adding a new gateway surface
  - added a builtin-registry external Modelica regression in
    `packages/rust/crates/xiuxian-wendao/tests/unit/studio_repo_sync_api.rs`
  - reused the stable external Modelica reference page
    `Projectionica.Controllers.PI` plus its `Anchors` page-index node
  - proved that the route reopens the requested node as a
    `page_index_node` hit while staying anchored to the requested Modelica
    projected page
  - synced the Wendao program note, surface inventory, Modelica package
    roadmap, and the active ExecPlan to the landed `M6` state
- **Validation Executed**:
  - `INSTA_UPDATE=always direnv exec . cargo test -p xiuxian-wendao gateway::studio::studio_repo_sync_api_tests::docs_retrieval_hit_endpoint_executes_over_external_modelica_plugin_path --lib`
  - `direnv exec . cargo test -p xiuxian-wendao --lib --no-run`
  - `direnv exec . git diff --check`

### [DONE] Feature: extend the `M6` external Modelica proof into the Studio docs retrieval-context route

- **Objective**: keep pushing the same `M6` additive external-plugin proof
  deeper into the non-planner Studio docs route family and prove that
  deterministic docs node-context reopening survives the external plugin
  path.
- **Update**:
  - reused the existing Studio docs retrieval-context route at
    `/api/docs/retrieval-context` instead of adding a new gateway surface
  - added a builtin-registry external Modelica regression in
    `packages/rust/crates/xiuxian-wendao/tests/unit/studio_repo_sync_api.rs`
  - reused the stable external Modelica reference page
    `Projectionica.Controllers.PI` plus its `Anchors` page-index node
  - proved that the route returns a node-context payload whose center stays
    anchored to the requested Modelica projected page
  - synced the Wendao program note, surface inventory, Modelica package
    roadmap, and the active ExecPlan to the landed `M6` state
- **Validation Executed**:
  - `INSTA_UPDATE=always direnv exec . cargo test -p xiuxian-wendao gateway::studio::studio_repo_sync_api_tests::docs_retrieval_context_endpoint_executes_over_external_modelica_plugin_path --lib`
  - `direnv exec . cargo test -p xiuxian-wendao --lib --no-run`
  - `direnv exec . git diff --check`

### [DONE] Feature: extend the `M6` external Modelica proof into the Studio docs retrieval route

- **Objective**: keep pushing the same `M6` additive external-plugin proof
  through the non-planner Studio docs route family and prove that mixed
  docs-facing retrieval survives the external plugin path.
- **Update**:
  - reused the existing Studio docs retrieval route at `/api/docs/retrieval`
    instead of adding a new gateway surface
  - added a builtin-registry external Modelica regression in
    `packages/rust/crates/xiuxian-wendao/tests/unit/studio_repo_sync_api.rs`
  - queried the stable external Modelica reference path
    `Projectionica.Controllers` with `kind=reference` and `limit=4`
  - proved that the route returns non-empty, limit-bounded mixed retrieval
    hits containing both `page` and `page_index_node` kinds, while staying
    anchored to the requested Modelica controller path
  - synced the Wendao program note, surface inventory, Modelica package
    roadmap, and the active ExecPlan to the landed `M6` state
- **Validation Executed**:
  - `INSTA_UPDATE=always direnv exec . cargo test -p xiuxian-wendao gateway::studio::studio_repo_sync_api_tests::docs_retrieval_endpoint_executes_over_external_modelica_plugin_path --lib`
  - `direnv exec . cargo test -p xiuxian-wendao --lib --no-run`
  - `direnv exec . git diff --check`

### [DONE] Feature: extend the `M6` external Modelica proof into the Studio docs search route

- **Objective**: keep pushing the same `M6` additive external-plugin proof
  beyond the Studio planner subtree and prove that a generic docs-facing
  gateway route also survives the external plugin path.
- **Update**:
  - reused the existing Studio docs search route at `/api/docs/search`
    instead of adding a new gateway surface
  - added a builtin-registry external Modelica regression in
    `packages/rust/crates/xiuxian-wendao/tests/unit/studio_repo_sync_api.rs`
  - queried the stable external Modelica reference path
    `Projectionica.Controllers` with `kind=reference` and `limit=3`
  - proved that the route returns non-empty, limit-bounded projected pages
    whose titles or page ids stay anchored to the requested Modelica
    controller path
  - synced the Wendao program note, surface inventory, Modelica package
    roadmap, and the active ExecPlan to the landed `M6` state
- **Validation Executed**:
  - `INSTA_UPDATE=always direnv exec . cargo test -p xiuxian-wendao gateway::studio::studio_repo_sync_api_tests::docs_search_endpoint_executes_over_external_modelica_plugin_path --lib`
  - `direnv exec . cargo test -p xiuxian-wendao --lib --no-run`
  - `direnv exec . git diff --check`

### [DONE] Feature: extend the `M6` external Modelica proof into the Studio docs planner-queue route

- **Objective**: keep pushing the same `M6` additive external-plugin proof
  across real gateway consumers and cover deterministic planner queue shaping
  at the Studio route layer.
- **Update**:
  - reused the existing Studio docs planner-queue route at
    `/api/docs/planner-queue` instead of adding a new gateway surface
  - added a builtin-registry external Modelica regression in
    `packages/rust/crates/xiuxian-wendao/tests/unit/studio_repo_sync_api.rs`
  - injected a local `Controllers/NoDocs.mo` fixture and filtered the route by
    `gap_kind=symbol_reference_without_documentation` and `page_kind=reference`
    so the external Modelica path deterministically selects the intended
    backlog lane
  - proved that the route returns non-empty grouped backlog output whose
    total-gap count matches grouped counts and whose preview gaps stay
    anchored to the injected `NoDocs` target
  - synced the Wendao program note, surface inventory, Modelica package
    roadmap, and the active ExecPlan to the landed `M6` state
- **Validation Executed**:
  - `INSTA_UPDATE=always direnv exec . cargo test -p xiuxian-wendao gateway::studio::studio_repo_sync_api_tests::docs_planner_queue_endpoint_executes_over_external_modelica_plugin_path --lib`
  - `direnv exec . cargo test -p xiuxian-wendao --lib --no-run`
  - `direnv exec . git diff --check`

### [DONE] Feature: extend the `M6` external Modelica proof into the Studio docs planner-rank route

- **Objective**: keep pushing the same `M6` additive external-plugin proof
  across real gateway consumers and cover deterministic planner ranking at
  the Studio route layer.
- **Update**:
  - reused the existing Studio docs planner-rank route at
    `/api/docs/planner-rank` instead of adding a new gateway surface
  - added a builtin-registry external Modelica regression in
    `packages/rust/crates/xiuxian-wendao/tests/unit/studio_repo_sync_api.rs`
  - injected a local `Controllers/NoDocs.mo` fixture and filtered the route by
    `gap_kind=symbol_reference_without_documentation` and `page_kind=reference`
    so the external Modelica path deterministically selects the intended
    ranked gap set
  - proved that the route returns non-empty, limit-bounded ranked hits whose
    reasons remain populated and whose ordering stays deterministic
  - synced the Wendao program note, surface inventory, Modelica package
    roadmap, and the active ExecPlan to the landed `M6` state
- **Validation Executed**:
  - `INSTA_UPDATE=always direnv exec . cargo test -p xiuxian-wendao gateway::studio::studio_repo_sync_api_tests::docs_planner_rank_endpoint_executes_over_external_modelica_plugin_path --lib`
  - `direnv exec . cargo test -p xiuxian-wendao --lib --no-run`
  - `direnv exec . git diff --check`

### [DONE] Feature: extend the `M6` external Modelica proof into the Studio docs planner-workset route

- **Objective**: keep pushing the same `M6` additive external-plugin proof
  across real gateway consumers and cover deterministic planner-workset
  shaping at the Studio route layer.
- **Update**:
  - reused the existing Studio docs planner-workset route at
    `/api/docs/planner-workset` instead of adding a new gateway surface
  - added a builtin-registry external Modelica regression in
    `packages/rust/crates/xiuxian-wendao/tests/unit/studio_repo_sync_api.rs`
  - injected a local `Controllers/NoDocs.mo` fixture and filtered the route by
    `gap_kind=symbol_reference_without_documentation` and `page_kind=reference`
    so the external Modelica path deterministically selects the intended
    planner workset item
  - proved that the route returns non-empty, limit-bounded items whose queue
    totals, grouped selected counts, and reopened items stay aligned
  - synced the Wendao program note, surface inventory, Modelica package
    roadmap, and the active ExecPlan to the landed `M6` state
- **Validation Executed**:
  - `INSTA_UPDATE=always direnv exec . cargo test -p xiuxian-wendao gateway::studio::studio_repo_sync_api_tests::docs_planner_workset_endpoint_executes_over_external_modelica_plugin_path --lib`
  - `direnv exec . cargo test -p xiuxian-wendao --lib --no-run`
  - `direnv exec . git diff --check`

### [DONE] Feature: extend the `M6` external Modelica proof into the Studio docs planner-item route

- **Objective**: keep pushing the same `M6` additive external-plugin proof
  across real gateway consumers and cover deterministic planner-gap reopening
  at the Studio route layer.
- **Update**:
  - reused the existing Studio docs planner-item route at
    `/api/docs/planner-item` instead of adding a new gateway surface
  - added a builtin-registry external Modelica regression in
    `packages/rust/crates/xiuxian-wendao/tests/unit/studio_repo_sync_api.rs`
  - injected a local `Controllers/NoDocs.mo` fixture so the external Modelica
    path deterministically emits one reopenable planner gap
  - sourced a stable `gap_id` through `docs_projected_gap_report_from_config(...)`
    and then proved that the route reopens that exact gap into aligned hit
    and navigation-center output
  - synced the Wendao program note, surface inventory, Modelica package
    roadmap, and the active ExecPlan to the landed `M6` state
- **Validation Executed**:
  - `INSTA_UPDATE=always direnv exec . cargo test -p xiuxian-wendao gateway::studio::studio_repo_sync_api_tests::docs_planner_item_endpoint_executes_over_external_modelica_plugin_path --lib`
  - `direnv exec . cargo test -p xiuxian-wendao --lib --no-run`
  - `direnv exec . git diff --check`

### [DONE] Feature: extend the `M6` external Modelica proof into the Studio docs planner-search route

- **Objective**: keep pushing the same `M6` additive external-plugin proof
  above analyzer-entry helpers and into a real gateway consumer without
  reopening host compatibility seams.
- **Update**:
  - reused the existing Studio docs planner-search route at
    `/api/docs/planner-search` instead of adding a new gateway surface
  - added a builtin-registry external Modelica regression in
    `packages/rust/crates/xiuxian-wendao/tests/unit/studio_repo_sync_api.rs`
  - injected a local `Controllers/NoDocs.mo` fixture so the external Modelica
    path deterministically emits a searchable planner gap for the route-level
    proof
  - proved that the route returns non-empty, limit-bounded planner hits whose
    gap payload stays anchored to the injected `NoDocs` target
  - repaired the shared lib-test projection fixture mount in
    `packages/rust/crates/xiuxian-wendao/src/analyzers/service/projection/tests.rs`
    by remounting `tests/support/repo_fixture.rs` next to
    `repo_intelligence.rs`, which keeps the shared support path compiling
    after the earlier test-topology cleanup
  - synced the Wendao program note, surface inventory, Modelica package
    roadmap, and the active ExecPlan to the landed `M6` state
- **Validation Executed**:
  - `INSTA_UPDATE=always direnv exec . cargo test -p xiuxian-wendao gateway::studio::studio_repo_sync_api_tests::docs_planner_search_endpoint_executes_over_external_modelica_plugin_path --lib`
  - `direnv exec . cargo test -p xiuxian-wendao analyzers::service::projection::tests::repo_projected_pages_wraps_projection_fixture --lib`
  - `direnv exec . cargo test -p xiuxian-wendao --lib --no-run`
  - `direnv exec . git diff --check`

### [DONE] Feature: remove retained Python command-decorator design in favor of CLI entrypoints

- **Objective**: align retained Python skills with the current CLI-first
  design and physically remove the leftover `tool_command` decorator surface
  instead of keeping registration metadata alive.
- **Update**:
  - removed `tool_command`, `get_command_metadata`, and
    `get_tool_annotations` from
    `packages/python/foundation/src/xiuxian_foundation/api/decorators.py`
  - deleted the local crawl4ai decorator shim at
    `skills/crawl4ai/scripts/utils.py`
  - converted active skill scripts in `skills/code`, `skills/git`,
    `skills/advanced_tools`, `skills/researcher`, `skills/crawl4ai`, and
    `skills/_template` to plain callable CLI entrypoints
  - rewrote decorator-centric tests into removal guards and direct helper or
    callable-surface assertions
- **Validation Executed**:
  - `direnv exec . uv run pytest /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/core/tests/units/skills/test_skill_script.py /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/tests/unit/api/test_command_metadata_surface_removal.py /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/tests/unit/api/test_foundation_decorators.py /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/tests/unit/utils/test_introspection.py /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/tests/unit/api/test_decorators.py /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/tests/unit/rag/test_handlers.py /Users/guangtao/projects/xiuxian-artisan-workshop/skills/code/tests/test_code_search.py -q`
  - `direnv exec . uv run pytest --import-mode=importlib /Users/guangtao/projects/xiuxian-artisan-workshop/skills/crawl4ai/tests/test_crawl4ai_skill.py -q`
  - `direnv exec . python -m compileall /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/src/xiuxian_foundation/api /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/core/src/xiuxian_core/runtime /Users/guangtao/projects/xiuxian-artisan-workshop/skills/code/scripts/search /Users/guangtao/projects/xiuxian-artisan-workshop/skills/crawl4ai/scripts /Users/guangtao/projects/xiuxian-artisan-workshop/skills/git/scripts /Users/guangtao/projects/xiuxian-artisan-workshop/skills/advanced_tools/scripts /Users/guangtao/projects/xiuxian-artisan-workshop/skills/researcher/scripts /Users/guangtao/projects/xiuxian-artisan-workshop/skills/_template/scripts`
  - `direnv exec . git diff --check`

### [DONE] Feature: rename retained Python isolation runner from `skill_*` to CLI-neutral `script_*`

- **Objective**: remove the next obvious post-decorator legacy seam by
  renaming retained Python isolated-runner APIs away from `skill_*`
  terminology.
- **Update**:
  - renamed the legacy isolated script runner to the CLI-neutral
    `run_script_command` surface during the Python runtime flattening
  - updated retained exports in `runtime/__init__.py` and top-level lazy
    loading in `xiuxian_foundation/__init__.py`
  - updated crawl4ai callers, tests, and active reference docs to the new
    CLI-neutral runner naming
- **Validation Executed**:
  - `direnv exec . uv run pytest -n0 /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/tests/unit/runtime/test_isolation.py /Users/guangtao/projects/xiuxian-artisan-workshop/skills/crawl4ai/tests/test_crawl4ai_skill.py --import-mode=importlib -q`
  - `direnv exec . python -m compileall /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/src/xiuxian_foundation/runtime /Users/guangtao/projects/xiuxian-artisan-workshop/skills/crawl4ai/scripts`
  - `direnv exec . git diff --check`

### [DONE] Feature: extend the `M6` external Modelica proof into docs-facing deterministic planner search

- **Objective**: keep pushing the same `M6` additive external-plugin proof
  across docs-facing planner-search consumers instead of reopening
  compatibility or host-owned package seams.
- **Update**:
  - reused the existing docs-facing planner-search seam centered on
    `DocsPlannerSearchQuery` and `docs_planner_search_from_config(...)`
  - reintroduced a builtin-registry external Modelica regression in
    `tests/integration/docs_planner_search.rs`
  - mounted the new proof in `tests/xiuxian-testing-gate.rs` so it stays on
    the same integration harness as the rest of the additive package-path
    proofs
  - added a local no-documentation Modelica model fixture inside the test so
    the external plugin path deterministically emits a searchable planner gap
  - proved that `docs_planner_search_from_config(...)` returns deterministic
    planner hits in stable score order over the external Modelica plugin path
  - synced the Wendao program note, surface inventory, Modelica package
    roadmap, and the active ExecPlan to the landed `M6` state
- **Validation Executed**:
  - `INSTA_UPDATE=always direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate docs_planner_search::modelica_plugin_docs_planner_search_executes_over_external_plugin_path --features modelica -- --exact`
  - `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate --features modelica --no-run`
  - `direnv exec . git diff --check`

### [DONE] Feature: extend the `M6` external Modelica proof into docs-facing deterministic planner item reopening

- **Objective**: keep pushing the same `M6` additive external-plugin proof
  across docs-facing planner-item consumers instead of reopening
  compatibility or host-owned package seams.
- **Update**:
  - reused the existing docs-facing planner-item seam centered on
    `DocsPlannerItemQuery` and `docs_planner_item_from_config(...)`
  - added a builtin-registry external Modelica regression in
    `tests/integration/docs_planner_item.rs`
  - mounted the new proof in `tests/xiuxian-testing-gate.rs` so it stays on
    the same integration harness as the rest of the additive package-path
    proofs
  - added a local no-documentation Modelica model fixture inside the test so
    the external plugin path deterministically emits one reopenable gap
  - proved that `docs_planner_item_from_config(...)` reopens a stable gap
    into aligned retrieval-hit and navigation-center output over the external
    Modelica plugin path
  - synced the Wendao program note, surface inventory, Modelica package
    roadmap, and the active ExecPlan to the landed `M6` state
- **Validation Executed**:
  - `INSTA_UPDATE=always direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate docs_planner_item::modelica_plugin_docs_planner_item_executes_over_external_plugin_path --features modelica -- --exact`
  - `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate --features modelica --no-run`
  - `direnv exec . git diff --check`

### [DONE] Feature: extend the `M6` external Modelica proof into docs-facing deterministic planner ranking

- **Objective**: keep pushing the same `M6` additive external-plugin proof
  across docs-facing planner-rank consumers instead of reopening
  compatibility or host-owned package seams.
- **Update**:
  - reused the existing docs-facing planner-rank seam centered on
    `DocsPlannerRankQuery` and `docs_planner_rank_from_config(...)`
  - added a builtin-registry external Modelica regression in
    `tests/integration/docs_planner_rank.rs`
  - mounted the new proof in `tests/xiuxian-testing-gate.rs` so it stays on
    the same integration harness as the rest of the additive package-path
    proofs
  - proved that `docs_planner_rank_from_config(...)` executes
    deterministically over the external Modelica plugin path and preserves
    stable ranking order, score explanations, and hit-limit truncation
  - synced the Wendao program note, surface inventory, Modelica package
    roadmap, and the active ExecPlan to the landed `M6` state
- **Validation Executed**:
  - `INSTA_UPDATE=always direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate docs_planner_rank::modelica_plugin_docs_planner_rank_executes_over_external_plugin_path --features modelica -- --exact`
  - `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate --features modelica --no-run`
  - `direnv exec . git diff --check`

### [DONE] Feature: extend the `M6` external Modelica proof into docs-facing deterministic planner workset shaping

- **Objective**: keep pushing the same `M6` additive external-plugin proof
  across docs-facing planner-workset consumers instead of reopening
  compatibility or host-owned package seams.
- **Update**:
  - reused the existing docs-facing planner-workset seam centered on
    `DocsPlannerWorksetQuery` and `docs_planner_workset_from_config(...)`
  - added a builtin-registry external Modelica regression in
    `tests/integration/docs_planner_workset.rs`
  - mounted the new proof in `tests/xiuxian-testing-gate.rs` so it stays on
    the same integration harness as the rest of the additive package-path
    proofs
  - proved that `docs_planner_workset_from_config(...)` executes
    deterministically over the external Modelica plugin path and keeps queue,
    ranked-hit, grouped-lane, and opened-item counts aligned
  - synced the Wendao program note, surface inventory, Modelica package
    roadmap, and the active ExecPlan to the landed `M6` state
- **Validation Executed**:
  - `INSTA_UPDATE=always direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate docs_planner_workset::modelica_plugin_docs_planner_workset_executes_over_external_plugin_path --features modelica -- --exact`
  - `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate --features modelica --no-run`
  - `direnv exec . git diff --check`

### [DONE] Feature: extend the `M6` external Modelica proof into docs-facing deterministic planner queue shaping

- **Objective**: keep pushing the same `M6` additive external-plugin proof
  across docs-facing planner-queue consumers instead of reopening
  compatibility or host-owned package seams.
- **Update**:
  - reused the existing thin docs-facing planner-queue wrapper seam centered
    on `DocsPlannerQueueQuery` and `docs_planner_queue_from_config(...)`
  - added a builtin-registry external Modelica regression in
    `tests/integration/docs_planner_queue.rs`
  - mounted the new proof in `tests/xiuxian-testing-gate.rs` so it stays on
    the same integration harness as the rest of the additive package-path
    proofs
  - proved that `docs_planner_queue_from_config(...)` executes
    deterministically over the external Modelica plugin path and preserves
    queue-group count shaping under `per_kind_limit`
  - synced the Wendao program note, surface inventory, Modelica package
    roadmap, and the active ExecPlan to the landed `M6` state
- **Validation Executed**:
  - `INSTA_UPDATE=always direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate docs_planner_queue::modelica_plugin_docs_planner_queue_executes_over_external_plugin_path --features modelica -- --exact`
  - `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate --features modelica --no-run`
  - `direnv exec . git diff --check`

### [DONE] Feature: extend the `M6` external Modelica proof into docs-facing projected gap reporting

- **Objective**: keep pushing the same `M6` additive external-plugin proof
  across docs-facing projected-gap-report consumers instead of reopening
  compatibility or host-owned package seams.
- **Update**:
  - reused the existing thin docs-facing projected-gap wrapper seam centered
    on `DocsProjectedGapReportQuery` and
    `docs_projected_gap_report_from_config(...)`
  - replaced the blocked planner-search detour with a builtin-registry
    external Modelica regression in
    `tests/integration/docs_projected_gap_report.rs`
  - mounted the new proof in `tests/xiuxian-testing-gate.rs` so it stays on
    the same integration harness as the rest of the additive package-path
    proofs
  - proved that `docs_projected_gap_report_from_config(...)` executes
    deterministically over the external Modelica plugin path and preserves
    summary-to-gap-count consistency
  - synced the Wendao program note, surface inventory, Modelica package
    roadmap, and the active ExecPlan to the landed `M6` state
- **Validation Executed**:
  - `INSTA_UPDATE=always direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate docs_projected_gap_report::modelica_plugin_docs_projected_gap_report_executes_over_external_plugin_path --features modelica -- --exact`
  - `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate --features modelica --no-run`
  - `direnv exec . git diff --check`

### [DONE] Feature: extend the `M6` external Modelica proof into docs-facing deterministic projected retrieval-hit reopening

- **Objective**: keep pushing the same `M6` additive external-plugin proof
  across docs-facing retrieval-hit consumers instead of reopening
  compatibility or host-owned package seams.
- **Update**:
  - reused the existing thin docs-facing retrieval-hit wrapper seam centered
    on `DocsRetrievalHitQuery` and `docs_retrieval_hit_from_config(...)`
  - added a builtin-registry external Modelica regression in
    `tests/integration/docs_retrieval_hit.rs`
  - mounted the new proof in `tests/xiuxian-testing-gate.rs` so it stays on
    the same integration harness as the rest of the additive package-path
    proofs
  - proved that `docs_retrieval_hit_from_config(...)` can reopen a
    deterministic docs-facing projected retrieval hit over the external
    Modelica plugin path
  - synced the Wendao program note, surface inventory, Modelica package
    roadmap, and the active ExecPlan to the landed `M6` state
- **Validation Executed**:
  - `INSTA_UPDATE=always direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate docs_retrieval_hit::modelica_plugin_docs_retrieval_hit_resolves_node_hit --features modelica -- --exact`
  - `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate --features modelica --no-run`
  - `direnv exec . git diff --check`

### [DONE] Feature: extend the `M6` external Modelica proof into docs-facing local projected retrieval context

- **Objective**: keep pushing the same `M6` additive external-plugin proof
  across docs-facing local-retrieval-context consumers instead of reopening
  compatibility or host-owned package seams.
- **Update**:
  - reused the existing thin docs-facing retrieval-context wrapper seam
    centered on `DocsRetrievalContextQuery` and
    `docs_retrieval_context_from_config(...)`
  - added a builtin-registry external Modelica regression in
    `tests/integration/docs_retrieval_context.rs`
  - mounted the new proof in `tests/xiuxian-testing-gate.rs` so it stays on
    the same integration harness as the rest of the additive package-path
    proofs
  - proved that `docs_retrieval_context_from_config(...)` can resolve
    deterministic docs-facing local projected retrieval context over the
    external Modelica plugin path
  - synced the Wendao program note, surface inventory, Modelica package
    roadmap, and the active ExecPlan to the landed `M6` state
- **Validation Executed**:
  - `INSTA_UPDATE=always direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate docs_retrieval_context::modelica_plugin_docs_retrieval_context_resolves_node_context --features modelica -- --exact`
  - `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate --features modelica --no-run`
  - `direnv exec . git diff --check`

### [DONE] Feature: extend the `M6` external Modelica proof into docs-facing mixed projected retrieval

- **Objective**: keep pushing the same `M6` additive external-plugin proof
  across docs-facing mixed-retrieval consumers instead of reopening
  compatibility or host-owned package seams.
- **Update**:
  - reused the existing thin docs-facing retrieval wrapper seam centered on
    `DocsRetrievalQuery` and `docs_retrieval_from_config(...)`
  - added a builtin-registry external Modelica regression in
    `tests/integration/docs_retrieval.rs`
  - mounted the new proof in `tests/xiuxian-testing-gate.rs` so it stays on
    the same integration harness as the rest of the additive package-path
    proofs
  - proved that `docs_retrieval_from_config(...)` can resolve deterministic
    docs-facing mixed projected retrieval hits over the external Modelica
    plugin path
  - synced the Wendao program note, surface inventory, Modelica package
    roadmap, and the active ExecPlan to the landed `M6` state
- **Validation Executed**:
  - `INSTA_UPDATE=always direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate docs_retrieval::modelica_plugin_docs_retrieval_merges_page_and_node_hits --features modelica -- --exact`
  - `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate --features modelica --no-run`
  - `direnv exec . git diff --check`

### [DONE] Feature: extend the `M6` external Modelica proof into docs-facing projected page search

- **Objective**: keep pushing the same `M6` additive external-plugin proof
  across docs-facing search consumers instead of reopening compatibility or
  host-owned package seams.
- **Update**:
  - reused the existing thin docs-facing search wrapper seam centered on
    `DocsSearchQuery` and `docs_search_from_config(...)`
  - added a builtin-registry external Modelica regression in
    `tests/integration/docs_search.rs`
  - mounted the new proof in `tests/xiuxian-testing-gate.rs` so it stays on
    the same integration harness as the rest of the additive package-path
    proofs
  - proved that `docs_search_from_config(...)` can resolve deterministic
    docs-facing projected page search hits over the external Modelica plugin
    path
  - synced the Wendao program note, surface inventory, Modelica package
    roadmap, and the active ExecPlan to the landed `M6` state
- **Validation Executed**:
  - `INSTA_UPDATE=always direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate docs_search::modelica_plugin_docs_search_matches_reference_pages --features modelica -- --exact`
  - `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate --features modelica --no-run`
  - `direnv exec . git diff --check`

### [DONE] Feature: extend the `M6` external Modelica proof into docs-facing projected markdown documents

- **Objective**: keep pushing the same `M6` additive external-plugin proof
  across docs-facing rendered-document consumers instead of reopening
  compatibility or host-owned package seams.
- **Update**:
  - added a thin docs-facing wrapper seam for projected markdown documents,
    centered on `DocsMarkdownDocumentsQuery` and
    `docs_markdown_documents_from_config(...)`
  - added a builtin-registry external Modelica regression in
    `tests/integration/docs_markdown_documents.rs`
  - mounted the new proof in `tests/xiuxian-testing-gate.rs` so it stays on
    the same integration harness as the rest of the additive package-path
    proofs
  - proved that `docs_markdown_documents_from_config(...)` can resolve
    deterministic docs-facing projected markdown documents over the external
    Modelica plugin path
  - synced the Wendao program note, surface inventory, Modelica package
    roadmap, and the active ExecPlan to the landed `M6` state
- **Validation Executed**:
  - `INSTA_UPDATE=always direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate docs_markdown_documents::modelica_plugin_docs_markdown_documents_resolve_projected_markdown_documents --features modelica -- --exact`
  - `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate --features modelica --no-run`
  - `direnv exec . git diff --check`

### [DONE] Feature: extend the `M6` external Modelica proof into docs-facing projected page-index documents

- **Objective**: keep pushing the same `M6` additive external-plugin proof
  across docs-facing parsed-document consumers instead of reopening
  compatibility or host-owned package seams.
- **Update**:
  - added a thin docs-facing wrapper seam for projected page-index documents,
    centered on `DocsPageIndexDocumentsQuery` and
    `docs_page_index_documents_from_config(...)`
  - added a builtin-registry external Modelica regression in
    `tests/integration/docs_page_index_documents.rs`
  - mounted the new proof in `tests/xiuxian-testing-gate.rs` so it stays on
    the same integration harness as the rest of the additive package-path
    proofs
  - proved that `docs_page_index_documents_from_config(...)` can resolve
    deterministic docs-facing parsed page-index-ready documents over the
    external Modelica plugin path
  - synced the Wendao program note, surface inventory, Modelica package
    roadmap, and the active ExecPlan to the landed `M6` state
- **Validation Executed**:
  - `INSTA_UPDATE=always direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate docs_page_index_documents::modelica_plugin_docs_page_index_documents_resolve_parsed_projection_documents --features modelica -- --exact`
  - `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate --features modelica --no-run`
  - `direnv exec . git diff --check`

### [DONE] Feature: extend the `M6` external Modelica proof into docs-facing projected page-index trees

- **Objective**: keep pushing the same `M6` additive external-plugin proof
  across docs-facing parsed-hierarchy listing consumers instead of reopening
  compatibility or host-owned package seams.
- **Update**:
  - added a thin docs-facing wrapper seam for projected page-index tree
    listing, centered on `DocsPageIndexTreesQuery` and
    `docs_page_index_trees_from_config(...)`
  - added a builtin-registry external Modelica regression in
    `tests/integration/docs_page_index_trees.rs`
  - mounted the new proof in `tests/xiuxian-testing-gate.rs` so it stays on
    the same integration harness as the rest of the additive package-path
    proofs
  - proved that `docs_page_index_trees_from_config(...)` can resolve a
    deterministic docs-facing parsed page hierarchy listing over the external
    Modelica plugin path
  - synced the Wendao program note, surface inventory, Modelica package
    roadmap, and the active ExecPlan to the landed `M6` state
- **Validation Executed**:
  - `INSTA_UPDATE=always direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate docs_page_index_trees::modelica_plugin_docs_page_index_trees_lookup_resolves_deterministic_tree_set --features modelica -- --exact`
  - `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate --features modelica --no-run`
  - `direnv exec . git diff --check`

### [DONE] Feature: extend the `M6` external Modelica proof into docs-facing projected page-index tree search

- **Objective**: keep pushing the same `M6` additive external-plugin proof
  across docs-facing parsed-hierarchy search consumers instead of reopening
  compatibility or host-owned package seams.
- **Update**:
  - added a thin docs-facing wrapper seam for projected page-index tree
    search, centered on `DocsPageIndexTreeSearchQuery` and
    `docs_page_index_tree_search_from_config(...)`
  - added a builtin-registry external Modelica regression in
    `tests/integration/docs_page_index_tree_search.rs`
  - mounted the new proof in `tests/xiuxian-testing-gate.rs` so it stays on
    the same integration harness as the rest of the additive package-path
    proofs
  - proved that `docs_page_index_tree_search_from_config(...)` can resolve
    deterministic docs-facing parsed page hierarchy search hits over the
    external Modelica plugin path
  - synced the Wendao program note, surface inventory, Modelica package
    roadmap, and the active ExecPlan to the landed `M6` state
- **Validation Executed**:
  - `INSTA_UPDATE=always direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate docs_page_index_tree_search::modelica_plugin_docs_page_index_tree_search_matches_section_hits --features modelica -- --exact`
  - `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate --features modelica --no-run`
  - `direnv exec . git diff --check`

### [DONE] Feature: extend the `M6` external Modelica proof into docs-facing projected page-index node lookup

- **Objective**: keep pushing the same `M6` additive external-plugin proof
  across docs-facing deterministic parsed-section consumers instead of
  reopening compatibility or host-owned package seams.
- **Update**:
  - added a thin docs-facing wrapper seam for projected page-index node
    lookup, centered on `DocsPageIndexNodeQuery` and
    `docs_page_index_node_from_config(...)`
  - added a builtin-registry external Modelica regression in
    `tests/integration/docs_page_index_node.rs`
  - mounted the new proof in `tests/xiuxian-testing-gate.rs` so it stays on
    the same integration harness as the rest of the additive package-path
    proofs
  - proved that `docs_page_index_node_from_config(...)` can resolve a
    deterministic docs-facing parsed section over a stable external Modelica
    symbol page
  - synced the Wendao program note, surface inventory, Modelica package
    roadmap, and the active ExecPlan to the landed `M6` state
- **Validation Executed**:
  - `INSTA_UPDATE=always direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate docs_page_index_node::modelica_plugin_docs_page_index_node_lookup_resolves_one_stable_node --features modelica -- --exact`
  - `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate --features modelica --no-run`
  - `direnv exec . git diff --check`

### [DONE] Maintenance: cut active how-to and config residuals to final tool-runtime wording

- **Objective**: keep pushing the same repo-wide legacy-protocol removal by
  rewriting still-active guidance and sample config surfaces instead of
  leaving protocol-era wording in live operator entrypoints.
- **Update**:
  - rewrote `assets/how-to/agent-native-development.md`,
    `assets/how-to/rag-usage.md`, `assets/how-to/testing-workflows.md`, and
    `assets/how-to/gitops.md` so they no longer present removed protocol
    server lanes or deleted Python package paths as current guidance
  - updated `packages/conf/skills.yaml` comments to final tool-runtime wording
  - rewrote the safe comment-only slice in `nix/modules/claude.nix` to stop
    describing the local helper as a legacy protocol server where the actual
    external config keys must stay untouched
- **Validation Executed**:
  - targeted legacy-term scan over the touched how-to and config files
  - `direnv exec . git diff --check`

### [DONE] Maintenance: cut active knowledge and skills guide residuals

- **Objective**: keep removing stale live guidance by rewriting retained
  knowledge and skill-directory docs that still pointed at deleted Python
  package paths or old protocol-server examples.
- **Update**:
  - updated `assets/knowledge/uv-best-practices.md` to use retained
    `xiuxian_foundation` imports and removed the obsolete protocol dependency
    example
  - rewrote `skills/README.md` so it no longer presents the removed Python
    protocol server as the active skill runtime architecture
  - rewrote `assets/how-to/documentation-workflow.md` to current
    `assets/knowledge/`, `assets/how-to/`, `skills/*`, and package-doc sync
    ownership instead of deleted `agent/*` paths
- **Validation Executed**:
  - targeted legacy-path and legacy-term scan over the touched files
  - `direnv exec . git diff --check`

### [DONE] Maintenance: cut retained skill namespace and tool-runtime residuals

- **Objective**: keep pushing the active-surface cleanup through retained
  `skills/*` docs, scripts, and tests so the skill layer stops depending on
  deleted `omni.*` Python namespaces and old protocol wording.
- **Update**:
  - rewrote `skills/git/*` and `skills/crawl4ai/*` docs to final tool-runtime
    wording
  - migrated retained `skills/git` and `skills/crawl4ai` Python imports from
    deleted `omni.*` modules to `xiuxian_foundation` / `xiuxian_rag`
  - converted stale loader/discovery assertions in
    `skills/crawl4ai/tests/test_crawl4ai_skill.py` to current retained-surface
    checks
  - fixed `skills/git` smart-commit script/test imports so the retained skill
    test path no longer depends on removed `get_git_toplevel` or package-name
    collisions with GitPython
- **Validation Executed**:
  - `direnv exec . rg -n 'from omni\\.|import omni\\.|\\bmcp\\b|MCP|mcp_' skills/git skills/crawl4ai -g '*.py' -g '*.md'`
  - `direnv exec . uv run pytest skills/crawl4ai/tests/test_crawl4ai_skill.py -q`
  - `direnv exec . uv run pytest skills/git/tests/test_smart_workflow_modular.py -q`
  - `direnv exec . python -m py_compile skills/crawl4ai/scripts/crawl_url.py skills/git/scripts/smart_commit_graphflow/commands.py skills/git/scripts/rendering.py skills/git/scripts/prepare.py skills/git/scripts/commit.py`
  - `direnv exec . git diff --check`

### [DONE] Maintenance: close the retained git extension tail

- **Objective**: finish the remaining `skills/git` active-surface cleanup by
  removing the last deleted `omni.*` namespace references from the git
  extension tail and replacing stale SDK-era tests with current boundary
  checks.
- **Update**:
  - rewrote `skills/git/extensions/rust_bridge/*` to retained
    `xiuxian` wording and a graceful no-op Rust-binding fallback, since the
    historical Python Rust sniffer bridge no longer exists
  - converted `skills/git/tests/test_git_modular.py` from deleted
    `xiuxian_skill` decorator coverage to current extension-surface and
    graceful-fallback assertions
- **Validation Executed**:
  - `direnv exec . rg -n 'from omni\\.|import omni\\.|\\bmcp\\b|MCP|mcp_' skills/git/extensions skills/git/tests/test_git_modular.py -g '*.py' -g '*.md'`
  - `direnv exec . uv run pytest skills/git/tests/test_git_modular.py -q`
  - `direnv exec . python -m py_compile skills/git/extensions/rust_bridge/bindings.py skills/git/extensions/rust_bridge/__init__.py skills/git/tests/test_git_modular.py`
  - `direnv exec . git diff --check`

### [DONE] Feature: extend the `M6` external Modelica proof into docs-facing projected page-index tree lookup

- **Objective**: keep pushing the same `M6` additive external-plugin proof
  across docs-facing deterministic parsed-hierarchy consumers instead of
  reopening compatibility or host-owned package seams.
- **Update**:
  - added a thin docs-facing wrapper seam for projected page-index tree
    lookup, centered on `DocsPageIndexTreeQuery` and
    `docs_page_index_tree_from_config(...)`
  - added a builtin-registry external Modelica regression in
    `tests/integration/docs_page_index_tree.rs`
  - mounted the new proof in `tests/xiuxian-testing-gate.rs` so it stays on
    the same integration harness as the rest of the additive package-path
    proofs
  - proved that `docs_page_index_tree_from_config(...)` can resolve a
    deterministic docs-facing parsed page hierarchy over a stable external
    Modelica symbol page
  - synced the Wendao program note, surface inventory, Modelica package
    roadmap, and the active ExecPlan to the landed `M6` state
- **Validation Executed**:
  - `INSTA_UPDATE=always direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate docs_page_index_tree::modelica_plugin_docs_page_index_tree_lookup_resolves_one_stable_tree --features modelica -- --exact`
  - `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate --features modelica --no-run`
  - `direnv exec . git diff --check`

### [DONE] Feature: tighten struct-backed `ToList{UInt8}` write loops

- **Objective**: shave remaining string-field write overhead inside
  struct-backed list children without changing missing-field semantics or
  touching `ToStruct` / `ToList` storage semantics.
- **Update**:
  - moved a `ToList{UInt8}` specialization into
    `.data/arrow-julia/src/arraytypes/struct.jl` so struct-backed string
    children can walk `ToStruct.data` directly during writes while still using
    `_structfield(...)` for missing-safe semantics
  - added an offset struct-string roundtrip regression in
    `.data/arrow-julia/test/runtests.jl` to lock the missing-row behavior for
    this path
  - tightened the inner loop further by materializing one contiguous `UInt8`
    buffer for offset-zero struct-backed children before a single write
  - measured the targeted `ToList(ToStruct(...))` write probe at
    `0.079 ms / 0.512 MiB`
  - replayed the full write-path harness and verified `offset_map` improved
    from `3.046 ms / 13.139 MiB` to `2.263 ms / 13.342 MiB`
- **Validation Executed**:
  - targeted struct-backed `ToList` write probe via
    `direnv exec . julia --project=.data/arrow-julia/test -e 'using Arrow, OffsetArrays, Statistics; ... Arrow.writearray(io, UInt8, flat) ...'`
  - `direnv exec . julia --project=.data/arrow-julia/test -e 'using Pkg; Pkg.instantiate(); Pkg.test("Arrow")'`
  - `direnv exec . git -C .data/arrow-julia diff --check`

### [DONE] Feature: tighten `ToList` build overhead for offset-backed wrappers

- **Objective**: keep pushing Arrow.jl write-path performance on shared
  list/string/binary paths without changing `ToList` conversion semantics or
  broadening the optimization beyond non-converting `ToArrow` wrappers.
- **Update**:
  - refactored `ToList(...)` in `.data/arrow-julia/src/arraytypes/list.jl`
    around a shared builder and added a non-converting `ArrowTypes.ToArrow`
    fast path that walks the underlying `data + offset` directly
  - kept converting `Any` wrappers on the old generic path so element
    conversion semantics stay unchanged
  - added offset binary/string eltype regression checks in
    `.data/arrow-julia/test/runtests.jl` to lock the branch boundary
  - replayed the full write-path harness and confirmed concrete gains on
    shared `ToList` consumers: `offset_binary` improved from
    `2.030 ms / 2.085 MiB` to `1.962 ms / 2.085 MiB`, and
    `offset_nested_list` improved from `2.136 ms / 16.004 MiB` to
    `2.036 ms / 16.003 MiB`
- **Validation Executed**:
  - targeted `ToList(::ToArrow)` probe via
    `direnv exec . julia --project=.data/arrow-julia/test -e 'using Arrow, OffsetArrays, Statistics; ... Arrow.ToList(x) ...'`
  - `direnv exec . julia --project=.data/arrow-julia/test -e 'using Pkg; Pkg.instantiate(); include(joinpath(pwd(), ".cache/agent/benchmarks/arrow_julia_writepaths_bench.jl"))'`
  - `direnv exec . julia --project=.data/arrow-julia/test -e 'using Pkg; Pkg.instantiate(); Pkg.test("Arrow")'`
  - `direnv exec . git -C .data/arrow-julia diff --check`

### [DONE] Feature: add chunked bitstype `ToList{T}` writes

- **Objective**: remove the remaining flattened-wrapper write fallback from
  nested primitive lists without changing string/binary `ToList{UInt8}` or
  missing inner-list semantics.
- **Update**:
  - added a chunked `writearray(...)` fast path in
    `.data/arrow-julia/src/arraytypes/list.jl` for non-string bitstype
    `ToList{T}` values, so nested primitive lists now stream each inner chunk
    through existing `writearray(...)` specializations instead of the generic
    per-element fallback
  - collapsed the `writearray(...)` entrypoint into one non-ambiguous method
    after the first draft conflicted with the retained `ToList{UInt8}` binary
    path
  - widened the offset nested-list roundtrip assertions in
    `.data/arrow-julia/test/runtests.jl` to lock the public contract
    (`AbstractVector{Int64}`) instead of overfitting to one concrete storage
    type
  - replayed the official write-path harness and verified
    `offset_nested_list` improved from `2.136 ms / 16.003 MiB` to
    `0.788 ms / 6.986 MiB`; `offset_binary` also improved from
    `1.892 ms / 2.085 MiB` to `1.788 ms / 2.085 MiB`
- **Validation Executed**:
  - `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_writepaths_bench.jl`
  - `direnv exec . julia --project=.data/arrow-julia/test -e 'using Pkg; Pkg.instantiate(); Pkg.test("Arrow")'`
  - `direnv exec . git -C .data/arrow-julia diff --check`

### [DONE] Research: reject buffered `ToList{UInt8}` writes

- **Objective**: test whether aggregating small `ToList{UInt8}` payloads into
  one buffered write could beat the retained chunk-write path on the actual
  end-to-end harness.
- **Update**:
  - added a buffered small-payload `ToList{UInt8}` write path in
    `.data/arrow-julia/src/arraytypes/list.jl` and replayed the official
    write-path harness instead of trusting local intuition
  - rejected the optimization immediately after the official harness regressed:
    `offset_string` moved from `1.742 ms / 2.085 MiB` to
    `2.024 ms / 2.414 MiB`, and `offset_binary` moved from
    `1.788 ms / 2.085 MiB` to `2.743 ms / 2.414 MiB`
  - reverted the code path, but kept the new offset append regression coverage
    in `.data/arrow-julia/test/runtests.jl` after fixing the test to use the
    correct stream split point
  - re-ran the full package tests and confirmed the stable production path at
    `Arrow | 66107/66107 | 3m15.6s`
- **Validation Executed**:
  - `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_writepaths_bench.jl`
  - `direnv exec . julia --project=.data/arrow-julia/test -e 'using Pkg; Pkg.instantiate(); Pkg.test("Arrow")'`
  - `direnv exec . git -C .data/arrow-julia diff --check`

### [DONE] Research: reject split `MapKind` offset-width paths

- **Objective**: test whether separating the common `Int32` map-offset path
  from the rare `Int64` overflow path would improve `offset_map` on the actual
  production harness.
- **Update**:
  - profiled the end-to-end `offset_map` case and confirmed the hotspot still
    sits inside `mapoffsetsandvalues(...)`; `code_warntype` also showed the
    current common path returning
    `Tuple{Union{Vector{Int32},Vector{Int64}},Vector{KeyValue}}`
  - split the common and rare offset-width paths in
    `.data/arrow-julia/src/arraytypes/map.jl`, then validated against the
    official write-path harness instead of local intuition
  - rejected the change immediately after the official harness regressed
    `offset_map` from `1.996 ms / 13.342 MiB` to `3.35 ms / 17.17 MiB`
  - restored the stable map implementation, but kept the new offset map append
    regression in `.data/arrow-julia/test/runtests.jl`
  - re-ran the full package tests and confirmed the stable production path at
    `Arrow | 66108/66108 | 3m05.8s`
- **Validation Executed**:
  - `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_writepaths_bench.jl`
  - `direnv exec . julia --project=.data/arrow-julia/test -e 'using Pkg; Pkg.instantiate(); Pkg.test("Arrow")'`
  - `direnv exec . git -C .data/arrow-julia diff --check`

### [DONE] Research: reject single-pass `MapKind` flatten

- **Objective**: test whether collapsing `mapoffsetsandvalues(...)` into a
  single `push!`-driven pass could beat the current two-pass `MapKind`
  builder.
- **Update**:
  - replaced the two-pass `mapoffsetsandvalues(...)` loops in
    `.data/arrow-julia/src/arraytypes/map.jl` with a single-pass builder that
    appended entries while growing offsets
  - replayed the official write-path harness and observed a catastrophic
    regression: `offset_map` ballooned from `2.208 ms / 13.342 MiB` to
    `21977.023 ms / 220724.888 MiB`
  - reverted the experiment immediately, kept the added empty-dict regression
    in `.data/arrow-julia/test/runtests.jl`, and confirmed the stable map
    baseline returned at `2.146 ms / 13.342 MiB`
- **Validation Executed**:
  - `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_writepaths_bench.jl`
  - `direnv exec . git -C .data/arrow-julia diff --check`

### [DONE] Feature: tighten `MapKind` `ToArrow` wrapper write-path overhead

- **Objective**: keep pushing the Arrow.jl write-path work on map columns
  without changing the stable `Vector{KeyValue}` storage shape or broadening
  the optimization beyond `MapKind`.
- **Update**:
  - specialized `mapoffsetsandvalues(...)` for `ArrowTypes.ToArrow` so
    offset-backed map columns walk `data + offset` directly instead of paying
    the generic wrapper iteration on both passes
  - added offset `Any`-typed and nullable map regressions in
    `.data/arrow-julia/test/runtests.jl` to lock conversion semantics and
    missing handling
  - measured and explicitly rejected the direct map child-array experiment
    after it regressed against the existing `Vector{KeyValue}` path
  - replayed the full write-path harness and verified `offset_map` improved
    from the restored `3.164 ms / 13.139 MiB` baseline to
    `3.046 ms / 13.139 MiB`
- **Validation Executed**:
  - targeted `ToArrow` map probe via
    `direnv exec . julia --project=.data/arrow-julia/test -e 'using Arrow, OffsetArrays, Statistics; ... Arrow.mapoffsetsandvalues(KT, x) ...'`
  - `direnv exec . julia --project=.data/arrow-julia/test -e 'using Pkg; Pkg.instantiate(); include(joinpath(pwd(), ".cache/agent/benchmarks/arrow_julia_writepaths_bench.jl"))'`
  - `direnv exec . julia --project=.data/arrow-julia/test -e 'using Pkg; Pkg.instantiate(); Pkg.test("Arrow")'`
  - `direnv exec . git -C .data/arrow-julia diff --check`

### [DONE] Feature: extend the `M6` external Modelica proof into docs-facing projected page lookup

- **Objective**: keep pushing the same `M6` additive external-plugin proof
  across docs-facing deterministic lookup consumers instead of reopening
  compatibility or host-owned package seams.
- **Update**:
  - added a builtin-registry external Modelica regression in
    `tests/integration/docs_page.rs`
  - mounted the new proof in `tests/xiuxian-testing-gate.rs` so it stays on
    the same integration harness as the rest of the additive package-path
    proofs
  - proved that `docs_page_from_config(...)` can resolve deterministic
    docs-facing single-page lookup over a stable external Modelica symbol page
  - synced the Wendao program note, surface inventory, Modelica package
    roadmap, and the active ExecPlan to the landed `M6` state
- **Validation Executed**:
  - `INSTA_UPDATE=always direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate docs_page::modelica_plugin_docs_page_lookup_resolves_one_stable_page --features modelica -- --exact`
  - `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate --features modelica --no-run`
  - `direnv exec . git diff --check`

### [DONE] Feature: extend the `M6` external Modelica proof into docs-facing projected page-family cluster lookup

- **Objective**: keep pushing the same `M6` additive external-plugin proof
  across docs-facing deterministic lookup consumers instead of reopening
  compatibility or host-owned package seams.
- **Update**:
  - added a builtin-registry external Modelica regression in
    `tests/integration/docs_family_cluster.rs`
  - mounted the new proof in `tests/xiuxian-testing-gate.rs` so it stays on
    the same integration harness as the rest of the additive package-path
    proofs
  - proved that `docs_family_cluster_from_config(...)` can resolve a
    deterministic docs-facing `HowTo` family cluster over a stable external
    Modelica reference page
  - synced the Wendao program note, surface inventory, Modelica package
    roadmap, and the active ExecPlan to the landed `M6` state
- **Validation Executed**:
  - `INSTA_UPDATE=always direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate docs_family_cluster::modelica_plugin_docs_family_cluster_resolves_how_to_cluster_for_reference_page --features modelica -- --exact`
  - `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate --features modelica --no-run`
  - `direnv exec . git diff --check`

### [DONE] Feature: tighten `MapKind` `ToArrow` wrapper write-path overhead

- **Objective**: keep pushing the Arrow.jl write-path work on map columns
  without changing the stable `Vector{KeyValue}` storage shape or broadening
  the optimization beyond `MapKind`.
- **Update**:
  - specialized `mapoffsetsandvalues(...)` for `ArrowTypes.ToArrow` so
    offset-backed map columns walk `data + offset` directly instead of paying
    the generic wrapper iteration on both passes
  - added offset `Any`-typed and nullable map regressions in
    `.data/arrow-julia/test/runtests.jl` to lock conversion semantics and
    missing handling
  - measured and explicitly rejected the direct map child-array experiment
    after it regressed against the existing `Vector{KeyValue}` path
  - replayed the full write-path harness and verified `offset_map` improved
    from the restored `3.164 ms / 13.139 MiB` baseline to
    `3.046 ms / 13.139 MiB`
- **Validation Executed**:
  - targeted `ToArrow` map probe via
    `direnv exec . julia --project=.data/arrow-julia/test -e 'using Arrow, OffsetArrays, Statistics; ... Arrow.mapoffsetsandvalues(KT, x) ...'`
  - `direnv exec . julia --project=.data/arrow-julia/test -e 'using Pkg; Pkg.instantiate(); include(joinpath(pwd(), ".cache/agent/benchmarks/arrow_julia_writepaths_bench.jl"))'`
  - `direnv exec . julia --project=.data/arrow-julia/test -e 'using Pkg; Pkg.instantiate(); Pkg.test("Arrow")'`
  - `direnv exec . git -C .data/arrow-julia diff --check`

### [DONE] Feature: extend the `M6` external Modelica proof into docs-facing projected page-navigation lookup

- **Objective**: keep pushing the same `M6` additive external-plugin proof
  across docs-facing deterministic lookup consumers instead of reopening
  compatibility or host-owned package seams.
- **Update**:
  - added a builtin-registry external Modelica regression in
    `tests/integration/docs_navigation.rs`
  - mounted the new proof in `tests/xiuxian-testing-gate.rs` so it stays on
    the same integration harness as the rest of the additive package-path
    proofs
  - proved that `docs_navigation_from_config(...)` can build deterministic
    docs-facing navigation lookup output, including stable node context and
    `HowTo` family clustering, over the external Modelica plugin path
  - synced the Wendao program note, surface inventory, Modelica package
    roadmap, and the active ExecPlan to the landed `M6` state
- **Validation Executed**:
  - `INSTA_UPDATE=always direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate docs_navigation::modelica_plugin_docs_navigation_bundle_resolves_tree_context_and_family_cluster --features modelica -- --exact`
  - `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate --features modelica --no-run`
  - `direnv exec . git diff --check`

### [DONE] Feature: extend the `M6` external Modelica proof into docs-facing projected page-family context

- **Objective**: keep pushing the same `M6` additive external-plugin proof
  across docs-facing lookup/context consumers instead of reopening
  compatibility or host-owned package seams.
- **Update**:
  - added a builtin-registry external Modelica regression in
    `tests/integration/docs_family_context.rs`
  - mounted the new proof in `tests/xiuxian-testing-gate.rs` so it stays on
    the same integration harness as the rest of the additive package-path
    proofs
  - proved that `docs_family_context_from_config(...)` can build
    deterministic docs-facing grouped family context over a stable external
    Modelica how-to page
  - synced the Wendao program note, surface inventory, Modelica package
    roadmap, and the active ExecPlan to the landed `M6` state
- **Validation Executed**:
  - `INSTA_UPDATE=always direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate docs_family_context::modelica_plugin_docs_family_context_groups_related_pages_by_family --features modelica -- --exact`
  - `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate --features modelica --no-run`
  - `direnv exec . git diff --check`

### [DONE] Feature: extend the `M6` external Modelica proof into docs-facing projected page-family search

- **Objective**: keep pushing the same `M6` additive external-plugin proof
  across docs-facing search consumers instead of reopening compatibility or
  host-owned package seams.
- **Update**:
  - added a builtin-registry external Modelica regression in
    `tests/integration/docs_family_search.rs`
  - mounted the new proof in `tests/xiuxian-testing-gate.rs` so it stays on
    the same integration harness as the rest of the additive package-path
    proofs
  - proved that `docs_family_search_from_config(...)` can expand
    deterministic docs-facing family search results over the external
    Modelica plugin path
  - synced the Wendao program note, surface inventory, Modelica package
    roadmap, and the active ExecPlan to the landed `M6` state
- **Validation Executed**:
  - `INSTA_UPDATE=always direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate docs_family_search::modelica_plugin_docs_family_search_matches_reference_family_clusters --features modelica -- --exact`
  - `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate --features modelica --no-run`
  - `direnv exec . git diff --check`

### [DONE] Feature: extend the `M6` external Modelica proof into docs-facing projected page-navigation search

- **Objective**: keep pushing the same `M6` additive external-plugin proof
  from repo-facing consumers into docs-facing search consumers instead of
  reopening compatibility or host-owned package seams.
- **Update**:
  - added a builtin-registry external Modelica regression in
    `tests/integration/docs_navigation_search.rs`
  - mounted the new proof in `tests/xiuxian-testing-gate.rs` rather than
    routing around the existing integration harness
  - proved that `docs_navigation_search_from_config(...)` can expand
    deterministic docs-facing navigation bundles, including `HowTo` family
    clustering, over the external Modelica plugin path
  - synced the Wendao program note, surface inventory, Modelica package
    roadmap, and the active ExecPlan to the landed `M6` state
- **Validation Executed**:
  - `INSTA_UPDATE=always direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate docs_navigation_search::modelica_plugin_docs_navigation_search_expands_reference_hits_into_navigation_bundles --features modelica -- --exact`
  - `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate --features modelica --no-run`
  - `direnv exec . git diff --check`

### [DONE] Feature: extend the `M6` external Modelica host proof into projected page-navigation search

- **Objective**: keep pushing the same `M6` additive external-plugin proof
  through real stage-two search consumers instead of reopening compatibility
  or host-owned package seams.
- **Update**:
  - added a builtin-registry external Modelica regression in
    `tests/integration/repo_projected_page_navigation_search.rs`
  - used the shared Modelica repo fixture and a stable reference query
    `Projectionica.Controllers`
  - proved that `repo_projected_page_navigation_search_from_config(...)` can
    expand deterministic navigation bundles, including `HowTo` family
    clustering, over the external Modelica plugin path
  - synced the Wendao program note, surface inventory, Modelica package
    roadmap, and the active ExecPlan to the landed `M6` state
- **Validation Executed**:
  - `INSTA_UPDATE=always direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate repo_projected_page_navigation_search::modelica_plugin_projected_page_navigation_search_expands_reference_hits_into_navigation_bundles --features modelica -- --exact`
  - `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate --features modelica --no-run`
  - `direnv exec . git diff --check`

### [DONE] Feature: extend the `M6` external Modelica host proof into projected page-family search

- **Objective**: keep pushing the same `M6` additive external-plugin proof
  through real stage-two search consumers instead of reopening compatibility
  or host-owned package seams.
- **Update**:
  - added a builtin-registry external Modelica regression in
    `tests/integration/repo_projected_page_family_search.rs`
  - used the shared Modelica repo fixture and a stable reference query
    `Projectionica.Controllers`
  - proved that `repo_projected_page_family_search_from_config(...)` can
    expand a deterministic projected family search result over the external
    Modelica plugin path
  - synced the Wendao program note, surface inventory, Modelica package
    roadmap, and the active ExecPlan to the landed `M6` state
- **Validation Executed**:
  - `INSTA_UPDATE=always direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate repo_projected_page_family_search::modelica_plugin_projected_page_family_search_matches_reference_family_clusters --features modelica -- --exact`
  - `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate --features modelica --no-run`
  - `direnv exec . git diff --check`

### [DONE] Feature: extend the `M6` external Modelica host proof into projected page-family cluster lookup

- **Objective**: keep pushing the same `M6` additive external-plugin proof
  deeper into real stage-two host consumers instead of reopening compatibility
  or host-owned package seams.
- **Update**:
  - added a builtin-registry external Modelica regression in
    `tests/integration/repo_projected_page_family_cluster.rs`
  - used the shared Modelica repo fixture and selected the stable module
    reference page `Projectionica.Controllers`
  - proved that `repo_projected_page_family_cluster_from_config(...)` can
    resolve a deterministic `HowTo` family cluster for the external Modelica
    plugin path
  - synced the Wendao program note, surface inventory, Modelica package
    roadmap, and the active ExecPlan to the landed `M6` state
- **Validation Executed**:
  - `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate repo_projected_page_family_cluster::modelica_plugin_projected_page_family_cluster_resolves_how_to_cluster_for_reference_page --features modelica -- --exact`
  - `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate --features modelica --no-run`
  - `direnv exec . git diff --check`

### [DONE] Chore: continue archive-safe compression of protocol-era quality-plan shards

- **Objective**: keep shrinking historical knowledge artifacts so removed
  protocol-client crate names and test-lane labels stop reading like current
  repository guidance.
- **Update**:
  - rewrote three remaining quality-plan shards around pedantic test
    convergence, dead-code allow removal, and workspace `schemars` unification
  - added archive notes and replaced explicit removed crate references with
    neutral historical descriptions
  - compressed old validation commands so they no longer foreground removed
    protocol-client package names
- **Validation Executed**:
  - targeted legacy-term scan across the three touched quality-plan shards and this daily log
  - `direnv exec . git diff --check`

### [DONE] Chore: continue archive-safe compression of quality-plan test-lane shards

- **Objective**: keep shrinking historical quality-plan notes so removed
  protocol-runtime lane names stop dominating archive text.
- **Update**:
  - rewrote the `xiuxian-llm` suppression-removal shard to replace explicit
    removed protocol-runtime test-lane names with neutral historical labels
  - rewrote the `runtime_agent_factory` remap-elimination shard to remove the
    last explicit old tool-config loader filename from the touched summary
  - rewrote the five-occurrence expect-cleanup shard to neutralize the old
    hard-timeout lane name
- **Validation Executed**:
  - targeted legacy-term scan across the three touched quality-plan shards and this daily log
  - `direnv exec . git diff --check`

### [DONE] Chore: compress the large Rust quality-adoption archive shard

- **Objective**: shrink the biggest remaining quality-plan ledger so old
  protocol-runtime pool paths and lane names stop dominating archive text.
- **Update**:
  - added an archive note to the large 2026-02-24 quality-adoption wave file
  - rewrote its historical tool-runtime pool, embedding fallback, and
    runtime-agent-factory references into neutral archive wording
  - replaced the last explicit old pool-path list entry with a historical
    placeholder
- **Validation Executed**:
  - targeted legacy-term scan across the touched large quality-plan shard and this daily log
  - `direnv exec . git diff --check`

### [DONE] Chore: continue archive-safe compression of residual package-and-test shards

- **Objective**: keep shrinking leftover archive notes so removed package paths
  and historical runtime test-lane names stop reading like current surfaces.
- **Update**:
  - rewrote the manual-let-else cleanup shard to replace old tool-runtime lane
    filenames with neutral historical placeholders
  - rewrote the subpackage pyproject alignment shard to replace removed Python
    package paths with archive-safe placeholders
  - rewrote the Python brand residual shard to remove the last “current
    protocol package identity” framing
- **Validation Executed**:
  - targeted legacy-term scan across the three touched quality-plan shards and this daily log
  - `direnv exec . git diff --check`

### [DONE] Chore: continue archive-safe compression of runtime-remap shards

- **Objective**: keep shrinking historical remap and test-cleanup notes so old
  tool-runtime lane names and removed dispatch filenames stop reading like live
  code ownership.
- **Update**:
  - rewrote the `xiuxian-llm` expect/unwrap convergence shard to replace old
    tool-runtime test lane names with neutral historical placeholders
  - rewrote the Discord and Telegram remap-elimination shards to replace the
    old runtime-dispatch filename and function wording with archive-safe
    descriptions
- **Validation Executed**:
  - targeted legacy-term scan across the three touched quality-plan shards and this daily log
  - `direnv exec . git diff --check`

### [DONE] Chore: continue archive-safe compression of fallback-and-recipe shards

- **Objective**: keep shrinking residual archive notes so old fallback wording,
  removed non-workspace client references, and stale recipe names stop reading
  like live operational guidance.
- **Update**:
  - rewrote the Telegram media test-support shard to neutralize the old
    non-workspace client-lane reference
  - rewrote the feature-gated profile validation shard to replace the old
    historical HTTP-to-tool-runtime fallback wording with archive-safe
    tool-runtime wording
  - rewrote the tasks.nix wrapper shard to replace the stale historical
    protocol-crate recipe name with neutral placeholders
- **Validation Executed**:
  - targeted legacy-term scan across the three touched quality-plan shards and this daily log
  - `direnv exec . git diff --check`

### [DONE] Chore: continue archive-safe compression of historical Python package-path shards

- **Objective**: keep shrinking archive notes so removed Python protocol-server
  package paths and handler-regression references stop reading like live
  package surfaces.
- **Update**:
  - rewrote the early evidence snapshot to replace the removed Python
    protocol-server hotspot path with a historical placeholder
  - rewrote the pyproject metadata completion shard to replace removed Python
    package manifest and README paths with archive-safe placeholders
  - rewrote the internal tool-module migration shard to replace removed Python
    protocol-server import, compile, and handler-regression references with
    historical placeholders
- **Validation Executed**:
  - targeted legacy-term scan across the three touched quality-plan shards and this daily log
  - `direnv exec . git diff --check`

### [DONE] Chore: continue archive-safe compression of marker-cleanup shards

- **Objective**: keep shrinking archive notes so old runtime-dispatch and
  tool-runtime test-lane filenames stop reading like live code ownership.
- **Update**:
  - rewrote the single-match-else follow-up shard to replace the old runtime
    dispatch source filename with a historical placeholder
  - rewrote the float-cmp convergence shard to replace the old discover-cache
    test lane filename with a historical placeholder
  - rewrote the marker-burndown wave-2 shard to replace the old health-gate
    test lane filename with a historical placeholder
- **Validation Executed**:
  - targeted legacy-term scan across the three touched quality-plan shards and this daily log
  - `direnv exec . git diff --check`

### [DONE] Chore: continue archive-safe compression of settings-and-startup shards

- **Objective**: keep shrinking archive notes so old settings-type names and
  historical tool-runtime startup lane names stop reading like live surfaces.
- **Update**:
  - rewrote the struct-field delegacy shard to replace the old settings type
    and key-family wording with neutral historical descriptions
  - rewrote the gateway/channel suppression shard to replace the old
    connect-startup lane name with a historical placeholder
  - rewrote the runtime/media suppression shard to replace the old agent
    startup lane name with a historical placeholder
- **Validation Executed**:
  - targeted legacy-term scan across the three touched quality-plan shards and this daily log
  - `direnv exec . git diff --check`

### [DONE] Chore: continue archive-safe compression of expect-queue shards

- **Objective**: keep shrinking archive notes so old tool-runtime
  health/discover/reconnect/startup lane names stop reading like live test
  ownership.
- **Update**:
  - rewrote the two-occurrence expect-cleanup shard to replace the old
    health-gate lane with a historical placeholder
  - rewrote the four-occurrence expect-cleanup shard to replace the old
    hard-timeout next-queue lane with a historical placeholder
  - rewrote the six- and seven-occurrence expect-cleanup shards to replace the
    old discover-cache and reconnect lane names with historical placeholders
- **Validation Executed**:
  - targeted legacy-term scan across the four touched quality-plan shards and this daily log
  - `direnv exec . git diff --check`

### [DONE] Chore: continue archive-safe compression of residual brand-and-fixture shards

- **Objective**: keep shrinking the last archive notes so stale protocol-client
  crate paths and old fixture field names stop reading like live repository
  surfaces.
- **Update**:
  - rewrote the crate-brand unification shard to replace the old protocol-client
    crate path with a historical placeholder
  - rewrote the Wendao fixture-contract shard to replace the old contract field
    name with a neutral historical description
- **Validation Executed**:
  - targeted legacy-term scan across the two touched quality-plan shards and this daily log
  - `direnv exec . git diff --check`

### [DONE] Chore: cut active tool-runtime config and wrapper residuals

- **Objective**: remove the remaining live protocol-era naming from active
  scripts, sample settings, and runtime schema descriptions.
- **Update**:
  - rewired quick-test and webhook helper paths to the retained `tool` endpoint
    helpers
  - switched Rust embedding benchmark scripts from the old protocol-era config
    section, file, flag, and strict-startup env names to their `tool`
    equivalents
  - renamed the shell wrapper for tool-list observability, updated
    `sync-for-gpt.sh` to exclude `.tool.json`, updated the sample settings file
    to `tool_runtime`/`.tool.json`, and removed the last live protocol-content
    wording from the retained tool-result schema
- **Validation Executed**:
  - targeted repo scan over the touched live scripts, config, and schema files
  - `direnv exec . git diff --check`

### [DONE] Feature: replace nested map flattening with direct flat entry storage

- **Objective**: keep pushing Arrow.jl write-path performance by removing the
  extra `Vector{Vector{KeyValue}} -> ToList` layer from `MapKind` build-side
  serialization.
- **Update**:
  - rewrote `.data/arrow-julia/src/arraytypes/map.jl` so `MapKind` now builds
    offsets and one flat `Vector{KeyValue}` directly before handing the child
    entries to struct serialization
  - kept the current logical map surface locked in
    `.data/arrow-julia/test/runtests.jl` with explicit `eltype` assertions for
    offset map roundtrips
  - confirmed the build-side hotspot now allocates much less and no longer
    dominates `offset_map` as heavily as before
- **Validation Executed**:
  - targeted map replay
    - `map_arrowvector`: `2.112 ms / 5.456 MiB`
    - `map_tobuffer`: `4.082 ms / 19.998 MiB` -> `3.317 ms / 14.373 MiB`
  - `direnv exec . julia --project=.data/arrow-julia/test -e 'using Pkg; Pkg.instantiate(); include("/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/agent/benchmarks/arrow_julia_writepaths_bench.jl")'`
    - `offset_map`: `4.074 ms / 19.998 MiB` -> `3.011 ms / 13.139 MiB`
    - `offset_uuid_any`: stayed in range at `1.445 ms / 10.109 MiB`
  - `direnv exec . julia --project=.data/arrow-julia/test -e 'using Pkg; Pkg.instantiate(); Pkg.test("Arrow")'`
    - result: `Arrow | 66082/66082` in `3m06.3s`
  - `direnv exec . git -C .data/arrow-julia diff --check`

### [DONE] Chore: cut retained skill-surface legacy imports in advanced_tools

- **Objective**: remove the remaining live `omni` and protocol-era naming from
  the retained `skills/advanced_tools` slice and the last stale `crawl4ai`
  engine example.
- **Update**:
  - rewired `skills/advanced_tools/scripts/{search,mutation}.py` from
    `omni.foundation.*` imports to retained `xiuxian_foundation.*` imports
  - rewrote `skills/advanced_tools/tests/test_advanced_tools_modular.py` away
    from the deleted decorator-based skill harness to direct module imports and
    `normalize_tool_result()` payload assertions
  - removed the last old protocol-era sample pattern from
    `skills/advanced_tools/README.md` and updated the `crawl4ai` engine
    docstring example to the isolated script runner helper
- **Validation Executed**:
  - `direnv exec . uv run pytest /Users/guangtao/projects/xiuxian-artisan-workshop/skills/advanced_tools/tests/test_advanced_tools_modular.py -q`
    - result: `13 passed, 1 skipped`
  - targeted legacy-term scan across
    `skills/advanced_tools` and `skills/crawl4ai/scripts/engine.py`
  - `direnv exec . python -m py_compile /Users/guangtao/projects/xiuxian-artisan-workshop/skills/advanced_tools/scripts/search.py /Users/guangtao/projects/xiuxian-artisan-workshop/skills/advanced_tools/scripts/mutation.py /Users/guangtao/projects/xiuxian-artisan-workshop/skills/advanced_tools/tests/test_advanced_tools_modular.py /Users/guangtao/projects/xiuxian-artisan-workshop/skills/crawl4ai/scripts/engine.py`
  - `direnv exec . git diff --check`

### [DONE] Chore: cut retained skill-code legacy imports and harness usage

- **Objective**: remove the remaining live `omni` namespace and deleted
  decorator-harness assumptions from the retained `skills/code` execution
  slice.
- **Update**:
  - rewired `skills/code/scripts/search/{commands,graph}.py` from old
    `omni` imports to retained `xiuxian_foundation` and `xiuxian_tracer`
    imports
  - updated `skills/code/scripts/search/nodes/engines.py` and
    `skills/code/scripts/smart_ast/engine.py` to use retained `xiuxian_core`
    and `xiuxian_ast` wording, while keeping vector search as graceful
    Rust/Wendao-owned degradation
  - rewrote `skills/code/tests/test_code_tools_modular.py` away from the
    deleted `xiuxian_skill` decorator to direct package imports plus
    canonical `normalize_tool_result()` assertions
  - removed the last stale test-kit example import from
    `skills/code/tests/test_standalone.py`
- **Validation Executed**:
  - `direnv exec . uv run pytest /Users/guangtao/projects/xiuxian-artisan-workshop/skills/code/tests/test_code_tools_modular.py -q`
    - result: `20 passed`
  - targeted legacy-term scan across the touched `skills/code` scripts and tests
  - `direnv exec . python -m py_compile /Users/guangtao/projects/xiuxian-artisan-workshop/skills/code/scripts/search/commands.py /Users/guangtao/projects/xiuxian-artisan-workshop/skills/code/scripts/search/graph.py /Users/guangtao/projects/xiuxian-artisan-workshop/skills/code/scripts/search/nodes/engines.py /Users/guangtao/projects/xiuxian-artisan-workshop/skills/code/scripts/smart_ast/engine.py /Users/guangtao/projects/xiuxian-artisan-workshop/skills/code/tests/test_code_tools_modular.py /Users/guangtao/projects/xiuxian-artisan-workshop/skills/code/tests/test_standalone.py`
  - `direnv exec . git diff --check`

### [DONE] Chore: remove deleted loader assumptions from remaining code-skill tests

- **Objective**: finish the retained `skills/code` test cleanup by removing the
  last direct dependencies on the deleted loader and skill-path registry.
- **Update**:
  - rewrote `skills/code/tests/test_code_search.py` to load retained modules
    directly from the local scripts package, unwrap canonical tool results via
    `normalize_tool_result()`, and stop assuming `create_tools_loader` /
    `SKILLS_DIR`
  - rewrote `skills/code/tests/test_search_graph_cache.py` to import the
    retained graph module directly instead of bootstrapping a deleted loader
  - removed the last loader-era import assumptions from these two test files
    without changing the retained code-search implementation surface
- **Validation Executed**:
  - `direnv exec . uv run pytest /Users/guangtao/projects/xiuxian-artisan-workshop/skills/code/tests/test_code_search.py /Users/guangtao/projects/xiuxian-artisan-workshop/skills/code/tests/test_search_graph_cache.py -q`
    - result: `23 passed`
  - targeted legacy-term scan across the two touched test files
  - `direnv exec . python -m py_compile /Users/guangtao/projects/xiuxian-artisan-workshop/skills/code/tests/test_code_search.py /Users/guangtao/projects/xiuxian-artisan-workshop/skills/code/tests/test_search_graph_cache.py`
  - `direnv exec . git diff --check`

### [DONE] Chore: cut retained researcher skill legacy imports and path helpers

- **Objective**: remove the remaining live `omni` imports and deleted
  skill-path/git-root assumptions from the retained `skills/researcher` slice.
- **Update**:
  - rewired `skills/researcher/scripts/research_entry.py` from old
    `omni.foundation.*` imports to retained `xiuxian_foundation.*` imports and
    switched manifest/root resolution to `get_skills_dir()` plus
    `get_project_root()`
  - rewired `skills/researcher/scripts/research.py` to retained
    `xiuxian_foundation.config.dirs` helpers and removed stale
    tool-timeout wording
  - updated `skills/researcher/tests/test_research_entry_qianji_env.py` and
    `skills/researcher/tests/test_researcher_modular.py` to match the retained
    subprocess signature and real project-root cwd behavior
- **Validation Executed**:
  - `direnv exec . uv run pytest /Users/guangtao/projects/xiuxian-artisan-workshop/skills/researcher/tests/test_research_entry_qianji_env.py /Users/guangtao/projects/xiuxian-artisan-workshop/skills/researcher/tests/test_researcher_modular.py -q`
    - result: `20 passed`
  - targeted legacy-term scan across the touched `skills/researcher` files
  - `direnv exec . python -m py_compile /Users/guangtao/projects/xiuxian-artisan-workshop/skills/researcher/scripts/research_entry.py /Users/guangtao/projects/xiuxian-artisan-workshop/skills/researcher/scripts/research.py /Users/guangtao/projects/xiuxian-artisan-workshop/skills/researcher/tests/test_research_entry_qianji_env.py /Users/guangtao/projects/xiuxian-artisan-workshop/skills/researcher/tests/test_researcher_modular.py`
  - `direnv exec . git diff --check`

### [DONE] Chore: cut template skill legacy teaching surface

- **Objective**: remove the remaining old namespace and protocol-era teaching
  examples from the reusable skill template so new skills are scaffolded
  against the retained runtime shape.
- **Update**:
  - rewrote `skills/_template/scripts/commands.py` from old `omni.foundation.*`
    imports to retained `xiuxian_foundation.*` imports
  - rewrote `skills/_template/{SKILL,README}.md` and
    `skills/_template/references/skill-workflow.md` to replace `agent.skills`,
    `@omni(...)`, and old protocol-runtime wording with retained `skills.*` and
    tool-runtime examples
  - removed the last `MCP Server auto-prefixes` teaching note from the template
- **Validation Executed**:
  - targeted legacy-term scan across `skills/_template`
  - `direnv exec . python -m py_compile /Users/guangtao/projects/xiuxian-artisan-workshop/skills/_template/scripts/commands.py`
  - `direnv exec . git diff --check`

### [DONE] Chore: clear remaining active legacy invocation examples across skills

- **Objective**: remove the last live `@omni(...)`, `agent.skills.*`, and
  protocol-era invocation wording from retained skill docs and inline examples.
- **Update**:
  - rewrote active invocation examples in `skills/crawl4ai`,
    `skills/git`, and `skills/researcher` docs to neutral `tool: ... with {...}`
    wording
  - rewrote the remaining old decorator example in `skills/README.md` to use
    retained `xiuxian_foundation.api.decorators.skill_command`
  - rewrote the last inline invocation strings in
    `skills/advanced_tools/scripts/mutation.py` and
    `skills/python_engineering/scripts/empty.py`
- **Validation Executed**:
  - targeted legacy-term scan across `/Users/guangtao/projects/xiuxian-artisan-workshop/skills`
    - result: no remaining hits for `omni`, `agent.skills`, `@omni(...)`, or
      old loader/protocol markers
  - `direnv exec . python -m py_compile /Users/guangtao/projects/xiuxian-artisan-workshop/skills/crawl4ai/scripts/crawl_url.py /Users/guangtao/projects/xiuxian-artisan-workshop/skills/advanced_tools/scripts/mutation.py /Users/guangtao/projects/xiuxian-artisan-workshop/skills/python_engineering/scripts/empty.py`
  - `direnv exec . git diff --check`

### [DONE] Chore: cut active legacy invocation and protocol wording from docs/config

- **Objective**: remove the remaining live `@omni(...)`, `SKILLS_DIR()`, and
  protocol-era `MCP` wording from active top-level guidance and retained
  configuration comments.
- **Update**:
  - rewrote `CLAUDE.md` from `@omni("skill.command")` /
    `SKILLS_DIR()` examples to retained `tool: skill.command` /
    `get_skills_dir()` wording
  - rewrote `docs/99_llm/memory-context.md` and
    `docs/99_llm/routing-guide.md` so all live invocation examples now use
    neutral `tool: ... with {...}` syntax instead of deleted decorator-era
    command forms
  - updated `pyproject.toml` pytest marker descriptions and coverage title to
    retained Xiuxian wording
  - updated `packages/conf/settings.yaml` comments so the retained embedding,
    tool-runtime, and memory-control guidance no longer describes the current
    system in old protocol-runtime terms
- **Validation Executed**:
  - targeted legacy-term scan across `CLAUDE.md`, `pyproject.toml`,
    `docs/99_llm/memory-context.md`, `docs/99_llm/routing-guide.md`, and
    `packages/conf/settings.yaml`
    - result: no remaining hits for `mcp`, `MCP`, `mcp_`, `@omni(...)`, or
      `SKILLS_DIR()`
  - `direnv exec . python - <<'PY' ... tomllib.load(pyproject.toml) ... PY`
    - result: `pyproject ok`
  - `direnv exec . git diff --check`

### [DONE] Chore: remove remaining live `xiuxian_skill` design residue

- **Objective**: stop describing the retained Python surface as if it still had
  a distinct `xiuxian_skill` design layer.
- **Update**:
  - rewrote `packages/python/foundation/src/xiuxian_tracer/storage.py` to use
    retained `skills.*` module names and neutral command-registration wording
    instead of `agent.skills.*` and decorator-era legacy comments
  - rewrote `packages/python/foundation/src/xiuxian_foundation/utils/__init__.py`
    to point usage examples at `get_skills_dir()` instead of deleted
    `config.skills` helpers
  - rewrote the contract example in
    `packages/python/foundation/tests/unit/services/test_runtime_contract_schemas.py`
    from `@omni(...)` to retained `tool: ... with {...}` syntax
  - renamed the unused pytest marker description in `pyproject.toml` from
    `xiuxian_skill` to neutral `project_skill`
- **Validation Executed**:
  - targeted legacy-term scan across the four touched files
    - result: no remaining hits for `xiuxian_skill`, `agent.skills`, or
      `@omni(...)`
  - `direnv exec . uv run pytest /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/tests/unit/services/test_runtime_contract_schemas.py -q`
    - result: `4 passed`
  - `direnv exec . python -m py_compile /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/src/xiuxian_tracer/storage.py /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/src/xiuxian_foundation/utils/__init__.py /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/tests/unit/services/test_runtime_contract_schemas.py`
  - `direnv exec . git diff --check`

### [DONE] Chore: remove remaining skill-command teaching noise from active guidance

- **Objective**: stop presenting `@skill_command` and older path/runtime
  phrasing as the primary conceptual layer in active skill and workflow
  documentation.
- **Update**:
  - rewrote `skills/README.md`, `skills/git/SKILL.md`,
    `skills/_template/SKILL.md`, and `skills/_template/README.md` so they now
    describe `scripts/*.py` command functions and retained runtime registration
    instead of treating `@skill_command` as the headline architectural pattern
  - rewrote `docs/99_llm/native-workflow-guide.md` from old `omni.*`
    imports and `Omni-Dev-Fusion` wording to retained `xiuxian_*` workflow
    imports and neutral runtime language
  - rewrote `docs/testing/test_kit_modernization.md` to remove deleted
    `SKILLS_DIR(...)` / `omni.foundation.*` path guidance
  - tightened `skills/code/scripts/search/commands.py` docstring so it no
    longer describes the command file as a legacy decorator-discovery artifact
- **Validation Executed**:
  - targeted legacy-term scan across the seven touched files
    - result: no remaining hits for `SKILLS_DIR(...)`, `agent.skills.*`,
      `@skill_command` teaching labels, `omni.foundation`, `omni.tracer`, or
      `Omni-Dev-Fusion`
  - `direnv exec . python -m py_compile /Users/guangtao/projects/xiuxian-artisan-workshop/skills/code/scripts/search/commands.py`
  - `direnv exec . git diff --check`

### [DONE] Chore: remove remaining command-decorator teaching focus from active testing docs

- **Objective**: stop centering active developer/testing docs on raw
  `@skill_command` examples when the retained runtime contract is command
  metadata plus runtime registration.
- **Update**:
  - rewrote `docs/developer/ast-grep-core.md` to describe
    `@skill_command($$$)` as a command-decorator matching pattern instead of a
    broader architectural headline
  - rewrote `docs/testing/router-file-discovery-intent-report.md` so
    `description` and `category` fields are described as registered command
    metadata rather than decorator-argument semantics
  - rewrote `docs/testing/scenario-test-driven-autofix-loop.md` so the main
    API examples focus on callable command signatures and returned structures,
    not decorator blocks
- **Validation Executed**:
  - targeted legacy-term scan across the three touched docs
    - result: no remaining hits for the old decorator-centric teaching phrases
  - `direnv exec . git diff --check`

### [DONE] Chore: remove remaining decorator-centric naming noise from active Python tests

- **Objective**: stop framing retained Python unit tests as if the main concept
  under test were a special `skill_command` design layer instead of ordinary
  retained command-registration behavior.
- **Update**:
  - rewrote module/class/docstring wording in
    `packages/python/foundation/tests/unit/api/test_foundation_decorators.py`
    to describe retained command-registration behavior rather than a
    standalone decorator-centric design
  - rewrote module/class/docstring wording in
    `packages/python/foundation/tests/unit/utils/test_introspection.py`
    to describe retained command metadata and schema extraction behavior
  - rewrote `packages/python/foundation/tests/unit/api/test_decorators.py`
    from “Decorator Validation Tests” to command-metadata validation wording
- **Validation Executed**:
  - targeted legacy-term scan across the three touched test files
    - result: no remaining hits for the old decorator-centric labels and class
      names
  - `direnv exec . uv run pytest /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/tests/unit/api/test_foundation_decorators.py /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/tests/unit/utils/test_introspection.py /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/tests/unit/api/test_decorators.py -q`
    - result: `47 passed, 3 skipped`
  - `direnv exec . python -m py_compile /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/tests/unit/api/test_foundation_decorators.py /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/tests/unit/utils/test_introspection.py /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/tests/unit/api/test_decorators.py`
  - `direnv exec . git diff --check`

### [DONE] Chore: remove remaining skills-path and handler naming noise from active Python surfaces

- **Objective**: clean the next active Python slice so handler tests and
  skills-path utilities no longer describe themselves with older
  decorator-centric or broad “skill utilities” wording.
- **Update**:
  - rewrote `packages/python/foundation/tests/unit/rag/test_handlers.py`
    module/class/docstrings to talk about retained command handlers and command
    metadata instead of a standalone `skill_command` design layer
  - rewrote `packages/python/foundation/tests/test_skills_path.py` to frame
    `skill_command()` as a scripts-path helper and renamed the test group to
    `TestSkillPathUtilities`
  - tightened the lazy-import comment in
    `packages/python/foundation/src/xiuxian_foundation/__init__.py` so it now
    describes `utils.skills` as skill-path and command-path utilities
- **Validation Executed**:
  - targeted legacy-term scan across the three touched files
    - result: no remaining hits for the replaced old labels
  - `direnv exec . uv run pytest /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/tests/unit/rag/test_handlers.py /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/tests/test_skills_path.py -q`
    - result: `33 passed`
  - `direnv exec . python -m py_compile /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/tests/unit/rag/test_handlers.py /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/tests/test_skills_path.py /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/src/xiuxian_foundation/__init__.py`
  - `direnv exec . git diff --check`

### [DONE] Chore: delete benchmark-era runtime test residue and tighten workflow/runtime wording

- **Objective**: remove one still-broken benchmark-era Python test slice that
  depended on deleted runtime modules, while further tightening retained
  runtime/workflow wording.
- **Update**:
  - deleted
    `packages/python/foundation/tests/unit/rag/test_benchmark_entities.py`,
    which still imported removed runtime helpers such as
    `agent.core.skill_runtime.context` and `test_skills`
  - added
    `packages/python/foundation/tests/unit/rag/test_benchmark_entities_package_removal.py`
    as a removal guard that locks those deleted modules out
  - tightened
    `packages/python/foundation/src/xiuxian_foundation/runtime/__init__.py`
    wording around isolation helpers
  - tightened
    `docs/99_llm/native-workflow-guide.md` so it presents workflow authoring as
    retained runtime command registration rather than a decorator-first design
- **Validation Executed**:
  - `direnv exec . uv run pytest /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/tests/unit/rag/test_benchmark_entities_package_removal.py -q`
    - result: `2 passed`
  - targeted scan across the touched runtime/doc files
    - result: remaining deleted module names appear only inside the new removal
      guard
  - `direnv exec . python -m py_compile /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/src/xiuxian_foundation/runtime/__init__.py /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/tests/unit/rag/test_benchmark_entities_package_removal.py`
  - `direnv exec . git diff --check`

### [DONE] Chore: tighten retained isolation/workflow authoring examples

- **Objective**: keep the retained runtime helpers but remove the remaining
  decorator-first and “skill script” teaching phrasing around them.
- **Update**:
  - rewrote
    `packages/python/foundation/src/xiuxian_foundation/runtime/isolation.py`
    module docstring and helper docstrings so they now describe isolated script
    entrypoints and external command execution rather than “heavy skill
    dependencies” plus decorator-driven examples
  - rewrote `docs/99_llm/native-workflow-guide.md` title and registration note
    so it frames workflow authoring around retained runtime command exposure,
    not a decorator-first authoring model
- **Validation Executed**:
  - targeted scan across `runtime/isolation.py` and
    `docs/99_llm/native-workflow-guide.md`
    - result: only the real isolated script-runner API name remains; the removed
      decorator-first teaching phrases are gone
  - `direnv exec . python -m py_compile /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/src/xiuxian_foundation/runtime/isolation.py`
  - `direnv exec . git diff --check`

### [DONE] Chore: remove remaining `skill_command`-prefixed test names from active Foundation tests

- **Objective**: keep the retained decorator implementation intact, but stop
  exposing old design language through Python test function names.
- **Update**:
  - renamed the remaining `test_skill_command_*` functions in
    `packages/python/foundation/tests/unit/api/test_foundation_decorators.py`
    to `test_registered_command_*`
  - renamed the remaining `test_skill_command_*` functions in
    `packages/python/foundation/tests/unit/utils/test_introspection.py`
    to `test_registered_command_*`
  - left runtime behavior and assertions untouched; only the test-facing names
    and wording were normalized
- **Validation Executed**:
  - targeted scan across the two touched test files
    - result: no remaining `def test_*skill_command*` or old
      decorator-centric labels
  - `direnv exec . uv run pytest /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/tests/unit/api/test_foundation_decorators.py /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/tests/unit/utils/test_introspection.py -q`
    - result: `47 passed`
  - `direnv exec . python -m py_compile /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/tests/unit/api/test_foundation_decorators.py /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/tests/unit/utils/test_introspection.py`
  - `direnv exec . git diff --check`

### [DONE] Chore: cut Python-side scanner and registration helper surfaces

- **Objective**: stop carrying Python-local command scanning and registration
  APIs that no longer reflect the retained runtime boundary.
- **Update**:
  - removed public decorator helper surfaces from
    `packages/python/foundation/src/xiuxian_foundation/api/decorators.py`:
    `is_skill_command`, `get_script_config`, and
    `_skill_command_registry`
  - switched retained decorator metadata to `_command_metadata` and updated the
    directly dependent test slices to assert that shape instead of the deleted
    registry-era fields
  - rewrote `packages/python/foundation/src/xiuxian_tracer/storage.py` to be a
    real trace-storage module again, eliminating the orphaned
    `SkillLoaderMixin` scanner surface
  - renamed the script path helper in
    `packages/python/foundation/src/xiuxian_foundation/utils/skills.py` from
    `skill_command()` to `skill_script()` and updated the exported utility
    surface plus tests
  - added
    `packages/python/foundation/tests/unit/api/test_command_metadata_surface_removal.py`
    as a removal guard for the deleted helper APIs
- **Validation Executed**:
  - `direnv exec . uv run pytest /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/tests/unit/api/test_command_metadata_surface_removal.py /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/tests/unit/api/test_foundation_decorators.py /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/core/tests/units/skills/test_skill_script.py /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/tests/unit/utils/test_introspection.py /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/tests/unit/api/test_decorators.py /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/tests/unit/rag/test_handlers.py /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/tests/test_skills_path.py /Users/guangtao/projects/xiuxian-artisan-workshop/skills/code/tests/test_code_search.py /Users/guangtao/projects/xiuxian-artisan-workshop/skills/crawl4ai/tests/test_crawl4ai_skill.py /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/tests/unit/tracer/test_tracer.py -q`
    - result: `119 passed, 3 skipped`, with two unrelated pre-existing import
      blockers in `skills/crawl4ai/tests/test_crawl4ai_skill.py` collection and
      `packages/python/foundation/tests/unit/tracer/test_tracer.py`
  - `direnv exec . python -m compileall packages/python/foundation/src/xiuxian_foundation/api packages/python/foundation/src/xiuxian_foundation/utils packages/python/foundation/src/xiuxian_tracer /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/tests/unit/api/test_command_metadata_surface_removal.py /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/tests/unit/api/test_foundation_decorators.py /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/core/tests/units/skills/test_skill_script.py /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/tests/unit/utils/test_introspection.py /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/tests/unit/api/test_decorators.py /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/tests/unit/rag/test_handlers.py /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/tests/test_skills_path.py /Users/guangtao/projects/xiuxian-artisan-workshop/skills/code/tests/test_code_search.py /Users/guangtao/projects/xiuxian-artisan-workshop/skills/crawl4ai/tests/test_crawl4ai_skill.py`
    - result: passed
  - `direnv exec . git diff --check`

### [DONE] Chore: remove `skill_command` from retained active Python decorator surface

- **Objective**: stop exposing the retained command decorator itself under the
  old skill-centric name.
- **Update**:
  - renamed the retained decorator in
    `packages/python/foundation/src/xiuxian_foundation/api/decorators.py`
    from `skill_command` to `tool_command` with no legacy export kept
  - rewired the retained skill scripts, command tests, handler tests, and the
    local `crawl4ai` shim decorator to import and use `tool_command`
  - tightened remaining inline examples and comments so active Python and
    skill-surface guidance no longer teaches `@skill_command`
  - extended the removal guard in
    `packages/python/foundation/tests/unit/api/test_command_metadata_surface_removal.py`
    so the old decorator name must stay absent
- **Validation Executed**:
  - `direnv exec . uv run pytest /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/tests/unit/api/test_command_metadata_surface_removal.py /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/tests/unit/api/test_foundation_decorators.py /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/core/tests/units/skills/test_skill_script.py /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/tests/unit/utils/test_introspection.py /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/tests/unit/rag/test_handlers.py /Users/guangtao/projects/xiuxian-artisan-workshop/skills/code/tests/test_code_search.py -q`
    - result: `108 passed`
  - `direnv exec . python -m compileall packages/python/foundation/src/xiuxian_foundation/api /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/core/tests/units/skills/test_skill_script.py /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/tests/unit/api/test_foundation_decorators.py /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/tests/unit/utils/test_introspection.py /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/tests/unit/rag/test_handlers.py /Users/guangtao/projects/xiuxian-artisan-workshop/skills/git/scripts/commit.py /Users/guangtao/projects/xiuxian-artisan-workshop/skills/git/scripts/smart_commit_graphflow/commands.py /Users/guangtao/projects/xiuxian-artisan-workshop/skills/_template/scripts/commands.py /Users/guangtao/projects/xiuxian-artisan-workshop/skills/advanced_tools/scripts/search.py /Users/guangtao/projects/xiuxian-artisan-workshop/skills/advanced_tools/scripts/mutation.py /Users/guangtao/projects/xiuxian-artisan-workshop/skills/code/scripts/search/commands.py /Users/guangtao/projects/xiuxian-artisan-workshop/skills/researcher/scripts/research_entry.py /Users/guangtao/projects/xiuxian-artisan-workshop/skills/crawl4ai/scripts/crawl_url.py /Users/guangtao/projects/xiuxian-artisan-workshop/skills/crawl4ai/scripts/utils.py`
    - result: passed

- **Update**:
  - renamed the retained runtime optimization helper module from
    `skill_optimization.py` to `runtime_optimization.py`
  - rewired `xiuxian_foundation.runtime.__init__`, `xiuxian_rag.retrieval`,
    and the moved unit test file to import the new module path
  - tightened the moved module and test docstrings so they describe runtime
    query and chunk workflows instead of a separate skill-level design layer
- **Validation Executed**:
  - `direnv exec . uv run pytest /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/tests/unit/runtime/test_runtime_optimization.py -q`
    - result: `15 passed`
  - `direnv exec . python -m compileall /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/src/xiuxian_foundation/runtime /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/src/xiuxian_rag/retrieval`
    - result: passed
  - `direnv exec . rg -n "skill_optimization|runtime\\.skill_optimization" /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/core /Users/guangtao/projects/xiuxian-artisan-workshop/skills -g '*.py' -g '*.md'`
    - result: no hits

- **Update**:
  - renamed the retained handler type from `SkillCommandHandler` to
    `CommandHandler`
  - rewired active Foundation decorator exports, handler tests, and command
    result wording so this slice no longer presents a distinct skill-command
    handler layer
- **Validation Executed**:
  - `direnv exec . uv run pytest /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/tests/unit/rag/test_handlers.py /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/tests/unit/api/test_foundation_decorators.py /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/tests/unit/api/test_decorators.py -q`
    - result: `17 passed`
  - `direnv exec . python -m compileall /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/src/xiuxian_foundation/api /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/tests/unit/rag/test_handlers.py`
    - result: passed

- **Update**:
  - physically renamed `xiuxian_foundation.skill_hooks` to
    `xiuxian_foundation.command_hooks`
  - renamed the retained lifecycle callbacks from `*_skill_execute` to
    `*_command_execute`
  - rewrote the moved unit test and the `test_skills_path.py` framing so this
    slice describes command execution hooks and skills-tree path helpers rather
    than a separate skill-command runtime layer
- **Validation Executed**:
  - `direnv exec . uv run pytest /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/tests/unit/runtime/test_command_hooks.py /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/tests/test_skills_path.py -q`
    - result: `15 passed`
  - `direnv exec . python -m compileall /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/src/xiuxian_foundation/command_hooks.py /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/tests/unit/runtime/test_command_hooks.py /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/tests/test_skills_path.py`
    - result: passed
  - `direnv exec . rg -n "skill_hooks|register_before_skill_execute|register_after_skill_execute|run_before_skill_execute|run_after_skill_execute" /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python /Users/guangtao/projects/xiuxian-artisan-workshop/skills /Users/guangtao/projects/xiuxian-artisan-workshop/docs -g '*.py' -g '*.md'`
    - result: no hits

- **Update**:
  - deleted the orphaned `skill_resource` / `is_skill_resource` /
    `get_resource_config` decorator surface from
    `xiuxian_foundation.api.decorators`
  - extended the removal guard so this dead resource-registration decorator
    surface must stay absent from the retained Python API
- **Validation Executed**:
  - `direnv exec . uv run pytest /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/tests/unit/api/test_command_metadata_surface_removal.py -q`
    - result: `3 passed`
  - `direnv exec . python -m compileall /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/src/xiuxian_foundation/api/decorators.py /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/tests/unit/api/test_command_metadata_surface_removal.py`
    - result: passed

- **Update**:
  - removed `current_skill_dir` and the generic `skill_path` helper from the
    retained public Python utility surface
  - collapsed `xiuxian_foundation.utils.skills` onto the narrower
    `skill_asset` / `skill_script` / `skill_reference` / `skill_data` helpers,
    keeping the underlying path joiner private
  - rewired top-level Foundation exports and removal guards so the broad
    skill-root helper surface cannot reappear
- **Validation Executed**:
  - `direnv exec . uv run pytest /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/tests/test_skills_path.py /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/tests/unit/api/test_command_metadata_surface_removal.py -q`
    - result: `11 passed`
  - `direnv exec . python -m compileall /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/src/xiuxian_foundation/utils/skills.py /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/src/xiuxian_foundation/utils/__init__.py /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/src/xiuxian_foundation/__init__.py /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/tests/test_skills_path.py /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/tests/unit/api/test_command_metadata_surface_removal.py`
    - result: passed

- **Update**:
  - deleted the orphaned bridge skill-scan surface:
    `SkillStructure`, `SkillScannerProvider`, and the related root exports
  - added a bridge removal guard so the deleted skill-scan type/protocol shell
    cannot silently reappear
- **Validation Executed**:
  - `direnv exec . uv run pytest /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/tests/unit/bridge/test_types.py /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/tests/unit/bridge/test_skill_scan_surface_removal.py -q`
    - result: `5 passed`
  - `direnv exec . python -m compileall /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/src/xiuxian_foundation/bridge /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/tests/unit/bridge/test_skill_scan_surface_removal.py`
    - result: passed

- **Update**:
  - deleted the orphaned local handler wrapper module
    `xiuxian_foundation.api.handlers`
  - removed the related `decorators.py` re-exports and replaced the old
    handler tests with a removal guard
  - rewrote the `_template` skill workflow example to use plain functions
    instead of `graph_node` wrappers
- **Validation Executed**:
  - `direnv exec . uv run pytest /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/tests/unit/api/test_handler_surface_removal.py /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/tests/unit/api/test_foundation_decorators.py /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/tests/unit/api/test_decorators.py -q`
    - result: `9 passed`
  - `direnv exec . python -m compileall /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/src/xiuxian_foundation/api/decorators.py /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/foundation/tests/unit/api/test_handler_surface_removal.py /Users/guangtao/projects/xiuxian-artisan-workshop/skills/_template/scripts/commands.py`
    - result: passed

### [DONE] Feature: align `MapKind` offsets/value assembly with a builder-style preallocated buffer path

- **Objective**: keep `Arrow.jl` map serialization on a production-grade path
  by adopting a low-risk builder/buffer optimization only if the official
  write-path harness improved end-to-end.
- **Update**:
  - revalidated the current `offset_map` baseline before touching code and
    rejected the broader `MapEntries` idea because it regressed the official
    harness
  - updated `.data/arrow-julia/src/arraytypes/map.jl` so
    `mapoffsetsandvalues(...)` computes total entry counts first, then fills
    preallocated offsets and flattened `KeyValue` buffers via cursor writes
    instead of `sizehint! + push!`
  - kept the existing Arrow map layout and `Struct` child path unchanged; the
    change stayed narrowly scoped to `MapKind` buffer assembly
  - extended `.data/arrow-julia/test/runtests.jl` with an empty offset-map
    append regression so the preallocated path stays honest on zero-entry map
    segments
- **Validation Executed**:
  - `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_writepaths_bench.jl`
    - result: `offset_map` improved from `2.187 ms / 13.342 MiB` to
      `2.035 ms / 13.342 MiB`
  - `direnv exec . julia --project=.data/arrow-julia/test -e 'using Pkg; Pkg.instantiate(); Pkg.test("Arrow")'`
    - result: `Arrow | 66109/66109 | 3m19.7s`
  - `direnv exec . git -C .data/arrow-julia diff --check -- src/arraytypes/map.jl test/runtests.jl`
  - `direnv exec . git diff --check -- .cache/agent/execplans/arrow_julia_map_preallocated_buffers.md docs/GTD/DAILY_2026_03_29.md`

### [DONE] Chore: reject the branchless `ToArrow` map-buffer path and keep only the regression coverage

- **Objective**: verify whether hoisting the `ToArrow` wrapper branch out of
  `mapoffsetsandvalues(::ToArrow, ...)` produced a production-grade gain
  instead of a local microbench mirage.
- **Update**:
  - prototyped separate raw and converting loops in
    `.data/arrow-julia/src/arraytypes/map.jl` after local wrapped-flatten
    probes suggested a small possible improvement
  - replayed the official write-path harness and rejected the code change once
    `offset_map` regressed instead of improving
  - reverted the branchless code path and kept the newly added `Any`
    offset-map append regression in `.data/arrow-julia/test/runtests.jl`
    because it covers a real path that had not been locked before
- **Validation Executed**:
  - `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_writepaths_bench.jl`
    - result: the prototype regressed `offset_map` from roughly
      `2.035 ms / 13.342 MiB` to `2.261 ms / 13.342 MiB`, so it was reverted
  - `direnv exec . julia --project=.data/arrow-julia/test -e 'using Pkg; Pkg.instantiate(); Pkg.test("Arrow")'`
    - result after revert plus the retained regression: `Arrow | 66110/66110 | 3m19.1s`
  - `direnv exec . git -C .data/arrow-julia diff --check -- src/arraytypes/map.jl test/runtests.jl`
  - `direnv exec . git diff --check -- .cache/agent/execplans/arrow_julia_map_toarrow_branchless_buffers.md docs/GTD/DAILY_2026_03_29.md`

### [DONE] Feature: add a direct UTF8 chunk write path for `ToList{UInt8}`

- **Objective**: improve the production `offset_string` write path without
  reopening the previously rejected buffered-write route or changing binary
  semantics.
- **Update**:
  - profiled `offset_string` and `offset_binary` and confirmed the retained
    UTF8 hotspots were `_buildtolist(...)` and
    `_writearray_tolist_uint8(...)`
  - rejected a preallocated `_buildtolist(...)` prototype because local probes
    were slower than the existing `sizehint! + push!` builder
  - added a narrow specialization in `.data/arrow-julia/src/arraytypes/list.jl`
    for `ToList{UInt8,true,<:AbstractVector{<:AbstractString}}` so string
    chunks write directly via `pointer(chunk)` and `ncodeunits(chunk)`
  - left the binary `CodeUnits` route on the existing `_writeuint8chunk(...)`
    path and reused the existing offset string/binary append regressions in
    `.data/arrow-julia/test/runtests.jl`
- **Validation Executed**:
  - `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_writepaths_bench.jl`
    - result: two consecutive replays kept `offset_string` below the
      immediately preceding `1.79 ms / 2.085 MiB` baseline, first at
      `1.71 ms / 2.085 MiB` and then at `1.262 ms / 2.085 MiB` on the warmed
      rerun
  - `direnv exec . julia --project=.data/arrow-julia/test -e 'using Pkg; Pkg.instantiate(); Pkg.test("Arrow")'`
    - result: `Arrow | 66110/66110 | 16m38.0s`
  - `direnv exec . git -C .data/arrow-julia diff --check -- src/arraytypes/list.jl`
  - `direnv exec . git diff --check -- .cache/agent/execplans/arrow_julia_tolist_string_direct_write.md docs/GTD/DAILY_2026_03_29.md`

### [DONE] Feature: delete the Python bridge package

- **Objective**: tighten the post-Flight Python deletion boundary by removing
  the remaining local Python bridge package rather than preserving a stub
  package around deleted analyzer/provider code.
- **Update**:
  - deleted `xiuxian_foundation.bridge` entirely, including the temporary
    validation-only stub that remained after the analyzer/provider teardown
  - removed the lazy `bridge` export from `xiuxian_foundation.__init__`
  - moved the lone live `SearchResult` model into
    `xiuxian_tracer.callbacks`, where the hybrid-search helper actually uses it
  - collapsed the old bridge test matrix into a single package-removal guard
    and updated the bindings removal test to assert the package itself is gone
  - removed the stale bridge row from `packages/python/foundation/README.md`
- **Validation Executed**:
  - `direnv exec . uv run pytest packages/python/foundation/tests/unit/bridge/test_bridge_package_removal.py packages/python/core/tests/units/skills/test_bindings_package_removal.py -q`
    - result: `5 passed`
  - `direnv exec . python -m compileall packages/python/foundation/src/xiuxian_foundation packages/python/foundation/tests/unit/bridge/test_bridge_package_removal.py packages/python/core/tests/units/skills/test_bindings_package_removal.py`
  - `direnv exec . git diff --check`

### [DONE] Feature: delete the orphaned runtime path-filter helper

- **Objective**: remove another non-Flight Python runtime shell after
  confirming it no longer serves any retained execution or transport path.
- **Update**:
  - deleted `xiuxian_foundation.runtime.path_filter`
  - added a focused removal guard to lock the module out of the retained
    runtime package
- **Validation Executed**:
  - `direnv exec . uv run pytest packages/python/foundation/tests/unit/runtime/test_path_filter_package_removal.py -q`
    - result: `2 passed`
  - `direnv exec . python -m compileall packages/python/foundation/src/xiuxian_foundation/runtime packages/python/foundation/tests/unit/runtime/test_path_filter_package_removal.py`
  - `direnv exec . git diff --check`

### [DONE] Feature: delete the unused runtime path and context shells

- **Objective**: continue shrinking the post-Flight Python runtime package by
  removing local sys.path and context-registry helpers that no longer have any
  retained callers.
- **Update**:
  - deleted `xiuxian_foundation.runtime.path`
  - deleted the entire `xiuxian_foundation.runtime.context` package
  - removed `temporary_sys_path` from `xiuxian_foundation.runtime.__all__`
  - added physical-removal guards for both deleted surfaces
- **Validation Executed**:
  - `direnv exec . uv run pytest packages/python/foundation/tests/unit/runtime/test_runtime_context_package_removal.py packages/python/foundation/tests/unit/runtime/test_runtime_path_package_removal.py packages/python/foundation/tests/unit/runtime/test_path_filter_package_removal.py -q`
    - result: `6 passed`
  - `direnv exec . python -m compileall packages/python/foundation/src/xiuxian_foundation/runtime packages/python/foundation/tests/unit/runtime/test_runtime_context_package_removal.py packages/python/foundation/tests/unit/runtime/test_runtime_path_package_removal.py`
  - `direnv exec . git diff --check`

### [DONE] Feature: narrow gitops to retained project-root helpers

- **Objective**: strip `xiuxian_foundation.runtime.gitops` down to the smallest
  retained surface still used by Python-side CLI helpers.
- **Update**:
  - kept only `get_project_root()`
  - inlined the former `get_instructions_dir()` caller in
    `utils/instructions/loader.py`
  - deleted `get_spec_dir`, `get_docs_dir`, `get_agent_dir`, `get_src_dir`,
    `get_instructions_dir`, `is_git_repo`, `is_project_root`, and `PROJECT`
  - removed those exports from `xiuxian_foundation.runtime` and
    `xiuxian_foundation.__init__`
  - replaced the old self-referential gitops test file with a retained-surface
    test plus a removal guard for the deleted helpers
- **Validation Executed**:
  - `direnv exec . uv run pytest packages/python/foundation/tests/test_gitops.py packages/python/foundation/tests/unit/runtime/test_gitops_surface_removal.py -q`
    - result: `5 passed`
  - `direnv exec . python -m compileall packages/python/foundation/src/xiuxian_foundation/runtime/gitops.py packages/python/foundation/src/xiuxian_foundation/runtime/__init__.py packages/python/foundation/src/xiuxian_foundation/__init__.py packages/python/foundation/tests/test_gitops.py packages/python/foundation/tests/unit/runtime/test_gitops_surface_removal.py`
  - `direnv exec . git diff --check`

### [DONE] Feature: downshift local cargo subprocess env helpers out of foundation

- **Objective**: remove another non-Flight Python runtime shell by moving the
  cargo subprocess env normalization helper out of `xiuxian_foundation.runtime`
  and into the local `skills/*` CLI surface that still owns those subprocesses.
- **Update**:
  - deleted `xiuxian_foundation.runtime.cargo_subprocess_env`
  - added `skills/_shared/cargo_subprocess_env.py` and `skills/__init__.py`
  - rewired the live git/researcher scripts and tests to import the helper
    from `skills._shared`
  - deleted the old foundation unit test module and replaced it with a removal
    guard locking the runtime helper out of foundation
- **Validation Executed**:
  - `direnv exec . uv run pytest skills/git/tests/test_smart_workflow_modular.py skills/researcher/tests/test_research_entry_qianji_env.py packages/python/foundation/tests/unit/runtime/test_cargo_subprocess_env_package_removal.py -q`
    - result: `18 passed`
  - `direnv exec . python -m compileall skills packages/python/foundation/tests/unit/runtime/test_cargo_subprocess_env_package_removal.py`
  - `direnv exec . git diff --check`

### [DONE] Feature: move retrieval optimization helpers out of foundation runtime

- **Objective**: remove another non-Flight Python runtime layer by relocating
  retrieval/query optimization helpers from `xiuxian_foundation.runtime` to
  the retrieval package that actually owns their behavior.
- **Update**:
  - deleted `xiuxian_foundation.runtime.runtime_optimization`
  - added `xiuxian_rag.retrieval.optimization`
  - rewired `xiuxian_rag.retrieval.postprocess` and
    `xiuxian_rag.retrieval.single_call` to the new local module
  - removed retrieval optimization exports from `xiuxian_foundation.runtime`
  - moved the functionality tests to the retrieval test slice and added a
    foundation runtime removal guard
- **Validation Executed**:
  - `direnv exec . uv run pytest packages/python/foundation/tests/unit/rag/test_retrieval_optimization.py packages/python/foundation/tests/unit/rag/test_retrieval_postprocess.py packages/python/foundation/tests/unit/rag/test_retrieval_single_call.py packages/python/foundation/tests/unit/runtime/test_runtime_optimization_package_removal.py -q`
    - result: `25 passed`
  - `direnv exec . python -m compileall packages/python/foundation/src/xiuxian_rag/retrieval/optimization.py packages/python/foundation/src/xiuxian_rag/retrieval/postprocess.py packages/python/foundation/src/xiuxian_rag/retrieval/single_call.py packages/python/foundation/src/xiuxian_foundation/runtime/__init__.py packages/python/foundation/tests/unit/rag/test_retrieval_optimization.py packages/python/foundation/tests/unit/runtime/test_runtime_optimization_package_removal.py`
  - `direnv exec . git diff --check`

### [DONE] Feature: remove foundation runtime isolation helpers

- **Objective**: delete another non-Flight Python runtime shell by removing the
  generic isolated script runner from foundation and keeping the minimal helper
  only where a local CLI skill still needs it.
- **Update**:
  - deleted `xiuxian_foundation.runtime.isolation`
  - deleted the old foundation isolation test module
  - added `skills/_shared/isolation.py`
  - rewired `skills/crawl4ai` to the local shared helper
  - removed `run_script_command` exports from `xiuxian_foundation.runtime` and
    `xiuxian_foundation.__init__`
  - added a focused foundation runtime removal guard for the deleted module
- **Validation Executed**:
  - `direnv exec . uv run pytest skills/crawl4ai/tests/test_crawl4ai_skill.py packages/python/foundation/tests/unit/runtime/test_isolation_package_removal.py -q`
    - result: `12 passed`
  - `direnv exec . python -m compileall skills/_shared/isolation.py skills/crawl4ai/scripts/crawl_url.py skills/crawl4ai/scripts/engine.py skills/crawl4ai/tests/test_crawl4ai_skill.py packages/python/foundation/src/xiuxian_foundation/runtime/__init__.py packages/python/foundation/src/xiuxian_foundation/__init__.py packages/python/foundation/tests/unit/runtime/test_isolation_package_removal.py`
  - `direnv exec . git diff --check`

### [DONE] Feature: delete the foundation runtime package wrapper

- **Objective**: finish the Python runtime teardown by removing the last
  `xiuxian_foundation.runtime` package shell and relocating the only retained
  helper, `get_project_root()`, into `xiuxian_foundation.config.prj`.
- **Update**:
  - added `get_project_root()` to `xiuxian_foundation.config.prj`
  - rewired the remaining foundation and skill callers/tests from
    `runtime.gitops` to `config.prj`
  - deleted `xiuxian_foundation.runtime.gitops`
  - deleted `xiuxian_foundation.runtime.__init__`
  - removed the now-empty `xiuxian_foundation/runtime/` directory
  - converted the remaining runtime removal tests to physical package guards
- **Validation Executed**:
  - `direnv exec . uv run pytest packages/python/foundation/tests/test_project_root.py packages/python/foundation/tests/unit/runtime/test_runtime_package_removal.py packages/python/foundation/tests/unit/runtime/test_gitops_surface_removal.py packages/python/foundation/tests/unit/runtime/test_path_filter_package_removal.py packages/python/foundation/tests/unit/runtime/test_skills_monitor_package_removal.py packages/python/foundation/tests/unit/services/test_contract_consistency.py packages/python/foundation/tests/unit/api/test_dual_layer_optimized.py skills/researcher/tests/test_research_entry_qianji_env.py skills/researcher/tests/test_researcher_modular.py -q`
    - result: `46 passed, 2 skipped`
  - `direnv exec . python -m compileall packages/python/foundation/src/xiuxian_foundation/config packages/python/foundation/src/xiuxian_foundation/utils packages/python/foundation/src/xiuxian_foundation/services packages/python/foundation/src/xiuxian_foundation/api skills/researcher skills/git/scripts packages/python/foundation/tests/test_project_root.py packages/python/foundation/tests/unit/runtime/test_runtime_package_removal.py packages/python/foundation/tests/unit/runtime/test_path_filter_package_removal.py packages/python/foundation/tests/unit/runtime/test_skills_monitor_package_removal.py packages/python/foundation/tests/unit/services/test_contract_consistency.py packages/python/foundation/tests/unit/api/test_dual_layer_optimized.py`
  - `direnv exec . git diff --check`

### [DONE] Feature: delete the redundant `utils.common` project-root wrapper

- **Objective**: remove the last generic Python wrapper around
  `get_project_root()` so retained path discovery lives only in
  `xiuxian_foundation.config.prj`.
- **Update**:
  - deleted `xiuxian_foundation.utils.common`
  - removed `project_root` from `xiuxian_foundation.utils` exports
  - removed the root-level lazy export path for the deleted wrapper
  - replaced the old unit test with a focused package-removal guard
- **Validation Executed**:
  - `direnv exec . uv run pytest packages/python/foundation/tests/unit/utils/test_common_package_removal.py -q`
    - result: `2 passed`
  - `direnv exec . python -m compileall packages/python/foundation/src/xiuxian_foundation/utils/__init__.py packages/python/foundation/src/xiuxian_foundation/__init__.py packages/python/foundation/tests/unit/utils/test_common_package_removal.py`
  - `direnv exec . git diff --check`

### [DONE] Feature: delete the `foundation.config.paths` bundle and DI hook

- **Objective**: remove the broad Python-side `ConfigPaths` helper surface and
  collapse active callers onto `get_project_root()` plus explicit CLI
  parameters.
- **Update**:
  - deleted `xiuxian_foundation.config.paths`
  - removed `ConfigPaths` and `get_config_paths` from `xiuxian_foundation.config`
  - removed `ConfigPaths` injection from `xiuxian_foundation.api.di` and
    schema-generation exclusions
  - rewired `advanced_tools` to take explicit `project_root` instead of a
    `paths` object
  - rewired remaining core/foundation callers to `get_project_root()`
  - replaced the old config-path unit test with a focused package-removal guard
- **Validation Executed**:
  - `direnv exec . uv run pytest packages/python/foundation/tests/unit/api/test_foundation_decorators.py packages/python/foundation/tests/unit/config/test_paths_package_removal.py skills/advanced_tools/tests/test_advanced_tools_modular.py -q`
    - result: `21 passed, 1 skipped`
  - `direnv exec . python -m compileall packages/python/foundation/src/xiuxian_foundation/api packages/python/foundation/src/xiuxian_foundation/config packages/python/core/src/xiuxian_core/context/providers.py skills/advanced_tools/scripts packages/python/foundation/tests/unit/config/test_paths_package_removal.py packages/python/foundation/tests/unit/api/test_foundation_decorators.py skills/advanced_tools/tests/test_advanced_tools_modular.py`
  - `direnv exec . git diff --check`

### [DONE] Feature: delete the orphaned API dependency-injection layer

- **Objective**: remove the last retained Python dependency-injection shell now
  that no active command surface uses `inject_resources`.
- **Update**:
  - deleted `xiuxian_foundation.api.di`
  - removed DI helper re-exports from `xiuxian_foundation.api.decorators`
  - replaced the old injection behavior assertion with negative export checks
  - added a focused package-removal guard for the deleted `api.di` module
- **Validation Executed**:
  - `direnv exec . uv run pytest packages/python/foundation/tests/unit/api/test_foundation_decorators.py packages/python/foundation/tests/unit/api/test_decorators.py packages/python/foundation/tests/unit/api/test_di_package_removal.py -q`
    - result: `8 passed`
  - `direnv exec . python -m compileall packages/python/foundation/src/xiuxian_foundation/api packages/python/foundation/tests/unit/api/test_foundation_decorators.py packages/python/foundation/tests/unit/api/test_decorators.py packages/python/foundation/tests/unit/api/test_di_package_removal.py`
  - `direnv exec . git diff --check`

### [DONE] Feature: delete foundation root config forwarding

- **Objective**: stop exposing config helpers from the `xiuxian_foundation`
  package root so retained Python callers must import from concrete config
  submodules.
- **Update**:
  - removed root-level forwarding for `PRJ_*`, `get_*_dir`, `get_setting`,
    `configure_logging`, and `get_project_root` from `xiuxian_foundation`
  - added a focused root-forwarding removal guard
  - kept `xiuxian_foundation.config` itself as the package namespace, but no
    longer present root convenience aliases
- **Validation Executed**:
  - `direnv exec . uv run pytest packages/python/foundation/tests/unit/config/test_root_config_forwarding_removal.py packages/python/foundation/tests/unit/config/test_directory_package_removal.py packages/python/foundation/tests/unit/config/test_database_package_removal.py -q`
    - result: `5 passed`
  - `direnv exec . python -m compileall packages/python/foundation/src/xiuxian_foundation/__init__.py packages/python/foundation/tests/unit/config/test_root_config_forwarding_removal.py packages/python/foundation/tests/unit/config/test_directory_package_removal.py packages/python/foundation/tests/unit/config/test_database_package_removal.py`
  - `direnv exec . git diff --check`

### [DONE] Feature: delete the config package convenience facade

- **Objective**: stop exposing broad helper symbols from
  `xiuxian_foundation.config` and require concrete submodule imports instead.
- **Update**:
  - collapsed `xiuxian_foundation.config.__init__` into a minimal package marker
  - rewired the remaining direct caller to import `get_project_root` and
    `get_setting` from concrete submodules
  - rewrote config-facade tests to use concrete `config.dirs` / `config.prj`
    imports
  - added a focused removal guard for helper-symbol re-exports from
    `xiuxian_foundation.config`
- **Validation Executed**:
  - `direnv exec . rg -n '^from xiuxian_foundation\\.config import ' packages/python skills -g '*.py'`
    - result: no hits
  - `direnv exec . uv run pytest packages/python/foundation/tests/unit/config/test_config_package_facade_removal.py packages/python/foundation/tests/unit/config/test_root_config_forwarding_removal.py packages/python/foundation/tests/unit/config/test_directory_package_removal.py packages/python/foundation/tests/unit/config/test_database_package_removal.py packages/python/foundation/tests/unit/api/test_dirs_forwarding_surface.py -q`
    - result: `9 passed`
  - `direnv exec . git diff --check`

### [DONE] Feature: collapse the foundation package root to a marker

- **Objective**: remove the remaining lazy helper facade from
  `xiuxian_foundation.__init__` so the package root no longer acts as a broad
  export surface.
- **Update**:
  - replaced `xiuxian_foundation.__init__` with a minimal package marker that
    only retains `__version__`
  - removed the old lazy forwarding for utils, workflow, service, rag, and
    embedding helpers
  - added a focused root-facade removal guard covering the deleted exports
- **Validation Executed**:
  - `direnv exec . uv run pytest packages/python/foundation/tests/unit/test_foundation_root_facade_removal.py packages/python/foundation/tests/unit/rag/test_graph_store_removal.py -q`
    - result: `3 passed`
  - `direnv exec . python -m compileall packages/python/foundation/src/xiuxian_foundation/__init__.py packages/python/foundation/tests/unit/test_foundation_root_facade_removal.py packages/python/foundation/tests/unit/rag/test_graph_store_removal.py`
  - `direnv exec . git diff --check`

### [DONE] Feature: collapse the `xiuxian_rag` package root to a marker

- **Objective**: remove the remaining `xiuxian_rag` root facade so retained
  Python callers import from concrete retrieval submodules instead of the
  package root.
- **Update**:
  - replaced `xiuxian_rag.__init__` with a minimal package marker
  - deleted the stale `xiuxian_rag/example.py` root-facade sample
  - rewired the remaining retrieval and tracer tests to concrete
    `xiuxian_rag.retrieval.*` imports
  - added a focused root-facade removal guard for the deleted package-root
    exports
- **Validation Executed**:
  - `direnv exec . uv run pytest packages/python/foundation/tests/unit/rag/test_retrieval_namespace.py packages/python/foundation/tests/unit/rag/test_retrieval_factory.py packages/python/foundation/tests/unit/rag/test_retrieval_node_factory.py packages/python/foundation/tests/unit/tracer/test_retrieval_invoker.py packages/python/foundation/tests/unit/rag/test_rag_root_facade_removal.py packages/python/foundation/tests/unit/rag/test_graph_store_removal.py -q`
    - result: `26 passed`
  - `direnv exec . python -m compileall packages/python/foundation/src/xiuxian_rag/__init__.py packages/python/foundation/tests/unit/rag/test_retrieval_namespace.py packages/python/foundation/tests/unit/rag/test_retrieval_factory.py packages/python/foundation/tests/unit/rag/test_retrieval_node_factory.py packages/python/foundation/tests/unit/tracer/test_retrieval_invoker.py packages/python/foundation/tests/unit/rag/test_rag_root_facade_removal.py`
  - `direnv exec . git diff --check`

### [DONE] Feature: collapse the `xiuxian_wendao_py` package root to a marker

- **Objective**: remove the remaining `xiuxian_wendao_py` root facade so the
  retained Python transport package exposes only concrete `transport` and
  `compat` modules.
- **Update**:
  - replaced `xiuxian_wendao_py.__init__` with a minimal package marker
  - rewired the retained transport and compat tests to import from concrete
    submodules
  - added a focused root-facade removal guard for the deleted package-root
    exports
- **Validation Executed**:
  - `direnv exec . uv run pytest packages/python/xiuxian-wendao-py/tests/test_transport.py packages/python/xiuxian-wendao-py/tests/test_compat_imports.py packages/python/xiuxian-wendao-py/tests/test_root_facade_removal.py -q`
    - result: `4 passed`
  - `direnv exec . python -m compileall packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py/__init__.py packages/python/xiuxian-wendao-py/tests/test_transport.py packages/python/xiuxian-wendao-py/tests/test_compat_imports.py packages/python/xiuxian-wendao-py/tests/test_root_facade_removal.py`
  - `direnv exec . git diff --check`

### [DONE] Feature: collapse the `xiuxian_wendao_py.compat` package root to a marker

- **Objective**: remove the remaining `xiuxian_wendao_py.compat` package-root
  facade while preserving the concrete `compat.config` and `compat.runtime`
  modules that still serve retained test-kit callers.
- **Update**:
  - replaced `xiuxian_wendao_py.compat.__init__` with a minimal package marker
  - rewired the retained compat import test to concrete `compat.config`
    imports
  - added a focused negative-export assertion proving the `compat` package
    root no longer re-exports config/runtime helpers
- **Validation Executed**:
  - `direnv exec . uv run pytest packages/python/xiuxian-wendao-py/tests/test_compat_imports.py -q`
    - result: `2 passed`
  - `direnv exec . python -m compileall packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py/compat/__init__.py packages/python/xiuxian-wendao-py/tests/test_compat_imports.py`
  - `direnv exec . rg -n '^from xiuxian_wendao_py\\.compat import ' packages/python skills -g '*.py'`
    - result: no hits
  - `direnv exec . git diff --check`

### [DONE] Feature: delete the retained `xiuxian_wendao_py.compat` package

- **Objective**: remove the remaining non-Flight `xiuxian_wendao_py.compat`
  implementation layer and push its surviving path/config callers onto
  `xiuxian_foundation.config.*`.
- **Update**:
  - deleted `xiuxian_wendao_py.compat.config`
  - deleted `xiuxian_wendao_py.compat.runtime`
  - deleted the now-empty `xiuxian_wendao_py.compat/` package directory
  - rewired the then-retained standalone Python test-helper surfaces,
    benchmark helpers, snapshot helpers, and benchmark/channel tests from
    `xiuxian_wendao_py.compat.*` to
    `xiuxian_foundation.config.prj` and `xiuxian_foundation.config.dirs`
  - rewrote the old compat test into a physical package-removal guard
- **Validation Executed**:
  - targeted archive-era compat-removal scans completed successfully during
    the original cut, with only the removal guard left in the historical scope
  - `direnv exec . uv run pytest packages/python/xiuxian-wendao-py/tests/test_transport.py packages/python/xiuxian-wendao-py/tests/test_compat_imports.py -q`
    - result: `3 passed`
  - targeted archive-era memory benchmark checks completed successfully during
    the original cut
  - `direnv exec . python -m compileall packages/python/xiuxian-wendao-py/tests/test_transport.py packages/python/xiuxian-wendao-py/tests/test_compat_imports.py`
  - `direnv exec . git diff --check`

### [DONE] Feature: shift the Python cleanup program from micro-slices to a staged RFC boundary

- **Objective**: stop driving the remaining Python work as isolated cleanup and
  instead bind it to a phase-based Arrow/Flight boundary program.
- **Update**:
  - added RFC `docs/rfcs/2026-03-29-python-arrow-flight-boundary-rfc.md`
  - rewrote the active Python ExecPlan so it now tracks macro phases
    `PY-P1` through `PY-P4`, phase deliverables, and stop conditions
  - aligned the plan with the active Wendao Arrow/Flight blueprint and the
    existing core/runtime plugin migration RFCs
- **Validation Executed**:
  - `direnv exec . rg -n 'Python Arrow Flight Boundary|macro phase|PY-P1|PY-P2|PY-P3|PY-P4' docs/rfcs .cache/codex/execplans/python-arrow-flight-redesign.md -g '*.md'`
    - result: RFC and ExecPlan both expose the staged phase language
  - `direnv exec . git diff --check`

### [DONE] PY-F1: land the first real Python Arrow Flight client seam

- **Objective**: move `xiuxian-wendao-py` beyond transport descriptors and
  land an actual `pyarrow.flight` connection surface.
- **Update**:
  - added `pyarrow>=14.0.0` to `packages/python/xiuxian-wendao-py`
  - extended `WendaoTransportClient` with:
    - `flight_location()`
    - `flight_call_options()`
    - `connect_flight()`
  - kept the scope narrow: real Flight connection setup only, no query verbs
    yet
  - updated `xiuxian-wendao-py` README and tests to reflect the new live seam
- **Validation Executed**:
  - `direnv exec . uv run pytest packages/python/xiuxian-wendao-py/tests/test_transport.py -q`
    - result: `4 passed`
  - `direnv exec . python -m compileall packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py packages/python/xiuxian-wendao-py/tests/test_transport.py`
  - `direnv exec . git diff --check`

### [DONE] PY-F2: land the first Flight `do_get` table-read path

- **Objective**: move the Python Flight client from connection-only setup to a
  minimal readable data path.
- **Update**:
  - added `make_ticket()` to build `pyarrow.flight.Ticket` values from text or
    raw bytes
  - added `read_table()` to execute `do_get(...)` and return a `pyarrow.Table`
  - kept the scope narrow: generic table reads only, still no higher-level
    Wendao query verbs
  - extended `xiuxian-wendao-py` tests and README to cover the first readable
    Flight path
- **Validation Executed**:
  - `direnv exec . uv run pytest packages/python/xiuxian-wendao-py/tests/test_transport.py -q`
    - result: `6 passed`
  - `direnv exec . git diff --check`

### [DONE] PY-F3: align the Python Flight client to the Rust route contract

- **Objective**: align the Python Flight client to the Rust-owned route-backed
  descriptor contract instead of stopping at generic `Ticket -> Table` reads.
- **Update**:
  - added `schema_version` plus `x-wendao-schema-version` metadata handling to
    `packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py/transport/config.py`
    and `packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py/transport/client.py`
  - added `flight_route()` and `flight_descriptor()` so Python builds
    route-backed `FlightDescriptor.for_path(...)` values the same way the Rust
    transport does
  - added `get_flight_info()` so the Python client can resolve a descriptor
    contract before executing data reads
  - extended `packages/python/xiuxian-wendao-py/tests/test_transport.py` and
    the package README to cover the descriptor and Flight-info path
- **Validation Executed**:
  - `direnv exec . uv run pytest packages/python/xiuxian-wendao-py/tests/test_transport.py -q`
  - `direnv exec . python -m compileall packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py packages/python/xiuxian-wendao-py/tests/test_transport.py`
  - `direnv exec . git diff --check`

### [DONE] PY-F4: add the first analyzer-plugin authoring seam on top of Arrow Flight

- **Objective**: reduce downstream Python analyzer work so plugin authors only
  implement Arrow-table analysis logic while `xiuxian-wendao-py` owns Flight
  descriptor, metadata, and fetch plumbing.
- **Update**:
  - added `packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py/transport/query.py`
    with a typed `WendaoFlightRouteQuery`
  - extended the transport client with `flight_descriptor_for_query(...)`,
    `get_query_info(...)`, and `read_query_table(...)`
  - added
    `packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py/analyzer.py`
    with:
    - `WendaoAnalyzerContext`
    - `WendaoAnalyzer`
    - `run_analyzer(...)`
  - restored an intentional public package-root API in
    `packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py/__init__.py`
    so downstream plugin authors can import one narrow SDK directly from
    `xiuxian_wendao_py`
  - updated the package README to show the intended authoring model:
    write an analyzer callable, then let `run_analyzer(...)` handle Flight I/O
- **Validation Executed**:
  - `direnv exec . uv run pytest packages/python/xiuxian-wendao-py/tests/test_transport.py packages/python/xiuxian-wendao-py/tests/test_analyzer.py -q`
    - result: `14 passed`
  - `direnv exec . python -m compileall packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py packages/python/xiuxian-wendao-py/tests/test_transport.py packages/python/xiuxian-wendao-py/tests/test_analyzer.py`
  - `direnv exec . git diff --check`

### [DONE] PY-F5: add the first plugin binding scaffold for downstream Python analyzers

- **Objective**: let downstream Python analyzer authors advertise one
  Rust-aligned plugin binding without hand-assembling capability/endpoint/
  transport records.
- **Update**:
  - added `packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py/plugin.py`
    with:
    - `PluginTransportKind`
    - `PluginProviderSelector`
    - `PluginTransportEndpoint`
    - `PluginCapabilityBinding`
    - `build_arrow_flight_binding(...)`
    - `WendaoAnalyzerPlugin`
  - re-exported the narrow plugin scaffold from the package root so downstream
    authors can import one stable SDK from `xiuxian_wendao_py`
  - updated the package README with a binding-and-run example for one analyzer
    plugin
  - added `packages/python/xiuxian-wendao-py/tests/test_plugin.py` to verify
    the generated binding contract and the analyzer-plugin run path
- **Validation Executed**:
  - `direnv exec . uv run pytest packages/python/xiuxian-wendao-py/tests/test_transport.py packages/python/xiuxian-wendao-py/tests/test_analyzer.py packages/python/xiuxian-wendao-py/tests/test_plugin.py -q`
    - result: `16 passed`
  - `direnv exec . python -m compileall packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py packages/python/xiuxian-wendao-py/tests/test_transport.py packages/python/xiuxian-wendao-py/tests/test_analyzer.py packages/python/xiuxian-wendao-py/tests/test_plugin.py`
  - `direnv exec . git diff --check`

### [DONE] PY-F6: add the first downstream plugin project scaffold

- **Objective**: let downstream Python analyzer authors start from a stable
  manifest plus project skeleton instead of hand-writing package layout and
  plugin metadata.
- **Update**:
  - added
    `packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py/scaffold.py`
    with:
    - `WendaoAnalyzerPluginManifest`
    - `scaffold_analyzer_plugin(...)`
  - the scaffold now generates a minimal:
    - `plugin.toml`
    - `pyproject.toml`
    - `src/<package>/__init__.py`
    - `src/<package>/analyzer.py`
  - re-exported the scaffold API from `xiuxian_wendao_py`
  - updated the package README with a concrete scaffold example
  - added `packages/python/xiuxian-wendao-py/tests/test_scaffold.py`
- **Validation Executed**:
  - `direnv exec . uv run pytest packages/python/xiuxian-wendao-py/tests/test_transport.py packages/python/xiuxian-wendao-py/tests/test_analyzer.py packages/python/xiuxian-wendao-py/tests/test_plugin.py packages/python/xiuxian-wendao-py/tests/test_scaffold.py -q`
    - result: `18 passed`
  - `direnv exec . python -m compileall packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py packages/python/xiuxian-wendao-py/tests/test_transport.py packages/python/xiuxian-wendao-py/tests/test_analyzer.py packages/python/xiuxian-wendao-py/tests/test_plugin.py packages/python/xiuxian-wendao-py/tests/test_scaffold.py`
  - `direnv exec . git diff --check`

### [DONE] PY-F7: add a local dev-loop scaffold for downstream analyzer projects

- **Objective**: make generated analyzer-plugin projects runnable locally
  instead of stopping at static manifest and package files.
- **Update**:
  - extended `scaffold_analyzer_plugin(...)` so generated projects now include:
    - one `project.scripts` entry in `pyproject.toml`
    - one `src/<package>/cli.py`
  - the generated CLI reads:
    - `WENDAO_HOST`
    - `WENDAO_PORT`
    - `WENDAO_ROUTE`
  - the generated CLI builds a `WendaoAnalyzerPlugin`, runs it through the
    Arrow Flight path, and prints JSON output
  - updated the package README so the public scaffold story now includes the
    local dev loop
- **Validation Executed**:
  - `direnv exec . uv run pytest packages/python/xiuxian-wendao-py/tests/test_scaffold.py packages/python/xiuxian-wendao-py/tests/test_plugin.py packages/python/xiuxian-wendao-py/tests/test_analyzer.py packages/python/xiuxian-wendao-py/tests/test_transport.py -q`
    - result: `18 passed`
  - `direnv exec . python -m compileall packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py packages/python/xiuxian-wendao-py/tests/test_scaffold.py packages/python/xiuxian-wendao-py/tests/test_plugin.py packages/python/xiuxian-wendao-py/tests/test_analyzer.py packages/python/xiuxian-wendao-py/tests/test_transport.py`
  - `direnv exec . git diff --check`

### [DONE] PY-F8: add a scaffold writer CLI for downstream analyzer projects

- **Objective**: let downstream authors materialize the generated analyzer
  project directly from `xiuxian-wendao-py` instead of manually copying the
  scaffolded file map.
- **Update**:
  - extended
    `packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py/scaffold.py`
    with:
    - `write_scaffold_project(...)`
    - `main(...)`
  - added a packaged script entry in
    `packages/python/xiuxian-wendao-py/pyproject.toml`:
    - `xiuxian-wendao-scaffold = "xiuxian_wendao_py.scaffold:main"`
  - re-exported the scaffold writer surface from the package root
  - updated the package README with the new CLI usage
  - extended `packages/python/xiuxian-wendao-py/tests/test_scaffold.py` to
    verify:
    - file writes
    - overwrite protection
    - CLI argument handling
- **Validation Executed**:
  - `direnv exec . uv run pytest packages/python/xiuxian-wendao-py/tests/test_scaffold.py -q`
    - result: `5 passed`
  - `direnv exec . python -m compileall packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py packages/python/xiuxian-wendao-py/tests/test_scaffold.py`
  - `direnv exec . python - <<'PY' ... tomllib.load(pyproject.toml) ... PY`
    - result: `xiuxian_wendao_py.scaffold:main`
  - `direnv exec . git diff --check`

### [DONE] PY-F9: add offline replay for downstream analyzer development

- **Objective**: let downstream Python analyzer authors iterate on analyzer
  logic before a live Arrow Flight service is available.
- **Update**:
  - extended
    `packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py/analyzer.py`
    with:
    - `run_analyzer_with_table(...)`
    - `run_analyzer_with_rows(...)`
  - widened `WendaoAnalyzerContext.client` to `WendaoTransportClient | None`
    so offline replay remains first-class instead of pretending a live client
    exists
  - extended generated scaffold CLIs to honor `WENDAO_SAMPLE_JSON` and run the
    analyzer against local row payloads without Flight
  - updated the package README with the offline replay flow
- **Validation Executed**:
  - `direnv exec . uv run pytest packages/python/xiuxian-wendao-py/tests/test_analyzer.py packages/python/xiuxian-wendao-py/tests/test_scaffold.py -q`
    - result: `9 passed`
  - `direnv exec . python -m compileall packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py packages/python/xiuxian-wendao-py/tests/test_analyzer.py packages/python/xiuxian-wendao-py/tests/test_scaffold.py`
  - `direnv exec . git diff --check`

### [DONE] PY-F10: fix a stable sample-row contract for offline analyzer replay

- **Objective**: stop treating offline replay as “any JSON file” and give
  downstream analyzer authors one stable sample input contract out of the box.
- **Update**:
  - added `default_sample_rows()` to
    `packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py/scaffold.py`
  - scaffolded projects now always include `sample_rows.json`
  - the generated sample uses a stable row shape with:
    - `id`
    - `path`
    - `title`
    - `score`
    - `language`
  - re-exported `default_sample_rows` from the package root
  - updated the package README so offline replay now points at the generated
    sample contract, not an unspecified JSON file
- **Validation Executed**:
  - `direnv exec . uv run pytest packages/python/xiuxian-wendao-py/tests/test_scaffold.py -q`
    - result: `6 passed`
  - `direnv exec . python -m compileall packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py packages/python/xiuxian-wendao-py/tests/test_scaffold.py`
  - `direnv exec . git diff --check`

### [DONE] PY-F11: add selectable sample templates for analyzer categories

- **Objective**: avoid forcing every downstream analyzer to start from the same
  replay payload shape.
- **Update**:
  - added `WendaoSampleTemplate` plus `sample_rows_for_template(...)` to
    `packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py/scaffold.py`
  - scaffolded projects can now choose from:
    - `docs_retrieval`
    - `repo_search`
    - `code_symbol`
  - `scaffold_analyzer_plugin(...)`, `write_scaffold_project(...)`, and the
    `xiuxian-wendao-scaffold` CLI now all accept the selected sample template
  - re-exported `WendaoSampleTemplate` and `sample_rows_for_template(...)`
    from the package root
  - updated README examples to show `--sample-template repo_search`
- **Validation Executed**:
  - `direnv exec . uv run pytest packages/python/xiuxian-wendao-py/tests/test_scaffold.py -q`
    - result: `9 passed`
  - `direnv exec . python -m compileall packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py packages/python/xiuxian-wendao-py/tests/test_scaffold.py`
  - `direnv exec . git diff --check`

### [DONE] PY-F12: push sample templates closer to analyzer starter profiles

- **Objective**: move beyond one generic replay sample and let downstream
  analyzer authors start from category-shaped payloads that better match real
  plugin use cases.
- **Update**:
  - added `WendaoSampleTemplate` and `sample_rows_for_template(...)` to
    `packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py/scaffold.py`
  - the scaffold now supports three starter profiles:
    - `docs_retrieval`
    - `repo_search`
    - `code_symbol`
  - `scaffold_analyzer_plugin(...)`, `write_scaffold_project(...)`, and the
    packaged scaffold CLI now all accept a selected template
  - re-exported the template and generator from the package root
  - updated README examples to show `--sample-template repo_search`
- **Validation Executed**:
  - `direnv exec . uv run pytest packages/python/xiuxian-wendao-py/tests/test_scaffold.py -q`
    - result: `10 passed`
  - `direnv exec . python -m compileall packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py packages/python/xiuxian-wendao-py/tests/test_scaffold.py`
  - `direnv exec . git diff --check`

### [DONE] PY-F13: make generated analyzer code follow the selected starter profile

- **Objective**: stop generating one generic `analyze()` body for every plugin
  template and start giving downstream authors a closer starting point.
- **Update**:
  - added `analyzer_body_for_template(...)` to
    `packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py/scaffold.py`
  - generated `analyzer.py` now changes with the selected template:
    - `docs_retrieval` starts with title/language summarization
    - `repo_search` starts with repo/language/top-path summarization
    - `code_symbol` starts with symbol/kind summarization
  - re-exported `analyzer_body_for_template(...)` from the package root
  - updated README to call out template-shaped starter analyzer logic
- **Validation Executed**:
  - `direnv exec . uv run pytest packages/python/xiuxian-wendao-py/tests/test_scaffold.py -q`
    - result: `10 passed`
  - `direnv exec . python -m compileall packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py packages/python/xiuxian-wendao-py/tests/test_scaffold.py`
  - `direnv exec . git diff --check`

### [DONE] PY-F14: add profile-driven scaffold defaults for downstream analyzer authors

- **Objective**: let downstream authors start from one named analyzer profile
  instead of manually coordinating capability IDs, routes, sample templates,
  and starter code shape.
- **Update**:
  - added `WendaoAnalyzerProfile` plus `profile_defaults(...)` to
    `packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py/scaffold.py`
  - the scaffold CLI now accepts `--profile` and derives:
    - `capability_id`
    - Flight route
    - replay sample template
    - starter `analyzer.py` body
  - re-exported the profile surface from the package root
  - updated README examples so `--profile repo_search` becomes the primary
    scaffold path
- **Validation Executed**:
  - `direnv exec . uv run pytest packages/python/xiuxian-wendao-py/tests/test_scaffold.py -q`
  - `direnv exec . python -m compileall packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py packages/python/xiuxian-wendao-py/tests/test_scaffold.py`
  - `direnv exec . git diff --check`

### [DONE] PY-F15: add mock-Flight replay mode for local analyzer development

- **Objective**: let downstream analyzer authors iterate against a Flight-like
  context before wiring a live Wendao Flight service.
- **Update**:
  - added `build_mock_flight_info(...)` and
    `run_analyzer_with_mock_rows(...)` to
    `packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py/analyzer.py`
  - scaffolded `cli.py` now supports `WENDAO_MOCK_FLIGHT=1`
  - mock mode replays bundled or explicit sample rows while still providing
    stable `flight_info` shape in analyzer context
  - updated README to document the mock-Flight local dev path
- **Validation Executed**:
  - `direnv exec . uv run pytest packages/python/xiuxian-wendao-py/tests/test_analyzer.py packages/python/xiuxian-wendao-py/tests/test_scaffold.py -q`
  - `direnv exec . python -m compileall packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py packages/python/xiuxian-wendao-py/tests/test_analyzer.py packages/python/xiuxian-wendao-py/tests/test_scaffold.py`
  - `direnv exec . git diff --check`

### [DONE] PY-F16: validate replay rows against the selected analyzer profile

- **Objective**: fail fast when downstream authors feed replay rows that do not
  match the chosen analyzer profile or sample template.
- **Update**:
  - added `required_fields_for_template(...)`,
    `validate_rows_against_template(...)`, and
    `validate_rows_against_profile(...)` to
    `packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py/scaffold.py`
  - scaffolded `cli.py` now validates replay rows before mock/offline analyzer
    execution
  - re-exported the validation helpers from the package root
  - updated README to describe replay-row contract validation as part of the
    starter profile workflow
- **Validation Executed**:
  - `direnv exec . uv run pytest packages/python/xiuxian-wendao-py/tests/test_scaffold.py -q`
  - `direnv exec . python -m compileall packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py packages/python/xiuxian-wendao-py/tests/test_scaffold.py`
  - `direnv exec . git diff --check`

### [DONE] PY-F17: make starter profiles shape plugin manifest metadata

- **Objective**: make the generated `plugin.toml` itself carry profile-linked
  metadata instead of leaving starter semantics implicit in CLI defaults.
- **Update**:
  - extended `WendaoAnalyzerPluginManifest` with optional starter metadata
  - added `profile_manifest_metadata(...)` to
    `packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py/scaffold.py`
  - scaffold generation now writes a `[starter]` block containing:
    - profile
    - sample template
    - display name
    - summary
    - tags
  - updated README to document the profile-linked manifest metadata
- **Validation Executed**:
  - `direnv exec . uv run pytest packages/python/xiuxian-wendao-py/tests/test_scaffold.py -q`
  - `direnv exec . python -m compileall packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py packages/python/xiuxian-wendao-py/tests/test_scaffold.py`
  - `direnv exec . git diff --check`

### [DONE] PY-F18: carry starter profile metadata into runtime binding payloads

- **Objective**: keep starter-profile semantics visible at runtime registration
  instead of limiting them to scaffold-time files.
- **Update**:
  - extended `PluginCapabilityBinding` with a dedicated `starter` payload
  - extended `build_arrow_flight_binding(...)` and `WendaoAnalyzerPlugin` to
    pass starter metadata through the runtime binding contract
  - updated README to call out that starter metadata now survives into runtime
    registration payloads as well as `plugin.toml`
- **Validation Executed**:
  - `direnv exec . uv run pytest packages/python/xiuxian-wendao-py/tests/test_plugin.py -q`
  - `direnv exec . python -m compileall packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py packages/python/xiuxian-wendao-py/tests/test_plugin.py`
  - `direnv exec . git diff --check`

### [DONE] PY-F19: build runtime plugins directly from named starter profiles

- **Objective**: remove the last manual duplication between scaffold-time
  starter metadata and runtime `WendaoAnalyzerPlugin(...)` construction.
- **Update**:
  - added `build_starter_payload(...)` to derive runtime starter metadata from
    a named `WendaoAnalyzerProfile`
  - added `build_profiled_analyzer_plugin(...)` so downstream authors can
    construct a runtime plugin directly from a profile without hand-writing the
    `starter={...}` payload
  - re-exported both helpers from the package root
  - updated README to describe the direct profile-to-runtime-plugin path
- **Validation Executed**:
  - `direnv exec . uv run pytest packages/python/xiuxian-wendao-py/tests/test_plugin.py -q`
  - `direnv exec . python -m compileall packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py packages/python/xiuxian-wendao-py/tests/test_plugin.py`
  - `direnv exec . git diff --check`

### [DONE] PY-F20: build runtime plugins directly from scaffolded manifests

- **Objective**: close the last gap between generated `plugin.toml` files and
  runtime plugin construction so manifests are not generation-only artifacts.
- **Update**:
  - added `load_plugin_manifest(...)` to load scaffolded TOML manifests
  - added `starter_from_manifest(...)` to recover starter metadata from parsed
    manifests
  - added `plugin_from_manifest(...)` to build `WendaoAnalyzerPlugin(...)`
    directly from manifest data plus an analyzer callable
  - re-exported the manifest-driven helpers from the package root
  - updated README to document the manifest-to-runtime path
- **Validation Executed**:
  - `direnv exec . uv run pytest packages/python/xiuxian-wendao-py/tests/test_plugin.py -q`
  - `direnv exec . python -m compileall packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py packages/python/xiuxian-wendao-py/tests/test_plugin.py`
  - `direnv exec . git diff --check`

### [DONE] PY-F21: load scaffolded analyzer entrypoints directly from manifest-backed projects

- **Objective**: remove the last manual import step between a scaffolded
  project directory and a runnable runtime plugin.
- **Update**:
  - added `load_manifest_entrypoint(...)` to resolve and import the analyzer
    callable referenced by a scaffolded manifest
  - extended `plugin_from_manifest(...)` so it can build a runtime plugin from
    manifest plus `project_root`, without requiring the caller to pass an
    analyzer callable explicitly
  - added a scaffold-to-manifest-to-runtime end-to-end smoke test covering the
    generated repo-search project path
  - updated README to call out the manifest-backed entrypoint loading flow
- **Validation Executed**:
  - `direnv exec . uv run pytest packages/python/xiuxian-wendao-py/tests/test_plugin.py -q`
  - `direnv exec . python -m compileall packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py packages/python/xiuxian-wendao-py/tests/test_plugin.py`
  - `direnv exec . git diff --check`

### [DONE] PY-F22: validate plugin manifest contract during manifest-driven runtime loading

- **Objective**: make manifest-backed runtime loading fail early on malformed
  `plugin.toml` files instead of surfacing unclear downstream errors.
- **Update**:
  - added `validate_plugin_manifest(...)` to
    `packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py/plugin.py`
  - `load_plugin_manifest(...)`, `load_manifest_entrypoint(...)`, and
    `plugin_from_manifest(...)` now all pass through the same explicit
    manifest validator
  - validator covers:
    - required `[plugin]` and `[entrypoint]` sections
    - string types for key fields
    - supported transport values
    - starter metadata shape when present
  - updated README to call out fail-fast manifest validation
- **Validation Executed**:
  - `direnv exec . uv run pytest packages/python/xiuxian-wendao-py/tests/test_plugin.py -q`
  - `direnv exec . python -m compileall packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py packages/python/xiuxian-wendao-py/tests/test_plugin.py`
  - `direnv exec . git diff --check`

### [DONE] PY-F23: add scaffolded-project CLI smoke test for mock-Flight execution

- **Objective**: verify that the generated project is executable as a real CLI
  entrypoint, not just structurally correct as generated files.
- **Update**:
  - added a smoke test that:
    - scaffolds a repo-search analyzer project
    - runs `python -m <generated_package>.cli`
    - sets `WENDAO_MOCK_FLIGHT=1`
    - asserts the JSON output from the generated analyzer
  - updated README to call out the generated-project executable contract
- **Validation Executed**:
  - `direnv exec . uv run pytest packages/python/xiuxian-wendao-py/tests/test_scaffold.py -q`
  - `direnv exec . python -m compileall packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py packages/python/xiuxian-wendao-py/tests/test_scaffold.py`
  - `direnv exec . git diff --check`

### [DONE] PY-F24: make the generated project CLI manifest-driven

- **Objective**: eliminate the last hand-wired runtime glue inside scaffolded
  projects so downstream authors can treat `plugin.toml` as the single source
  of truth for route and entrypoint changes.
- **Update**:
  - updated the generated `cli.py` template in
    `packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py/scaffold.py` so
    it now:
    - loads `plugin.toml` through `load_plugin_manifest(...)`
    - derives the default Flight route from `plugin.route`
    - resolves the analyzer callable via `load_manifest_entrypoint(...)`
    - builds the runtime plugin via `plugin_from_manifest(...)`
  - removed the remaining scaffold-time `WendaoAnalyzerPlugin(...)` hand-build
    from the generated CLI path
  - added a smoke test proving that editing `plugin.toml` route changes the
    generated CLI behavior in mock-Flight mode
  - updated the package README to document the manifest-driven generated CLI
- **Validation Executed**:
  - `direnv exec . uv run pytest packages/python/xiuxian-wendao-py/tests/test_scaffold.py -q`
    - result: `19 passed`
  - `direnv exec . python -m compileall packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py packages/python/xiuxian-wendao-py/tests/test_scaffold.py`
  - `direnv exec . git diff --check`

### [DONE] PY-F25: add Python Arrow Flight `do_exchange` transport support

- **Objective**: align the Python transport surface with the Rust-owned Flight
  reality by supporting the already-landed `do_exchange` batch/table seam, not
  only read-only `do_get`.
- **Update**:
  - added `exchange_table(...)` and `exchange_query_table(...)` to
    `packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py/transport/client.py`
  - the new helpers now:
    - build a route-backed `FlightDescriptor`
    - call `FlightClient.do_exchange(...)`
    - stream one request table through the Flight writer
    - read one response table back through the Flight reader
  - added transport tests that lock descriptor path, schema-version metadata,
    request timeout, and request-table propagation for both config-route and
    typed-query paths
  - updated README with the new Rust-aligned `do_exchange(...)` example using
    `/rerank/flight`
- **Validation Executed**:
  - `direnv exec . uv run pytest packages/python/xiuxian-wendao-py/tests/test_transport.py -q`
    - result: `14 passed`
  - `direnv exec . python -m compileall packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py packages/python/xiuxian-wendao-py/tests/test_transport.py`
  - `direnv exec . git diff --check`

### [DONE] PY-F26: prove Python `do_exchange` against a real Rust Flight server

- **Objective**: close the remaining cross-runtime gap so Python `do_exchange`
  support is not only verified through Python-side fakes.
- **Update**:
  - added
    `packages/rust/crates/xiuxian-wendao-runtime/examples/mock_flight_exchange_server.rs`
    as a minimal Rust Arrow Flight `do_exchange` server example
  - added an integration test in
    `packages/python/xiuxian-wendao-py/tests/test_transport.py` that:
    - launches the built Rust example binary
    - waits for readiness
    - sends one Arrow table via `exchange_table(...)`
    - asserts the returned row payload from the Rust server
  - updated README to note that the Python `do_exchange(...)` seam is now
    verified against a real Rust process
- **Validation Executed**:
  - `direnv exec . cargo build -p xiuxian-wendao-runtime --features julia --example mock_flight_exchange_server`
  - `direnv exec . uv run pytest packages/python/xiuxian-wendao-py/tests/test_transport.py -q`
    - result: `15 passed`
  - `direnv exec . python -m compileall packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py packages/python/xiuxian-wendao-py/tests/test_transport.py`
  - `direnv exec . git diff --check`

### [DONE] PY-F27: prove Python Flight query reads against a real Rust Flight server

- **Objective**: close the remaining query-side cross-runtime gap so Python
  `get_flight_info(...)` and `do_get(...)` are verified against a live
  Rust-owned Flight route instead of only Python-side fakes.
- **Update**:
  - extended
    `packages/rust/crates/xiuxian-wendao-runtime/examples/mock_flight_exchange_server.rs`
    so the same Rust example now also serves:
    - `get_flight_info(...)` for `/search/repos/main`
    - `do_get(...)` with a route-backed ticket
    - schema-version header validation for query reads
  - added a Python integration test in
    `packages/python/xiuxian-wendao-py/tests/test_transport.py` that:
    - launches the Rust example binary
    - calls `get_query_info(...)`
    - calls `read_query_table(...)`
    - asserts descriptor path, returned ticket, and row payload
  - tightened the Rust example `do_exchange(...)` handler to drain the inbound
    request stream before returning a response table, so query and exchange
    smoke tests pass against one shared live server example
  - updated the package README to record that both query-side and exchange-side
    Flight paths are now verified against a real Rust process
- **Validation Executed**:
  - `direnv exec . env CARGO_TARGET_DIR=$PRJ_ROOT/.cache/pyflight-rust-example-target-q1 cargo build -p xiuxian-wendao-runtime --features julia --example mock_flight_exchange_server`
  - `direnv exec . env WENDAO_MOCK_SERVER_BINARY=$PRJ_ROOT/.cache/pyflight-rust-example-target-q1/debug/examples/mock_flight_exchange_server uv run pytest packages/python/xiuxian-wendao-py/tests/test_transport.py -q`
    - result: `16 passed`
  - `direnv exec . python -m compileall packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py packages/python/xiuxian-wendao-py/tests/test_transport.py`
  - `direnv exec . git diff --check`

### [DONE] PY-P3/PY-P4: retire the standalone Python test-helper surface

- **Objective**: complete the delete-first retirement of the standalone Python
  test-helper package and all of its package-level helper facades.
- **Update**:
  - removed the former decorator, plugin, gitops, logging, benchmark, and
    snapshot helper modules instead of preserving them as compatibility shims
  - deleted the non-Arrow fixture slices for knowledge, AST, temp files,
    link-graph, project/git helpers, memory protection, vector payloads, and
    the final Arrow fixture facade
  - collapsed and then removed the remaining `fixtures` package shell
  - deleted the data-driven plugin surface and the remaining package/plugin
    wrapper logic
  - retired the standalone package itself, removed it from workspace/config
    ownership, deleted the old removal-guard tail, and moved surviving live
    tests into `scripts/channel/`, `scripts/tests/`, and
    `packages/python/foundation/tests/unit/services/`
- **Validation Executed**:
  - phase-local verification was completed during each deletion cut
  - final redistribution verification:
    - `direnv exec . uv run pytest scripts/channel/test_agent_channel_blackbox.py scripts/channel/test_agent_channel_command_events.py scripts/channel/test_agent_channel_session_matrix.py scripts/tests/test_llm_fingerprint_audit.py packages/python/foundation/tests/unit/services/test_arrow_fixtures.py -q`
      - result: `115 passed`
    - `direnv exec . python -m compileall scripts/channel scripts/tests packages/python/foundation/tests/unit/services`
    - `direnv exec . rg -n '<retired-standalone-test-surface>' pyproject.toml scripts packages/python/foundation/tests/unit/services -g '*.py' -g '*.md' -g '*.toml'`
      - result: no active-surface hits
    - `direnv exec . git diff --check`

- **Known Existing Failure**:
  - `direnv exec . uv run pytest skills/code/tests/test_standalone.py -q`
    - still fails with `ModuleNotFoundError: No module named 'code.scripts'; 'code' is not a package`
    - this is a preexisting standalone skill test import-path problem, not a regression from the Python boundary deletions
