---
type: knowledge
metadata:
  title: "GTD: Sovereign Nexus (2026-03-18)"
---

# GTD: Sovereign Nexus (2026-03-18)

## Current Delivery

### [DONE] Feature: Wendao OpenAI-Compatible Semantic Ignition Runtime Wiring

- **Objective**: Carry the existing OpenAI-compatible semantic ignition bridge into Wendao's real planned-search payload path so local GLM/OpenAI-compatible embedding gateways can enrich live retrieval output.
- **Status**: [Landed] Runtime config now resolves `link_graph.retrieval.semantic_ignition.*` and matching environment variables, planned-search payloads now emit `semantic_ignition` telemetry plus `quantum_contexts`, HTTP/RPC/XML-Lite surfaces render the new retrieval context, and text-only semantic ignition requests no longer get dropped when `query_vector` is empty.
- **Validation**:
  - `direnv exec /Users/guangtao/ghq/github.com/tao3k/omni-dev-fusion cargo test -p xiuxian-wendao --lib link_graph::runtime_config::tests::`
  - `direnv exec /Users/guangtao/ghq/github.com/tao3k/omni-dev-fusion env CARGO_TARGET_DIR=/tmp/omni-dev-fusion-codex-semantic-ignition-target cargo test -p xiuxian-wendao --test planned_search_semantic_ignition`
  - `direnv exec /Users/guangtao/ghq/github.com/tao3k/omni-dev-fusion env CARGO_TARGET_DIR=/tmp/omni-dev-fusion-codex-semantic-ignition-target cargo test -p xiuxian-wendao --test quantum_fusion_openai_ignition`
  - `direnv exec /Users/guangtao/ghq/github.com/tao3k/omni-dev-fusion env CARGO_TARGET_DIR=/tmp/omni-dev-fusion-codex-semantic-ignition-target cargo test -p xiuxian-wendao --lib zhenfa_router::rpc::tests::render_markdown_includes_hits -- --exact`
  - `direnv exec /Users/guangtao/ghq/github.com/tao3k/omni-dev-fusion env CARGO_TARGET_DIR=/tmp/omni-dev-fusion-codex-semantic-ignition-target cargo clippy -p xiuxian-wendao --tests -- -D warnings`

### [DONE] Feature: Studio Markdown AST Search for Qianji Docs

- **Objective**: Include configured Markdown docs in Wendao Studio AST search and surface the results in Qianji Studio search.
- **Status**: [Landed] `qianji-studio/docs` now contributes Markdown section, task, property drawer, and `:OBSERVE:` nodes to `/api/search/ast`, `SearchBar` renders the hits with Markdown-aware labels, and duplicate doc roots now resolve through project-scoped aliases such as `main/docs/...` instead of numeric suffixes.
- **Validation**:
  - `direnv exec /Users/guangtao/ghq/github.com/tao3k/omni-dev-fusion cargo test -p xiuxian-wendao gateway::studio::search::`
  - `direnv exec /Users/guangtao/ghq/github.com/tao3k/omni-dev-fusion npm --prefix .data/qianji-studio test -- src/components/SearchBar/__tests__/SearchBar.test.tsx`

### [DONE] Feature: Studio Backend-First Search and Bilink Navigation Tightening

- **Objective**: Remove remaining Qianji Studio path/category inference by making Wendao gateway own search selections, graph selections, and bilink fallback resolution.
- **Status**: [Landed] `search_knowledge` now emits `navigationTarget`, `SearchBar` graph actions now pass full backend-ready selections, App bi-link graph-center hydration consumes `GraphNode.navigationTarget`, and graph-miss bilink fallback now resolves through `GET /api/vfs/resolve?path=` instead of frontend candidate expansion plus local category guessing.
- **Validation**:
  - `direnv exec /Users/guangtao/ghq/github.com/tao3k/omni-dev-fusion env CARGO_TARGET_DIR=/tmp/omni-dev-fusion-codex-gateway-studio-nav-target INSTA_UPDATE=always cargo test -p xiuxian-wendao --lib gateway::studio::`
  - `direnv exec /Users/guangtao/ghq/github.com/tao3k/omni-dev-fusion npm --prefix .data/qianji-studio test -- src/App.test.tsx`

### [DONE] Feature: Qianji Scenario-Audit Contract Gate

- **Objective**: Promote the `qianji` scenario-audit lane into a first-class CI gate and retire the old workspace-only audit package.
- **Status**: [Landed] `xiuxian-qianji` now exposes the pinned audit scenario contract as a real Cargo test target, a dedicated `scripts/rust/xiuxian_qianji_scenario_audit_contracts.sh` gate runs the scenario-audit contract lane, the new `just` target is wired into CI entrypoints and workflow jobs, and `xiuxian-test-audit` has been removed from the workspace.
- **Validation**:
  - `direnv exec /Users/guangtao/ghq/github.com/tao3k/omni-dev-fusion cargo test -p xiuxian-qianji --test audit_scenario_contract`
  - `direnv exec /Users/guangtao/ghq/github.com/tao3k/omni-dev-fusion bash scripts/rust/xiuxian_qianji_scenario_audit_contracts.sh`
  - `direnv exec /Users/guangtao/ghq/github.com/tao3k/omni-dev-fusion cargo test -p xiuxian-testing --test docs_kernel_contract`

## Blueprint Alignment

- Reference: [[.cache/agent/blueprints/project_anchor_semantic_addressing.md]]
- Path identity remains config-driven: Markdown AST hits are emitted only for files under configured project dirs and keep studio display paths stable for frontend jumps.
