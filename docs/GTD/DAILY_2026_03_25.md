# Daily Log - 2026-03-25

### [DONE] Wendao repo-wide code-search content-fallback tuning

- **Objective**: keep pushing repo-wide `code_search` tail latency down by trimming the heavier `repo_content_chunk` fallback path without changing explicit repo search semantics.
- **Update**:
  - replaced the single repo-wide per-repo result cap with split limits in `gateway/studio/search/handlers/code_search/query.rs`
  - all-repo fanout now keeps the repo-entity local cap at `min(limit, 12)` but lowers repo-content fallback to `min(limit, 4)`
  - explicit `repo:` searches still use the full requested limit for both repo-entity and repo-content paths
  - added a regression that proves repo-wide search still prefers repo-entity hits and does not fall through to repo-content for the same repo when an entity hit already exists
- **Validation Executed**:
  - `direnv exec . cargo nextest run -p xiuxian-wendao -E 'test(repo_wide_code_search_scopes_per_repo_limits) | test(build_code_search_response_returns_repo_entity_hits_from_search_plane) | test(build_code_search_response_skips_unsupported_repositories_when_searching_all_repos) | test(build_code_search_response_prefers_repo_entity_hits_before_repo_content_fallback)'`
  - `direnv exec . cargo check -p xiuxian-wendao`
  - `direnv exec . git diff --check`

### [DONE] Wendao repo-wide code-search tail-latency tuning

- **Objective**: reduce steady-state all-repo `code_search` tail latency without reopening the recently stabilized repo-sync and repo-backed pressure regressions.
- **Update**:
  - added a repo-wide per-repo result cap in `gateway/studio/search/handlers/code_search/query.rs`, while keeping explicit repo-hint searches on the full requested limit
  - wired the repo-wide fanout path to use that capped local limit instead of issuing every repo-local entity/content query with the global top-level limit
  - raised the default `XIUXIAN_WENDAO_REPO_SEARCH_READ_CONCURRENCY` heuristic from `available_parallelism() / 8` to `/ 5`, which moves the current host class from a repo-read budget of `2` to `3` without removing the existing env override or clamp
  - added focused regressions for the new repo-wide local-limit helper and the adjusted repo-read concurrency defaults
- **Validation Executed**:
  - `direnv exec . cargo nextest run -p xiuxian-wendao -E 'test(repo_wide_code_search_timeout_applies_only_without_repo_hint) | test(repo_wide_code_search_scopes_per_repo_limit) | test(repo_search_parallelism_reuses_search_plane_read_budget) | test(build_code_search_response_marks_partial_when_repo_wide_budget_expires) | test(build_code_search_response_returns_repo_entity_hits_from_search_plane) | test(build_code_search_response_skips_unsupported_repositories_when_searching_all_repos) | test(repo_search_read_concurrency_limit_defaults_from_parallelism) | test(repo_search_read_concurrency_limit_accepts_positive_override) | test(repo_search_read_concurrency_limit_ignores_invalid_override) | test(repo_search_parallelism_reuses_repo_read_budget)'`
  - `direnv exec . cargo check -p xiuxian-wendao`

### [DONE] Wendao repo-index state tests modularization

- **Objective**: continue the lossless Rust feature-folder migration on the `xiuxian-wendao` repo-index state test slice with crate-style imports only.
- **Update**:
  - `gateway/studio/repo_index/state/tests.rs` is now split into `gateway/studio/repo_index/state/tests/` with interface-only `mod.rs`, plus `helpers.rs`, `sync.rs`, `status.rs`, `concurrency.rs`, `runtime.rs`, and `collect.rs`
  - the repo-index state test surface still covers sync enqueue behavior, status aggregation, concurrency throttling, runtime snapshot sync, and code-document collection cancellation
- **Validation Executed**:
  - `direnv exec . cargo fmt -p xiuxian-wendao`
  - `direnv exec . cargo check -p xiuxian-wendao --lib --keep-going`
  - `direnv exec . cargo test -p xiuxian-wendao gateway::studio::repo_index::state::tests:: --lib`
  - `direnv exec . git diff --check`

### [DONE] Wendao search-index definitions modularization

- **Objective**: continue the lossless Rust feature-folder migration on the `xiuxian-wendao` search-index definitions slice with crate-style imports only.
- **Update**:
  - `gateway/studio/types/search_index/definitions.rs` is now split into `gateway/studio/types/search_index/definitions/` with interface-only `mod.rs`, plus `lifecycle.rs`, `issues.rs`, `status_reason.rs`, `maintenance.rs`, `telemetry.rs`, and `corpus.rs`
  - the search-index DTO surface still covers lifecycle phase, issue summaries, status reasons, maintenance rollups, query telemetry, corpus status rows, and the aggregated Studio response payload
- **Validation Executed**:
  - `direnv exec . cargo fmt -p xiuxian-wendao`
  - `direnv exec . cargo check -p xiuxian-wendao --lib --keep-going`
  - `direnv exec . cargo test -p xiuxian-wendao gateway::studio::types::search_index:: --lib`
  - `direnv exec . git diff --check`

### [DONE] Wendao search ranking modularization

- **Objective**: continue the lossless Rust feature-folder migration on the `xiuxian-wendao` search-ranking slice with crate-style imports only.
- **Update**:
  - `analyzers/service/search/ranking.rs` is now split into `analyzers/service/search/ranking/` with interface-only `mod.rs`, plus `shared.rs`, `module.rs`, `symbol.rs`, `example.rs`, and `tests.rs`
  - the ranked module/symbol/example search flows still expose the same `search::ranking::*` call sites to sibling search modules
  - the new leaf tests exercise the empty-query ordering path for modules, symbols, and examples so the split keeps a direct regression anchor
- **Validation Executed**:
  - `direnv exec . cargo fmt -p xiuxian-wendao`
  - `direnv exec . cargo check -p xiuxian-wendao --lib --keep-going`
  - `direnv exec . cargo test -p xiuxian-wendao analyzers::service::search:: --lib`
  - `direnv exec . git diff --check`

### [DONE] Wendao search-plane modularization: repo content build, repo-content status tests, and manifest

- **Objective**: continue the Rust feature-folder migration on the `xiuxian-wendao` search-plane slices with crate-style imports only.
- **Update**:
  - `search_plane/repo_content_chunk/build.rs` is now split into `search_plane/repo_content_chunk/build/` with interface-only `mod.rs`, plus `orchestration.rs`, `plan.rs`, `types.rs`, `write.rs`, and `tests.rs`
  - `search_plane/service/tests/status/repo_content.rs` is now split into `search_plane/service/tests/status/repo_content/` with interface-only `mod.rs`, plus `helpers.rs`, `ready.rs`, `snapshot.rs`, `publication.rs`, and `failure.rs`
  - `search_plane/manifest.rs` is now split into `search_plane/manifest/` with interface-only `mod.rs`, plus `keyspace.rs`, `fingerprint.rs`, `records.rs`, and `tests.rs`
  - all touched paths use crate-root imports, and the old `build.rs` / `repo_content.rs` entry files were removed
- **Validation Executed**:
  - `direnv exec . cargo fmt -p xiuxian-wendao`
  - `direnv exec . cargo test -p xiuxian-wendao search_plane::repo_content_chunk::build::tests:: --lib`
  - `direnv exec . cargo test -p xiuxian-wendao search_plane::service::tests::status::repo_content:: --lib`
  - `direnv exec . cargo test -p xiuxian-wendao search_plane::manifest::tests:: --lib`
  - `direnv exec . cargo check -p xiuxian-wendao --lib --keep-going`
  - `direnv exec . git diff --check`

### [DONE] Feature: Markdown waterfall inspector

- **Objective**: render markdown-backed DirectReader content as a structured waterfall instead of a flat prose viewer.
- **Update**:
  - added `MarkdownWaterfall` as the markdown-rich renderer for `DirectReader`
  - markdown documents now render a document identity card with a `Title / Tags / Linked` top card followed by heading-based section cards
  - section headers were compressed into compact index strips to reduce the panel feeling of stacked sub-cards
  - code, table, and mermaid blocks now render inside nested rich slots so the body reads as structured cards instead of flat prose blocks
  - the rich slots now share a common slot header so code, table, and mermaid blocks read as one visual language
  - ZenSearch now wraps markdown results in a dedicated reading tray so the preview sits inside a stronger outer boundary
  - markdown section cards now expose lightweight `Copy for RAG` and `Pivot section` actions so each section can serve as an atomic retrieval anchor
  - markdown section cards now expose compact retrieval metadata (`chunk id`, `semantic type`, `fingerprint`, `token estimate`) and the section-copy payload now carries the same atom contract
  - rich markdown slots for tables, math, mermaid, bi-links, and fenced code blocks remain available inside the waterfall
  - the first H1 now serves as document identity when no frontmatter title is present, which avoids duplicate heading rendering in the waterfall layout
  - source mode remains available for exact line inspection when the operator toggles it
- **Validation Executed**:
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm test -- src/components/panels/DirectReader/MarkdownWaterfall.test.tsx src/components/panels/DirectReader/DirectReader.test.tsx src/components/ZenSearch/__tests__/ZenSearchPreviewContent.test.tsx src/components/ZenSearch/__tests__/ZenSearchPreviewPane.test.tsx'`
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm run build'`

### [DONE] Feature: DiagramWindow markdown waterfall fallback

- **Objective**: keep DiagramWindow structured when a markdown file has no embedded diagram body and the markdown projection does not surface Mermaid.
- **Update**:
  - DiagramWindow now renders `MarkdownWaterfall` for markdown-backed files when no embedded Mermaid or BPMN diagram body is available
  - the fallback stays structured instead of collapsing to the empty-state card
  - Mermaid and BPMN diagram projections remain the preferred path when they are present
- **Validation Executed**:
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm test -- src/components/panels/DiagramWindow/__tests__/DiagramWindow.test.tsx src/components/panels/DirectReader/MarkdownWaterfall.test.tsx src/components/panels/DirectReader/DirectReader.test.tsx'`
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm run build'`

### [DONE] Feature: Zen shell persistent overlay

- **Objective**: keep the Zen Search shell mounted after first open so workspace state survives close/reopen cycles while the shell toggles visibility through `isOpen`.
- **Update**:
  - `App` now keeps the Zen shell mounted after the first open and passes an explicit `isOpen` flag into `ZenSearchWindow`
  - `ZenSearchWindow` now behaves as a persistent overlay shell, with `hidden` / `aria-hidden` gating when closed
  - the Zen shell visibility contract is now documented in the package README and the panel handbook so the persistent overlay behavior is visible in the architecture notes
- **Validation Executed**:
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm test -- src/App.test.tsx src/components/ZenSearch/__tests__/ZenSearchWindow.test.tsx src/components/ZenSearch/__tests__/ZenSearchLayout.test.tsx src/components/ZenSearch/__tests__/ZenSearchWorkspace.test.tsx'`
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm run build'`
