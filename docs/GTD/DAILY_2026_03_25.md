# Daily Log - 2026-03-25

### [DONE] Arrow transport substrate moved into xiuxian-vector

- **Objective**: place the new Arrow-over-HTTP transport substrate on the correct ownership boundary by landing it in `xiuxian-vector` instead of `xiuxian-wendao`, so Wendao can consume it later without owning a duplicate transport layer.
- **Update**:
  - added `xiuxian-vector/src/arrow_transport/` with generic Arrow IPC codec helpers, TOML-backed endpoint config, HTTP transport client, and focused tests
  - updated `xiuxian-vector/src/search/search_impl/ipc.rs` to reuse the shared single-batch codec instead of maintaining a private manual `StreamWriter` path
  - updated `xiuxian-vector/README.md` to document the new Arrow transport surface and its role in speaking to the standalone Julia `WendaoArrow` package
  - kept `xiuxian-wendao` untouched for this pass, so the transport lands as a reusable substrate before any downstream search-pipeline wiring
- **Validation Executed**:
  - `direnv exec . cargo fmt -p xiuxian-vector`
  - `direnv exec . cargo test -p xiuxian-vector arrow_transport:: --lib`
  - `direnv exec . cargo check -p xiuxian-vector --lib`
  - `direnv exec . git diff --check`

### [DONE] WendaoArrow Julia package modularization

- **Objective**: split the WendaoArrow Julia interface package by responsibility so the transport layer follows the repository modularization standard instead of accumulating configuration, codec, response, and server logic in one file.
- **Update**:
  - reduced `src/WendaoArrow.jl` to an interface-only root module with shared constants, imports, includes, and exports
  - split configuration parsing into `src/config.jl`
  - split Arrow IPC encode/decode into `src/codec.jl`
  - split HTTP response helpers into `src/http_responses.jl`
  - split handler and serve orchestration into `src/server.jl`
  - kept the package public API and the existing hardening behavior unchanged
- **Validation Executed**:
  - `direnv exec . julia --project=.data/WendaoArrow -e 'using Pkg; Pkg.resolve(); Pkg.instantiate(); Pkg.test()'`

### [DONE] WendaoArrow operational hardening

- **Objective**: land the first operational hardening slice for `.data/WendaoArrow` so the interface package can be supervised and integrated by the existing Wendao Rust gateway.
- **Update**:
  - added a configurable health route with default `/health`
  - extended runtime configuration to cover `health_route` through package defaults, TOML, and `--health-route`
  - hardened `build_handler` to map invalid Arrow IPC requests to HTTP 400 and processor or output failures to HTTP 500 while logging the underlying exception server-side
  - extended test coverage to validate health probes, invalid request rejection, processor crash containment, and TOML/flag precedence
- **Validation Executed**:
  - `direnv exec . julia --project=.data/WendaoArrow -e 'using Pkg; Pkg.resolve(); Pkg.instantiate(); Pkg.test()'`

### [DONE] Wendao construction, repo-analysis search, and section-create modularization

- **Objective**: continue the lossless Rust feature-folder migration on the current search-plane and section-edit hot spots with crate-style imports only.
- **Update**:
  - `search_plane/service/core/construction.rs` is now split into `search_plane/service/core/construction/` with dedicated runtime, paths, concurrency, and tests modules
  - `gateway/studio/router/handlers/repo/analysis/search.rs` is now split into `gateway/studio/router/handlers/repo/analysis/search/` with dedicated cache, publication, module, symbol, example, and tests modules
  - `zhenfa_router/native/section_create.rs` is now split into `zhenfa_router/native/section_create/` with dedicated types, insertion, building, and tests modules
  - the public surfaces remain unchanged, and the targeted unit tests plus package checks are green
  - the next bounded target is `analyzers/query/docs/planner.rs`
- **Validation Executed**:
  - `direnv exec . cargo fmt -p xiuxian-wendao`
  - `direnv exec . cargo test -p xiuxian-wendao zhenfa_router::native::section_create::tests:: --lib`
  - `direnv exec . cargo test -p xiuxian-wendao zhenfa_router::native::semantic_edit:: --lib`
  - `direnv exec . cargo check -p xiuxian-wendao --lib --keep-going`
  - `direnv exec . git diff --check`

### [DONE] Wendao code-AST router modularization

- **Objective**: continue the lossless Rust feature-folder migration on the Studio code-AST router slice with crate-style imports only.
- **Update**:
  - `gateway/studio/router/code_ast.rs` is now split into `gateway/studio/router/code_ast/` with dedicated response, resolve, blocks, and atoms modules
  - the public code-AST router surface remains unchanged, and the targeted router code-AST tests are green
  - the next bounded target is `gateway/studio/search/handlers/knowledge/intent.rs`
- **Validation Executed**:
  - `direnv exec . cargo fmt -p xiuxian-wendao`
  - `direnv exec . cargo test -p xiuxian-wendao gateway::studio::router::tests::code_ast --lib`
  - `direnv exec . cargo check -p xiuxian-wendao --lib --keep-going`
  - `direnv exec . git diff --check`

### [DONE] Wendao definition resolver modularization

- **Objective**: continue the lossless Rust feature-folder migration on the Studio definition-resolution slice with crate-style imports only.
- **Update**:
  - `gateway/studio/search/definition.rs` is now split into `gateway/studio/search/definition/` with dedicated resolve, filters, and tests modules
  - the public definition-resolution surface remains unchanged, and the targeted definition tests are green
  - the next bounded target is `analyzers/service/projection/planner/api.rs`
- **Validation Executed**:
  - `direnv exec . cargo fmt -p xiuxian-wendao`
  - `direnv exec . cargo test -p xiuxian-wendao gateway::studio::search::definition::tests:: --lib`
  - `direnv exec . cargo check -p xiuxian-wendao --lib --keep-going`
  - `direnv exec . git diff --check`

### [DONE] WendaoArrow Julia interface package init

- **Objective**: initialize `.data/WendaoArrow` as a Julia interface package for pure Arrow IPC communication with the existing Wendao Rust gateway, without embedding analyzer logic.
- **Update**:
  - created a root Julia package `WendaoArrow` with Arrow IPC encode/decode helpers, HTTP handler composition, and a passthrough example server
  - added runtime configuration through package defaults, TOML under `[interface]`, and CLI flags with precedence `defaults < TOML < flags`
  - aligned project docs to a Wendao-style tree under `.data/WendaoArrow/docs/01_core/wendao_arrow/`
  - removed the incorrect local Rust gateway scaffold and the earlier analyzer-specific framing so future Julia analyzer packages can depend on `WendaoArrow` as a transport interface
  - removed an unintended nested `.git` directory inside `.data/WendaoArrow`
- **Validation Executed**:
  - `direnv exec . julia --project=.data/WendaoArrow -e 'using Pkg; Pkg.instantiate(); Pkg.test()'`

### [DONE] Wendao repo gateway hot-query cache

- **Objective**: reduce steady-state mixed-hotset pressure on explicit repo gateway search endpoints without changing the HTTP contracts for `module-search`, `symbol-search`, or `example-search`.
- **Update**:
  - wrapped `repo/module-search`, `repo/symbol-search`, and `repo/example-search` with a repo-aware Search Plane hot-query cache keyed by repo publication/runtime state
  - cache hits now return typed payloads directly and skip both Search Plane reads and cached-analyzer fallback work for repeated `(repo, query, limit)` traffic
  - kept error behavior unchanged by caching only successful payloads; pending repos still return `REPO_INDEX_PENDING`
  - added a focused unit regression that proves the hot-query cache reuses the first successful typed payload instead of executing the loader twice
  - added a `SearchPlaneService::with_test_cache(...)` helper under `#[cfg(test)]` so cache-backed service tests do not need to reach into private cache modules
- **Validation Executed**:
  - `direnv exec . cargo nextest run -p xiuxian-wendao -E 'test(cached_repo_search_result_reuses_hot_query_payload) | test(repo_module_search_endpoint_returns_module_payload) | test(repo_symbol_search_endpoint_returns_symbol_payload) | test(repo_example_search_endpoint_returns_example_payload) | test(repo_cached_search_endpoints_can_serve_from_published_repo_entity_search_plane) | test(repo_cached_search_endpoints_return_pending_without_ready_analysis_cache)'`
  - `direnv exec . cargo check -p xiuxian-wendao`
  - `direnv exec . cargo check -p xiuxian-wendao --tests`
  - `direnv exec . git diff --check`

### [DONE] Wendao gateway command modularization

- **Objective**: continue the lossless Rust feature-folder migration on the gateway command slice with crate-style imports only.
- **Update**:
  - `bin/wendao/execute/gateway.rs` is now split into `bin/wendao/execute/gateway/` with dedicated command, config, health, registry, shared, and status modules
  - the public gateway command surface remains unchanged, and the gateway test suite is green
  - the next bounded target is `search_plane/service/core/maintenance.rs`
- **Validation Executed**:
  - `direnv exec . cargo fmt -p xiuxian-wendao`
  - `direnv exec . cargo test -p xiuxian-wendao --bin wendao gateway`
  - `direnv exec . cargo check -p xiuxian-wendao --bin wendao --tests`
  - `direnv exec . git diff --check`

### [DONE] Wendao repo analysis modularization

- **Objective**: continue the lossless Rust feature-folder migration on the repo-analysis handler slice with crate-style imports only.
- **Update**:
  - `gateway/studio/router/handlers/repo/analysis.rs` is now split into `gateway/studio/router/handlers/repo/analysis/` with dedicated overview, search, doc coverage, sync, and tests modules
  - the public repo-analysis handler surface remains unchanged, and the targeted repo-analysis unit tests are green
  - the package docs and ExecPlan are synchronized, and the next bounded target is `search_plane/service/core/construction.rs`
- **Validation Executed**:
  - `direnv exec . cargo fmt -p xiuxian-wendao`
  - `direnv exec . cargo test -p xiuxian-wendao gateway::studio::router::handlers::repo::analysis:: --lib`
  - `direnv exec . cargo check -p xiuxian-wendao --lib --keep-going`
  - `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate repo_` *(repo-analysis endpoint cases passed; one unrelated projected-page snapshot drift remains in the broader harness)*
  - `direnv exec . git diff --check`

### [DONE] Wendao knowledge section query modularization

- **Objective**: continue the lossless Rust feature-folder migration on the knowledge-section query slice with crate-style imports only.
- **Update**:
  - `search_plane/knowledge_section/query.rs` is now split into `search_plane/knowledge_section/query/` with dedicated errors, ranking, candidates, search, and tests modules
  - the public knowledge-section query surface remains unchanged, and the knowledge-section query plus build tests are green
  - `zhenfa_router/native/semantic_check/docs_governance/rendering.rs` and `skill_vfs/zhixing/indexer/resource_graph.rs` also landed in this pass, and the next bounded target is `search_plane/service/core/maintenance.rs`
- **Validation Executed**:
  - `direnv exec . cargo fmt -p xiuxian-wendao`
  - `direnv exec . cargo check -p xiuxian-wendao --lib --keep-going`
  - `direnv exec . cargo test -p xiuxian-wendao search_plane::knowledge_section:: --lib`
  - `direnv exec . git diff --check`

### [DONE] Wendao semantic check checks modularization

- **Objective**: continue the lossless Rust feature-folder migration on the semantic-check check slice with crate-style imports only.
- **Update**:
  - `zhenfa_router/native/semantic_check/checks.rs` is now split into `zhenfa_router/native/semantic_check/checks/` with dedicated contracts, identity, links, observations, and structure modules
  - the public semantic-check surface remains unchanged, and the semantic-check test suite is green
  - the next bounded target is `zhenfa_router/native/semantic_check/docs_governance/rendering.rs`
- **Validation Executed**:
  - `direnv exec . cargo fmt -p xiuxian-wendao`
  - `direnv exec . cargo check -p xiuxian-wendao --lib --keep-going`
  - `direnv exec . cargo test -p xiuxian-wendao semantic_check::tests:: --lib`
  - `direnv exec . cargo test -p xiuxian-wendao semantic_check::docs_governance::tests:: --lib`
  - `direnv exec . git diff --check`

### [DONE] Wendao git checkout tests modularization

- **Objective**: continue the lossless Rust feature-folder migration on the Git checkout test slice with crate-style imports only.
- **Update**:
  - `git/checkout/tests.rs` is now split into `git/checkout/tests/` with dedicated materialization, layout, lock, and retry modules
  - the public checkout surface remains unchanged, and the checkout test suite is green
  - the next bounded target is `zhenfa_router/native/semantic_check/docs_governance/rendering.rs`
- **Validation Executed**:
  - `direnv exec . cargo fmt -p xiuxian-wendao`
  - `direnv exec . cargo check -p xiuxian-wendao --lib --keep-going`
  - `direnv exec . cargo test -p xiuxian-wendao git::checkout:: --lib`
  - `direnv exec . git diff --check`

### [DONE] Wendao router tests modularization

- **Objective**: continue the lossless Rust feature-folder migration on the Studio router test slice with crate-style imports only.
- **Update**:
  - `gateway/studio/router/tests.rs` is now split into `gateway/studio/router/tests/` with dedicated code AST, config, graph, and helpers modules
  - the public router test surface remains unchanged, and the router test suite is green
  - the next bounded target is `gateway/studio/search/handlers/tests/code_search.rs`
- **Validation Executed**:
  - `direnv exec . cargo fmt -p xiuxian-wendao`
  - `direnv exec . cargo check -p xiuxian-wendao --lib --keep-going`
  - `direnv exec . cargo test -p xiuxian-wendao gateway::studio::router::tests:: --lib`
  - `direnv exec . git diff --check`

### [DONE] Wendao symbol index state modularization

- **Objective**: continue the lossless Rust feature-folder migration on the Studio symbol-index state slice with crate-style imports only.
- **Update**:
  - `gateway/studio/symbol_index/state.rs` is now split into `gateway/studio/symbol_index/state/` with dedicated coordinator, build, helpers, and tests modules
  - the public symbol-index state surface remains unchanged, and the state tests are green
  - the next bounded target is `gateway/studio/search/handlers/tests/code_search.rs`
- **Validation Executed**:
  - `direnv exec . cargo fmt -p xiuxian-wendao`
  - `direnv exec . cargo check -p xiuxian-wendao --lib --keep-going`
  - `direnv exec . cargo test -p xiuxian-wendao gateway::studio::symbol_index::state::tests:: --lib`
  - `direnv exec . git diff --check`

### [DONE] Wendao analyzers cache modularization

- **Objective**: continue the lossless Rust feature-folder migration on the analyzer cache slice with crate-style imports only.
- **Update**:
  - `analyzers/cache.rs` is now split into `analyzers/cache/` with dedicated keys, analysis cache, artifacts cache, query cache, valkey placeholder, and tests modules
  - the public analyzer cache surface remains unchanged, and the cache roundtrip plus query-isolation tests are green
  - the next bounded target is `gateway/studio/router/tests.rs`
- **Validation Executed**:
  - `direnv exec . cargo fmt -p xiuxian-wendao`
  - `direnv exec . cargo check -p xiuxian-wendao --lib --keep-going`
  - `direnv exec . cargo test -p xiuxian-wendao analyzers::cache::tests:: --lib`
  - `direnv exec . git diff --check`

### [DONE] Wendao repo-entity query hydrate modularization

- **Objective**: continue the lossless Rust feature-folder migration on the repo-entity hydrate slice with crate-style imports only.
- **Update**:
  - `search_plane/repo_entity/query/hydrate.rs` is now split into `search_plane/repo_entity/query/hydrate/` with dedicated columns, load, parse, rows, and tests modules
  - the public repo-entity query surface remains unchanged, and the repo-entity query tests plus hydrate helper tests are green
  - the next bounded target is `analyzers/cache.rs`
- **Validation Executed**:
  - `direnv exec . cargo fmt -p xiuxian-wendao`
  - `direnv exec . cargo check -p xiuxian-wendao --lib --keep-going`
  - `direnv exec . cargo test -p xiuxian-wendao search_plane::repo_entity::query::hydrate::tests:: --lib`
  - `direnv exec . cargo test -p xiuxian-wendao search_plane::repo_entity::query::tests:: --lib`
  - `direnv exec . git diff --check`

### [DONE] Wendao planner workset modularization

- **Objective**: continue the lossless Rust feature-folder migration on the docs planner workset slice with crate-style imports only.
- **Update**:
  - `analyzers/service/projection/planner/workset.rs` is now split into `analyzers/service/projection/planner/workset/` with dedicated orchestration, groups, balance, strategy, and math modules
  - the public planner workset surface remains unchanged, and the planner unit tests plus docs planner endpoint regression remain green
  - the next bounded target is `search_plane/repo_entity/query/hydrate.rs`
- **Validation Executed**:
  - `direnv exec . cargo fmt -p xiuxian-wendao`
  - `direnv exec . cargo check -p xiuxian-wendao --lib --keep-going`
  - `direnv exec . cargo test -p xiuxian-wendao analyzers::service::projection::planner::tests:: --lib`
  - `direnv exec . cargo test -p xiuxian-wendao docs_planner_workset_endpoint_returns_opened_gap_batch --lib`
  - `direnv exec . git diff --check`

### [DONE] Wendao planner API modularization

- **Objective**: continue the lossless Rust feature-folder migration on the docs planner API slice with crate-style imports only.
- **Update**:
  - `analyzers/service/projection/planner/api.rs` is now split into `analyzers/service/projection/planner/api/` with dedicated item, search, queue, and rank modules
  - the public planner API surface remains unchanged, and the planner unit tests are green
  - the next bounded target is `link_graph/index/build/assemble.rs`
- **Validation Executed**:
  - `direnv exec . cargo fmt -p xiuxian-wendao`
  - `direnv exec . cargo check -p xiuxian-wendao --lib --keep-going`
  - `direnv exec . cargo test -p xiuxian-wendao analyzers::service::projection::planner::tests:: --lib`
  - `direnv exec . git diff --check`

### [DONE] Wendao link-graph assemble modularization

- **Objective**: continue the lossless Rust feature-folder migration on the link-graph index build slice with crate-style imports only.
- **Update**:
  - `link_graph/index/build/assemble.rs` is now split into `link_graph/index/build/assemble/` with dedicated inputs, notes, edges, virtual-nodes, finalize, and api modules
  - the public link-graph index build surface remains unchanged, and the PPR precision regression is green
  - `gateway/studio/search/handlers/code_search/search.rs` is now split into `gateway/studio/search/handlers/code_search/search/` with dedicated response, repo-search, buffered, and task modules
  - the next bounded target is `link_graph/context_snapshot.rs`
- **Validation Executed**:
  - `direnv exec . cargo fmt -p xiuxian-wendao`
  - `direnv exec . cargo check -p xiuxian-wendao --lib --keep-going`
  - `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate test_ppr_weight_precision_impact`
  - `direnv exec . git diff --check`

### [DONE] Wendao link-graph context snapshot modularization

- **Objective**: continue the lossless Rust feature-folder migration on the link-graph context-snapshot slice with crate-style imports only.
- **Update**:
  - `link_graph/context_snapshot.rs` is now split into `link_graph/context_snapshot/` with dedicated types, id, runtime, store, and tests modules
  - the public quantum-context snapshot surface remains unchanged, and the targeted unit tests plus the saliency-window regression are green
  - the next bounded target is `link_graph/index/ppr/kernel.rs`
- **Validation Executed**:
  - `direnv exec . cargo fmt -p xiuxian-wendao`
  - `direnv exec . cargo check -p xiuxian-wendao --lib --keep-going`
  - `direnv exec . cargo test -p xiuxian-wendao link_graph::index::search::quantum_fusion::orchestrate::tests:: --lib`
  - `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate test_quantum_fusion_related_window_expands_for_hot_seed`
  - `direnv exec . git diff --check`

### [DONE] Wendao repo-index coordinator runtime modularization

- **Objective**: continue the lossless Rust feature-folder migration on the repo-index coordinator runtime slice with crate-style imports only.
- **Update**:
  - `gateway/studio/repo_index/state/coordinator/runtime.rs` is now split into `gateway/studio/repo_index/state/coordinator/runtime/` with dedicated scheduler, task, repository, and state modules
  - the public `RepoIndexCoordinator` runtime surface remains unchanged, and the runtime split keeps the existing repo-index state tests green
  - the next bounded target is `analyzers/service/projection/planner/workset.rs`
- **Validation Executed**:
  - `direnv exec . cargo fmt -p xiuxian-wendao`
  - `direnv exec . cargo check -p xiuxian-wendao --lib --keep-going`
  - `direnv exec . cargo test -p xiuxian-wendao gateway::studio::repo_index::state::tests:: --lib`
  - `direnv exec . git diff --check`

### [DONE] Wendao repo-wide code-search content-fallback tuning

- **Objective**: keep pushing repo-wide `code_search` tail latency down by trimming the heavier `repo_content_chunk` fallback path without changing explicit repo search semantics.
- **Update**:
  - replaced the single repo-wide per-repo result cap with split limits in `gateway/studio/search/handlers/code_search/query.rs`
  - all-repo fanout now keeps the repo-entity local cap at `min(limit, 12)` but lowers repo-content fallback to `min(limit, 4)`
  - explicit `repo:` searches still use the full requested limit for both repo-entity and repo-content paths
  - added a regression that proves repo-wide search still prefers repo-entity hits and does not fall through to repo-content for the same repo when an entity hit already exists
- **Validation Executed**:
  - `direnv exec . cargo nextest run -p xiuxian-wendao -E 'test(repo_wide_code_search_scopes_per_repo_limits) | test(build_code_search_response_returns_repo_entity_hits_from_search_plane) | test(build_code_search_response_skips_unsupported_repositories_when_searching_all_repos) | test(build_code_search_response_prefers_repo_entity_hits_before_repo_content_fallback)'`
  - `direnv exec . cargo check -p xiuxian-wendao`
  - `direnv exec . git diff --check`

### [DONE] Wendao repo-wide code-search tail-latency tuning

- **Objective**: reduce steady-state all-repo `code_search` tail latency without reopening the recently stabilized repo-sync and repo-backed pressure regressions.
- **Update**:
  - added a repo-wide per-repo result cap in `gateway/studio/search/handlers/code_search/query.rs`, while keeping explicit repo-hint searches on the full requested limit
  - wired the repo-wide fanout path to use that capped local limit instead of issuing every repo-local entity/content query with the global top-level limit
  - raised the default `XIUXIAN_WENDAO_REPO_SEARCH_READ_CONCURRENCY` heuristic from `available_parallelism() / 8` to `/ 5`, which moves the current host class from a repo-read budget of `2` to `3` without removing the existing env override or clamp
  - added focused regressions for the new repo-wide local-limit helper and the adjusted repo-read concurrency defaults
- **Validation Executed**:
  - `direnv exec . cargo nextest run -p xiuxian-wendao -E 'test(repo_wide_code_search_timeout_applies_only_without_repo_hint) | test(repo_wide_code_search_scopes_per_repo_limit) | test(repo_search_parallelism_reuses_search_plane_read_budget) | test(build_code_search_response_marks_partial_when_repo_wide_budget_expires) | test(build_code_search_response_returns_repo_entity_hits_from_search_plane) | test(build_code_search_response_skips_unsupported_repositories_when_searching_all_repos) | test(repo_search_read_concurrency_limit_defaults_from_parallelism) | test(repo_search_read_concurrency_limit_accepts_positive_override) | test(repo_search_read_concurrency_limit_ignores_invalid_override) | test(repo_search_parallelism_reuses_repo_read_budget)'`
  - `direnv exec . cargo check -p xiuxian-wendao`

### [DONE] Wendao repo-index state tests modularization

- **Objective**: continue the lossless Rust feature-folder migration on the `xiuxian-wendao` repo-index state test slice with crate-style imports only.
- **Update**:
  - `gateway/studio/repo_index/state/tests.rs` is now split into `gateway/studio/repo_index/state/tests/` with interface-only `mod.rs`, plus `helpers.rs`, `sync.rs`, `status.rs`, `concurrency.rs`, `runtime.rs`, and `collect.rs`
  - the repo-index state test surface still covers sync enqueue behavior, status aggregation, concurrency throttling, runtime snapshot sync, and code-document collection cancellation
- **Validation Executed**:
  - `direnv exec . cargo fmt -p xiuxian-wendao`
  - `direnv exec . cargo check -p xiuxian-wendao --lib --keep-going`
  - `direnv exec . cargo test -p xiuxian-wendao gateway::studio::repo_index::state::tests:: --lib`
  - `direnv exec . git diff --check`

### [DONE] Wendao search-index definitions modularization

- **Objective**: continue the lossless Rust feature-folder migration on the `xiuxian-wendao` search-index definitions slice with crate-style imports only.
- **Update**:
  - `gateway/studio/types/search_index/definitions.rs` is now split into `gateway/studio/types/search_index/definitions/` with interface-only `mod.rs`, plus `lifecycle.rs`, `issues.rs`, `status_reason.rs`, `maintenance.rs`, `telemetry.rs`, and `corpus.rs`
  - the search-index DTO surface still covers lifecycle phase, issue summaries, status reasons, maintenance rollups, query telemetry, corpus status rows, and the aggregated Studio response payload
- **Validation Executed**:
  - `direnv exec . cargo fmt -p xiuxian-wendao`
  - `direnv exec . cargo check -p xiuxian-wendao --lib --keep-going`
  - `direnv exec . cargo test -p xiuxian-wendao gateway::studio::types::search_index:: --lib`
  - `direnv exec . git diff --check`

### [DONE] Wendao search ranking modularization

- **Objective**: continue the lossless Rust feature-folder migration on the `xiuxian-wendao` search-ranking slice with crate-style imports only.
- **Update**:
  - `analyzers/service/search/ranking.rs` is now split into `analyzers/service/search/ranking/` with interface-only `mod.rs`, plus `shared.rs`, `module.rs`, `symbol.rs`, `example.rs`, and `tests.rs`
  - the ranked module/symbol/example search flows still expose the same `search::ranking::*` call sites to sibling search modules
  - the new leaf tests exercise the empty-query ordering path for modules, symbols, and examples so the split keeps a direct regression anchor
- **Validation Executed**:
  - `direnv exec . cargo fmt -p xiuxian-wendao`
  - `direnv exec . cargo check -p xiuxian-wendao --lib --keep-going`
  - `direnv exec . cargo test -p xiuxian-wendao analyzers::service::search:: --lib`
  - `direnv exec . git diff --check`

### [DONE] Wendao search-plane modularization: repo content build, repo-content status tests, and manifest

- **Objective**: continue the Rust feature-folder migration on the `xiuxian-wendao` search-plane slices with crate-style imports only.
- **Update**:
  - `search_plane/repo_content_chunk/build.rs` is now split into `search_plane/repo_content_chunk/build/` with interface-only `mod.rs`, plus `orchestration.rs`, `plan.rs`, `types.rs`, `write.rs`, and `tests.rs`
  - `search_plane/service/tests/status/repo_content.rs` is now split into `search_plane/service/tests/status/repo_content/` with interface-only `mod.rs`, plus `helpers.rs`, `ready.rs`, `snapshot.rs`, `publication.rs`, and `failure.rs`
  - `search_plane/manifest.rs` is now split into `search_plane/manifest/` with interface-only `mod.rs`, plus `keyspace.rs`, `fingerprint.rs`, `records.rs`, and `tests.rs`
  - all touched paths use crate-root imports, and the old `build.rs` / `repo_content.rs` entry files were removed
- **Validation Executed**:
  - `direnv exec . cargo fmt -p xiuxian-wendao`
  - `direnv exec . cargo test -p xiuxian-wendao search_plane::repo_content_chunk::build::tests:: --lib`
  - `direnv exec . cargo test -p xiuxian-wendao search_plane::service::tests::status::repo_content:: --lib`
  - `direnv exec . cargo test -p xiuxian-wendao search_plane::manifest::tests:: --lib`
  - `direnv exec . cargo check -p xiuxian-wendao --lib --keep-going`
  - `direnv exec . git diff --check`

### [DONE] Feature: Markdown waterfall inspector

- **Objective**: render markdown-backed DirectReader content as a structured waterfall instead of a flat prose viewer.
- **Update**:
  - added `MarkdownWaterfall` as the markdown-rich renderer for `DirectReader`
  - markdown documents now render a document identity card with a `Title / Tags / Linked` top card followed by heading-based section cards
  - section headers were compressed into compact index strips to reduce the panel feeling of stacked sub-cards
  - code, table, and mermaid blocks now render inside nested rich slots so the body reads as structured cards instead of flat prose blocks
  - the rich slots now share a common slot header so code, table, and mermaid blocks read as one visual language
  - ZenSearch now wraps markdown results in a dedicated reading tray so the preview sits inside a stronger outer boundary
  - markdown section cards now expose lightweight `Copy for RAG` and `Pivot section` actions so each section can serve as an atomic retrieval anchor
  - markdown section cards now expose compact retrieval metadata (`chunk id`, `semantic type`, `fingerprint`, `token estimate`) and the section-copy payload now carries the same atom contract
  - rich markdown slots for tables, math, mermaid, bi-links, and fenced code blocks remain available inside the waterfall
  - the first H1 now serves as document identity when no frontmatter title is present, which avoids duplicate heading rendering in the waterfall layout
  - source mode remains available for exact line inspection when the operator toggles it
- **Validation Executed**:
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm test -- src/components/panels/DirectReader/MarkdownWaterfall.test.tsx src/components/panels/DirectReader/DirectReader.test.tsx src/components/ZenSearch/__tests__/ZenSearchPreviewContent.test.tsx src/components/ZenSearch/__tests__/ZenSearchPreviewPane.test.tsx'`
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm run build'`

### [DONE] Feature: Backend-issued markdown retrieval atoms

- **Objective**: move Zen Search markdown section cards from frontend-only chunk derivation toward a backend-issued retrieval contract.
- **Update**:
  - added `MarkdownRetrievalAtom` to the Wendao Studio markdown analysis payload and now emit document/section retrieval atoms from `/api/analysis/markdown`
  - finalized markdown section `line_end` ranges during backend compile so section atoms span until the next heading or EOF instead of stopping at the heading line
  - Zen Search preview now fetches markdown analysis for markdown-backed results and passes the analysis payload into `MarkdownWaterfall`
  - markdown waterfall cards now prefer backend-issued document/section atoms when available and fall back to local chunk derivation only when no matching backend atom exists
  - the section-level `Copy for RAG` payload continues to expose the same compact retrieval contract, but now uses backend-issued chunk ids, fingerprints, and excerpts when available
- **Validation Executed**:
  - `direnv exec . cargo fmt -p xiuxian-wendao`
  - `direnv exec . cargo test -p xiuxian-wendao analyze_markdown_emits_retrieval_atoms_for_document_and_sections --lib`
  - `direnv exec . cargo check -p xiuxian-wendao --lib --keep-going`
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm test -- src/components/panels/DirectReader/MarkdownWaterfall.test.tsx src/components/ZenSearch/__tests__/ZenSearchPreviewEntity.test.tsx src/components/ZenSearch/__tests__/useZenSearchPreview.test.tsx src/components/ZenSearch/__tests__/ZenSearchPreviewPane.test.tsx'`
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm run build'`

### [DONE] Feature: Backend-issued markdown rich-slot atoms

- **Objective**: extend the markdown retrieval contract from section cards into code-oriented rich slots so the waterfall can emit RAG-ready atoms below the section level.
- **Update**:
  - backend markdown analysis now also emits retrieval atoms for `code block` nodes, with semantic types such as `code:rust`, `code:python`, and `mermaid`
  - markdown waterfall code and mermaid slots now resolve backend-issued atoms within the active section span instead of relying only on local fallback chunk ids
  - code and mermaid rich slots now expose the same compact retrieval metadata rail and `Copy for RAG` action already used by markdown section cards, and their copy payloads now prefer backend-issued excerpts when present
  - backend markdown analysis now also emits retrieval atoms for `table` nodes, and markdown waterfall table slots now resolve those atoms within the active section span instead of remaining presentation-only
  - table slots now expose the same compact retrieval metadata rail and `Copy for RAG` action already used by section, code, and mermaid atoms, and their copy payloads now prefer backend-issued excerpts when present
- **Validation Executed**:
  - `direnv exec . cargo test -p xiuxian-wendao analyze_markdown_emits_retrieval_atoms_for_ --lib`
  - `direnv exec . cargo check -p xiuxian-wendao --lib --keep-going`
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm test -- src/components/panels/DirectReader/MarkdownWaterfall.test.tsx'`
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm run build'`

### [DONE] Feature: Backend-issued markdown display-math atoms

- **Objective**: extend the markdown retrieval contract into display-math slots so formula cards become first-class RAG atoms instead of opaque KaTeX render output.
- **Update**:
  - backend markdown analysis now emits `math:block` retrieval atoms for display-math nodes
  - markdown waterfall now upgrades `katex-display` blocks into math rich slots that expose the same compact retrieval metadata rail and `Copy for RAG` action used by section/code/table atoms, and math copy payloads now prefer backend-issued excerpts when present
  - the frontend math-slot path is implemented and the targeted markdown waterfall tests are green
- **Validation Executed**:
  - `direnv exec . cargo test -p xiuxian-wendao gateway::studio::analysis:: --lib`
  - `direnv exec . cargo check -p xiuxian-wendao --lib --keep-going`
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm test -- src/components/panels/DirectReader/MarkdownWaterfall.test.tsx'`
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm run build'`

### [DONE] Feature: Backend-issued markdown observation atoms

- **Objective**: extend the markdown retrieval contract into blockquote-style observation cards so quoted findings become first-class waterfall atoms instead of passive prose decoration.
- **Update**:
  - backend markdown compile now emits `Observation` nodes and `observation` retrieval atoms for blockquote spans
  - markdown waterfall now upgrades blockquote cards into observation rich slots that expose the same compact retrieval metadata rail and `Copy for RAG` action used by section/code/table/math atoms
  - observation copy payloads now use backend-issued chunk ids, fingerprints, and excerpts when present
- **Validation Executed**:
  - `direnv exec . cargo fmt -p xiuxian-wendao`
  - `direnv exec . cargo test -p xiuxian-wendao compile_markdown_ir_emits_observation_atoms --lib`
  - `direnv exec . cargo test -p xiuxian-wendao analyze_markdown_emits_retrieval_atoms_for_ --lib`
  - `direnv exec . cargo check -p xiuxian-wendao --lib --keep-going`
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm test -- src/components/panels/DirectReader/MarkdownWaterfall.test.tsx src/components/panels/DirectReader/DirectReader.test.tsx src/components/ZenSearch/__tests__/ZenSearchPreviewEntity.test.tsx src/components/ZenSearch/__tests__/ZenSearchPreviewPane.test.tsx'`
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm run build'`

### [DONE] Feature: Markdown waterfall modularization

- **Objective**: split the oversized markdown waterfall renderer into stable modules so the retrieval contract, bi-link parsing, model assembly, and rich-slot rendering can evolve without leaving a 1500+ line preview file in the hot path.
- **Update**:
  - `MarkdownWaterfall.tsx` is now a thin orchestration shell
  - shared contracts and copy strings live in `markdownWaterfallShared.ts`
  - bi-link parsing and URL transforms live in `markdownWaterfallBiLinks.ts`
  - waterfall model assembly and retrieval helpers live in `markdownWaterfallModel.ts`
  - markdown rich-slot rendering lives in `markdownWaterfallComponents.tsx`
- **Validation Executed**:
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm test -- src/components/panels/DirectReader/MarkdownWaterfall.test.tsx src/components/panels/DirectReader/DirectReader.test.tsx src/components/ZenSearch/__tests__/ZenSearchPreviewEntity.test.tsx src/components/ZenSearch/__tests__/ZenSearchPreviewPane.test.tsx'`
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm run build'`

### [DONE] Feature: DiagramWindow markdown waterfall fallback

- **Objective**: keep DiagramWindow structured when a markdown file has no embedded diagram body and the markdown projection does not surface Mermaid.
- **Update**:
  - DiagramWindow now renders `MarkdownWaterfall` for markdown-backed files when no embedded Mermaid or BPMN diagram body is available
  - the fallback stays structured instead of collapsing to the empty-state card
  - Mermaid and BPMN diagram projections remain the preferred path when they are present
- **Validation Executed**:
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm test -- src/components/panels/DiagramWindow/__tests__/DiagramWindow.test.tsx src/components/panels/DirectReader/MarkdownWaterfall.test.tsx src/components/panels/DirectReader/DirectReader.test.tsx'`
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm run build'`

### [DONE] Feature: Code AST retrieval atoms

- **Objective**: expose explicit retrieval atom metadata on the code AST waterfall so declaration and logic-block stages can act as RAG-ready units instead of only visual sections.
- **Update**:
  - added declaration-level retrieval metadata (`chunk id`, `semantic type`, `fingerprint`, `token estimate`) to the AST waterfall declaration card
  - added logic-block retrieval metadata to validation / execution / return stages using the same atom contract
  - aligned code AST atom ids to a stable `ast:*` prefix with deterministic path / scope / locator composition
  - added `Copy for RAG` actions to declaration and logic-block atoms so the right pane can emit direct retrieval payloads from the code waterfall
  - added explicit `Pivot declaration` and `Pivot block` actions so code atoms use visible action controls instead of relying only on implicit card clicks
  - upgraded local and external symbol surfaces into atom cards with retrieval metadata, `Pivot symbol`, and `Copy for RAG`
  - upgraded ranked pivot anchors with retrieval metadata, `Pivot anchor`, and `Copy for RAG`
  - kept the AST waterfall single-column layout intact while making declaration and block surfaces explicitly retrieval-oriented
- **Validation Executed**:
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm test -- src/components/ZenSearch/__tests__/CodeAstAnatomyView.test.tsx src/components/ZenSearch/StructuredDashboard/__tests__/StructuredIntelligenceDashboard.test.tsx src/components/ZenSearch/StructuredDashboard/__tests__/StructuredCodeInspector.test.tsx src/components/ZenSearch/__tests__/ZenSearchPreviewPane.test.tsx'`
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm run build'`

### [DONE] Feature: Zen shell persistent overlay

- **Objective**: keep the Zen Search shell mounted after first open so workspace state survives close/reopen cycles while the shell toggles visibility through `isOpen`.
- **Update**:
  - `App` now keeps the Zen shell mounted after the first open and passes an explicit `isOpen` flag into `ZenSearchWindow`
  - `ZenSearchWindow` now behaves as a persistent overlay shell, with `hidden` / `aria-hidden` gating when closed
  - the Zen shell visibility contract is now documented in the package README and the panel handbook so the persistent overlay behavior is visible in the architecture notes
- **Validation Executed**:
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm test -- src/App.test.tsx src/components/ZenSearch/__tests__/ZenSearchWindow.test.tsx src/components/ZenSearch/__tests__/ZenSearchLayout.test.tsx src/components/ZenSearch/__tests__/ZenSearchWorkspace.test.tsx'`
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm run build'`

### [DONE] Feature: Wendao gateway runtime log mirroring

- **Objective**: restore workspace-local runtime evidence for intermittent
  gateway dropouts so browser-side `NetworkError` events can be diagnosed from
  `.run/logs` instead of only ephemeral process-compose logs under `/tmp`.
- **Update**:
  - confirmed the active gateway launch path is the `process-compose` entry in
    `nix/modules/process.nix`
  - verified the existing supervisor already captured gateway output, but only
    under `/private/tmp/devenv-*/processes/logs/`, which left `.run/logs`
    empty during outage triage
  - patched the `wendao-gateway` process definition to mirror stdout/stderr into
    `.run/logs/wendao-gateway.stdout.log` and
    `.run/logs/wendao-gateway.stderr.log` with `tee`, while preserving the
    supervisor-owned logs
  - extended the existing Nix regression so the log directory and mirror wiring
    stay pinned in the process definition
- **Validation Executed**:
  - `direnv exec . env PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 uv run pytest -c /dev/null tests/test_wendao_gateway_process_nix.py`
  - `direnv exec . git diff --check`

### [DONE] Feature: Wendao FTS `_score` warning suppression

- **Objective**: reduce avoidable gateway stderr churn after the runtime log
  mirror landed, so post-restart outage triage is not dominated by Lance
  deprecation noise.
- **Update**:
  - verified that the new `.run/logs/wendao-gateway.stderr.log` mirror was
    healthy after restart and that the main remaining log volume came from Lance
    `_score` autoprojection deprecation warnings rather than panic/error output
  - patched the shared `xiuxian-vector` FTS streaming wrapper to call
    `disable_scoring_autoprojection()`, which matches Lance 3.0.1's future
    projection contract for explicit column projections
  - added a Lance FTS regression proving projected FTS batches no longer gain an
    implicit `_score` column when the caller does not request it
- **Validation Executed**:
  - `direnv exec . cargo nextest run -p xiuxian-vector --test test_lance_fts`
  - `direnv exec . cargo check -p xiuxian-wendao`
  - `direnv exec . git diff --check`

### [DONE] Feature: Backend-issued code AST retrieval atoms

- **Objective**: upgrade the code AST retrieval contract so declaration, logic-block, and symbol atoms come from the backend response instead of being frontend-only derivations.
- **Update**:
  - extended `GET /api/analysis/code-ast` with compact retrieval atoms for declaration-, block-, and symbol-backed surfaces
  - the backend now emits `owner id`, `surface`, `chunk id`, `semantic type`, `fingerprint`, `token estimate`, optional `display label / excerpt`, and optional `line start / line end` spans on block chunks
  - the frontend AST waterfall now prefers backend-issued declaration atoms for declaration cards, backend-issued block atoms for validation / execution / return stages, and backend-issued symbol atoms for symbol cards and ranked anchor cards
  - logic-block titles and copy payload excerpts now also prefer backend-issued block metadata, and logic-block atoms stay frontend-derived only as a fallback path when no matching backend block chunk is present
  - added backend router regression coverage and frontend view coverage proving backend-issued atom ids override the local fallback contract
- **Validation Executed**:
  - `direnv exec . cargo fmt -p xiuxian-wendao`
  - `direnv exec . cargo test -p xiuxian-wendao gateway::studio::router::tests::code_ast --lib`
  - `direnv exec . cargo check -p xiuxian-wendao --lib --keep-going`
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm test -- src/components/ZenSearch/__tests__/CodeAstAnatomyView.test.tsx src/components/ZenSearch/StructuredDashboard/__tests__/StructuredIntelligenceDashboard.test.tsx src/components/ZenSearch/StructuredDashboard/__tests__/StructuredCodeInspector.test.tsx src/components/ZenSearch/__tests__/ZenSearchPreviewPane.test.tsx'`
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm run build'`

### [DONE] Feature: Shared RetrievalChunk contract for markdown and code atoms

- **Objective**: unify markdown and code AST retrieval metadata behind one explicit cross-surface payload so RAG- and agent-facing surfaces no longer depend on two unrelated atom shapes.
- **Update**:
  - introduced shared backend `RetrievalChunk` / `RetrievalChunkSurface` types under `gateway/studio/types/retrieval.rs`
  - switched markdown and code AST `retrievalAtoms` to that shared payload while preserving per-surface narrowing through `surface`
  - updated frontend bindings to expose the shared contract and keep markdown/code-specific atom types as narrowed views over it
  - kept the Zen markdown waterfall and code AST waterfall behavior stable while moving their lookup paths onto the shared contract
- **Validation Executed**:
  - `direnv exec . cargo fmt -p xiuxian-wendao`
  - `direnv exec . cargo test -p xiuxian-wendao gateway::studio::router::tests::code_ast --lib`
  - `direnv exec . cargo test -p xiuxian-wendao analyze_markdown_emits_retrieval_atoms_for_ --lib`
  - `direnv exec . cargo check -p xiuxian-wendao --lib --keep-going`
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm test -- src/components/panels/DirectReader/MarkdownWaterfall.test.tsx src/components/ZenSearch/__tests__/CodeAstAnatomyView.test.tsx src/components/ZenSearch/__tests__/useZenSearchPreview.test.tsx src/components/ZenSearch/__tests__/ZenSearchPreviewEntity.test.tsx'`
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm run build'`

### [DONE] Feature: Repo-seed routing for long-tail code search queries

- **Objective**: cut the remaining steady-state `code_search` tail by routing
  exact repo-name seed queries to one repo instead of sending them through
  all-repo fanout.
- **Update**:
  - added repo-seed normalization and unique repo inference in
    `gateway/studio/search/handlers/code_search/query.rs`
  - `build_code_search_response_with_budget` now upgrades exact repo-name
    queries such as `SciMLBase` into an effective repo hint before cache-key
    construction, repo dispatch, per-repo limit selection, and repo-wide budget
    selection
  - this keeps explicit `repo:` syntax unchanged, but collapses benchmark-like
    repo seed queries off the expensive all-repo path
  - added query-level regressions for unique and ambiguous repo-seed inference,
    plus an indexing regression proving `SciMLBase` no longer inherits pending
    state from unrelated queued repos
- **Validation Executed**:
  - `direnv exec . cargo nextest run -p xiuxian-wendao -E 'test(parse_code_search_query_extracts_repo_lang_and_kind_filters) | test(repo_wide_code_search_timeout_applies_only_without_repo_hint) | test(repo_wide_code_search_scopes_per_repo_limits) | test(parse_code_search_query_preserves_repo_identifier_case) | test(infer_repo_hint_from_query_matches_unique_normalized_repo_seed) | test(infer_repo_hint_from_query_ignores_ambiguous_normalized_repo_seed) | test(build_code_search_response_infers_repo_seed_for_exact_repo_name_query) | test(build_code_search_response_marks_partial_when_repo_wide_budget_expires) | test(build_code_search_response_skips_unsupported_repositories_when_searching_all_repos) | test(build_code_search_response_returns_pending_payload_for_explicit_repo_without_snapshot)'`
  - `direnv exec . cargo check -p xiuxian-wendao`
  - `direnv exec . git diff --check`
