# Daily GTD Log - 2026-03-27

### [DONE] Feature: Python local retrieval backend removal slice

- **Objective**: remove more Python-owned local vector execution surfaces so
  `foundation` stops presenting LanceDB-backed retrieval as a supported
  runtime path.
- **Update**:
  - removed `lancedb` from `packages/python/foundation/pyproject.toml`
  - deleted `packages/python/foundation/src/omni/rag/retrieval/lancedb.py`
  - removed `LanceRetrievalBackend` from `omni.rag` and
    `omni.rag.retrieval` exports
  - changed `create_retrieval_backend(...)` to raise an explicit
    Arrow-Flight-only removal error instead of creating local Python backends
  - deleted the old `test_retrieval_quality.py` suite that assumed local
    Python retrieval ownership
- **Validation Executed**:
  - `direnv exec . uv run pytest packages/python/foundation/tests/unit/rag/test_retrieval_factory.py packages/python/foundation/tests/unit/rag/test_retrieval_namespace.py packages/python/foundation/tests/unit/rag/test_retrieval_architecture_rules.py packages/python/foundation/tests/unit/rag/test_namespace_lazy_imports.py -q`
  - `direnv exec . rg -n "LanceRetrievalBackend|lancedb>=|from \\.lancedb|\\.lancedb|create_retrieval_backend\\(\\\"lance\\\"|create_retrieval_backend\\(\\\"hybrid\\\"" packages/python/foundation -g '*.py' -g '*.toml'`
  - `direnv exec . git diff --check`

### [DONE] Feature: Python memory LanceDB store removal slice

- **Objective**: remove the remaining Python-owned LanceDB memory backend so
  `ProjectMemory` stops pretending Python still owns durable local memory
  storage.
- **Update**:
  - deleted `packages/python/foundation/src/omni/foundation/services/memory/stores/lancedb.py`
  - changed
    `packages/python/foundation/src/omni/foundation/services/memory/core/project_memory.py`
    into an explicit removal surface that raises a Rust/Arrow Flight migration
    error
  - rewrote `memory` package docs and exports to describe the Python storage
    backend as removed rather than still available
  - replaced the old Lance-only core memory tests with a small removal test
    suite and deleted the old memory fixtures
  - converted `packages/python/foundation/src/omni/foundation/services/__init__.py`
    to lazy exports so targeted memory tests do not import the entire vector
    stack during collection
- **Validation Executed**:
  - `direnv exec . uv run pytest packages/python/core/tests/units/test_memory/test_project_memory.py -q`
  - `direnv exec . rg -n "LanceDBMemoryStore|services\\.memory\\.stores\\.lancedb|memory\\.lance|Use LanceDB for Memory Storage|LanceDB-backed ProjectMemory" packages/python/core/tests packages/python/foundation/src packages/python/foundation/tests -g '*.py'`
  - `direnv exec . git diff --check`

### [DONE] Feature: Python vector bridge public-surface removal slice

- **Objective**: stop exposing Python vector bindings as a supported public
  bridge feature while the repository moves to Rust-owned Arrow Flight
  transport.
- **Update**:
  - changed
    `packages/python/foundation/src/omni/foundation/bridge/rust_vector.py`
    into a removal stub that raises an Arrow Flight migration error
  - removed `RustVectorStore`, `get_vector_store`, and `RUST_AVAILABLE` from
    the public `omni.foundation.bridge` package exports
  - rewired
    `packages/python/foundation/src/omni/foundation/services/router_scores.py`
    to import the stub directly instead of depending on the bridge package root
  - deleted the foundation-side Rust vector bridge test files that assumed the
    bindings remained a supported feature
  - added a small bridge removal test covering the new public behavior
- **Validation Executed**:
  - `direnv exec . uv run pytest packages/python/foundation/tests/unit/bridge/test_vector_removal.py -q`
  - `direnv exec . rg -n "test_skills_rust_bridge_contract|test_phase2_vector_features|test_rust_vector_bridge_schema|test_list_all_tools_integration|test_vector_memory_guard|test_connection|test_search_cache_integration|RustVectorStore|get_vector_store\\(|RUST_AVAILABLE" packages/python/foundation/tests packages/python/foundation/src/omni/foundation/bridge packages/python/foundation/src/omni/foundation/services/router_scores.py -g '*.py'`
  - `direnv exec . git diff --check`

### [DONE] Feature: Tracer retrieval runtime de-Lance cleanup

- **Objective**: remove the remaining implicit local Lance backend assumption
  from `foundation.tracer` runtime/retrieval surfaces without preserving a
  Python local execution path.
- **Update**:
  - changed `RetrievalToolInvoker` to require explicit injected backends and
    removed implicit local backend construction
  - changed tracer runtime/schema defaults from `lance` to `vector`
  - updated tracer tests to cover explicit failure when no vector backend is
    injected
  - converted `packages/python/foundation/src/omni/tracer/__init__.py` to lazy
    exports so healthy tracer slices can import without tripping unrelated
    broken modules during package initialization
- **Validation Executed**:
  - `direnv exec . uv run pytest packages/python/foundation/tests/unit/tracer/test_retrieval_invoker.py packages/python/foundation/tests/unit/tracer/test_invoker_stack.py packages/python/foundation/tests/unit/tracer/test_pipeline_runtime_checkpointer.py -q`
  - `direnv exec . rg -n "\\blance\\b|LanceRetrievalBackend" packages/python/foundation/src/omni/tracer packages/python/foundation/tests/unit/tracer -g '*.py'`
  - `direnv exec . git diff --check`

### [DONE] Feature: Python test-kit fixture surface shrink

- **Objective**: keep shrinking Python's legacy surface so `omni.test_kit`
  stops auto-importing broken or MCP-bound fixture modules from its package
  root.
- **Update**:
  - removed dead package-level fixture exports from the then-retained
    standalone Python test-helper package
  - removed package-level auto-exports for Live-Wire protocol fixtures so the
    fixture namespace no longer pulled old server code in by default
  - added a smoke test for the narrowed package-level export surface
- **Validation Executed**:
  - targeted archive-era fixture export checks completed successfully during
    the original cut
  - `direnv exec . uv run python - <<'PY' ... import omni.test_kit.fixtures ... PY`
  - `direnv exec . git diff --check`

### [DONE] Feature: Python MCP delete-first cleanup slice

- **Objective**: physically remove the lowest-value standalone MCP-only Python
  verification surfaces so repository entrypoints stop preserving dead
  `omni.agent.mcp_server` paths.
- **Update**:
  - deleted `scripts/verify_mcp_interface.py`
  - deleted a matching legacy Live-Wire fixture module from the old
    standalone Python test-helper surface
  - removed the stale `scripts/verify_mcp_interface.py` call from
    `justfile`'s `verify-native-runtime` recipe
- **Validation Executed**:
  - targeted archive-era fixture export checks completed successfully during
    the original cut
  - `direnv exec . uv run python - <<'PY' ... import omni.test_kit.fixtures ... PY`
  - targeted residual scans confirmed the deleted interface and Live-Wire
    surfaces were absent from active scripts
  - `direnv exec . git diff --check`

### [DONE] Feature: Python standalone MCP package removal

- **Objective**: remove the standalone Python MCP package and its direct
  script/test/documentation surfaces so MCP stops existing as a first-class
  Python package boundary.
- **Update**:
  - deleted `packages/python/mcp-server/`
  - deleted standalone MCP scripts:
    `scripts/verify_system.py`,
    `scripts/channel/test-xiuxian-daochang-mcp-tools-list-concurrency-sweep.sh`,
    and the associated `mcp_tools_list_concurrency_sweep_runtime*` helpers
  - removed root `pytest`/`justfile`/CI/docs references to the deleted
    package and scripts
  - removed `xiuxian-mcp` from root and `packages/python/agent` Python
    workspace dependency declarations
- **Validation Executed**:
  - `direnv exec . rg -n "packages/python/mcp-server|verify_system\\.py|test-xiuxian-daochang-mcp-tools-list-concurrency-sweep|ci:mcp-tools-list-sweep" pyproject.toml packages/python/agent/pyproject.toml justfile docs scripts -g '*'`
  - `direnv exec . git diff --check`

### [DONE] Feature: Python MCP startup surface removal

- **Objective**: remove the last Python-side MCP process-management entrypoints
  so `just`, `scripts/channel`, and `test-kit` stop exposing restart/startup
  workflows for a Python MCP server that no longer exists.
- **Update**:
  - deleted the Python MCP restart and startup-suite/stress scripts from
    `scripts/channel/`
  - deleted the matching tests from the then-retained standalone Python
    test-helper surface
    scripts
  - removed the `benchmark-mcp-tools-list-sweep`, `mcp-restart`,
    `agent-channel-mcp-startup-stress`, and
    `agent-channel-mcp-startup-suite` recipes from `justfile`
  - removed the matching smoke reference from `scripts/ci_scripts_smoke.sh`
- **Validation Executed**:
  - targeted residual scans confirmed the deleted startup and restart surfaces
    were absent from active scripts, recipes, and the then-retained
    test-helper slice
  - `direnv exec . git diff --check`

### [DONE] Feature: Rust `xiuxian-mcp` entrypoint retirement slice

- **Objective**: remove the standalone Rust crate validation/process surfaces
  for `xiuxian-mcp` so the repository stops presenting it as a supported
  top-level package workflow while the remaining `xiuxian-daochang` imports are
  being unwound.
- **Update**:
  - removed `rust-xiuxian-mcp` and `test-mcp-integration` from `justfile`
  - deleted `scripts/rust/xiuxian_mcp_check.sh`
  - updated Wendao feature docs so `xiuxian-mcp` is no longer described as a
    permanent dependency target
- **Validation Executed**:
  - `direnv exec . rg -n "rust-xiuxian-mcp|test-mcp-integration|xiuxian_mcp_check\\.sh" justfile scripts docs -g '*'`
  - `direnv exec . git diff --check`

### [DONE] Feature: `xiuxian-daochang` source-level `xiuxian-mcp` decoupling slice

- **Objective**: remove direct `xiuxian_mcp` helper usage from
  `xiuxian-daochang/src` so the remaining Rust MCP dependency is confined to
  the crate boundary and tests rather than spread across runtime/agent source
  modules.
- **Update**:
  - added local MCP helper utilities in
    `packages/rust/crates/xiuxian-daochang/src/agent/mcp/helpers.rs`
  - rewired agent MCP dispatch, tool-schema shaping, timeout/degraded payload
    shaping, and soft-fail logic to use those local helpers instead of
    `xiuxian_mcp::*`
  - inlined runtime MCP option/server resolution into
    `packages/rust/crates/xiuxian-daochang/src/runtime_agent_factory/mcp.rs`
  - fixed `packages/rust/crates/xiuxian-daochang/src/runtime_agent_factory/mod.rs`
    to consume the current flat runtime MCP option fields rather than the
    stale `connect_config.*` path
- **Validation Executed**:
  - `direnv exec . rg -n "use xiuxian_mcp|xiuxian_mcp::" packages/rust/crates/xiuxian-daochang/src -g '*.rs'`
  - `direnv exec . git diff --check`
  - `direnv exec . cargo check -p xiuxian-daochang` (still running through the
    full dependency tree at handoff time; no source-level `xiuxian_mcp`
    regressions surfaced before compaction)

### [DONE] Feature: Rust `xiuxian-mcp` crate removal

- **Objective**: remove the `packages/rust/crates/xiuxian-mcp` crate itself so
  Rust no longer carries a dedicated MCP transport/runtime package boundary.
- **Update**:
  - copied `omni.mcp.tool_result.v1.schema.json` into
    `packages/rust/crates/xiuxian-wendao/resources/` and rewired
    `xiuxian-wendao/src/schemas.rs` to load it locally
  - removed `xiuxian-mcp` from `packages/rust/crates/xiuxian-daochang/Cargo.toml`
  - removed `packages/rust/crates/xiuxian-mcp` from the workspace member list
  - deleted the last `xiuxian-daochang` test that directly imported
    `xiuxian_mcp`
  - deleted `packages/rust/crates/xiuxian-mcp/`
- **Validation Executed**:
  - `direnv exec . rg -n "use xiuxian_mcp|xiuxian_mcp::|xiuxian-mcp/resources|packages/rust/crates/xiuxian-mcp" packages/rust Cargo.toml -g '*.rs' -g 'Cargo.toml'`
  - `direnv exec . git diff --check`

### [DONE] Feature: Rust MCP CI surface cleanup

- **Objective**: remove the remaining default CI and `justfile` validation
  lanes that still treated MCP as a first-class Rust verification target after
  the crate and Python runtime were removed.
- **Update**:
  - removed `rust-xiuxian-llm-mcp` and
    `rust-xiuxian-daochang-mcp-facade-smoke` from `justfile`
  - removed the matching calls from `scripts/ci/rust_quality_gate_ci.sh` and
    `scripts/ci_scripts_smoke.sh`
  - removed the stale `xiuxian-mcp` presence assertions from
    `scripts/rust/xiuxian_daochang_dependency_assertions.sh`
- **Validation Executed**:
  - `direnv exec . rg -n "rust-xiuxian-daochang-mcp-facade-smoke|rust-xiuxian-llm-mcp|xiuxian_daochang_mcp_facade_smoke|xiuxian_llm_mcp_check|xiuxian-mcp v" justfile scripts docs -g '*'`
  - `direnv exec . git diff --check`

### [DONE] Feature: Developer docs de-MCP cleanup

- **Objective**: remove the most misleading developer-facing instructions that
  still described `omni mcp`, Python MCP, `.mcp.json`, or `MCP-Server` as the
  current way to run or validate the system.
- **Update**:
  - rewrote
    `packages/rust/crates/xiuxian-daochang/README.md` to describe external
    tool integration as legacy compatibility rather than the preferred path
  - removed `omni mcp` / `.mcp.json` runbook guidance from
    `docs/02_dev/HANDBOOK.md`
  - reframed `docs/developer/hot-reload.md` as a legacy Python-skill note
    instead of an MCP-server feature guide
  - removed stale `MCP-Server` package/testing language from
    `docs/developer/testing.md`
  - removed the stale `mcp` command row from `docs/developer/cli.md`
- **Validation Executed**:
  - `direnv exec . rg -n "omni mcp|Python MCP|MCP-Server|OMNI_MCP_URL|\\.mcp\\.json|mcp\\.json|xiuxian-mcp" packages/rust/crates/xiuxian-daochang/README.md docs/02_dev/HANDBOOK.md docs/developer/hot-reload.md docs/developer/testing.md docs/developer/cli.md -g '*.md'`
  - `direnv exec . git diff --check`

### [DONE] Feature: Python config paths module cleanup

- **Objective**: remove a physically corrupted legacy module so
  `omni.foundation.config.paths` becomes a clean PRJ-path resolver instead of
  carrying unrelated writer/MCP implementation debris.
- **Update**:
  - rebuilt `packages/python/foundation/src/omni/foundation/config/paths.py`
    as a minimal `ConfigPaths` + `get_config_paths()` module
  - removed stale path-helper lazy exports from
    `packages/python/foundation/src/omni/foundation/__init__.py`
  - fixed `packages/python/foundation/src/omni/rag/retrieval/response.py` to
    call `get_config_paths()` instead of constructing `ConfigPaths()` without
    arguments
  - added
    `packages/python/foundation/tests/unit/config/test_paths.py` as focused
    coverage for the cleaned path resolver
- **Validation Executed**:
  - `direnv exec . python -m compileall packages/python/foundation/src/omni/foundation/config packages/python/foundation/src/omni/foundation/api packages/python/foundation/src/omni/rag/retrieval`
  - `direnv exec . uv run pytest packages/python/foundation/tests/unit/config/test_paths.py -q`
  - `direnv exec . rg -n "get_mcp_config_path|get_project_config_path|get_anthropic_settings_path" packages/python/foundation/src/omni/foundation/__init__.py packages/python/foundation/src/omni/foundation/config/paths.py -g '*.py'`
  - `direnv exec . git diff --check`
  - noted but not expanded in this slice: broader pre-existing Python test
    failures still remain around `omni_core_rs`, `omni.rag.zk_search`, and
    `skills_monitor` import breakage

### [DONE] Feature: StatusBar shared contract extraction

- **Objective**: move shared StatusBar-facing frontend contracts into the
  `statusBar/` feature folder so dependent modules stop importing shared types
  from `StatusBar.tsx`.
- **Update**:
  - added `.data/wendao-frontend/src/components/statusBar/types.ts`
  - added `.data/wendao-frontend/src/components/statusBar/index.ts`
  - rewired `StatusBar.tsx`, repo-diagnostics, FileTree, MainView, and the
    frontend component barrel to consume shared status-bar contracts from the
    feature-folder surface instead of from `StatusBar.tsx`
  - switched those imports to explicit `statusBar/index` and
    `statusBar/types` file paths so `StatusBar.tsx` no longer collides with
    `statusBar/` on case-insensitive filesystems during test/module resolution
- **Validation Executed**:
  - `direnv exec . npm --prefix .data/wendao-frontend test -- --run src/components/statusBar/model.test.ts src/components/statusBar/RepoIndexStatusView.test.tsx src/components/juliaDeploymentInspection/controller.test.tsx src/components/juliaDeploymentInspection/View.test.tsx src/components/juliaDeploymentInspection/index.test.ts src/components/RepoDiagnosticsPage.test.tsx src/components/panels/MainView/useMainViewController.test.tsx src/components/StatusBar.test.tsx src/App.test.tsx src/api/apiContracts.test.ts src/api/clientFacade.test.ts src/api/client.test.ts`
  - `direnv exec . git diff --check`

### [DONE] Feature: StatusBar repo-index view contract extraction

- **Objective**: move the repo-index subview props contract into the shared
  `statusBar/` feature-folder surface so `RepoIndexStatusView.tsx` no longer
  owns its view contract inline.
- **Update**:
  - added `RepoIndexStatusViewModel` plus shared `StatusTone` to
    `.data/wendao-frontend/src/components/statusBar/types.ts`
  - rewired
    `.data/wendao-frontend/src/components/statusBar/model.ts` to return the
    shared repo-index view contract
  - rewired
    `.data/wendao-frontend/src/components/statusBar/RepoIndexStatusView.tsx`
    to consume the shared contract instead of maintaining a private props shape
  - re-exported the shared view contract from
    `.data/wendao-frontend/src/components/statusBar/index.ts`
- **Validation Executed**:
  - `direnv exec . npm --prefix .data/wendao-frontend test -- --run src/components/statusBar/model.test.ts src/components/statusBar/RepoIndexStatusView.test.tsx src/components/StatusBar.test.tsx src/App.test.tsx src/api/apiContracts.test.ts src/api/clientFacade.test.ts src/api/client.test.ts`
  - `direnv exec . git diff --check`

### [DONE] Feature: Wendao core runtime plugin migration package

- **Objective**: produce a complete, implementation-grade migration plan for
  Wendao's move toward `core`, `runtime`, and independently published
  Arrow-native plugin packages.
- **Update**:
  - added `docs/rfcs/2026-03-27-wendao-core-runtime-plugin-migration-rfc.md`
  - added `.data/blueprints/wendao_arrow_plugin_core_runtime_migration.md`
  - defined target crate topology, phase gates, ownership rules, and migration
    sequencing for `core`, `runtime`, and plugin-package externalization
  - revised the migration package so feature-folder-first modularization,
    interface-only `mod.rs`, and responsibility-oriented namespace naming are
    hard gates rather than style suggestions
  - added
    `packages/rust/crates/xiuxian-wendao/docs/06_roadmap/409_core_runtime_plugin_surface_inventory.md`
    as the `P0 / Mapping Gate` inventory for current Julia-specific host
    surfaces, target owners, and destination feature folders
  - added
    `packages/rust/crates/xiuxian-wendao/docs/06_roadmap/410_p1_generic_plugin_contract_staging.md`
    to freeze the first generic host vocabulary and type families for
    capability, provider, transport, and artifact generalization
  - added
    `packages/rust/crates/xiuxian-wendao/docs/06_roadmap/411_p1_first_code_slice_plan.md`
    to define the first implementation module tree, compatibility shim rules,
    file touch order, and test scope before code changes begin
  - synced `docs/index.md`,
    `packages/rust/crates/xiuxian-wendao/docs/index.md`, and
    `packages/rust/crates/xiuxian-wendao/docs/06_roadmap/404_repo_intelligence_for_sciml_and_msl.md`
    plus `packages/rust/crates/xiuxian-wendao/docs/06_roadmap/405_large_rust_modularization.md`
    so the migration package is visible from both global and Wendao-local docs
- **Validation Executed**:
  - `direnv exec . git diff --check`

### [DONE] Feature: Wendao `P1` generic plugin runtime first code slice

- **Objective**: land the first in-tree generic plugin runtime seam so new
  link-graph plugin logic can target capability/artifact/provider/transport
  vocabulary instead of growing new Julia-specific host types.
- **Update**:
  - added
    `packages/rust/crates/xiuxian-wendao/src/link_graph/plugin_runtime/`
    with feature-folder ownership for `ids`, `transport`, `capabilities`,
    `artifacts`, `compat`, and focused tests
  - added generic runtime-facing records for plugin ids, artifact ids,
    capability bindings, transport endpoints, launch specs, and artifact
    payloads
  - added Julia compatibility conversion helpers so the current rerank runtime
    config and deployment artifact can map into the new generic records
  - rewired
    `packages/rust/crates/xiuxian-wendao/src/link_graph/runtime_config.rs`
    so the legacy Julia deployment-artifact helpers delegate through the
    generic artifact resolver and renderer
  - extended that generic artifact path through
    `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/capabilities/deployment.rs`,
    `packages/rust/crates/xiuxian-wendao/src/gateway/studio/types/config.rs`,
    `packages/rust/crates/xiuxian-wendao/src/gateway/openapi/paths/ui.rs`,
    and
    `packages/rust/crates/xiuxian-wendao/src/zhenfa_router/native/deployment.rs`
    while preserving the current Julia-compatible outward surface
  - added generic rerank-binding normalization in
    `packages/rust/crates/xiuxian-wendao/src/link_graph/runtime_config/models/`
    and `packages/rust/crates/xiuxian-wendao/src/link_graph/runtime_config.rs`
    so the retrieval runtime can now resolve a provider-neutral
    `PluginCapabilityBinding` from the current Julia-backed config
  - replaced
    `packages/rust/crates/xiuxian-wendao/src/link_graph/index/search/plan/payload/quantum.rs`
    with the `payload/quantum/` feature folder so the first retrieval
    execution path now consumes the generic rerank binding behind a modular
    `flow.rs` / `rerank.rs` split instead of continuing as one flat
    Julia-heavy file
  - updated
    `packages/rust/crates/xiuxian-wendao/src/link_graph/index/search/quantum_fusion/vector_ignition.rs`
    and
    `packages/rust/crates/xiuxian-wendao/src/link_graph/index/search/quantum_fusion/openai_ignition.rs`
    so plugin rerank request-batch builders now use the generic
    `build_plugin_rerank_request_batch(...)` name, with Julia-named builders
    preserved as compatibility shims
  - updated those same semantic-ignition adapters so request error types now
    use capability-oriented `*PluginRerankRequestError` names, while the old
    Julia-named error types remain as compatibility aliases on the legacy shim
    signatures
  - updated
    `packages/rust/crates/xiuxian-wendao/src/analyzers/service/julia_transport.rs`
    so analyzer transport now exposes `PluginArrowRequestRow` and
    `build_plugin_arrow_request_batch(...)` as the primary request-contract
    names, while the Julia-named request row and builder remain as
    compatibility alias/shim surfaces
  - updated that same analyzer transport surface so response handling now uses
    `PluginArrowScoreRow`, `decode_plugin_arrow_score_rows(...)`, and
    `fetch_plugin_arrow_score_rows_for_repository(...)` as the primary names,
    while the Julia-named response row and helpers remain as compatibility
    alias/shim surfaces
  - rewired `quantum_fusion/vector_ignition.rs`,
    `quantum_fusion/openai_ignition.rs`, and
    `analyzers/service/julia_transport_tests.rs` to consume the new
    `PluginArrow*` request vocabulary
  - rewired `payload/quantum/rerank.rs`, `payload/quantum/tests.rs`, and the
    analyzer transport tests to consume the new `PluginArrow*` response
    vocabulary
  - replaced
    `packages/rust/crates/xiuxian-wendao/src/analyzers/service/julia_transport.rs`
    with the `analyzers/service/julia_transport/` feature folder so schema,
    request, response, fetch, error, and test ownership are now split behind
    an interface-only `mod.rs`
  - replaced
    `packages/rust/crates/xiuxian-wendao/src/link_graph/runtime_config/resolve/policy.rs`
    with the `runtime_config/resolve/policy/` feature folder so retrieval
    policy resolution now lives behind an interface-only `mod.rs`
  - then split retrieval resolution again into
    `runtime_config/resolve/policy/retrieval/` with interface-only `mod.rs`
    plus dedicated `base.rs`, `semantic_ignition.rs`, and `provider.rs`
  - added `julia_rerank_provider_selector()` and
    `julia_deployment_artifact_selector()` as the primary typed selector seams
    for Julia compatibility routing, then rewired runtime-side payload
    assembly and focused tests to assert against those selectors instead of
    repeating raw Julia ids in host logic
  - added `build_rerank_provider_binding(...)` as the primary Julia-backed
    rerank binding builder, with
    `build_plugin_capability_binding_for_julia_rerank(...)` retained only as a
    compatibility shim over the same provider-oriented helper
  - stopped re-exporting raw `JULIA_*` ids from the higher-level
    `plugin_runtime` and `runtime_config::models` host surfaces so selector
    and helper seams, not raw language ids, are now the primary integration
    vocabulary outside the Julia compatibility layer
  - narrowed the legacy
    `build_plugin_capability_binding_for_julia_rerank(...)` shim to the Julia
    ownership seam and added a focused compatibility test there, so the
    higher-level host surface now exposes only
    `build_rerank_provider_binding(...)` as the primary binding-builder entry
    point
  - removed `LinkGraphJuliaAnalyzerServiceDescriptor` from the higher-level
    `runtime_config`, `link_graph`, and crate-root re-export surfaces so this
    Julia-named DTO now stays inside the Julia ownership seam instead of
    continuing to leak through the general host API
  - rewired `gateway/studio/types/config.rs` so the Julia-compatible Studio UI
    payloads now derive directly from `PluginLaunchSpec` and
    `PluginArtifactPayload`, with focused tests proving that the legacy Julia
    UI surface no longer depends on Julia runtime DTOs at that layer
  - simplified the Julia deployment-artifact HTTP JSON path so
    `gateway/studio/router/handlers/capabilities/deployment.rs` now maps the
    resolved generic `PluginArtifactPayload` directly into
    `UiJuliaDeploymentArtifact` instead of routing through `UiPluginArtifact`
  - stopped re-exporting `UiJuliaAnalyzerLaunchManifest` from the higher-level
    `gateway::studio::types` module so this Julia-specific UI DTO now stays in
    the config ownership seam instead of continuing to leak through the
    general Studio type surface
  - stopped re-exporting `UiJuliaDeploymentArtifact` from the higher-level
    `gateway::studio::types` surface as well; crate-internal handler and test
    call sites now use the `gateway::studio::types::config` seam directly
  - removed the Julia launch-manifest/deployment/runtime records from the
    `link_graph` middle-layer re-export surface and rewired crate-root
    exports to come directly from the `runtime_config` seam instead, so the
    public crate API stays intact while the intermediate host layer gets
    narrower
  - removed `LinkGraphJuliaAnalyzerLaunchManifest` and
    `LinkGraphJuliaDeploymentArtifact` from the higher-level
    `runtime_config::models` surface as well, then had `runtime_config.rs`
    re-export those DTOs directly from `models::retrieval` so the public
    runtime-config seam stays stable while the broader models layer gets
    narrower
  - removed `LinkGraphJuliaRerankRuntimeConfig` from the higher-level
    `runtime_config::models` surface too, moving crate-internal call sites to
    `models::retrieval` while keeping the public runtime-config seam stable
  - introduced `API_UI_COMPAT_DEPLOYMENT_ARTIFACT_*` as the primary internal
    constants for the legacy Julia deployment-artifact endpoint, then rewired
    route inventory/tests to use those compatibility-oriented names while
    retaining the older Julia-named constants as aliases
    ownership so the retrieval namespace itself no longer keeps
    implementation logic in `mod.rs`, and the rerank-provider parsing surface
    now stops using a Julia-specific filename for host-side ownership
  - added provider/capability-first primary helpers on
    `LinkGraphJuliaRerankRuntimeConfig`:
    `provider_launch_descriptor()`, `plugin_launch_spec()`,
    `plugin_artifact_payload()`, and `rerank_provider_binding()`, while the
    older Julia-named helper methods now delegate to those generic seams
  - rewired generic artifact resolution and Julia compatibility binding
    assembly to consume the new primary helper names instead of reaching
    through the Julia-named helper surface directly
  - replaced
    `packages/rust/crates/xiuxian-wendao/src/link_graph/runtime_config/models.rs`
    with the `runtime_config/models/` feature folder so cache,
    coactivation, related, retrieval/plugin-facing runtime records, agentic,
    and index scope now each live behind dedicated leaf modules while
    `models/mod.rs` remains interface-only
  - then split the new retrieval runtime record surface again into
    `runtime_config/models/retrieval/` with interface-only `mod.rs` plus
    dedicated `policy.rs`, `semantic_ignition.rs`, and `julia_rerank/`
    ownership so the retrieval/plugin-facing runtime record namespace does
    not regress into another flat warehouse file
  - then split `runtime_config/models/retrieval/julia_rerank.rs` into the
    `julia_rerank/` feature folder with interface-only `mod.rs` plus
    dedicated `runtime.rs`, `launch.rs`, `artifact.rs`, and
    `conversions.rs` ownership so the Julia compatibility surface no longer
    keeps config, DTOs, artifact IO, and conversion impls in one file
  - moved Julia plugin ids plus primary launch/artifact/binding conversion
    ownership into `runtime_config/models/retrieval/julia_rerank/`, reducing
    `plugin_runtime/compat/julia.rs` to a thin re-export/shim layer instead
    of a real ownership boundary
  - added `PluginArtifactSelector` plus
    `julia_deployment_artifact_selector()` and rewired runtime-config,
    gateway, Zhenfa, and plugin-runtime deployment-artifact call sites to use
    typed selector helpers instead of stitching Julia plugin/artifact
    constants together directly
  - added `julia_rerank_provider_selector()` and rewired the Julia rerank
    generic binding builder plus focused tests to use a typed capability
    selector helper instead of directly asserting on stitched Julia
    provider/capability constants
  - extracted Arrow transport-client construction into
    `packages/rust/crates/xiuxian-wendao/src/link_graph/plugin_runtime/transport/client.rs`
    so rerank execution can reuse a generic binding-to-transport adapter
  - serialized the affected config-override tests with `serial_test` to avoid
    global runtime-config races
- **Validation Executed**:
  - `direnv exec . cargo test -p xiuxian-wendao link_graph::plugin_runtime:: --lib`
  - `direnv exec . cargo test -p xiuxian-wendao link_graph::runtime_config::tests:: --lib`
  - `direnv exec . cargo test -p xiuxian-wendao julia_deployment_artifact_handler_returns --lib`
  - `direnv exec . cargo test -p xiuxian-wendao render_julia_deployment_artifact --lib`
  - `direnv exec . cargo test -p xiuxian-wendao link_graph::index::search::plan::payload::quantum:: --lib`
  - `direnv exec . cargo test -p xiuxian-wendao build_plugin_rerank_request_batch_uses_anchor_ids_as_request_doc_ids --lib`
  - `direnv exec . cargo test -p xiuxian-wendao build_plugin_rerank_request_batch_uses_explicit_query_vector --lib`
  - `direnv exec . cargo test -p xiuxian-wendao build_plugin_rerank_request_batch_rejects_missing_query_signal --lib`
  - `direnv exec . cargo test -p xiuxian-wendao analyzers::service::julia_transport::tests:: --lib`
  - `direnv exec . cargo test -p xiuxian-wendao link_graph::index::search::plan::payload::quantum:: --lib`
  - `direnv exec . cargo test -p xiuxian-wendao link_graph::runtime_config::tests:: --lib`
  - `direnv exec . git diff --check`

### [DONE] Feature: Wendao Arrow plugin transport RFC draft

- **Objective**: formalize a Wendao plugin architecture that keeps Arrow IPC
  and DataFusion as core assumptions, gives Arrow Flight highest priority, and
  defines a sanctioned fallback path for runtimes such as Julia whose current
  Arrow packages do not yet expose Flight support.
- **Update**:
  - added `docs/rfcs/2026-03-27-wendao-arrow-plugin-flight-rfc.md`
  - defined an Arrow-first plugin protocol, a capability-first package model,
    Flight-first transport ordering, and Arrow IPC fallback semantics
  - synced `packages/rust/crates/xiuxian-wendao/docs/index.md` so the RFC is
    visible from the Wendao package documentation surface
- **Validation Executed**:
  - `direnv exec . git diff --check`

### [DONE] Feature: Wendao RFC Phase 2 graph service consolidation

- **Objective**: pull the Studio graph handler cluster behind one shared
  service boundary so route files stop owning query-core graph execution,
  explain logging, and topology assembly directly.
- **Update**:
  - added
    `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/graph/service.rs`
    with typed `run_graph_neighbors(...)`, `run_node_neighbors(...)`, and
    `run_topology_3d(...)` entrypoints
  - rewired
    `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/graph/neighbors.rs`
    into a thin route adapter over the new service boundary
  - rewired
    `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/graph/topology.rs`
    into a thin route adapter over `run_topology_3d(...)`
  - kept `graph/mod.rs` interface-only while moving center-node resolution,
    query-core projection execution, explain summary logging, and topology
    construction below the route layer
- **Validation Executed**:
  - `direnv exec . cargo check -p xiuxian-wendao`
  - `direnv exec . cargo test -p xiuxian-wendao --lib graph_neighbors_returns_center_nodes_and_links`
  - `direnv exec . cargo test -p xiuxian-wendao --lib graph_neighbors_resolves_project_prefixed_display_paths`
  - `direnv exec . cargo test -p xiuxian-wendao --lib node_neighbors_returns_stable_payload_shape`
  - `direnv exec . cargo test -p xiuxian-wendao --lib topology_3d_returns_non_empty_global_graph_payload`
  - `direnv exec . git diff --check`

### [DONE] Feature: Wendao RFC Phase 2 graph boundary closure

- **Objective**: split the remaining flat graph shared-state surface so graph
  query parsing and graph rendering helpers stop living in one file.
- **Update**:
  - replaced
    `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/graph/shared.rs`
    with:
    - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/graph/shared/mod.rs`
    - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/graph/shared/query.rs`
    - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/graph/shared/render.rs`
  - moved graph-neighbor query parsing and normalization into `query.rs`
  - moved graph-node resolution, path/label helpers, unique-path sorting, and
    topology layout helpers into `render.rs`
  - kept `graph/shared/mod.rs` interface-only and retained the stable shared
    import surface for `neighbors.rs` and `service.rs`
- **Validation Executed**:
  - `direnv exec . cargo check -p xiuxian-wendao`
  - `direnv exec . cargo test -p xiuxian-wendao --lib graph_neighbors_returns_center_nodes_and_links`
  - `direnv exec . cargo test -p xiuxian-wendao --lib node_neighbors_returns_stable_payload_shape`
  - `direnv exec . cargo test -p xiuxian-wendao --lib topology_3d_returns_non_empty_global_graph_payload`
  - `direnv exec . git diff --check`

### [DONE] Feature: Wendao RFC Phase 2 graph test boundary closure

- **Objective**: split the remaining flat graph test-support surface so
  fixture runtime, response helpers, assertions, and snapshot shaping stop
  sharing one file.
- **Update**:
  - replaced
    `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/graph/tests/fixtures.rs`
    with:
    - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/graph/tests/support/mod.rs`
    - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/graph/tests/support/fixture.rs`
    - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/graph/tests/support/response.rs`
    - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/graph/tests/support/assertions.rs`
    - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/graph/tests/support/snapshot.rs`
  - kept `graph/tests/mod.rs` interface-only and preserved the stable helper
    imports used by `neighbors.rs` and `topology.rs`
  - moved fixture runtime, graph response helpers, graph assertions, and
    snapshot payload shaping into dedicated support modules
- **Validation Executed**:
  - `direnv exec . cargo check -p xiuxian-wendao`
  - `direnv exec . cargo test -p xiuxian-wendao --lib graph_neighbors_returns_center_nodes_and_links`
  - `direnv exec . cargo test -p xiuxian-wendao --lib graph_neighbors_resolves_project_prefixed_display_paths`
  - `direnv exec . cargo test -p xiuxian-wendao --lib topology_3d_returns_non_empty_global_graph_payload`
  - `direnv exec . git diff --check`

### [DONE] Feature: Wendao RFC Phase 2 graph router export closure

- **Objective**: group the outward-facing graph handler exports behind one
  dedicated router-export seam instead of flat re-exports in `handlers/mod.rs`.
- **Update**:
  - added
    `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/graph_exports.rs`
  - updated
    `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/mod.rs`
    to delegate graph re-export ownership to `graph_exports.rs`
  - kept the external `graph_neighbors`, `node_neighbors`, and `topology_3d`
    handler names unchanged
- **Validation Executed**:
  - `direnv exec . cargo check -p xiuxian-wendao`
  - `direnv exec . cargo test -p xiuxian-wendao --lib graph_neighbors_returns_center_nodes_and_links`
  - `direnv exec . cargo test -p xiuxian-wendao --lib topology_3d_returns_non_empty_global_graph_payload`
  - `direnv exec . git diff --check`

### [DONE] Feature: Wendao RFC Phase 2 repo router export closure

- **Objective**: group the outward-facing repo handler exports behind one
  dedicated router-export seam instead of flat re-exports in `handlers/mod.rs`.
- **Update**:
  - added
    `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/repo_exports.rs`
  - updated
    `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/mod.rs`
    to delegate repo re-export ownership to `repo_exports.rs`
  - kept the external repo handler names unchanged
- **Validation Executed**:
  - `direnv exec . cargo check -p xiuxian-wendao`
  - `direnv exec . cargo test -p xiuxian-wendao --lib repo_index_status_endpoint_returns_status_payload`
  - `direnv exec . cargo test -p xiuxian-wendao --lib repo_projected_retrieval_endpoint_returns_mixed_hit_payload`
  - `direnv exec . git diff --check`

### [DONE] Feature: Wendao RFC Phase 2 analysis router export closure

- **Objective**: group the outward-facing analysis handler exports behind one
  dedicated router-export seam instead of flat re-exports in `handlers/mod.rs`.
- **Update**:
  - added
    `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/analysis_exports.rs`
  - updated
    `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/mod.rs`
    to delegate analysis re-export ownership to `analysis_exports.rs`
  - kept the external analysis handler names unchanged
- **Validation Executed**:
  - `direnv exec . cargo check -p xiuxian-wendao`
  - `direnv exec . cargo test -p xiuxian-wendao --lib resolve_code_ast_repository_and_path_requires_repo_when_ambiguous`
  - `direnv exec . git diff --check`

### [DONE] Feature: Wendao RFC Phase 2 capabilities, UI-config, and VFS router export closure

- **Objective**: close the remaining top-level router export seams so
  capabilities, UI-config, and VFS outward-facing exports stop being flat
  re-export blocks in `handlers/mod.rs`.
- **Update**:
  - added
    `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/capabilities_exports.rs`
  - added
    `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/ui_config_exports.rs`
  - added
    `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/vfs_exports.rs`
  - updated
    `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/mod.rs`
    to delegate those re-export seams to dedicated modules
  - kept all external handler names unchanged
- **Validation Executed**:
  - `direnv exec . cargo check -p xiuxian-wendao`
  - `direnv exec . cargo test -p xiuxian-wendao --lib julia_deployment_artifact_handler_returns_resolved_artifact`
  - `direnv exec . cargo test -p xiuxian-wendao --lib ui_capabilities_reports_builtin_plugin_languages`
  - `direnv exec . git diff --check`

### [DONE] Feature: Wendao RFC Phase 2 analysis handler closure

- **Objective**: replace the flat `analysis.rs` handler surface with a feature
  folder so markdown routes, code-AST routes, shared loader logic, and query
  types stop sharing one file.
- **Update**:
  - replaced
    `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/analysis.rs`
    with:
    - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/analysis/mod.rs`
    - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/analysis/types.rs`
    - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/analysis/service.rs`
    - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/analysis/markdown.rs`
    - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/analysis/code_ast.rs`
  - moved query types into `types.rs`
  - moved shared loader logic into `service.rs`
  - moved markdown and code-AST route entrypoints into dedicated route modules
  - kept `analysis/mod.rs` interface-only and preserved the external handler
    names
- **Validation Executed**:
  - `direnv exec . cargo check -p xiuxian-wendao`
  - `direnv exec . cargo test -p xiuxian-wendao --lib resolve_code_ast_repository_and_path_requires_repo_when_ambiguous`
  - `direnv exec . git diff --check`

### [DONE] Feature: Wendao RFC Phase 2 capabilities handler closure

- **Objective**: replace the flat `capabilities.rs` handler surface with a
  feature folder so capability routes, deployment-artifact routes, and query
  types stop sharing one file.
- **Update**:
  - replaced
    `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/capabilities.rs`
    with:
    - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/capabilities/mod.rs`
    - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/capabilities/types.rs`
    - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/capabilities/service.rs`
    - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/capabilities/deployment.rs`
  - moved deployment-artifact query types into `types.rs`
  - moved UI-capabilities and deployment-artifact handlers into dedicated route
    modules
  - kept `capabilities/mod.rs` interface-only and preserved the external
    handler names
- **Validation Executed**:
  - `direnv exec . cargo check -p xiuxian-wendao`
  - `direnv exec . cargo test -p xiuxian-wendao --lib ui_capabilities_reports_builtin_plugin_languages`
  - `direnv exec . cargo test -p xiuxian-wendao --lib julia_deployment_artifact_handler_returns_resolved_artifact`
  - `direnv exec . git diff --check`

### [DONE] Feature: WendaoAnalyzer Studio contract formalization

- **Objective**: formalize the Studio Julia deployment inspection endpoint as a
  first-class OpenAPI and Studio-type contract without changing runtime
  behavior.
- **Update**:
  - added canonical OpenAPI path constants and route-inventory coverage for
    `GET /api/ui/julia-deployment-artifact`
  - introduced Studio-owned JSON response types for the deployment artifact and
    launch manifest instead of returning the raw link-graph runtime struct from
    the handler
  - patched the bundled gateway OpenAPI artifact to document the Julia
    deployment endpoint, its optional `format=json|toml` query parameter, and
    the existing `repo/import-search` path that the shared route inventory was
    already expecting
- **Validation Executed**:
  - `direnv exec . cargo fmt -p xiuxian-wendao`
  - `direnv exec . cargo test -p xiuxian-wendao julia_deployment_artifact_handler_returns_resolved_artifact --lib`
  - `direnv exec . cargo test -p xiuxian-wendao julia_deployment_artifact_handler_returns_toml_when_requested --lib`
  - `direnv exec . cargo test -p xiuxian-wendao route_inventory_keeps_core_endpoints --lib`
  - `direnv exec . cargo test -p xiuxian-wendao bundled_gateway_openapi_document_covers_declared_route_inventory --lib`
  - `direnv exec . cargo check -p xiuxian-wendao --tests`
  - `direnv exec . git diff --check`

### [DONE] Feature: WendaoAnalyzer deployment artifact metadata

- **Objective**: add artifact-level inspection metadata so Julia deployment
  artifact exports can identify their own contract version and render time
  without overloading the WendaoArrow transport `schema_version`.
- **Update**:
  - extended the runtime-owned deployment artifact with
    `artifact_schema_version` and `generated_at`
  - propagated the same metadata through native/RPC export, Studio JSON
    response types, and the bundled OpenAPI examples
  - kept the launch manifest and Julia transport coordinates unchanged while
    separating artifact-level inspection metadata from transport-level schema
    metadata
- **Validation Executed**:
  - `direnv exec . cargo fmt -p xiuxian-wendao`
  - `direnv exec . cargo test -p xiuxian-wendao test_retrieval_runtime_resolves_julia_rerank_config --lib`
  - `direnv exec . cargo test -p xiuxian-wendao render_julia_deployment_artifact_json_uses_runtime_config --lib`
  - `direnv exec . cargo test -p xiuxian-wendao julia_deployment_artifact_handler_returns_resolved_artifact --lib`
  - `direnv exec . cargo check -p xiuxian-wendao --tests`
  - `direnv exec . git diff --check`

### [DONE] Feature: WendaoAnalyzer deployment file export

- **Objective**: let the visible Julia deployment inspection tool persist the
  resolved artifact to disk without adding a new endpoint family.
- **Update**:
  - extended the runtime-owned deployment artifact model with `to_json_string()`
    and `write_json_file(...)` so JSON export uses the same crate-owned
    persistence conventions as TOML export
  - added optional `output_path` support to
    `wendao.julia_deployment_artifact`, preserving existing inline render
    behavior when the path is omitted
  - updated the JSON-RPC export surface so file export works through the same
    existing tool boundary
- **Validation Executed**:
  - `direnv exec . cargo fmt -p xiuxian-wendao`
  - `direnv exec . cargo test -p xiuxian-wendao test_julia_deployment_artifact_writes_toml_file --lib`
  - `direnv exec . cargo test -p xiuxian-wendao test_julia_deployment_artifact_writes_json_file --lib`
  - `direnv exec . cargo test -p xiuxian-wendao export_julia_deployment_artifact_from_rpc_params_returns_json --lib`
  - `direnv exec . cargo test -p xiuxian-wendao export_julia_deployment_artifact_from_rpc_params_writes_json_file --lib`
  - `direnv exec . cargo check -p xiuxian-wendao --tests`
  - `direnv exec . git diff --check`

### [DONE] Feature: WendaoAnalyzer frontend deployment consumer

- **Objective**: expose the Studio Julia deployment artifact through the
  existing frontend API client surface so debug consumers can load typed JSON
  or TOML without issuing raw fetch calls.
- **Update**:
  - added frontend contract types `UiJuliaAnalyzerLaunchManifest` and
    `UiJuliaDeploymentArtifact` in
    `.data/wendao-frontend/src/api/apiContracts.ts`
  - extended the shared response transport with `handleTextResponse(...)` and
    added `api.getJuliaDeploymentArtifact()` plus
    `api.getJuliaDeploymentArtifactToml()` to the frontend runtime/facade
  - kept the current backend endpoint behavior unchanged; this milestone is a
    frontend consumer-layer landing only
- **Validation Executed**:
  - `direnv exec . npm --prefix .data/wendao-frontend test -- --run src/api/apiContracts.test.ts src/api/clientFacade.test.ts src/api/client.test.ts`
  - `direnv exec . git diff --check`

### [DONE] Feature: WendaoAnalyzer frontend StatusBar inspection

- **Objective**: surface the resolved Studio Julia deployment artifact inside
  the live Qianji Studio shell so analyzer deployment state is visible without
  leaving the workspace or issuing manual debug requests.
- **Update**:
  - extended `.data/wendao-frontend/src/App.tsx` to probe the typed
    `api.getJuliaDeploymentArtifact()` endpoint on startup and keep the result
    as additive UI state without blocking workspace startup on failures
  - extended `.data/wendao-frontend/src/components/StatusBar.tsx` with a
    compact Julia rerank chip plus hover inspection popover showing artifact
    metadata, launcher path, service mode, analyzer strategy, and Arrow
    transport coordinates
  - kept the backend endpoint and frontend API contract unchanged; this
    milestone lands the first visible consumer of the existing deployment
    artifact surface
- **Validation Executed**:
  - `direnv exec . npm --prefix .data/wendao-frontend test -- --run src/App.test.tsx src/components/StatusBar.test.tsx src/api/apiContracts.test.ts src/api/clientFacade.test.ts src/api/client.test.ts`
  - `direnv exec . git diff --check`

### [DONE] Feature: WendaoAnalyzer frontend StatusBar export actions

- **Objective**: add direct export actions to the live StatusBar deployment
  inspection surface so Studio users can copy the resolved TOML artifact and
  download the structured JSON artifact without leaving the workspace shell.
- **Update**:
  - extended `.data/wendao-frontend/src/App.tsx` with additive callbacks that
    copy `api.getJuliaDeploymentArtifactToml()` to the clipboard and download
    the current structured artifact as `julia-deployment-artifact.json`
  - extended `.data/wendao-frontend/src/components/StatusBar.tsx` with
    `Copy TOML` and `Download JSON` actions plus minimal success/error feedback
    inside the Julia deployment popover
  - added matching interaction coverage in
    `.data/wendao-frontend/src/App.test.tsx` and
    `.data/wendao-frontend/src/components/StatusBar.test.tsx`
- **Validation Executed**:
  - `direnv exec . npm --prefix .data/wendao-frontend test -- --run src/App.test.tsx src/components/StatusBar.test.tsx src/api/apiContracts.test.ts src/api/clientFacade.test.ts src/api/client.test.ts`
  - `direnv exec . git diff --check`

### [DONE] Feature: WendaoAnalyzer frontend inspection module

- **Objective**: consolidate Julia deployment inspection formatting and export
  behavior behind one frontend helper module so `App.tsx` and `StatusBar.tsx`
  stop sharing that logic implicitly.
- **Update**:
  - added
    `.data/wendao-frontend/src/components/juliaDeploymentInspection.ts`
    with typed chip/popover formatting plus TOML copy and JSON download helpers
  - rewired `.data/wendao-frontend/src/App.tsx` and
    `.data/wendao-frontend/src/components/StatusBar.tsx` to consume that
    helper instead of keeping duplicate artifact-format/export logic inline
  - added focused helper coverage in
    `.data/wendao-frontend/src/components/juliaDeploymentInspection.test.ts`
- **Validation Executed**:
  - `direnv exec . npm --prefix .data/wendao-frontend test -- --run src/components/juliaDeploymentInspection.test.ts src/components/StatusBar.test.tsx src/App.test.tsx src/api/apiContracts.test.ts src/api/clientFacade.test.ts src/api/client.test.ts`
  - `direnv exec . git diff --check`

### [DONE] Feature: WendaoAnalyzer frontend inspection feature folder

- **Objective**: align the frontend Julia deployment inspection surface with
  the project modularization standard by splitting the flat helper into a
  small feature folder with explicit seams.
- **Update**:
  - replaced the flat helper with
    `.data/wendao-frontend/src/components/juliaDeploymentInspection/`
  - split the surface into `format.ts`, `actions.ts`, `types.ts`, and
    `index.ts`, with focused tests in `index.test.ts`
  - kept `App.tsx` and `StatusBar.tsx` on the same stable module entrypoint,
    so the visible UI and export behavior remain unchanged
- **Validation Executed**:
  - `direnv exec . npm --prefix .data/wendao-frontend test -- --run src/components/juliaDeploymentInspection/index.test.ts src/components/StatusBar.test.tsx src/App.test.tsx src/api/apiContracts.test.ts src/api/clientFacade.test.ts src/api/client.test.ts`
  - `direnv exec . git diff --check`

### [DONE] Feature: WendaoAnalyzer frontend inspection subview

- **Objective**: move the remaining Julia deployment popover rendering out of
  `StatusBar.tsx` so the frontend inspection feature folder owns not only
  formatting/export logic but also the dedicated artifact subview.
- **Update**:
  - added
    `.data/wendao-frontend/src/components/juliaDeploymentInspection/View.tsx`
    as the dedicated artifact popover/chip rendering surface
  - rewired `.data/wendao-frontend/src/components/StatusBar.tsx` to mount the
    subview instead of owning the Julia artifact render block inline
  - added focused subview coverage in
    `.data/wendao-frontend/src/components/juliaDeploymentInspection/View.test.tsx`
- **Validation Executed**:
  - `direnv exec . npm --prefix .data/wendao-frontend test -- --run src/components/juliaDeploymentInspection/View.test.tsx src/components/juliaDeploymentInspection/index.test.ts src/components/StatusBar.test.tsx src/App.test.tsx src/api/apiContracts.test.ts src/api/clientFacade.test.ts src/api/client.test.ts`
  - `direnv exec . git diff --check`

### [DONE] Feature: StatusBar repo-index subview

- **Objective**: pull the repo-index diagnostics chip/popover rendering out of
  `StatusBar.tsx` so the top-level status shell no longer owns the second large
  inline popover tree either.
- **Update**:
  - added
    `.data/wendao-frontend/src/components/statusBar/RepoIndexStatusView.tsx`
    as the dedicated repo-index diagnostics chip/popover subview
  - rewired `.data/wendao-frontend/src/components/StatusBar.tsx` to mount the
    new repo-index subview instead of rendering that branch inline
  - added focused coverage in
    `.data/wendao-frontend/src/components/statusBar/RepoIndexStatusView.test.tsx`
- **Validation Executed**:
  - `direnv exec . npm --prefix .data/wendao-frontend test -- --run src/components/statusBar/RepoIndexStatusView.test.tsx src/components/juliaDeploymentInspection/View.test.tsx src/components/juliaDeploymentInspection/index.test.ts src/components/StatusBar.test.tsx src/App.test.tsx src/api/apiContracts.test.ts src/api/clientFacade.test.ts src/api/client.test.ts`
  - `direnv exec . git diff --check`

### [DONE] Feature: StatusBar model extraction

- **Objective**: remove the remaining label/tone derivation bulk from
  `StatusBar.tsx` so the component becomes a thin assembly shell over dedicated
  subviews and a dedicated model helper.
- **Update**:
  - added `.data/wendao-frontend/src/components/statusBar/model.ts`
    to derive VFS labels, repo-index labels/tones, runtime labels, and Julia
    inspection labels from the existing props
  - rewired `.data/wendao-frontend/src/components/StatusBar.tsx` to consume the
    derived model data instead of recomputing that logic inline
  - added focused model coverage in
    `.data/wendao-frontend/src/components/statusBar/model.test.ts`
- **Validation Executed**:
  - `direnv exec . npm --prefix .data/wendao-frontend test -- --run src/components/statusBar/model.test.ts src/components/statusBar/RepoIndexStatusView.test.tsx src/components/juliaDeploymentInspection/View.test.tsx src/components/juliaDeploymentInspection/index.test.ts src/components/StatusBar.test.tsx src/App.test.tsx src/api/apiContracts.test.ts src/api/clientFacade.test.ts src/api/client.test.ts`
  - `direnv exec . git diff --check`

### [DONE] Feature: Julia inspection action-state extraction

- **Objective**: remove the remaining Julia deployment export feedback state
  and handler orchestration from `StatusBar.tsx` so the inspection feature
  folder owns its own local interaction state.
- **Update**:
  - added
    `.data/wendao-frontend/src/components/juliaDeploymentInspection/controller.ts`
    with the copy/download action-state controller
  - rewired `.data/wendao-frontend/src/components/StatusBar.tsx` to consume the
    controller instead of owning the Julia export feedback logic inline
  - added focused controller coverage in
    `.data/wendao-frontend/src/components/juliaDeploymentInspection/controller.test.tsx`
- **Validation Executed**:
  - `direnv exec . npm --prefix .data/wendao-frontend test -- --run src/components/juliaDeploymentInspection/controller.test.tsx src/components/juliaDeploymentInspection/View.test.tsx src/components/juliaDeploymentInspection/index.test.ts src/components/statusBar/model.test.ts src/components/statusBar/RepoIndexStatusView.test.tsx src/components/StatusBar.test.tsx src/App.test.tsx src/api/apiContracts.test.ts src/api/clientFacade.test.ts src/api/client.test.ts`
  - `direnv exec . git diff --check`

### [DONE] Feature: Wendao RFC Phase 2 non-search analysis service consolidation

- **Objective**: pull `repo/overview` and `repo/doc-coverage` onto the same
  shared repo-analysis service-boundary style as the Phase-2 search handlers,
  so route files stop owning `with_repo_analysis(...)` orchestration directly.
- **Update**:
  - added
    `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/repo/analysis/service.rs`
    with typed `run_repo_overview(...)` and `run_repo_doc_coverage(...)`
    entrypoints
  - rewired
    `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/repo/analysis/overview.rs`
    into a thin route adapter over `run_repo_overview(...)`
  - rewired
    `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/repo/analysis/doc_coverage.rs`
    into a thin route adapter over `run_repo_doc_coverage(...)`
  - kept the external Studio payload contract unchanged while moving shared
    panic-code ownership and `with_repo_analysis(...)` orchestration below the
    route layer
- **Validation Executed**:
  - `direnv exec . cargo check -p xiuxian-wendao`
  - `direnv exec . cargo test -p xiuxian-wendao --lib repo_overview_endpoint_returns_repo_summary_payload`
  - `direnv exec . cargo test -p xiuxian-wendao --lib repo_doc_coverage_endpoint_returns_coverage_payload`

### [DONE] Feature: Wendao RFC Phase 2 projected retrieval service consolidation

- **Objective**: pull the projected retrieval endpoint cluster behind one shared
  service boundary so route files stop owning repeated repository-analysis and
  cached projected-page search orchestration directly.
- **Update**:
  - added
    `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/repo/projected_service.rs`
    with typed entrypoints for projected retrieval hit, projected retrieval
    context, projected page-index tree search, projected page search, and
    projected retrieval
  - rewired
    `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/repo/retrieval.rs`
    into a thin route adapter over that shared projected-service seam
  - kept the cached projected-page search behavior and external Studio payload
    contracts unchanged while moving panic-code ownership and orchestration
    below the route layer
- **Validation Executed**:
  - `direnv exec . cargo check -p xiuxian-wendao`
  - `direnv exec . cargo test -p xiuxian-wendao --lib repo_projected_page_index_tree_search_endpoint_returns_hit_payload`
  - `direnv exec . cargo test -p xiuxian-wendao --lib repo_projected_page_search_endpoint_returns_projection_payload`
  - `direnv exec . cargo test -p xiuxian-wendao --lib repo_projected_retrieval_endpoint_returns_mixed_hit_payload`
  - `direnv exec . cargo test -p xiuxian-wendao --lib repo_projected_retrieval_hit_endpoint_returns_page_payload`
  - `direnv exec . cargo test -p xiuxian-wendao --lib repo_projected_retrieval_context_endpoint_returns_node_context_payload`

### [DONE] Feature: Wendao RFC Phase 2 projected pages service consolidation

- **Objective**: pull the projected page lookup cluster behind the same shared
  projected-service boundary so `pages.rs` stops owning repeated
  `with_repo_analysis(...)` orchestration directly.
- **Update**:
  - extended
    `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/repo/projected_service.rs`
    with typed entrypoints for projected pages, projected gap report,
    projected page, projected page-index tree, projected page-index node, and
    projected page-index trees
  - rewired
    `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/repo/pages.rs`
    into a thin route adapter over that shared projected-service seam
  - kept the external Studio payload contracts unchanged while moving panic-code
    ownership and repository-analysis orchestration below the route layer for
    the whole projected page cluster
- **Validation Executed**:
  - `direnv exec . cargo check -p xiuxian-wendao`
  - `direnv exec . cargo test -p xiuxian-wendao --lib repo_projected_pages_endpoint_returns_projection_payload`
  - `direnv exec . cargo test -p xiuxian-wendao --lib repo_projected_gap_report_endpoint_returns_projection_gap_payload`
  - `direnv exec . cargo test -p xiuxian-wendao --lib repo_projected_page_endpoint_returns_projection_payload`
  - `direnv exec . cargo test -p xiuxian-wendao --lib repo_projected_page_index_tree_endpoint_returns_tree_payload`
  - `direnv exec . cargo test -p xiuxian-wendao --lib repo_projected_page_index_node_endpoint_returns_node_payload`
  - `direnv exec . cargo test -p xiuxian-wendao --lib repo_projected_page_index_trees_endpoint_returns_tree_payload`

### [DONE] Feature: Wendao RFC Phase 2 projected family service consolidation

- **Objective**: pull the projected family and navigation endpoint cluster
  behind the same shared projected-service boundary so `family.rs` stops owning
  repeated `with_repo_analysis(...)` orchestration directly.
- **Update**:
  - extended
    `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/repo/projected_service.rs`
    with typed entrypoints for projected page family context, family search,
    family cluster, page navigation, and page navigation search
  - rewired
    `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/repo/family.rs`
    into a thin route adapter over that shared projected-service seam
  - kept the external Studio payload contracts unchanged while moving panic-code
    ownership and repository-analysis orchestration below the route layer for
    the whole projected family/navigation cluster
- **Validation Executed**:
  - `direnv exec . cargo check -p xiuxian-wendao`
  - `direnv exec . cargo test -p xiuxian-wendao --lib repo_projected_page_family_context_endpoint_returns_family_clusters`
  - `direnv exec . cargo test -p xiuxian-wendao --lib repo_projected_page_family_search_endpoint_returns_family_clusters`
  - `direnv exec . cargo test -p xiuxian-wendao --lib repo_projected_page_family_cluster_endpoint_returns_family_payload`
  - `direnv exec . cargo test -p xiuxian-wendao --lib repo_projected_page_navigation_endpoint_returns_navigation_bundle`
  - `direnv exec . cargo test -p xiuxian-wendao --lib repo_projected_page_navigation_search_endpoint_returns_navigation_hits`

### [DONE] Feature: Wendao RFC Phase 2 repo command service closure

- **Objective**: close the remaining repo-handler route-local command seams by
  moving repo index and refine-doc execution behind one shared repo-command
  service.
- **Update**:
  - added
    `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/repo/command_service.rs`
    with typed entrypoints for repo index enqueue, repo index status, and
    refine-doc execution
  - rewired
    `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/repo/index.rs`
    into a thin route adapter over `run_repo_index(...)` and
    `run_repo_index_status(...)`
  - rewired
    `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/repo/refine.rs`
    into a thin route adapter over `run_refine_entity_doc(...)`
  - added snapshot-backed Studio endpoint regressions for repo index status and
    refine-doc payloads
- **Validation Executed**:
  - `direnv exec . cargo check -p xiuxian-wendao`
  - `direnv exec . cargo test -p xiuxian-wendao --lib repo_index_status_endpoint_returns_status_payload`
  - `direnv exec . cargo test -p xiuxian-wendao --lib repo_refine_entity_doc_endpoint_returns_refined_payload`
  - `direnv exec . git diff --check`

### [DONE] Feature: Wendao RFC Phase 2 repo shared-state boundary audit

- **Objective**: split the overloaded `repo/shared.rs` helper surface into a
  feature folder so repository resolution and execution helpers stop sharing
  one flat module.
- **Update**:
  - replaced
    `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/repo/shared.rs`
    with:
    - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/repo/shared/mod.rs`
    - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/repo/shared/repository.rs`
    - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/repo/shared/execution.rs`
  - moved repository resolution and repo-index repository selection into
    `repository.rs`
  - moved `with_repo_analysis(...)`,
    `with_repo_cached_analysis_bundle(...)`, and `with_repository(...)` into
    `execution.rs`
  - added a focused unit test for managed-remote repository detection
- **Verification Status**:
  - `direnv exec . cargo check -p xiuxian-wendao`
  - `direnv exec . cargo test -p xiuxian-wendao repository_uses_managed_remote_source_only_when_url_is_present --lib`
  - `direnv exec . cargo test -p xiuxian-wendao --lib repo_overview_endpoint_returns_repo_summary_payload`
  - `direnv exec . cargo test -p xiuxian-wendao --lib repo_projected_page_search_endpoint_returns_projection_payload`
  - `direnv exec . cargo test -p xiuxian-wendao --lib repo_sync_endpoint_returns_repo_status_payload`
  - `direnv exec . git diff --check`

### [DONE] Feature: Wendao RFC Phase 2 docs service consolidation

- **Objective**: pull the docs search, retrieval, page, family, and navigation
  endpoint cluster behind one shared docs service boundary so docs route files
  stop owning repeated `with_repo_analysis(...)` orchestration directly.
- **Update**:
  - added
    `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/docs/service.rs`
    with typed entrypoints for docs search, retrieval, retrieval context,
    retrieval hit, page, family context, family search, family cluster,
    navigation, and navigation search
  - rewired
    `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/docs/search.rs`,
    `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/docs/retrieval.rs`,
    `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/docs/page.rs`,
    `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/docs/family.rs`,
    and
    `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/docs/navigation.rs`
    into thin route adapters over that shared service seam
  - moved panic-code ownership and retrieval-hit nested error translation below
    the route layer while keeping the external Studio payload contracts
    unchanged
- **Validation Executed**:
  - `direnv exec . cargo check -p xiuxian-wendao`
  - `direnv exec . cargo test -p xiuxian-wendao --lib docs_search_endpoint_returns_projection_payload`
  - `direnv exec . cargo test -p xiuxian-wendao --lib docs_retrieval_endpoint_returns_mixed_hits`
  - `direnv exec . cargo test -p xiuxian-wendao --lib docs_retrieval_context_endpoint_returns_node_context_payload`
  - `direnv exec . cargo test -p xiuxian-wendao --lib docs_retrieval_hit_endpoint_returns_hit_payload`
  - `direnv exec . cargo test -p xiuxian-wendao --lib docs_page_endpoint_returns_projection_payload`
  - `direnv exec . cargo test -p xiuxian-wendao --lib docs_family_context_endpoint_returns_family_context`
  - `direnv exec . cargo test -p xiuxian-wendao --lib docs_family_search_endpoint_returns_family_clusters`
  - `direnv exec . cargo test -p xiuxian-wendao --lib docs_family_cluster_endpoint_returns_requested_cluster`
  - `direnv exec . cargo test -p xiuxian-wendao --lib docs_navigation_endpoint_returns_navigation_bundle`
  - `direnv exec . cargo test -p xiuxian-wendao --lib docs_navigation_search_endpoint_returns_navigation_hits`
  - `direnv exec . git diff --check`

### [DONE] Feature: Wendao RFC Phase 2 docs planner and gap service closure

- **Objective**: pull the remaining docs planner and projected-gap endpoint
  cluster behind the shared docs service boundary so the docs route layer stops
  owning inline `with_repo_analysis(...)` orchestration completely.
- **Update**:
  - extended
    `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/docs/service.rs`
    with typed entrypoints for docs projected-gap report, planner item,
    planner search, planner queue, planner rank, and planner workset
  - rewired
    `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/docs/projected_gap.rs`
    and
    `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/docs/planner.rs`
    into thin route adapters over that shared service seam
  - completed the docs handler closure so search, retrieval, page, family,
    navigation, planner, and gap endpoints now all route through one shared
    docs-service boundary
- **Validation Executed**:
  - `direnv exec . cargo check -p xiuxian-wendao`
  - `direnv exec . cargo test -p xiuxian-wendao --lib docs_projected_gap_report_endpoint_returns_projection_gap_payload`
  - `direnv exec . cargo test -p xiuxian-wendao --lib docs_planner_item_endpoint_returns_gap_bundle`
  - `direnv exec . cargo test -p xiuxian-wendao --lib docs_planner_search_endpoint_returns_gap_hits`
  - `direnv exec . cargo test -p xiuxian-wendao --lib docs_planner_queue_endpoint_returns_grouped_gap_backlog`
  - `direnv exec . cargo test -p xiuxian-wendao --lib docs_planner_rank_endpoint_returns_priority_sorted_gaps`
  - `direnv exec . cargo test -p xiuxian-wendao --lib docs_planner_workset_endpoint_returns_opened_gap_batch`
  - `direnv exec . git diff --check`

### [DONE] Feature: Wendao RFC Phase 2 docs shared-state boundary audit

- **Objective**: split the remaining flat docs query-param surface into a
  feature folder so the docs handler cluster follows the same feature-boundary
  rule as the repo handler cluster.
- **Update**:
  - replaced
    `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/docs/types.rs`
    with:
    - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/docs/types/mod.rs`
    - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/docs/types/planner.rs`
    - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/docs/types/projected_gap.rs`
  - kept `types/mod.rs` interface-only and moved planner-vs-gap query params
    into separate responsibility-scoped modules
  - updated docs planner and projected-gap routes to import the shared types
    through crate-qualified docs type paths
- **Validation Executed**:
  - `direnv exec . cargo check -p xiuxian-wendao`
  - `direnv exec . cargo test -p xiuxian-wendao --lib docs_projected_gap_report_endpoint_returns_projection_gap_payload`
  - `direnv exec . cargo test -p xiuxian-wendao --lib docs_planner_workset_endpoint_returns_opened_gap_batch`
  - `direnv exec . git diff --check`

### [DONE] Feature: Wendao RFC Phase 2 docs handler closure audit

- **Objective**: replace the remaining flat docs route surface with feature
  folders so the docs handler topology matches the service and shared-state
  boundaries already landed in Phase 2.
- **Update**:
  - replaced the flat docs route files with:
    - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/docs/projection/`
    - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/docs/planner/`
  - kept
    `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/docs/mod.rs`
    interface-only over the feature folders plus `service` and `types`
  - updated
    `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/mod.rs`
    so router-level re-exports now flow through the new folder boundaries while
    keeping the external handler names unchanged
- **Validation Executed**:
  - `direnv exec . cargo check -p xiuxian-wendao`
  - `direnv exec . cargo test -p xiuxian-wendao --lib docs_search_endpoint_returns_projection_payload`
  - `direnv exec . cargo test -p xiuxian-wendao --lib docs_planner_workset_endpoint_returns_opened_gap_batch`
  - `direnv exec . git diff --check`

### [DONE] Feature: Wendao RFC Phase 2 docs service audit

- **Objective**: split the remaining single-file docs service surface into a
  feature folder so the service topology matches the route and shared-state
  boundaries.
- **Update**:
  - replaced
    `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/docs/service.rs`
    with:
    - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/docs/service/mod.rs`
    - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/docs/service/runtime.rs`
    - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/docs/service/projection.rs`
    - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/docs/service/planner.rs`
  - separated projection-oriented and planner-oriented service entrypoints into
    dedicated modules
  - moved shared `with_repo_analysis(...)` execution plumbing into
    `service/runtime.rs`
  - kept `service/mod.rs` interface-only over the stable internal docs service
    entrypoint surface
- **Validation Executed**:
  - `direnv exec . cargo check -p xiuxian-wendao`
  - `direnv exec . cargo test -p xiuxian-wendao --lib docs_search_endpoint_returns_projection_payload`
  - `direnv exec . cargo test -p xiuxian-wendao --lib docs_retrieval_hit_endpoint_returns_hit_payload`
  - `direnv exec . cargo test -p xiuxian-wendao --lib docs_planner_workset_endpoint_returns_opened_gap_batch`
  - `direnv exec . git diff --check`

### [DONE] Feature: Wendao RFC Phase 2 docs router export audit

- **Objective**: clean up the flat docs re-export surface at the top router
  handler boundary so the final docs export seam is grouped behind one
  explicit module.
- **Update**:
  - added
    `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/docs_exports.rs`
    as the dedicated docs handler export module
  - updated
    `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/mod.rs`
    to delegate docs re-export ownership to that module while keeping all
    external `docs_*` handler names unchanged
- **Validation Executed**:
  - `direnv exec . cargo check -p xiuxian-wendao`
  - `direnv exec . cargo test -p xiuxian-wendao --lib docs_search_endpoint_returns_projection_payload`
  - `direnv exec . cargo test -p xiuxian-wendao --lib docs_planner_workset_endpoint_returns_opened_gap_batch`
  - `direnv exec . git diff --check`

### [DONE] Feature: WendaoAnalyzer deployment artifact

- **Objective**: export the resolved Julia launch manifest as a serializable
  deployment artifact that also carries transport coordinates.
- **Update**:
  - added `LinkGraphJuliaDeploymentArtifact` and
    `LinkGraphJuliaRerankRuntimeConfig::deployment_artifact()`
  - updated runtime-config tests to verify artifact field mapping and TOML
    serialization
  - updated integration support so analyzer-owned services can be spawned from
    the deployment artifact, not only the raw launch manifest
  - added `.data/WendaoAnalyzer/config/julia_deployment_artifact.example.toml`
    as the concrete serialized artifact sample
- **Validation Executed**:
  - `direnv exec . cargo test -p xiuxian-wendao test_retrieval_runtime_resolves_julia_rerank_config --lib`
  - `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate test_planned_search_payload_applies_wendaoanalyzer_similarity_only_strategy`
  - `direnv exec . cargo check -p xiuxian-wendao --tests`
  - `direnv exec . julia --project=.data/WendaoAnalyzer -e 'using Pkg; Pkg.test()'`
  - `direnv exec . git diff --check`

### [DONE] Feature: WendaoAnalyzer deployment artifact export

- **Objective**: promote the Julia deployment artifact from a serializable
  struct to a crate-owned export surface that can render and write TOML
  directly.
- **Update**:
  - added `LinkGraphJuliaDeploymentArtifact::to_toml_string()` for pretty TOML
    rendering
  - added `LinkGraphJuliaDeploymentArtifact::write_toml_file(...)` with parent
    directory creation
  - extended runtime-config tests to verify serialization parity and file-write
    roundtrip
- **Validation Executed**:
  - `direnv exec . cargo test -p xiuxian-wendao test_retrieval_runtime_resolves_julia_rerank_config --lib`
  - `direnv exec . cargo test -p xiuxian-wendao deployment_artifact --lib`
  - `direnv exec . cargo check -p xiuxian-wendao --tests`
  - `direnv exec . git diff --check`

### [DONE] Feature: WendaoAnalyzer deployment artifact consumer

- **Objective**: provide a minimal runtime/debug consumer that resolves the
  current Julia deployment artifact and emits TOML without exposing internal
  retrieval-policy resolution steps.
- **Update**:
  - added `resolve_link_graph_julia_deployment_artifact()`
  - added `export_link_graph_julia_deployment_artifact_toml()`
  - extended runtime-config tests to verify the top-level consumer helpers
    match the same resolved artifact and TOML output
- **Validation Executed**:
  - `direnv exec . cargo test -p xiuxian-wendao test_retrieval_runtime_resolves_julia_rerank_config --lib`
  - `direnv exec . cargo test -p xiuxian-wendao deployment_artifact --lib`
  - `direnv exec . cargo check -p xiuxian-wendao --tests`
  - `direnv exec . git diff --check`

### [DONE] Feature: WendaoAnalyzer deployment visible export

- **Objective**: expose the current Julia deployment artifact through an
  existing visible inspection surface instead of leaving it as a library-only
  helper.
- **Update**:
  - added native tool `wendao.julia_deployment_artifact`
  - registered the same export over the existing `zhenfa_router` JSON-RPC
    method surface
  - added unit coverage for both the native render helper and RPC export path
- **Validation Executed**:
  - `direnv exec . cargo test -p xiuxian-wendao test_julia_deployment_artifact_writes_toml_file --lib`
  - `direnv exec . cargo test -p xiuxian-wendao render_julia_deployment_artifact_toml_uses_runtime_config --lib`
  - `direnv exec . cargo test -p xiuxian-wendao export_julia_deployment_artifact_from_rpc_params_returns_toml --lib`
  - `direnv exec . cargo check -p xiuxian-wendao --tests`
  - `direnv exec . git diff --check`

### [DONE] Feature: WendaoAnalyzer deployment structured export

- **Objective**: extend the visible deployment export surface with a structured
  JSON mode for machine-readable inspection consumers.
- **Update**:
  - added `output_format = "toml" | "json"` to
    `wendao.julia_deployment_artifact`
  - kept TOML as the default output mode
  - added native and RPC unit coverage for the JSON render path
- **Validation Executed**:
  - `direnv exec . cargo test -p xiuxian-wendao render_julia_deployment_artifact_toml_uses_runtime_config --lib`
  - `direnv exec . cargo test -p xiuxian-wendao render_julia_deployment_artifact_json_uses_runtime_config --lib`
  - `direnv exec . cargo test -p xiuxian-wendao export_julia_deployment_artifact_from_rpc_params_returns_toml --lib`
  - `direnv exec . cargo test -p xiuxian-wendao export_julia_deployment_artifact_from_rpc_params_returns_json --lib`
  - `direnv exec . cargo check -p xiuxian-wendao --tests`
  - `direnv exec . git diff --check`

### [DONE] Feature: WendaoAnalyzer Studio deployment endpoint

- **Objective**: expose the resolved Julia deployment artifact through a Studio
  gateway debug endpoint for browser-side inspection.
- **Update**:
  - added Studio handler `get_julia_deployment_artifact`
  - registered `GET /api/ui/julia-deployment-artifact`
  - added router test coverage for the resolved handler payload
- **Validation Executed**:
  - `direnv exec . cargo test -p xiuxian-wendao julia_deployment_artifact_handler_returns_resolved_artifact --lib`
  - `direnv exec . cargo check -p xiuxian-wendao --tests`
  - `direnv exec . git diff --check`

### [DONE] Feature: WendaoAnalyzer Studio deployment format alignment

- **Objective**: align the Studio deployment endpoint with the tool-side export
  surface by supporting both JSON and TOML output.
- **Update**:
  - added `format=json|toml` query support to
    `/api/ui/julia-deployment-artifact`
  - kept JSON as the default Studio response mode
  - added handler tests for both JSON and TOML responses
- **Validation Executed**:
  - `direnv exec . cargo test -p xiuxian-wendao julia_deployment_artifact_handler_returns_resolved_artifact --lib`
  - `direnv exec . cargo test -p xiuxian-wendao julia_deployment_artifact_handler_returns_toml_when_requested --lib`
  - `direnv exec . cargo check -p xiuxian-wendao --tests`
  - `direnv exec . git diff --check`

### [DONE] Feature: Python package redesign for Arrow/DataFusion/Flight alignment

- **Objective**: define the new `packages/python` ownership model now that
  Wendao runtime and data semantics are Rust-owned, Arrow/DataFusion-driven,
  and Flight-first.
- **Update**:
  - added `.cache/codex/execplans/python-arrow-flight-redesign.md`
  - added `packages/python/README.md`
  - added `packages/python/P0_surface_inventory.md`
  - documented the current mismatch between Python's MCP-era topology and the
    active Wendao blueprint
  - corrected the target architecture to be transport-first rather than
    bindings-first: Flight is the default Python path and Arrow IPC is the
    fallback
  - demoted direct bindings to embedded-mode or compatibility-only seams
    instead of the default integration model
  - classified `agent`, `core`, `foundation`, and `mcp-server` as frozen
    compatibility slices or retirement candidates
  - inventoried the live public surface and classified the first migration
    targets into transport client, temporary shim, and retire-after-compat
    buckets
  - staged the migration into freeze/inventory, transport-first expansion,
    compatibility extraction, retirement, and verification-tightening phases
- **Validation Executed**:
  - `direnv exec . git diff --check`

### [DONE] Feature: `xiuxian-wendao-py` transport-first public surface

- **Objective**: land the first real `xiuxian_*` Python package slice so the
  new Wendao Python entrypoint stops centering bindings-first and starts
  exposing transport-first client semantics.
- **Update**:
  - added `packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py/transport/`
    with `mode.py`, `config.py`, `client.py`, and interface-only
    `__init__.py`
  - updated `packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py/__init__.py`
    to use lazy public exports so transport imports do not eagerly pull the
    legacy backend chain
  - updated `packages/python/xiuxian-wendao-py/README.md` and
    `packages/python/xiuxian-wendao-py/pyproject.toml` to describe
    `xiuxian-wendao-py` as a transport-first package and direct bindings as
    embedded-mode compatibility only
  - updated `packages/python/README.md` and
    `packages/python/P0_surface_inventory.md` to state that `xiuxian_*` is the
    canonical namespace and `omni.*` is legacy inventory only
  - added `packages/python/xiuxian-wendao-py/tests/test_transport.py`
- **Validation Executed**:
  - `direnv exec . uv run python - <<'PY' ... PY`
  - `direnv exec . git diff --check`
  - `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 direnv exec . uv run pytest -o addopts='' packages/python/xiuxian-wendao-py/tests/test_transport.py`
    - blocked by repo environment issues: root pytest config includes
      unsupported `asyncio_default_fixture_loop_scope`, and `uv` warns that
      `packages/python/README.md` plus `packages/python/P0_surface_inventory.md`
      are being treated as workspace members

### [DONE] Feature: `xiuxian-wendao-py` canonical config compatibility slice

- **Objective**: provide the first narrow `xiuxian_*` compatibility exports for
  high-traffic config helpers without depending on the unstable old
  `omni.foundation` import chain.
- **Update**:
  - added `packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py/compat/`
    with lazy `__init__.py`, `config.py`, and `bridge.py`
  - replaced `compat/config.py` with a self-contained implementation for
    `PRJ_*`, `ConfigPaths`, `get_config_paths()`, `get_setting()`, and
    `get_settings()`
  - fixed the missing `ConfigPaths` contract in
    `packages/python/foundation/src/omni/foundation/config/paths.py`
  - fixed the missing `PROJECT` compatibility alias in
    `packages/python/foundation/src/omni/foundation/runtime/gitops.py`
  - extended
    `packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py/__init__.py`
    so the new canonical `xiuxian_*` package can expose both transport-first
    APIs and the first narrow config compatibility surface
- **Validation Executed**:
  - `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 direnv exec . uv run python - <<'PY' ... PRJ_DATA / get_config_paths / get_setting ... PY`
  - `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 direnv exec . uv run python - <<'PY' ... WendaoTransportClient ... PY`
  - `direnv exec . git diff --check`

### [DONE] Feature: Python package retirement matrix and low-risk script cutover

- **Objective**: move the redesign from compatibility-only framing into a real
  package deletion program, while continuing to reduce live script dependence
  on `omni.foundation.runtime.*`.
- **Update**:
  - added `packages/python/P1_retirement_matrix.md` to define:
    - canonical keep set
    - delete-first targets
    - shim-only targets
    - package-level retirement order
  - updated `packages/python/README.md` and
    `packages/python/P0_surface_inventory.md` to reference the explicit
    retirement matrix
  - updated `.cache/codex/execplans/python-arrow-flight-redesign.md` so the
    Python redesign plan now treats package deletion as a tracked deliverable
  - switched `scripts/validate_wendao_gate_reports.py` from
    `omni.foundation.runtime.gitops` to
    `xiuxian_wendao_py.compat.runtime`
  - switched `scripts/rust/cargo_check_with_timeout.py` from
    `omni.foundation.runtime.cargo_subprocess_env` to
    `xiuxian_wendao_py.compat.runtime`
- **Validation Executed**:
  - `direnv exec . uv run python scripts/validate_wendao_gate_reports.py --help`
  - `direnv exec . uv run python scripts/rust/cargo_check_with_timeout.py 1`
  - `direnv exec . git diff --check`

### [DONE] Feature: `test-kit` helper cutover to `xiuxian_*`

- **Objective**: keep shrinking Python deletion blockers by moving `test-kit`
  off the easiest remaining `omni.foundation.runtime/config` helpers without
  touching old `Settings`, `skills_monitor`, or logging semantics.
- **Update**:
  - switched `scripts/channel/test_xiuxian_daochang_valkey_suite.py` and
    `scripts/rust/xiuxian_daochang_embedding_role_perf_smoke.py` to
    `xiuxian_wendao_py.compat.runtime.prepare_cargo_subprocess_env`
  - switched the then-retained standalone Python test-helper modules and first
    test batch to `xiuxian_*` helper imports
- **Validation Executed**:
  - `direnv exec . uv run python - <<'PY' ... omni.test_kit.plugin / skill / benchmark / snapshot helpers ... PY`
  - `direnv exec . uv run python scripts/channel/test_xiuxian_daochang_valkey_suite.py --help`
  - `direnv exec . uv run python scripts/rust/xiuxian_daochang_embedding_role_perf_smoke.py --help`
  - targeted residual scans confirmed the old runtime/config imports were gone
    from scripts and the then-retained test-helper slice
  - `direnv exec . git diff --check`

### [DONE] Feature: clear `SKILLS_DIR` from `scripts + test-kit`

- **Objective**: remove the last low-risk `omni.foundation.config.skills`
  dependency path from active scripts and `test-kit` helpers before tackling
  harder `Settings` / logging / skills-monitor seams.
- **Update**:
  - switched `scripts/verify_skill_descriptions.py` to
    `xiuxian_wendao_py.compat.config.get_skills_dir`
  - switched `scripts/generate_llm_index.py` to
    `xiuxian_wendao_py.compat.config.get_skills_dir`
  - switched the remaining execution helper in the old standalone Python
    test-helper surface to `xiuxian_wendao_py.compat.config.get_skills_dir`
  - confirmed there are no remaining `SKILLS_DIR` / `get_skills_dir` imports
    from `omni.foundation.config.*` under `scripts` or the then-retained
    standalone Python test-helper slice
- **Validation Executed**:
  - `direnv exec . uv run python scripts/verify_skill_descriptions.py`
  - `direnv exec . uv run python scripts/generate_llm_index.py`
  - targeted residual scans confirmed the old `SKILLS_DIR` imports were gone
    from scripts and the then-retained test-helper slice
  - `direnv exec . git diff --check`

### [DONE] Feature: minimal compat reset hooks for `test-kit` git fixtures

- **Objective**: remove another direct `omni.foundation.runtime/config` test
  dependency by giving `xiuxian_*` the smallest possible cache-reset surface
  needed by `test-kit` git fixtures.
- **Update**:
  - added `clear_project_root_cache()` to
    `packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py/compat/runtime.py`
    as a compatibility no-op
  - added `PRJ_DIRS.clear_cache()` and `reset_config_paths_cache()` to
    `packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py/compat/config.py`
    as compatibility no-ops
  - switched the old standalone Python test-helper git fixture to the new
    `xiuxian_*` reset hooks
  - switched `scripts/verify_grand_integration.py` to
    `xiuxian_wendao_py.compat.runtime.get_project_root`
- **Validation Executed**:
  - `direnv exec . uv run python - <<'PY' ... clear_project_root_cache / PRJ_DIRS.clear_cache / reset_config_paths_cache ... PY`
  - targeted residual scans confirmed the old runtime/config imports were gone
    from scripts and the then-retained test-helper slice
  - `direnv exec . git diff --check`
  - note: importing `omni.test_kit.fixtures.git` and running
    `scripts/verify_grand_integration.py` still hit a deeper existing blocker in
    `omni.core.kernel`, which imports the now-missing legacy `SKILLS_DIR`
    symbol from `omni.foundation.config.skills`

### [DONE] Feature: restore minimal legacy `SKILLS_DIR` and `UnifiedRegistry`

- **Objective**: unblock deeper legacy import chains without reintroducing broad
  Python ownership, so deletion work can continue past broken `core/test-kit`
  module surfaces.
- **Update**:
  - replaced the mislocated contents of
    `packages/python/foundation/src/omni/foundation/config/skills.py` with a
    minimal legacy shim exposing `SKILLS_DIR` and `get_skills_dir`
  - added `UnifiedRegistry = SkillRegistry` compatibility alias in
    `packages/python/core/src/omni/core/kernel/components/registry.py`
  - updated `scripts/verify_grand_integration.py` to match current
    `Kernel.__slots__` and to skip the missing `AsyncPersistenceService`
    module instead of hard-failing on import
- **Validation Executed**:
  - `direnv exec . uv run python - <<'PY' ... SKILLS_DIR / get_skills_dir ... PY`
  - `direnv exec . uv run python - <<'PY' ... from omni.core.kernel.components import UnifiedRegistry ... PY`
  - `direnv exec . uv run python scripts/verify_grand_integration.py`
  - `direnv exec . git diff --check`
  - note: `omni.test_kit.fixtures.git` import now advances past the previous
    runtime/config blockers and fails later because
    `omni.test_kit.fixtures.__init__` references a missing `zk` module

### [DONE] Feature: de-MCP historical architecture/testing docs

- **Objective**: remove stale MCP framing from active historical docs so the
  repository no longer presents MCP as the intended end-state for routing,
  loop/runtime architecture, or prompt/memory evolution.
- **Update**:
  - rewrote `docs/01_core/omega/trinity-control.md` to describe the old stack
    as a legacy external-tool transition rather than an MCP target state
  - rewrote `docs/01_core/memory/injection-evolution.md` so tool-facade
    boundaries are expressed in protocol-neutral terms
  - rewrote `docs/testing/rust-agent-loop-memory-testing.md` to remove the
    legacy MCP note from the active testing narrative
  - rewrote `docs/99_llm/routing-guide.md` so tool discovery points to current
    CLI/runtime surfaces rather than legacy MCP resources
- **Validation Executed**:
  - `direnv exec . rg -n "\\bMCP\\b|omni mcp|Python MCP|MCP-Server|mcp\\.json|\\.mcp\\.json|OMNI_MCP_URL|xiuxian-mcp" docs/01_core/omega/trinity-control.md docs/testing/rust-agent-loop-memory-testing.md docs/01_core/memory/injection-evolution.md docs/99_llm/routing-guide.md -g '*.md'`
  - `direnv exec . git diff --check`

### [DONE] Feature: de-MCP research and README surfaces

- **Objective**: clear the next layer of active notes and READMEs that still
  presented MCP as a recommended entrypoint for knowledge, memory, or HTTP
  tool invocation.
- **Update**:
  - removed the remaining external-client MCP wording from
    `packages/rust/crates/xiuxian-qianhuan/README.md`
  - rewrote `docs/01_core/zhenfa/architecture/invocation-paradigm.md` to
    describe HTTP exposure in protocol-neutral terms
  - rewrote the MemRL and Pensieve/StateLM research workflows so they point to
    the current tool runtime surface instead of recommending MCP
  - rewrote `docs/01_core/memory/memrl-evolution.md` so diagrams and
    acceptance workflow talk about external tool runtime paths rather than MCP
- **Validation Executed**:
  - `direnv exec . rg -n "\\bMCP\\b|omni mcp|Python MCP|MCP-Server|mcp\\.json|\\.mcp\\.json|OMNI_MCP_URL|xiuxian-mcp" packages/rust/crates/xiuxian-qianhuan/README.md docs/01_core/zhenfa/architecture/invocation-paradigm.md docs/02_dev/workflows/research-memrl-vs-omni-memory.md docs/02_dev/workflows/research-pensieve-statelm-long-content.md docs/01_core/memory/memrl-evolution.md -g '*.md'`
  - `direnv exec . git diff --check`

### [DONE] Feature: remove final MCP wording from active guidance docs

- **Objective**: finish the current guidance-layer cleanup so handbooks,
  subsystem notes, and active research docs stop implying that MCP remains the
  live operational surface.
- **Update**:
  - rewrote the remaining MCP timeout, end-to-end, and integration-test wording
    in `docs/02_dev/HANDBOOK.md`
  - rewrote the vector subsystem and Omega router notes to use external-tool
    runtime terminology
  - updated `docs/testing/llm_comprehension_test.md` so the Trinity layering
    answer no longer references `MCP-Server`
  - removed one stale `knowledge-mcp-query-paper` reference and one remaining
    Wendao research note that still described QMD via an MCP interface
- **Validation Executed**:
  - `direnv exec . rg -n "\\bMCP\\b|omni mcp|Python MCP|MCP-Server|mcp\\.json|\\.mcp\\.json|OMNI_MCP_URL|xiuxian-mcp" docs/02_dev/HANDBOOK.md docs/01_core/wendao/VECTOR_SUBSYSTEM.md docs/01_core/omega/router.md docs/testing/llm_comprehension_test.md docs/02_dev/workflows/research-pensieve-statelm-long-content.md packages/rust/crates/xiuxian-wendao/RESEARCH_QMD_CONTRAST.md -g '*.md'`
  - `direnv exec . git diff --check`

### [IN-PROGRESS] Feature: de-MCP user-visible Rust code wording

- **Objective**: remove MCP-centric wording from the `xiuxian-daochang`
  user-visible Rust surface without breaking legacy compatibility flags, file
  names, or wire formats in the same pass.
- **Update**:
  - rewrote `packages/rust/crates/xiuxian-daochang/src/cli.rs` help text from
    "LLM + MCP tools" to "LLM + external tools" and clarified that
    `.mcp.json` is a legacy filename for the external tool config surface
  - rewrote crate/module docs in `src/lib.rs`, `src/main.rs`, `src/agent/mod.rs`,
    `src/agent/bootstrap/{builder,memory}.rs`, and `src/tools.rs` to describe
    external tool runtimes rather than MCP
  - renamed gateway health response type exports to
    `GatewayExternalToolHealthResponse` and changed the response field from
    `mcp` to `tools`, with `serde(alias = "mcp")` kept on deserialization for
    compatibility
  - rewrote the runtime agent logging message to say "external tool settings"
- **Validation Executed**:
  - `direnv exec . rg -n "GatewayMcpHealthResponse|GatewayExternalToolHealthResponse|pub mcp:|\\.mcp\\b" packages/rust/crates/xiuxian-daochang/src -g '*.rs'`
  - `direnv exec . cargo check -p xiuxian-daochang`
  - note: `cargo check` is currently blocked by a pre-existing unrelated
    compile failure in `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/capabilities/deployment.rs`
    (`E0603`: private module `crate::gateway::studio::types::config`)
  - `direnv exec . rg -n "strict MCP startup|continuing without MCP tools|MCP client found|MCP error result|before MCP response|Native \\+ Zhenfa \\+ MCP|mcp tool failed" packages/rust/crates/xiuxian-daochang/src/agent -g '*.rs'`
  - `direnv exec . git diff --check`
  - `direnv exec . rg -n "MCP config loader|Top-level mcp\\.json|Per-server entry in mcp\\.json|Load MCP server list|One MCP server entry|Agent configuration: inference API, model, API key, MCP server list|Agent config: inference API \\+ MCP|MCP servers to connect|MCP pool size|MCP handshake timeout|MCP connect retries|without MCP|MCP tool call timeout|MCP tools/list|MCP integration settings|MCP runtime settings section|conflict with MCP servers|MCP server list" packages/rust/crates/xiuxian-daochang/src/config packages/rust/crates/xiuxian-daochang/src/test_support -g '*.rs'`
  - `direnv exec . rg -n "When inference goes to our own MCP server|Build an agent instance from runtime settings and an MCP config file|MCP config loading|shares origin .*MCP server|run MCP and inference on one origin|optional MCP fallback URL|explicit MCP fallback URL|no MCP fallback|disables MCP fallback" packages/rust/crates/xiuxian-daochang/src/runtime_agent_factory packages/rust/crates/xiuxian-daochang/src/embedding packages/rust/crates/xiuxian-daochang/src/config/agent -g '*.rs'`
  - `direnv exec . rg -n "MCP integration namespace|MCP startup helpers exposed|Build MCP pool connect|Return Rust MCP pool|Return Rust MCP discover|agent config and MCP config loading" packages/rust/crates/xiuxian-daochang/src/mcp/mod.rs packages/rust/crates/xiuxian-daochang/src/agent/mcp/mod.rs packages/rust/crates/xiuxian-daochang/src/test_support/mcp_startup.rs packages/rust/crates/xiuxian-daochang/src/agent/mcp_pool_state.rs packages/rust/crates/xiuxian-daochang/src/config/mod.rs -g '*.rs'`

### [DONE] Feature: inventory internal `mcp*` rename boundary in xiuxian-daochang

- **Objective**: define the safe rename surface for the remaining internal
  `mcp*` names before attempting any high-churn module/type renames.
- **Update**:
  - added
    `packages/rust/crates/xiuxian-daochang/INTERNAL_MCP_RENAME_PLAN.md`
  - split the remaining surface into:
    - public Rust API symbols
    - medium-risk internal modules
    - high-churn config fields and local variables
    - legacy compatibility names that must remain temporarily
  - defined a four-wave plan:
    - neutral type aliases
    - internal method/local cleanup
    - module-path migration behind compatibility bridges
    - test/fixture rename sweep
- **Validation Executed**:
  - `direnv exec . rg -n "\\bMcp[A-Z]\\w*|\\bmcp_[a-zA-Z0-9_]+|\\bmod mcp\\b|\\bpub use mcp\\b|crate::mcp::|agent::mcp|mcp__" packages/rust/crates/xiuxian-daochang/src packages/rust/crates/xiuxian-daochang/tests -g '*.rs'`
  - `direnv exec . git diff --check`

### [IN-PROGRESS] Feature: Wendao P1 plugin/runtime compatibility surface narrowing

- **Objective**: continue reducing Julia-specific vocabulary on host-side
  outward surfaces while keeping the Julia deployment-artifact tool and route
  names stable.
- **Update**:
  - changed `zhenfa_router/native/deployment.rs` to use
    `WendaoCompatDeploymentArtifactOutputFormat`,
    `WendaoCompatDeploymentArtifactArgs`,
    `render_compat_deployment_artifact_*`, and
    `export_compat_deployment_artifact` as the primary host-side API
  - kept `WendaoJuliaDeploymentArtifact*` names and
    `export_julia_deployment_artifact*` helpers as thin compatibility aliases
    and shims inside the deployment ownership seam
  - rewired `zhenfa_router/native/mod.rs`, `zhenfa_router/mod.rs`,
    `zhenfa_router/http.rs`, `zhenfa_router/rpc.rs`, and the Studio capability
    handler query types to use the compatibility-oriented surface
  - added focused compatibility tests so legacy Julia aliases and JSON-RPC shim
    stay exercised and do not regress into dead code
  - changed the Studio capability handler surface to use
    `CompatDeploymentArtifactQuery` and `get_compat_deployment_artifact` as
    the primary host-side API, while keeping `JuliaDeploymentArtifactQuery` and
    `get_julia_deployment_artifact` as `pub(crate)` compatibility shims
  - changed the Studio capability handler error codes/messages and the route
    inventory primary constant name to compatibility-oriented wording, while
    keeping the Julia-named route constant aliases for compatibility
  - changed `link_graph/runtime_config.rs`, `link_graph/mod.rs`, and crate-root
    exports to use compat deployment-artifact helpers as the primary host-side
    API, while keeping Julia-named runtime-config helpers as public
    compatibility shims
  - added compat-first DTO aliases for the Julia deployment-artifact,
    launch-manifest, and rerank-runtime records, and rewired the high-level
    `runtime_config` / `link_graph` / crate-root exports to expose those compat
    names without removing the Julia aliases yet
  - switched the remaining Wendao integration support fixture and planned
    search test slice to use `LinkGraphCompat*` DTO aliases as the primary
    vocabulary, so Julia-backed retrieval coverage now exercises the compat
    host surface instead of reintroducing Julia-named records into new tests
  - narrowed legacy Julia route/helper aliases behind focused compatibility
    seams and `#[cfg(test)]` where they are only needed for regression
    coverage, closing the touched-scope warning loop for the Julia feature
    build
  - removed the Julia deployment handler/query shim from the higher-level
    `gateway::studio::router::handlers::capabilities` re-export surface and
    moved its focused coverage into the `deployment.rs` ownership test module,
    so crate-internal callers now use the compat handler path directly while
    the Julia alias stays local to the seam that owns it
  - removed the Julia deployment-artifact helper functions from the
    `link_graph` middle-layer re-export surface and rewired crate-root to
    expose those legacy compatibility helpers directly from
    `link_graph::runtime_config`, keeping the public compatibility surface
    stable while shrinking the intermediate host namespace
  - switched the remaining focused host-side rerank binding and transport
    override tests to build their fixtures with
    `LinkGraphCompatRerankRuntimeConfig`, so internal regression slices now
    default to compat DTO vocabulary unless they are explicitly testing a
    legacy Julia alias
  - reorganized the public re-export blocks in `link_graph::runtime_config`
    and crate root so the compat DTO/export surface comes first and the
    Julia-named compatibility aliases are grouped behind an explicit legacy
    compatibility block, without changing the actual public API
  - added `WendaoCompatDeploymentArtifactTool` as the primary higher-level
    Zhenfa tool type export and moved the Julia-named tool type behind the
    deployment ownership seam, so `zhenfa_router` now follows the same
    compat-first naming discipline as the rest of the host surface
  - changed the compat Zhenfa deployment-artifact descriptions, execution
    errors, and success message text from Julia-specific wording to
    compatibility wording, while keeping the stable tool name and the focused
    Julia shim coverage unchanged
  - removed the Julia-named route-contract alias from the OpenAPI shared
    inventory layer, so the route inventory now keeps only the compat
    deployment-artifact contract while the legacy Julia route remains preserved
    as an API-path constant alias
  - grouped the remaining Julia-named UI API-path constants under an explicit
    legacy compatibility section in `gateway/openapi/paths/ui.rs`, so the
    compat deployment-artifact path stays visually primary even where the
    stable Julia route aliases still exist
  - changed the compatibility deployment-artifact JSON-RPC failure wording in
    `zhenfa_router/rpc.rs` from Julia-specific language to compatibility
    wording, keeping the stable RPC method name and legacy shim behavior
    unchanged
  - rewrote the remaining Julia-facing documentation comments in
    `gateway/studio/types/config.rs` and
    `zhenfa_router/native/deployment.rs` so they describe those surfaces as
    legacy Julia-compatible compatibility shims rather than primary Julia
    contracts
  - added a formal compatibility ledger to
    `409_core_runtime_plugin_surface_inventory.md` that separates the
    remaining intentional Julia-named outward surfaces from the Julia names
    that have already been pushed behind narrow compatibility seams
  - added `link_graph/runtime_config/compatibility/` and moved the Julia-named
    runtime DTO/helper ownership behind that explicit feature folder, so the
    main `runtime_config.rs` surface now re-exports those legacy names from a
    dedicated compatibility seam instead of owning them inline
  - added `gateway/studio/types/compatibility/` and moved the `UiJulia*`
    Studio DTO definitions plus their conversion tests behind that explicit
    feature folder, so `types/config.rs` no longer owns those legacy
    compatibility records inline
- **Validation Executed**:
  - `direnv exec . cargo test -p xiuxian-wendao zhenfa_router::native::deployment::tests:: --lib`
  - `direnv exec . cargo test -p xiuxian-wendao zhenfa_router::rpc::tests:: --lib`
  - `direnv exec . cargo test -p xiuxian-wendao gateway::studio::types::config::tests:: --lib`
  - `direnv exec . cargo test -p xiuxian-wendao gateway::studio::router::tests::config:: --lib`
  - `direnv exec . cargo test -p xiuxian-wendao route_inventory_keeps_core_endpoints --lib`
  - `direnv exec . cargo test -p xiuxian-wendao route_inventory_paths_are_unique --lib`
  - `direnv exec . cargo test -p xiuxian-wendao link_graph::runtime_config::tests:: --lib`
  - `direnv exec . cargo test -p xiuxian-wendao link_graph::plugin_runtime:: --lib`
  - `direnv exec . cargo test -p xiuxian-wendao link_graph::runtime_config::tests::legacy_julia_runtime_type_aliases_match_compat_aliases --lib`
  - `direnv exec . cargo test -p xiuxian-wendao test_planned_search_payload_applies_wendaoanalyzer_similarity_only_strategy --features julia -- --exact`
  - `direnv exec . cargo test -p xiuxian-wendao gateway::studio::router::handlers::capabilities::deployment::tests::legacy_julia_deployment_artifact_handler_alias_still_works --lib`
  - `direnv exec . cargo test -p xiuxian-wendao gateway::studio::router::tests::config:: --lib`
  - `direnv exec . cargo test -p xiuxian-wendao link_graph::runtime_config::tests:: --lib`
  - `direnv exec . cargo test -p xiuxian-wendao --lib --no-run`
  - `direnv exec . cargo test -p xiuxian-wendao link_graph::plugin_runtime::tests::compat_julia::julia_rerank_runtime_converts_into_generic_binding --lib`
  - `direnv exec . cargo test -p xiuxian-wendao link_graph::index::search::plan::payload::quantum::tests::build_plugin_rerank_transport_client_honors_runtime_overrides --lib`
  - `direnv exec . cargo test -p xiuxian-wendao link_graph::runtime_config::tests::legacy_julia_runtime_type_aliases_match_compat_aliases --lib`
  - `direnv exec . cargo test -p xiuxian-wendao --lib --no-run`
  - `direnv exec . cargo test -p xiuxian-wendao --lib --no-run`
  - `direnv exec . cargo test -p xiuxian-wendao zhenfa_router::native::deployment::tests:: --lib`
  - `direnv exec . cargo test -p xiuxian-wendao zhenfa_router::rpc::tests:: --lib`
  - `direnv exec . cargo test -p xiuxian-wendao route_inventory_keeps_core_endpoints --lib`
  - `direnv exec . cargo test -p xiuxian-wendao route_inventory_paths_are_unique --lib`
  - `direnv exec . cargo test -p xiuxian-wendao legacy_julia_route_alias_matches_compat_route --lib`
  - `direnv exec . cargo test -p xiuxian-wendao zhenfa_router::rpc::tests:: --lib`
  - `direnv exec . cargo test -p xiuxian-wendao gateway::studio::types::config::tests:: --lib`
  - `direnv exec . cargo test -p xiuxian-wendao zhenfa_router::native::deployment::tests:: --lib`
  - `direnv exec . cargo test -p xiuxian-wendao link_graph::runtime_config::tests::legacy_julia_runtime_type_aliases_match_compat_aliases --lib`
  - `direnv exec . cargo test -p xiuxian-wendao --lib --no-run`
  - `direnv exec . cargo test -p xiuxian-wendao gateway::studio::types::compatibility::julia::tests:: --lib`
  - `direnv exec . cargo test -p xiuxian-wendao gateway::studio::router::handlers::capabilities::deployment::tests::legacy_julia_deployment_artifact_handler_alias_still_works --lib`
  - `direnv exec . git diff --check`

### [DONE] Feature: hard-delete xiuxian-wendao-py bindings surface

- **Objective**: stop treating `xiuxian-wendao-py` as an embedded/bindings
  package and make it transport-only in line with the Arrow Flight / Arrow IPC
  architecture.
- **Update**:
  - removed `xiuxian-core-rs` from root dev dependencies and from
    `packages/python/xiuxian-wendao-py/pyproject.toml`
  - deleted `packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py/engine.py`
    and the whole `backend/` package
  - deleted `packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py/compat/bridge.py`
    and removed bindings/vector/PyO3 exports from package `__init__` and
    `compat/__init__`
  - deleted `test_engine.py` and `test_backend.py`
  - removed `prepare_cargo_subprocess_env` from the `xiuxian-wendao-py`
    runtime compat surface
  - updated `xiuxian-wendao-py/README.md` and `models.py` to describe the
    package as Flight/Arrow transport-only rather than embedded-mode capable
- **Validation Executed**:
  - `direnv exec . uv run pytest packages/python/xiuxian-wendao-py/tests/test_transport.py packages/python/xiuxian-wendao-py/tests/test_compat_imports.py -q`
  - `direnv exec . rg -n "xiuxian_core_rs|WendaoEngine|WendaoBackend|RustWendaoUnavailableError|create_engine|RUST_AVAILABLE|RustVectorStore|get_vector_store|prepare_cargo_subprocess_env|backend core|embedded-mode" packages/python/xiuxian-wendao-py -g '*.py' -g '*.md' -g '*.toml'`

### [DONE] Feature: hard-delete Python sync/reindex command surfaces

- **Objective**: stop treating Python as a host runtime that can rebuild or
  synchronize symbols, skills, router state, knowledge, or memory locally now
  that those responsibilities live in Rust/Wendao and communicate over Arrow
  Flight.
- **Update**:
  - deleted `packages/python/agent/src/omni/agent/cli/commands/sync.py`
  - deleted `packages/python/agent/src/omni/agent/cli/commands/reindex.py`
  - removed `sync` / `reindex` from the lazy CLI command registry and from
    `omni.agent.cli.commands` package exports
  - deleted `packages/python/agent/src/omni/agent/services/sync.py`
  - deleted `packages/python/agent/src/omni/agent/services/reindex.py`
  - removed public service re-exports from `omni.agent.services`
  - changed skill inventory commands to fail fast with Arrow Flight / Wendao
    guidance instead of attempting local Python indexing
  - changed CLI command-group registration to skip removed/broken modules
    cleanly during package import
  - updated `test_cli.py` to stop expecting removed `mcp`, `sync`, and
    `reindex` command surfaces
- **Validation Executed**:
  - `direnv exec . python -m compileall packages/python/agent/src/omni/agent/cli packages/python/core/src/omni/core/skills packages/python/agent/src/omni/agent/services`
  - `direnv exec . uv run pytest packages/python/agent/tests/unit/cli/test_cli.py -q`
  - `direnv exec . git diff --check`

### [DONE] Feature: hard-delete Python route command surface

- **Objective**: remove the Python-owned local routing/hybrid search CLI entry
  so route/search host behavior lives only in Rust/Wendao over Arrow Flight.
- **Update**:
  - deleted `packages/python/agent/src/omni/agent/cli/commands/route.py`
  - deleted `packages/python/agent/tests/unit/cli/test_route_command.py`
  - removed `route` from the lazy CLI command registry
  - trimmed `omni.agent.cli.commands` eager exports so unrelated imports no
    longer drag local route dependencies into package initialization
- **Validation Executed**:
  - `direnv exec . uv run pytest packages/python/agent/tests/unit/cli/test_cli.py -q`
  - `direnv exec . git diff --check`

### [DONE] Feature: hard-delete Python local generation-RAG and stale CLI surface

- **Objective**: remove additional Python host-runtime behavior that still
  assumed local index/router/bindings ownership, especially skill-generation
  RAG retrieval and stale CLI metadata.
- **Update**:
  - deleted `packages/python/agent/src/omni/agent/cli/commands/skill/_generate_modular/rag.py`
  - removed the local RAG retrieval step from `skill generate`
  - deleted a benchmark from the old standalone Python test-helper surface
    because it still exercised deleted local indexing and routing paths
  - removed deleted command names and options from `completions.py`
  - removed bindings/Lance dependency output from the CLI `version` command
  - rewrote CLI help/tips so they point to Rust/Wendao over Arrow Flight
    instead of local Python rebuilds
  - updated `test_load_requirements.py` to match the current top-level CLI
    command registry
- **Validation Executed**:
  - `direnv exec . python -m compileall packages/python/agent/src/omni/agent/cli`
  - `direnv exec . uv run pytest packages/python/agent/tests/unit/cli/test_cli.py packages/python/agent/tests/unit/cli/test_load_requirements.py -q`
  - targeted residual scans confirmed the deleted local indexing and routing
    terms were absent from the surviving CLI surface
  - `direnv exec . git diff --check`

### [DONE] Feature: hard-delete Python MCP embedding/server fallback surfaces

- **Objective**: remove the remaining Python-side MCP embedding and server
  execution paths so Python no longer keeps a hidden non-Flight transport or
  server fallback.
- **Update**:
  - deleted `packages/python/agent/src/omni/agent/embedding_override.py`
  - deleted `packages/python/agent/src/omni/agent/cli/mcp_embed.py`
  - deleted `packages/python/agent/src/omni/agent/server.py`
  - removed embedding-override auto-install logic from
    `packages/python/agent/src/omni/agent/cli/app.py`
  - removed MCP HTTP-client cleanup hooks and MCP timeout wording from
    `runner.py` / `runner_json.py`
  - changed `foundation/services/vector/search.py` to HTTP-only embedding and
    removed the Python-side MCP probe/backoff/fallback path
  - deleted `packages/python/foundation/tests/unit/services/test_vector_search_embedding_path.py`
  - updated CLI fast-path tests and skills-monitor tests to match the new
    HTTP-only embedding path
- **Validation Executed**:
  - `direnv exec . python -m compileall packages/python/agent/src/omni/agent/cli packages/python/foundation/src/omni/foundation/services/vector`
  - `direnv exec . uv run pytest packages/python/agent/tests/unit/cli/test_app_fast_path.py packages/python/agent/tests/unit/cli/test_cli.py packages/python/agent/tests/unit/cli/test_load_requirements.py -q`
  - `direnv exec . rg -n "mcp_embed|AgentMCPHandler|create_agent_handler|vector\\.embed\\.mcp|OMNI_EMBED_OVERRIDE_ENABLED|install_skill_embedding_override" packages/python/agent/src packages/python/agent/tests packages/python/foundation/src packages/python/foundation/tests -g '*.py'`
  - `direnv exec . git diff --check`
- **Validation Blocked**:
  - `direnv exec . uv run pytest packages/python/foundation/tests/unit/runtime/test_skills_monitor.py -q`
  - blocked by an existing import error in
    `omni.foundation.runtime.skills_monitor.context`
    (`suppress_skill_command_phase_events` missing), which is outside this
    deletion slice

### [DONE] Feature: hard-delete Python MCP timeout and mode naming

- **Objective**: remove the last active Python-side MCP configuration and mode
  naming from timeout handling and TUI environment detection.
- **Update**:
  - changed `packages/python/foundation/src/omni/foundation/api/tool_context.py`
    to read `tool.timeout` / `tool.idle_timeout`
  - deleted `packages/python/foundation/tests/unit/api/test_config_paths.py`
    because it was asserting removed `.mcp.json` / YAML config behavior and
    methods that no longer exist on the current `ConfigPaths`
  - extended
    `packages/python/foundation/tests/unit/api/test_tool_context.py` with
    timeout config coverage for `tool.*`
  - changed `packages/python/agent/src/omni/agent/cli/tui_bridge.py` and
    `packages/python/agent/tests/unit/cli/test_tui_bridge.py` from `OMNI_MODE=mcp`
    to `OMNI_MODE=service`
- **Validation Executed**:
  - `direnv exec . uv run pytest packages/python/foundation/tests/unit/api/test_tool_context.py packages/python/agent/tests/unit/cli/test_tui_bridge.py packages/python/agent/tests/unit/cli/test_app_fast_path.py packages/python/agent/tests/unit/cli/test_cli.py packages/python/agent/tests/unit/cli/test_load_requirements.py -q`
  - `direnv exec . rg -n "mcp\\.timeout|mcp\\.idle_timeout|mcp\\.config_file|\\.mcp\\.json|OMNI_MODE.*mcp|\\bmcp\\b" packages/python/foundation/src/omni/foundation/api/tool_context.py packages/python/foundation/tests/unit/api/test_tool_context.py packages/python/agent/src/omni/agent/cli/tui_bridge.py packages/python/agent/tests/unit/cli/test_tui_bridge.py -g '*.py'`
  - `direnv exec . git diff --check`

### [DONE] Feature: hard-delete Python meta-cognition prompt chain and shrink main-loop context

- **Objective**: remove the dead Python-side routing/meta-cognition prompt
  chain so the default main-loop context no longer pretends Python owns tool
  routing guidance or local prompt orchestration.
- **Update**:
  - deleted `packages/python/agent/src/omni/agent/core/common/prompts.py`
  - deleted `packages/python/agent/src/omni/agent/core/context/providers.py`
  - deleted `packages/python/agent/tests/unit/core/test_meta_cognition.py`
  - changed `packages/python/core/src/omni/core/context/orchestrator.py` so
    `create_omni_loop_context()` is persona-only
  - trimmed `packages/python/core/tests/units/test_context.py` to stop testing
    obsolete Python skill/tool provider paths
- **Validation Executed**:
  - `direnv exec . rg -n "omni\\.agent\\.core\\.common\\.prompts|RoutingGuidanceProvider|intent_protocol|omni\\.agent\\.core\\.context\\.providers" packages/python -g '*.py'`
  - `direnv exec . uv run pytest packages/python/core/tests/units/test_context.py -q`
  - `direnv exec . python -m compileall packages/python/core/src/omni/core/context packages/python/agent/src/omni/agent/core/context`
  - `direnv exec . git diff --check`

### [DONE] Feature: rebuild Python retrieval response helpers and remove stray local ZK search

- **Objective**: clean a physically wrong retrieval module so Python keeps only
  thin recall payload shaping instead of a half-dead local search
  implementation.
- **Update**:
  - rebuilt `packages/python/foundation/src/omni/rag/retrieval/response.py`
    as pure response-payload helpers
  - removed the stray `run_zk_search` / local `ZkReasoningSearcher` logic from
    that file
  - kept `single_call.py` on the normalized payload helper surface
  - retained focused retrieval payload tests only
- **Validation Executed**:
  - `direnv exec . uv run pytest packages/python/foundation/tests/unit/rag/test_retrieval_response.py -q`
  - `direnv exec . python -m compileall packages/python/foundation/src/omni/rag/retrieval`
  - `direnv exec . git diff --check`

### [DONE] Feature: collapse Python context providers to persona-only

- **Objective**: remove the last default Python-side context providers that
  still implied local skill/tool/memory injection for the main loop.
- **Update**:
  - rebuilt `packages/python/core/src/omni/core/context/providers.py` so it
    only exports `SystemPersonaProvider`
  - removed `ActiveSkillProvider`, `AvailableToolsProvider`, and
    `EpisodicMemoryProvider` from
    `packages/python/core/src/omni/core/context/__init__.py`
  - trimmed `packages/python/core/src/omni/core/context/orchestrator.py` to
    depend only on the persona provider
  - deleted `packages/python/agent/tests/unit/test_session_retrospective.py`
    because it still exercised a Python episodic-memory path that no longer
    belongs in the architecture
- **Validation Executed**:
  - `direnv exec . uv run pytest packages/python/core/tests/units/test_context.py -q`
  - `direnv exec . rg -n "ActiveSkillProvider|AvailableToolsProvider|EpisodicMemoryProvider" packages/python -g '*.py'`
  - `direnv exec . python -m compileall packages/python/core/src/omni/core/context`
  - `direnv exec . git diff --check`
