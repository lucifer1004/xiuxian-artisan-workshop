# Daily GTD - 2026-04-01

## WendaoArrow Gateway Flight Smoke and Benchmark

- Landed one narrow WendaoArrow production slice:
  - added explicit gateway Flight client helpers in
    `.data/WendaoArrow/src/gateway_flight.jl`
  - kept the local scoring-service default contract on `v1`
  - added one standalone live benchmark script in
    `.data/WendaoArrow/scripts/benchmark_gateway_flight.jl`
- Concrete live-runtime facts now verified against the running gateway on
  `127.0.0.1:9517`:
  - `/search/repos/main` with
    `x-wendao-schema-version = v2`,
    `x-wendao-repo-search-query = flight`,
    `x-wendao-repo-search-limit = 5`
    returned `4` rows
  - `/search/knowledge` with
    `x-wendao-schema-version = v2`,
    `x-wendao-search-query = flight`,
    `x-wendao-search-limit = 5`
    returned `5` rows
- Package-local coverage added:
  - descriptor/header contract tests
  - transport validation tests
  - env-gated live smoke in
    `.data/WendaoArrow/test/runtests/gateway_flight.jl`
- Local benchmark artifact:
  - `.cache/agent/tmp/wendaoarrow-gateway-flight-bench.tsv`
  - `repo_search median_ms = 10.945`
  - `knowledge_search median_ms = 24.242`
- Restarted-gateway rerun:
  - `.cache/agent/tmp/wendaoarrow-gateway-flight-bench-restart.tsv`
  - `.cache/agent/tmp/wendaoarrow-gateway-flight-phases-restart.txt`
  - `.cache/agent/execplans/wendaoarrow_gateway_search_perf_report.md`
  - `repo_search median_ms = 6.613`
  - `knowledge_search median_ms = 9.821`
  - repo-search phase median:
    - `getflightinfo = 4.897 ms`
    - `doget = 0.747 ms`
    - `decode = 0.166 ms`
  - knowledge-search phase median:
    - `getflightinfo = 8.552 ms`
    - `doget = 0.852 ms`
    - `decode = 0.145 ms`
  - conclusion:
    - there is no large WendaoArrow-local optimization headroom left for this
      path because decode is already negligible relative to gateway roundtrip
- Arrow source lock:
  - WendaoArrow now locks `Arrow` / `ArrowTypes` to
    `https://github.com/JuliaCN/arrow-julia.git`
  - revision:
    `d133dcbe7445fc5a2b3d72c5ebd258cb47ce10a4`
  - WendaoArrow commit:
    `7ba5b66 Add gateway Flight client and lock Arrow source`
- Validation:
  - `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/runtests.jl`
  - `env WENDAO_GATEWAY_FLIGHT_SMOKE=1 WENDAO_GATEWAY_FLIGHT_ADDR=127.0.0.1:9517 direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/runtests.jl`
  - `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/flight_grpcserver.jl`
  - `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/scripts/benchmark_gateway_flight.jl --host 127.0.0.1 --port 9517 --query flight --limit 5 --samples 10`
- Active ExecPlan:
  - `.cache/agent/execplans/wendaoarrow_gateway_flight_smoke_bench.md`

## WENDAO Frontend Flight Client v2 Refresh and Perf Harness

- Refreshed the frontend browser Flight stack to the latest working set:
  - `.data/wendao-frontend/package.json`
  - `.data/wendao-frontend/package-lock.json`
  - `.data/wendao-frontend/scripts/generate-flight-client.mjs`
  - `.data/wendao-frontend/src/api/flight/generated/Flight_pb.ts`
  - removed `.data/wendao-frontend/src/api/flight/generated/Flight_connect.ts`
  - `.data/wendao-frontend/src/api/flightSearchTransport.ts`
  - `.data/wendao-frontend/src/api/flightSearchTransport.test.ts`
  - `.data/wendao-frontend/src/api/liveFlightSearchPerf.test.ts`
- Concrete dependency state:
  - `apache-arrow 21.1.0` was already current
  - `@connectrpc/connect 2.1.1`
  - `@connectrpc/connect-web 2.1.1`
  - `@bufbuild/protobuf 2.11.0`
  - `@bufbuild/protoc-gen-es 2.11.0`
  - retired `@connectrpc/protoc-gen-connect-es`
- Concrete implementation result:
  - Flight proto generation now uses protobuf v2 `protoc-gen-es` only
  - the frontend now consumes the generated `Flight_pb.ts` service descriptor
    directly with Connect 2.x
  - transport/tests now use protobuf v2 `create(...Schema)` message creation
  - `npm run test:live-flight-perf` now benchmarks same-port `/search/knowledge`
    against the checked-in `wendao.toml`
  - the perf harness now writes JSON/CSV artifacts to
    `$PRJ_CACHE_HOME/agent/tmp/wendao_frontend_live_flight_search_perf.{json,csv}`
    and now treats exported `PRJ_CACHE_HOME` as the absolute cache root
  - the perf harness now also records per-phase timing for `GetFlightInfo`,
    `DoGet`, Arrow IPC reassembly, hit decoding, and metadata decoding
- Current local config size:
  - `179` `link_graph.projects.*`
- Current default live perf profile:
  - queries: `diffeq`, `solver`, `optimization`
  - overall `avgMs 12.58`, `p50Ms 13.05`, `p95Ms 14.23`, `maxMs 14.23`
- Fresh same-port hotspot follow-up on a newly built `127.0.0.1:9519` gateway:
  - landed one runtime payload-cache slice in
    `packages/rust/crates/xiuxian-wendao-runtime/src/transport/server.rs` so
    `DoGet` reuses the payload already materialized by `GetFlightInfo` instead
    of executing the same search twice
  - restored the `xiuxian-vector` crate-root Arrow transport exports in
    `packages/rust/crates/xiuxian-vector/src/lib.rs` so
    `xiuxian-wendao-julia` and fresh gateway builds no longer fail on
    `attach_record_batch_metadata` import drift
  - fresh live same-port perf runs against `http://127.0.0.1:9519` now show
    `DoGet` at roughly `1.96-2.12ms` average while `GetFlightInfo` dominates at
    roughly `117.58-126.14ms` average
  - conclusion: frontend decode/reassembly is not the current hotspot; the
    next optimization target is to avoid full search materialization during
    `GetFlightInfo`
- Fresh same-port aggregate-provider follow-up on a newly built
  `127.0.0.1:9519` gateway:
  - landed one Studio aggregate-provider response-reuse slice in
    `packages/rust/crates/xiuxian-wendao/src/gateway/studio/search/handlers/knowledge/search.rs`,
    `packages/rust/crates/xiuxian-wendao/src/gateway/studio/search/handlers/knowledge/intent/flight.rs`,
    and `packages/rust/crates/xiuxian-wendao/src/gateway/studio/search/handlers/flight.rs`
    so the Studio search provider reuses the already-built
    `SearchFlightRouteResponse` instead of rebuilding batch plus app metadata
    after the search response exists
  - validation for the touched scope is now green:
    `direnv exec . cargo check -p xiuxian-vector --lib --message-format short`,
    `direnv exec . cargo test -p xiuxian-wendao studio_search_flight_provider_dispatches_knowledge_route --lib --features julia -- --test-threads=1`,
    and `direnv exec . cargo check -p xiuxian-wendao --lib --features julia --message-format short`
  - fresh live same-port perf runs against `http://127.0.0.1:9519` now show
    `DoGet` at roughly `0.75-1.10ms` average while `GetFlightInfo` dominates at
    roughly `41.03-46.15ms` average
  - conclusion: the latest optimization meaningfully reduced `GetFlightInfo`,
    but the main local `127.0.0.1:9517` gateway must be restarted before the
    same numbers can be observed there
- Default gateway verification after restarting `127.0.0.1:9517`:
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm run build'` passed
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && RUN_LIVE_GATEWAY_TEST=1 npm run test:live-gateway'` passed with `3/3` live tests
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && RUN_LIVE_GATEWAY_TEST=1 npm run test:live-flight-perf'` passed on the default same-origin gateway
  - current default-gateway perf profile over the checked-in 179-repository
    config is:
    - overall average `5.21ms`, P95 `6.07ms`
    - `GetFlightInfo` average `3.73ms`, P95 `4.46ms`
    - `DoGet` average `1.00ms`, P95 `1.54ms`
  - generated one frontend-facing report at
    `.data/wendao-frontend/docs/05_research/308_live_flight_search_perf_report.md`
- Validation:
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm ls @bufbuild/protobuf @connectrpc/connect @connectrpc/connect-web @bufbuild/protoc-gen-es apache-arrow'`
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm run build'`
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm test -- src/api/flightSearchTransport.test.ts src/api/client.test.ts src/config/__tests__/loader.test.ts'`
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm run test:live-gateway'`
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm run test:live-flight-perf'`
  - `direnv exec . cargo test -p xiuxian-wendao-runtime reuses_cached_search_family_payload --lib --features julia -- --test-threads=1`
  - `direnv exec . cargo test -p xiuxian-wendao-runtime get_flight_info_uses_search_family_provider --lib --features julia -- --test-threads=1`
  - `direnv exec . cargo check -p xiuxian-vector --lib --message-format short`
  - `direnv exec . cargo check -p xiuxian-wendao-julia --lib`
  - `direnv exec . cargo test -p xiuxian-wendao studio_search_flight_provider_dispatches_knowledge_route --lib --features julia -- --test-threads=1`
  - `direnv exec . cargo check -p xiuxian-wendao --lib --features julia --message-format short`
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm run build'`
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && RUN_LIVE_GATEWAY_TEST=1 npm run test:live-gateway'`
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && RUN_LIVE_GATEWAY_TEST=1 STUDIO_LIVE_GATEWAY_URL=http://127.0.0.1:9519 npm run test:live-flight-perf'`
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && RUN_LIVE_GATEWAY_TEST=1 npm run test:live-flight-perf'`
- Artifact outputs:
  - `.cache/agent/tmp/wendao_frontend_live_flight_search_perf.json`
  - `.cache/agent/tmp/wendao_frontend_live_flight_search_perf.csv`

## WENDAO Same-Port Flight Gateway Warning Closure

- Collapsed the remaining touched-scope warnings on the integrated
  same-process same-port pure Flight gateway build:
  - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/search/handlers/code_search/query.rs`
  - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/search/handlers/code_search/types.rs`
  - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/search/handlers/code_search/search/mod.rs`
  - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/search/handlers/code_search/search/repo_search.rs`
  - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/search/handlers/queries.rs`
  - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/search/handlers/knowledge/intent/types.rs`
  - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/search/handlers/knowledge/merge.rs`
- Kept the slice narrow:
  - retired code-search response/buffered/task seams are now test-only
  - test-only `SearchQuery.repo` and intent transport metadata no longer sit
    in the production compile graph
  - no outward Flight contract changed
- Validation:
  - `direnv exec . cargo check -p xiuxian-wendao --bin wendao --features julia --message-format short`
  - `direnv exec . cargo test -p xiuxian-wendao gateway::studio::search::handlers::tests::query:: --lib --features julia -- --test-threads=1`
  - `direnv exec . cargo test -p xiuxian-wendao gateway::studio::search::handlers::tests::intent:: --lib --features julia -- --test-threads=1`
  - `direnv exec . cargo test -p xiuxian-wendao gateway::studio::search::handlers::tests::code_search:: --lib --features julia -- --test-threads=1`

## WENDAO Julia Plugin Flight Transport Closure

- Finished the remaining `xiuxian-wendao-julia` transport cutover from local
  Arrow IPC HTTP assumptions to runtime-owned Flight negotiation:
  - `packages/rust/crates/xiuxian-wendao-julia/src/plugin/transport.rs`
  - `packages/rust/crates/xiuxian-wendao-julia/src/plugin/test_support/contract.rs`
  - `packages/rust/crates/xiuxian-wendao-julia/src/plugin/test_support/official_examples.rs`
  - `packages/rust/crates/xiuxian-wendao-julia/README.md`
- The Julia plugin transport builder now:
  - normalizes repository `flight_transport` config into a generic
    `PluginCapabilityBinding`
  - negotiates only `ArrowFlight` through `xiuxian-wendao-runtime`
  - stamps `wendao.schema_version = v1` onto outgoing request batches before
    the Flight call
  - validates the returned Julia response batches against the crate-owned `v1`
    contract after the Flight roundtrip
- Closed the last analyzer-owned loopback blocker in the same slice:
  - `.data/WendaoAnalyzer/scripts/run_stream_linear_blend_server.sh`
  - `.data/WendaoAnalyzer/scripts/run_table_linear_blend_server.sh`
  - `.data/WendaoAnalyzer/scripts/run_analyzer_example.jl`
  - `.data/WendaoAnalyzer/README.md`
- Concrete fixes:
  - the linear-blend wrappers now invoke `run_analyzer_service.sh` through
    `bash`, so they no longer fail on a missing executable bit
  - the analyzer launcher now reuses a cached
    `.cache/julia/wendaoanalyzer-flight-env` instead of rebuilding a fresh temp
    env on every Flight service boot
- Validation:
  - `CARGO_TARGET_DIR=.cache/cargo-target/codex-xiuxian-wendao-julia-flight direnv exec . cargo test -p xiuxian-wendao-julia transport --lib -- --test-threads=1`
  - `CARGO_TARGET_DIR=.cache/cargo-target/codex-xiuxian-wendao-julia-flight direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate planned_search_wendaoanalyzer_linear_blend::test_planned_search_payload_applies_wendaoanalyzer_linear_blend_example --features julia -- --exact --test-threads=1`

## WENDAO Julia Rerank Flight Gate Closure

- Closed the remaining Flight-only Julia rerank gate failures after the
  `/rerank` cutover:
  - `packages/rust/crates/xiuxian-wendao/src/link_graph/index/search/plan/payload/quantum/rerank.rs`
  - `packages/rust/crates/xiuxian-wendao/src/link_graph/index/search/plan/payload/quantum/tests.rs`
  - `packages/rust/crates/xiuxian-wendao/tests/integration/support/wendaoarrow_custom_service.rs`
  - `packages/rust/crates/xiuxian-wendao/tests/integration/planned_search_julia_rerank.rs`
  - `packages/rust/crates/xiuxian-wendao/tests/integration/planned_search_julia_rerank_metadata_example.rs`
  - `.data/WendaoAnalyzer/examples/analyzer_service.jl`
  - `.data/WendaoAnalyzer/README.md`
- Fixed two concrete Flight-only breakages:
  - Julia rerank request batches now attach both `trace_id` and
    `wendao.schema_version = v1`, so WendaoArrow stream services no longer
    reject Rust-side Flight requests as schema-version-less.
  - the analyzer launcher no longer redefines bootstrap constants or
    re-activates the package-root environment, so `run_analyzer_service.sh`
    can actually boot the vendored `gRPCServer`-backed Flight server.
- Validation:
  - `direnv exec . cargo test -q -p xiuxian-wendao --lib link_graph::index::search::plan::payload::quantum::tests::attach_plugin_rerank_request_metadata_sets_schema_metadata --features julia -- --exact --test-threads=1`
  - `direnv exec . cargo test -q -p xiuxian-wendao --test xiuxian-testing-gate planned_search_julia_rerank --features julia -- --test-threads=1`
  - `direnv exec . cargo test -q -p xiuxian-wendao --test xiuxian-testing-gate planned_search_wendaoanalyzer --features julia -- --test-threads=1`
  - `direnv exec . julia --project=.data/WendaoAnalyzer .data/WendaoAnalyzer/test/runtests.jl`

## WENDAO Frontend Pure Flight Search Live Proof

- Made the dedicated Flight binary browser-consumable:
  - `packages/rust/crates/xiuxian-wendao/src/bin/wendao_search_flight_server.rs`
  - added `tonic_web::GrpcWebLayer`
  - enabled `accept_http1(true)`
- Added a dedicated frontend Flight config lane in:
  - `.data/wendao-frontend/wendao.toml`
  - `[gateway] bind = "127.0.0.1:9517"`
  - `[search_flight] bind = "127.0.0.1:9527"`
  - `[search_flight] schema_version = "v2"`
- Added the browser/runtime Flight transport seam in:
  - `.data/wendao-frontend/src/api/flightSearchTransport.ts`
  - `.data/wendao-frontend/src/api/flight/generated/Flight_connect.ts`
  - `.data/wendao-frontend/src/api/flight/generated/Flight_pb.ts`
- Switched frontend knowledge search to pure Flight in:
  - `.data/wendao-frontend/src/api/clientRuntime.ts`
- Added dev/proxy/config support in:
  - `.data/wendao-frontend/src/config/loader.ts`
  - `.data/wendao-frontend/scripts/rspack/build-environment.ts`
  - `.data/wendao-frontend/scripts/rspack/index.ts`
  - `.data/wendao-frontend/rspack.config.ts`
- Updated tests in:
  - `.data/wendao-frontend/src/api/flightSearchTransport.test.ts`
  - `.data/wendao-frontend/src/api/client.test.ts`
  - `.data/wendao-frontend/src/build/rspackBuildEnvironment.test.ts`
  - `.data/wendao-frontend/src/config/__tests__/loader.test.ts`
  - `.data/wendao-frontend/src/api/liveGateway.test.ts`
- Live end-to-end result:
  - ran the dedicated Flight server on `127.0.0.1:9527`
  - ran the current gateway on `127.0.0.1:9517`
  - `src/api/liveGateway.test.ts` now passes against the live pair
  - the live test now also passes without gateway/Flight URL overrides because
    `.data/wendao-frontend/wendao.toml` is realigned to the process-compose
    gateway lane and the active Flight server lane
  - this validates frontend config push, live gateway graph resolution, and
    pure Flight knowledge search through `/search/knowledge`
- Validation:
  - `direnv exec . cargo check -p xiuxian-wendao-runtime --lib --features julia`
  - `direnv exec . cargo check -p xiuxian-wendao --bin wendao_search_flight_server --features julia`
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm test -- src/api/flightSearchTransport.test.ts src/build/rspackBuildEnvironment.test.ts src/config/__tests__/loader.test.ts src/api/client.test.ts'`
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && RUN_LIVE_GATEWAY_TEST=1 STUDIO_LIVE_GATEWAY_URL=http://127.0.0.1:9518 STUDIO_LIVE_FLIGHT_URL=http://127.0.0.1:9527 npm test -- src/api/liveGateway.test.ts'`
  - `direnv exec . git diff --check`

## WENDAO Search Knowledge Flight Alignment

- Added the semantic Flight route
  `SEARCH_KNOWLEDGE_ROUTE = /search/knowledge` to the runtime query contract
  and the generic search-family matcher in:
  - `packages/rust/crates/xiuxian-wendao-runtime/src/transport/query_contract.rs`
  - `packages/rust/crates/xiuxian-wendao-runtime/src/transport/server.rs`
  - `packages/rust/crates/xiuxian-wendao-runtime/src/transport/mod.rs`
- Added the Studio-owned knowledge Flight materialization seam in:
  - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/search/handlers/knowledge/search.rs`
- Reused the shared `SearchHit` Arrow batch shape from:
  - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/search/handlers/knowledge/intent/flight.rs`
- Extended the aggregate Studio search Flight provider to multiplex:
  - `/search/intent`
  - `/search/knowledge`
  - `/search/references`
  - `/search/symbols`
  - `/search/ast`
- Updated the shared semantic-search Flight snapshot:
  - `packages/rust/crates/xiuxian-wendao/tests/snapshots/gateway/studio/search_flight_service_route_contracts.snap`
- Validation:
  - `direnv exec . cargo test -p xiuxian-wendao-runtime transport::query_contract::tests:: --lib`
  - `direnv exec . cargo test -p xiuxian-wendao-runtime search_family_route_matcher_accepts_semantic_business_routes --lib --features julia -- --test-threads=1`
  - `direnv exec . cargo test -p xiuxian-wendao studio_search_flight_provider_dispatches_knowledge_route --lib --features julia -- --test-threads=1`
  - `env INSTA_UPDATE=always direnv exec . cargo test -p xiuxian-wendao build_studio_search_flight_service_snapshots_search_route_contracts --lib --features julia -- --test-threads=1`
  - `direnv exec . cargo test -p xiuxian-wendao build_studio_search_flight_service_snapshots_search_route_contracts --lib --features julia -- --test-threads=1`
  - `direnv exec . cargo check -p xiuxian-wendao --lib --features julia`
  - `direnv exec . git diff --check`

## WENDAO Search Flight Helper Retirement

- Removed the dead in-crate search Arrow IPC helper modules:
  - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/search/handlers/arrow_transport.rs`
  - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/search/handlers/knowledge/intent/arrow.rs`
- Removed the obsolete attachments/references/symbols Arrow roundtrip helper
  coverage from:
  - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/search/handlers/attachments.rs`
  - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/search/handlers/references.rs`
  - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/search/handlers/symbols.rs`
- Tightened `retrieval_arrow.rs` so only the actual test-only IPC encoder
  remains behind `#[cfg(test)]`.
- Result:
  - the semantic search contract is now pinned by
    `packages/rust/crates/xiuxian-wendao/tests/snapshots/gateway/studio/search_flight_service_route_contracts.snap`
  - touched-scope `cargo check -p xiuxian-wendao --lib --features julia`
    no longer reports the old local search-helper warnings
- Validation:
  - `direnv exec . cargo check -p xiuxian-wendao --lib --features julia`
  - `direnv exec . cargo test -p xiuxian-wendao gateway::studio::search::handlers::flight::tests::build_studio_search_flight_service_snapshots_search_route_contracts --lib --features julia -- --exact --test-threads=1`
  - `direnv exec . cargo test -p xiuxian-wendao gateway::openapi::document::tests::bundled_gateway_openapi_document_omits_flight_only_http_paths --lib --features julia -- --exact --test-threads=1`
  - `git diff --check`

## WENDAO Search Wrapper Retirement

- Removed the retired semantic-search HTTP wrapper shells from the unit-test
  graph:
  - `search_intent(...)`
  - `search_attachments(...)`
  - `search_references(...)`
  - `search_symbols(...)`
- The remaining search unit proofs now call the active response seams
  directly:
  - `load_intent_search_response_with_metadata(...)`
  - `load_attachment_search_response_from_studio(...)`
  - `load_reference_search_response(...)`
  - `load_symbol_search_response(...)`
- Result:
  - pure-Flight search cleanup no longer keeps dead HTTP wrapper code alive
    just for unit coverage
  - the shared Flight snapshot remains the semantic contract lock
- Validation:
  - `direnv exec . cargo test -p xiuxian-wendao gateway::studio::search::handlers::studio_search_tests::search_intent_requires_query --lib --features julia -- --exact --test-threads=1`
  - `direnv exec . cargo test -p xiuxian-wendao gateway::studio::search::handlers::studio_search_tests::search_attachments_returns_payload --lib --features julia -- --exact --test-threads=1`
  - `direnv exec . cargo test -p xiuxian-wendao gateway::studio::search::handlers::studio_search_tests::search_references_returns_payload --lib --features julia -- --exact --test-threads=1`
  - `direnv exec . cargo test -p xiuxian-wendao gateway::studio::search::handlers::studio_search_tests::search_symbols_returns_payload --lib --features julia -- --exact --test-threads=1`

## WENDAO Pure Flight Surface Cut

- Removed the remaining legacy HTTP business surfaces for:
  - `/api/search/ast`
  - `/api/analysis/markdown`
  - `/api/analysis/code-ast`
- Kept the canonical business contracts on Arrow Flight only:
  - `/search/ast`
  - `/analysis/markdown`
  - `/analysis/code-ast`
- Removed the corresponding route registrations and `OpenAPI` surface entries
  instead of leaving `410 Gone` compatibility shims in place.
- Updated the active Wendao package docs and ExecPlan so the governing record
  now matches the direct-cut pure Flight boundary.

## ARROW-FLIGHT Emit Buffer Sizing

- Continued the Arrow.jl Flight runtime line with one narrow emit-path change:
  - `/Users/guangtao/projects/xiuxian-artisan-workshop/.data/arrow-julia/src/flight/convert/framing.jl`
- `_message_body(...)` now uses `IOBuffer(; sizehint=Int(msg.bodylen))`
  instead of an unhinted buffer.
- Kept the slice bounded:
  - no schema contract changes
  - no dictionary/record-batch contract changes
  - no request buffer default changes
- Focused phase validation indicates the win is in body-buffer growth, not in
  `toarrowtable` or message flatbuffer construction:
  - previous `full_flightdata`: `1.581 ms / 3.79 MiB`
  - current `full_flightdata`: `1.242 ms / 2.127 MiB`
  - `schema_message`: `0.281 ms / 0.207 MiB`
  - `recordbatch_message`: `0.274 ms / 0.207 MiB`
- Full validation passed:
  - Flight suite remained green
  - `Arrow | 66285/66285 | 3m40.4s`
  - `git diff --check` remained clean
- Archived artifacts:
  - `.cache/agent/tmp/arrow-julia-flight-emit-phase-body-sizehint.tsv`
  - `.cache/agent/tmp/arrow-julia-flight-stream-bench-body-sizehint.tsv`
- Evaluated and rejected one adjacent upstream candidate in
  `/Users/guangtao/projects/xiuxian-artisan-workshop/.data/arrow-julia/src/arraytypes/list.jl`:
  - widening `_sizehint_iobuffer!` to Julia 1.12 `Memory{UInt8}`
  - then narrowing it to string-only
- Local string-loop A/B looked mildly positive, but broad write-path validation
  rejected it:
  - `.cache/agent/tmp/arrow-julia-list-sizehint-ab.tsv`
  - `.cache/agent/tmp/arrow-julia-writepaths-string-only-sizehint.tsv`
- Result:
  - no `list.jl` helper change was retained
  - the accepted slice remains only the `framing.jl` body-buffer sizehint
- Decomposed the remaining `putflightdata!` emit hotspot further:
  - `.cache/agent/tmp/arrow-julia-flight-writebuffer-columns.tsv`
  - `.cache/agent/tmp/arrow-julia-flight-writebuffer-name-components.tsv`
- Current finding:
  - the residual cost is dominated by string payload writing in the `name`
    column data segment
  - primitive column emit, bitmap emit, and offset emit are comparatively tiny
- Evaluated and rejected one structural candidate:
  - body-buffer reuse across messages
  - `.cache/agent/tmp/arrow-julia-flight-reuse-body-buffer-ab.tsv`
  - result was allocation-negative, so nothing was retained

## ARROW-FLIGHT Async Producer Contract

- Continued the Arrow.jl Flight production-runtime line instead of widening API
  surface.
- Replaced `Threads.@spawn` request/response producer tasks with one shared
  async producer seam:
  - `/Users/guangtao/projects/xiuxian-artisan-workshop/.data/arrow-julia/src/flight/client/transport.jl`
  - `/Users/guangtao/projects/xiuxian-artisan-workshop/.data/arrow-julia/src/flight/client/methods/data.jl`
  - `/Users/guangtao/projects/xiuxian-artisan-workshop/.data/arrow-julia/src/flight/server/exchange_helpers.jl`
- Kept the slice narrow:
  - no new Flight RPC methods
  - no request payload format changes
  - no metadata-contract changes
- Verified the production effect with focused Flight benches:
  - `.cache/agent/tmp/arrow-julia-flight-request-emit-async.tsv`
  - `.cache/agent/tmp/arrow-julia-flight-stream-bench-async.tsv`
- Current perf readout shows:
  - `native_request_emit 2.414 ms / 3.809 MiB`
  - `legacy_request_collect 2.607 ms / 3.808 MiB`
  - `legacy_request_emit 2.523 ms / 3.809 MiB`
- Interpretation:
  - request-side native source overloads are still primarily a runtime-contract
    and ergonomics win
  - but the async producer seam avoids unnecessary thread scheduling overhead
    and now leaves the native request path slightly ahead of both legacy
    baselines in the full Flight bench
- Evaluated a second request-side tuning candidate: lowering the default
  request channel buffer from `16` to `8`.
- Rejected it after direct API-level probes:
  - the focused channel probe favored `8`
  - in-process `Arrow.Flight.table(service, context, source; request_capacity=...)`
    did not show a stable enough win to justify changing the production
    default
- Kept only the async producer seam; request buffer defaults remain unchanged.

## ARROW-FLIGHT Server Metadata Contract

- Tightened the server-core Flight contract tests instead of expanding API
  surface.
- Updated:
  - `/Users/guangtao/projects/xiuxian-artisan-workshop/.data/arrow-julia/test/flight/server_core/support.jl`
  - `/Users/guangtao/projects/xiuxian-artisan-workshop/.data/arrow-julia/test/flight/server_core/exchange_helper_tests.jl`
  - `/Users/guangtao/projects/xiuxian-artisan-workshop/.data/arrow-julia/test/flight/server_core.jl`
- The shared `sample_table` fixture now carries table metadata plus field
  metadata.
- `exchangeservice`, `tableservice`, and `streamservice` now explicitly assert:
  - request decode preserves metadata
  - response re-emit preserves metadata
  - local helper paths use the same contract
- Added explicit `using gRPCClient` and `using Tables` to the standalone
  `server_core.jl` entrypoint so it matches the dependencies used by the
  included test files.
- Validation passed:
  - `Flight server core surface | 89/89 | 6.1s`
  - `Flight IPC conversion helpers | 64/64`
  - `Flight pyarrow interop | 35/35`
  - `Arrow | 66285/66285 | 3m50.6s`
- Interpretation:
  - this was a contract-tightening slice
  - no new Flight performance claim was made
  - confidence is higher that native Julia Flight preserves schema metadata and
    field metadata through decode, processing, and re-emit

## ARROW-FLIGHT Decode Materialization Helper

- Landed one narrow decode-side helper instead of chasing another buffer tweak:
  - `/Users/guangtao/projects/xiuxian-artisan-workshop/.data/arrow-julia/src/table.jl`
  - `/Users/guangtao/projects/xiuxian-artisan-workshop/.data/arrow-julia/src/flight/convert/streaming.jl`
- Added `materializecolumns(::VectorIterator)` and routed:
  - core `collect_cols!`
  - native Flight record-batch decode
    through the same helper
- Motivation:
  - `collect(VectorIterator(...))` currently materializes as `Vector{Any}`
  - this helper keeps the column materialization shape explicit and shared
- Focused probe stayed small/noisy:
  - `.cache/agent/tmp/arrow-julia-flight-vectoriterator-ab.tsv`
  - `collect_iter 0.024 ms / 0.005 MiB`
  - `manual_iter 0.026 ms / 0.005 MiB`
- Broad Flight bench stayed in-band:
  - `.cache/agent/tmp/arrow-julia-flight-stream-bench-vectoriterator.tsv`
  - `native_full 0.052 ms / 0.015 MiB`
  - `native_table_full 0.063 ms / 0.017 MiB`
  - `native_reemit_stream 0.219 ms / 1.758 MiB`
  - `legacy_table_full 0.142 ms / 4.026 MiB`
- Full validation passed:
  - `Arrow | 66285/66285 | 3m37.9s`
  - `git diff --check` clean
- Interpretation:
  - keep this as a small shared decode helper
  - do not overstate it as a major throughput win

## ARROW-FLIGHT Server Response App Metadata Contract

- Closed a real server-side Flight runtime gap instead of continuing another
  string-payload micro-optimization.
- Updated:
  - `/Users/guangtao/projects/xiuxian-artisan-workshop/.data/arrow-julia/src/flight/server/exchange_helpers.jl`
  - `/Users/guangtao/projects/xiuxian-artisan-workshop/.data/arrow-julia/test/flight/server_core/exchange_helper_tests.jl`
- `exchangeservice`, `tableservice`, and `streamservice` now accept
  `app_metadata=...` for response re-emit, with the same shared
  `putflightdata!` runtime contract used on the client-side request path.
- Local helper entrypoints are now aligned too:
  - `Arrow.Flight.table(service, ...)`
  - `Arrow.Flight.stream(service, ...; include_app_metadata=true)`
- Validation passed:
  - `Flight server core surface | 102/102`
  - full Flight suite stayed green
  - `Arrow | 66285/66285 | 3m39.9s`
- Perf rerun stayed in-band:
  - `.cache/agent/tmp/arrow-julia-flight-stream-bench-app-metadata.tsv`
  - `native_full 0.051 ms / 0.015 MiB`
  - `native_table_full 0.074 ms / 0.017 MiB`
  - `native_reemit_stream 0.208 ms / 1.758 MiB`
- Interpretation:
  - this is a runtime-contract completion slice
  - not a new throughput claim

## ARROW-FLIGHT Helper Request App Metadata Contract

- Continued the same native Flight runtime line by moving request-side
  `app_metadata` into the local server helper contract as well.
- Updated:
  - `/Users/guangtao/projects/xiuxian-artisan-workshop/.data/arrow-julia/src/flight/server/exchange_helpers.jl`
  - `/Users/guangtao/projects/xiuxian-artisan-workshop/.data/arrow-julia/test/flight/server_core/exchange_helper_tests.jl`
- `tableservice(...)` and `streamservice(...)` now accept
  `include_request_app_metadata=true`.
- This means helper processors can consume request-side batch metadata through
  the same native `stream(...)` / `table(...)` contract already used by the
  gRPC fixture path, instead of forcing downstream callers to decode raw
  request channels by hand.
- Validation passed:
  - `Flight server core surface | 114/114`
  - full Flight suite remained green
  - `Arrow | 66285/66285 | 3m32.1s`
- Perf rerun stayed in-band:
  - `.cache/agent/tmp/arrow-julia-flight-stream-bench.tsv`
  - `native_request_emit 1.632 ms / 2.145 MiB`
  - `legacy_request_collect 1.699 ms / 2.144 MiB`
  - `legacy_request_emit 1.614 ms / 2.145 MiB`
  - `native_reemit_stream 0.198 ms / 1.758 MiB`
  - `legacy_reemit_table 1.91 ms / 8.275 MiB`
- Interpretation:
  - this slice closes another client/server contract gap
  - it is not being claimed as a new throughput optimization

## ARROW-FLIGHT PyArrow DoPut App Metadata Contract

- Continued the same Flight runtime contract line on the pyarrow interop side.
- Updated:
  - `/Users/guangtao/projects/xiuxian-artisan-workshop/.data/arrow-julia/test/flight_pyarrow_server.py`
  - `/Users/guangtao/projects/xiuxian-artisan-workshop/.data/arrow-julia/test/flight/pyarrow_interop/upload_tests.jl`
  - `/Users/guangtao/projects/xiuxian-artisan-workshop/.data/arrow-julia/src/flight/server/exchange_helpers.jl`
- `pyarrow` test-server `do_put(...)` no longer collapses request input through
  `read_all()`.
- It now reads request chunks incrementally and reflects request-side
  `app_metadata` in the returned `PutResult`, so the interop test can verify
  the upstream native Julia request contract instead of treating
  `app_metadata` as an internal-only feature.
- `upload_tests.jl` now sends batch-wise `app_metadata` through the source
  overload and asserts pyarrow received both entries.
- Validation passed:
  - full Flight suite remained green
  - `Arrow | 66285/66285 | 3m35.3s`
- Perf rerun stayed in-band:
  - `.cache/agent/tmp/arrow-julia-flight-stream-bench-put-app.tsv`
  - `native_request_emit 1.556 ms / 2.145 MiB`
  - `legacy_request_collect 1.547 ms / 2.144 MiB`
  - `legacy_request_emit 1.529 ms / 2.145 MiB`
  - `native_reemit_stream 0.217 ms / 1.758 MiB`
  - `legacy_reemit_table 2.062 ms / 8.275 MiB`
- Interpretation:
  - this closes the cross-language `DoPut` request-side `app_metadata`
    contract

## ARROW-FLIGHT Decode Header Cleanup

- Continued the same Flight runtime line with a decode-side cleanup in:
  - `/Users/guangtao/projects/xiuxian-artisan-workshop/.data/arrow-julia/src/flight/convert/streaming.jl`
- Native Flight decode now parses the message flatbuffer header first and only
  constructs `Arrow.Batch` for actual `DictionaryBatch` and `RecordBatch`
  messages.
- This removes one eager batch-materialization step from the native decode
  seam while leaving the public API unchanged.
- Validation passed:
  - full Flight suite remained green
  - `Arrow | 66285/66285 | 3m42.6s`
  - `git diff --check` remained clean
- Archived artifact:
  - `.cache/agent/tmp/arrow-julia-flight-stream-bench-header-dispatch.tsv`
- Current accepted numbers:
  - `native_request_emit 1.626 ms / 2.145 MiB`
  - `legacy_request_collect 1.733 ms / 2.144 MiB`
  - `native_full 0.054 ms / 0.015 MiB`
  - `native_table_full 0.066 ms / 0.017 MiB`
  - `native_reemit_stream 0.215 ms / 1.758 MiB`

## ARROW-FLIGHT Direct Emit Dispatch Rejection

- Evaluated and rejected a follow-up candidate in:
  - `/Users/guangtao/projects/xiuxian-artisan-workshop/.data/arrow-julia/src/flight/convert/flightdata.jl`
- Candidate:
  - dispatch `_emitflightdata!` directly on `Channel{FlightData}` instead of
    keeping the existing `message -> put!(sink, message)` closure seam
- Rejection reason:
  - the request-side signal was not positive enough for a production slice
  - it did not improve the accepted native request path strongly enough to
    justify extra churn in the emit contract
- Archived artifacts:
  - `.cache/agent/tmp/arrow-julia-flight-stream-bench-direct-emit-dispatch.tsv`
  - `.cache/agent/tmp/arrow-julia-flight-stream-bench-revert-dispatch.tsv`
- Accepted reverted baseline:
  - `native_request_emit 1.767 ms / 2.145 MiB`
  - `legacy_request_collect 1.614 ms / 2.144 MiB`
  - `legacy_request_emit 1.792 ms / 2.145 MiB`
- Result:
  - kept the decode-header cleanup
  - explicitly dropped the direct-emit dispatch candidate
  - current request-side native source overload remains a runtime-contract and
    ergonomics win first, not yet a throughput win
  - it is not a new throughput claim

## ARROW-FLIGHT Single-Batch Table Copy Elision

- Continued the production Flight runtime line by removing one unnecessary
  materialization boundary on the native `table(...)` path.
- Updated:
  - `/Users/guangtao/projects/xiuxian-artisan-workshop/.data/arrow-julia/src/flight/convert/streaming.jl`
  - `/Users/guangtao/projects/xiuxian-artisan-workshop/.data/arrow-julia/test/flight/ipc_conversion.jl`
- `_materialize_flight_table(...)` no longer copies the first decoded batch
  when the Flight response contains only a single record batch.
- Multi-batch aggregation behavior stays unchanged; this slice is narrowly
  about the single-batch fast path.
- Added a regression that locks single-batch metadata and `app_metadata`
  preservation through `Arrow.Flight.table(...; include_app_metadata=true)`.
- Validation passed:
  - full Flight suite remained green
  - `Arrow | 66285/66285 | 3m44.4s`
- Perf artifacts:
  - `.cache/agent/tmp/arrow-julia-flight-singlebatch-table.tsv`
  - `.cache/agent/tmp/arrow-julia-flight-stream-bench-singlebatch-copy-elision.tsv`
- Focused result:
  - `native_table_single 0.053 ms / 0.008 MiB`
  - `native_table_single_app 0.057 ms / 0.009 MiB`
  - `legacy_table_single 0.104 ms / 1.346 MiB`
- Broad Flight bench stayed in-band:
  - `native_table_full 0.068 ms / 0.017 MiB`
  - `legacy_table_full 0.199 ms / 4.026 MiB`
  - `native_reemit_stream 0.218 ms / 1.758 MiB`
- Interpretation:
  - this removes one real copy/materialization boundary from the native Flight
    runtime
  - the gain is intentionally targeted at single-batch responses

## ARROW-FLIGHT Header Dispatch Cleanup

- Continued the native Flight decode line with a narrow runtime cleanup in:
  - `/Users/guangtao/projects/xiuxian-artisan-workshop/.data/arrow-julia/src/flight/convert/streaming.jl`
- Schema/tensor discrimination no longer constructs a full `Batch` before the
  header type is known.
- `Batch` materialization now happens only when the message is a
  `DictionaryBatch` or `RecordBatch`, which is the first point where the full
  body-backed wrapper is actually needed.
- Validation passed:
  - full Flight suite remained green
  - `Arrow | 66285/66285 | 3m51.0s`
- Perf rerun stayed in-band:
  - `.cache/agent/tmp/arrow-julia-flight-stream-bench-header-dispatch.tsv`
  - `native_request_emit 1.626 ms / 2.145 MiB`
  - `legacy_request_collect 1.733 ms / 2.144 MiB`
  - `legacy_request_emit 1.901 ms / 2.145 MiB`
  - `native_full 0.054 ms / 0.015 MiB`
  - `native_table_full 0.066 ms / 0.017 MiB`
  - `native_reemit_stream 0.215 ms / 1.758 MiB`
- Interpretation:
  - this is a decode-side runtime cleanup
  - it stays consistent with the current Flight performance envelope, but is
    not being claimed as a large new throughput milestone

## ARROW-FLIGHT Direct-IOBuffer Payload Rejection

- Evaluated and rejected another emit-side candidate in:
  - `/Users/guangtao/projects/xiuxian-artisan-workshop/.data/arrow-julia/src/arraytypes/list.jl`
- Candidate:
  - copy `ToList{UInt8}` string/binary chunks directly into a pre-sized
    `IOBuffer` backing store instead of using the retained chunk write loop
- Focused local A/B looked positive:
  - `.cache/agent/tmp/arrow-julia-flight-string-direct-iobuffer-sized-ab.tsv`
    - `current_loop 1.147 ms / 0.564 MiB`
    - `direct_iobuffer_loop 0.949 ms / 0.564 MiB`
  - `.cache/agent/tmp/arrow-julia-flight-binary-direct-iobuffer-sized-ab.tsv`
    - `current_loop 1.104 ms / 0.564 MiB`
    - `direct_iobuffer_loop 0.829 ms / 0.564 MiB`
- Broad validation rejected it:
  - `.cache/agent/tmp/arrow-julia-writepaths-iobuffer-direct-rerun.tsv`
    - `offset_string 2.533 ms`
    - `offset_binary 2.727 ms`
    - `offset_nested_list 0.88 ms`
    - `offset_map 2.39 ms`
  - `.cache/agent/tmp/arrow-julia-flight-stream-bench-iobuffer-direct-rerun.tsv`
    - `native_request_emit 3.626 ms / 2.145 MiB`
    - `legacy_request_collect 2.945 ms / 2.144 MiB`
    - `legacy_request_emit 2.917 ms / 2.145 MiB`
- Reverted the candidate and revalidated:
  - `.cache/agent/tmp/arrow-julia-writepaths-iobuffer-revert.tsv`
  - `.cache/agent/tmp/arrow-julia-flight-stream-bench-iobuffer-revert.tsv`
  - `Arrow | 66285/66285 | 18m32.7s`
  - `git diff --check` remained clean
- Result:
  - kept the accepted Flight body `sizehint` slice in
    `/Users/guangtao/projects/xiuxian-artisan-workshop/.data/arrow-julia/src/flight/convert/framing.jl`
  - explicitly dropped the direct-IOBuffer payload writer
  - next hotspot remains string payload body write itself, not another buffer
    layout trick

## ARROW-FLIGHT Flight-Only Exact Body Buffer

- Continued the same emit-side hotspot line, but kept the optimization local to
  Flight instead of touching the general Arrow write path.
- Updated:
  - `/Users/guangtao/projects/xiuxian-artisan-workshop/.data/arrow-julia/src/flight/convert/framing.jl`
- Landed:
  - an internal exact-size `FlightBodyBuffer`
  - a Flight-only `writezeros(...)` specialization
  - a Flight-only `writearray(::FlightBodyBuffer, ::Type{UInt8}, ::ToList{UInt8})`
    specialization that copies string/binary chunks directly into the exact
    body buffer
- Focused Flight-body benchmark stayed positive:
  - `.cache/agent/tmp/arrow-julia-flight-string-body-buffer-flightonly.tsv`
  - `string_current_sizehint 1.363 ms / 0.564 MiB`
  - `string_direct_exact 1.14 ms / 0.578 MiB`
  - `binary_current_sizehint 1.947 ms / 0.564 MiB`
  - `binary_direct_exact 1.474 ms / 0.578 MiB`
- Broad Flight bench remained in-band:
  - `.cache/agent/tmp/arrow-julia-flight-stream-bench-flightbodybuffer.tsv`
  - `native_request_emit 2.009 ms / 2.156 MiB`
  - `legacy_request_collect 2.025 ms / 2.155 MiB`
  - `legacy_request_emit 1.979 ms / 2.156 MiB`
  - `native_reemit_stream 0.21 ms / 1.769 MiB`
  - `legacy_reemit_table 1.886 ms / 8.285 MiB`
- Validation passed:
  - `Arrow | 66285/66285 | 3m32.3s`
  - `git diff --check` remained clean
- Interpretation:
  - the earlier global direct-IOBuffer path was still wrong for production
  - the same idea is acceptable when constrained to the exact Flight body
    buffer shape
  - this improves the Flight body-write seam without perturbing the general
    Arrow write contract

## ARROW-FLIGHT Request Emit Commit Boundary

- Added a focused component decomposition in:
  - `.cache/agent/benchmarks/arrow_julia_flight_request_emit_components.jl`
- Result artifact:
  - `.cache/agent/tmp/arrow-julia-flight-request-emit-components.tsv`
- Key numbers:
  - `vector_collect 0.962 ms / 2.155 MiB`
  - `channel_only 0.073 ms / 0.019 MiB`
  - `vector_then_channel 1.034 ms / 2.156 MiB`
  - `native_channel 1.122 ms / 2.156 MiB`
- Interpretation:
  - the remaining request-side gap is now small and mostly structural
  - native request streaming is close to the `vector_then_channel` baseline
  - this is not enough headroom to justify another risky request-side rewrite
    before the next commit
- Decision:
  - the next commit boundary is the current accepted Arrow.jl Flight/docs/runtime
    batch, including the Flight-only exact body buffer slice
  - further request-side performance work should start from that clean point

## PY-F96 Analyzer Repo-Search Convenience API

- Continued the `xiuxian-wendao-analyzer` real-host v1 line instead of adding
  more transport-harness-only coverage.
- Promoted the already-proven repo-search live seam into a public analyzer API:
  - `packages/python/xiuxian-wendao-analyzer/src/xiuxian_wendao_analyzer/runtime.py`
  - added `analyze_repo_search(client, request, ...)`
- Kept the package boundary clean:
  - the helper still routes through `xiuxian-wendao-py`
  - it reuses `repo_search_query()` and `repo_search_metadata(...)`
  - it does not rebuild raw Arrow/Flight metadata inside the analyzer package
- Added local and live-host verification:
  - `packages/python/xiuxian-wendao-analyzer/tests/test_transport_runtime.py`
  - local monkeypatch test proves the helper forwards typed request metadata
  - real-host integration test proves it works against
    `wendao_search_flight_server`
- Synced package docs and the active ExecPlan:
  - `packages/python/xiuxian-wendao-analyzer/README.md`
  - `.cache/codex/execplans/python_wendao_analyzer_real_host_v1.md`

## PY-F97 Typed Analyzer Result Model

- Added a lightweight typed analyzer result surface:
  - `packages/python/xiuxian-wendao-analyzer/src/xiuxian_wendao_analyzer/models.py`
  - `AnalyzerResultRow`
  - `parse_analyzer_result_rows(...)`
- Extended analyzer runtime helpers with typed result variants:
  - `analyze_result_rows(...)`
  - `analyze_table_results(...)`
  - `analyze_query_results(...)`
  - `analyze_repo_search_results(...)`
- Kept backward compatibility:
  - existing raw `dict`-returning helpers remain unchanged
  - typed helpers sit on top of the same analyzer outputs
- Added coverage at both layers:
  - local model/runtime tests
  - live-host repo-search typed-result smoke through
    `wendao_search_flight_server`

## PY-F98 Typed Rerank Convenience Surface

- Added analyzer-owned local convenience helpers for typed rerank input rows:
  - `analyze_rerank_rows(...)`
  - `analyze_rerank_result_rows(...)`
- Kept the package boundary explicit:
  - input type is `WendaoRerankRequestRow` from `xiuxian-wendao-py`
  - execution still uses analyzer-local strategy logic
  - this is not yet a live-host rerank-input seam
- Added local runtime coverage proving the new helpers accept typed rerank rows
  and return both raw and typed analyzer outputs
- Synced package docs to distinguish:
  - landed local typed rerank helper
  - still-missing host-backed rerank input route

## PY-F99 Arrow-Native Rerank Table Surface

- Added Arrow-native local rerank convenience helpers:
  - `analyze_rerank_table(...)`
  - `analyze_rerank_table_results(...)`
- Kept the seam aligned with the substrate:
  - input table is validated with `validate_rerank_request_table(...)`
  - callers can reuse `build_rerank_request_table(...)` from `xiuxian-wendao-py`
- Added local runtime coverage proving the analyzer package can consume a typed
  rerank request Arrow table without forcing callers to unpack rows first

## PY-F100 Built-In Score-Rank Strategy

- Added a second built-in analyzer strategy:
  - `score_rank`
- Extended `AnalyzerConfig` / `build_analyzer(...)` so repo-search analysis can
  run through analyzer-owned configuration rather than requiring an ad hoc test
  analyzer class
- Added local coverage for:
  - raw row analysis with `strategy="score_rank"`
  - typed result analysis with `strategy="score_rank"`
- Added transport-backed coverage for:
  - `analyze_repo_search(...)`
  - `analyze_repo_search_results(...)`
  - including one real-host `wendao_search_flight_server` smoke using
    `AnalyzerConfig(strategy="score_rank")`

## PY-F101 Repo-Search Query-Text Pipeline API

- Added a higher-level analyzer-facing repo-search pipeline:
  - `analyze_repo_query_text(...)`
  - `analyze_repo_query_text_results(...)`
- The helper now builds `repo_search_request(...)` internally, so callers can
  stay at the query-text/filter level instead of assembling typed requests
  themselves
- Added local coverage proving:
  - request metadata is built from the high-level parameters
  - typed result rows still work through the same pipeline
- Added a real-host `wendao_search_flight_server` smoke using:
  - query text
  - `path_prefixes`
  - built-in `score_rank`

## PY-F102 Repo Analysis Pipeline Result

- Added an analyzer-owned repo-search pipeline result model:
  - `RepoAnalysisRun`
  - carries:
    - effective `WendaoRepoSearchRequest`
    - typed analyzed rows
- Added a high-level pipeline entrypoint:
  - `run_repo_analysis(...)`
- Added local and real-host coverage proving callers can now get:
  - one pipeline call
  - one effective request object
  - one typed result set
    without manually stitching those layers together

## PY-F103 Local Rerank Analysis Pipeline Result

- Added a symmetric local rerank pipeline result:
  - `RerankAnalysisRun`
  - `run_rerank_analysis(...)`
- Kept the boundary explicit:
  - input is still local typed rerank rows
  - output is typed analyzed rows
  - this does not claim a live host rerank-input orchestration path exists yet
- Added local model/runtime coverage proving callers can now get:
  - one local rerank pipeline call
  - one preserved typed input batch
  - one typed analyzed output batch

## PY-F104 Analysis Summary Surface

- Added a shared analyzer summary model:
  - `AnalysisSummary`
- Added summary helpers for both pipeline families:
  - `summarize_result_rows(...)`
  - `summarize_repo_analysis(...)`
  - `summarize_rerank_analysis(...)`
- Added local and real-host coverage proving repo-analysis runs can now be
  reduced to a lightweight top-row snapshot without downstream callers manually
  scanning the result rows

## PY-F105 Direct Summary Entry Points

- Added one-step summary helpers:
  - `summarize_repo_query_text(...)`
  - `summarize_rerank_rows(...)`
- Kept the same truthful boundary split:
  - repo query text summary is host-backed
  - rerank row summary is local typed-input only
- Added local and real-host coverage for the repo-query-text summary path
- Added local runtime coverage for the rerank-rows summary path

## PY-F116 Analyzer Export Kind Guard

- Strengthened the `xiuxian-wendao-analyzer` package-root export contract:
  - `packages/python/xiuxian-wendao-analyzer/tests/test_exports.py`
  - still asserts every `__all__` symbol resolves from package root
  - now also asserts key exports preserve their expected kind:
    - config/model/run/analyzer symbols remain classes
    - public helper surface remains callable
- Kept the slice bounded:
  - no new analyzer helpers
  - no runtime or host behavior changes
- Synced package docs and the active analyzer ExecPlan:
  - `packages/python/xiuxian-wendao-analyzer/README.md`
  - `.cache/codex/execplans/python_wendao_analyzer_real_host_v1.md`

## PY-F117 Analyzer V1 Sufficiency Audit

- Performed a bounded v1 sufficiency audit for `xiuxian-wendao-analyzer`
  instead of continuing helper-by-helper surface expansion.
- Recorded the current operational baseline in
  `packages/python/xiuxian-wendao-analyzer/README.md`:
  - host-backed repo-search paths are sufficient for first external use
  - local rerank paths are sufficient for analyzer-owned scoring workflows
  - raw rows, typed rows, run objects, and summaries are all available
  - repo-search typed-result and summary paths already have real-host coverage
- Recorded the current non-goals explicitly:
  - no host-backed analyzer rerank-input claim
  - no plugin migration
  - no package-local Flight runtime
  - no more helper-symmetry work without a new workflow gap
- Synced the active analyzer ExecPlan with the same boundary.

## PY-F118 Analyzer Reference Workflow Smokes

- Added one explicit reference workflow smoke for each side of the current v1
  boundary:
  - local rerank workflow in
    `packages/python/xiuxian-wendao-analyzer/tests/test_runtime.py`
  - host-backed repo-search workflow in
    `packages/python/xiuxian-wendao-analyzer/tests/test_transport_runtime.py`
- The repo-search workflow now proves one realistic external-user path:
  - `run_repo_analysis(...)`
  - `summarize_repo_analysis(...)`
  - built-in `AnalyzerConfig(strategy="score_rank")`
  - real `wendao_search_flight_server`
- The local rerank workflow now proves the parallel local path:
  - `run_rerank_analysis(...)`
  - `summarize_rerank_analysis(...)`
  - default `linear_blend`
- Synced the README and active analyzer ExecPlan so the recommended workflows
  are part of the package baseline.

## PY-F119 Custom Analyzer Repo Workflow Smoke

- Added a real-host external-user workflow for custom repo analyzers:
  - `packages/python/xiuxian-wendao-analyzer/tests/test_transport_runtime.py`
  - `test_custom_repo_analyzer_workflow_reads_run_and_summary_via_wendao_search_flight_server(...)`
- This slice proves a downstream Python author can:
  - provide `analyzer=<custom analyzer object>`
  - call `run_repo_analysis(...)`
  - call `summarize_repo_analysis(...)`
  - stay on the real `wendao_search_flight_server` path
- The boundary remains clean:
  - transport still comes from `xiuxian-wendao-py`
  - analyzer semantics still come from Python user code
- Synced the package README and active analyzer ExecPlan.

## PY-F120 Analyzer Workflow Selection Guide

## PY-F121 External Consumer Dry-Run Correction

- Performed a bounded external-consumer dry run for the shipped analyzer
  examples from `packages/python/xiuxian-wendao-analyzer`.
- Confirmed the current honest beta invocation shape is:

## Wendao Analysis Shared Transport Helper

- Landed a bounded cleanup for the canonical analysis handlers instead of
  widening the outward route surface.
- Added one shared helper at
  `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/analysis/transport.rs`
  for Arrow IPC `Accept` negotiation on:
  - `/api/analysis/markdown`
  - `/api/analysis/code-ast`
- Kept the slice intentionally narrow:
  - no new outward routes
  - no payload schema changes
  - no reuse of a misleading generic response builder
- Repaired the adjacent touched-scope drift that blocked focused validation:
  - `retrieval_arrow.rs` now uses explicit `Vec<u64>` /
    `Vec<Option<u64>>` collection types for Lance Arrow 57 builders
  - the Studio search Flight builder now fills the added analysis-provider
    slots explicitly
  - dead re-exports / dead response helpers in the touched scope were removed
- Focused verification passed:
  - `gateway::studio::router::handlers::analysis::transport::tests::analysis_request_prefers_arrow_ipc_accepts_arrow_media_type`
  - `gateway::studio::router::handlers::analysis::code_ast::tests::code_ast_route_returns_arrow_payload_when_accept_prefers_arrow`
  - `gateway::studio::router::handlers::analysis::markdown::tests::markdown_route_returns_arrow_payload_when_accept_prefers_arrow`
  - `direnv exec . git diff --check`
  - `uv run python examples/...`
- Recorded one concrete failure mode that should not be hidden:
  - plain `python examples/local_rerank_workflow.py` from the package
    directory raised `ModuleNotFoundError` until the package environment was
    provided by `uv run`
- Corrected the public docs so they no longer imply bare `python examples/...`
  is the default beta path:
  - `packages/python/xiuxian-wendao-analyzer/README.md`
  - `packages/python/xiuxian-wendao-analyzer/docs/external_consumer_checklist.md`
- Expanded the doc guard in:
  - `packages/python/xiuxian-wendao-analyzer/tests/test_docs.py`
  - so the `uv run python ...` expectation stays explicit in the beta
    documentation set
- Updated the example regression tests in:
  - `packages/python/xiuxian-wendao-analyzer/tests/test_examples.py`
  - so shipped examples now run through the same `uv run python ...`
    invocation shape used by the public beta docs
- Synced the active analyzer ExecPlan with the same dry-run finding.

## PY-F122 Host-Backed Trial Checklist

- Extended the external-consumer checklist with a copy-paste host-backed trial
  sequence for the current beta slice:
  - build `wendao_search_flight_server` and `wendao_search_seed_sample`
  - seed a temporary sample repo workspace
  - boot a real `wendao_search_flight_server`
  - run `examples/repo_search_workflow.py` against that host
- Kept the slice documentation-only:
  - no new package APIs
  - no new transport or host behavior
- Expanded the docs guard in:
  - `packages/python/xiuxian-wendao-analyzer/tests/test_docs.py`
  - so the host-backed trial sequence remains part of the external consumer
    contract

- Added a workflow-oriented support matrix to
  `packages/python/xiuxian-wendao-analyzer/README.md`.
- The guide now tells external users which high-level entrypoint to choose for:
  - built-in repo-search ranking
  - custom Python repo analyzers
  - generic host-backed queries
  - local rerank over typed rows
  - local rerank over Arrow tables
- The guide also makes one boundary explicit in operator-facing language:
  - repo-search is the real-host path
  - rerank is still local-only for analyzer-owned workflows
- Synced the active analyzer ExecPlan with that workflow-first interpretation.

## PY-F121 Analyzer Runnable Examples

- Added runnable example entrypoints under
  `packages/python/xiuxian-wendao-analyzer/examples/`:
  - `local_rerank_workflow.py`
  - `repo_search_workflow.py`
- Kept the examples honest:
  - local rerank example is fully runnable without a host
  - repo-search example is a host-backed template with `--help` smoke coverage
- Added example regression coverage in
  `packages/python/xiuxian-wendao-analyzer/tests/test_examples.py`
- Synced the README and active analyzer ExecPlan with the new example layer.

## PY-F122 Host-Backed Example Execution Smoke

- Promoted the repo-search example from `--help` coverage to a real host-backed
  execution smoke:
  - `packages/python/xiuxian-wendao-analyzer/tests/test_examples.py`
- The new example test now:
  - seeds a sample repo with the Rust seed binary
  - boots `wendao_search_flight_server`
  - runs `examples/repo_search_workflow.py`
  - asserts the script prints a real analyzed top path and rank
- The local example remains the zero-host runnable path; the repo-search
  example is now also proven executable when a real host is available.

## PY-F123 Custom Analyzer Example

- Added `packages/python/xiuxian-wendao-analyzer/examples/custom_repo_analyzer_workflow.py`
  as the runnable example for downstream custom analyzer authors.
- Added both help coverage and real-host execution coverage in
  `packages/python/xiuxian-wendao-analyzer/tests/test_examples.py`.
- This keeps the package boundary explicit in example form:
  - transport from `xiuxian-wendao-py`
  - analyzer logic from user-owned Python code

## PY-F124 First Analyzer Author Tutorial

- Added a workflow-first onboarding tutorial:
  - `packages/python/xiuxian-wendao-analyzer/docs/first_analyzer_author_tutorial.md`
- The tutorial now gives one honest path for:
  - local rerank experiments
  - host-backed repo-search with built-in ranking
  - host-backed repo-search with a custom Python analyzer
- Added a lightweight doc smoke in
  `packages/python/xiuxian-wendao-analyzer/tests/test_docs.py`
  so the tutorial keeps pointing at the currently supported workflows and
  example entrypoints.

## PY-F125 Custom Analyzer Tutorial

- Added a follow-up authoring guide:
  - `packages/python/xiuxian-wendao-analyzer/docs/write_your_first_custom_analyzer.md`
- The new guide now explains:
  - the smallest `analyze_rows(...)` contract
  - the need for a stable `rank` field
  - how to attach a custom analyzer to `run_repo_analysis(...)`
  - when to stay on the local rerank path instead
- Extended `packages/python/xiuxian-wendao-analyzer/tests/test_docs.py`
  so both tutorials stay aligned with the supported examples and workflow seams.

## PY-F126 Analyzer Docs Freeze Audit

- Performed a bounded package-docs freeze audit for `xiuxian-wendao-analyzer`.
- Recorded the intended v1 public docs surface in
  `packages/python/xiuxian-wendao-analyzer/README.md`:
  - package overview in `README.md`
  - first-stop onboarding tutorial
  - follow-up custom-analyzer tutorial
- Added a doc-set freeze assertion in
  `packages/python/xiuxian-wendao-analyzer/tests/test_docs.py`
  so the package does not silently drift into multiple overlapping onboarding
  documents without an explicit workflow reason.

## PY-F127 Analyzer Package Status Review

- Performed a package-level status review for `xiuxian-wendao-analyzer`.
- Corrected the docs posture from `planned/scaffold` to the more honest
  implemented-beta state:
  - `packages/python/xiuxian-wendao-analyzer/README.md`
  - `docs/rfcs/2026-03-31-python-wendao-analyzer-package-rfc.md`
- Recorded the current truth:
  - package exists
  - workflows exist
  - real-host repo-search validation exists
  - the package is still beta, not feature-complete
- Extended `packages/python/xiuxian-wendao-analyzer/tests/test_docs.py`
  with a small status-language guard so the README does not drift back to
  `planned` wording by accident.

## PY-F128 Analyzer Beta Readiness Audit

- Added a bounded beta-readiness section to
  `packages/python/xiuxian-wendao-analyzer/README.md`.
- The section now separates:
  - ready-now workflows
  - known gaps before broader adoption
  - the stop condition for this beta slice
- Added a matching doc regression guard in
  `packages/python/xiuxian-wendao-analyzer/tests/test_docs.py`
  so the readiness statement stays explicit as the package evolves.

## PY-F129 Analyzer Release Compatibility Policy

- Added a bounded beta compatibility policy:
  - `packages/python/xiuxian-wendao-analyzer/docs/release_and_compatibility_policy.md`
- The policy now explains:
  - what external users may rely on today
  - what they should not yet assume
  - that workflow stability matters more than helper-shape stability during beta
- Synced the README and doc tests so the policy becomes part of the intended
  public doc set.

## PY-F130 External Consumer Checklist

- Added a first-trial checklist for external consumers:
  - `packages/python/xiuxian-wendao-analyzer/docs/external_consumer_checklist.md`
- The checklist now covers:
  - minimum environment expectations
  - host-backed versus local-only prerequisites
  - known skip paths that are still unsupported
  - the recommended first success bar for external trial use
- Synced the README and doc tests so the checklist becomes part of the intended
  beta docs surface.

## Wendao: Gateway Startup CPU Hotfix

- Continued the bounded Wendao gateway startup-CPU hotfix instead of reopening
  the broader plugin-RFC track.
- Confirmed the first mitigation stays in place:
  - bootstrap `wendao.toml` apply no longer eagerly starts repo/symbol
    background indexing by default
  - eager bootstrap indexing is now opt-in through
    `XIUXIAN_WENDAO_GATEWAY_BOOTSTRAP_BACKGROUND_INDEXING`
- Landed the second observability slice:
  - `/api/notify` now reports
    `studioBootstrapBackgroundIndexingEnabled` and
    `studioBootstrapBackgroundIndexingMode`
  - `/api/notify` now also reports
    `studioBootstrapBackgroundIndexingDeferredActivationAt` and
    `studioBootstrapBackgroundIndexingDeferredActivationSource`
  - `/api/search/index/status` now reports the same fields through a
    handler-local envelope without changing the core search-plane status type
  - `/api/repo/index/status` now reports the same fields through a
    handler-local envelope without changing the core repo-index status type
  - `/api/ui/capabilities` now reports the same bootstrap-indexing mode
    directly in the UI-facing capability payload
  - `/api/search/index/status` and `/api/repo/index/status` now also expose
    `studioBootstrapBackgroundIndexingDeferredActivationAt` and
    `studioBootstrapBackgroundIndexingDeferredActivationSource`
  - the current real deferred-activation trigger is
    `symbol_index_status`; repo status mirrors that gateway-level state but
    does not implicitly start repo sync on read
  - `/api/notify`, `/api/search/index/status`, and `/api/repo/index/status`
    now all read that bootstrap-indexing telemetry from one shared
    `StudioState` helper instead of hand-reading the same fields separately
  - the shared helper now also exposes
    `studioBootstrapBackgroundIndexingDeferredActivationObserved`, and
    `/api/ui/capabilities` now reports the same summary boolean so UI clients
    do not need to infer activation state from nullable timestamp/source fields
- Updated focused tests/snapshots for the repo/search status surfaces to pin
  the new bootstrap-indexing status contract.
- Validation in this slice:
  - `direnv exec . rustfmt ...`
    -> passed on the touched files
  - `direnv exec . git diff --check`
    -> passed
  - `direnv exec . cargo test -p xiuxian-wendao symbol_index_status_records_first_deferred_bootstrap_activation --lib -- --test-threads=1`
    -> passed
  - `INSTA_UPDATE=always direnv exec . cargo test -p xiuxian-wendao gateway::studio::studio_repo_sync_api_tests::repo_index_status_endpoint_ --lib -- --test-threads=1`
    -> passed and refreshed the two repo-index-status snapshots
  - `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate search_index_status_endpoint_returns_idle_corpora_snapshot -- --test-threads=1`
    -> passed
  - `direnv exec . cargo test -p xiuxian-wendao test_notify_status_ -- --test-threads=1`
    -> passed
  - `direnv exec . cargo test -p xiuxian-wendao bootstrap_background_indexing_telemetry_reports_default_deferred_state --lib -- --test-threads=1`
    -> passed
  - `direnv exec . cargo test -p xiuxian-wendao ui_capabilities_reports_builtin_plugin_languages --lib -- --test-threads=1`
    -> passed

## Wendao: Pure Arrow Flight Boundary

- Opened a new governing ExecPlan:
  - `.cache/codex/execplans/wendao_pure_arrow_flight_boundary.md`
- Recorded an explicit operator boundary override against the older
  `Flight-first + Arrow IPC fallback` target.
- The new governed boundary is:
  - high-performance-first `Arrow Flight` is the formal business surface
  - `JSON` is the formal control surface for process liveness/bootstrap,
    operator config/status/control, and static manifest/artifact inspection
  - the retired HTTP Arrow path and local-process IPC are tracked only in the
    retirement debt ledger, not as a third formal surface
- Recorded the first Stage-A classification snapshot:
  - `Flight` business surface includes Studio search, analysis, docs,

## Wendao: First Concrete Studio Intent Flight Provider

- Continued the pure-Flight boundary line with one bounded materialization
  slice instead of opening HTTP handler cutover.
- Landed the first concrete Studio-backed semantic search provider in:
  - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/search/handlers/knowledge/intent/flight.rs`
- The new `StudioIntentSearchFlightRouteProvider` now:
  - implements the runtime-owned `SearchFlightRouteProvider` seam
  - serves only `SEARCH_INTENT_ROUTE = /search/intent`
  - builds a Studio-owned Arrow batch shaped like the current intent
    `hits-arrow` payload
  - proves one repo-backed code-biased query path through
    `lang:julia reexport`
- Continued the same slice with the first local IPC-mirror alignment:
  - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/search/handlers/knowledge/intent/arrow.rs`
  - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/search/handlers/knowledge/intent/entry.rs`
  - `/api/search/intent/hits-arrow` now materializes through the provider-backed
    Flight batch seam instead of hand-building a separate Arrow IPC payload
  - the Arrow-accepting `/api/search/intent` branch now uses the same seam
  - both routes now report internal `flight_contract` plus
    `/search/intent` metadata while still staying outwardly on the current
    browser Arrow IPC surface
- Kept the scope bounded and honest:
  - no HTTP handler cutover yet
  - no active binary wiring yet
  - broad re-exports were intentionally removed again because this slice does
    not yet have a real consumer outside the local module
- Validation in this slice:
  - `direnv exec . cargo test -p xiuxian-wendao studio_intent_flight_provider_reads_repo_backed_hits --lib --features julia -- --test-threads=1`
    -> passed
  - `direnv exec . cargo test -p xiuxian-wendao search_intent_hits_arrow_reports_internal_flight_route --lib --features julia -- --test-threads=1`
    -> passed
  - `direnv exec . cargo test -p xiuxian-wendao search_hit_batch_arrow_response_reports_intent_flight_headers --lib --features julia -- --test-threads=1`
    -> passed
  - `direnv exec . cargo test -p xiuxian-wendao studio_intent_flight_provider_rejects_non_intent_routes --lib --features julia -- --test-threads=1`
    -> passed
  - `direnv exec . git diff --check`
    -> passed

## Wendao: Symbol Flight Bridge

- Continued the same pure-Flight materialization line with the next bounded
  semantic search family instead of opening a broader handler cutover.
- Landed the next Studio-backed provider-backed bridge in:
  - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/search/handlers/symbols.rs`
- The new `StudioSymbolSearchFlightRouteProvider` now:
  - implements the runtime-owned `SearchFlightRouteProvider` seam
  - serves only `SEARCH_SYMBOLS_ROUTE = /search/symbols`
  - builds a Studio-owned Arrow batch shaped like the current symbol
    `hits-arrow` payload
- Continued the same slice with the local IPC mirror alignment:
  - `/api/search/symbols/hits-arrow` now materializes through the
    provider-backed Flight batch seam instead of hand-building a separate
    Arrow IPC payload
  - the route now reports internal `flight_contract` plus
    `/search/symbols` metadata while still remaining outwardly on the current
    browser Arrow IPC surface
- Kept the scope bounded and honest:
  - no active Flight binary wiring yet
  - no retirement of the browser-facing IPC route yet
  - `attachments` was intentionally not folded into this slice because its
    current query contract still carries extra filters beyond the generic
    `query + limit` Flight provider seam
- Validation in this slice:
  - `direnv exec . cargo test -p xiuxian-wendao search_symbol_batch_arrow_response_reports_symbol_flight_headers --lib --features julia -- --test-threads=1`
    -> passed
  - `direnv exec . cargo test -p xiuxian-wendao studio_symbol_flight_provider_rejects_non_symbol_routes --lib --features julia -- --test-threads=1`
    -> passed
  - `direnv exec . cargo test -p xiuxian-wendao search_symbols_hits_arrow_reports_internal_flight_route --lib --features julia -- --test-threads=1`
    -> passed
  - `direnv exec . cargo test -p xiuxian-wendao search_symbols_returns_payload --lib --features julia -- --test-threads=1`
    -> passed

## Wendao: AST Flight Bridge

- Continued the same pure-Flight materialization line with the next bounded
  semantic search family instead of opening a broader handler cutover.
- Landed the next Studio-backed provider-backed bridge in:
  - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/search/handlers/ast.rs`
- The new `StudioAstSearchFlightRouteProvider` now:
  - implements the runtime-owned `SearchFlightRouteProvider` seam
  - serves only `SEARCH_AST_ROUTE = /search/ast`
  - builds a Studio-owned Arrow batch shaped like the current AST
    `hits-arrow` payload
- Continued the same slice with the local IPC mirror alignment:
  - `/api/search/ast/hits-arrow` now materializes through the provider-backed
    Flight batch seam instead of hand-building a separate Arrow IPC payload
  - the route now reports internal `flight_contract` plus `/search/ast`
    metadata while still remaining outwardly on the current browser Arrow IPC
    surface
- Kept the scope bounded and honest:
  - no active Flight binary wiring yet
  - no retirement of the browser-facing IPC route yet
  - touched-scope import drift was also cleaned up by replacing
    `super::super::project_scope::...` with the crate-owned path
- Validation in this slice:
  - `direnv exec . cargo test -p xiuxian-wendao search_ast_batch_arrow_response_reports_ast_flight_headers --lib --features julia -- --test-threads=1`
    -> passed
  - `direnv exec . cargo test -p xiuxian-wendao studio_ast_flight_provider_rejects_non_ast_routes --lib --features julia -- --test-threads=1`
    -> passed
  - `direnv exec . cargo test -p xiuxian-wendao search_ast_hits_arrow_reports_internal_flight_route --lib --features julia -- --test-threads=1`
    -> passed
  - `direnv exec . cargo test -p xiuxian-wendao search_ast_returns_payload --lib --features julia -- --test-threads=1`
    -> passed

## Wendao: Gateway Flight-Backed Search Bridge

- Continued the bounded browser-search transport line without expanding into a
  browser Flight client migration.
- Re-adapted the Studio intent Arrow IPC entrypoint after the shared
  `query_contract` route rename:
  - retained the legacy `/api/search/intent/hits-arrow` route as transitional
    IPC debt
  - kept `/api/search/intent` as the negotiated JSON-or-Arrow entrypoint
  - dispatches to Arrow IPC when the caller sends
    `Accept: application/vnd.apache.arrow.stream`
  - keeps the existing JSON response as the default for browser callers that
    do not request Arrow IPC explicitly
- Landed one concrete internal bridge seam in:
  - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/search/handlers/code_search/search/repo_search.rs`
- The bridge boundary is explicit:
  - browser-facing `intent/hits-arrow` remains on the retired browser HTTP
    Arrow transport
  - repo-content retrieval inside that route now flows through the runtime
    `RepoSearchFlightRouteProvider` query contract
  - the gateway decodes the returned repo-search Arrow batch back into
    `SearchHit` rows before the existing HTTP Arrow IPC response layer
- Added focused regression coverage proving the Flight-backed repo-content path
  still preserves the Studio-facing search contract:
  - file doc type
  - repo-code navigation target
  - `kind:file` and `lang:julia` tags
  - intent hybrid inclusion for debug lookup
- Added bounded observability on the same browser Arrow route:
  - `/api/search/intent/hits-arrow` now returns
    `x-wendao-browser-search-transport` with the retired browser HTTP Arrow
    transport marker
  - the same route now returns
    `x-wendao-internal-repo-content-transport: flight_contract` whenever the
    repo-content lane participates in the intent merge
  - the transport metadata is additive and route-local; it does not change the
    browser Arrow schema
- Synced the milestone plan:
  - `.cache/codex/execplans/wendao_gateway_flight_backed_search_bridge.md`
- Validation in this slice:
  - `direnv exec . cargo test -p xiuxian-wendao repo_content_search_hits_find_matching_julia_source_lines --lib`
    -> passed
  - `direnv exec . cargo test -p xiuxian-wendao load_intent_search_response_reports_repo_content_flight_transport_metadata --lib`
    -> passed
  - `direnv exec . cargo test -p xiuxian-wendao search_hit_arrow_roundtrip_preserves_nested_fields --lib`
    -> passed
  - `direnv exec . cargo test -p xiuxian-wendao build_intent_search_response_includes_repo_content_hits_for_debug_lookup --lib`
    -> passed
  - `direnv exec . cargo test -p xiuxian-wendao build_intent_search_response_includes_repo_entity_hits_for_debug_lookup --lib`
    -> passed
  - `direnv exec . git diff --check`
    -> passed
    planner, repo query/projection, graph, and VFS families
  - `JSON` control surface includes `/api/notify`, `/api/ui/config`,
    `/api/ui/capabilities`, plugin artifact inspection, and repo/search index
    control-status endpoints
  - Arrow IPC mirrors and runtime fallback transports now live only in the
    retirement debt ledger
- Recorded the first retirement-ledger mapping:
  - current concrete Flight query-contract routes already live today are
    `/search/repos/main` and `/rerank`
  - current Studio Arrow IPC mirror families
    `/api/search/*/hits-arrow` and `/api/analysis/*/retrieval-arrow` do not
    yet have one-for-one Flight replacements
  - next governed move is to define canonical Flight route families for
    search-hit and analysis-retrieval business surfaces before IPC retirement
- Landed that next contract slice:
  - `xiuxian-wendao-runtime/src/transport/query_contract.rs` now declares
    canonical Flight route constants for the current search-hit and
    analysis-retrieval families
  - those route identities are semantic business paths like `/search/ast` and
    `/analysis/markdown`, not new `.../hits` or `.../retrieval` HTTP-style
    suffixes
  - rerank was audited in the same pass and normalized to `/rerank`, so it no
    longer carries transport semantics either
  - at that point those new routes were still contract-only; only
    `/search/repos/main` and `/rerank` were materially backed by live runtime
    service seams
  - IPC retirement is still blocked on service materialization and handler
    cutover, not on route naming anymore
- Landed the first bounded service-materialization slice:
  - `xiuxian-wendao-runtime/src/transport/server.rs` now owns
    `SearchFlightRouteProvider` plus
    `WendaoFlightService::new_with_route_providers(...)` for the semantic
    `/search/*` and `/analysis/*` route family
  - the shared runtime metadata contract for that family is now
    `x-wendao-search-query` plus `x-wendao-search-limit`
  - focused runtime regressions now prove stable search-family metadata
    validation, provider-backed `get_flight_info`, provider-backed `do_get`,
    and the explicit unconfigured-route failure path
  - these routes are no longer contract-only inside the runtime service layer,
    but gateway/provider wiring and Studio handler cutover are still open, so
    IPC retirement remains blocked on consumer materialization
- Synced the same boundary note into:
  - `docs/rfcs/2026-03-27-wendao-arrow-plugin-flight-rfc.md`
  - `docs/rfcs/2026-03-27-wendao-core-runtime-plugin-migration-rfc.md`
  - `packages/rust/crates/xiuxian-wendao/docs/06_roadmap/409_core_runtime_plugin_surface_inventory.md`
  - `packages/rust/crates/xiuxian-wendao/docs/06_roadmap/412_core_runtime_plugin_program.md`
- Validation:
  - `direnv exec . git diff --check`
    -> pending final run

## Frontend: MarkdownWaterfall Slot-Level Lazy Split

- Opened a dedicated milestone ExecPlan:
  - `.cache/codex/execplans/frontend_markdown_waterfall_slot_lazy_split.md`
- Verified physical reality before implementation:
  - `MarkdownWaterfall.tsx` still rendered section bodies through a synchronous `ReactMarkdown` path
  - the heaviest section-body runtime still coupled `ReactMarkdown`, remark/rehype plugins, Mermaid rendering, and code-slot highlighting into the same waterfall module
  - `MarkdownWaterfall.test.tsx` still assumed synchronous section-body hydration
- Landed the slot-level lazy split:
  - added `.data/wendao-frontend/src/components/panels/DirectReader/MarkdownWaterfallSectionBody.tsx`
  - rewired `.data/wendao-frontend/src/components/panels/DirectReader/MarkdownWaterfall.tsx`
    to lazy-load section-body markdown rendering with `Suspense`
  - kept the identity card, section chrome, chunk metadata row, and section actions in the eager waterfall shell
  - updated focused `MarkdownWaterfall` tests to wait for lazy section-body hydration
- Validation:
  - `direnv exec . npm --prefix .data/wendao-frontend test -- --run src/components/panels/DirectReader/MarkdownWaterfall.test.tsx src/components/panels/DirectReader/DirectReader.test.tsx src/components/panels/MainView/MainViewContentPanel.test.tsx src/components/panels/MainView/MainView.test.tsx src/build/rspackChunkPolicy.test.ts src/App.test.tsx`
    -> passed
  - `direnv exec . npm --prefix .data/wendao-frontend run build`
    -> passed
  - build audit result:
    - `Initial entry assets: react.js, vendors.js, main.js, vendors.css, main.css`
    - `Entrypoint total: 0.481 MiB / 3.624 MiB`
    - the section-body lazy seam did not regress the improved initial entrypoint budget
  - `direnv exec . git diff --check`
    -> pending final run

## Frontend: MarkdownWaterfall Code-Slot Renderer Split

- Opened a dedicated milestone ExecPlan:
  - `.cache/codex/execplans/frontend_markdown_waterfall_slot_renderer_split.md`
- Verified physical reality before implementation:
  - `markdownWaterfallComponents.tsx` still rendered rich code slots inline
  - the code-slot path still coupled slot chrome and `CodeSyntaxHighlighter` hydration
  - `DirectReader.test.tsx` and `MarkdownWaterfall.test.tsx` still assumed immediate code-slot availability after section-body hydration
- Landed the code-slot renderer split:
  - added `.data/wendao-frontend/src/components/panels/DirectReader/MarkdownWaterfallCodeSlot.tsx`
  - rewired `.data/wendao-frontend/src/components/panels/DirectReader/markdownWaterfallComponents.tsx`
    so code slots mount through a nested lazy seam with a bounded slot fallback
  - updated focused reader/waterfall tests to wait for code-slot hydration
- Validation:
  - `direnv exec . npm --prefix .data/wendao-frontend test -- --run src/components/panels/DirectReader/MarkdownWaterfall.test.tsx src/components/panels/DirectReader/DirectReader.test.tsx src/components/panels/MainView/MainViewContentPanel.test.tsx src/components/panels/MainView/MainView.test.tsx src/build/rspackChunkPolicy.test.ts src/App.test.tsx`
    -> passed
  - `direnv exec . npm --prefix .data/wendao-frontend run build`
    -> passed
  - build audit result:
    - `Initial entry assets: react.js, vendors.js, main.js, vendors.css, main.css`
    - `Entrypoint total: 0.481 MiB / 3.624 MiB`
    - the nested code-slot seam did not regress the improved initial entrypoint budget
  - `direnv exec . git diff --check`
    -> pending final run

## Frontend: Markdown Bundle Audit Checkpoint

- Opened a dedicated milestone ExecPlan:
  - `.cache/codex/execplans/frontend_markdown_bundle_audit_checkpoint.md`
- Verified the current build/report surface before the audit:
  - frontend production builds emit directly into `.data/wendao-frontend/dist`
  - the existing build budget script checks initial-entry totals but does not itself explain async chunk ownership
  - the current chunk policy already names `markdown-core`, `mermaid`, `katex`, and `shiki-core`
- Re-ran the production build and inspected the emitted assets:
  - `mermaid.js` is the largest remaining markdown-rich async asset at `1672216` bytes
  - `shiki-core.js` is `705648` bytes, with language packs split into separate async assets
  - `katex.js` is `261440` bytes
  - `MarkdownWaterfallCodeSlot` now emits as its own small async chunk `757.js`
  - source-map inspection confirms:
    - `MarkdownWaterfallCodeSlot.tsx` -> `757.js`
    - DiagramWindow Mermaid runtime remains a separate path in `149.js`
- Audit conclusion:
  - the current nested seams are real in the emitted bundle
  - the next bundle-reduction slice should target Mermaid-specific runtime, not more Shiki splitting
- Validation:
  - `direnv exec . npm --prefix .data/wendao-frontend run build`
    -> passed
  - emitted asset inspection:
    - `find .data/wendao-frontend/dist -maxdepth 1 -name '*.js' -type f -exec stat -f '%z %N' {} \\; | sort -nr | sed -n '1,20p'`
    - `rg -n "DirectReaderRichContent|MarkdownWaterfallSectionBody|MarkdownWaterfallCodeSlot|MarkdownWaterfall|beautiful-mermaid|CodeSyntaxHighlighter" .data/wendao-frontend/dist/*.map`
  - `direnv exec . git diff --check`
    -> pending final run

## Frontend: Mermaid Runtime Ownership Split

- Opened a dedicated milestone ExecPlan:
  - `.cache/codex/execplans/frontend_mermaid_runtime_ownership_split.md`
- Verified physical reality before implementation:
  - `markdownWaterfallComponents.tsx` still owned a static
    `beautiful-mermaid` import and inline Mermaid slot rendering
  - DiagramWindow already consumed Mermaid through its own dynamic loader path
  - focused waterfall/reader tests still assumed Mermaid slots hydrated with
    the rest of the markdown section body
- Landed the bounded Mermaid split:
  - added `.data/wendao-frontend/src/components/panels/DirectReader/MarkdownWaterfallMermaidSlot.tsx`
  - rewired `.data/wendao-frontend/src/components/panels/DirectReader/markdownWaterfallComponents.tsx`
    so Mermaid slots mount through a dedicated nested lazy seam with a bounded
    slot fallback
  - kept DiagramWindow semantics unchanged in this milestone to avoid widening
    the runtime ownership change
- Validation:
  - `direnv exec . npm --prefix .data/wendao-frontend test -- --run src/components/panels/DiagramWindow/__tests__/DiagramWindow.test.tsx src/components/panels/DirectReader/MarkdownWaterfall.test.tsx src/components/panels/DirectReader/DirectReader.test.tsx src/components/panels/MainView/MainViewContentPanel.test.tsx src/components/panels/MainView/MainView.test.tsx src/App.test.tsx`
    -> passed
  - `direnv exec . npm --prefix .data/wendao-frontend run build`
    -> passed
  - emitted asset inspection result:
    - `MarkdownWaterfallMermaidSlot.tsx` now lands behind its own async chunk
    - `mermaid.js` remains the dominant markdown-rich async asset at `1672216`
      bytes
    - the initial entrypoint remains `0.481 MiB / 3.624 MiB`
  - `direnv exec . git diff --check`
    -> pending final run

## WENDAOARROW Flight App-Metadata Carrier Sink

- Added upstream `Arrow.Flight.withappmetadata(...)` so batch-wise Flight
  `app_metadata` can travel with a source/partitioner directly instead of
  requiring every caller to thread `app_metadata=...` through
  `flightdata(...)`, `putflightdata!(...)`, or source-based `doexchange(...)`.
- Rewired `WendaoArrow` to consume that upstream carrier directly. The
  package-local `flight_response(...; app_metadata=...)` surface now remains
  only as a thin compatibility alias; the runtime no longer owns a dedicated
  `FlightResponse` wrapper.
- Rewired the packaged response-`app_metadata` example and local Flight proof
  to use the upstream carrier directly.

## Verification

- `direnv exec . julia --project=.data/arrow-julia/test -e 'using Pkg; Pkg.instantiate(); using Test, Arrow, Tables, gRPCClient; include(".data/arrow-julia/test/flight/ipc_conversion.jl")'` -> passed
- `direnv exec . julia --project=.data/arrow-julia/test -e 'using Pkg; Pkg.instantiate(); using Test, Arrow, Tables, Dates, UUIDs, gRPCClient; include(".data/arrow-julia/test/flight.jl")'` -> passed
- `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/runtests.jl` -> passed
- `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/flight_grpcserver.jl` -> passed

## Frontend: Mermaid Runtime Shared Loader

- Opened a dedicated milestone ExecPlan:
  - `.cache/codex/execplans/frontend_mermaid_runtime_shared_loader.md`
- Verified physical reality before implementation:
  - `MarkdownWaterfallMermaidSlot.tsx` still statically imported
    `beautiful-mermaid`
  - `DirectReaderRichContent.tsx` still statically imported
    `beautiful-mermaid`
  - `DiagramWindow` already used a dynamic Mermaid loader, but through its own
    panel-local hook
- Landed the shared-loader consolidation:
  - added `.data/wendao-frontend/src/components/panels/mermaidRuntime/runtime.ts`
  - added `.data/wendao-frontend/src/components/panels/mermaidRuntime/useSharedMermaidRenderer.ts`
  - added `.data/wendao-frontend/src/components/panels/mermaidRuntime/index.ts`
  - rewired DiagramWindow Mermaid loading through the shared loader surface
  - rewired `MarkdownWaterfallMermaidSlot.tsx` to async-load the shared runtime
    instead of statically importing it
  - rewired `DirectReaderRichContent.tsx` to async-load the shared runtime for
    fenced Mermaid blocks instead of statically importing it
- Validation:
  - `direnv exec . npm --prefix .data/wendao-frontend test -- --run src/components/panels/DiagramWindow/__tests__/DiagramWindow.test.tsx src/components/panels/DirectReader/MarkdownWaterfall.test.tsx src/components/panels/DirectReader/DirectReader.test.tsx src/components/panels/MainView/MainViewContentPanel.test.tsx src/components/panels/MainView/MainView.test.tsx src/App.test.tsx`
    -> passed
  - `direnv exec . npm --prefix .data/wendao-frontend run build`
    -> passed
  - emitted asset/source audit result:
    - `mermaid.js` remains the dominant markdown-rich async asset at `1672216`
      bytes
    - `rg -n "beautiful-mermaid" .data/wendao-frontend/src/components/panels`
      now only reports the shared `panels/mermaidRuntime/runtime.ts` surface
    - the initial entrypoint remains `0.481 MiB / 3.624 MiB`
  - `direnv exec . git diff --check`
    -> pending final run

## Frontend: Mermaid Preflight Gate

- Opened a dedicated milestone ExecPlan:
  - `.cache/codex/execplans/frontend_mermaid_preflight_gate.md`
- Verified physical reality before implementation:
  - the shared Mermaid loader still activated on any Mermaid presence, even for
    explicit dialects we do not currently intend to render inline
  - DiagramWindow, DirectReader rich markdown, and MarkdownWaterfall Mermaid
    slots all depended on the same loader but had no shared dialect gate
- Landed the preflight gate:
  - added `.data/wendao-frontend/src/components/panels/mermaidRuntime/analysis.ts`
    with Mermaid dialect classification
  - gated runtime activation in DiagramWindow, DirectReader, and
    MarkdownWaterfall to the currently supported inline dialects
  - explicit `sequenceDiagram`, `classDiagram`, `erDiagram`, and `xychart`
    sources now fall back directly to source/error UI instead of a loading
    placeholder
  - extended DiagramWindow and DirectReader/Waterfall copy surfaces with an
    explicit unsupported-dialect message
- Validation:
  - `direnv exec . npm --prefix .data/wendao-frontend test -- --run src/components/panels/DiagramWindow/__tests__/mermaidRenderResults.test.ts src/components/panels/DiagramWindow/__tests__/DiagramWindow.test.tsx src/components/panels/DirectReader/MarkdownWaterfall.test.tsx src/components/panels/DirectReader/DirectReader.test.tsx src/components/panels/MainView/MainViewContentPanel.test.tsx src/components/panels/MainView/MainView.test.tsx src/App.test.tsx`
    -> passed
  - `direnv exec . npm --prefix .data/wendao-frontend run build`
    -> passed
  - source audit result:
    - `rg -n "beautiful-mermaid" .data/wendao-frontend/src/components/panels`
      still only reports the shared runtime module plus test mocks/imports
    - explicit unsupported Mermaid dialects now fail fast without activating
      the inline runtime path
    - the initial entrypoint remains `0.481 MiB / 3.624 MiB`
  - `direnv exec . git diff --check`
    -> passed

## Frontend: Mermaid Provider Adapter And Telemetry

- Opened a dedicated milestone ExecPlan:
  - `.cache/codex/execplans/frontend_mermaid_provider_adapter_and_telemetry.md`
- Verified physical reality before implementation:
  - `panels/mermaidRuntime/runtime.ts` still exposed a loader contract shaped
    directly like `beautiful-mermaid`
  - consumer hooks already converged on the shared runtime surface, so the
    provider seam could be extracted locally without widening panel APIs
  - build tooling still lacked a stable Mermaid-specific telemetry/report
    surface; the payload frontier only lived in ad hoc shell audits
- Landed the provider-adapter and telemetry slice:
  - added `.data/wendao-frontend/src/components/panels/mermaidRuntime/provider.ts`
    with the provider-neutral render contract
  - added `.data/wendao-frontend/src/components/panels/mermaidRuntime/providers/beautifulMermaid.ts`
    as the current library adapter
  - rewired `.data/wendao-frontend/src/components/panels/mermaidRuntime/runtime.ts`
    and `useSharedMermaidRenderer.ts` to load providers instead of a
    library-specific module
  - added Mermaid bundle telemetry helpers under
    `.data/wendao-frontend/scripts/build/mermaid-bundle-report-model.mjs` and
    `.data/wendao-frontend/scripts/build/mermaid-bundle-report-runtime.mjs`
  - added the stable CLI `.data/wendao-frontend/scripts/mermaid-bundle-report.mjs`
    plus `npm run build:mermaid-report`
- Validation:
  - `direnv exec . npm --prefix .data/wendao-frontend test -- --run src/build/mermaidBundleReport.test.ts src/build/mermaidBundleReportModelRuntime.test.ts src/build/mermaidBundleReportCli.test.ts src/build/rspackChunkPolicy.test.ts src/components/panels/DiagramWindow/__tests__/mermaidRenderResults.test.ts src/components/panels/DiagramWindow/__tests__/DiagramWindow.test.tsx src/components/panels/DirectReader/MarkdownWaterfall.test.tsx src/components/panels/DirectReader/DirectReader.test.tsx src/components/panels/MainView/MainViewContentPanel.test.tsx src/components/panels/MainView/MainView.test.tsx src/App.test.tsx`
    -> passed
  - `direnv exec . npm --prefix .data/wendao-frontend run build`
    -> passed
  - `direnv exec . npm --prefix .data/wendao-frontend run build:mermaid-report`
    -> passed
  - telemetry result:
    - `Mermaid assets: 1`
    - `Mermaid bytes: 1604102`
    - `Largest JS asset: mermaid.js (1604102)`
    - `Dominant Mermaid asset: mermaid.js (1604102)`
    - the initial entrypoint remains `0.481 MiB / 3.624 MiB`
  - `direnv exec . git diff --check`
    -> passed

## Frontend: Mermaid Provider Manifest

- Opened a dedicated milestone ExecPlan:
  - `.cache/codex/execplans/frontend_mermaid_provider_manifest.md`
- Verified physical reality before implementation:
  - the provider-neutral seam already existed, but the active provider still
    surfaced only a render function and name
  - the Mermaid report CLI measured asset sizes, but it did not yet explain
    which provider was active or why the payload frontier stayed large
  - the earlier source audit had already shown the decisive fact: current
    inline flowchart/state rendering still rides on ELK-backed layout
- Landed the provider-manifest slice:
  - extended `.data/wendao-frontend/src/components/panels/mermaidRuntime/provider.ts`
    with a typed provider manifest contract
  - added runtime manifest metadata to
    `.data/wendao-frontend/src/components/panels/mermaidRuntime/providers/beautifulMermaid.ts`
  - added the aligned build-side manifest
    `.data/wendao-frontend/scripts/build/mermaid-provider-manifest.mjs`
  - extended the Mermaid bundle report model/runtime so CLI output now prints
    provider, package, inline dialects, and payload notes ahead of byte totals
- Validation:
  - `direnv exec . npm --prefix .data/wendao-frontend test -- --run src/build/mermaidProviderManifest.test.ts src/build/mermaidBundleReport.test.ts src/build/mermaidBundleReportModelRuntime.test.ts src/build/mermaidBundleReportCli.test.ts src/components/panels/DiagramWindow/__tests__/mermaidRenderResults.test.ts src/components/panels/DirectReader/DirectReader.test.tsx`
    -> passed
  - `direnv exec . npm --prefix .data/wendao-frontend run build:mermaid-report`
    -> passed
  - telemetry result:
    - `Provider: beautiful-mermaid`
    - `Package: beautiful-mermaid`
    - `Inline dialects: flowchart, graph, state, unknown`
    - `Mermaid assets: 1`
    - `Mermaid bytes: 1604102`
    - `Largest JS asset: mermaid.js (1604102)`
    - `Dominant Mermaid asset: mermaid.js (1604102)`
    - payload note: current inline flowchart/state rendering still routes
      through ELK-backed graph layout
  - `direnv exec . git diff --check`
    -> passed

## Frontend: Mermaid Alternative Provider Spike

- Opened a dedicated milestone ExecPlan:
  - `.cache/codex/execplans/frontend_mermaid_alternative_provider_spike.md`
- Verified physical reality before implementation:
  - the runtime/provider seam was already isolated enough to host a second
    provider without touching DiagramWindow or DirectReader
  - the real unknown had shifted from wiring to coverage: whether a
    non-ELK provider could satisfy even a minimal useful subset under the same
    render contract
- Landed the bounded spike:
  - added the local provider
    `.data/wendao-frontend/src/components/panels/mermaidRuntime/providers/compactFlow.ts`
  - added compact-flow parsing/rendering under
    `.data/wendao-frontend/src/components/panels/mermaidRuntime/providers/compactFlow/`
  - extended runtime loading so providers can be selected by name while
    defaulting to `beautiful-mermaid`
  - extended the Mermaid report CLI to accept `--provider <name>` for
    manifest-level comparison output
- Validation:
  - `direnv exec . npm --prefix .data/wendao-frontend test -- --run src/build/mermaidProviderManifest.test.ts src/build/mermaidBundleReport.test.ts src/build/mermaidBundleReportModelRuntime.test.ts src/build/mermaidBundleReportCli.test.ts src/components/panels/DiagramWindow/__tests__/mermaidRenderResults.test.ts src/components/panels/DirectReader/DirectReader.test.tsx`
    -> passed
  - `direnv exec . npm --prefix .data/wendao-frontend run build:mermaid-report -- --provider compact-flow`
    -> passed
  - comparison output:
    - `Provider: compact-flow`
    - `Package: local-compact-flow`
    - `Inline dialects: flowchart, graph, state`
    - payload note: bounded spike provider, simple arrow-connected subset only
    - payload note: no ELK and no `beautiful-mermaid`
  - `direnv exec . npm --prefix .data/wendao-frontend run build`
    -> passed
  - build result:
    - `Rspack compiled successfully in 1.25 s`
    - `Entrypoint total: 0.481 MiB / 3.624 MiB`
  - `direnv exec . git diff --check`
    -> passed

## Frontend: Mermaid Provider Bakeoff Corpus

- Opened a dedicated milestone ExecPlan:
  - `.cache/codex/execplans/frontend_mermaid_provider_bakeoff_corpus.md`
- Verified physical reality before implementation:
  - the runtime/provider seam already supported named providers, so the next
    missing piece was a fixed comparison corpus rather than more wiring
  - the bounded `compact-flow` provider still lacked an explicit regression
    harness that separated “intended subset” from accidental parser behavior
- Landed the bakeoff-corpus slice:
  - added the fixed fixture corpus
    `.data/wendao-frontend/src/components/panels/mermaidRuntime/bakeoffFixtures.ts`
  - added focused provider comparison tests in
    `.data/wendao-frontend/src/build/mermaidProviderBakeoff.test.ts`
  - tightened `compact-flow` parsing so `subgraph` and `{}` decision/composite
    syntax fail explicitly instead of slipping through as generic labels
- Validation:
  - `direnv exec . npm --prefix .data/wendao-frontend test -- --run src/build/mermaidProviderBakeoff.test.ts src/build/mermaidProviderManifest.test.ts src/build/mermaidBundleReport.test.ts src/build/mermaidBundleReportModelRuntime.test.ts src/build/mermaidBundleReportCli.test.ts`
    -> passed
  - bakeoff result:
    - `beautiful-mermaid` passes the full bounded corpus
    - `compact-flow` passes the simple linear flowchart/state fixtures
    - `compact-flow` fails the `subgraph` and decision-node fixtures by design
  - `direnv exec . git diff --check`
    -> passed

## Frontend: Compact-Flow Subgraph Support

- Opened a dedicated milestone ExecPlan:
  - `.cache/codex/execplans/frontend_compact_flow_subgraph_support.md`
- Verified physical reality before implementation:
  - the bakeoff corpus had isolated `subgraph` as the highest-value next gap
    for `compact-flow`
  - the current compact-flow renderer already laid out nodes linearly, so a
    bounded group shell around member nodes was the smallest viable subgraph
    increment
- Landed the bounded subgraph slice:
  - extended `.data/wendao-frontend/src/components/panels/mermaidRuntime/providers/compactFlow/parser.ts`
    to parse single-layer flowchart `subgraph ... end`
  - extended `.data/wendao-frontend/src/components/panels/mermaidRuntime/providers/compactFlow/render.ts`
    to render a dashed group shell and label around grouped nodes
  - updated the bakeoff fixture expectation in
    `.data/wendao-frontend/src/components/panels/mermaidRuntime/bakeoffFixtures.ts`
    so `flowchart-subgraph` is now a `pass` for `compact-flow`
  - added focused provider assertions in
    `.data/wendao-frontend/src/build/mermaidCompactFlowProvider.test.ts`
- Validation:
  - `direnv exec . npm --prefix .data/wendao-frontend test -- --run src/build/mermaidCompactFlowProvider.test.ts src/build/mermaidProviderBakeoff.test.ts src/build/mermaidProviderManifest.test.ts src/build/mermaidBundleReport.test.ts src/build/mermaidBundleReportModelRuntime.test.ts src/build/mermaidBundleReportCli.test.ts`
    -> passed
  - compact-flow coverage result:
    - `subgraph` now passes in the bakeoff corpus
    - decision-node syntax still fails by design
  - `direnv exec . git diff --check`
    -> passed

## Frontend: Compact-Flow Decision Support

- Opened a dedicated milestone ExecPlan:
  - `.cache/codex/execplans/frontend_compact_flow_decision_support.md`
- Verified physical reality before implementation:
  - the bakeoff corpus had isolated decision nodes as the next remaining
    flowchart miss after subgraph support
  - the compact-flow renderer already had a bounded shape surface, so a diamond
    node was the smallest viable next increment
- Landed the bounded decision slice:
  - extended `.data/wendao-frontend/src/components/panels/mermaidRuntime/providers/compactFlow/parser.ts`
    to parse flowchart `{Decision}` node syntax while still rejecting state
    composite syntax
  - extended `.data/wendao-frontend/src/components/panels/mermaidRuntime/providers/compactFlow/render.ts`
    to render decision nodes as bounded diamond shapes
  - updated the bakeoff fixture expectation in
    `.data/wendao-frontend/src/components/panels/mermaidRuntime/bakeoffFixtures.ts`
    so `flowchart-decision` is now a `pass` for `compact-flow`
  - updated focused provider assertions in
    `.data/wendao-frontend/src/build/mermaidCompactFlowProvider.test.ts`
- Validation:
  - `direnv exec . npm --prefix .data/wendao-frontend test -- --run src/build/mermaidCompactFlowProvider.test.ts src/build/mermaidProviderBakeoff.test.ts src/build/mermaidProviderManifest.test.ts src/build/mermaidBundleReport.test.ts src/build/mermaidBundleReportModelRuntime.test.ts src/build/mermaidBundleReportCli.test.ts`
    -> passed
  - compact-flow coverage result:
    - decision-node fixtures now pass
    - state composite syntax still fails by design
  - `direnv exec . git diff --check`
    -> passed

## Frontend: Compact-Flow State Composite Support

- Opened a dedicated milestone ExecPlan:
  - `.cache/codex/execplans/frontend_compact_flow_state_composite_support.md`
- Verified physical reality before implementation:
  - the bakeoff corpus had isolated state composites as the next remaining miss
    after the flowchart subgraph and decision slices were closed
  - the compact-flow renderer already had a bounded group-shell path, so the
    smallest viable increment was parser closure for `state Name { ... }`
    without widening into nested or fully general state-machine semantics
- Landed the bounded state composite slice:
  - extended `.data/wendao-frontend/src/components/panels/mermaidRuntime/providers/compactFlow/parser.ts`
    so state composites open on `state Name {` and close on `}` through the
    same single-layer group path already used for flowchart `subgraph`
  - updated the bakeoff fixture expectation in
    `.data/wendao-frontend/src/components/panels/mermaidRuntime/bakeoffFixtures.ts`
    so `state-composite` is now a `pass` for `compact-flow`
  - updated focused provider assertions in
    `.data/wendao-frontend/src/build/mermaidCompactFlowProvider.test.ts`
    to validate the bounded group shell and expected labels for state composite
    output
- Validation:
  - `direnv exec . npm --prefix .data/wendao-frontend test -- --run src/build/mermaidCompactFlowProvider.test.ts src/build/mermaidProviderBakeoff.test.ts src/build/mermaidProviderManifest.test.ts src/build/mermaidBundleReport.test.ts src/build/mermaidBundleReportModelRuntime.test.ts src/build/mermaidBundleReportCli.test.ts`
    -> passed
  - compact-flow coverage result:
    - basic flowchart/state fixtures: pass
    - single-layer flowchart `subgraph`: pass
    - flowchart decision nodes: pass
    - single-layer state composite blocks: pass
    - remaining out-of-scope surface: nested composites and richer state-machine
      semantics
  - `direnv exec . git diff --check`
    -> passed

## WendaoArrow: Flight Transport Upstreaming

- Verified physical reality before implementation:
  - `.data/arrow-julia/src/flight/descriptors.jl` existed and the packaged
    Flight descriptor helper in `.data/WendaoArrow/src/flight.jl` had already
    become a thin alias over upstream logic
  - `.data/WendaoArrow/test/flight_grpcserver/support.jl` already contained the
    new `_start_flight_server(...)` startup-retry seam, so the remaining work
    was package-surface cleanup plus record synchronization
- Landed the transport-thinning slice:
  - added upstream `Arrow.Flight.pathdescriptor(...)` documentation in
    `.data/arrow-julia/README.md` and `.data/arrow-julia/docs/src/manual.md`
  - removed the dead packaged `flight_response(...; app_metadata=...)` alias
    from `.data/WendaoArrow/src/flight.jl` and the public export surface in
    `.data/WendaoArrow/src/WendaoArrow.jl`
  - rewrote `.data/WendaoArrow/README.md` and
    `.data/WendaoArrow/docs/01_core/wendao_arrow/architecture/transport-profiles.md`
    so response-side batch metadata now points directly at upstream
    `Arrow.Flight.withappmetadata(...)`, while `flight_descriptor(path)` is
    documented as a packaged default-path helper over upstream
    `Arrow.Flight.pathdescriptor(...)`
- Validation:
  - `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/runtests.jl`
    -> passed
  - `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/flight_grpcserver.jl`
    -> passed
  - `direnv exec . git diff --check -- .data/arrow-julia .data/WendaoArrow docs/GTD/DAILY_2026_04_01.md .cache/codex/execplans/wendaoarrow_arrow_flight_adaptation.md`
    -> passed

- Continued the native Julia Flight upstreaming slice:
  - added upstream `Arrow.Flight.exchangeservice(...)`,
    `Arrow.Flight.tableservice(...)`, and
    `Arrow.Flight.streamservice(...)` in
    `.data/arrow-julia/src/flight/server/exchange_helpers.jl`
  - added focused server-core proof in
    `.data/arrow-julia/test/flight/server_core/exchange_helper_tests.jl`
  - rewired `.data/WendaoArrow/src/flight.jl` so packaged service builders now
    reuse upstream `exchangeservice(...)` and only keep domain decode,
    schema-version validation, and descriptor-aware logging
- Validation:
  - `direnv exec . julia --project=.data/arrow-julia/test -e 'using Pkg; Pkg.instantiate(); using Test, Arrow, Tables, Dates, UUIDs, gRPCClient; include(".data/arrow-julia/test/flight.jl")'`
    -> passed
  - `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/runtests.jl`
    -> passed
  - `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/flight_grpcserver.jl`
    -> passed

- Continued the local in-process invocation slice:
  - extended the same upstream helper layer with source-based
    `Arrow.Flight.doexchange(service, context, source; ...)`,
    `Arrow.Flight.table(service, context, source; ...)`, and
    `Arrow.Flight.stream(service, context, source; ...)`
  - added local invocation proof to
    `.data/arrow-julia/test/flight/server_core/exchange_helper_tests.jl`
  - rewired `.data/WendaoArrow/test/runtests/local_flight.jl` to use the new
    upstream local invocation helpers instead of hand-built
    request/response `Channel{FlightData}` plumbing
  - deleted the now-dead `message_channel(...)` helper from
    `.data/WendaoArrow/test/runtests/support.jl`
- Validation:
  - `direnv exec . julia --project=.data/arrow-julia/test -e 'using Pkg; Pkg.instantiate(); using Test, Arrow, Tables, Dates, UUIDs, gRPCClient; include(".data/arrow-julia/test/flight.jl")'`
    -> passed
  - `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/runtests.jl`
    -> passed
  - `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/flight_grpcserver.jl`
    -> passed

- Hardened the split cross-process native Julia error-path harness:
  - updated `.data/WendaoArrow/test/flight_grpcserver/support.jl` so
    `native_julia_doexchange_failure(...)` retries once only for the narrow
    transient shape `GRPC_DEADLINE_EXCEEDED` + `"Deadline exceeded."` +
    empty trailing gRPC status/message
  - kept package/runtime semantics unchanged; the retry is test-harness-only
    and exists to stabilize the already-split native Julia error matrix
- Validation:
  - `direnv exec . julia --project=.data/WendaoArrow -e 'include(".data/WendaoArrow/test/flight_grpcserver/support.jl"); include(".data/WendaoArrow/test/flight_grpcserver/metadata_contract_errors.jl")'`
    -> passed

## Wendao: Browser Arrow Transport Contract

- Opened a dedicated milestone ExecPlan:
  - `.cache/codex/execplans/wendao_browser_arrow_transport_contract.md`
- Verified physical reality before implementation:
  - frontend search still decodes Arrow IPC through `apache-arrow` and uses
    `fetch`, not a browser Flight client
  - internal Rust and Julia surfaces already support Flight-first paths, so the
    missing piece was an explicit browser-facing contract rather than more
    transport wiring
- Landed the bounded capability-contract slice:
  - extended
    `packages/rust/crates/xiuxian-wendao/src/gateway/studio/types/config.rs`
    with `UiBrowserArrowTransport` plus
    `UiCapabilities.search_arrow_transport`
  - populated the new capability in
    `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/state/ui.rs`
    with the truthful current retired browser HTTP Arrow marker
  - updated frontend capability contracts and cache normalization in
    `.data/wendao-frontend/src/api/apiContracts.ts` and
    `.data/wendao-frontend/src/api/uiConfigTransport.ts`
- Validation:
  - `direnv exec . cargo test -p xiuxian-wendao ui_capabilities_reports_builtin_plugin_languages --lib`
    -> passed
  - `direnv exec . npm --prefix .data/wendao-frontend test -- --run src/api/apiContracts.test.ts src/api/uiConfigTransport.test.ts src/api/client.test.ts`
    -> passed
  - `direnv exec . git diff --check`
    -> passed
- Progress:
  - browser-visible Arrow transport truth is now explicit
  - current value is the retired browser HTTP Arrow marker
  - internal Flight-first runtime paths remain available, but they are not yet
    the browser search transport

## PY-F106 Typed Repo-Search Pipeline Entry Points

- Added `run_repo_search_analysis(...)` and `summarize_repo_search(...)` to
  `xiuxian-wendao-analyzer`
- This closes the symmetry gap between:
  - typed `WendaoRepoSearchRequest` callers
  - query-text repo-analysis callers
- Added local and real-host tests against `wendao_search_flight_server`
- Boundary remains unchanged:
  - repo-search stays host-backed
  - rerank stays local typed-input backed

## PY-F107 Arrow-Native Rerank Pipeline Entry Points

- Added `run_rerank_table_analysis(...)` and `summarize_rerank_table(...)` to
  `xiuxian-wendao-analyzer`
- This closes the symmetry gap between:
  - typed rerank row callers
  - Arrow-native rerank table callers
- Added local runtime coverage for the new table-level pipeline and summary
- Boundary remains unchanged:
  - rerank remains local-only
  - no new host claims were introduced

## PY-F108 Generic Host-Backed Query Pipeline Entry Points

- Added `QueryAnalysisRun`, `run_query_analysis(...)`, and `summarize_query(...)`
  to `xiuxian-wendao-analyzer`
- This closes the symmetry gap below the repo-specific helpers:
  - generic `WendaoFlightRouteQuery` callers now have the same pipeline and
    summary shape as repo-search callers
- Added local and real-host coverage against `wendao_search_flight_server`
- Boundary remains unchanged:
  - this is still substrate-backed host I/O
  - no analyzer-local Flight transport logic was introduced

## PY-F109 Direct Generic Query Summary Entry Point

- Added `summarize_query_route(...)` to `xiuxian-wendao-analyzer`
- This closes the last generic query symmetry gap:
  - generic host-backed query callers now have the same one-shot summary shape
    as repo-specific callers
- Added local and real-host coverage against `wendao_search_flight_server`
- Boundary remains unchanged:
  - still substrate-backed host I/O
  - still no analyzer-local Flight transport logic

## PY-F110 Direct Local Summary Entry Points

- Added `summarize_rows(...)` and `summarize_table(...)` to
  `xiuxian-wendao-analyzer`
- This closes the lowest-level local symmetry gap:
  - row-list callers and Arrow-table callers now have one-shot summary helpers
- Added local runtime coverage
- Boundary remains unchanged:
  - no host I/O involved
  - no new analyzer strategy semantics introduced

## PY-F111 Local Row/Table Pipeline Run Objects

- Added `RowsAnalysisRun`, `TableAnalysisRun`, `run_rows_analysis(...)`, and
  `run_table_analysis(...)` to `xiuxian-wendao-analyzer`
- This closes the last local pipeline symmetry gap:
  - row-list callers and Arrow-table callers now have typed run objects, not
    only raw helpers and summary helpers
- Added model and runtime coverage
- Boundary remains unchanged:
  - no host I/O involved
  - no new analyzer strategy semantics introduced

## PY-F112 Local Run-Object Summary Helpers

- Added `summarize_rows_analysis(...)` and `summarize_table_analysis(...)` to
  `xiuxian-wendao-analyzer`
- This closes the run-object summary gap at the lowest local layer:
  - row-list and Arrow-table run objects now have direct summary helpers
- Added local runtime coverage
- Boundary remains unchanged:
  - no host I/O involved
  - no new analyzer strategy semantics introduced

## PY-F113 Typed-Result Summary Helpers

- Added typed-result summary convenience helpers across analyzer surfaces:
  - `summarize_query_results(...)`
  - `summarize_repo_search_results(...)`
  - `summarize_repo_query_text_results(...)`
  - `summarize_rerank_result_rows(...)`
  - `summarize_rerank_table_results(...)`
- This closes the typed-result summary gap:
  - callers can stay on typed result helpers without manually dropping to
    `summarize_result_rows(...)`
- Added local runtime and transport-backed coverage
- Boundary remains unchanged:
  - no new host behavior
  - no new analyzer strategy semantics

## PY-F114 Typed-Result Summary Real-Host Coverage

- Added real-host `wendao_search_flight_server` coverage for:
  - `summarize_query_results(...)`
  - `summarize_repo_search_results(...)`
  - `summarize_repo_query_text_results(...)`
- This lifts the new typed-result summary helpers from local/mock-only proof to
  actual repo-search host coverage
- Boundary remains unchanged:
  - still repo-search only on the host-backed side
  - rerank typed-result summary helpers remain local-only

## PY-F115 Analyzer Package Export Surface Smoke

- Added `tests/test_exports.py` for `xiuxian-wendao-analyzer`
- The new smoke locks:
  - every symbol in `__all__` resolves from the package root
  - the core analyzer surface remains exported across models, runtime helpers,
    pipeline runs, and summary helpers
- This gives the package a lightweight public-surface regression guard without
  freezing internal implementation layout

## Wendao: References Flight-Backed Browser Arrow Bridge

- Landed the second bounded browser-facing Arrow bridge without changing the
  browser transport contract:
  - `/api/search/references/hits-arrow` still returns HTTP Arrow IPC
  - the gateway now materializes that payload internally through the runtime
    generic Flight search contract using `SEARCH_REFERENCES_ROUTE`
- Added one shared re-encode helper in:
  - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/search/handlers/arrow_transport.rs`
  - `build_arrow_search_ipc_from_lance_batch(...)`
- Reworked the references route in:
  - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/search/handlers/references.rs`
  - the route now uses a route-local `SearchFlightRouteProvider`
  - the resulting Lance batch is re-emitted as the existing references Arrow
    schema
  - the route now reports additive inspection headers:
    - `x-wendao-browser-search-transport` with the retired browser HTTP Arrow
      transport marker
    - `x-wendao-internal-search-transport: flight_contract`
    - `x-wendao-internal-flight-route: /search/references`
- Added focused coverage for:
  - references Arrow roundtrip preservation
  - route-level internal Flight bridge headers
  - existing JSON payload stability
- While closing the milestone, also trimmed adjacent intent-only dead entry
  points back to test/runtime ownership so the touched scope does not leave
  fresh warning debt
- Validation in this slice:
  - `direnv exec . cargo test -p xiuxian-wendao search_reference_arrow_roundtrip_preserves_navigation_target --lib`
    -> passed
  - `direnv exec . cargo test -p xiuxian-wendao search_references_hits_arrow_reports_internal_flight_bridge --lib`
    -> passed
  - `direnv exec . cargo test -p xiuxian-wendao search_references_returns_payload --lib`
    -> passed
  - `direnv exec . git diff --check`
    -> passed

## Wendao: Symbols Flight-Backed Browser Arrow Bridge

- Landed the next bounded browser-facing Arrow bridge on the symbols lane:
  - `/api/search/symbols/hits-arrow` still returns HTTP Arrow IPC to the
    browser
  - the gateway now materializes that payload internally through the runtime
    generic Flight search contract using `SEARCH_SYMBOLS_ROUTE`
- Tightened the implementation in:
  - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/search/handlers/symbols.rs`
  - removed the stale symbol-only Arrow IPC builder path left from the
    pre-bridge implementation
  - kept the browser Arrow schema unchanged
  - preserved additive inspection headers:
    - `x-wendao-browser-search-transport` with the retired browser HTTP Arrow
      transport marker
    - `x-wendao-internal-search-transport: flight_contract`
    - `x-wendao-internal-flight-route: /search/symbols`
- Added focused coverage for:
  - handler-local Arrow roundtrip preservation through the Flight-backed batch
  - route-level internal Flight bridge header reporting
  - existing JSON symbol payload stability
- Validation in this slice:
  - `direnv exec . cargo test -p xiuxian-wendao search_symbol_arrow_roundtrip_preserves_navigation_target --lib`
    -> passed
  - `direnv exec . cargo test -p xiuxian-wendao search_symbols_hits_arrow_reports_internal_flight_bridge --lib`
    -> passed
  - `direnv exec . cargo test -p xiuxian-wendao search_symbols_returns_payload --lib`
    -> passed
  - `direnv exec . git diff --check`
    -> passed

## Wendao: Search-Family Flight Fit Audit

- Audited the remaining Studio search-family routes against the current generic
  Flight search contract instead of forcing another bridge blindly.
- The current generic Flight seam remains truthfully bounded to `query + limit`.
- Audit conclusions:
  - `references` fits that seam
  - `symbols` fits that seam
  - `attachments` does not fit because it requires `ext`, `kind`, and
    `case_sensitive`
  - `ast` is not a clean generic-seam candidate because it is served from the
    local symbol-index path and would need a dedicated contract
- Synced the bounded-fit guidance into:
  - `packages/rust/crates/xiuxian-wendao/README.md`
  - `.cache/codex/execplans/wendao_gateway_search_family_flight_fit_audit.md`
- Validation in this slice:
  - `direnv exec . git diff --check`
    -> passed

## Wendao Runtime: Attachment Flight Contract

- Landed the next runtime-only milestone after the search-family fit audit:
  `/search/attachments` now has a dedicated Flight contract in
  `xiuxian-wendao-runtime` instead of being misleadingly covered by the generic
  `query + limit` search-family seam.
- Added attachment-specific Flight metadata headers for:
  - `x-wendao-attachment-search-ext-filters`
  - `x-wendao-attachment-search-kind-filters`
  - `x-wendao-attachment-search-case-sensitive`
- Added `validate_attachment_search_request(...)` in the shared query contract
  and `validate_attachment_search_request_metadata(...)` in the runtime server
  so attachment search can validate `ext`, `kind`, and `case_sensitive`
  separately from the generic search-family parser.
- Added a dedicated `AttachmentSearchFlightRouteProvider` seam to
  `WendaoFlightService` and routed `/search/attachments` through that provider.
- Removed `/search/attachments` from the generic `is_search_family_route(...)`
  matcher so the runtime contract is now truthful about what the generic
  Flight search surface actually supports.
- Synced the new runtime boundary into:
  - `.cache/codex/execplans/wendao_runtime_attachment_flight_contract.md`
  - `packages/rust/crates/xiuxian-wendao/README.md`
- Validation in this slice:
  - `direnv exec . cargo test -p xiuxian-wendao-runtime --features julia attachment_search_request_validation_accepts_stable_request --lib`
    -> passed
  - `direnv exec . cargo test -p xiuxian-wendao-runtime --features julia validate_attachment_search_request_metadata_accepts_stable_request --lib`
    -> passed
  - `direnv exec . cargo test -p xiuxian-wendao-runtime --features julia wendao_flight_service_get_flight_info_uses_attachment_search_provider --lib`
    -> passed
  - `direnv exec . cargo test -p xiuxian-wendao-runtime --features julia wendao_flight_service_rejects_unconfigured_attachment_search_route --lib`
    -> passed
  - `direnv exec . cargo check -p xiuxian-wendao-runtime --tests --features julia`
    -> passed
  - `direnv exec . git diff --check`
    -> passed

## Wendao: Intent Arrow Route Legacy Helper Removal

- Removed the remaining test-only `search_intent_hits_arrow` helper so the
  intent Arrow path now has one entry shape everywhere:
  - runtime uses `/api/search/intent`
  - tests also call `search_intent_route(...)` with
    `Accept: application/vnd.apache.arrow.stream`
- Updated the intent route regression to assert the internal Julia Flight
  bridge headers through the real route surface instead of a legacy helper
  namespace
- While validating that slice, fixed one adjacent compile blocker in
  `search/handlers/symbols.rs` by restoring the missing symbols
  Flight-batch/provider bridge so targeted intent-route tests can build again
- Validation in this slice:
  - `direnv exec . cargo test -p xiuxian-wendao --lib search_intent_route_reports_internal_flight_route_for_arrow_accept -- --test-threads=1`
    -> passed
  - `direnv exec . cargo test -p xiuxian-wendao --lib search_intent_route_returns_arrow_payload_when_accept_prefers_arrow -- --test-threads=1`
    -> passed
  - `direnv exec . cargo test -p xiuxian-wendao --lib accept_header_prefers_arrow_ipc_when_arrow_stream_is_requested -- --test-threads=1`
    -> passed

## Wendao: References And Symbols Arrow Route Consolidation

- Removed the legacy browser-facing `/api/search/references/hits-arrow` and
  `/api/search/symbols/hits-arrow` runtime mounts
- `/api/search/references` and `/api/search/symbols` now each own one outward
  entry shape:
  - JSON by default
  - HTTP Arrow IPC when `Accept: application/vnd.apache.arrow.stream`
- Removed the remaining references/symbols `*_hits_arrow` helper surface from
  runtime exports and switched the route-level regressions to call the real
  route wrappers with `Accept` headers
- Validation in this slice:
  - `direnv exec . cargo test -p xiuxian-wendao --lib search_references_route_reports_internal_flight_bridge_for_arrow_accept -- --test-threads=1`
    -> passed
  - `direnv exec . cargo test -p xiuxian-wendao --lib search_symbols_route_reports_internal_flight_bridge_for_arrow_accept -- --test-threads=1`
    -> passed
  - `direnv exec . cargo test -p xiuxian-wendao --lib search_symbols_route_reports_internal_flight_route_for_arrow_accept -- --test-threads=1`
    -> passed

## Wendao: Attachments And AST Arrow Route Consolidation

- Removed the remaining browser-facing `/api/search/attachments/hits-arrow`
  and `/api/search/ast/hits-arrow` runtime mounts
- `/api/search/attachments` and `/api/search/ast` now each own one outward
  entry shape:
  - JSON by default
  - browser Arrow IPC when `Accept: application/vnd.apache.arrow.stream`
- `attachments` now materializes its Arrow branch through the dedicated
  attachment Flight provider seam and reports internal `/search/attachments`
  bridge metadata on the real route surface
- `ast` now follows the same single-entrypoint browser contract while still
  reporting the internal Flight bridge metadata for `/search/ast`
- While validating the attachment seam cutover, restored the
  `AttachmentSearchFlightRouteProvider` runtime re-export and added a
  provider-level regression so the Studio route now consumes the dedicated
  runtime contract instead of gateway-local Arrow glue
- Validation in this slice:
  - `direnv exec . cargo test -p xiuxian-wendao --lib search_attachments_returns_payload -- --test-threads=1`
    -> passed
  - `direnv exec . cargo test -p xiuxian-wendao --lib search_attachments_route_returns_arrow_payload_when_accept_prefers_arrow -- --test-threads=1`
    -> passed
  - `direnv exec . cargo test -p xiuxian-wendao --lib studio_attachment_search_flight_provider_uses_attachment_contract -- --test-threads=1`
    -> passed
  - `direnv exec . cargo test -p xiuxian-wendao --lib search_ast_returns_payload -- --test-threads=1`
    -> passed
  - `direnv exec . cargo test -p xiuxian-wendao --lib search_ast_route_reports_internal_flight_route_for_arrow_accept -- --test-threads=1`
    -> passed

## Wendao: Aggregate Semantic Search Flight Dispatch

- Landed the first host-side aggregate semantic search Flight dispatch seam in
  `packages/rust/crates/xiuxian-wendao/src/gateway/studio/search/handlers/flight.rs`
- `StudioSearchFlightRouteProvider` now multiplexes the already-aligned
  semantic search families:
  - `/search/intent`
  - `/search/references`
  - `/search/symbols`
  - `/search/ast`
- This is a host-side aggregation seam only; active Flight binary wiring and
  browser IPC retirement are still open
- Touched visibility cleanup stayed crate-scoped:
  - `knowledge/mod.rs` now exposes `intent` as `pub(crate) mod`
  - `knowledge/intent/mod.rs` now exposes `flight` as `pub(crate) mod`
- No `super::super::...` remained in the touched aggregate-provider scope
- Validation in this slice:
  - `direnv exec . cargo test -p xiuxian-wendao gateway::studio::search::handlers::flight::tests::studio_search_flight_provider_dispatches_symbol_route --lib --features julia -- --exact --test-threads=1`
    -> passed
  - `direnv exec . cargo test -p xiuxian-wendao gateway::studio::search::handlers::flight::tests::studio_search_flight_provider_dispatches_ast_route --lib --features julia -- --exact --test-threads=1`
    -> passed
  - `direnv exec . cargo test -p xiuxian-wendao gateway::studio::search::handlers::flight::tests::studio_search_flight_provider_rejects_unknown_routes --lib --features julia -- --exact --test-threads=1`
    -> passed
  - `direnv exec . cargo test -p xiuxian-wendao gateway::studio::search::handlers::symbols::tests::studio_symbol_flight_provider_rejects_non_symbol_routes --lib --features julia -- --exact --test-threads=1`
    -> passed
  - `direnv exec . git diff --check`
    -> passed

## Wendao: Flight Search Contract Snapshot Sync

- Retired the outward HTTP business routes `/api/search/intent`,
  `/api/search/attachments`, `/api/search/references`, `/api/search/symbols`,
  and `/api/search/ast` from the semantic search surface
- Added the new contract lock
  `packages/rust/crates/xiuxian-wendao/tests/snapshots/gateway/studio/search_flight_service_route_contracts.snap`
  for:
  - `/search/intent`
  - `/search/attachments`
  - `/search/references`
  - `/search/symbols`
  - `/search/ast`
- Removed the stale HTTP-route unit, OpenAPI, and perf assumptions that still
  treated those routes as live gateway business surfaces
- Validation in this slice:
  - `INSTA_UPDATE=always direnv exec . cargo test -p xiuxian-wendao gateway::studio::search::handlers::flight::tests::build_studio_search_flight_service_snapshots_search_route_contracts --lib --features julia -- --exact --test-threads=1`
    -> passed

## Wendao: Search Flight Service Builder

- Landed the first host-side semantic search Flight service builder slice in:
  - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/search/handlers/flight.rs`
  - `packages/rust/crates/xiuxian-wendao/src/link_graph/plugin_runtime/transport/server.rs`
- `build_studio_search_flight_service_with_repo_provider(...)` now
  materializes one `WendaoFlightService` with:
  - repo-search provider
  - aggregate semantic Studio search provider
  - dedicated attachment provider
  - dedicated AST provider
- `build_search_plane_studio_flight_service(...)` now exposes the same builder
  from the host search-plane seam
- This closes the first host-side service-materialization slice, but active
  binary wiring is still open, so browser Arrow IPC retirement is still
  blocked
- Validation in this slice:
  - `direnv exec . cargo test -p xiuxian-wendao gateway::studio::search::handlers::flight::tests::build_studio_search_flight_service_wires_search_routes --lib --features julia -- --exact --test-threads=1`
    -> passed
  - `direnv exec . cargo test -p xiuxian-wendao gateway::studio::search::handlers::flight::tests::build_studio_search_flight_service_wires_attachment_routes --lib --features julia -- --exact --test-threads=1`
    -> passed
  - `direnv exec . cargo test -p xiuxian-wendao link_graph::plugin_runtime::transport::server::tests::build_search_plane_studio_flight_service_accepts_runtime_studio_providers --lib --features julia -- --exact --test-threads=1`
    -> passed
  - `direnv exec . git diff --check`
    -> passed

## Wendao: Active Search Flight Binary Wiring

- Landed the first active semantic-search Flight binary slice in:
  - `packages/rust/crates/xiuxian-wendao/src/bin/wendao_search_flight_server.rs`
  - `packages/rust/crates/xiuxian-wendao/src/link_graph/plugin_runtime/transport/server.rs`
  - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/state/lifecycle.rs`
- Added a roots-based Studio bootstrap constructor so runtime-owned builders can
  materialize Studio search providers from explicit `project_root` and
  `config_root`
- Added roots-based convenience builders:
  - `build_search_plane_studio_flight_service_for_roots(...)`
  - `build_search_plane_studio_flight_service_for_roots_with_weights(...)`
- `wendao_search_flight_server` now uses that roots-based builder, so the
  dedicated search Flight binary actively serves:
  - repo-search
  - the aligned semantic Studio search family
- This is active Flight binary wiring for search, not browser IPC retirement
  and not analysis-family activation yet
- Validation in this slice:
  - `direnv exec . cargo test -p xiuxian-wendao link_graph::plugin_runtime::transport::server::tests::build_search_plane_studio_flight_service_for_roots_accepts_runtime_studio_providers --lib --features julia -- --exact --test-threads=1`
    -> passed
  - `direnv exec . cargo check -p xiuxian-wendao --bin wendao_search_flight_server --features julia`
    -> passed with two pre-existing lib warnings in `attachments.rs` / `ast.rs`

## Wendao: Analysis Arrow Route Consolidation

- Retired the legacy `/api/analysis/markdown/retrieval-arrow` and
  `/api/analysis/code-ast/retrieval-arrow` outward routes.
- `/api/analysis/markdown` and `/api/analysis/code-ast` are now the only
  public analysis entrypoints; JSON remains default, and browser Arrow IPC is
  selected through `Accept: application/vnd.apache.arrow.stream`.
- Added route-level Arrow payload tests for both analysis handlers and aligned
  the router/export barrels to the new single-entrypoint contract.
- Added one bundled OpenAPI regression in
  `packages/rust/crates/xiuxian-wendao/src/gateway/openapi/document.rs`
  so the retired `/api/analysis/*/retrieval-arrow` paths cannot silently
  return through contract drift.
- Validation in this slice:
  - `direnv exec . cargo test -p xiuxian-wendao bundled_gateway_openapi_document_omits_legacy_analysis_retrieval_arrow_paths --lib -- --exact --test-threads=1`
    -> passed

## Wendao: Analysis Flight Provider Cutover

- Landed dedicated runtime-owned analysis Flight contracts in
  `packages/rust/crates/xiuxian-wendao-runtime/src/transport/{query_contract,server}.rs`
  for:
  - `ANALYSIS_MARKDOWN_ROUTE`
  - `ANALYSIS_CODE_AST_ROUTE`
- Added stable analysis metadata headers:
  - `x-wendao-analysis-path`
  - `x-wendao-analysis-repo`
  - `x-wendao-analysis-line`
- `WendaoFlightService::new_with_route_providers(...)` now accepts dedicated
  markdown-analysis and code-AST-analysis providers instead of incorrectly
  routing analysis through the generic `query + limit` search-family seam
- Added host-side analysis Flight providers in
  `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/analysis/flight.rs`
  and rewired `/api/analysis/markdown` plus `/api/analysis/code-ast` so their
  Arrow branches materialize through the provider-backed Flight batch path
  before being re-emitted as browser Arrow IPC compatibility payloads
- Consolidated `RetrievalChunk -> LanceRecordBatch` shaping into
  `router/retrieval_arrow.rs`, so analysis HTTP compatibility encoding and
  internal Flight serving now share one batch schema builder
- Extended the Studio Flight service builder to wire the new analysis
  providers, which closes the remaining analysis-family host-side service
  materialization gap
- Validation in this slice:
  - `direnv exec . cargo test -p xiuxian-wendao-runtime --lib wendao_flight_service_get_flight_info_uses_markdown_analysis_provider --features julia -- --test-threads=1`
    -> passed
  - `direnv exec . cargo test -p xiuxian-wendao-runtime --lib wendao_flight_service_get_flight_info_uses_code_ast_analysis_provider --features julia -- --test-threads=1`
    -> passed
  - `direnv exec . cargo test -p xiuxian-wendao-runtime --lib code_ast_analysis_request_validation_accepts_stable_request --features julia -- --test-threads=1`
    -> passed
  - `direnv exec . cargo test -q -p xiuxian-wendao --lib markdown_route_returns_arrow_payload_when_accept_prefers_arrow -- --test-threads=1`
    -> passed
  - `direnv exec . cargo test -q -p xiuxian-wendao --lib code_ast_route_returns_arrow_payload_when_accept_prefers_arrow -- --test-threads=1`
    -> passed
  - `direnv exec . cargo test -q -p xiuxian-wendao --lib build_studio_search_flight_service_wires_markdown_analysis_routes -- --test-threads=1`
    -> passed
  - `direnv exec . cargo test -q -p xiuxian-wendao --lib build_studio_search_flight_service_wires_code_ast_analysis_routes -- --test-threads=1`
    -> passed
  - `direnv exec . git diff --check`
    -> passed

## Wendao: AST Dedicated Flight Contract

- Landed the dedicated AST Flight seam in:
  - `packages/rust/crates/xiuxian-wendao-runtime/src/transport/server.rs`
  - `packages/rust/crates/xiuxian-wendao-runtime/src/transport/mod.rs`
  - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/search/handlers/ast.rs`
  - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/search/handlers/flight.rs`
- `WendaoFlightService` now owns a dedicated `AstSearchFlightRouteProvider`
  contract, and the generic `is_search_family_route(...)` matcher no longer
  claims `/search/ast`.
- The Studio AST Arrow branch continues returning the existing browser Arrow
  schema and route-local inspection headers, but its internal materialization
  now goes through the dedicated AST provider seam instead of the generic
  search-family provider.
- The host-side Studio Flight builder also now wires AST through the dedicated
  seam, keeping the aggregate builder compatible without reintroducing a
  generic AST contract.
- Validation in this slice:
  - `direnv exec . cargo test -p xiuxian-wendao-runtime --features julia wendao_flight_service_get_flight_info_uses_ast_search_provider --lib`
    -> passed
  - `direnv exec . cargo test -p xiuxian-wendao-runtime --features julia wendao_flight_service_rejects_unconfigured_ast_search_route --lib`
    -> passed
  - `direnv exec . cargo test -p xiuxian-wendao gateway::studio::search::handlers::ast::tests::studio_ast_flight_provider_materializes_ast_batches --lib -- --exact --test-threads=1`
    -> passed
  - `direnv exec . cargo test -p xiuxian-wendao gateway::studio::search::handlers::ast::tests::search_ast_batch_arrow_response_reports_ast_flight_headers --lib -- --exact --test-threads=1`
    -> passed
  - `direnv exec . cargo test -p xiuxian-wendao gateway::studio::search::handlers::flight::tests::build_studio_search_flight_service_wires_ast_routes --lib -- --exact --test-threads=1`
    -> passed
  - `direnv exec . git diff --check`
    -> passed

## PY-F123 Host-Backed Beta Smoke Script

- Added a one-shot host-backed beta smoke example:
  - `packages/python/xiuxian-wendao-analyzer/examples/host_backed_repo_search_beta_smoke.py`
- The script now owns the current honest end-to-end beta path:
  - locate or build `wendao_search_flight_server` and `wendao_search_seed_sample`
  - seed a sample repo workspace
  - boot a real `wendao_search_flight_server`
  - run `examples/repo_search_workflow.py` through `uv run python`
- Added example coverage in:
  - `packages/python/xiuxian-wendao-analyzer/tests/test_examples.py`
  - help smoke for the new script
  - integration smoke for the full host-backed path
- Synced:
  - `packages/python/xiuxian-wendao-analyzer/README.md`
  - `packages/python/xiuxian-wendao-analyzer/docs/external_consumer_checklist.md`
  - `packages/python/xiuxian-wendao-analyzer/tests/test_docs.py`

## PY-F124 Host-Backed Beta Smoke Custom Mode

- Extended the one-shot host-backed beta smoke example with:
  - `--mode built_in`
  - `--mode custom`
- The same script can now validate both external repo-search paths:
  - built-in `score_rank`
  - custom Python analyzer workflow
- Added coverage in:
  - `packages/python/xiuxian-wendao-analyzer/tests/test_examples.py`
  - help smoke now locks the new CLI mode switch
  - integration smoke now exercises `--mode custom`
- Synced:
  - `packages/python/xiuxian-wendao-analyzer/README.md`
  - `packages/python/xiuxian-wendao-analyzer/docs/external_consumer_checklist.md`
  - `packages/python/xiuxian-wendao-analyzer/tests/test_docs.py`

## PY-F125 Host-Backed Beta Smoke Keep-Workspace

- Extended the one-shot host-backed beta smoke example with:
  - `--keep-workspace`
- The smoke script now supports a more practical external debug loop:
  - seed a temporary workspace
  - run the real host-backed analyzer smoke
  - keep the workspace on disk when the caller wants to inspect it afterward
- Added coverage in:
  - `packages/python/xiuxian-wendao-analyzer/tests/test_examples.py`
  - help smoke for `--keep-workspace`
  - integration smoke proving a temporary workspace survives when requested
- Synced:
  - `packages/python/xiuxian-wendao-analyzer/README.md`
  - `packages/python/xiuxian-wendao-analyzer/docs/external_consumer_checklist.md`
  - `packages/python/xiuxian-wendao-analyzer/tests/test_docs.py`

## PY-F126 Analyzer Beta Exit Audit

- Performed a package-level beta exit audit for
  `packages/python/xiuxian-wendao-analyzer`
- Recorded the current exit reading in:
  - `packages/python/xiuxian-wendao-analyzer/README.md`
  - `packages/python/xiuxian-wendao-analyzer/docs/release_and_compatibility_policy.md`
- Kept the audit workflow-first:
  - built-in host-backed repo-search is exit-ready
  - custom host-backed repo-search is exit-ready
  - local rerank workflows are exit-ready
  - live-host analyzer rerank-input remains the main missing workflow
- Expanded `packages/python/xiuxian-wendao-analyzer/tests/test_docs.py`
  so the beta-exit reading stays part of the public docs contract

## PY-F127 Live Rerank Feasibility Proof

- Added one bounded analyzer-package feasibility proof for the live rerank
  transport seam:
  - `packages/python/xiuxian-wendao-analyzer/tests/test_transport_runtime.py`
  - `test_transport_substrate_exchanges_rerank_rows_via_runtime_wendao_flight_server(...)`
- Kept the slice honest:
  - no new analyzer API
  - no claim that analyzer-owned live-host rerank workflow is solved
  - only proved that the substrate-level `/rerank/flight` exchange already
    works against `wendao_flight_server`
- Tightened docs wording in:
  - `packages/python/xiuxian-wendao-analyzer/README.md`
  - `packages/python/xiuxian-wendao-analyzer/docs/release_and_compatibility_policy.md`
    so the blocker is now described precisely as:
  - missing analyzer-owned host-backed rerank workflow
  - not missing transport reachability

## PY-F131 Host-Backed Rerank Workflow

- Closed the analyzer-package gap above the already-proven live
  `/rerank/flight` transport route.
- Added a host-backed rerank workflow in:
  - `packages/python/xiuxian-wendao-analyzer/src/xiuxian_wendao_analyzer/runtime.py`
  - `run_rerank_exchange_analysis(...)`
  - `summarize_rerank_exchange(...)`
- Kept the boundary explicit:
  - the runtime host still owns rerank scoring
  - the analyzer package now owns a workflow-level wrapper above
    `xiuxian-wendao-py`
  - caller-supplied Python analyzer logic over rerank rows remains local-only
- Added verification at both layers:
  - monkeypatch coverage in
    `packages/python/xiuxian-wendao-analyzer/tests/test_transport_runtime.py`
  - real-host runtime coverage against `wendao_flight_server`
- Synced package docs and the active ExecPlan:
  - `packages/python/xiuxian-wendao-analyzer/README.md`
  - `packages/python/xiuxian-wendao-analyzer/docs/release_and_compatibility_policy.md`
  - `packages/python/xiuxian-wendao-analyzer/docs/external_consumer_checklist.md`
  - `.cache/codex/execplans/python_wendao_analyzer_real_host_v1.md`

## PY-F132 Host-Backed Rerank Example

- Added a runnable host-backed rerank example:
  - `packages/python/xiuxian-wendao-analyzer/examples/rerank_exchange_workflow.py`
- The example now demonstrates the honest live rerank path:
  - connect to `wendao_flight_server`
  - exchange typed rerank rows
  - print run and summary outputs from
    `run_rerank_exchange_analysis(...)`
    and `summarize_rerank_exchange(...)`
- Added example coverage in:
  - `packages/python/xiuxian-wendao-analyzer/tests/test_examples.py`
  - help smoke for the new example
  - runtime-host integration smoke against `wendao_flight_server`
- Synced:
  - `packages/python/xiuxian-wendao-analyzer/README.md`
  - `packages/python/xiuxian-wendao-analyzer/docs/external_consumer_checklist.md`
  - `packages/python/xiuxian-wendao-analyzer/tests/test_docs.py`

## PY-F133 Host-Backed Rerank Beta Smoke

- Added a one-shot host-backed rerank beta smoke:
  - `packages/python/xiuxian-wendao-analyzer/examples/host_backed_rerank_beta_smoke.py`
- The script now owns the honest end-to-end rerank beta path:
  - locate or build `wendao_flight_server`
  - boot a real runtime Flight host
  - run `examples/rerank_exchange_workflow.py` through `uv run python`
- Added example coverage in:
  - `packages/python/xiuxian-wendao-analyzer/tests/test_examples.py`
  - help smoke for the new script
  - integration smoke for the full runtime-host rerank path
- Synced:
  - `packages/python/xiuxian-wendao-analyzer/README.md`
  - `packages/python/xiuxian-wendao-analyzer/docs/external_consumer_checklist.md`
  - `packages/python/xiuxian-wendao-analyzer/tests/test_docs.py`

## PY-F134 Analyzer Beta Freeze Audit

- Performed a package-level beta freeze audit for
  `packages/python/xiuxian-wendao-analyzer`.
- Recorded the freeze reading in:
  - `packages/python/xiuxian-wendao-analyzer/README.md`
  - `packages/python/xiuxian-wendao-analyzer/docs/release_and_compatibility_policy.md`
- Kept the freeze narrow and workflow-first:
  - repo-search workflows are frozen for the current beta
  - host-backed rerank exchange workflow is frozen for the current beta
  - local rerank workflows are frozen for the current beta
  - helper-shaped convenience symbols remain non-frozen
- Added regression guards in:
  - `packages/python/xiuxian-wendao-analyzer/tests/test_docs.py`
  - `packages/python/xiuxian-wendao-analyzer/tests/test_examples.py`
    so both the public freeze reading and the shipped example set stay explicit

## Wendao Analysis Flight Response Helper Cleanup

- Continued the canonical analysis-route cleanup without widening the outward
  `/api/analysis/markdown` and `/api/analysis/code-ast` surface.
- Extracted shared Arrow IPC response shaping into:
  - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/analysis/transport.rs`
- Rebound both canonical analysis handlers to that helper:
  - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/analysis/code_ast.rs`
  - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/analysis/markdown.rs`
- Narrowed `analysis/flight.rs` back to provider loading/materialization only;
  it no longer owns route-local Arrow response assembly.
- Added focused helper coverage for the additive Flight bridge headers:
  - `analysis_arrow_response_from_batch_sets_flight_headers`
- Verified:
  - `gateway::studio::router::handlers::analysis::transport::tests::analysis_request_prefers_arrow_ipc_accepts_arrow_media_type`
  - `gateway::studio::router::handlers::analysis::transport::tests::analysis_arrow_response_from_batch_sets_flight_headers`
  - `gateway::studio::router::handlers::analysis::code_ast::tests::code_ast_route_returns_arrow_payload_when_accept_prefers_arrow`
  - `gateway::studio::router::handlers::analysis::markdown::tests::markdown_route_returns_arrow_payload_when_accept_prefers_arrow`
  - `direnv exec . git diff --check`

## Wendao Analysis Feature Folder Split

- Continued the bounded `analysis/` cleanup after the shared transport and
  response-helper milestones.
- Split the remaining mixed internals into lane-owned feature folders:
  - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/analysis/service/mod.rs`
  - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/analysis/service/markdown.rs`
  - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/analysis/service/code_ast.rs`
  - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/analysis/types/mod.rs`
  - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/analysis/types/markdown.rs`
  - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/analysis/types/code_ast.rs`
- Retired the flat mixed files:
  - `analysis/service.rs`
  - `analysis/types.rs`
- Kept the slice bounded:
  - no outward route changes
  - no request/response schema changes
  - no analysis Flight contract changes
- Verified:
  - `gateway::studio::router::handlers::analysis::code_ast::tests::code_ast_route_returns_arrow_payload_when_accept_prefers_arrow`
  - `gateway::studio::router::handlers::analysis::markdown::tests::markdown_route_returns_arrow_payload_when_accept_prefers_arrow`
  - `direnv exec . git diff --check`

## Wendao Analysis Export Barrel Cleanup

- Removed the leftover `analysis_exports.rs` forwarding hop after the
  `analysis/` feature-folder split.
- `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/mod.rs`
  now re-exports:
  - `analysis::code_ast_route`
  - `analysis::markdown_route`
    directly from the feature folder.
- Kept the slice bounded:
  - no outward router export changes
  - no route name changes
  - no request/response schema changes
- Verified:
  - `gateway::studio::router::handlers::analysis::code_ast::tests::code_ast_route_returns_arrow_payload_when_accept_prefers_arrow`
  - `gateway::studio::router::handlers::analysis::markdown::tests::markdown_route_returns_arrow_payload_when_accept_prefers_arrow`
  - `direnv exec . git diff --check`

## Wendao Capabilities Export Barrel Cleanup

- Applied the same bounded barrel cleanup pattern to the `capabilities`
  handler family.
- Removed:
  - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/capabilities_exports.rs`
- Rebound
  `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/mod.rs`
  to re-export directly from the `capabilities` feature folder while keeping
  the outward handler names unchanged:
  - `get_ui_capabilities`
  - `get_plugin_artifact`
- Kept the slice bounded:
  - no route changes
  - no capability payload changes
  - no router public-surface rename
- Verified:
  - `gateway::studio::router::tests::config::ui_capabilities_reports_builtin_plugin_languages`
  - `direnv exec . git diff --check`

## Wendao UI Config Export Barrel Cleanup

- Removed the old
  `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/ui_config_exports.rs`
  forwarding hop.
- Rebound
  `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/mod.rs`
  to re-export directly from `ui_config.rs` while keeping outward names stable:
  - `get_ui_config`
  - `set_ui_config`
- Kept the slice bounded:
  - no route changes
  - no UI config payload changes
  - no outward symbol rename
- Verified:
  - `gateway::studio::router::tests::config::ui_capabilities_reports_builtin_plugin_languages`
  - `direnv exec . git diff --check`

## Wendao VFS Export Barrel Cleanup

- Removed the old
  `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/vfs_exports.rs`
  forwarding hop.
- Rebound
  `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/mod.rs`
  to re-export directly from `vfs.rs` while keeping outward handler names
  stable:
  - `vfs_root_entries`
  - `vfs_scan`
  - `vfs_cat`
  - `vfs_resolve`
  - `vfs_entry`
- During validation, one real bundled OpenAPI drift surfaced: the checked-in
  `wendao_gateway.openapi.json` was missing the already-declared generic plugin
  artifact inspection path
  `/api/ui/plugins/{plugin_id}/artifacts/{artifact_id}`.
- Restored that missing bundled OpenAPI path so route inventory and checked-in
  contract are aligned again.
- Kept the slice bounded:
  - no VFS route changes
  - no VFS payload changes
  - no outward symbol rename
- Verified:
  - `gateway::openapi::paths::shared::tests::route_inventory_keeps_core_endpoints`
  - `gateway::openapi::document::tests::bundled_gateway_openapi_document_covers_declared_route_inventory`
  - `direnv exec . git diff --check`

## Wendao Graph Export Barrel Cleanup

- Removed the old
  `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/graph_exports.rs`
  forwarding hop.
- Rebound
  `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/mod.rs`
  to re-export directly from the `graph` feature folder while keeping outward
  handler names stable:
  - `graph_neighbors`
  - `node_neighbors`
  - `topology_3d`
- Kept the slice bounded:
  - no graph route changes
  - no graph payload changes
  - no outward symbol rename
- Verified:
  - `gateway::studio::router::tests::graph::graph_neighbors_prefers_kernel_project_docs_over_repo_root_docs`
  - `direnv exec . git diff --check`

## Wendao Active Flight Analysis Wiring

- Closed the remaining verification gap between the already-landed analysis
  provider seams and the active Flight binary path.
- Runtime `FlightService` contract coverage now explicitly pins:
  - markdown analysis metadata validation and provider dispatch
  - code-AST analysis metadata validation and provider dispatch
  - unconfigured `ANALYSIS_CODE_AST_ROUTE` rejection
- Roots-based active Flight builder coverage now explicitly pins:
  - `build_search_plane_studio_flight_service_for_roots(...)` resolves
    `/analysis/markdown`
  - `build_search_plane_studio_flight_service_for_roots(...)` resolves
    `/analysis/code-ast`
- This changes the authoritative boundary state:
  - active Flight binary wiring is now landed for both search and analysis
  - browser-facing HTTP Arrow IPC remains retirement debt
- Verified:
  - `direnv exec . cargo test -p xiuxian-wendao-runtime wendao_flight_service_get_flight_info_uses_markdown_analysis_provider --lib --features julia -- --test-threads=1`
  - `direnv exec . cargo test -p xiuxian-wendao-runtime code_ast_analysis --lib --features julia -- --test-threads=1`
  - `direnv exec . cargo test -p xiuxian-wendao build_search_plane_studio_flight_service_for_roots_accepts_markdown_analysis_routes --lib --features julia -- --test-threads=1`
  - `direnv exec . cargo test -p xiuxian-wendao build_search_plane_studio_flight_service_for_roots_accepts_code_ast_analysis_routes --lib --features julia -- --test-threads=1`

## Wendao Analysis OpenAPI Retirement Shim Alignment

- Retired the remaining HTTP analysis business surfaces:
  - `/api/analysis/markdown`
  - `/api/analysis/code-ast`
- Both routes now return `410 Gone` and point callers to the canonical Flight
  contracts:
  - `/analysis/markdown`
  - `/analysis/code-ast`
- The shim details now carry the stable Flight metadata mapping:
  - `x-wendao-analysis-path`
  - `x-wendao-analysis-repo`
  - `x-wendao-analysis-line`
- Deleted the now-dead analysis-specific browser Arrow IPC transport helper:
  - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/analysis/transport.rs`
- Updated the bundled OpenAPI artifact to describe both routes as retirement
  shims with `410` JSON error responses instead of `200` JSON/Arrow business
  payloads.
- Added or aligned bundled artifact regressions in:
  - `packages/rust/crates/xiuxian-wendao/src/gateway/openapi/document.rs`
- Verified:
  - `direnv exec . cargo test -p xiuxian-wendao --lib gateway::openapi::document::tests::bundled_gateway_openapi_document_declares_analysis_retirement_shims -- --exact --test-threads=1`
  - `direnv exec . cargo test -p xiuxian-wendao --lib gateway::openapi::document::tests::bundled_gateway_openapi_document_omits_legacy_analysis_retrieval_arrow_paths -- --exact --test-threads=1`
  - `direnv exec . cargo test -p xiuxian-wendao --lib gateway::studio::router::handlers::analysis::markdown::tests::markdown_route_returns_gone_with_flight_contract_details -- --exact --test-threads=1`
  - `direnv exec . cargo test -p xiuxian-wendao --lib gateway::studio::router::handlers::analysis::code_ast::tests::code_ast_route_returns_gone_with_flight_contract_details -- --exact --test-threads=1`
  - `direnv exec . git diff --check -- packages/rust/crates/xiuxian-wendao docs/GTD/DAILY_2026_04_01.md .cache/codex/execplans/wendao-studio-search-plane.md`

## Wendao Search OpenAPI Arrow Negotiation

- Updated the bundled gateway OpenAPI artifact so the canonical search routes
  now explicitly declare their real outward transport boundary:
  - `/api/search/intent`
  - `/api/search/attachments`
  - `/api/search/ast`
  - `/api/search/references`
  - `/api/search/symbols`
- Each route now advertises:
  - `application/json` as the default response surface
  - `application/vnd.apache.arrow.stream` as the browser Arrow IPC branch
    selected through `Accept`
- The bundled artifact now also declares the additive response headers for the
  Arrow branch:
  - `x-wendao-browser-search-transport`
  - `x-wendao-internal-search-transport`
  - `x-wendao-internal-flight-route`
  - `x-wendao-internal-repo-content-transport` on `/api/search/intent`
- Added one bundled artifact regression in:
  - `packages/rust/crates/xiuxian-wendao/src/gateway/openapi/document.rs`
- Synchronized the package README so the public docs and the checked-in
  OpenAPI artifact describe the same search-family contract.
- Verified:
  - `direnv exec . cargo test -p xiuxian-wendao --lib gateway::openapi::document::tests::bundled_gateway_openapi_document_declares_search_arrow_negotiation -- --exact --test-threads=1`
  - `direnv exec . cargo test -p xiuxian-wendao --lib gateway::openapi::document::tests::bundled_gateway_openapi_document_declares_search_arrow_bridge_headers -- --exact --test-threads=1`
  - `direnv exec . git diff --check -- packages/rust/crates/xiuxian-wendao docs/GTD/DAILY_2026_04_01.md .cache/codex/execplans/wendao-studio-search-plane.md`
- Verified:
  - `direnv exec . cargo test -p xiuxian-wendao markdown_route_ --lib -- --test-threads=1`
  - `direnv exec . cargo test -p xiuxian-wendao code_ast_route_ --lib -- --test-threads=1`
  - `direnv exec . cargo test -p xiuxian-wendao bundled_gateway_openapi_document_declares_analysis_ --lib -- --test-threads=1`
  - `direnv exec . cargo test -p xiuxian-wendao bundled_gateway_openapi_document_omits_legacy_analysis_retrieval_arrow_paths --lib -- --test-threads=1`
  - `direnv exec . git diff --check`

## Docs Export Barrel Cleanup

- Removed the final legacy docs forwarding barrel:
  - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/docs_exports.rs`
- `handlers/mod.rs` now re-exports docs handlers directly from:
  - `docs::planner`
  - `docs::projection`
- Preserved the outward `docs_*` symbol surface by aliasing projection exports
  at the top router barrel instead of keeping a second forwarding file.
- Closed the adjacent validation drift that surfaced while landing the cleanup:
  - removed a duplicated `#[test]` in `gateway/openapi/document.rs`
  - kept `/api/search/ast` out of the shared stable OpenAPI route inventory
  - deleted the dead `gateway/studio/router/handlers/analysis/types/` lane
- Verified:
  - `direnv exec . cargo test -p xiuxian-wendao gateway::openapi::paths::shared::tests::route_inventory_keeps_core_endpoints --lib -- --exact --test-threads=1`
  - `direnv exec . cargo test -p xiuxian-wendao gateway::openapi::document::tests::bundled_gateway_openapi_document_covers_declared_route_inventory --lib -- --exact --test-threads=1`
  - `direnv exec . git diff --check`

## Repo Export Barrel Cleanup

- Removed the legacy repository forwarding barrel:
  - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/repo_exports.rs`
- `handlers/mod.rs` now re-exports repository handlers directly from:
  - `handlers/repo/mod.rs`
- This keeps the outward repository symbol surface unchanged while removing one
  extra forwarding hop from the Studio router barrel.
- Verified:
  - `direnv exec . cargo test -p xiuxian-wendao gateway::openapi::paths::shared::tests::route_inventory_keeps_core_endpoints --lib -- --exact --test-threads=1`
  - `direnv exec . git diff --check`

## Gateway Handler Barrel Regression

- Added a focused regression test at:
  - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/tests/mod.rs`
- The test walks `gateway/studio/router/handlers/` and fails if any legacy
  `*_exports.rs` forwarding files reappear.
- This pins the current cleanup state instead of relying on manual review to
  notice new forwarding barrels.
- Verified:
  - `direnv exec . cargo test -p xiuxian-wendao gateway::studio::router::tests::handlers_surface_has_no_legacy_export_barrels --lib -- --exact --test-threads=1`
  - `direnv exec . git diff --check`

## Route Inventory Retirement Regression

- Added a focused negative regression test at:
  - `packages/rust/crates/xiuxian-wendao/src/gateway/openapi/paths/shared/tests.rs`
- The stable shared route inventory now explicitly rejects retired Flight-only
  HTTP business paths if they ever drift back into
  `WENDAO_GATEWAY_ROUTE_CONTRACTS`, including:
  - `/api/search/ast`
  - `/api/analysis/markdown`
  - `/api/analysis/code-ast`
- Verified:
  - `direnv exec . cargo test -p xiuxian-wendao gateway::openapi::paths::shared::tests::route_inventory_omits_retired_flight_only_http_paths --lib -- --exact --test-threads=1`
  - `direnv exec . git diff --check`

## Flight-Only Cutover For AST And Analysis

- Removed the remaining outward HTTP compatibility surfaces for:
  - `/api/search/ast`
  - `/api/analysis/markdown`
  - `/api/analysis/code-ast`
- The canonical business contracts are now Flight-only:
  - `/search/ast`
  - `/analysis/markdown`
  - `/analysis/code-ast`
- Deleted the remaining analysis route handlers:
  - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/analysis/markdown.rs`
  - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/analysis/code_ast.rs`
- Trimmed the last AST-only browser Arrow IPC test/helpers from:
  - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/search/handlers/ast.rs`
- Kept the provider-backed Flight seams intact:
  - `SEARCH_AST_ROUTE`
  - `ANALYSIS_MARKDOWN_ROUTE`
  - `ANALYSIS_CODE_AST_ROUTE`
- Corrected package docs, roadmap notes, and the bundled OpenAPI story so they
  no longer describe `410 Gone` shims as the live behavior.

## Flight-Only Cutover For Semantic Search

- Re-verified physical reality before editing:
  - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/routes.rs`
    already no longer mounted `/api/search/intent`,
    `/api/search/attachments`, `/api/search/references`, or
    `/api/search/symbols`
  - the bundled OpenAPI artifact had already dropped those four HTTP business
    paths too
- Landed the remaining synchronization slice:
  - updated
    `packages/rust/crates/xiuxian-wendao/src/gateway/openapi/paths/shared/tests.rs`
    so the stable route inventory now explicitly rejects all retired
    search-family HTTP business paths, not only AST/analysis
  - corrected
    `packages/rust/crates/xiuxian-wendao/docs/06_roadmap/409_core_runtime_plugin_surface_inventory.md`
    and
    `packages/rust/crates/xiuxian-wendao/docs/06_roadmap/412_core_runtime_plugin_program.md`
    so they no longer describe `/api/search/{intent,attachments,references,symbols}`
    as live browser Arrow IPC routes
  - corrected `.cache/codex/execplans/wendao-studio-search-plane.md` so the
    search-family cutover record now states the canonical business contracts
    are Flight `/search/*`, not HTTP single-entrypoint negotiation
- Stable semantic search business routes are now Flight-only:
  - `/search/intent`
  - `/search/knowledge`
  - `/search/attachments`
  - `/search/references`
  - `/search/symbols`
  - `/search/ast`

## UI Capabilities Search Transport Cleanup

- Removed the stale browser-search transport capability from the live Studio
  contract:
  - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/types/config.rs`
  - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/state/ui.rs`
  - `.data/wendao-frontend/src/api/apiContracts.ts`
  - `.data/wendao-frontend/src/api/uiConfigTransport.ts`
- Reason:
  - semantic search business routes are now Flight-only, so
    `/api/ui/capabilities` should not keep advertising
    `searchArrowTransport` as the retired browser HTTP Arrow transport
- Verified by updating the Rust and frontend capability tests together with the
  contract removal

## Runtime Negotiation Canonical Order Narrowing

- Narrowed the canonical runtime negotiation order in:
  - `packages/rust/crates/xiuxian-wendao-runtime/src/transport/negotiation.rs`
  - `packages/rust/crates/xiuxian-wendao/src/link_graph/plugin_runtime/tests/compat_julia.rs`
- New rule:
  - canonical runtime negotiation now advertises only
    `ArrowFlight`
  - the retired local-process IPC variant stays in the generic transport
    vocabulary, but it is no longer part of the canonical runtime negotiation
    order because this seam still cannot materialize it into a live client
- Added a focused negative regression asserting that
  local-process IPC bindings are rejected with the explicit
  materialization error instead of being treated as a negotiated candidate
- Synchronized package docs and ExecPlan so the Phase-7 runtime story no
  longer claims a three-step canonical order:
  - `packages/rust/crates/xiuxian-wendao/docs/06_roadmap/409_core_runtime_plugin_surface_inventory.md`
  - `packages/rust/crates/xiuxian-wendao/docs/06_roadmap/412_core_runtime_plugin_program.md`
  - `.cache/codex/execplans/wendao-studio-search-plane.md`

## Julia Artifact Flight Truth-Surface Sync

- Re-synchronized the remaining Julia deployment artifact truth surfaces to the
  live Flight route:
  - `packages/rust/crates/xiuxian-wendao/resources/openapi/wendao_gateway.openapi.json`
  - `packages/rust/crates/xiuxian-wendao/src/gateway/openapi/document.rs`
  - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/config/tests.rs`
  - `packages/rust/crates/xiuxian-wendao/src/analyzers/config/tests.rs`
  - `.data/wendao-frontend/src/api/client.test.ts`
  - `.data/wendao-frontend/src/components/juliaDeploymentInspection/format.ts`
  - `.data/wendao-frontend/src/components/juliaDeploymentInspection/index.test.ts`
  - `.data/wendao-frontend/src/components/StatusBar.test.tsx`
  - `packages/rust/crates/xiuxian-wendao/README.md`
  - `packages/rust/crates/xiuxian-wendao/docs/06_roadmap/409_core_runtime_plugin_surface_inventory.md`
  - `packages/rust/crates/xiuxian-wendao/docs/06_roadmap/412_core_runtime_plugin_program.md`
  - `.cache/codex/execplans/wendao-studio-search-plane.md`
- Scope:
  - OpenAPI plugin-artifact and Julia deployment-artifact examples now show
    `/rerank` and `/healthz`
  - frontend Julia deployment inspection now renders `Flight route ...`
    instead of the stale `Arrow route ...`
  - the remaining repo/config fixtures no longer seed the retired IPC route
  - package docs now match the real runtime state: canonical negotiation is
    `ArrowFlight` only, while the retired HTTP Arrow variant is retained only
    as rejected generic transport vocabulary

## WendaoArrow Flight Runtime Alignment

- Started a bounded WendaoArrow package slice to align its package-local Flight
  builders with the current upstream `Arrow.Flight` runtime contract.
- Scope is intentionally narrow:
  - request-side `app_metadata` support for local package builders
  - optional gRPCServer helper passthrough
  - local Flight regression coverage
- Physical-reality check completed before edits:
  - `.data/WendaoArrow/src/flight.jl`
  - `.data/WendaoArrow/ext/WendaoArrowgRPCServerExt.jl`
  - `.data/WendaoArrow/test/runtests/local_flight.jl`
  - `.data/WendaoArrow/test/flight_grpcserver/support.jl`
- Active ExecPlan:
  - `.cache/agent/execplans/wendaoarrow_flight_runtime_alignment.md`
- Landed the alignment slice in:
  - `.data/WendaoArrow/src/codec.jl`
  - `.data/WendaoArrow/src/flight.jl`
  - `.data/WendaoArrow/ext/WendaoArrowgRPCServerExt.jl`
  - `.data/WendaoArrow/test/runtests/support.jl`
  - `.data/WendaoArrow/test/runtests/local_flight.jl`
  - `.data/WendaoArrow/test/flight_grpcserver/service_bootstrap.jl`
  - `.data/WendaoArrow/README.md`
- Result:
  - `build_flight_service(...)` and `build_stream_flight_service(...)` now
    accept `include_request_app_metadata=true`
  - local processors receive the same upstream-native wrappers as
    `Arrow.Flight.table(...; include_app_metadata=true)` and
    `Arrow.Flight.stream(...; include_app_metadata=true)`
  - `serve_flight(...)` and `serve_stream_flight(...)` now pass the same
    keyword through the optional `gRPCServer` surface
- Verified:
  - `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/runtests.jl`
  - `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/flight_grpcserver.jl`
  - `direnv exec . git -C .data/WendaoArrow diff --check`
- Focused perf probe:
  - `.cache/agent/tmp/wendaoarrow-flight-request-app-metadata.tsv`
  - `table_plain 0.053 ms / 0.033 MiB`
  - `table_request_app 0.062 ms / 0.038 MiB`
  - `stream_plain 0.081 ms / 0.045 MiB`
  - `stream_request_app 0.072 ms / 0.051 MiB`

## Julia Flight Config Naming Closure

- Removed the remaining legacy inline repository config key from the active
  Julia transport surfaces:
  - `packages/rust/crates/xiuxian-wendao-julia/src/plugin/transport.rs`
  - `packages/rust/crates/xiuxian-wendao/src/analyzers/config/tests.rs`
  - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/config/tests.rs`
  - `packages/rust/crates/xiuxian-wendao-julia/README.md`
  - `packages/rust/crates/xiuxian-wendao/README.md`
- Repository inline plugin config is now `flight_transport = { ... }` with no
  local `arrow_transport` fallback on the Julia plugin seam.
- Replaced the stale retired HTTP Arrow mock in
  `packages/rust/crates/xiuxian-wendao/src/analyzers/service/julia_transport/tests.rs`
  with a real WendaoArrow Flight example loopback.
- The host-side Julia transport regression now proves the same production
  contract the runtime uses:
  - spawn `.data/WendaoArrow/scripts/run_stream_scoring_flight_server.sh`
  - wait for Flight socket readiness
  - call `fetch_plugin_arrow_score_rows_for_repository(...)`
  - assert the returned scores match the official WendaoArrow scoring example

## Julia Flight Public Symbol Rename

- Renamed the active transport-facing Julia plugin API away from the legacy
  `arrow_transport` naming:
  - `build_julia_arrow_transport_client` -> `build_julia_flight_transport_client`
  - `process_julia_arrow_batches` -> `process_julia_flight_batches`
  - `process_julia_arrow_batches_for_repository` -> `process_julia_flight_batches_for_repository`
  - `fetch_julia_arrow_score_rows_for_repository` -> `fetch_julia_flight_score_rows_for_repository`
- Kept Arrow contract names such as request/response schema builders and Arrow
  column constants unchanged, because those still describe the batch contract
  rather than the transport.
- Updated the standalone plugin crate, host re-export surfaces, tests, and
  package READMEs so the active public API now describes Flight directly.

## Runtime Flight Negotiation Symbol Rename

- Renamed the active runtime negotiation surface away from `ArrowTransport`
  naming on the live Flight path:
  - `NegotiatedArrowTransportClient` -> `NegotiatedFlightTransportClient`
  - `negotiate_arrow_transport_client_from_bindings(...)` ->
    `negotiate_flight_transport_client_from_bindings(...)`
- Updated the runtime crate plus the host/plugin call sites that consume the
  negotiated client:
  - `packages/rust/crates/xiuxian-wendao-runtime/src/transport/negotiation.rs`
  - `packages/rust/crates/xiuxian-wendao-runtime/src/transport/mod.rs`
  - `packages/rust/crates/xiuxian-wendao-julia/src/plugin/transport.rs`
  - `packages/rust/crates/xiuxian-wendao/src/link_graph/index/search/plan/payload/quantum/rerank.rs`
  - `packages/rust/crates/xiuxian-wendao/src/link_graph/plugin_runtime/{mod.rs,transport/mod.rs,transport/client.rs,artifacts/resolve.rs}`
- Kept the low-level `xiuxian-vector` HTTP helper names unchanged in this slice;
  this cut only touched the active runtime-owned Flight negotiation seam.

## Runtime-Owned Flight Contract Defaults

- Moved the live Julia Flight-path defaults and metadata keys into
  `xiuxian-wendao-runtime::transport`:
  - `DEFAULT_FLIGHT_BASE_URL`
  - `DEFAULT_FLIGHT_SCHEMA_VERSION`
  - `DEFAULT_FLIGHT_TIMEOUT_SECS`
  - `FLIGHT_SCHEMA_VERSION_METADATA_KEY`
  - `FLIGHT_TRACE_ID_METADATA_KEY`
- Added runtime-owned validation helpers for the active Flight path:
  - `validate_flight_schema_version(...)`
  - `validate_flight_timeout_secs(...)`
  - `resolve_flight_timeout(...)`
- Updated the active consumers to use that runtime-owned contract instead of
  `xiuxian-vector::ArrowTransportConfig` and the old
  `ARROW_TRANSPORT_*` defaults/constants:
  - `packages/rust/crates/xiuxian-wendao-runtime/src/transport/client.rs`
  - `packages/rust/crates/xiuxian-wendao-julia/src/plugin/transport.rs`
  - `packages/rust/crates/xiuxian-wendao-julia/src/plugin/test_support/contract.rs`
  - `packages/rust/crates/xiuxian-wendao/src/link_graph/index/search/plan/payload/quantum/rerank.rs`
- Left the lower `xiuxian-vector` Arrow transport helpers intentionally
  untouched as quarantined legacy utilities; they are no longer part of the
  verified runtime-owned Julia Flight path.
- Validation:
  - `CARGO_TARGET_DIR=.cache/cargo-target/codex-xiuxian-wendao-julia-flight direnv exec . cargo test -q -p xiuxian-wendao-julia transport --lib -- --test-threads=1`
  - `CARGO_TARGET_DIR=.cache/cargo-target/codex-xiuxian-wendao-julia-flight direnv exec . cargo test -q -p xiuxian-wendao-runtime --lib transport::client::tests::flight_transport_client_builder_uses_flight_defaults_for_schema_and_timeout --features julia -- --exact --test-threads=1`
  - `CARGO_TARGET_DIR=.cache/cargo-target/codex-xiuxian-wendao-julia-flight direnv exec . cargo test -q -p xiuxian-wendao --lib link_graph::index::search::plan::payload::quantum::tests::build_plugin_rerank_transport_client_accepts_arrow_flight_bindings --features julia -- --exact --test-threads=1`

## Remove Legacy Public Transport Surface

- Dropped the temporary public legacy transport namespace from
  `xiuxian-vector`; the old Arrow-over-HTTP helpers are no longer re-exported
  as any public transport surface.
- `packages/rust/crates/xiuxian-vector/src/arrow_transport/` remains only as an
  internal implementation detail used by the crate's own IPC search helper.
- `packages/rust/crates/xiuxian-vector/src/search/search_impl/ipc.rs` now goes
  back to the private `crate::arrow_transport::encode_record_batch_ipc(...)`
  path instead of routing through a public legacy namespace.
- Updated `packages/rust/crates/xiuxian-vector/README.md` so the crate no
  longer documents the temporary legacy transport namespace as an opt-in
  public module.
- Validation:
  - `CARGO_TARGET_DIR=.cache/cargo-target/codex-xiuxian-vector-internal direnv exec . cargo test -q -p xiuxian-vector arrow_transport --lib -- --test-threads=1`
  - `CARGO_TARGET_DIR=.cache/cargo-target/codex-xiuxian-vector-internal direnv exec . cargo check -q -p xiuxian-wendao --lib --features julia`

## Remove `arrow_transport` Itself

- Finished the cleanup instead of leaving a renamed shim behind:
  - deleted `packages/rust/crates/xiuxian-vector/src/arrow_transport/{client.rs,config.rs,error.rs,mod.rs,tests.rs}`
  - moved the surviving generic batch helpers into
    `packages/rust/crates/xiuxian-vector/src/arrow_codec.rs`
- `xiuxian-vector` now exposes only the neutral Arrow batch helper surface:
  - `encode_record_batch_ipc`
  - `encode_record_batches_ipc`
  - `decode_record_batches_ipc`
  - `attach_record_batch_metadata`
  - `attach_record_batch_trace_id`
- Removed the dead HTTP-transport dependency set from
  `packages/rust/crates/xiuxian-vector/Cargo.toml`:
  - `reqwest`
  - `url`
  - `toml`
  - dev-only `axum`
- Fixed the adjacent Studio search compile drift that surfaced during the
  final `cargo check` pass:
  - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/search/handlers/knowledge/intent/flight.rs`
  - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/search/handlers/knowledge/search.rs`
- Validation:
  - `CARGO_TARGET_DIR=.cache/cargo-target/codex-xiuxian-vector-clean direnv exec . cargo check -q -p xiuxian-wendao --lib --features julia`
  - `CARGO_TARGET_DIR=.cache/cargo-target/codex-xiuxian-vector-clean direnv exec . cargo test -q -p xiuxian-vector --lib -- --test-threads=1`

## Flight Truth Surface Follow-Through

- Updated the generic plugin artifact example in
  `packages/rust/crates/xiuxian-wendao-core/src/artifacts/payload.rs` so it no
  longer serializes the retired HTTP Arrow fallback story.
  The canonical sample now points at:
  - `base_url = "http://127.0.0.1:8815"`
  - `route = "/rerank"`
  - `health_route = "/healthz"`
  - `selected_transport = "arrow_flight"`
- Updated the frontend Julia deployment fixture in
  `.data/wendao-frontend/src/App.test.tsx` to the same Flight-only truth
  surface and added `selectedTransport = "arrow_flight"`.
- Updated `.data/wendao-frontend/README.md` so it no longer claims the browser
  surface still advertises the retired browser HTTP Arrow transport; the
  README now describes the active Flight inspection route instead.
- Validation:
  - `CARGO_TARGET_DIR=.cache/cargo-target/codex-flight-truth direnv exec . cargo test -q -p xiuxian-wendao-core artifacts::payload::tests::artifact_payload_serializes_to_toml_and_json --lib -- --exact --test-threads=1`
  - `direnv exec . npm --prefix .data/wendao-frontend test -- --run src/App.test.tsx`

## Flight-Only Transport Kind Contract

- Removed the retired HTTP Arrow and local-process IPC variants from
  the active public plugin transport contract:
  - `packages/rust/crates/xiuxian-wendao-core/src/transport/kind.rs`
  - `packages/rust/crates/xiuxian-wendao-runtime/src/transport/negotiation.rs`
  - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/types/config.rs`
  - `packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py/plugin.py`
- The runtime-owned plugin transport type system is now Flight-only end to
  end:
  - core `PluginTransportKind`
  - runtime negotiation surface
  - Studio outward inspection enum
  - Python scaffold mirror
- Synced the active roadmap notes in:
  - `packages/rust/crates/xiuxian-wendao/docs/06_roadmap/409_core_runtime_plugin_surface_inventory.md`
  - `packages/rust/crates/xiuxian-wendao/docs/06_roadmap/412_core_runtime_plugin_program.md`
- Validation:
  - `CARGO_TARGET_DIR=.cache/cargo-target/codex-transport-kind direnv exec . cargo test -q -p xiuxian-wendao-runtime --lib transport::negotiation::tests::canonical_transport_preference_order_is_flight_first --features julia -- --exact --test-threads=1`
  - `CARGO_TARGET_DIR=.cache/cargo-target/codex-transport-kind direnv exec . cargo test -q -p xiuxian-wendao --lib gateway::studio::types::config::tests::generic_ui_artifact_builds_from_plugin_artifact_payload --features julia -- --exact --test-threads=1`
  - `CARGO_TARGET_DIR=.cache/cargo-target/codex-transport-kind direnv exec . cargo check -q -p xiuxian-wendao --lib --features julia`

## Historical Staging Doc Alignment

- Updated
  `packages/rust/crates/xiuxian-wendao/docs/06_roadmap/410_p1_generic_plugin_contract_staging.md`
  so the remaining historical Phase-1 note no longer contradicts the current
  Flight-only codebase.
- Marked the document `SUPERSEDED` and added an explicit note pointing readers
  at the active runtime truth sources:
  - `409_core_runtime_plugin_surface_inventory.md`
  - `412_core_runtime_plugin_program.md`
- Rewrote the old staging snippets so they no longer mention retired transport
  variants or the retired HTTP Arrow route:
  - `PluginTransportKind` example is now `ArrowFlight` only
  - retrieval config examples now use `transport = "arrow_flight"`
  - example routes now use `/rerank`
- Validation:
  - `direnv exec . rg -n "<retired-flight-transport-tokens>" packages/rust/crates/xiuxian-wendao/docs/06_roadmap/409_core_runtime_plugin_surface_inventory.md packages/rust/crates/xiuxian-wendao/docs/06_roadmap/412_core_runtime_plugin_program.md packages/rust/crates/xiuxian-wendao/docs/06_roadmap/410_p1_generic_plugin_contract_staging.md packages/rust/crates/xiuxian-wendao-core packages/rust/crates/xiuxian-wendao-runtime packages/rust/crates/xiuxian-wendao packages/python/xiuxian-wendao-py .data/wendao-frontend`

## Historical Label Scrub

- Rewrote the same-day GTD references so the historical log preserves the
  migration story without carrying retired transport enum names, old browser
  transport labels, or the retired HTTP Arrow route as raw literals.
- Validation:
  - `direnv exec . rg -n "<retired-flight-transport-tokens>" docs/GTD/DAILY_2026_04_01.md .cache/codex/execplans/wendao-studio-search-plane.md`

## xiuxian-wendao-analyzer 0.1.1 Lock Boundary

- Promoted `packages/python/xiuxian-wendao-analyzer` to a lockable beta
  baseline at `0.1.1`.
- Added a package-root `__version__` export and a regression asserting that it
  matches `pyproject.toml`.
- Synced package docs so the current versioned boundary explicitly states:
  - host-backed repo-search remains on the Flight `schema_version=v2` workflow
  - host-backed rerank exchange remains on the live `/rerank/flight` seam
  - local rerank remains part of the same beta workflow surface
- Validation:
  - `direnv exec . bash -lc 'cd packages/python/xiuxian-wendao-analyzer && uv run pytest'`

## Strict VFS-Qualified Analysis Contract

- Restored the frontend analysis boundary so ZenSearch and API client calls
  keep sending project-qualified VFS addresses such as `main/docs/index.md`
  and `kernel/src/lib.rs`; the browser no longer strips project prefixes
  before calling analysis surfaces.
- Moved path normalization ownership back to the gateway:
  - markdown analysis now resolves files through the Studio VFS registry
  - code-AST analysis now normalizes `<repo>/<path>` into a repo-relative
    source path on the server while preserving the outward response path as
    the project-qualified VFS address
- Added strict regression locks:
  - `analysis_markdown_payload.snap` now snapshots the project-qualified
    markdown path and chunk-id namespace
  - `analysis_markdown_unqualified_path_error.snap` now locks the rejection of
    unqualified markdown paths
  - code-AST resolver tests now pin explicit repo-prefix stripping and
    conflicting repo-prefix rejection
- Validation:
  - `INSTA_UPDATE=always direnv exec . cargo test -p xiuxian-wendao analyze_markdown_ --lib -- --test-threads=1`
  - `direnv exec . cargo test -p xiuxian-wendao resolve_code_ast_repository_and_path_ --lib -- --test-threads=1`
  - `direnv exec . cargo check -p xiuxian-wendao --lib --features julia --message-format short`
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm test -- src/utils/__tests__/selectionPath.test.ts src/components/ZenSearch/__tests__/zenSearchPreviewLoaders.test.ts src/components/ZenSearch/__tests__/useZenSearchPreview.test.tsx src/api/client.test.ts src/api/flightAnalysisTransport.test.ts'`
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm run build'`

## WENDAO Same-Port Pure Flight Final Search Cut

- Removed the last generic HTTP knowledge-search business shell:
  - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/routes.rs`
  - `packages/rust/crates/xiuxian-wendao/src/gateway/studio/search/handlers/knowledge/search.rs`
  - `packages/rust/crates/xiuxian-wendao/src/gateway/openapi/paths/search.rs`
  - `packages/rust/crates/xiuxian-wendao/src/gateway/openapi/paths/shared/inventory/{search,mod}.rs`
  - `packages/rust/crates/xiuxian-wendao/resources/openapi/wendao_gateway.openapi.json`
- Current contract result:
  - `/api/search` is no longer part of the active gateway surface
  - `/search/knowledge` is the only active knowledge-search business contract
  - the frontend source tree no longer relies on `/api/search` or
    `/api/search/*/hits-arrow`
- Closed the touched warning front in the same slice:
  - `SearchQuery` is now test-only
  - dead references/symbols provider wrappers and batch-helper shims are gone
- Modularized the runtime Flight service out of the old monolith:
  - retired `packages/rust/crates/xiuxian-wendao-runtime/src/transport/server.rs`
  - added
    `packages/rust/crates/xiuxian-wendao-runtime/src/transport/server/{mod,types,request_metadata,service,tests}.rs`
- Fresh same-port proof on a newly built `127.0.0.1:9519` gateway:
  - `GET /api/health -> 200`
  - `POST /arrow.flight.protocol.FlightService/GetFlightInfo -> 400`
  - `GET /api/search?q=wendao&limit=1 -> 404`
  - `RUN_LIVE_GATEWAY_TEST=1 STUDIO_LIVE_GATEWAY_URL=http://127.0.0.1:9519 npm test -- src/api/liveGateway.test.ts` passed with `4/4`
  - `RUN_LIVE_GATEWAY_TEST=1 STUDIO_LIVE_GATEWAY_URL=http://127.0.0.1:9519 npm run test:live-flight-perf` passed on the current `179`-repo config
  - fresh cold-gateway perf profile:
    - overall average `46.15ms`, P95 `48.78ms`
    - `GetFlightInfo` average `44.8ms`
    - `DoGet` average `0.9ms`
- Current local process fact:
  - the running `127.0.0.1:9517` gateway still returns `200` for `/api/search`
  - conclusion: that process is stale relative to this cut and must be
    restarted before the default local stack reflects the final Flight-only
    search boundary
