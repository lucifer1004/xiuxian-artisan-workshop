# Daily GTD Log - 2026-03-30

### [DONE] PY-F57: carry repo-search title filters through the real `xiuxian-wendao` Flight host

- **Objective**: extend the typed repo-search request contract with one
  search-intent filter that is more semantic than path narrowing while still
  staying stable on the current repo-content Flight route.
- **Update**:
  - extended the Rust transport contract with
    `x-wendao-repo-search-title-filters`
  - rewired
    `packages/rust/crates/xiuxian-wendao-runtime/src/transport/server.rs`
    so repo-search metadata now parses and forwards optional title filters
  - rewired
    `packages/rust/crates/xiuxian-wendao/src/link_graph/plugin_runtime/transport/server.rs`
    so the real `SearchPlaneRepoSearchFlightRouteProvider` applies
    case-insensitive title matching against the stable title surface
    (`hit.title.unwrap_or(path)`) after backend repo-content search
  - extended the Python typed request surface in
    `packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py/transport/query.py`
    so `repo_search_request(..., title_filters=(...))` emits sorted stable
    metadata and rejects blank title filters
  - tightened the direct Python real-host smokes in
    `packages/python/xiuxian-wendao-py/tests/test_transport.py`
    so the same persisted repo corpus now proves:
    - `readme` narrows `alpha` to `README.md`
    - `flight_search` narrows `flightbridgetoken` to the matching flight row
- **Validation Executed**:
  - `direnv exec . cargo test -p xiuxian-wendao-runtime --features julia query_contract -- --nocapture`
  - `direnv exec . env CARGO_TARGET_DIR=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-f56-target cargo test -p xiuxian-wendao --features julia link_graph::plugin_runtime::transport::server::tests:: -- --nocapture`
  - `direnv exec . uv run pytest packages/python/xiuxian-wendao-py/tests/test_query.py packages/python/xiuxian-wendao-py/tests/test_transport.py -q -k "repo_search_request or fetches_repo_search_info_with_typed_request"`
  - `direnv exec . env WENDAO_SEARCH_SERVER_BINARY=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-f56-target/debug/wendao_search_flight_server WENDAO_SEARCH_SEED_BINARY=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-f56-target/debug/wendao_search_seed_sample uv run pytest packages/python/xiuxian-wendao-py/tests/test_transport.py -q -k wendao_search_flight_server`

### [DONE] Feature: stabilize Arrow.jl Enum extension support and record the Milestone 0 performance baseline

- **Objective**: finish the current Arrow.jl `Enum` slice as a standalone
  milestone so canonical type-alignment work can start from a clean,
  production-validated baseline.
- **Update**:
  - kept `Enum` support on the Julia-private `JuliaLang.Enum` extension path
    instead of inventing a canonical Arrow enum
  - added trait-layer support in
    `.data/arrow-julia/src/ArrowTypes/src/ArrowTypes.jl`
    so `@enum` values serialize through their primitive basetype while
    retaining qualified Julia type metadata for Julia-native roundtrips
  - added focused unit coverage in
    `.data/arrow-julia/src/ArrowTypes/test/tests.jl`
    and end-to-end package coverage in
    `.data/arrow-julia/test/runtests.jl`
    for typed roundtrips and `convert=false` raw storage behavior
  - synchronized package docs in
    `.data/arrow-julia/README.md`
    and `.data/arrow-julia/docs/src/manual.md`
  - archived the post-Enum write-path and throughput baselines under
    `.cache/agent/tmp/`
- **Validation Executed**:
  - `direnv exec . julia --project=.data/arrow-julia/src/ArrowTypes -e 'using Pkg; Pkg.test()'`
  - `direnv exec . julia --project=.data/arrow-julia/test -e 'using Pkg; Pkg.instantiate(); Pkg.test("Arrow")'`
  - `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_writepaths_bench.jl > .cache/agent/tmp/arrow-julia-m0-enum-writepaths.tsv`
  - `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_writepaths_bench.jl > .cache/agent/tmp/arrow-julia-m0-enum-writepaths-rerun.tsv`
  - `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_writepaths_bench.jl > .cache/agent/tmp/arrow-julia-m0-enum-writepaths-rerun2.tsv`
  - `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_throughput_matrix.jl > .cache/agent/tmp/arrow-julia-m0-enum-throughput.tsv`
  - `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_throughput_matrix.jl > .cache/agent/tmp/arrow-julia-m0-enum-throughput-rerun.tsv`
  - `git -C .data/arrow-julia diff --check`

### [IN PROGRESS] Feature: land Arrow.jl Milestone 1 canonical core support

- **Objective**: complete the first production-facing canonical type-alignment
  slice in `arrow-julia`: canonical UUID metadata, a new offset-only canonical
  timestamp logical type, and explicit rejection for Run-End Encoded arrays.
- **Update**:
  - changed
    `.data/arrow-julia/src/ArrowTypes/src/ArrowTypes.jl`
    so `UUID` now writes the canonical `arrow.uuid` extension name by default
    while still reading legacy `JuliaLang.UUID` metadata
  - added `Arrow.TimestampWithOffset{U}` in
    `.data/arrow-julia/src/eltypes.jl`
    on the canonical `arrow.timestamp_with_offset` path, preserving raw struct
    storage under `convert=false` instead of coercing to `ZonedDateTime`
  - kept legacy `JuliaLang.ZonedDateTime-UTC` and `JuliaLang.ZonedDateTime`
    readers intact for backward compatibility
  - added explicit Run-End Encoded rejection in
    `.data/arrow-julia/src/eltypes.jl`
    and `.data/arrow-julia/src/table.jl`
    so both `Arrow.Table` and `Arrow.Stream` fail directly with a clear
    unsupported error
  - added focused coverage in
    `.data/arrow-julia/test/runtests.jl`
    plus a real IPC fixture at
    `.data/arrow-julia/test/run_end_encoded_small.arrow`
  - synchronized package docs in
    `.data/arrow-julia/README.md`
    and `.data/arrow-julia/docs/src/manual.md`
- **Validation Executed**:
  - `direnv exec . julia --project=.data/arrow-julia/src/ArrowTypes -e 'using Pkg; Pkg.test()'`
  - `direnv exec . julia --project=.data/arrow-julia/test -e 'using Pkg; Pkg.instantiate(); Pkg.test("Arrow")'`
  - `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_writepaths_bench.jl > .cache/agent/tmp/arrow-julia-m1-writepaths.tsv`
  - `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_writepaths_bench.jl > .cache/agent/tmp/arrow-julia-m1-writepaths-rerun.tsv`
  - `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_writepaths_bench.jl > .cache/agent/tmp/arrow-julia-m1-writepaths-rerun2.tsv`
  - `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_writepaths_bench.jl > .cache/agent/tmp/arrow-julia-m1-writepaths-rerun3.tsv`
  - `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_throughput_matrix.jl > .cache/agent/tmp/arrow-julia-m1-throughput.tsv`
  - `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_throughput_matrix.jl > .cache/agent/tmp/arrow-julia-m1-throughput-rerun.tsv`
  - `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_throughput_matrix.jl > .cache/agent/tmp/arrow-julia-m1-throughput-rerun2.tsv`
  - `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_timestamp_with_offset_bench.jl > .cache/agent/tmp/arrow-julia-m1-timestamp-with-offset.tsv`
  - `git -C .data/arrow-julia diff --check`
- **Status**:
  - functional gates passed:
    - `ArrowTypes | 190/190`
    - `Arrow | 66157/66157 | 3m35.2s`
  - targeted canonical benchmark is strongly positive:
    - `canonical_fixed_offset 0.149 ms`
    - `canonical_mixed_offset 0.145 ms`
    - `legacy_totimestamp 0.624 ms`
    - `legacy_zoned_datetime 128.968 ms`
  - throughput reruns are broadly near the Milestone 0 archive
  - write-path microbench reruns for untouched `offset_binary` and
    `offset_nested_list` remain noisier/slower than the Milestone 0 archive, so
    the strict perf gate is not yet recorded as clean-pass

### [DONE] Feature: close the `xiuxian-daochang` `zhixing` reminder/bootstrap owner drift bundle

- **Objective**: move reminder queue backfill and reminder notice rendering
  back onto the live `xiuxian-zhixing` owner surface so the host bootstrap
  stops carrying parallel reminder logic.
- **Update**:
  - extended
    `packages/rust/crates/xiuxian-zhixing/src/heyi/reminders.rs`
    so `ZhixingHeyi` now owns:
    - queue-aware reminder polling
    - due-queue backfill
    - reminder notice rendering against `reminder_notice.md`
  - extended
    `packages/rust/crates/xiuxian-zhixing/src/heyi/tasks.rs`
    so scheduled task creation also enqueues the due-queue backend when it is
    configured
  - updated
    `packages/rust/crates/xiuxian-zhixing/src/heyi/reminder_queue.rs`
    and `packages/rust/crates/xiuxian-zhixing/src/lib.rs`
    so the richer reminder payload and root exports are consistent again
  - rewired
    `packages/rust/crates/xiuxian-daochang/src/agent/bootstrap/zhixing.rs`
    to consume the live `ZhixingHeyi` reminder surface instead of calling the
    stale `backfill_reminder_queue` and
    `render_reminder_notice_markdown_v2` assumptions
  - confirmed that the `xiuxian-daochang --lib` compile front no longer stops
    in `agent/bootstrap/zhixing.rs`; the active next blockers now start in
    `agent/turn_execution/react_loop/*` and
    `session/bounded_store/window_ops/*`
- **Validation Executed**:
  - `direnv exec . rustfmt packages/rust/crates/xiuxian-zhixing/src/heyi/reminders.rs packages/rust/crates/xiuxian-zhixing/src/heyi/reminder_queue.rs packages/rust/crates/xiuxian-zhixing/src/heyi/tasks.rs packages/rust/crates/xiuxian-zhixing/src/lib.rs packages/rust/crates/xiuxian-daochang/src/agent/bootstrap/zhixing.rs`
  - `direnv exec . rg -n "super::super::" packages/rust/crates/xiuxian-zhixing/src/heyi packages/rust/crates/xiuxian-daochang/src/agent/bootstrap/zhixing.rs -g '*.rs'`
  - `direnv exec . cargo test -p xiuxian-zhixing heyi::reminders::tests::poll_reminders_carries_task_metadata --lib -- --exact`
  - `direnv exec . cargo test -p xiuxian-zhixing heyi::reminders::tests::render_reminder_notice_uses_live_template_surface --lib -- --exact`
  - `direnv exec . cargo check -p xiuxian-daochang --lib --message-format short`

### [DONE] PY-F56: carry repo-search path-prefix filters through the real `xiuxian-wendao` Flight host

- **Objective**: extend the typed repo-search request contract beyond language
  filtering so the real Flight host can prove structural narrowing on stable
  repo paths through the same request surface.
- **Update**:
  - extended the Rust transport contract with
    `x-wendao-repo-search-path-prefixes`
  - rewired
    `packages/rust/crates/xiuxian-wendao-runtime/src/transport/server.rs`
    so repo-search metadata now parses and forwards optional path prefixes
  - rewired
    `packages/rust/crates/xiuxian-wendao/src/link_graph/plugin_runtime/transport/server.rs`
    so the real `SearchPlaneRepoSearchFlightRouteProvider` applies those
    prefixes against `SearchHit.path` after backend repo-content search
  - extended the Python typed request surface in
    `packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py/transport/query.py`
    so `repo_search_request(..., path_prefixes=(...))` emits sorted stable
    metadata and rejects blank prefixes
  - tightened the direct Python real-host smokes in
    `packages/python/xiuxian-wendao-py/tests/test_transport.py`
    so the same persisted repo corpus now proves:
    - `src/` excludes `README.md`
    - `src/flight` narrows `flightbridgetoken` to `src/flight*.rs`
- **Validation Executed**:
  - `direnv exec . cargo test -p xiuxian-wendao-runtime --features julia query_contract -- --nocapture`
  - `direnv exec . cargo test -p xiuxian-wendao --features julia link_graph::plugin_runtime::transport::server::tests:: -- --nocapture`
  - `direnv exec . uv run pytest packages/python/xiuxian-wendao-py/tests/test_query.py packages/python/xiuxian-wendao-py/tests/test_transport.py -q -k "repo_search_request or fetches_repo_search_info_with_typed_request"`
  - `direnv exec . cargo build -p xiuxian-wendao --features julia --bin wendao_search_seed_sample --bin wendao_search_flight_server`
  - `direnv exec . env WENDAO_SEARCH_SERVER_BINARY="$PWD/.cache/pyflight-rust-contract-target/debug/wendao_search_flight_server" WENDAO_SEARCH_SEED_BINARY="$PWD/.cache/pyflight-rust-contract-target/debug/wendao_search_seed_sample" uv run pytest packages/python/xiuxian-wendao-py/tests/test_transport.py -q -k wendao_search_flight_server`

### [DONE] PY-F55: carry repo-search language filters through the real `xiuxian-wendao` Flight host

- **Objective**: move the repo-search Flight contract beyond query text and
  result limit so the real host can prove backend filtering behavior through
  the same typed request path.
- **Update**:
  - extended the Rust transport contract with
    `x-wendao-repo-search-language-filters`
  - rewired
    `packages/rust/crates/xiuxian-wendao-runtime/src/transport/server.rs`
    so repo-search metadata now parses and forwards optional language filters
  - rewired
    `packages/rust/crates/xiuxian-wendao/src/link_graph/plugin_runtime/transport/server.rs`
    so the real `SearchPlaneRepoSearchFlightRouteProvider` applies those
    filters against `search_repo_content_chunks(...)`
  - extended the Python typed request surface in
    `packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py/transport/query.py`
    so `repo_search_request(..., language_filters=(...))` emits sorted stable
    metadata and rejects blank filters
  - tightened the direct Python real-host smokes in
    `packages/python/xiuxian-wendao-py/tests/test_transport.py`
    so the same persisted repo corpus now proves:
    - `markdown` filters collapse `alpha` to `README.md`
    - `rust` filters exclude `README.md`
- **Validation Executed**:
  - `direnv exec . env CARGO_HOME="$PWD/.cache/cargo-home" CARGO_TARGET_DIR="$PWD/.cache/pyflight-rust-contract-target" cargo test -p xiuxian-wendao-runtime --features julia query_contract -- --nocapture`
  - `direnv exec . env CARGO_HOME="$PWD/.cache/cargo-home" CARGO_TARGET_DIR="$PWD/.cache/pyflight-rust-contract-target" cargo test -p xiuxian-wendao --features julia link_graph::plugin_runtime::transport::server::tests:: -- --nocapture`
  - `direnv exec . uv run pytest packages/python/xiuxian-wendao-py/tests/test_query.py packages/python/xiuxian-wendao-py/tests/test_transport.py -q -k "repo_search_request or fetches_repo_search_info_with_typed_request"`
  - `direnv exec . env CARGO_HOME="$PWD/.cache/cargo-home" CARGO_TARGET_DIR="$PWD/.cache/pyflight-rust-contract-target" cargo build -p xiuxian-wendao --features julia --bin wendao_search_seed_sample --bin wendao_search_flight_server`
  - `direnv exec . env WENDAO_SEARCH_SERVER_BINARY="$PWD/.cache/pyflight-rust-contract-target/debug/wendao_search_flight_server" WENDAO_SEARCH_SEED_BINARY="$PWD/.cache/pyflight-rust-contract-target/debug/wendao_search_seed_sample" uv run pytest packages/python/xiuxian-wendao-py/tests/test_transport.py -q -k wendao_search_flight_server`

### [DONE] PY-F54: lock exact-match tie ordering on the real `xiuxian-wendao` repo-search host

- **Objective**: stop treating real-host repo-search verification as only
  presence/limit coverage and prove one concrete ranking rule from the live
  repo-content search implementation.
- **Update**:
  - expanded
    `packages/rust/crates/xiuxian-wendao/src/link_graph/plugin_runtime/transport/server.rs`
    so the persisted sample corpus now includes two exact-match tie fixtures:
    - `src/a_rank.rs`
    - `src/z_rank.rs`
  - added Rust bootstrap/provider coverage proving the live search-plane
    route breaks that exact-match tie by lexicographic path order
  - tightened the direct Python integration smokes in
    `packages/python/xiuxian-wendao-py/tests/test_transport.py`
    so `ranktieexacttoken` with `limit=1` now asserts the real
    `wendao_search_flight_server` host returns `src/a_rank.rs`
- **Validation Executed**:
  - `direnv exec . env CARGO_HOME="$PWD/.cache/cargo-home" CARGO_TARGET_DIR="$PWD/.cache/pyflight-rust-contract-target" cargo test -p xiuxian-wendao --features julia link_graph::plugin_runtime::transport::server::tests:: -- --nocapture`
  - `direnv exec . env CARGO_HOME="$PWD/.cache/cargo-home" CARGO_TARGET_DIR="$PWD/.cache/pyflight-rust-contract-target" cargo build -p xiuxian-wendao --features julia --bin wendao_search_seed_sample --bin wendao_search_flight_server`
  - `direnv exec . env WENDAO_SEARCH_SERVER_BINARY="$PWD/.cache/pyflight-rust-contract-target/debug/wendao_search_flight_server" WENDAO_SEARCH_SEED_BINARY="$PWD/.cache/pyflight-rust-contract-target/debug/wendao_search_seed_sample" uv run pytest packages/python/xiuxian-wendao-py/tests/test_transport.py -q -k wendao_search_flight_server`

### [DONE] PY-F53: tighten the real `xiuxian-wendao` repo-search host smoke with richer persisted fixtures

- **Objective**: move the direct `wendao_search_flight_server` repo-search
  smoke beyond one broad happy-path read and prove that the persisted fixture
  corpus supports multiple query cases plus explicit `limit` behavior on the
  real host path.
- **Update**:
  - expanded
    `packages/rust/crates/xiuxian-wendao/src/link_graph/plugin_runtime/transport/server.rs`
    so `bootstrap_sample_repo_search_content(...)` now seeds a richer
    repo-search corpus with query-distinguishable documents for:
    - `alpha`
    - `searchonlytoken`
    - `flightbridgetoken`
  - added Rust bootstrap coverage that proves the seeded search-plane corpus
    supports both multi-query reads and one-row `limit` behavior
  - tightened the direct Python integration smokes in
    `packages/python/xiuxian-wendao-py/tests/test_transport.py` so the real
    `wendao_search_flight_server` path now asserts:
    - `alpha` hits the seeded repo body
    - `searchonlytoken` returns exactly one row from `src/search.rs`
    - `flightbridgetoken` reaches `src/flight_search.rs`
    - the same query/limit behavior survives host restart
- **Validation Executed**:
  - `direnv exec . env CARGO_HOME="$PWD/.cache/cargo-home" CARGO_TARGET_DIR="$PWD/.cache/pyflight-rust-contract-target" cargo test -p xiuxian-wendao --features julia link_graph::plugin_runtime::transport::server::tests:: -- --nocapture`
  - `direnv exec . env CARGO_HOME="$PWD/.cache/cargo-home" CARGO_TARGET_DIR="$PWD/.cache/pyflight-rust-contract-target" cargo build -p xiuxian-wendao --features julia --bin wendao_search_seed_sample --bin wendao_search_flight_server`
  - `direnv exec . env WENDAO_SEARCH_SERVER_BINARY="$PWD/.cache/pyflight-rust-contract-target/debug/wendao_search_flight_server" WENDAO_SEARCH_SEED_BINARY="$PWD/.cache/pyflight-rust-contract-target/debug/wendao_search_seed_sample" uv run pytest packages/python/xiuxian-wendao-py/tests/test_transport.py -q -k wendao_search_flight_server`

### [DONE] Feature: advance the compile front beyond runtime transport and helper drift

- **Objective**: keep the bounded compile-front remediation moving after the
  Telegram/Discord channel-family slice, without jumping directly into the
  much larger `llm/*` and `runtime_agent_factory/*` ownership repairs.
- **Update**:
  - restored the missing
    `RepoSearchFlightRouteProvider` re-export in
    `packages/rust/crates/xiuxian-wendao-runtime/src/transport/mod.rs`, so
    the compile front no longer exits `xiuxian-daochang` through a stale
    `xiuxian-wendao` transport import
  - replaced the dead `command_router/session.rs` test-helper content with
    the live Telegram session command router, so the current command router
    again owns help / agenda / reset / resume / stop / admin / feedback /
    injection / partition dispatch
  - repaired the `jobs/heartbeat` owner import by switching
    `packages/rust/crates/xiuxian-daochang/src/jobs/heartbeat/health.rs` to
    the current `crate::jobs::JobMetricsSnapshot` surface
  - repaired the initial embedding helper drift by replacing the dead
    `env_first_non_empty` macro dependency with a local env scan and by
    making the currently unwired `mistral_sdk` embedding path fail explicitly
    instead of failing the build
  - confirmed with scoped compile-front checks that the current first failure
    set now starts deeper in
    `packages/rust/crates/xiuxian-daochang/src/llm/*`,
    `packages/rust/crates/xiuxian-daochang/src/resolve.rs`, and
    `packages/rust/crates/xiuxian-daochang/src/runtime_agent_factory/*`
- **Validation Executed**:
  - `direnv exec . rustfmt packages/rust/crates/xiuxian-wendao-runtime/src/transport/mod.rs`
  - `direnv exec . cargo check -p xiuxian-wendao-runtime --lib --features julia`
  - `direnv exec . cargo check -p xiuxian-wendao --lib --features julia`
  - `direnv exec . rustfmt packages/rust/crates/xiuxian-daochang/src/channels/telegram/runtime/jobs/command_router/session.rs packages/rust/crates/xiuxian-daochang/src/channels/telegram/runtime/jobs/command_router/mod.rs packages/rust/crates/xiuxian-daochang/src/jobs/heartbeat/health.rs packages/rust/crates/xiuxian-daochang/src/embedding/client/support.rs packages/rust/crates/xiuxian-daochang/src/embedding/transport_mistral_sdk.rs`
  - `direnv exec . cargo check -p xiuxian-daochang --lib`

### [DONE] PY-F50: switch the real `xiuxian-wendao` Flight smoke to a persisted fixture

- **Objective**: stop relying on startup-time sample injection for the direct
  Python smoke against `wendao_search_flight_server` and prove that a seeded
  repo-search corpus can survive process restart through runtime-owned local
  metadata.
- **Update**:
  - added local repo-corpus record/snapshot sidecar paths under
    `packages/rust/crates/xiuxian-wendao/src/search_plane/service/core/construction/paths.rs`
  - updated
    `packages/rust/crates/xiuxian-wendao/src/search_plane/service/core/publication.rs`
    to persist repo-corpus record/snapshot JSON alongside publication updates
  - updated
    `packages/rust/crates/xiuxian-wendao/src/search_plane/service/core/repo_runtime/reads.rs`
    so cache misses can hydrate repo-corpus publication metadata from those
    local runtime sidecars, while preserving the existing manifest-only
    fallback prohibition
  - updated
    `packages/rust/crates/xiuxian-wendao/src/search_plane/service/core/repo_runtime/tests.rs`
    so test cleanup removes the new local sidecars too
  - restored the restart regression in
    `packages/rust/crates/xiuxian-wendao/src/link_graph/plugin_runtime/transport/server.rs`
    to prove bootstrapped repo-search content stays queryable after a new
    `SearchPlaneService` instance starts
  - added the helper binary
    `packages/rust/crates/xiuxian-wendao/src/bin/wendao_search_seed_sample.rs`
    and updated
    `packages/python/xiuxian-wendao-py/tests/test_transport.py`
    so the direct `wendao_search_flight_server` smoke now seeds once, then
    starts a fresh Flight host process with no `WENDAO_BOOTSTRAP_SAMPLE_REPO`
    dependency
- **Validation Executed**:
  - `direnv exec . env CARGO_HOME=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/cargo-home CARGO_TARGET_DIR=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-rust-contract-target cargo test -p xiuxian-wendao --features julia bootstrap_sample_repo_search_content_persists_across_service_restart -- --nocapture`
  - `direnv exec . env CARGO_HOME=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/cargo-home CARGO_TARGET_DIR=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-rust-contract-target cargo test -p xiuxian-wendao --features julia repo_search_publication_state_does_not_hydrate_from_manifest_without_repo_corpus_cache -- --nocapture`
  - `direnv exec . env CARGO_HOME=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/cargo-home CARGO_TARGET_DIR=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-rust-contract-target cargo build -p xiuxian-wendao --features julia --bin wendao_search_seed_sample --bin wendao_search_flight_server`
  - `direnv exec . env WENDAO_SEARCH_SERVER_BINARY=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-rust-contract-target/debug/wendao_search_flight_server WENDAO_SEARCH_SEED_BINARY=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-rust-contract-target/debug/wendao_search_seed_sample uv run pytest packages/python/xiuxian-wendao-py/tests/test_transport.py -q -k wendao_search_flight_server`

### [DONE] PY-F51: prove persisted repo-search fixtures survive repeated Flight host restarts

- **Objective**: strengthen the direct Python smoke beyond one fresh host
  launch and prove that the persisted repo-search fixture remains queryable
  across repeated `wendao_search_flight_server` restarts.
- **Update**:
  - extended
    `packages/python/xiuxian-wendao-py/tests/test_transport.py`
    with a new integration smoke that seeds one project root once, then
    starts and stops `wendao_search_flight_server` twice against that same
    persisted fixture
  - the restart smoke now checks both `get_repo_search_info(...)` and
    `read_repo_search_rows(...)`, so the full repo-search query contract is
    exercised across repeated host restarts instead of only one happy-path
    process
- **Validation Executed**:
  - `direnv exec . env WENDAO_SEARCH_SERVER_BINARY=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-rust-contract-target/debug/wendao_search_flight_server WENDAO_SEARCH_SEED_BINARY=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-rust-contract-target/debug/wendao_search_seed_sample uv run pytest packages/python/xiuxian-wendao-py/tests/test_transport.py -q -k wendao_search_flight_server`

### [DONE] PY-F52: prove `/rerank/flight` through the real `xiuxian-wendao` Flight host

- **Objective**: stop validating rerank semantics only through the runtime or
  example Flight hosts and prove that the real `xiuxian-wendao` binary serves
  the stable rerank exchange contract too.
- **Update**:
  - extended
    `packages/python/xiuxian-wendao-py/tests/test_transport.py`
    with direct `wendao_search_flight_server` integration smokes for:
    - typed rerank happy-path scoring/order
    - duplicate-`doc_id` rejection on `/rerank/flight`
  - the same direct binary smoke command now covers:
    - one-start repo-search read
    - repeated-start repo-search read
    - typed rerank exchange success
    - typed rerank exchange rejection
- **Validation Executed**:
  - `direnv exec . env WENDAO_SEARCH_SERVER_BINARY=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-rust-contract-target/debug/wendao_search_flight_server WENDAO_SEARCH_SEED_BINARY=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-rust-contract-target/debug/wendao_search_seed_sample uv run pytest packages/python/xiuxian-wendao-py/tests/test_transport.py -q -k wendao_search_flight_server`

### [DONE] PY-F49: prove Python repo-search against the real `xiuxian-wendao` Flight binary

- **Objective**: stop validating the Flight query seam only through the
  runtime-owned host and prove that Python can read repo-search rows through
  the real `xiuxian-wendao` binary with a minimal seeded search-plane corpus.
- **Update**:
  - added
    `bootstrap_sample_repo_search_content(...)` to
    `packages/rust/crates/xiuxian-wendao/src/link_graph/plugin_runtime/transport/server.rs`
    and exported it through the plugin-runtime transport surface
  - updated
    `packages/rust/crates/xiuxian-wendao/src/bin/wendao_search_flight_server.rs`
    so `WENDAO_BOOTSTRAP_SAMPLE_REPO=1` seeds one minimal repo corpus into
    `SearchPlaneService` before mounting the Flight service
  - added a Python integration smoke in
    `packages/python/xiuxian-wendao-py/tests/test_transport.py` that launches
    `wendao_search_flight_server`, queries `/search/repos/main` through the
    typed repo-search client, and asserts real rows from the seeded search
    plane
- **Validation Executed**:
  - `direnv exec . env CARGO_HOME=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/cargo-home CARGO_TARGET_DIR=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-rust-contract-target cargo test -p xiuxian-wendao --features julia link_graph::plugin_runtime::transport::server::tests:: -- --nocapture`
  - `direnv exec . env WENDAO_SEARCH_SERVER_BINARY=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-rust-contract-target/debug/wendao_search_flight_server uv run pytest packages/python/xiuxian-wendao-py/tests/test_transport.py -q -k wendao_search_flight_server`

### [DONE] PY-F48: mount a real `xiuxian-wendao` repo-search backend behind the Flight provider seam

- **Objective**: stop leaving the new repo-search provider seam as a static
  runtime placeholder and prove that `WendaoFlightService` can be mounted on
  top of the real `SearchPlaneService` without introducing a runtime→wendao
  dependency cycle.
- **Update**:
  - changed
    `packages/rust/crates/xiuxian-wendao-runtime/src/transport/server.rs`
    so `RepoSearchFlightRouteProvider` is async; `WendaoFlightService`
    now awaits provider-backed repo-search batches in both
    `get_flight_info(...)` and `do_get(...)`
  - added
    `packages/rust/crates/xiuxian-wendao/src/link_graph/plugin_runtime/transport/server.rs`
    with:
    - `SearchPlaneRepoSearchFlightRouteProvider`
    - `build_search_plane_flight_service(...)`
    - crate-local tests proving repo-content publications can be surfaced as
      Flight repo-search batches
  - updated
    `packages/rust/crates/xiuxian-wendao/src/link_graph/plugin_runtime/transport/mod.rs`
    and
    `packages/rust/crates/xiuxian-wendao/src/link_graph/plugin_runtime/mod.rs`
    to export the new provider/builder surface
  - added the real `xiuxian-wendao` binary
    `packages/rust/crates/xiuxian-wendao/src/bin/wendao_search_flight_server.rs`
    which mounts `WendaoFlightService` against `SearchPlaneService` for one
    repo-backed search surface instead of a static sample batch
  - added the required direct crate dependencies in
    `packages/rust/crates/xiuxian-wendao/Cargo.toml` for the new server
    binary (`arrow-flight`, `tonic`, `tokio-stream`)
- **Validation Executed**:
  - `direnv exec . env CARGO_HOME=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/cargo-home CARGO_TARGET_DIR=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-rust-contract-target cargo test -p xiuxian-wendao-runtime --features julia wendao_flight_service -- --nocapture`
  - `direnv exec . env CARGO_HOME=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/cargo-home CARGO_TARGET_DIR=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-rust-contract-target cargo test -p xiuxian-wendao --features julia search_plane_repo_search_provider -- --nocapture`
  - `direnv exec . env CARGO_HOME=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/cargo-home CARGO_TARGET_DIR=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-rust-contract-target cargo build -p xiuxian-wendao --features julia --bin wendao_search_flight_server`
  - `direnv exec . env WENDAO_MOCK_SERVER_BINARY=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-rust-contract-target/debug/wendao_flight_server uv run pytest packages/python/xiuxian-wendao-py/tests/test_query.py packages/python/xiuxian-wendao-py/tests/test_transport.py -q`

### [DONE] Feature: normalize the remaining `xiuxian-daochang` channel-family owner paths

- **Objective**: close the live Telegram/Discord channel-family compile-front
  drift without widening another layer of relative-path imports or carrying
  dead duplicate leaves.
- **Update**:
  - mounted the existing
    `packages/rust/crates/xiuxian-daochang/src/channels/telegram/channel/outbound_text.rs`
    owner leaf through the live Telegram channel module tree
  - re-exported the current
    `jobs::{JobRecord, QueuedJob, epoch_millis}` owner seam from
    `packages/rust/crates/xiuxian-daochang/src/jobs/mod.rs`
  - retired the dead Telegram
    `packages/rust/crates/xiuxian-daochang/src/channels/telegram/runtime/jobs/command_handlers/session_commands/session_context.rs`
    duplicate instead of patching another stale command path
  - retired the dead Discord managed-session
    `admin.rs` / `injection.rs` handler leaves and the matching dead parsing
    duplicates, because the live managed command dispatch no longer routes
    through those surfaces
  - updated the touched live owner seams to use `crate::...` paths instead
    of `super::super::...`
  - confirmed with a scoped compile-front check that `cargo check
-p xiuxian-daochang --lib` now exits this channel-family slice and stops
    deeper in a pre-existing `xiuxian-wendao` transport import failure
- **Validation Executed**:
  - `direnv exec . rustfmt packages/rust/crates/xiuxian-daochang/src/jobs/mod.rs packages/rust/crates/xiuxian-daochang/src/channels/telegram/channel/mod.rs packages/rust/crates/xiuxian-daochang/src/channels/telegram/runtime/jobs/command_handlers/session_commands/mod.rs packages/rust/crates/xiuxian-daochang/src/channels/telegram/runtime/jobs/command_router/mod.rs packages/rust/crates/xiuxian-daochang/src/channels/telegram/runtime/run_polling/run.rs packages/rust/crates/xiuxian-daochang/src/channels/discord/runtime/managed/handlers/command_dispatch/session/mod.rs`
  - `direnv exec . cargo check -p xiuxian-daochang --lib`

### [DONE] Feature: repair the Discord runtime dispatch surface and restore the shared turn-content helper

- **Objective**: clear the live `xiuxian-daochang` compile-front cluster in
  `channels/discord/runtime/dispatch/` without expanding the slice into the
  broader Telegram/runtime export failures.
- **Update**:
  - restored the mounted `dispatch` child-module wiring in
    `packages/rust/crates/xiuxian-daochang/src/channels/discord/runtime/dispatch/mod.rs`
  - added the new local leaf
    `packages/rust/crates/xiuxian-daochang/src/channels/discord/runtime/dispatch/support.rs`
    for inbound-message and preemption logging
  - restored the `process_discord_message(...)` compatibility wrapper used by
    the current Discord runtime tests
  - updated
    `packages/rust/crates/xiuxian-daochang/src/channels/discord/runtime/dispatch/turn.rs`
    to call the current direct
    `run_foreground_turn_with_interrupt(...)` API instead of the removed
    `ForegroundTurnRequest`
  - widened
    `packages/rust/crates/xiuxian-daochang/src/channels/discord/runtime/interrupt.rs`
    to `pub(crate)` so the runtime re-export and test-support wrapper can use
    the live controller surface legally
  - restored the shared
    `compose_turn_content(...)` helper in
    `packages/rust/crates/xiuxian-daochang/src/channels/managed_runtime/turn.rs`
    so the repaired Discord dispatch path stops depending on another missing
    leaf
  - confirmed with a scoped compile-front grep that the repaired Discord
    dispatch cluster no longer appears in `cargo check --lib`; the remaining
    front now starts deeper in the Telegram runtime surface and other
    pre-existing export drift
- **Validation Executed**:
  - `direnv exec . rustfmt packages/rust/crates/xiuxian-daochang/src/channels/discord/runtime/dispatch/mod.rs packages/rust/crates/xiuxian-daochang/src/channels/discord/runtime/dispatch/support.rs packages/rust/crates/xiuxian-daochang/src/channels/discord/runtime/dispatch/turn.rs packages/rust/crates/xiuxian-daochang/src/channels/discord/runtime/interrupt.rs packages/rust/crates/xiuxian-daochang/src/channels/managed_runtime/turn.rs`
  - `direnv exec . cargo check -p xiuxian-daochang --lib`
  - `direnv exec . git diff --check`

### [DONE] Feature: repair dictionary-encoded `CategoricalArray` missing-value interop

- **Objective**: fix the production-facing seam where
  `CategoricalArray{Union{Missing,T}}` written with `dictencode=true` could
  read back with incorrect values and fail in `copy(tt.x)` and
  `DataFrame(tt; copycols=true)`.
- **Update**:
  - reproduced the bug with a minimal
    `CategoricalArray(Union{Missing,String}["a", missing, "c"])` roundtrip
  - traced the mismatch to dictionary index normalization in
    `/.data/arrow-julia/src/arraytypes/dictencoding.jl`: the writer was
    subtracting `firstindex(refarray)` even though `CategoricalArray` missing
    pools are indexed from `firstindex(refpool) == 0`
  - updated Arrow's dictionary-code normalization to align to the pool axis
    instead of the ref-storage axis
  - added a narrow regression in
    `/.data/arrow-julia/test/runtests.jl` covering:
    - `collect(tt.x)`
    - `copy(tt.x)`
    - `DataFrame(tt; copycols=true)`
- **Validation Executed**:
  - `direnv exec . julia --project=.data/arrow-julia/test -e 'using DataFrames, CategoricalArrays; import Arrow; x = CategoricalArray(Union{Missing,String}["a", missing, "c"]); bytes = Arrow.tobuffer((x=x,); dictencode=true); tbl = Arrow.Table(bytes); println("table=" * repr(collect(tbl.x))); println("copycol=" * repr(collect(copy(tbl.x)))); df = DataFrame(tbl; copycols=true); println("df=" * repr(collect(df.x)))'`
  - `direnv exec . julia --project=.data/arrow-julia/test -e 'using Pkg; Pkg.instantiate(); Pkg.test("Arrow")'`
    - result: `Arrow | 66137/66137 | 3m29.9s`
  - `direnv exec . git diff --check -- .data/arrow-julia/README.md .data/arrow-julia/src/arraytypes/dictencoding.jl .data/arrow-julia/test/runtests.jl .cache/agent/execplans/arrow_julia_categorical_dict_missing.md docs/GTD/DAILY_2026_03_30.md`

### [DONE] Feature: restore the shared channel embedding-memory guard leaf

- **Objective**: repair the live shared helper missing from
  `packages/rust/crates/xiuxian-daochang/src/nodes/channel/common.rs` so the
  Discord and Telegram node launch paths stop depending on a nonexistent
  `apply_channel_embedding_memory_guard(...)`.
- **Update**:
  - restored `apply_channel_embedding_memory_guard(...)` in
    `packages/rust/crates/xiuxian-daochang/src/nodes/channel/common.rs`
  - restored the matching test shim
    `apply_channel_embedding_memory_guard_for_tests(...)` with the existing
    channel-test semantics: inherited `mistral_sdk` defaults are guarded to
    `http`, while explicit env-level backend overrides and explicit
    `allow_inproc_embed` test inputs remain authoritative
  - confirmed by a scoped `cargo check --lib` grep that the repaired helper
    names no longer appear at the compile front
  - confirmed by a focused lib-test build that the compile front has moved
    past this leaf and now fails deeper in pre-existing Discord runtime
    dispatch and Telegram channel wiring clusters
- **Validation Executed**:
  - `direnv exec . rustfmt packages/rust/crates/xiuxian-daochang/src/nodes/channel/common.rs`
  - `direnv exec . cargo test -p xiuxian-daochang nodes::channel::tests:: --lib`
  - `direnv exec . cargo check -p xiuxian-daochang --lib`

### [DONE] Feature: harden `Utf8View` / `BinaryView` variadic-buffer inference against real IPC under-reporting

- **Objective**: fix a real-world Arrow IPC read failure for Polars-generated
  long-string view columns without relying on test-only magic values.
- **Update**:
  - reproduced the failure with a generated IPC file containing two adjacent
    long `Utf8View` columns, where the second column under-reported
    `variadicBufferCounts`
  - moved the inline/external view boundary into shared layout helpers in
    `/.data/arrow-julia/src/arraytypes/views.jl` instead of leaving implicit
    numeric thresholds in the reader path
  - updated `/.data/arrow-julia/src/table.jl` so view-backed columns infer the
    required external buffer count from valid parsed view elements when the
    declared variadic count is too low
  - explicitly excluded invalid/null view slots from that inference so null
    payload bytes do not affect external buffer attachment
  - added regression coverage in
    `/.data/arrow-julia/test/runtests.jl` for the shared boundary helper and
    the valid-slot-only buffer-count inference behavior
- **Validation Executed**:
  - `direnv exec . julia --project=.data/arrow-julia/test -e 'using DataFrames; import Arrow; path = joinpath(pwd(), ".cache/agent/tmp/long.arrow"); df = Arrow.Table(path) |> DataFrame; @assert nrow(df) == 100; @assert ncol(df) == 2; display(first(df, 3))'`
  - `direnv exec . julia --project=.data/arrow-julia/test -e 'using Test; import Arrow; include(joinpath(pwd(), ".data/arrow-julia/test/runtests.jl"))'`
    - result: `Arrow | 66134/66134 | 2m56.8s`
  - `direnv exec . git diff --check -- .data/arrow-julia/README.md .data/arrow-julia/src/arraytypes/views.jl .data/arrow-julia/src/table.jl .data/arrow-julia/test/runtests.jl .cache/agent/execplans/arrow_julia_direct_tobuffer_fastpath.md docs/GTD/DAILY_2026_03_30.md`

### [DONE] Feature: retire the dead Discord channel constructors duplicate

- **Objective**: remove the stale mounted
  `packages/rust/crates/xiuxian-daochang/src/channels/discord/channel/constructors.rs`
  duplicate instead of widening more internal visibility just to keep an
  obsolete launch path compiling.
- **Update**:
  - removed `mod constructors;` from
    `packages/rust/crates/xiuxian-daochang/src/channels/discord/channel/mod.rs`
  - deleted the unreferenced duplicate
    `packages/rust/crates/xiuxian-daochang/src/channels/discord/channel/constructors.rs`
  - verified by source search that the mounted Discord channel tree no longer
    contains the stale constructor path
  - confirmed with a scoped compile probe that the previous
    `constructors.rs` import-drift cluster no longer appears at the compile
    front
- **Validation Executed**:
  - `direnv exec . rg -n "mod constructors;|constructors\\.rs|run_discord_channel_command\\b" packages/rust/crates/xiuxian-daochang/src/channels/discord -g '!**/target/**'`
  - `direnv exec . cargo check -p xiuxian-daochang --lib`

### [DONE] Feature: repair the `runtime_agent_factory` test-support visibility seam

- **Objective**: remove the private-import breakage between
  `packages/rust/crates/xiuxian-daochang/src/runtime_agent_factory/` and
  `packages/rust/crates/xiuxian-daochang/src/test_support/runtime_agent_factory.rs`
  without widening the public API beyond the crate.
- **Update**:
  - promoted the reused helper functions in
    `runtime_agent_factory/inference.rs` and
    `runtime_agent_factory/memory.rs` from `pub(super)` to `pub(crate)`
  - kept the outward crate boundary unchanged; the widened visibility stops at
    crate scope and only serves the internal `test_support` wrappers
  - confirmed by a scoped compile probe that the previous
    `runtime_agent_factory` private-import errors no longer appear at the
    compile front
- **Validation Executed**:
  - `direnv exec . rustfmt packages/rust/crates/xiuxian-daochang/src/runtime_agent_factory/inference.rs packages/rust/crates/xiuxian-daochang/src/runtime_agent_factory/memory.rs`
  - `direnv exec . cargo check -p xiuxian-daochang --lib`

### [DONE] Feature: repair the `xiuxian-daochang` session prompt-injection owner seam

- **Objective**: remove the stale `qianhuan` API and missing owner drift in
  `packages/rust/crates/xiuxian-daochang/src/agent/system_prompt_injection_state.rs`
  so this session prompt-injection path is owned locally again.
- **Update**:
  - replaced the dead `SessionSystemPromptInjectionSnapshot` /
    `normalize_session_system_prompt_injection_xml(...)` dependency with a
    local snapshot/cache/storage seam backed by
    `xiuxian_qianhuan::SystemPromptInjectionWindow`
  - added local persistence helpers for snapshot session IDs, snapshot chat
    messages, and JSON storage round-trips
  - restored the exported
    `SYSTEM_PROMPT_INJECTION_CONTEXT_MESSAGE_NAME` constant inside the local
    leaf
  - removed the stale `manifestation_manager` dependency from this path so it
    now compiles against the current `Agent` shape
  - confirmed by a targeted compile attempt that the compile front no longer
    stops on the `system_prompt_injection` cluster and has moved on to larger
    pre-existing import/export failures elsewhere in `xiuxian-daochang`
- **Validation Executed**:
  - `direnv exec . rustfmt packages/rust/crates/xiuxian-daochang/src/agent/system_prompt_injection_state.rs`
  - `direnv exec . cargo test -p xiuxian-daochang --test agent_system_prompt_injection_state --no-run`

### [DONE] Feature: remove the stale `xiuxian-daochang` `comrak` / `regex` direct dependencies

- **Objective**: close the bounded `cargo-machete` follow-on bundle after the
  `rmcp` exit by removing the remaining stale direct dependencies
  `comrak` and `regex` from `xiuxian-daochang`.
- **Update**:
  - removed `comrak` and `regex` from
    `packages/rust/crates/xiuxian-daochang/Cargo.toml`
  - verified by source search that the package has no concrete code owner for
    either dependency; the only remaining `regex` hit is comment text
  - confirmed that `cargo-machete` is now clean on
    `packages/rust/crates/xiuxian-daochang`
  - confirmed that full `cargo check -p xiuxian-daochang --lib` remains
    blocked by large pre-existing module/export failures outside the touched
    dependency surface
- **Validation Executed**:
  - `direnv exec . rg -n "\\b(comrak|regex)\\b|use regex|use comrak|Regex::|Comrak|comrak::" packages/rust/crates/xiuxian-daochang/src packages/rust/crates/xiuxian-daochang/tests -g '!**/target/**'`
  - `direnv exec . cargo machete packages/rust/crates/xiuxian-daochang`
  - `direnv exec . cargo check -p xiuxian-daochang --lib`

### [DONE] Feature: retire the last `xiuxian-daochang` production `rmcp` client seam

- **Objective**: finish the production-side `rmcp` exit by replacing the last
  live `xiuxian-daochang/src/tool_runtime/bridge.rs` client seam instead of
  only shrinking callers around it.
- **Update**:
  - replaced the `bridge.rs` `rmcp` client with a self-owned streamable-HTTP
    JSON-RPC client that covers `initialize`, `notifications/initialized`,
    `tools/list`, and `tools/call`
  - added local public tool-runtime transport types in
    `packages/rust/crates/xiuxian-daochang/src/tool_runtime/types.rs` and
    re-exported them through `tool_runtime/mod.rs` and `src/lib.rs`
  - moved `rmcp` from `packages/rust/crates/xiuxian-daochang/Cargo.toml`
    production dependencies to `dev-dependencies`, leaving it owned only by
    the integration test server harness
  - updated
    `packages/rust/crates/xiuxian-daochang/tests/tool_pool_reconnect.rs` and
    `packages/rust/crates/xiuxian-daochang/tests/tool_discover_cache.rs` to
    match the local result types
  - verified by source search that direct `rmcp` usage no longer exists in
    `packages/rust/crates/xiuxian-daochang/src/`
  - confirmed that crate-level `xiuxian-daochang` compile/test gates are still
    blocked by large pre-existing module/export failures outside the touched
    seam
- **Validation Executed**:
  - `direnv exec . rustfmt packages/rust/crates/xiuxian-daochang/src/tool_runtime/bridge.rs packages/rust/crates/xiuxian-daochang/src/tool_runtime/types.rs packages/rust/crates/xiuxian-daochang/src/tool_runtime/mod.rs packages/rust/crates/xiuxian-daochang/src/lib.rs packages/rust/crates/xiuxian-daochang/tests/tool_pool_reconnect.rs packages/rust/crates/xiuxian-daochang/tests/tool_discover_cache.rs`
  - `direnv exec . rg -n "\\brmcp::|\\buse rmcp\\b|rmcp =" packages/rust/crates/xiuxian-daochang/src packages/rust/crates/xiuxian-daochang/Cargo.toml -g '!**/target/**'`
  - `direnv exec . cargo machete packages/rust/crates/xiuxian-daochang`
  - `direnv exec . cargo check -p xiuxian-daochang --lib`
  - `direnv exec . cargo test -p xiuxian-daochang tool_runtime::bridge::tests:: --lib`

### [DONE] Feature: contract the `xiuxian-daochang` production rmcp surface to one bridge seam

- **Objective**: continue the final live-owner `rmcp` exit work by shrinking
  `xiuxian-daochang` production code away from `rmcp` request/response model
  types before attempting a full client replacement.
- **Update**:
  - added local `ToolRuntimeListResult`, `ToolRuntimeToolDefinition`, and
    `ToolRuntimeCallResult` in
    `packages/rust/crates/xiuxian-daochang/src/tool_runtime/types.rs`
  - updated
    `packages/rust/crates/xiuxian-daochang/src/tool_runtime/bridge.rs` to
    translate `rmcp` list/call payloads into those local types at the bridge
    boundary
  - updated
    `packages/rust/crates/xiuxian-daochang/src/agent/tool_dispatch/helpers.rs`
    so the agent-side caller no longer depends on `rmcp` model types
  - verified by source search that direct `rmcp` usage in
    `packages/rust/crates/xiuxian-daochang/src/` is now narrowed to
    `tool_runtime/bridge.rs`
  - confirmed that crate-level `xiuxian-daochang` compile/test gates remain
    blocked by large pre-existing module/export failures outside the touched
    seam, so this slice was validated by local formatting plus source-surface
    reduction rather than a clean crate build
- **Validation Executed**:
  - `direnv exec . rustfmt packages/rust/crates/xiuxian-daochang/src/tool_runtime/mod.rs packages/rust/crates/xiuxian-daochang/src/tool_runtime/bridge.rs packages/rust/crates/xiuxian-daochang/src/tool_runtime/types.rs packages/rust/crates/xiuxian-daochang/src/agent/tool_dispatch/helpers.rs`
  - `direnv exec . rg -n "use rmcp::|rmcp::" packages/rust/crates/xiuxian-daochang/src -g '!**/target/**'`
  - `direnv exec . cargo check -p xiuxian-daochang --lib`
  - `direnv exec . cargo test -p xiuxian-daochang llm_tool_definitions_maps_description_and_schema --lib`

### [DONE] PY-F28: publish a Rust-owned Wendao Flight query contract surface

- **Objective**: stop treating Wendao Flight query routes as scattered test
  literals and publish a Rust-owned transport contract for the schema-version
  header plus canonical query/exchange routes.
- **Update**:
  - added
    `packages/rust/crates/xiuxian-wendao-runtime/src/transport/query_contract.rs`
    with:
    - `WENDAO_SCHEMA_VERSION_HEADER`
    - `REPO_SEARCH_ROUTE`
    - `RERANK_EXCHANGE_ROUTE`
    - `normalize_flight_route(...)`
    - `flight_descriptor_path(...)`
  - re-exported that contract through
    `packages/rust/crates/xiuxian-wendao-runtime/src/transport/mod.rs`
  - rewired
    `packages/rust/crates/xiuxian-wendao-runtime/src/transport/flight.rs`
    so the runtime Flight client now consumes the shared route-normalization
    and descriptor-path helpers instead of keeping its own private route logic
  - rewired
    `packages/rust/crates/xiuxian-wendao-runtime/examples/mock_flight_exchange_server.rs`
    so the live Python↔Rust smoke server now consumes the same shared header
    and route contract
  - updated the `xiuxian-wendao-py` README to record that `/search/repos/main`
    and `/rerank/flight` are now backed by a Rust-owned transport contract
    instead of remaining duplicated test literals
- **Validation Executed**:
  - `direnv exec . env CARGO_TARGET_DIR=$PRJ_ROOT/.cache/pyflight-rust-contract-target cargo test -p xiuxian-wendao-runtime --features julia query_contract -- --nocapture`
    - result: `4 passed`
  - `direnv exec . env CARGO_TARGET_DIR=$PRJ_ROOT/.cache/pyflight-rust-contract-target cargo build -p xiuxian-wendao-runtime --features julia --example mock_flight_exchange_server`
  - `direnv exec . env WENDAO_MOCK_SERVER_BINARY=$PRJ_ROOT/.cache/pyflight-rust-contract-target/debug/examples/mock_flight_exchange_server uv run pytest packages/python/xiuxian-wendao-py/tests/test_transport.py -q`
    - result: `16 passed`
  - `direnv exec . python -m compileall packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py packages/python/xiuxian-wendao-py/tests/test_transport.py`
  - `direnv exec . git diff --check`

### [DONE] PY-F29: add a typed repo-search query/result layer on top of Flight tables

- **Objective**: stop forcing downstream Python analyzer authors to manually
  parse raw Arrow tables for the stable repo-search route and publish one
  typed helper layer backed by the Rust-owned query contract.
- **Update**:
  - extended
    `packages/rust/crates/xiuxian-wendao-runtime/src/transport/query_contract.rs`
    with canonical repo-search response columns:
    - `doc_id`
    - `path`
    - `title`
    - `score`
    - `language`
  - updated the Rust mock Flight example so:
    - query reads on `/search/repos/main` now return the stable repo-search
      response shape
    - exchange round-trips keep their separate response shape
  - extended Python transport query helpers in
    `packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py/transport/query.py`
    with:
    - `repo_search_query()`
    - `WendaoRepoSearchResultRow`
    - `validate_repo_search_table(...)`
    - `parse_repo_search_rows(...)`
  - extended
    `packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py/transport/client.py`
    with:
    - `get_repo_search_info()`
    - `read_repo_search_table()`
    - `read_repo_search_rows()`
  - added unit coverage in
    `packages/python/xiuxian-wendao-py/tests/test_query.py`
  - extended the live Rust-backed transport smoke in
    `packages/python/xiuxian-wendao-py/tests/test_transport.py` so it now
    validates the full typed repo-search response shape, not just `doc_id`
  - updated the package README with the typed repo-search helper path
- **Validation Executed**:
  - `direnv exec . env CARGO_TARGET_DIR=$PRJ_ROOT/.cache/pyflight-rust-contract-target cargo test -p xiuxian-wendao-runtime --features julia query_contract -- --nocapture`
    - result: `4 passed`
  - `direnv exec . env CARGO_TARGET_DIR=$PRJ_ROOT/.cache/pyflight-rust-contract-target cargo build -p xiuxian-wendao-runtime --features julia --example mock_flight_exchange_server`
  - `direnv exec . env WENDAO_MOCK_SERVER_BINARY=$PRJ_ROOT/.cache/pyflight-rust-contract-target/debug/examples/mock_flight_exchange_server uv run pytest packages/python/xiuxian-wendao-py/tests/test_query.py packages/python/xiuxian-wendao-py/tests/test_transport.py -q`
    - result: `20 passed`
  - `direnv exec . python -m compileall packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py packages/python/xiuxian-wendao-py/tests/test_query.py packages/python/xiuxian-wendao-py/tests/test_transport.py`
  - `direnv exec . git diff --check`

### [DONE] PY-F30: add a typed rerank request builder for the live Flight exchange route

- **Objective**: stop forcing downstream Python authors to hand-build Arrow
  request tables for the stable rerank exchange route and publish one typed
  request helper that still executes against the live Rust-owned `do_exchange`
  path.
- **Update**:
  - extended
    `packages/rust/crates/xiuxian-wendao-runtime/src/transport/query_contract.rs`
    with stable rerank request columns:
    - `doc_id`
    - `vector_score`
  - extended Python query helpers in
    `packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py/transport/query.py`
    with:
    - `rerank_exchange_query()`
    - `WendaoRerankRequestRow`
    - `validate_rerank_request_table(...)`
    - `build_rerank_request_table(...)`
  - extended
    `packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py/transport/client.py`
    with:
    - `exchange_rerank_table()`
    - `exchange_rerank_rows()`
  - exported the new request helpers through the package root and transport
    root
  - added unit coverage for the rerank request builder in
    `packages/python/xiuxian-wendao-py/tests/test_query.py`
  - added both unit and live Rust-backed transport coverage for
    `exchange_rerank_rows()` in
    `packages/python/xiuxian-wendao-py/tests/test_transport.py`
  - updated the package README with the typed rerank request path
- **Validation Executed**:
  - `direnv exec . env CARGO_TARGET_DIR=$PRJ_ROOT/.cache/pyflight-rust-contract-target cargo test -p xiuxian-wendao-runtime --features julia query_contract -- --nocapture`
    - result: `4 passed`
  - `direnv exec . env WENDAO_MOCK_SERVER_BINARY=$PRJ_ROOT/.cache/pyflight-rust-contract-target/debug/examples/mock_flight_exchange_server uv run pytest packages/python/xiuxian-wendao-py/tests/test_query.py packages/python/xiuxian-wendao-py/tests/test_transport.py -q`
    - result: `25 passed`
  - `direnv exec . python -m compileall packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py packages/python/xiuxian-wendao-py/tests/test_query.py packages/python/xiuxian-wendao-py/tests/test_transport.py`
  - `direnv exec . git diff --check`

### [DONE] PY-F33: lift rerank schema validation into the shared Rust transport contract

- **Objective**: stop keeping the live `/rerank/flight` schema gate buried in
  the mock Flight example and move that validation into the shared
  `xiuxian-wendao-runtime` transport contract surface.
- **Update**:
  - extended
    `packages/rust/crates/xiuxian-wendao-runtime/src/transport/query_contract.rs`
    with `validate_rerank_request_schema(...)`
  - added contract-level unit coverage for:
    - accepted stable rerank shape
    - wrong scalar-type rejection
    - embedding-dimension drift rejection
  - re-exported the shared validator through
    `packages/rust/crates/xiuxian-wendao-runtime/src/transport/mod.rs`
  - removed the duplicate example-local rerank schema validator from
    `packages/rust/crates/xiuxian-wendao-runtime/examples/mock_flight_exchange_server.rs`
    so the live Python↔Rust smoke path now consumes the shared contract
    directly
  - updated the `xiuxian-wendao-py` README to record that rerank schema
    enforcement now belongs to the shared Rust transport contract, not just
    the example server
- **Validation Executed**:
  - `direnv exec . env CARGO_HOME=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/cargo-home CARGO_TARGET_DIR=$PRJ_ROOT/.cache/pyflight-rust-contract-target cargo test -p xiuxian-wendao-runtime --features julia query_contract -- --nocapture`
    - result: `7 passed`
  - `direnv exec . env CARGO_HOME=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/cargo-home CARGO_TARGET_DIR=$PRJ_ROOT/.cache/pyflight-rust-contract-target cargo build -p xiuxian-wendao-runtime --features julia --example mock_flight_exchange_server`
  - `direnv exec . env WENDAO_MOCK_SERVER_BINARY=$PRJ_ROOT/.cache/pyflight-rust-contract-target/debug/examples/mock_flight_exchange_server uv run pytest packages/python/xiuxian-wendao-py/tests/test_query.py packages/python/xiuxian-wendao-py/tests/test_transport.py -q`
    - result: `28 passed`
  - `direnv exec . python -m compileall packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py packages/python/xiuxian-wendao-py/tests/test_query.py packages/python/xiuxian-wendao-py/tests/test_transport.py`
  - `direnv exec . git diff --check`

### [DONE] PY-F34: add a stable repo-search request contract on the live Flight query route

- **Objective**: stop treating `/search/repos/main` as a parameterless table
  read and publish one Rust-owned semantic request contract for repo-search
  query text plus result limit.
- **Update**:
  - extended
    `packages/rust/crates/xiuxian-wendao-runtime/src/transport/query_contract.rs`
    with:
    - `x-wendao-repo-search-query`
    - `x-wendao-repo-search-limit`
    - `REPO_SEARCH_DEFAULT_LIMIT`
    - `validate_repo_search_request(...)`
  - re-exported that request contract through
    `packages/rust/crates/xiuxian-wendao-runtime/src/transport/mod.rs`
  - updated
    `packages/rust/crates/xiuxian-wendao-runtime/examples/mock_flight_exchange_server.rs`
    so the live query route now rejects blank query text and non-positive
    limits for both `get_flight_info(...)` and `do_get(...)`
  - extended Python transport config/query/client surfaces in:
    - `packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py/transport/config.py`
    - `packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py/transport/query.py`
    - `packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py/transport/client.py`
      with:
    - `WendaoRepoSearchRequest`
    - `repo_search_request(...)`
    - `repo_search_metadata(...)`
    - request-aware `get_repo_search_info(request)`,
      `read_repo_search_table(request)`, and `read_repo_search_rows(request)`
  - extended Python exports and tests so repo-search now has both a typed
    request contract and a typed response contract
  - updated the `xiuxian-wendao-py` README so the repo-search route is now
    documented as one request+response contract instead of a parameterless
    query read
- **Validation Executed**:
  - `direnv exec . env CARGO_HOME=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/cargo-home CARGO_TARGET_DIR=$PRJ_ROOT/.cache/pyflight-rust-contract-target cargo test -p xiuxian-wendao-runtime --features julia query_contract -- --nocapture`
    - result: `10 passed`
  - `direnv exec . env CARGO_HOME=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/cargo-home CARGO_TARGET_DIR=$PRJ_ROOT/.cache/pyflight-rust-contract-target cargo build -p xiuxian-wendao-runtime --features julia --example mock_flight_exchange_server`
  - `direnv exec . env WENDAO_MOCK_SERVER_BINARY=$PRJ_ROOT/.cache/pyflight-rust-contract-target/debug/examples/mock_flight_exchange_server uv run pytest packages/python/xiuxian-wendao-py/tests/test_query.py packages/python/xiuxian-wendao-py/tests/test_transport.py -q`
    - result: `32 passed`
  - `direnv exec . python -m compileall packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py packages/python/xiuxian-wendao-py/tests/test_query.py packages/python/xiuxian-wendao-py/tests/test_transport.py`
  - `direnv exec . git diff --check`

### [DONE] PY-F31: extend the typed rerank request contract with embeddings

- **Objective**: move the Python rerank request contract from metadata-only
  rows into one real embedding-bearing Arrow request shape, so downstream
  analyzer authors can send stable vector payloads without hand-building
  fixed-size-list columns.
- **Update**:
  - extended
    `packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py/transport/query.py`
    so `WendaoRerankRequestRow` now carries:
    - `doc_id`
    - `vector_score`
    - `embedding`
    - `query_embedding`
  - extended the same module with stable request-column exports for:
    - `embedding`
    - `query_embedding`
  - updated `build_rerank_request_table(...)` to emit Arrow
    `fixed_size_list<float32>` columns for both embedding vectors
  - added fail-fast dimension validation so mixed embedding sizes and
    query/embedding drift are rejected before transport
  - updated package-root and transport-root exports for the expanded request
    contract
  - updated unit coverage in
    `packages/python/xiuxian-wendao-py/tests/test_query.py`
  - updated transport coverage in
    `packages/python/xiuxian-wendao-py/tests/test_transport.py`
    so both the local typed request path and the live Rust-backed
    `exchange_rerank_rows()` smoke now exercise embedding-bearing rows
  - updated the package README with the new rerank request shape
- **Validation Executed**:
  - `direnv exec . env WENDAO_MOCK_SERVER_BINARY=$PRJ_ROOT/.cache/pyflight-rust-contract-target/debug/examples/mock_flight_exchange_server uv run pytest packages/python/xiuxian-wendao-py/tests/test_query.py packages/python/xiuxian-wendao-py/tests/test_transport.py -q`
    - result: `24 passed, 3 skipped`
  - `direnv exec . python -m compileall packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py packages/python/xiuxian-wendao-py/tests/test_query.py packages/python/xiuxian-wendao-py/tests/test_transport.py`
  - `direnv exec . git diff --check`

### [DONE] PY-F32: add a Rust-owned rerank dimension gate for the live Flight exchange route

- **Objective**: give the live Rust `do_exchange` seam one stable route-scoped
  rerank request gate without depending on brittle cross-version Arrow batch
  decoding, so typed Python rerank requests are distinguishable from ad hoc
  table uploads at runtime.
- **Update**:
  - extended
    `packages/rust/crates/xiuxian-wendao-runtime/src/transport/query_contract.rs`
    with the canonical rerank dimension metadata header:
    - `x-wendao-rerank-embedding-dimension`
  - re-exported that header through
    `packages/rust/crates/xiuxian-wendao-runtime/src/transport/mod.rs`
  - updated
    `packages/rust/crates/xiuxian-wendao-runtime/examples/mock_flight_exchange_server.rs`
    so the live rerank exchange route now rejects requests that do not carry a
    positive rerank dimension header
  - extended
    `packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py/transport/config.py`
    with the matching Python-side header constant
  - extended
    `packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py/transport/client.py`
    so `flight_call_options(...)`, `get_query_info(...)`,
    `read_query_table(...)`, and `exchange_query_table(...)` can carry
    per-call metadata
  - updated `exchange_rerank_rows()` so the typed helper path automatically
    attaches the rerank dimension header derived from the validated embedding
    batch
  - updated the transport tests so:
    - typed rerank rows still succeed against the live Rust mock server
    - malformed manual rerank uploads are rejected by the Rust route gate
  - updated the package README with the new runtime gate semantics
- **Validation Executed**:
  - `direnv exec . env CARGO_HOME=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/cargo-home CARGO_TARGET_DIR=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-rust-contract-target cargo test -p xiuxian-wendao-runtime --features julia query_contract -- --nocapture`
    - result: `4 passed`
  - `direnv exec . env CARGO_HOME=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/cargo-home CARGO_TARGET_DIR=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-rust-contract-target cargo build -p xiuxian-wendao-runtime --features julia --example mock_flight_exchange_server`
  - `direnv exec . env WENDAO_MOCK_SERVER_BINARY=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-rust-contract-target/debug/examples/mock_flight_exchange_server uv run pytest packages/python/xiuxian-wendao-py/tests/test_query.py packages/python/xiuxian-wendao-py/tests/test_transport.py -q`
    - result: `28 passed`
  - `direnv exec . python -m compileall packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py packages/python/xiuxian-wendao-py/tests/test_query.py packages/python/xiuxian-wendao-py/tests/test_transport.py`
  - `direnv exec . git diff --check`

### [DONE] PY-F33: promote the live rerank exchange gate from metadata-only to schema-aware

- **Objective**: harden the live Rust rerank route so it validates the first
  non-empty Arrow schema frame, not just the rerank dimension header, while
  still avoiding brittle cross-version batch decoding.
- **Update**:
  - added `arrow-schema` support to the `julia` feature in
    `packages/rust/crates/xiuxian-wendao-runtime/Cargo.toml`
  - updated
    `packages/rust/crates/xiuxian-wendao-runtime/examples/mock_flight_exchange_server.rs`
    so `/rerank/flight` now:
    - reads the first non-empty exchange frame as an Arrow schema
    - validates stable rerank columns:
      - `doc_id`
      - `vector_score`
      - `embedding`
      - `query_embedding`
    - validates the expected Arrow types:
      - `doc_id: Utf8`
      - `vector_score: Float32`
      - `embedding/query_embedding: FixedSizeList<Float32>`
    - validates that the embedding list dimension matches the rerank-dimension
      metadata header
  - updated transport integration coverage in
    `packages/python/xiuxian-wendao-py/tests/test_transport.py`
    so:
    - a manual but schema-valid `exchange_query_table(...)` call succeeds
    - typed `exchange_rerank_rows()` still succeeds
    - malformed rerank schema uploads are rejected with a Rust-owned column/type
      error
  - updated the package README to document that direct `/rerank/flight`
    callers must satisfy the same stable schema as the typed helper path
- **Validation Executed**:
  - `direnv exec . env CARGO_HOME=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/cargo-home CARGO_TARGET_DIR=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-rust-contract-target cargo test -p xiuxian-wendao-runtime --features julia query_contract -- --nocapture`
    - result: `4 passed`
  - `direnv exec . env CARGO_HOME=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/cargo-home CARGO_TARGET_DIR=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-rust-contract-target cargo build -p xiuxian-wendao-runtime --features julia --example mock_flight_exchange_server`
  - `direnv exec . env WENDAO_MOCK_SERVER_BINARY=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-rust-contract-target/debug/examples/mock_flight_exchange_server uv run pytest packages/python/xiuxian-wendao-py/tests/test_query.py packages/python/xiuxian-wendao-py/tests/test_transport.py -q`
    - result: `28 passed`
  - `direnv exec . python -m compileall packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py packages/python/xiuxian-wendao-py/tests/test_query.py packages/python/xiuxian-wendao-py/tests/test_transport.py`
  - `direnv exec . git diff --check`

### [DONE] PY-F35: promote the live rerank exchange gate from schema-aware to semantic-aware

- **Objective**: make the live Rust `/rerank/flight` seam reject semantically
  invalid rerank batches after schema validation, instead of only checking
  transport shape.
- **Update**:
  - added `arrow-array` support to the `julia` feature in
    `packages/rust/crates/xiuxian-wendao-runtime/Cargo.toml`
  - extended
    `packages/rust/crates/xiuxian-wendao-runtime/src/transport/query_contract.rs`
    with `validate_rerank_request_batch(...)`, so the shared Rust transport
    contract now enforces:
    - non-empty rerank batches
    - non-blank `doc_id`
    - finite `vector_score`
    - stable `query_embedding` across all rows
  - re-exported that batch validator through
    `packages/rust/crates/xiuxian-wendao-runtime/src/transport/mod.rs`
  - updated
    `packages/rust/crates/xiuxian-wendao-runtime/examples/mock_flight_exchange_server.rs`
    so the live example server:
    - skips pyarrow `do_exchange(...)` frames that carry no Arrow message
      header
    - decodes inbound rerank batches through `FlightRecordBatchStream`
    - applies the shared semantic batch validator before returning a response
  - extended
    `packages/python/xiuxian-wendao-py/tests/test_transport.py`
    with a live Python↔Rust smoke that proves query-embedding drift is now
    rejected by the Rust-owned route contract
  - updated the `xiuxian-wendao-py` README so the rerank route docs now record
    the row-level semantic gate, not just the schema gate
- **Validation Executed**:
  - `direnv exec . env CARGO_HOME=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/cargo-home CARGO_TARGET_DIR=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-rust-contract-target cargo test -p xiuxian-wendao-runtime --features julia query_contract -- --nocapture`
    - result: `13 passed`
  - `direnv exec . env CARGO_HOME=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/cargo-home CARGO_TARGET_DIR=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-rust-contract-target cargo build -p xiuxian-wendao-runtime --features julia --example mock_flight_exchange_server`
  - `direnv exec . env WENDAO_MOCK_SERVER_BINARY=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-rust-contract-target/debug/examples/mock_flight_exchange_server uv run pytest packages/python/xiuxian-wendao-py/tests/test_query.py packages/python/xiuxian-wendao-py/tests/test_transport.py -q`
    - result: `33 passed`
  - `direnv exec . python -m compileall packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py packages/python/xiuxian-wendao-py/tests/test_query.py packages/python/xiuxian-wendao-py/tests/test_transport.py`
  - `direnv exec . git diff --check`

### [DONE] PY-F36: publish stable candidate semantics on the live rerank route

- **Objective**: make `/rerank/flight` express a minimal Rust-owned candidate
  contract beyond structural validity, so rerank uploads now fail on clearly
  invalid candidate semantics instead of only on transport/schema issues.
- **Update**:
  - extended
    `packages/rust/crates/xiuxian-wendao-runtime/src/transport/query_contract.rs`
    so `validate_rerank_request_batch(...)` now also enforces:
    - unique `doc_id` values across one rerank batch
    - `vector_score` inside the inclusive range `[0.0, 1.0]`
  - added contract-level unit coverage for duplicate-document rejection and
    out-of-range score rejection
  - extended
    `packages/python/xiuxian-wendao-py/tests/test_transport.py`
    with a live Python↔Rust smoke proving that duplicate `doc_id` values are
    now rejected by the Rust-owned rerank route
  - updated the `xiuxian-wendao-py` README so the rerank contract docs now
    record candidate-level semantics, not only request integrity
- **Validation Executed**:
  - `direnv exec . env CARGO_HOME=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/cargo-home CARGO_TARGET_DIR=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-rust-contract-target cargo test -p xiuxian-wendao-runtime --features julia query_contract -- --nocapture`
    - result: `15 passed`
  - `direnv exec . env CARGO_HOME=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/cargo-home CARGO_TARGET_DIR=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-rust-contract-target cargo build -p xiuxian-wendao-runtime --features julia --example mock_flight_exchange_server`
  - `direnv exec . env WENDAO_MOCK_SERVER_BINARY=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-rust-contract-target/debug/examples/mock_flight_exchange_server uv run pytest packages/python/xiuxian-wendao-py/tests/test_query.py packages/python/xiuxian-wendao-py/tests/test_transport.py -q`
    - result: `34 passed`
  - `direnv exec . python -m compileall packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py packages/python/xiuxian-wendao-py/tests/test_query.py packages/python/xiuxian-wendao-py/tests/test_transport.py`
  - `direnv exec . git diff --check`

### [DONE] PY-F37: publish a stable rerank response contract on `/rerank/flight`

- **Objective**: stop returning an ad hoc rerank response table and publish a
  Rust-owned response schema that downstream Python analyzers can consume as a
  typed result surface.
- **Update**:
  - extended
    `packages/rust/crates/xiuxian-wendao-runtime/src/transport/query_contract.rs`
    with stable rerank response columns:
    - `doc_id`
    - `final_score`
    - `rank`
  - updated
    `packages/rust/crates/xiuxian-wendao-runtime/examples/mock_flight_exchange_server.rs`
    so `/rerank/flight` now returns those three columns instead of only
    `doc_id`
  - extended
    `packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py/transport/query.py`
    with:
    - `RERANK_RESPONSE_COLUMNS`
    - `WendaoRerankResultRow`
    - `validate_rerank_response_table(...)`
    - `parse_rerank_response_rows(...)`
  - extended
    `packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py/transport/client.py`
    with `exchange_rerank_result_rows()`
  - updated Python exports and tests so both raw exchange tables and typed
    rerank result rows now validate against the stable response contract
  - updated the `xiuxian-wendao-py` README so rerank now has both a typed
    request contract and a typed response contract
- **Validation Executed**:
  - `direnv exec . env CARGO_HOME=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/cargo-home CARGO_TARGET_DIR=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-rust-contract-target cargo test -p xiuxian-wendao-runtime --features julia query_contract -- --nocapture`
    - result: `15 passed`
  - `direnv exec . env CARGO_HOME=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/cargo-home CARGO_TARGET_DIR=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-rust-contract-target cargo build -p xiuxian-wendao-runtime --features julia --example mock_flight_exchange_server`
  - `direnv exec . env WENDAO_MOCK_SERVER_BINARY=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-rust-contract-target/debug/examples/mock_flight_exchange_server uv run pytest packages/python/xiuxian-wendao-py/tests/test_query.py packages/python/xiuxian-wendao-py/tests/test_transport.py -q`
    - result: `38 passed`
  - `direnv exec . python -m compileall packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py packages/python/xiuxian-wendao-py/tests/test_query.py packages/python/xiuxian-wendao-py/tests/test_transport.py`
  - `direnv exec . git diff --check`

### [DONE] PY-F38: move rerank response validation into the shared Rust transport contract

- **Objective**: stop treating the rerank response contract as an example-only
  shape and publish shared Rust-owned response validation alongside the shared
  request validation.
- **Update**:
  - extended
    `packages/rust/crates/xiuxian-wendao-runtime/src/transport/query_contract.rs`
    with:
    - `validate_rerank_response_schema(...)`
    - `validate_rerank_response_batch(...)`
  - the shared rerank response validator now enforces:
    - stable response columns and Arrow types
    - non-empty response batches
    - non-blank and unique `doc_id`
    - finite `final_score` within `[0.0, 1.0]`
    - positive and unique `rank`
  - re-exported those validators through
    `packages/rust/crates/xiuxian-wendao-runtime/src/transport/mod.rs`
  - updated
    `packages/rust/crates/xiuxian-wendao-runtime/examples/mock_flight_exchange_server.rs`
    so the live `/rerank/flight` example validates its outbound response batch
    through the shared contract before encoding
  - updated the `xiuxian-wendao-py` README to record that rerank now has
    shared Rust-owned validation on both the request and response side
- **Validation Executed**:
  - `direnv exec . env CARGO_HOME=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/cargo-home CARGO_TARGET_DIR=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-rust-contract-target cargo test -p xiuxian-wendao-runtime --features julia query_contract -- --nocapture`
    - result: `20 passed`
  - `direnv exec . env CARGO_HOME=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/cargo-home CARGO_TARGET_DIR=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-rust-contract-target cargo build -p xiuxian-wendao-runtime --features julia --example mock_flight_exchange_server`
  - `direnv exec . env WENDAO_MOCK_SERVER_BINARY=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-rust-contract-target/debug/examples/mock_flight_exchange_server uv run pytest packages/python/xiuxian-wendao-py/tests/test_query.py packages/python/xiuxian-wendao-py/tests/test_transport.py -q`
    - result: `38 passed`
  - `direnv exec . python -m compileall packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py packages/python/xiuxian-wendao-py/tests/test_query.py packages/python/xiuxian-wendao-py/tests/test_transport.py`
  - `direnv exec . git diff --check`

### [DONE] PY-F39: validate the live rerank response route through the shared Rust response gate

- **Objective**: stop letting the example Flight route rely on local response
  discipline and prove that the published rerank response contract is enforced
  by the shared Rust transport layer itself.
- **Update**:
  - updated
    `packages/rust/crates/xiuxian-wendao-runtime/examples/mock_flight_exchange_server.rs`
    so `/rerank/flight` validates its outbound response batch via
    `validate_rerank_response_batch(...)` before encoding
  - extended shared unit coverage in
    `packages/rust/crates/xiuxian-wendao-runtime/src/transport/query_contract.rs`
    for:
    - accepted rerank response shape
    - wrong `rank` type
    - duplicate `rank`
    - out-of-range `final_score`
  - kept the existing Python rerank response smoke green, so the route still
    returns the stable `doc_id/final_score/rank` contract while now being
    validated on the Rust side before it leaves the service
  - updated the `xiuxian-wendao-py` README to note that the live example route
    now uses the shared Rust response gate rather than an example-local shape
- **Validation Executed**:
  - `direnv exec . env CARGO_HOME=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/cargo-home CARGO_TARGET_DIR=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-rust-contract-target cargo test -p xiuxian-wendao-runtime --features julia query_contract -- --nocapture`
    - result: `20 passed`
  - `direnv exec . env CARGO_HOME=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/cargo-home CARGO_TARGET_DIR=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-rust-contract-target cargo build -p xiuxian-wendao-runtime --features julia --example mock_flight_exchange_server`
  - `direnv exec . env WENDAO_MOCK_SERVER_BINARY=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-rust-contract-target/debug/examples/mock_flight_exchange_server uv run pytest packages/python/xiuxian-wendao-py/tests/test_query.py packages/python/xiuxian-wendao-py/tests/test_transport.py -q`
    - result: `38 passed`
  - `direnv exec . python -m compileall packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py packages/python/xiuxian-wendao-py/tests/test_query.py packages/python/xiuxian-wendao-py/tests/test_transport.py`
  - `direnv exec . git diff --check`

### [DONE] PY-F40: derive the live rerank response deterministically from inbound candidates

- **Objective**: stop letting the live `/rerank/flight` example return a fixed
  placeholder payload and make it express stable rerank behavior derived from
  the validated inbound candidate batch.
- **Update**:
  - updated
    `packages/rust/crates/xiuxian-wendao-runtime/examples/mock_flight_exchange_server.rs`
    so the rerank route now:
    - extracts `doc_id` and `vector_score` from each validated inbound batch
    - sorts candidates by descending `vector_score`
    - breaks ties by ascending `doc_id`
    - emits `doc_id`, `final_score`, and `rank` from that deterministic order
  - removed the old fixed rerank response payload from the example service,
    so the live route behavior is now request-driven rather than example-local
  - updated the live Python integration expectations in
    `packages/python/xiuxian-wendao-py/tests/test_transport.py` to assert the
    deterministic rerank ordering and score propagation
  - updated the `xiuxian-wendao-py` README to record that the live rerank
    route now derives response rows from inbound candidates instead of
    returning a static example table
- **Validation Executed**:
  - `direnv exec . env CARGO_HOME=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/cargo-home CARGO_TARGET_DIR=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-rust-contract-target cargo test -p xiuxian-wendao-runtime --features julia query_contract -- --nocapture`
    - result: `20 passed`
  - `direnv exec . env CARGO_HOME=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/cargo-home CARGO_TARGET_DIR=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-rust-contract-target cargo build -p xiuxian-wendao-runtime --features julia --example mock_flight_exchange_server`
  - `direnv exec . env WENDAO_MOCK_SERVER_BINARY=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-rust-contract-target/debug/examples/mock_flight_exchange_server uv run pytest packages/python/xiuxian-wendao-py/tests/test_query.py packages/python/xiuxian-wendao-py/tests/test_transport.py -q`
    - result: `38 passed`
  - `direnv exec . python -m compileall packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py packages/python/xiuxian-wendao-py/tests/test_query.py packages/python/xiuxian-wendao-py/tests/test_transport.py`
  - `direnv exec . git diff --check`

### [DONE] PY-F41: move live rerank behavior onto a shared Rust-owned scoring rule

- **Objective**: stop treating the live `/rerank/flight` behavior as a
  transport-only sort example and push one reusable Rust-owned rerank scoring
  rule underneath the same contract.
- **Update**:
  - added `score_rerank_request_batch(...)` to
    `packages/rust/crates/xiuxian-wendao-runtime/src/transport/query_contract.rs`
    so the shared transport contract now owns a stable rerank scoring rule
  - the current shared rule blends:
    - inbound `vector_score`
    - cosine similarity between `embedding` and `query_embedding`
      into one bounded `final_score`
  - updated
    `packages/rust/crates/xiuxian-wendao-runtime/examples/mock_flight_exchange_server.rs`
    so the live `/rerank/flight` example consumes that shared scorer instead
    of doing local `vector_score` ordering
  - added Rust unit coverage proving that stronger semantic alignment can rank
    ahead of a higher raw vector score
  - updated Python live integration expectations in
    `packages/python/xiuxian-wendao-py/tests/test_transport.py` so the
    Rust-backed rerank smoke now asserts semantic-aware ordering and score
    propagation
  - updated the `xiuxian-wendao-py` README to record that the live rerank
    route now uses a shared Rust-owned scoring rule rather than a pure
    transport-level sort
- **Validation Executed**:
  - `direnv exec . env CARGO_HOME=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/cargo-home CARGO_TARGET_DIR=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-rust-contract-target cargo test -p xiuxian-wendao-runtime --features julia query_contract -- --nocapture`
    - result: `21 passed`
  - `direnv exec . env CARGO_HOME=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/cargo-home CARGO_TARGET_DIR=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-rust-contract-target cargo build -p xiuxian-wendao-runtime --features julia --example mock_flight_exchange_server`
  - `direnv exec . env WENDAO_MOCK_SERVER_BINARY=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-rust-contract-target/debug/examples/mock_flight_exchange_server uv run pytest packages/python/xiuxian-wendao-py/tests/test_query.py packages/python/xiuxian-wendao-py/tests/test_transport.py -q`
    - result: `38 passed`
  - `direnv exec . python -m compileall packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py packages/python/xiuxian-wendao-py/tests/test_query.py packages/python/xiuxian-wendao-py/tests/test_transport.py`
  - `direnv exec . git diff --check`

### [DONE] PY-F42: move scored rerank response assembly into the runtime Flight module

- **Objective**: stop keeping rerank response assembly in the example binary
  and make the `xiuxian-wendao-runtime` Flight module own the scored response
  builder that both examples and crate-local transport tests can reuse.
- **Update**:
  - added `build_scored_rerank_response_batch(...)` to
    `packages/rust/crates/xiuxian-wendao-runtime/src/transport/flight.rs`
  - re-exported that helper through
    `packages/rust/crates/xiuxian-wendao-runtime/src/transport/mod.rs`
  - updated
    `packages/rust/crates/xiuxian-wendao-runtime/examples/mock_flight_exchange_server.rs`
    so the live `/rerank/flight` example now consumes the runtime-owned helper
    instead of assembling response batches locally
  - upgraded the crate-local Flight transport test in
    `packages/rust/crates/xiuxian-wendao-runtime/src/transport/flight.rs` so
    it now round-trips a real semantic rerank request and asserts the scored
    `doc_id/final_score/rank` response inside the runtime crate itself
  - updated the `xiuxian-wendao-py` README to record that the semantic rerank
    path is now shared by both the example binary and the runtime-owned Flight
    transport test surface
- **Validation Executed**:
  - `direnv exec . env CARGO_HOME=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/cargo-home CARGO_TARGET_DIR=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-rust-contract-target cargo test -p xiuxian-wendao-runtime --features julia query_contract -- --nocapture`
    - result: `21 passed`
  - `direnv exec . env CARGO_HOME=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/cargo-home CARGO_TARGET_DIR=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-rust-contract-target cargo test -p xiuxian-wendao-runtime --features julia flight_transport_client_roundtrips_batches_over_lance_arrow_line -- --nocapture`
    - result: `1 passed`
  - `direnv exec . env CARGO_HOME=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/cargo-home CARGO_TARGET_DIR=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-rust-contract-target cargo build -p xiuxian-wendao-runtime --features julia --example mock_flight_exchange_server`
  - `direnv exec . env WENDAO_MOCK_SERVER_BINARY=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-rust-contract-target/debug/examples/mock_flight_exchange_server uv run pytest packages/python/xiuxian-wendao-py/tests/test_query.py packages/python/xiuxian-wendao-py/tests/test_transport.py -q`
    - result: `38 passed`
  - `direnv exec . python -m compileall packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py packages/python/xiuxian-wendao-py/tests/test_query.py packages/python/xiuxian-wendao-py/tests/test_transport.py`
  - `direnv exec . git diff --check`

### [DONE] PY-F43: expose a runtime-owned rerank Flight route handler

- **Objective**: stop letting server-side rerank exchange behavior exist only
  as an example-plus-helper pattern and publish an explicit runtime-owned
  server entrypoint for the stable rerank Flight route.
- **Update**:
  - added
    `packages/rust/crates/xiuxian-wendao-runtime/src/transport/server.rs`
    with `RerankFlightRouteHandler`
  - the new runtime-owned handler now owns:
    - validated rerank request batch intake
    - shared semantic scoring
    - stable `doc_id/final_score/rank` response batch assembly
  - re-exported the handler through
    `packages/rust/crates/xiuxian-wendao-runtime/src/transport/mod.rs`
  - updated
    `packages/rust/crates/xiuxian-wendao-runtime/examples/mock_flight_exchange_server.rs`
    so the live `/rerank/flight` example now calls the runtime-owned handler
    instead of assembling rerank responses directly
  - updated the crate-local Flight transport roundtrip test in
    `packages/rust/crates/xiuxian-wendao-runtime/src/transport/flight.rs`
    so it also runs through `RerankFlightRouteHandler`, proving the same
    semantic rerank path inside the runtime crate without depending on the
    external example binary
  - added focused unit coverage for the new server handler in
    `packages/rust/crates/xiuxian-wendao-runtime/src/transport/server.rs`
  - updated the `xiuxian-wendao-py` README to record that the live rerank
    server seam now has an explicit runtime-owned route handler
- **Validation Executed**:
  - `direnv exec . env CARGO_HOME=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/cargo-home CARGO_TARGET_DIR=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-rust-contract-target cargo test -p xiuxian-wendao-runtime --features julia query_contract -- --nocapture`
    - result: `21 passed`
  - `direnv exec . env CARGO_HOME=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/cargo-home CARGO_TARGET_DIR=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-rust-contract-target cargo test -p xiuxian-wendao-runtime --features julia rerank_route_handler -- --nocapture`
    - result: `2 passed`
  - `direnv exec . env CARGO_HOME=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/cargo-home CARGO_TARGET_DIR=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-rust-contract-target cargo test -p xiuxian-wendao-runtime --features julia flight_transport_client_roundtrips_batches_over_lance_arrow_line -- --nocapture`
    - result: `1 passed`
  - `direnv exec . env CARGO_HOME=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/cargo-home CARGO_TARGET_DIR=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-rust-contract-target cargo build -p xiuxian-wendao-runtime --features julia --example mock_flight_exchange_server`
  - `direnv exec . env WENDAO_MOCK_SERVER_BINARY=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-rust-contract-target/debug/examples/mock_flight_exchange_server uv run pytest packages/python/xiuxian-wendao-py/tests/test_query.py packages/python/xiuxian-wendao-py/tests/test_transport.py -q`

### [DONE] PY-F44: expose a runtime-owned Wendao Flight service surface

- **Objective**: stop treating the Rust Flight host as an example-local
  service shell plus one reusable rerank route helper and publish a
  runtime-owned service surface that owns both the stable repo-search query
  path and the rerank exchange path.
- **Update**:
  - added
    `packages/rust/crates/xiuxian-wendao-runtime/src/transport/server.rs`
    `WendaoFlightService`, so the runtime crate now owns:
    - schema-version validation
    - repo-search metadata validation
    - repo-search `get_flight_info(...)` / `do_get(...)`
    - rerank `do_exchange(...)`
  - kept `RerankFlightRouteHandler` as the route-local scorer/assembler seam,
    but moved the outer Flight service host behavior into the same runtime
    module
  - rewired
    `packages/rust/crates/xiuxian-wendao-runtime/examples/mock_flight_exchange_server.rs`
    to mount `WendaoFlightService` directly instead of carrying its own
    duplicate `FlightService` implementation and helper validators
  - re-exported `WendaoFlightService` through
    `packages/rust/crates/xiuxian-wendao-runtime/src/transport/mod.rs`
    so future non-example runtime hosts can consume the same server surface
  - confirmed that the crate-local rerank handler tests, the new
    `WendaoFlightService` construction test, the existing crate-local Flight
    roundtrip, the example build, and the Python live transport smoke all stay
    green on that shared runtime-owned host surface
- **Validation Executed**:
  - `direnv exec . env CARGO_HOME=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/cargo-home CARGO_TARGET_DIR=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-rust-contract-target cargo test -p xiuxian-wendao-runtime --features julia query_contract -- --nocapture`
  - `direnv exec . env CARGO_HOME=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/cargo-home CARGO_TARGET_DIR=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-rust-contract-target cargo test -p xiuxian-wendao-runtime --features julia rerank_route_handler -- --nocapture`
  - `direnv exec . env CARGO_HOME=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/cargo-home CARGO_TARGET_DIR=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-rust-contract-target cargo test -p xiuxian-wendao-runtime --features julia wendao_flight_service -- --nocapture`
  - `direnv exec . env CARGO_HOME=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/cargo-home CARGO_TARGET_DIR=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-rust-contract-target cargo test -p xiuxian-wendao-runtime --features julia flight_transport_client_roundtrips_batches_over_lance_arrow_line -- --nocapture`
  - `direnv exec . env CARGO_HOME=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/cargo-home CARGO_TARGET_DIR=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-rust-contract-target cargo build -p xiuxian-wendao-runtime --features julia --example mock_flight_exchange_server`
  - `direnv exec . env WENDAO_MOCK_SERVER_BINARY=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-rust-contract-target/debug/examples/mock_flight_exchange_server uv run pytest packages/python/xiuxian-wendao-py/tests/test_query.py packages/python/xiuxian-wendao-py/tests/test_transport.py -q`
  - `direnv exec . python -m compileall packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py packages/python/xiuxian-wendao-py/tests/test_query.py packages/python/xiuxian-wendao-py/tests/test_transport.py`
  - `direnv exec . git diff --check`

### [DONE] PY-F45: unify the crate-local Rust Flight host and client with the runtime-owned rerank contract

- **Objective**: stop leaving the crate-local Rust Flight roundtrip on a
  test-local host shell and close the remaining contract gap where the
  runtime-owned Rust Flight client did not automatically attach rerank
  dimension metadata for `/rerank/flight`.
- **Update**:
  - rewired
    `packages/rust/crates/xiuxian-wendao-runtime/src/transport/flight.rs`
    so the crate-local roundtrip test now mounts `WendaoFlightService`
    directly instead of maintaining another test-local `MockFlightService`
  - added route-aware rerank dimension metadata derivation inside the same
    runtime-owned Rust Flight client, so `/rerank/flight` requests now
    automatically attach `x-wendao-rerank-embedding-dimension` from the typed
    request batch schema
  - confirmed that the crate-local Rust roundtrip, the example binary, and
    the Python live transport smoke all stay green on that same runtime-owned
    rerank contract
- **Validation Executed**:
  - `direnv exec . env CARGO_HOME=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/cargo-home CARGO_TARGET_DIR=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-rust-contract-target cargo test -p xiuxian-wendao-runtime --features julia flight_transport_client_roundtrips_batches_over_lance_arrow_line -- --nocapture`
  - `direnv exec . env CARGO_HOME=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/cargo-home CARGO_TARGET_DIR=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-rust-contract-target cargo build -p xiuxian-wendao-runtime --features julia --example mock_flight_exchange_server`
  - `direnv exec . env WENDAO_MOCK_SERVER_BINARY=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-rust-contract-target/debug/examples/mock_flight_exchange_server uv run pytest packages/python/xiuxian-wendao-py/tests/test_query.py packages/python/xiuxian-wendao-py/tests/test_transport.py -q`
  - `direnv exec . git diff --check`

### [DONE] PY-F46: land a non-example runtime Flight server entrypoint

- **Objective**: stop requiring Python live transport validation to depend on
  an example binary and publish a real crate binary entrypoint that mounts the
  runtime-owned `WendaoFlightService`.
- **Update**:
  - added
    `packages/rust/crates/xiuxian-wendao-runtime/src/bin/wendao_flight_server.rs`
    as a runtime-facing Arrow Flight server binary
  - wired that binary directly to `WendaoFlightService` with the same stable
    repo-search and rerank service surface used by the example and crate-local
    roundtrip
  - promoted `tokio-stream` onto the `julia` feature dependency line so the
    crate binary can serve `TcpListenerStream` outside dev-only contexts
  - confirmed that the Python live transport smoke passes when
    `WENDAO_MOCK_SERVER_BINARY` points at the built crate binary instead of the
    example binary
- **Validation Executed**:
  - `direnv exec . rustfmt packages/rust/crates/xiuxian-wendao-runtime/src/bin/wendao_flight_server.rs`
  - `direnv exec . env CARGO_HOME=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/cargo-home CARGO_TARGET_DIR=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-rust-contract-target cargo build -p xiuxian-wendao-runtime --features julia --bin wendao_flight_server`
  - `direnv exec . env WENDAO_MOCK_SERVER_BINARY=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-rust-contract-target/debug/wendao_flight_server uv run pytest packages/python/xiuxian-wendao-py/tests/test_query.py packages/python/xiuxian-wendao-py/tests/test_transport.py -q`
  - `direnv exec . git diff --check`

### [DONE] PY-F47: make the runtime Flight service repo-search source pluggable

- **Objective**: stop freezing `WendaoFlightService` to one in-memory
  repo-search batch so a real `xiuxian-wendao` search backend can later mount
  behind the same Flight host without replacing the transport layer.
- **Update**:
  - added a runtime-owned `RepoSearchFlightRouteProvider` trait in
    `packages/rust/crates/xiuxian-wendao-runtime/src/transport/server.rs`
  - updated `WendaoFlightService` to hold a provider instead of a raw static
    batch for repo-search reads
  - kept `WendaoFlightService::new(...)` as the compatibility constructor by
    wrapping the old static batch path in an internal provider
  - added `WendaoFlightService::new_with_provider(...)` for future real
    backend wiring
  - confirmed that the new crate binary and the Python live transport smoke
    still pass on the provider-backed service surface
- **Validation Executed**:
  - `direnv exec . env CARGO_HOME=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/cargo-home CARGO_TARGET_DIR=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-rust-contract-target cargo test -p xiuxian-wendao-runtime --features julia wendao_flight_service -- --nocapture`
  - `direnv exec . env CARGO_HOME=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/cargo-home CARGO_TARGET_DIR=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-rust-contract-target cargo build -p xiuxian-wendao-runtime --features julia --bin wendao_flight_server`
  - `direnv exec . env WENDAO_MOCK_SERVER_BINARY=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-rust-contract-target/debug/wendao_flight_server uv run pytest packages/python/xiuxian-wendao-py/tests/test_query.py packages/python/xiuxian-wendao-py/tests/test_transport.py -q`
  - `direnv exec . git diff --check`
    - result: `38 passed`
  - `direnv exec . python -m compileall packages/python/xiuxian-wendao-py/src/xiuxian_wendao_py packages/python/xiuxian-wendao-py/tests/test_query.py packages/python/xiuxian-wendao-py/tests/test_transport.py`
  - `direnv exec . git diff --check`

### [DONE] Feature: normalize scoring-style response columns before encode

- **Objective**: keep the packaged scoring examples on a stable WendaoArrow
  `v1` response shape by normalizing outbound `doc_id`,
  `analyzer_score`, and `final_score` columns before Arrow encode instead of
  relying on whatever tuple element types the processor happened to assemble.
- **Update**:
  - changed `.data/WendaoArrow/src/contracts.jl` to add
    `normalize_scoring_response(...)`, which:
    - requires `doc_id`, `analyzer_score`, and `final_score`
    - reuses the existing column-length guard
    - coerces `doc_id` to `String`
    - coerces `analyzer_score` and `final_score` to finite `Float64`
    - preserves additive columns already present on the response
  - exported that helper from `.data/WendaoArrow/src/WendaoArrow.jl`
  - updated `.data/WendaoArrow/examples/stream_scoring_server.jl`,
    `.data/WendaoArrow/examples/stream_scoring_flight_server.jl`, and
    `.data/WendaoArrow/examples/stream_metadata_server.jl` so the packaged
    scoring-style examples normalize their outbound scoring columns before
    returning
  - extended `.data/WendaoArrow/test/runtests.jl` with helper coverage for
    normalization, missing required response columns, and non-finite outbound
    score rejection
  - synchronized package docs and the active ExecPlan so the current boundary
    now states that scoring-style examples normalize their response contract
    instead of only shaping it by convention
- **Validation Executed**:
  - `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/runtests.jl`
  - `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/flight_grpcserver.jl`
  - `direnv exec . git diff --check -- .data/WendaoArrow docs/GTD/DAILY_2026_03_30.md .cache/codex/execplans/wendaoarrow_arrow_flight_adaptation.md`

### [DONE] Feature: remove the first xiuxian-daochang telegram/discord visibility drift bundle

- **Objective**: clear the first bounded `xiuxian-daochang` lib compile-front
  cluster without drifting into broader rewrites, by fixing internal
  visibility and re-export seams that no longer match the live owner paths.
- **Update**:
  - widened the live Discord session-partition handler visibility in
    `packages/rust/crates/xiuxian-daochang/src/channels/discord/runtime/managed/handlers/command_dispatch/session/partition.rs`
    to the actual `command_dispatch` owner seam so the existing parent
    re-export is legal again
  - normalized the touched Telegram command parser exports in:
    - `packages/rust/crates/xiuxian-daochang/src/channels/telegram/commands/background.rs`
    - `packages/rust/crates/xiuxian-daochang/src/channels/telegram/commands/help.rs`
    - `packages/rust/crates/xiuxian-daochang/src/channels/telegram/commands.rs`
      so crate-internal parsers are no longer re-exported as outward `pub`
  - widened the touched Telegram reply/session-memory/session-partition
    helper seams from `replies::*`-local visibility to the actual
    `runtime::jobs` consumer boundary in:
    - `.../replies/mod.rs`
    - `.../replies/session_partition.rs`
    - `.../replies/session_memory/mod.rs`
    - `.../replies/session_memory/snapshot/not_found.rs`
    - `.../replies/session_memory/snapshot/render.rs`
  - replaced the touched `super::super::super::...` imports in
    `.../command_handlers/session_commands/session_partition.rs` with
    direct `crate::...` imports
  - confirmed the landed bundle exits the compile front; the next live
    blockers now start in `llm/*`, `resolve.rs`, `runtime_agent_factory/*`,
    and `agent` private-module exposure consumed by `test_support/*`
- **Validation Executed**:
  - `direnv exec . rustfmt packages/rust/crates/xiuxian-daochang/src/channels/discord/runtime/managed/handlers/command_dispatch/session/partition.rs packages/rust/crates/xiuxian-daochang/src/channels/telegram/commands/background.rs packages/rust/crates/xiuxian-daochang/src/channels/telegram/commands/help.rs packages/rust/crates/xiuxian-daochang/src/channels/telegram/commands.rs packages/rust/crates/xiuxian-daochang/src/channels/telegram/runtime/jobs/replies/session_memory/mod.rs packages/rust/crates/xiuxian-daochang/src/channels/telegram/runtime/jobs/replies/session_memory/snapshot/not_found.rs packages/rust/crates/xiuxian-daochang/src/channels/telegram/runtime/jobs/replies/session_memory/snapshot/render.rs packages/rust/crates/xiuxian-daochang/src/channels/telegram/runtime/jobs/replies/mod.rs packages/rust/crates/xiuxian-daochang/src/channels/telegram/runtime/jobs/replies/session_partition.rs packages/rust/crates/xiuxian-daochang/src/channels/telegram/runtime/jobs/command_handlers/session_commands/session_partition.rs`
  - `direnv exec . cargo check -p xiuxian-daochang --lib`
  - `direnv exec . rg -n "super::super::" packages/rust/crates/xiuxian-daochang/src/channels/telegram/runtime/jobs/command_handlers/session_commands/session_partition.rs packages/rust/crates/xiuxian-daochang/src/channels/telegram/commands.rs packages/rust/crates/xiuxian-daochang/src/channels/telegram/commands/background.rs packages/rust/crates/xiuxian-daochang/src/channels/telegram/commands/help.rs`
  - `direnv exec . git diff --check`

### [DONE] Feature: remove the stale xiuxian-daochang llm import and owner drift bundle

- **Objective**: clear the next bounded `xiuxian-daochang` compile-front
  cluster by aligning the local `llm/*` surface to the current live
  `client/mod.rs`, LiteLLM type paths, and `xiuxian-llm` DeepSeek export
  boundaries.
- **Update**:
  - replaced the stale duplicate owner in
    `packages/rust/crates/xiuxian-daochang/src/llm/client/chat.rs` with a
    live `impl crate::llm::client::LlmClient` surface that now delegates
    LiteLLM requests through `LiteLlmRuntime` and `LiteLlmDispatchConfig`
  - aligned touched LiteLLM imports in:
    - `.../src/llm/converters.rs`
    - `.../src/llm/tools.rs`
      from the removed flat `core::types::*` path to the current split
      `chat/message/tools/content/context` surfaces
  - aligned touched DeepSeek imports in:
    - `.../src/llm/client/init.rs`
    - `.../src/llm/compat/litellm_ocr.rs`
      from `xiuxian_llm::llm::vision::*` to the live
      `xiuxian_llm::llm::vision::deepseek::*` owner path where needed
  - replaced touched `super::super::...` imports in `init.rs` with direct
    `crate::llm::*` imports
  - confirmed the landed bundle exits the compile front; the next live
    blockers now start in `resolve.rs`, `runtime_agent_factory/*`,
    `session/*`, `test_support/*`, and root outward re-export drift in
    `lib.rs`
- **Validation Executed**:
  - `direnv exec . rustfmt packages/rust/crates/xiuxian-daochang/src/llm/client/chat.rs packages/rust/crates/xiuxian-daochang/src/llm/client/init.rs packages/rust/crates/xiuxian-daochang/src/llm/compat/litellm_ocr.rs packages/rust/crates/xiuxian-daochang/src/llm/converters.rs packages/rust/crates/xiuxian-daochang/src/llm/tools.rs`
  - `direnv exec . cargo check -p xiuxian-daochang --lib`
  - `direnv exec . rg -n "super::super::" packages/rust/crates/xiuxian-daochang/src/llm/client/chat.rs packages/rust/crates/xiuxian-daochang/src/llm/client/init.rs packages/rust/crates/xiuxian-daochang/src/llm/compat/litellm_ocr.rs packages/rust/crates/xiuxian-daochang/src/llm/converters.rs packages/rust/crates/xiuxian-daochang/src/llm/tools.rs`
  - `direnv exec . git diff --check`

### [DONE] Feature: remove the stale xiuxian-daochang resolve and runtime-agent-factory owner drift bundle

- **Objective**: clear the next bounded `xiuxian-daochang` compile-front
  cluster by rebinding `resolve.rs` and `runtime_agent_factory/*` to
  crate-owned owners instead of stale bin-only or removed legacy surfaces.
- **Update**:
  - introduced the crate-owned
    `packages/rust/crates/xiuxian-daochang/src/channel_runtime.rs` seam and
    re-exported its runtime enums through
    `packages/rust/crates/xiuxian-daochang/src/lib.rs`, so
    `resolve.rs` and the bin-side CLI now share one live owner
  - switched
    `packages/rust/crates/xiuxian-daochang/src/resolve.rs`
    from the invalid `crate::cli::*` lib dependency to crate-owned exports
    and from `omni_macros::env_non_empty` to `xiuxian_macros::env_non_empty`
  - restored the missing env helpers in
    `packages/rust/crates/xiuxian-daochang/src/env_parse.rs`:
    `parse_positive_u32_from_env`,
    `parse_positive_f32_from_env`,
    and `parse_unit_f32_from_env`
  - rebound the touched
    `packages/rust/crates/xiuxian-daochang/src/runtime_agent_factory/*`
    modules to crate-owned `RuntimeSettings`, `MemoryConfig`,
    `ToolServerEntry`, `Agent`, and `shared::non_empty_env` owners instead
    of stale `omni_agent` or deeper relative-path seams
  - replaced touched relative imports in the bundle with `crate::...` paths
  - confirmed the landed bundle exits the compile front; the next live
    blockers now start in `session/*`, `test_support/*`, `lib.rs`, and
    `agent/*` private-module exposure consumed by `test_support/*`
- **Validation Executed**:
  - `direnv exec . rustfmt packages/rust/crates/xiuxian-daochang/src/channel_runtime.rs packages/rust/crates/xiuxian-daochang/src/lib.rs packages/rust/crates/xiuxian-daochang/src/cli.rs packages/rust/crates/xiuxian-daochang/src/resolve.rs packages/rust/crates/xiuxian-daochang/src/env_parse.rs packages/rust/crates/xiuxian-daochang/src/runtime_agent_factory/shared.rs packages/rust/crates/xiuxian-daochang/src/runtime_agent_factory/inference.rs packages/rust/crates/xiuxian-daochang/src/runtime_agent_factory/memory.rs packages/rust/crates/xiuxian-daochang/src/runtime_agent_factory/memory/embedding.rs packages/rust/crates/xiuxian-daochang/src/runtime_agent_factory/memory/env_overrides.rs packages/rust/crates/xiuxian-daochang/src/runtime_agent_factory/memory/runtime.rs`
  - `direnv exec . rg -n "super::super::" packages/rust/crates/xiuxian-daochang/src/resolve.rs packages/rust/crates/xiuxian-daochang/src/runtime_agent_factory -g '!**/target/**'`
  - `direnv exec . cargo check -p xiuxian-daochang --lib`
  - `direnv exec . git diff --check`

### [DONE] Feature: restore the xiuxian-daochang session owner seam

- **Objective**: clear the next bounded `xiuxian-daochang` compile-front
  cluster by restoring the missing unbounded session owner and aligning the
  remaining session-side `TurnSlot` and Valkey snapshot seams to the current
  live owners.
- **Update**:
  - restored the local
    `packages/rust/crates/xiuxian-daochang/src/session/store.rs`
    owner with a live `SessionStore` backed by in-memory state plus optional
    Valkey sync, instead of leaving `session/mod.rs` pointed at an empty
    anchor
  - widened
    `packages/rust/crates/xiuxian-daochang/src/session/mod.rs`
    to expose the internal `redis_backend` namespace to crate-internal
    consumers, which removes the stale private-module stop at the session
    boundary
  - switched touched `TurnSlot` imports in:
    - `.../src/session/redis_backend/window_store.rs`
    - `.../src/agent/session_context/types.rs`
      from `omni_window` to the live `xiuxian_window` owner
  - fixed
    `packages/rust/crates/xiuxian-daochang/src/session/redis_backend/backend.rs`
    so `message_content_max_chars` is again carried consistently through the
    backend and runtime snapshot
  - rebound the touched session tests in:
    - `.../tests/config_and_session.rs`
    - `.../tests/session_redis.rs`
      from `omni_agent` to the live `xiuxian_daochang` owner surface
  - confirmed the landed bundle exits the compile front; the next live
    blockers now start in `test_support/*`, `lib.rs`, and `agent/*`
    private-module exposure consumed by `test_support/*`
- **Validation Executed**:
  - `direnv exec . rustfmt packages/rust/crates/xiuxian-daochang/src/session/mod.rs packages/rust/crates/xiuxian-daochang/src/session/store.rs packages/rust/crates/xiuxian-daochang/src/session/redis_backend/backend.rs packages/rust/crates/xiuxian-daochang/src/session/redis_backend/window_store.rs packages/rust/crates/xiuxian-daochang/src/agent/session_context/types.rs packages/rust/crates/xiuxian-daochang/tests/config_and_session.rs packages/rust/crates/xiuxian-daochang/tests/session_redis.rs`
  - `direnv exec . cargo check -p xiuxian-daochang --lib`
  - `direnv exec . git diff --check`

### [DONE] Feature: prove empty string metadata request failures over remote Flight clients

- **Objective**: stop leaving `trace_id` / `tenant_id` ingress validation as
  a helper-only guarantee by proving that empty string metadata fails against
  real remote Flight clients.
- **Update**:
  - extended `.data/WendaoArrow/test/runtests.jl` with direct helper coverage
    for empty `tenant_id` metadata, alongside the existing empty `trace_id`
    check
  - extended `.data/WendaoArrow/test/flight_grpcserver.jl` so
    `pyarrow.flight` and native Julia both prove:
    - empty `trace_id` request metadata fails as an explicit request-metadata
      `INVALID_ARGUMENT`
    - empty `tenant_id` request metadata fails through the same request
      metadata contract path
  - synchronized package docs and the active ExecPlan so the current boundary
    now states that metadata request-ingress proof covers both typed failures
    and empty-string string-key failures
- **Validation Executed**:
  - `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/runtests.jl`
  - `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/flight_grpcserver.jl`
  - `direnv exec . git diff --check -- .data/WendaoArrow docs/GTD/DAILY_2026_03_30.md .cache/codex/execplans/wendaoarrow_arrow_flight_adaptation.md`

### [DONE] Feature: validate typed metadata values on request ingress

- **Objective**: stop letting metadata example request values such as
  `attempt_count` stay unchecked until outbound normalization by validating
  them as request metadata at ingest time.
- **Update**:
  - extended `.data/WendaoArrow/src/contracts.jl` with
    `coerce_metadata_optional_string(...)` and
    `coerce_metadata_optional_int64(...)` so metadata keys can be validated
    directly with request-metadata-specific diagnostics
  - updated
    `.data/WendaoArrow/examples/stream_metadata_server.jl`,
    `.data/WendaoArrow/examples/stream_metadata_flight_server.jl`, and
    `.data/WendaoArrow/examples/stream_metadata_bad_response_flight_server.jl`
    so metadata values are normalized on ingress before row iteration
  - extended `.data/WendaoArrow/test/runtests.jl` with direct helper coverage
    for valid/missing metadata values plus bad `trace_id` / `attempt_count`
    diagnostics, and updated the local HTTP metadata proof to use the new
    ingress helpers
  - extended `.data/WendaoArrow/test/flight_grpcserver.jl` so
    `pyarrow.flight` and native Julia both prove that invalid request
    `attempt_count` metadata now fails on the metadata Flight path as an
    explicit request-metadata `INVALID_ARGUMENT`
  - synchronized package docs and the active ExecPlan so the current boundary
    now states that typed metadata values are validated on ingress, not only
    at response normalization time
- **Validation Executed**:
  - `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/runtests.jl`
  - `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/flight_grpcserver.jl`
  - `direnv exec . git diff --check -- .data/WendaoArrow docs/GTD/DAILY_2026_03_30.md .cache/codex/execplans/wendaoarrow_arrow_flight_adaptation.md`

### [DONE] Feature: prove typed metadata response failures over remote Flight clients

- **Objective**: stop leaving typed additive response columns with only a
  happy-path remote proof by showing that bad outbound `attempt_count` data
  is also surfaced to real Flight clients as a contract error.
- **Update**:
  - added
    `.data/WendaoArrow/examples/stream_metadata_bad_response_flight_server.jl`
    as a metadata-specific bad-response Flight example that emits invalid
    outbound `attempt_count` data after a valid request is accepted
  - added
    `.data/WendaoArrow/scripts/run_stream_metadata_bad_response_flight_server.sh`
    so the new example uses the same isolated optional Flight startup path as
    the rest of the package examples
  - updated `.data/WendaoArrow/test/flight_grpcserver.jl` with:
    - a metadata bad-response Flight server launcher
    - a `pyarrow.flight` regression that captures the remote
      `INVALID_ARGUMENT` failure text for bad `attempt_count`
    - a native Julia regression that asserts the same response-side
      `grpc_message` for bad `attempt_count`
  - synchronized package docs and the active ExecPlan so the current boundary
    now states that typed additive metadata columns have remote bad-response
    proof, not only present/missing happy-path coverage
- **Validation Executed**:
  - `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/flight_grpcserver.jl`
  - `direnv exec . git diff --check -- .data/WendaoArrow docs/GTD/DAILY_2026_03_30.md .cache/codex/execplans/wendaoarrow_arrow_flight_adaptation.md`

### [DONE] Feature: prove scoring-response normalization failures over remote Flight clients

- **Objective**: verify that scoring-style response normalization failures stay
  visible to real Flight clients as contract errors, instead of collapsing
  into a generic processor failure once the request path itself succeeds.
- **Update**:
  - added
    `.data/WendaoArrow/examples/stream_scoring_bad_response_flight_server.jl`
    as a dedicated Flight example that accepts a valid scoring request and
    then fails inside `normalize_scoring_response(...)` with a non-finite
    `analyzer_score`
  - added
    `.data/WendaoArrow/scripts/run_stream_scoring_bad_response_flight_server.sh`
    so the bad-response example is runnable through the same isolated Flight
    startup path as the happy-path scoring example
  - updated `.data/WendaoArrow/test/flight_grpcserver.jl` with:
    - a generic Flight server launcher helper for alternate startup scripts
    - a `pyarrow.flight` regression that asserts the bad-response example
      returns `grpc-status: 3` with the outbound `analyzer_score` diagnostic
    - a native Julia `Arrow.Flight.Client` regression that asserts the same
      response-side diagnostic via `grpc_message`
  - synchronized package docs and the active ExecPlan so the current Flight
    boundary now includes explicit remote proof for response-side scoring
    normalization failures
- **Validation Executed**:
  - `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/runtests.jl`
  - `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/flight_grpcserver.jl`
  - `direnv exec . git diff --check -- .data/WendaoArrow docs/GTD/DAILY_2026_03_30.md .cache/codex/execplans/wendaoarrow_arrow_flight_adaptation.md`

### [DONE] Feature: validate `trace_id` as an optional additive scoring-response column

- **Objective**: stop treating `trace_id` as an unvalidated passthrough field
  in the metadata example by making its nullability and row-count alignment
  explicit at the scoring-response helper layer.
- **Update**:
  - changed `.data/WendaoArrow/src/contracts.jl` so
    `normalize_scoring_response(...)` now accepts
    `optional_string_columns=(...)` and validates those additive columns as
    length-aligned optional strings
  - added `coerce_optional_string(...)`, which allows `missing` but rejects
    empty strings and non-string values with row/column diagnostics
  - updated `.data/WendaoArrow/examples/stream_metadata_server.jl` so
    `trace_id` is now normalized through
    `normalize_scoring_response(...; optional_string_columns=("trace_id",))`
  - extended `.data/WendaoArrow/test/runtests.jl` with helper coverage for
    valid `missing`/string `trace_id` values, bad `trace_id` types, empty
    `trace_id` values, mismatched `trace_id` length, and a local HTTP handler
    proof that schema metadata is re-emitted as a normalized response column
  - synchronized package docs and the active ExecPlan so the current boundary
    now states that metadata-style additive response columns are validated
    explicitly rather than preserved by convention
- **Validation Executed**:
  - `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/runtests.jl`
  - `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/flight_grpcserver.jl`
  - `direnv exec . git diff --check -- .data/WendaoArrow docs/GTD/DAILY_2026_03_30.md .cache/codex/execplans/wendaoarrow_arrow_flight_adaptation.md`

### [DONE] Feature: prove additive `trace_id` response columns over remote Flight clients

- **Objective**: move `trace_id` from a local metadata example into a real
  remote Flight proof so additive response columns are demonstrated across
  actual client boundaries, not only helper and local handler tests.
- **Update**:
  - added
    `.data/WendaoArrow/examples/stream_metadata_flight_server.jl` as a
    stream-first Flight example that re-emits request metadata `trace_id` as
    an additive normalized response column
  - added
    `.data/WendaoArrow/scripts/run_stream_metadata_flight_server.sh` so that
    metadata example is runnable through the same isolated Flight startup path
    as the packaged scoring examples
  - updated `.data/WendaoArrow/test/flight_grpcserver.jl` with:
    - a metadata Flight server launcher
    - `pyarrow.flight` and native Julia regressions proving `trace_id`
      round-trips when present
    - matching regressions proving that missing request metadata produces
      remote null/missing `trace_id` values rather than dropping the column
  - synchronized package docs and the active ExecPlan so the current
    Flight boundary now includes remote additive-column proof for `trace_id`
- **Validation Executed**:
  - `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/flight_grpcserver.jl`
  - `direnv exec . git diff --check -- .data/WendaoArrow docs/GTD/DAILY_2026_03_30.md .cache/codex/execplans/wendaoarrow_arrow_flight_adaptation.md`

### [DONE] Feature: widen additive metadata response proof to `tenant_id`

- **Objective**: stop treating the metadata Flight path as a `trace_id`-only
  special case by proving that a second additive metadata column can follow
  the same local normalization and remote round-trip contract.
- **Update**:
  - updated
    `.data/WendaoArrow/examples/stream_metadata_server.jl` and
    `.data/WendaoArrow/examples/stream_metadata_flight_server.jl` so the
    metadata examples now re-emit both `trace_id` and `tenant_id` as
    normalized additive response columns
  - extended `.data/WendaoArrow/test/runtests.jl` with:
    - multi-column optional-string helper coverage
    - local HTTP metadata response proof for both `trace_id` and `tenant_id`
  - extended `.data/WendaoArrow/test/flight_grpcserver.jl` so
    `pyarrow.flight` and native Julia now prove:
    - both additive metadata columns round-trip when present
    - both columns remain present as remote `null` / `missing` when request
      metadata omits them
  - synchronized package docs and the active ExecPlan so the current boundary
    now states that metadata-style additive response proof covers more than a
    single hard-coded column
- **Validation Executed**:
  - `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/runtests.jl`
  - `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/flight_grpcserver.jl`
  - `direnv exec . git diff --check -- .data/WendaoArrow docs/GTD/DAILY_2026_03_30.md .cache/codex/execplans/wendaoarrow_arrow_flight_adaptation.md`

### [DONE] Feature: widen direct `Arrow.tobuffer` to targeted multi-partition string/binary streams

- **Objective**: carry the already-landed direct single-partition `tobuffer`
  path into the production multi-record-batch case, but only where the
  benchmark signal is strong enough to justify extra code shape.
- **Update**:
  - extended `.data/arrow-julia/src/utils.jl` with a narrow inspectable
    multi-partition direct stream path that:
    - peeks the first partition
    - reuses direct record-batch emission across remaining partitions
    - keeps the existing writer path for all non-target shapes
  - restricted the widening to:
    - single-column top-level `AbstractString`
    - single-column top-level non-missing `Base.CodeUnits`
  - explicitly kept these on the old writer path:
    - multi-column partitions
    - `Arrow.Map`-style inputs
    - dictionary-encoded top-level columns
    - missing-binary/code-units partitions
  - extended `.data/arrow-julia/test/runtests.jl` with byte-equivalence
    coverage for:
    - multi-partition missing-string
    - multi-partition binary
    - multi-partition missing-binary
- **Validation Executed**:
  - `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_partition_direct_stream_ab.jl > .cache/agent/tmp/arrow-julia-partition-direct-stream-ab-postpatch.tsv`
    - key post-change results:
      - `500000,string,20,current_stream = 6.043 ms`
      - `500000,string,20,direct_stream = 6.043 ms`
      - `500000,binary,20,current_stream = 6.178 ms`
      - `500000,binary,20,direct_stream = 6.248 ms`
  - `direnv exec . julia --project=.data/arrow-julia/test -e 'using Pkg; Pkg.instantiate(); Pkg.test("Arrow")'`
    - result: `Arrow | 66126/66126 | 3m44.6s`
  - `direnv exec . git diff --check -- .data/arrow-julia/src/utils.jl .data/arrow-julia/test/runtests.jl docs/GTD/DAILY_2026_03_30.md .cache/agent/execplans/arrow_julia_direct_tobuffer_fastpath.md`

### [DONE] Feature: reuse targeted direct multi-partition streaming in `Arrow.write(io, ...)`

- **Objective**: stop paying the full legacy `Writer` orchestration cost for
  the same multi-partition string/binary shapes that were already proven to
  benefit from direct streaming in `Arrow.tobuffer(...)`.
- **Update**:
  - extended `.data/arrow-julia/src/write.jl` so
    `Arrow.write(io, Tables.partitioner(...))` now reuses the same narrow
    eligible direct stream path as `Arrow.tobuffer(...)`
  - kept the scope intentionally narrow:
    - single-column top-level `AbstractString`
    - single-column top-level non-missing `Base.CodeUnits`
  - kept these on the legacy writer route:
    - multi-column partitions
    - map-heavy inputs
    - dictionary-encoded top-level columns
    - missing-binary/code-units partitions
  - refactored the shared direct stream emission into
    `.data/arrow-julia/src/utils.jl`
  - fixed a transient local-buffer shadowing bug in `_directstreamwrite!`
    before validation; eligible cases now return/write full stream bytes again
  - extended `.data/arrow-julia/test/runtests.jl` with a multi-partition map
    byte-equivalence regression so the fallback boundary is locked
  - updated `.data/arrow-julia/README.md` so the package surface records that
    targeted partitioned `Arrow.write(io, ...)` now shares the direct stream
    path
- **Validation Executed**:
  - `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_partition_writeio_ab.jl > .cache/agent/tmp/arrow-julia-partition-writeio-ab-legacy.tsv`
    - same-process A/B highlights:
      - `100000,string,20`: `current_write 1.105 ms` vs `legacy_write 1.743 ms`
      - `100000,binary,20`: `current_write 1.132 ms` vs `legacy_write 2.116 ms`
      - `500000,string,20`: `current_write 7.626 ms` vs `legacy_write 11.413 ms`
      - `500000,binary,20`: `current_write 7.473 ms` vs `legacy_write 12.355 ms`
  - `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_partition_direct_stream_ab.jl > .cache/agent/tmp/arrow-julia-partition-direct-stream-ab-postfix.tsv`
  - `direnv exec . julia --project=.data/arrow-julia/test -e 'using Pkg; Pkg.instantiate(); Pkg.test("Arrow")'`
    - result: `Arrow | 66127/66127 | 3m36.8s`
  - `direnv exec . git diff --check -- .data/arrow-julia/src/utils.jl .data/arrow-julia/src/write.jl .data/arrow-julia/test/runtests.jl .data/arrow-julia/README.md docs/GTD/DAILY_2026_03_30.md .cache/agent/execplans/arrow_julia_direct_tobuffer_fastpath.md`

### [DONE] Feature: widen additive metadata response proof to typed `attempt_count`

- **Objective**: stop limiting metadata-style additive response columns to
  optional strings by proving that a typed optional integer column can use
  the same normalization and remote round-trip surface.
- **Update**:
  - extended `.data/WendaoArrow/src/contracts.jl` so
    `normalize_scoring_response(...)` now accepts
    `optional_int64_columns=(...)` and normalizes them through the new
    `coerce_optional_int64(...)` helper
  - updated
    `.data/WendaoArrow/examples/stream_metadata_server.jl` and
    `.data/WendaoArrow/examples/stream_metadata_flight_server.jl` so the
    metadata examples now re-emit `attempt_count` alongside `trace_id` and
    `tenant_id`
  - extended `.data/WendaoArrow/test/runtests.jl` with:
    - helper coverage for valid/missing string-encoded integer metadata
    - bad `attempt_count` diagnostics
    - local HTTP metadata response proof that `attempt_count` is normalized
      to `Int64`
  - extended `.data/WendaoArrow/test/flight_grpcserver.jl` so
    `pyarrow.flight` and native Julia both prove:
    - `attempt_count` round-trips as a real integer column when present
    - `attempt_count` remains present as remote `null` / `missing` when
      omitted from request metadata
  - synchronized package docs and the active ExecPlan so the current boundary
    now states that metadata-style additive response proof includes typed
    columns, not only optional strings
- **Validation Executed**:
  - `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/runtests.jl`
  - `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/flight_grpcserver.jl`
  - `direnv exec . git diff --check -- .data/WendaoArrow docs/GTD/DAILY_2026_03_30.md .cache/codex/execplans/wendaoarrow_arrow_flight_adaptation.md`

### [DONE] Feature: consolidate metadata request-ingress validation behind `normalize_metadata_values(...)`

- **Objective**: stop open-coding additive request-metadata coercion across
  the metadata HTTP and Flight startup examples so the ingress contract stays
  aligned on one helper surface.
- **Update**:
  - extended `.data/WendaoArrow/src/contracts.jl` with
    `normalize_metadata_values(...)`, which validates duplicate keys across
    type groups and normalizes shared string/int64 metadata keys through the
    existing coercion helpers
  - exported the helper from `.data/WendaoArrow/src/WendaoArrow.jl`
  - updated:
    - `.data/WendaoArrow/examples/stream_metadata_server.jl`
    - `.data/WendaoArrow/examples/stream_metadata_flight_server.jl`
    - `.data/WendaoArrow/examples/stream_metadata_bad_response_flight_server.jl`
      so those startup examples now read `trace_id`, `tenant_id`, and
      `attempt_count` from one normalized metadata map instead of hand-written
      per-key coercion
  - extended `.data/WendaoArrow/test/runtests.jl` with:
    - positive helper coverage for shared metadata normalization
    - duplicate-key-across-type-groups regression coverage
    - local HTTP metadata response proof that now exercises the shared helper
  - synchronized `.data/WendaoArrow/README.md` and the active ExecPlan so the
    package surface now records that metadata ingress validation is shared
    across the metadata HTTP and Flight entrypoints
- **Validation Executed**:
  - `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/runtests.jl`
  - `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/flight_grpcserver.jl`
  - `direnv exec . git diff --check -- .data/WendaoArrow docs/GTD/DAILY_2026_03_30.md .cache/codex/execplans/wendaoarrow_arrow_flight_adaptation.md`

### [DONE] Feature: extend shared metadata contracts to optional `cache_hit::Bool`

- **Objective**: widen the metadata-style additive column surface beyond
  strings and `Int64` so one more typed request/response metadata key is
  proven end to end.
- **Update**:
  - extended `.data/WendaoArrow/src/contracts.jl` so:
    - `normalize_scoring_response(...)` now accepts
      `optional_bool_columns=(...)`
    - `normalize_metadata_values(...)` now accepts `bool_keys=(...)`
    - new `coerce_optional_bool(...)` /
      `coerce_metadata_optional_bool(...)` helpers normalize optional Bool
      values from column data and request metadata
  - exported `coerce_optional_bool(...)` from
    `.data/WendaoArrow/src/WendaoArrow.jl`
  - updated:
    - `.data/WendaoArrow/examples/stream_metadata_server.jl`
    - `.data/WendaoArrow/examples/stream_metadata_flight_server.jl`
    - `.data/WendaoArrow/examples/stream_metadata_bad_response_flight_server.jl`
      so the packaged metadata examples now carry `cache_hit` alongside
      `trace_id`, `tenant_id`, and `attempt_count`
  - extended `.data/WendaoArrow/test/runtests.jl` with:
    - local Bool normalization coverage for scoring-response helpers
    - request-metadata helper coverage for valid/missing/invalid `cache_hit`
    - local HTTP metadata response proof for `cache_hit`
  - extended `.data/WendaoArrow/test/flight_grpcserver.jl` so:
    - `pyarrow.flight` and native Julia both prove `cache_hit` round-trips
      as a typed optional Bool response column
    - both clients also prove invalid request metadata for `cache_hit`
      surfaces as `INVALID_ARGUMENT`
    - the Flight startup wait window now tolerates cold temporary
      environment compilation instead of failing at the old 30-second edge
  - synchronized package docs and the active ExecPlan so the metadata
    contract now explicitly includes a Bool typed key
- **Validation Executed**:
  - `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/runtests.jl`
  - `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/flight_grpcserver.jl`
  - `direnv exec . git diff --check -- .data/WendaoArrow docs/GTD/DAILY_2026_03_30.md .cache/codex/execplans/wendaoarrow_arrow_flight_adaptation.md`

### [DONE] Feature: extend shared metadata contracts to optional `cache_score::Float64`

- **Objective**: widen the metadata-style additive column surface to one
  finite floating-point key so the shared metadata contract is no longer
  limited to strings, integers, and booleans.
- **Update**:
  - extended `.data/WendaoArrow/src/contracts.jl` so:
    - `normalize_scoring_response(...)` now accepts
      `optional_float64_columns=(...)`
    - `normalize_metadata_values(...)` now accepts `float64_keys=(...)`
    - new `coerce_optional_float64(...)` /
      `coerce_metadata_optional_float64(...)` helpers normalize optional
      finite `Float64` values from column data and request metadata
  - exported `coerce_optional_float64(...)` from
    `.data/WendaoArrow/src/WendaoArrow.jl`
  - updated:
    - `.data/WendaoArrow/examples/stream_metadata_server.jl`
    - `.data/WendaoArrow/examples/stream_metadata_flight_server.jl`
    - `.data/WendaoArrow/examples/stream_metadata_bad_response_flight_server.jl`
      so the packaged metadata examples now carry `cache_score` alongside
      `trace_id`, `tenant_id`, `attempt_count`, and `cache_hit`
  - extended `.data/WendaoArrow/test/runtests.jl` with:
    - local Float64 normalization coverage for scoring-response helpers
    - request-metadata helper coverage for valid/missing/invalid `cache_score`
    - local HTTP metadata response proof for `cache_score`
  - extended `.data/WendaoArrow/test/flight_grpcserver.jl` so:
    - `pyarrow.flight` and native Julia both prove `cache_score`
      round-trips as a typed optional Float64 response column
    - both clients also prove invalid request metadata for `cache_score`
      surfaces as `INVALID_ARGUMENT`
  - synchronized package docs and the active ExecPlan so the metadata
    contract now explicitly includes a Float64 typed key
- **Validation Executed**:
  - `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/runtests.jl`
  - `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/flight_grpcserver.jl`
  - `direnv exec . git diff --check -- .data/WendaoArrow docs/GTD/DAILY_2026_03_30.md .cache/codex/execplans/wendaoarrow_arrow_flight_adaptation.md`

### [DONE] Feature: extend shared metadata contracts to optional `cache_generated_at::DateTime`

- **Objective**: widen the metadata-style additive column surface to one
  temporal key so the shared metadata contract is no longer limited to
  strings, numeric scalars, and booleans.
- **Update**:
  - extended `.data/WendaoArrow/src/contracts.jl` so:
    - `normalize_scoring_response(...)` now accepts
      `optional_datetime_columns=(...)`
    - `normalize_metadata_values(...)` now accepts `datetime_keys=(...)`
    - new `coerce_optional_datetime(...)` /
      `coerce_metadata_optional_datetime(...)` helpers normalize optional
      ISO8601 `DateTime` values from column data and request metadata
  - added `Dates` to `.data/WendaoArrow/Project.toml` and exported
    `coerce_optional_datetime(...)` from
    `.data/WendaoArrow/src/WendaoArrow.jl`
  - updated:
    - `.data/WendaoArrow/examples/stream_metadata_server.jl`
    - `.data/WendaoArrow/examples/stream_metadata_flight_server.jl`
    - `.data/WendaoArrow/examples/stream_metadata_bad_response_flight_server.jl`
      so the packaged metadata examples now carry `cache_generated_at`
      alongside `trace_id`, `tenant_id`, `attempt_count`, `cache_hit`, and
      `cache_score`
  - extended `.data/WendaoArrow/test/runtests.jl` with:
    - local DateTime normalization coverage for scoring-response helpers
    - request-metadata helper coverage for valid/missing/invalid
      `cache_generated_at`
    - local HTTP metadata response proof for `cache_generated_at`
  - extended `.data/WendaoArrow/test/flight_grpcserver.jl` so:
    - `pyarrow.flight` and native Julia both prove `cache_generated_at`
      round-trips as a typed optional DateTime response column
    - both clients also prove invalid request metadata for
      `cache_generated_at` surfaces as `INVALID_ARGUMENT`
    - the `pyarrow.flight` JSON probe now renders DateTime response values as
      ISO8601 strings before assertion
  - synchronized package docs and the active ExecPlan so the metadata
    contract now explicitly includes a temporal typed key
- **Validation Executed**:
  - `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/runtests.jl`
  - `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/flight_grpcserver.jl`
  - `direnv exec . git diff --check -- .data/WendaoArrow docs/GTD/DAILY_2026_03_30.md .cache/codex/execplans/wendaoarrow_arrow_flight_adaptation.md`

### [DONE] Feature: extend shared metadata contracts to constrained enum `cache_backend`

- **Objective**: widen the shared metadata contract to one constrained enum
  key so additive metadata is no longer limited to free strings and scalar
  typed values.
- **Update**:
  - extended `.data/WendaoArrow/src/contracts.jl` so:
    - `normalize_scoring_response(...)` now accepts
      `optional_enum_string_columns=(...)`
    - `normalize_metadata_values(...)` now accepts
      `enum_string_keys=(...)`
    - new `coerce_optional_enum_string(...)` /
      `coerce_metadata_optional_enum_string(...)` helpers normalize optional
      enum-string values from column data and request metadata
  - exported `coerce_optional_enum_string(...)` from
    `.data/WendaoArrow/src/WendaoArrow.jl`
  - updated:
    - `.data/WendaoArrow/examples/stream_metadata_server.jl`
    - `.data/WendaoArrow/examples/stream_metadata_flight_server.jl`
    - `.data/WendaoArrow/examples/stream_metadata_bad_response_flight_server.jl`
      so the packaged metadata examples now carry `cache_backend` alongside
      `trace_id`, `tenant_id`, `attempt_count`, `cache_hit`, `cache_score`,
      and `cache_generated_at`
  - extended `.data/WendaoArrow/test/runtests.jl` with:
    - local enum-string normalization coverage for scoring-response helpers
    - request-metadata helper coverage for valid/missing/invalid
      `cache_backend`
    - local HTTP metadata response proof for `cache_backend`
  - extended `.data/WendaoArrow/test/flight_grpcserver.jl` so:
    - `pyarrow.flight` and native Julia both prove `cache_backend`
      round-trips as an optional constrained enum response column
    - both clients also prove invalid request metadata for `cache_backend`
      surfaces as `INVALID_ARGUMENT`
  - synchronized package docs and the active ExecPlan so the shared metadata
    contract now explicitly includes a constrained enum key
- **Validation Executed**:
  - `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/runtests.jl`
  - `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/flight_grpcserver.jl`
  - `direnv exec . git diff --check -- .data/WendaoArrow docs/GTD/DAILY_2026_03_30.md .cache/codex/execplans/wendaoarrow_arrow_flight_adaptation.md`

### [DONE] Feature: prove enum metadata response failures over remote Flight clients

- **Objective**: stop leaving constrained enum additive response columns with
  only local helper coverage by proving that bad outbound enum data reaches
  real Flight clients as explicit contract failures.
- **Update**:
  - added `.data/WendaoArrow/examples/stream_metadata_bad_enum_response_flight_server.jl`
    as a metadata-specific bad-response Flight example that accepts valid
    request metadata and then emits invalid outbound `cache_backend` data
  - added
    `.data/WendaoArrow/scripts/run_stream_metadata_bad_enum_response_flight_server.sh`
    so the enum bad-response example is runnable through the same isolated
    Flight startup path as the other packaged examples
  - extended `.data/WendaoArrow/test/flight_grpcserver.jl` with:
    - a `pyarrow.flight` regression that asserts invalid outbound
      `cache_backend` data surfaces as `INVALID_ARGUMENT`
    - a native Julia regression that asserts the same status and exact
      `grpc_message` for bad `cache_backend`
  - synchronized package docs and the active ExecPlan so the metadata
    response-failure boundary now explicitly includes constrained enum
    additive columns, not only typed scalar columns
- **Validation Executed**:
  - `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/flight_grpcserver.jl`
  - `direnv exec . git diff --check -- .data/WendaoArrow docs/GTD/DAILY_2026_03_30.md .cache/codex/execplans/wendaoarrow_arrow_flight_adaptation.md`

### [DONE] Feature: extend shared metadata contracts to constrained enum `cache_scope`

- **Objective**: prove that the shared metadata enum surface can carry more
  than one constrained key, instead of treating `cache_backend` as a
  one-off special case.
- **Update**:
  - updated:
    - `.data/WendaoArrow/examples/stream_metadata_server.jl`
    - `.data/WendaoArrow/examples/stream_metadata_flight_server.jl`
    - `.data/WendaoArrow/examples/stream_metadata_bad_response_flight_server.jl`
    - `.data/WendaoArrow/examples/stream_metadata_bad_enum_response_flight_server.jl`
      so the packaged metadata examples now also carry
      `cache_scope ∈ {request, tenant, global}` alongside the existing typed
      and enum metadata keys
  - extended `.data/WendaoArrow/test/runtests.jl` with:
    - local scoring-response normalization coverage for valid/missing
      `cache_scope`
    - invalid response-column diagnostics for bad `cache_scope`
    - request-metadata helper coverage for valid/missing/invalid
      `cache_scope`
    - local HTTP metadata response proof for `cache_scope`
  - extended `.data/WendaoArrow/test/flight_grpcserver.jl` so:
    - `pyarrow.flight` and native Julia both prove `cache_scope`
      round-trips as an optional constrained enum response column
    - both clients also prove invalid request metadata for `cache_scope`
      surfaces as `INVALID_ARGUMENT`
  - synchronized package docs and the active ExecPlan so the metadata enum
    surface now explicitly covers more than one constrained key
- **Validation Executed**:
  - `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/runtests.jl`
  - `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/flight_grpcserver.jl`
  - `direnv exec . git diff --check -- .data/WendaoArrow docs/GTD/DAILY_2026_03_30.md .cache/codex/execplans/wendaoarrow_arrow_flight_adaptation.md`

### [DONE] Feature: prove `cache_scope` response failures over remote Flight clients

- **Objective**: stop leaving the second constrained enum key with only
  local response-column coverage by proving that bad outbound `cache_scope`
  data reaches real Flight clients as explicit contract failures.
- **Update**:
  - added `.data/WendaoArrow/examples/stream_metadata_bad_scope_response_flight_server.jl`
    as a metadata-specific bad-response Flight example that accepts valid
    request metadata and then emits invalid outbound `cache_scope` data
  - added
    `.data/WendaoArrow/scripts/run_stream_metadata_bad_scope_response_flight_server.sh`
    so the `cache_scope` bad-response example is runnable through the same
    isolated Flight startup path as the other packaged examples
  - extended `.data/WendaoArrow/test/flight_grpcserver.jl` with:
    - a `pyarrow.flight` regression that asserts invalid outbound
      `cache_scope` data surfaces as `INVALID_ARGUMENT`
    - a native Julia regression that asserts the same status and exact
      `grpc_message` for bad `cache_scope`
  - synchronized package docs and the active ExecPlan so the metadata
    response-failure boundary now explicitly includes more than one
    constrained enum key
- **Validation Executed**:
  - `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/flight_grpcserver.jl`
  - `direnv exec . git diff --check -- .data/WendaoArrow docs/GTD/DAILY_2026_03_30.md .cache/codex/execplans/wendaoarrow_arrow_flight_adaptation.md`

### [DONE] Feature: extend metadata enum proofs beyond cache keys with `ranking_strategy`

- **Objective**: prove that the shared constrained-enum metadata contract is
  not cache-specific by landing a non-cache enum key with local and remote
  coverage for happy-path, request-invalid, and response-invalid boundaries.
- **Update**:
  - extended the packaged metadata HTTP / Flight examples so
    `ranking_strategy` now round-trips as an optional constrained enum
    additive column with allowed values `lexical | semantic | hybrid`
  - added `.data/WendaoArrow/examples/stream_metadata_bad_strategy_response_flight_server.jl`
    and `.data/WendaoArrow/scripts/run_stream_metadata_bad_strategy_response_flight_server.sh`
    so invalid outbound `ranking_strategy` data has its own runnable Flight
    failure path
  - extended `.data/WendaoArrow/test/runtests.jl` with local helper coverage
    for valid and invalid `ranking_strategy` request / response metadata and
    updated the HTTP metadata response proof
  - extended `.data/WendaoArrow/test/flight_grpcserver.jl` so
    `pyarrow.flight` and native Julia now both prove:
    - `ranking_strategy` round-trips with present and missing values
    - invalid request metadata for `ranking_strategy` surfaces as
      `INVALID_ARGUMENT`
    - invalid outbound `ranking_strategy` response data surfaces as
      `INVALID_ARGUMENT`
  - synchronized package docs and the active ExecPlan so the metadata enum
    contract now explicitly covers a non-cache constrained key
- **Validation Executed**:
  - `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/runtests.jl`
  - `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/flight_grpcserver.jl`
  - `direnv exec . git diff --check -- .data/WendaoArrow docs/GTD/DAILY_2026_03_30.md .cache/codex/execplans/wendaoarrow_arrow_flight_adaptation.md`

### [DONE] Feature: land Julia Enum extension types in `arrow-julia` and wire `ranking_strategy` to typed enums

- **Objective**: stop leaving `ranking_strategy` as a constrained string-only
  contract in WendaoArrow by giving vendored `arrow-julia` a first-class Base
  Julia `Enum` extension path and consuming it in the downstream metadata
  HTTP/Flight proofs.
- **Update**:
  - added generic Base Julia `Enum` support to
    `.data/arrow-julia/src/ArrowTypes/src/ArrowTypes.jl`, including:
    - basetype-backed serialization through `ArrowTypes.ArrowType(::Type{<:Enum})`
      and `ArrowTypes.toarrow(...)`
    - `JuliaLang.Enum` extension labeling plus metadata that records the
      qualified Julia type path and label/value mapping
    - deserialization hooks that reconstruct the original enum type when the
      same Julia type is available at read time
  - extended `.data/arrow-julia/src/ArrowTypes/test/tests.jl` and
    `.data/arrow-julia/test/runtests.jl` with trait-level and end-to-end
    regressions proving:
    - native Julia reads reconstruct the enum type
    - `convert=false` continues to expose the primitive storage type
  - added `.data/WendaoArrow/src/ranking_strategy.jl` and upgraded
    `.data/WendaoArrow/src/contracts.jl` so:
    - request metadata normalization now supports typed enum keys through
      `enum_keys=(...)`
    - response normalization now supports typed enum columns through
      `optional_enum_columns=(...)`
    - `ranking_strategy` now normalizes to `WendaoArrow.RankingStrategy`
      instead of remaining a constrained string column in native Julia flows
  - upgraded the metadata examples and regressions so:
    - native Julia HTTP / Flight clients receive typed
      `WendaoArrow.RankingStrategy` values
    - `pyarrow.flight` sees the primitive integer storage values together
      with `JuliaLang.Enum` field metadata
    - invalid request / response `ranking_strategy` values still surface as
      explicit `INVALID_ARGUMENT`
  - synchronized Arrow.jl and WendaoArrow docs plus the active ExecPlan so
    the package narrative now reflects the native-Julia typed-enum /
    cross-language primitive-storage split
- **Validation Executed**:
  - `direnv exec . julia --project=.data/arrow-julia/src/ArrowTypes -e 'using Pkg; Pkg.instantiate(); Pkg.test()'`
  - `direnv exec . julia --project=.data/arrow-julia/test -e 'using Pkg; Pkg.instantiate(); Pkg.develop(PackageSpec(path=".data/arrow-julia")); Pkg.develop(PackageSpec(path=".data/arrow-julia/src/ArrowTypes")); include(".data/arrow-julia/test/runtests.jl")'`
  - `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/runtests.jl`
  - `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/flight_grpcserver.jl`

### [DONE] Feature: promote `cache_backend` and `cache_scope` to typed Julia enums

- **Objective**: stop leaving cache metadata enums half-upgraded after the
  `ranking_strategy` typed-enum work by moving `cache_backend` and
  `cache_scope` onto the same native-Julia enum path.
- **Update**:
  - extended `.data/WendaoArrow/src/ranking_strategy.jl` with
    `WendaoArrow.CacheBackend` and `WendaoArrow.CacheScope`, keeping the same
    shared enum-definition surface as `WendaoArrow.RankingStrategy`
  - upgraded `.data/WendaoArrow/examples/stream_metadata_server.jl` and the
    matching Flight startup / bad-response examples so request normalization
    now consumes:
    - `cache_backend` as `WendaoArrow.CacheBackend`
    - `cache_scope` as `WendaoArrow.CacheScope`
    - `ranking_strategy` as `WendaoArrow.RankingStrategy`
  - upgraded response normalization so native Julia HTTP / Flight flows now
    emit typed enum columns for `cache_backend`, `cache_scope`, and
    `ranking_strategy` instead of leaving the cache keys as constrained
    strings
  - extended `.data/WendaoArrow/test/runtests.jl` and
    `.data/WendaoArrow/test/flight_grpcserver.jl` so:
    - native Julia sees typed `CacheBackend` / `CacheScope` values
    - `pyarrow.flight` sees primitive `Int32` storage values plus
      `JuliaLang.Enum` field metadata for all three enum columns
    - invalid request / response `cache_backend` and `cache_scope` values
      still surface as explicit `INVALID_ARGUMENT`
  - synchronized package docs and the active ExecPlan so the package
    narrative no longer describes cache enums as constrained strings
- **Validation Executed**:
  - `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/runtests.jl`
  - `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/flight_grpcserver.jl`
  - `direnv exec . git diff --check -- .data/WendaoArrow docs/GTD/DAILY_2026_03_30.md .cache/codex/execplans/wendaoarrow_arrow_flight_adaptation.md`

### [DONE] Feature: retire `xiuxian-daochang` runtime-agent-factory owner drift

- **Objective**: remove the duplicate `runtime_agent_factory/*` memory-runtime
  owner so the crate-health remediation lane can move below builder/runtime
  configuration drift.
- **Update**:
  - replaced the duplicate
    `packages/rust/crates/xiuxian-daochang/src/runtime_agent_factory/memory/runtime.rs`
    owner with the live runtime-settings applicator for `MemoryConfig`
  - kept memory runtime inspection on the canonical
    `packages/rust/crates/xiuxian-daochang/src/agent/memory_state.rs` owner
    instead of widening `Agent` field visibility
  - updated runtime embedding negotiation so
    `EmbeddingBackendKind::MistralSdk` is handled explicitly and no longer
    leaves a non-exhaustive branch in
    `packages/rust/crates/xiuxian-daochang/src/runtime_agent_factory/inference.rs`
  - confirmed the compile front no longer stops in `runtime_agent_factory/*`
    and now starts deeper in:
    - `agent/bootstrap/zhixing.rs`
    - `agent/turn_execution/react_loop/*`
    - `session/bounded_store/window_ops/*`
- **Validation Executed**:
  - `direnv exec . rustfmt packages/rust/crates/xiuxian-daochang/src/runtime_agent_factory/memory/runtime.rs packages/rust/crates/xiuxian-daochang/src/runtime_agent_factory/inference.rs`
  - `direnv exec . cargo check -p xiuxian-daochang --lib --message-format short`
