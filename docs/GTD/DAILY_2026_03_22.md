---
type: knowledge
metadata:
  title: "GTD: Sovereign Nexus (2026-03-22)"
---

# GTD: Sovereign Nexus (2026-03-22)

## Current Delivery

### [IN-PROGRESS] Feature: Wendao Deep Wiki Engine

- **Objective**: Turn Stage-1 repository truth plus Stage-2 projected pages into a deterministic deep-wiki planning kernel before any LLM-backed generation loop is introduced.
- **Status**: [Docs Retrieval Context Slice Landed] The governing execution records are `[[.cache/codex/execplans/wendao-deep-wiki-gap-report.md]]`, `[[.cache/codex/execplans/wendao-deep-wiki-gap-surface.md]]`, `[[.cache/codex/execplans/wendao-deep-wiki-docs-gap-surface.md]]`, `[[.cache/codex/execplans/wendao-deep-wiki-docs-search-surface.md]]`, `[[.cache/codex/execplans/wendao-deep-wiki-docs-retrieval-surface.md]]`, `[[.cache/codex/execplans/wendao-deep-wiki-docs-retrieval-context-surface.md]]`, `[[.cache/codex/execplans/wendao-deep-wiki-docs-page-surface.md]]`, `[[.cache/codex/execplans/wendao-deep-wiki-docs-family-context-surface.md]]`, `[[.cache/codex/execplans/wendao-deep-wiki-docs-family-search-surface.md]]`, `[[.cache/codex/execplans/wendao-deep-wiki-docs-family-cluster-surface.md]]`, `[[.cache/codex/execplans/wendao-deep-wiki-docs-navigation-surface.md]]`, and `[[.cache/codex/execplans/wendao-deep-wiki-docs-navigation-search-surface.md]]`, and the package roadmap sync is `[[packages/rust/crates/xiuxian-wendao/docs/06_roadmap/403_document_projection_and_retrieval_enhancement.md]]`. The deterministic planner-facing docs namespace now exposes `GET /api/docs/projected-gap-report?repo=<id>`, `GET /api/docs/search?repo=<id>&query=<text>&kind=<family>&limit=<n>`, `GET /api/docs/retrieval?repo=<id>&query=<text>&kind=<family>&limit=<n>`, `GET /api/docs/retrieval-context?repo=<id>&page_id=<stable-id>&node_id=<stable-node-id?>&related_limit=<n>`, `GET /api/docs/page?repo=<id>&page_id=<stable-id>`, `GET /api/docs/family-context?repo=<id>&page_id=<stable-id>&per_kind_limit=<n>`, `GET /api/docs/family-search?repo=<id>&query=<text>&kind=<family>&limit=<n>&per_kind_limit=<n>`, `GET /api/docs/family-cluster?repo=<id>&page_id=<stable-id>&kind=<family>&limit=<n>`, `GET /api/docs/navigation?repo=<id>&page_id=<stable-id>&node_id=<stable-node-id?>&family_kind=<family?>&related_limit=<n>&family_limit=<n>`, and `GET /api/docs/navigation-search?repo=<id>&query=<text>&kind=<family>&family_kind=<family?>&limit=<n>&related_limit=<n>&family_limit=<n>`, while still deliberately stopping short of materialized wiki pages or Qianji-assisted generation.
- **Update**: Landing the slice required one minimal compile-boundary repair in the concurrent workspace state: `gateway::studio::search` is now crate-visible for internal `search_plane::reference_occurrence` consumers, and the same search-plane module now compiles with a narrower schema import path and a more explicit `WalkDir::new(&root)` call.
- **Update**: The repo-surface wiring also surfaced one real bundled OpenAPI drift: runtime route inventory already expected `GET /api/search/index/status`, but the checked-in `resources/openapi/wendao_gateway.openapi.json` had lost that path. The slice restores that bundled entry while adding the new projected-gap-report route.
- **Update**: The docs-facing slice deliberately reuses the same deterministic gap payload instead of introducing a second docs-specific schema. The new `GET /api/docs/projected-gap-report?repo=<id>` route is therefore a namespace boundary for deep-wiki planning, not a duplicate generator surface.
- **Update**: The next docs namespace refinement follows the same boundary rule. `GET /api/docs/search?repo=<id>&query=<text>&kind=<family>&limit=<n>` reuses the existing deterministic projected-page search payload instead of introducing a docs-only search schema, so deep-wiki discovery can start from stable Stage-2 pages without forking contracts.
- **Update**: `GET /api/docs/retrieval?repo=<id>&query=<text>&kind=<family>&limit=<n>` now reuses the same deterministic mixed projected-retrieval payload as the repo inspection lane, so docs discovery can span projected pages and builder-native anchors without introducing a docs-only retrieval schema.
- **Update**: `GET /api/docs/retrieval-context?repo=<id>&page_id=<stable-id>&node_id=<stable-node-id?>&related_limit=<n>` now reuses the same deterministic mixed projected-retrieval-context payload as the repo inspection lane, so docs opening can expand one mixed hit into related pages and optional node neighborhood without introducing a docs-only retrieval-context schema.
- **Update**: `GET /api/docs/page?repo=<id>&page_id=<stable-id>` now reuses the same deterministic projected-page lookup payload as the repo inspection lane, so docs search can hand off directly to docs page opening without a second page contract or a repo-only route dependency.
- **Update**: `GET /api/docs/family-context?repo=<id>&page_id=<stable-id>&per_kind_limit=<n>` now reuses the same deterministic projected-page family-context payload as the repo inspection lane, so docs page opening can expand into grouped family context without introducing a docs-only grouped-family schema.
- **Update**: `GET /api/docs/family-search?repo=<id>&query=<text>&kind=<family>&limit=<n>&per_kind_limit=<n>` now reuses the same deterministic projected-page family-search payload as the repo inspection lane, so docs discovery can return grouped family clusters directly without introducing a docs-only grouped-family search schema.
- **Update**: `GET /api/docs/family-cluster?repo=<id>&page_id=<stable-id>&kind=<family>&limit=<n>` now reuses the same deterministic projected-page family-cluster payload as the repo inspection lane, so docs discovery can reopen one requested family cluster directly without introducing a docs-only grouped-family cluster schema.
- **Update**: `GET /api/docs/navigation?repo=<id>&page_id=<stable-id>&node_id=<stable-node-id?>&family_kind=<family?>&related_limit=<n>&family_limit=<n>` now reuses the same deterministic projected-page navigation payload as the repo inspection lane, so docs page opening can expand straight into local navigation context without leaving the docs namespace.
- **Update**: `GET /api/docs/navigation-search?repo=<id>&query=<text>&kind=<family>&family_kind=<family?>&limit=<n>&related_limit=<n>&family_limit=<n>` now reuses the same deterministic projected-page navigation-search payload as the repo inspection lane, so docs discovery can return full page-centric navigation bundles directly without leaving the docs namespace or introducing a second search contract.
- **Validation Executed**:
  - `cargo check -p xiuxian-wendao`
  - `env INSTA_UPDATE=always cargo test -p xiuxian-wendao --test repo_projected_gap_report_test`
  - `env INSTA_UPDATE=always cargo test -p xiuxian-wendao --lib studio_repo_sync_api -- --nocapture`
  - `cargo test -p xiuxian-wendao --lib bundled_gateway_openapi_document_covers_declared_route_inventory -- --nocapture`
- **Next Bounded Step**:
  - extend the docs namespace beyond projected gaps, docs search, docs retrieval, docs retrieval-context, docs page, docs family-context, docs family-search, docs family-cluster, docs navigation, and docs navigation-search into a deterministic docs retrieval-hit surface
  - keep the next slice deterministic and planner-facing; do not introduce Qianji generation yet

### [IN-PROGRESS] Feature: Repo Intelligence Hot-Path Removal and Visible Progress

- **Objective**: Keep repository intelligence off the Studio request hot path while improving live observability, runtime plugin completeness, and adaptive indexing reliability under large multi-repo Julia/Modelica workloads.
- **Status**: [Ready for Live Validation] The governing execution record remains `[[.cache/codex/execplans/wendao-repo-index-hot-path-removal.md]]`. Live validation after gateway restart confirms the previous `mcl` failure is now gone from the repo-index terminal state; `mcl` is queued for indexing instead of failing with `repo intelligence plugin \`modelica\` is not registered`. The default gateway build now carries both builtin Julia and Modelica analyzers. Repo indexing also now applies generation-aware stale-task checks before analysis result commit and during code-document collection, while the adaptive concurrency controller uses an efficiency-gradient signal (`concurrency / EMA latency`) to avoid expanding into IO congestion. The next live checkpoint is the nested-root Modelica layout fix for `ModelicaStandardLibrary`, which now treats a dominant top-level package directory such as `Modelica/package.mo` as the analysis root and rewrites emitted record paths back to repository-relative paths for Studio navigation.
- **Update**: Frontend status presentation now surfaces unsupported/failed repo issue details directly from `/api/repo/index/status`, so parser/layout gaps can be triaged from Studio without manual `curl` inspection. The same status payload now also carries per-repo `queuePosition`, and the status bar renders the next queued repo preview so interactive repo promotion can be observed directly after `POST /api/repo/index`.
- **Validation Executed**:
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo check -p xiuxian-wendao`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo test -p xiuxian-wendao --bin wendao test_build_plugin_registry_bootstraps_builtin_plugins`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo test -p xiuxian-wendao --lib gateway::studio::repo_index::state::tests::`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo test -p xiuxian-wendao-modelica analysis::tests::`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop npm test -- src/components/StatusBar.test.tsx src/components/panels/FileTree/repoIndexStatus.test.ts`
- **Live Checkpoint**:
  - `/api/repo/index/status?repo=mcl` now returns `queued`, not `failed`
  - aggregate repo-index status currently shows the queue progressing with adaptive metadata (`targetConcurrency`, `maxConcurrency`, `activeRepoIds`)

### [IN-PROGRESS] Refactor: Wendao Large Rust File Modularization

- **Objective**: Losslessly split oversized Rust source files in `xiuxian-wendao` into feature folders with interface-only `mod.rs` façades, preserving public module paths and behavior while reducing future refactor blast radius.
- **Status**: [Parser Code Observation Slice Landed] The governing execution record is `[[.cache/codex/execplans/wendao-large-rust-modularization.md]]`, and the package roadmap sync is `[[packages/rust/crates/xiuxian-wendao/docs/06_roadmap/405_large_rust_modularization.md]]`. Twenty slices are now landed in the current worktree: `gateway/studio/search/handlers.rs`, `gateway/studio/types.rs`, `analyzers/query.rs`, `search/fuzzy.rs`, `search/tantivy.rs`, `analyzers/service/mod.rs`, `gateway/studio/router/mod.rs`, `gateway/studio/repo_index/state.rs`, `gateway/studio/router/handlers/repo.rs`, `gateway/studio/vfs.rs`, `gateway/openapi/paths.rs`, `gateway/studio/search/source_index.rs`, `analyzers/service/search.rs`, `analyzers/service/projection.rs`, `analyzers/service/helpers.rs`, `enhancer/markdown_config.rs`, `skill_vfs/internal_manifest.rs`, `link_graph/addressing/mod.rs`, `link_graph/index.rs`, and `link_graph/parser/code_observation.rs`. The next bounded target is `link_graph/parser/sections.rs`.
- **Update**: `analyzers/service/search.rs` is now an interface-only feature folder, with `documents.rs`, `legacy.rs`, `indexed_exact.rs`, `indexed_fuzzy.rs`, `ranking.rs`, `overview.rs`, `module.rs`, `symbol.rs`, `example.rs`, `imports.rs`, `coverage.rs`, and `tests.rs` carrying document indexing, legacy/indexed ranking, repo overview, repo search, import search, and doc-coverage logic while preserving the existing `crate::analyzers::service::*` API surface.
- **Update**: `analyzers/service/projection.rs` is now an interface-only feature folder, with `gap.rs`, `pages.rs`, `page.rs`, `family.rs`, `navigation.rs`, `search.rs`, `index_tree.rs`, `retrieval.rs`, `registry.rs`, and `tests.rs` carrying projected-page wrappers, family/navigation bundles, tree lookup/search, retrieval wrappers, and gap-report wrappers while preserving the existing `crate::analyzers::service::*` projection API surface.
- **Update**: `analyzers/service/helpers.rs` is now an interface-only feature folder, with `uri.rs`, `ecosystem.rs`, `path.rs`, `ranking.rs`, `backlinks.rs`, `projection_lookup.rs`, `example_relations.rs`, `scope.rs`, and `tests.rs` carrying URI, ecosystem, path, ranking, backlink, projection lookup, example relation, and scope helpers while preserving the existing analyzer-service helper call sites.
- **Update**: `enhancer/markdown_config.rs` is now an interface-only feature folder, with `types.rs`, `index.rs`, `blocks.rs`, `links.rs`, and `tests.rs` carrying markdown config block parsing, link-target extraction, and memory-index helpers while preserving the enhancer and `resource_registry` call sites.
- **Update**: `skill_vfs/internal_manifest.rs` is now an interface-only feature folder, with `types.rs`, `load.rs`, `authority.rs`, and `tests.rs` carrying internal skill manifest parsing, authority resolution, and workflow-type helpers while preserving `skill_vfs::internal_manifest::*` and resolver call sites.
- **Update**: `link_graph/index.rs` is now an interface-only feature folder, with `constants.rs`, `types.rs`, `lookup.rs`, `symbol_cache.rs`, and the preserved `build/`, `search/`, `ids.rs`, `page_indices.rs`, `passages.rs`, `ppr.rs`, `rank.rs`, `scoring.rs`, `semantic_documents.rs`, `shared.rs`, and `traversal.rs` child namespaces carrying the index core and lookup helpers while preserving `link_graph::index::*` and its child-module call sites.
- **Update**: `link_graph/parser/code_observation.rs` is now an interface-only feature folder, with `types.rs`, `glob.rs`, `extract.rs`, `format.rs`, and the preserved tests fixture carrying code-observation parsing, glob matching, formatting, and observation extraction while preserving `link_graph::parser::code_observation::*` and the parser-facing `CodeObservation` API.
- **Update**: The slice also required one minimal concurrent compile-boundary repair outside the new folder: `gateway/studio/router/handlers/docs.rs` now imports `with_repo_analysis` through `repo::shared`, and `repo/mod.rs` exposes `shared` to its parent module instead of relying on a crate-private re-export that Rust rejected during verification.
- **Validation Executed**:
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo fmt -p xiuxian-wendao`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo check -p xiuxian-wendao --lib --keep-going`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo test -p xiuxian-wendao analyzers::service::projection::tests:: --lib`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo test -p xiuxian-wendao repo_projected_`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop git diff --check`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo test -p xiuxian-wendao analyzers::service:: --lib`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo test -p xiuxian-wendao repo_module_search`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo test -p xiuxian-wendao repo_symbol_search`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo test -p xiuxian-wendao repo_example_search`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop git diff --check`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo test -p xiuxian-wendao analyzers::service::helpers::tests:: --lib`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo test -p xiuxian-wendao enhancer:: --lib`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo test -p xiuxian-wendao skill_vfs::internal_manifest::tests:: --lib`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo check -p xiuxian-wendao --lib --keep-going`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo test -p xiuxian-wendao link_graph::addressing:: --lib`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo test -p xiuxian-wendao link_graph::index:: --lib`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo test -p xiuxian-wendao link_graph::parser::code_observation:: --lib`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo test -p xiuxian-wendao link_graph::index:: --lib`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo test -p xiuxian-wendao link_graph::page_index:: --lib`

### [IN-PROGRESS] Feature: Studio Search Plane Reconstruction

- **Objective**: Move Studio search off request-path AST/references/content scans and onto a background-built Lance/Arrow/Valkey search plane owned by `xiuxian-vector` and orchestrated by `xiuxian-wendao`.
- **Status**: [Repo Entity Slice Landed] The governing execution record is `[[.cache/codex/execplans/wendao-studio-search-plane.md]]`. Lance/Arrow dependency ownership is now unified under `xiuxian-vector`, reusable columnar table APIs are in place, `xiuxian_wendao::search_plane` is live, `local_symbol` serves `/api/search/ast`, `/api/search/autocomplete`, and `/api/search/definition`, `reference_occurrence` serves `/api/search/references`, `attachment` serves `/api/search/attachments`, `repo_content_chunk` serves the code-search file fallback path without keeping repository source blobs inside `RepoIndexSnapshot`, `knowledge_section` serves `/api/search`, non-code `/api/search/intent` now merges `knowledge_section`, `local_symbol`, repo entity hits, and repo content hits into one hybrid response path, `repo_entity` now materializes analyzer output into per-repository Lance tables for code search and code-biased intent, Valkey hot caches front repeat autocomplete, knowledge, and non-repo intent lookups, and corpus publish now schedules background compaction while same-fingerprint schema bumps force a fresh rebuild lease instead of returning stale-ready.
- **Update**: Knowledge search no longer queries `LinkGraphIndex::execute_search` on the request path. The new `knowledge_section` builder derives per-document and per-section rows from parsed notes, stores section/body text in Lance `search_text`, deduplicates by note path at query time, and preserves the existing `SearchResponse` payload contract.
- **Update**: Intent search is no longer knowledge-only. Code-biased intents now fan out across local symbol search and repo content search, normalize scores into one merged hit list, preserve repo pending/skipped metadata, and keep the request path non-blocking when one corpus is still warming.
- **Update**: Snapshot baselines for Studio knowledge search now assert real corpus hits instead of empty placeholder payloads. This makes the search-plane migration visible in tests and prevents future regressions from silently dropping knowledge hits back to zero.
- **Update**: Snapshot baselines for Studio intent search now assert hybrid symbol plus knowledge hits, and handler-level tests explicitly verify that repo content search can enter the same merged intent response.
- **Update**: Repo indexing continues to publish per-repository content tables into the search plane before committing a ready snapshot, so code-search fallback no longer scans `snapshot.code_documents[*].contents` on demand. The repository policy still references `[[.data/blueprints/project_anchor_semantic_addressing.md]]`, but that blueprint is not present in the current checkout, so implementation continues with that gap explicitly recorded instead of assumed.
- **Update**: `search_plane::cache` is now the first real Valkey-backed fast path for Studio search. `SearchPlaneService` resolves an optional Valkey client from `XIUXIAN_WENDAO_SEARCH_PLANE_VALKEY_URL`, then falls back to existing Valkey environment variables, applies low async connection and response timeouts, and degrades silently to direct Lance reads when Valkey is absent or unavailable.
- **Update**: `/api/search/autocomplete` now uses an epoch-scoped prefix cache keyed off the published `local_symbol` epoch, while `/api/search` and non-repo `/api/search/intent` now use short-lived hot query caches keyed off the active `knowledge_section` and `local_symbol` epochs. This keeps cache invalidation publication-safe without introducing explicit delete storms.
- **Update**: `SearchPlaneCoordinator::begin_build` now treats schema version as part of build identity. If a corpus fingerprint is unchanged but the code expects a newer schema version, the coordinator leases a new staging epoch instead of returning `AlreadyReady` or `AlreadyIndexing`, which closes the schema-evolution hole noted in the audit.
- **Update**: `SearchPlaneService` now turns `maintenance.compaction_pending` into real background work. After publish, it asks the coordinator for a pending compaction task, runs `xiuxian-vector` table compaction on the active epoch, and records the resulting fragment count plus compaction reason back into corpus status once the task completes.
- **Update**: `/api/search/index/status` now synthesizes `repo_content_chunk` state from live repo-index phases plus published per-repo Lance table metadata. The status surface now reports repo-content readiness, indexing pressure, row counts, fragment counts, and missing-table failures instead of leaving that corpus permanently idle.
- **Update**: Repo indexing now publishes a second per-repository search-plane surface: `repo_entity`. Analyzer-produced modules, symbols, and examples are materialized into Lance rows with precomputed tags, navigation targets, backlink metadata, and saliency-aware search text before a repo snapshot is marked ready.
- **Update**: Studio `code_search` no longer ranks repo symbols/modules/examples directly out of `snapshot.analysis` on the request path. It now queries `repo_entity` first and falls back to `repo_content_chunk` only when no structured entity hit survives filtering, which removes another hot-path analyzer dependency from request serving.
- **Update**: Code-biased hybrid intent search now merges `repo_entity` hits before repo-content fallback hits, and `/api/search/index/status` now synthesizes `repo_entity` readiness from live repo-index phases plus published per-repo Lance table metadata so both repo-backed corpora are visible in one status snapshot.
- **Update**: Attachment snapshots were refreshed to match current relative-path resolution semantics. A note stored at `docs/alpha.md` with `![Topology](assets/topology.png)` now materializes as `docs/assets/topology.png`, which matches the current attachment builder's document-relative path normalization.
- **Validation Executed**:
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo test -p xiuxian-vector --test test_columnar_tables --test test_migration`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo test -p xiuxian-wendao search_ast --lib`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo test -p xiuxian-wendao autocomplete --lib`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo test -p xiuxian-wendao search_definition --lib`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo test -p xiuxian-wendao local_symbol --lib`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo test -p xiuxian-wendao reference_occurrence --lib`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo test -p xiuxian-wendao search_references --lib`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo test -p xiuxian-wendao search_attachments`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo test -p xiuxian-wendao attachment_query_reads_hits_from_published_epoch --lib`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo test -p xiuxian-wendao gateway::studio::search::handlers::tests:: --lib`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo test -p xiuxian-wendao gateway::studio::repo_index::state::tests:: --lib`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo test -p xiuxian-wendao studio_repo_sync --lib`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo test -p xiuxian-wendao search_knowledge --lib`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo test -p xiuxian-wendao search_intent --lib`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo test -p xiuxian-wendao build_intent_search_response_includes_repo_content_hits_for_debug_lookup --lib`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo test -p xiuxian-wendao autocomplete_key_is_stable_for_epoch_prefix_and_limit --lib`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo test -p xiuxian-wendao search_query_key_tracks_scope_epochs_and_query_shape --lib`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo test -p xiuxian-wendao disabled_cache_skips_key_generation --lib`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo test -p xiuxian-wendao service_disables_cache_for_explicit_test_paths --lib`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo test -p xiuxian-wendao search_plane::coordinator::tests:: --lib`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo test -p xiuxian-wendao compact_pending_corpus_updates_maintenance_status --lib`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo test -p xiuxian-wendao status_with_repo_content_surfaces_ready_repo_tables --lib`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo test -p xiuxian-wendao status_with_repo_content_reports_indexing_before_publish --lib`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo test -p xiuxian-wendao search_repo_entities_reads_hits_from_published_table --lib`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo test -p xiuxian-wendao build_code_search_response_returns_repo_entity_hits_from_search_plane --lib`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo test -p xiuxian-wendao build_intent_search_response_includes_repo_entity_hits_for_debug_lookup --lib`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo test -p xiuxian-wendao --test studio_search_index_api_test`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo check -p xiuxian-wendao`
- **Next Bounded Step**:
  - extend repo-aware cache keys and Valkey hot-query caching so code-biased intent and future repo-scoped search hits can safely key off published `repo_entity` plus `repo_content_chunk` table state
  - decide whether repo-backed corpora should remain per-repository synthetic status rows or graduate into a fuller coordinator-managed epoch model when repo indexing needs stronger publication semantics

### [DONE] Verification: Repo Intelligence Tier-3 Closure

- **Objective**: Close the full verification gate for the active `xiuxian-wendao` plus `xiuxian-wendao-modelica` repo-intelligence and Stage-2 projection slice before any further cleanup or feature expansion.
- **Status**: [Closed] The shared gate is now green. Contract drift in the external Modelica package is fixed, projection/search snapshot drift is reconciled, and the next bounded action is safe debris deletion plus documentation cleanup rather than more endpoint growth.
- **Validation Executed**:
  - `cargo clippy -p xiuxian-wendao -p xiuxian-wendao-modelica --all-targets --all-features -- -D warnings`
  - `cargo nextest run -p xiuxian-wendao -p xiuxian-wendao-modelica --no-fail-fast`
- **Follow-Up Constraint**:
  - the tracked `packages/rust/crates/xiuxian-wendao/src/analyzers/service/mod.rs.bak2` shadow copy is now removed after confirming the service split is live
  - the next cleanup slice can stay focused on real modularization leftovers instead of backup-file debris
