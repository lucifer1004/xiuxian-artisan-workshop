---
type: knowledge
metadata:
  title: "GTD: Sovereign Nexus (2026-03-22)"
---

# GTD: Sovereign Nexus (2026-03-22)

## Current Delivery

### [IN-PROGRESS] Feature: Repo Intelligence Hot-Path Removal and Visible Progress

- **Objective**: Keep repository intelligence off the Studio request hot path while improving live observability, runtime plugin completeness, and adaptive indexing reliability under large multi-repo Julia/Modelica workloads.
- **Status**: [Ready for Live Validation] The governing execution record remains `[[.cache/codex/execplans/wendao-repo-index-hot-path-removal.md]]`. Live validation after gateway restart confirms the previous `mcl` failure is now gone from the repo-index terminal state; `mcl` is queued for indexing instead of failing with `repo intelligence plugin \`modelica\` is not registered`. The default gateway build now carries both builtin Julia and Modelica analyzers. Repo indexing also now applies generation-aware stale-task checks before analysis result commit and during code-document collection, while the adaptive concurrency controller uses an efficiency-gradient signal (`concurrency / EMA latency`) to avoid expanding into IO congestion. The next live checkpoint is the nested-root Modelica layout fix for `ModelicaStandardLibrary`, which now treats a dominant top-level package directory such as `Modelica/package.mo` as the analysis root and rewrites emitted record paths back to repository-relative paths for Studio navigation.
- **Update**: Frontend status presentation now surfaces unsupported/failed repo issue details directly from `/api/repo/index/status`, so parser/layout gaps can be triaged from Studio without manual `curl` inspection. The same status payload now also carries per-repo `queuePosition`, and the status bar renders the next queued repo preview so interactive repo promotion can be observed directly after `POST /api/repo/index`.
- **Validation Executed**:
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo check -p xiuxian-wendao`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo test -p xiuxian-wendao --bin wendao test_build_plugin_registry_bootstraps_builtin_plugins`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo test -p xiuxian-wendao --lib gateway::studio::repo_index::state::tests::`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo test -p xiuxian-wendao-modelica analysis::tests::`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop npm test -- src/components/StatusBar.test.tsx src/components/panels/FileTree/repoIndexStatus.test.ts`
- **Live Checkpoint**:
  - `/api/repo/index/status?repo=mcl` now returns `queued`, not `failed`
  - aggregate repo-index status currently shows the queue progressing with adaptive metadata (`targetConcurrency`, `maxConcurrency`, `activeRepoIds`)

### [IN-PROGRESS] Refactor: Wendao Large Rust File Modularization

- **Objective**: Losslessly split oversized Rust source files in `xiuxian-wendao` into feature folders with interface-only `mod.rs` façades, preserving public module paths and behavior while reducing future refactor blast radius.
- **Status**: [Wave 3 Active] The governing execution record is `[[.cache/codex/execplans/wendao-large-rust-modularization.md]]`, and the package roadmap sync is `[[packages/rust/crates/xiuxian-wendao/docs/06_roadmap/405_large_rust_modularization.md]]`. Nine slices are now landed in the current worktree: `gateway/studio/search/handlers.rs`, `gateway/studio/types.rs`, `analyzers/query.rs`, `search/fuzzy.rs`, `search/tantivy.rs`, `analyzers/service/mod.rs`, `gateway/studio/router/mod.rs`, `gateway/studio/repo_index/state.rs`, and `gateway/studio/router/handlers/repo.rs`. The next bounded target is `gateway/studio/vfs.rs`.
- **Update**: `gateway/studio/router/handlers/repo.rs` is now an interface-only feature folder, with `query.rs`, `parse.rs`, `shared.rs`, `analysis.rs`, `index.rs`, `pages.rs`, `retrieval.rs`, `family.rs`, and `refine.rs` carrying query DTOs, input parsing, shared repo-analysis execution, repo index endpoints, projected-page/retrieval/family endpoint groups, and refinement logic while preserving the existing repo handler names and route bindings.
- **Validation Executed**:
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo fmt -p xiuxian-wendao`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo check -p xiuxian-wendao --lib --keep-going`
  - `cargo test -p xiuxian-wendao gateway::studio::repo_index:: --lib`
  - `cargo test -p xiuxian-wendao gateway::studio::router:: --lib`
  - `cargo test -p xiuxian-wendao gateway::studio::studio_repo_sync_api_tests:: --lib`

### [DONE] Verification: Repo Intelligence Tier-3 Closure

- **Objective**: Close the full verification gate for the active `xiuxian-wendao` plus `xiuxian-wendao-modelica` repo-intelligence and Stage-2 projection slice before any further cleanup or feature expansion.
- **Status**: [Closed] The shared gate is now green. Contract drift in the external Modelica package is fixed, projection/search snapshot drift is reconciled, and the next bounded action is safe debris deletion plus documentation cleanup rather than more endpoint growth.
- **Validation Executed**:
  - `cargo clippy -p xiuxian-wendao -p xiuxian-wendao-modelica --all-targets --all-features -- -D warnings`
  - `cargo nextest run -p xiuxian-wendao -p xiuxian-wendao-modelica --no-fail-fast`
- **Follow-Up Constraint**:
  - the tracked `packages/rust/crates/xiuxian-wendao/src/analyzers/service/mod.rs.bak2` shadow copy is now removed after confirming the service split is live
  - the next cleanup slice can stay focused on real modularization leftovers instead of backup-file debris
