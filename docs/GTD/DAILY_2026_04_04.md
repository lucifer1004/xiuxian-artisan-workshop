# Daily Log - 2026-04-04

## Wendao Search Repo-Content Execution Extraction

- opened the next gateway-downpressure blueprint at
  `.cache/agent/blueprints/wendao_search_gateway_downpressure.md`
- synchronized package docs before implementation so the next bounded slice is
  explicit:
  repo-content business-search execution should move out of
  `gateway/studio/search/handlers/flight/repo_search.rs` into a shared
  `src/search/` feature seam, while gateway keeps Flight adaptation and
  Studio-facing response shaping
- opened the active execution plan for this slice at
  `.cache/agent/execplans/wendao-search-repo-content-execution-extraction.md`
- landed a shared repo-content execution seam under
  `packages/rust/crates/xiuxian-wendao/src/search/repo_search/`, split into a
  feature-folder `mod.rs` plus `batch.rs` and `search.rs`
- rewired
  `packages/rust/crates/xiuxian-wendao/src/gateway/studio/search/handlers/flight/repo_search.rs`
  so the Flight provider delegates to the shared seam instead of owning the
  repo-content execution path itself
- rewired
  `packages/rust/crates/xiuxian-wendao/src/gateway/studio/search/handlers/code_search/search/repo_search.rs`
  so code-search no longer routes repo-content business logic back through the
  Flight provider; it now calls the shared seam directly
- removed the old code-search Flight-batch decode path and kept only the
  gateway-local DTO shaping that still belongs to the Studio response layer
- confirmed focused and final validation is green:
  `direnv exec . cargo fmt -p xiuxian-wendao`
  `direnv exec . cargo nextest run -p xiuxian-wendao --lib --features julia gateway::studio::search::handlers::flight::repo_search::tests::`
  `direnv exec . cargo nextest run -p xiuxian-wendao --lib --features julia gateway::studio::search::handlers::tests::repo_content::`
  `direnv exec . cargo clippy -p xiuxian-wendao --all-targets --all-features --message-format=short -- -D warnings`
  `direnv exec . git diff --check`

## Wendao Search Repo-Entity Execution Extraction

- opened the next active execution plan for the repo-entity slice at
  `.cache/agent/execplans/wendao-search-repo-entity-execution-extraction.md`
- kept the existing gateway-downpressure blueprint active at
  `.cache/agent/blueprints/wendao_search_gateway_downpressure.md`
- synchronized package docs before implementation so the next bounded slice is
  explicit:
  repo-entity execution and relation-to-`SearchHit` shaping should move into
  `src/search/repo_search/` so code-search and knowledge-intent merge stop
  carrying a gateway-only repo-entity execution owner
- landed the shared repo-entity seam under
  `packages/rust/crates/xiuxian-wendao/src/search/repo_search/entity.rs` and
  re-exported it through the feature-folder interface seam in
  `packages/rust/crates/xiuxian-wendao/src/search/repo_search/mod.rs`
- rewired
  `packages/rust/crates/xiuxian-wendao/src/gateway/studio/search/handlers/code_search/search/repo_search.rs`
  so the gateway entrypoint now delegates repo-entity execution to the shared
  seam instead of owning query-core orchestration, relation decoding, and
  telemetry recording locally
- removed the old gateway-local repo-entity relation-to-`SearchHit` helpers,
  while keeping the thin Studio error-mapping wrapper in place for the
  knowledge-intent path
- confirmed focused and final validation is green:
  `direnv exec . cargo fmt -p xiuxian-wendao`
  `direnv exec . cargo nextest run -p xiuxian-wendao --lib --features julia gateway::studio::search::handlers::tests::code_search::repo_entity::`
  `direnv exec . cargo nextest run -p xiuxian-wendao --lib --features julia gateway::studio::search::handlers::tests::intent::`
  `direnv exec . cargo clippy -p xiuxian-wendao --all-targets --all-features --message-format=short -- -D warnings`
  `direnv exec . git diff --check`

## Wendao Search Repo Dispatch Extraction

- re-ran the gateway-downpressure blueprint check against
  `.cache/agent/blueprints/wendao_search_gateway_downpressure.md`
- synchronized package docs before implementation so the next bounded slice is
  explicit:
  repo dispatch planning ownership should move into
  `src/search/repo_search/`, specifically `RepoSearchTarget`,
  `RepoSearchDispatch`, `collect_repo_search_targets`, and
  `repo_search_parallelism`, while code-search query parsing remains
  gateway-local
- opened the active execution plan for this slice at
  `.cache/agent/execplans/wendao-search-repo-dispatch-extraction.md`
- landed the shared dispatch seam under
  `packages/rust/crates/xiuxian-wendao/src/search/repo_search/dispatch.rs`
  and re-exported it through the feature-folder interface seam in
  `packages/rust/crates/xiuxian-wendao/src/search/repo_search/mod.rs`
- rewired gateway code-search and knowledge-intent callers to consume the
  shared dispatch seam instead of the old gateway-local owner in
  `code_search/query.rs`
- removed the old gateway-local dispatch types from
  `code_search/types.rs`, so `RepoSearchTarget` no longer lives only inside
  the Studio gateway namespace
- confirmed focused and final validation is green:
  `direnv exec . cargo fmt -p xiuxian-wendao`
  `direnv exec . cargo nextest run -p xiuxian-wendao --lib --features julia gateway::studio::search::handlers::tests::query::`
  `direnv exec . cargo nextest run -p xiuxian-wendao --lib --features julia gateway::studio::search::handlers::tests::intent::`
  `direnv exec . cargo nextest run -p xiuxian-wendao --lib --features julia gateway::studio::search::handlers::tests::code_search::indexing::`
  `direnv exec . cargo clippy -p xiuxian-wendao --all-targets --all-features --message-format=short -- -D warnings`
  `direnv exec . git diff --check`

## Wendao Search Repo Buffered Orchestration Extraction

- re-ran the gateway-downpressure blueprint check against
  `.cache/agent/blueprints/wendao_search_gateway_downpressure.md`
- synchronized package docs before implementation so the next bounded slice is
  explicit:
  buffered repo-search queue draining, spawn policy, and shared repo-search
  query execution should move into `src/search/repo_search/`, while
  gateway-owned code-search parsing, cache policy, and response shaping remain
  in the Studio layer
- opened the active execution plan for this slice at
  `.cache/agent/execplans/wendao-search-repo-buffered-orchestration-extraction.md`
- landed the shared buffered repo-search seam under
  `packages/rust/crates/xiuxian-wendao/src/search/repo_search/{buffered,query}.rs`
  and rewired the feature-folder interface seam in
  `packages/rust/crates/xiuxian-wendao/src/search/repo_search/mod.rs`
- moved repo-level query parsing plus buffered task spawning and queue
  draining into the shared `src/search/repo_search/` owner, so
  `knowledge/merge.rs` and code-search now consume one shared orchestration
  path
- retired the old gateway-local
  `packages/rust/crates/xiuxian-wendao/src/gateway/studio/search/handlers/code_search/search/{buffered,task}.rs`
  modules and thinned the remaining `code_search/search/` namespace to
  adapter/test-facing seams only
- confirmed focused and final validation is green:
  `direnv exec . cargo fmt -p xiuxian-wendao`
  `direnv exec . cargo nextest run -p xiuxian-wendao --lib --features julia gateway::studio::search::handlers::tests::query::`
  `direnv exec . cargo nextest run -p xiuxian-wendao --lib --features julia gateway::studio::search::handlers::tests::intent::`
  `direnv exec . cargo nextest run -p xiuxian-wendao --lib --features julia gateway::studio::search::handlers::tests::code_search::indexing::`
  `direnv exec . cargo nextest run -p xiuxian-wendao --lib --features julia gateway::studio::search::handlers::tests::code_search::budget::`
  `direnv exec . cargo clippy -p xiuxian-wendao --all-targets --all-features --message-format=short -- -D warnings`
  `direnv exec . git diff --check`

## Wendao Search Repo Response Orchestration Extraction

- re-ran the gateway-downpressure blueprint check against
  `.cache/agent/blueprints/wendao_search_gateway_downpressure.md`
- synchronized package docs before implementation so the next bounded slice is
  explicit:
  publication-state lookup, dispatch telemetry, and repo-search
  partial/pending/skipped state assembly should move into
  `src/search/repo_search/`, while `SearchResponse` and
  `IntentSearchTransportMetadata` shaping remain gateway-owned
- opened the active execution plan for this slice at
  `.cache/agent/execplans/wendao-search-repo-response-orchestration-extraction.md`
- landed the shared repo-response orchestration seam under
  `packages/rust/crates/xiuxian-wendao/src/search/repo_search/orchestration.rs`
  and re-exported the canonical shared entrypoints through
  `packages/rust/crates/xiuxian-wendao/src/search/repo_search/mod.rs`
- moved publication-state lookup, dispatch telemetry recording, and shared
  `pending_repos` / `skipped_repos` / `partial_timeout` assembly into the
  shared `src/search/repo_search/` owner, so `knowledge/merge.rs` and
  code-search response callers no longer recompute that orchestration locally
- kept Studio config-driven repo-id resolution, cache policy, and final
  response DTO shaping in the gateway layer, so this slice stopped short of a
  `SearchResponse` contract move
- confirmed focused and final validation is green:
  `direnv exec . cargo fmt -p xiuxian-wendao`
  `direnv exec . cargo nextest run -p xiuxian-wendao --lib --features julia gateway::studio::search::handlers::tests::intent::`
  `direnv exec . cargo nextest run -p xiuxian-wendao --lib --features julia gateway::studio::search::handlers::tests::code_search::indexing::`
  `direnv exec . cargo nextest run -p xiuxian-wendao --lib --features julia gateway::studio::search::handlers::tests::code_search::budget::`
  `direnv exec . cargo clippy -p xiuxian-wendao --all-targets --all-features --message-format=short -- -D warnings`
  `direnv exec . git diff --check`

## Wendao Search Query Snapshot Namespace Migration

- opened the next dedicated execution plan for the shared-query snapshot
  namespace migration slice at
  `.cache/agent/execplans/wendao-search-query-snapshot-namespace-migration.md`
- re-ran the blueprint check against the active shared-query architecture
  blueprint at
  `.cache/agent/blueprints/wendao_search_shared_queries_adapters.md`
- synchronized package docs so the bounded migration is explicit before code
  changes:
  canonical shared-query snapshot baselines should move from the legacy
  `tests/snapshots/gateway/studio/` namespace into a dedicated
  `tests/snapshots/search/queries/` tree because gateway and CLI now consume
  one canonical query system under `src/search/queries/`
- landed a canonical shared-query snapshot helper under
  `packages/rust/crates/xiuxian-wendao/src/search/queries/tests/snapshots.rs`
  and retargeted SQL, GraphQL, FlightSQL, and REST snapshot suites to it
- moved the shared-query baselines into
  `packages/rust/crates/xiuxian-wendao/tests/snapshots/search/queries/` with
  canonical names:
  `sql_query_surface_payload.snap`
  `sql_discovery_surface_payload.snap`
  `graphql_query_surface_payload.snap`
  `flightsql_query_surface_payload.snap`
  `rest_query_surface_payload.snap`
- tightened `search/queries/tests/structure.rs` so the canonical query
  adapters must keep both their `tests/snapshots.rs` module wiring and the
  expected `.snap` baseline files, while rejecting the legacy `studio_*`
  query baselines
- confirmed focused validation is green:
  `direnv exec . cargo fmt -p xiuxian-wendao`
  `direnv exec . env INSTA_UPDATE=always cargo nextest run -p xiuxian-wendao --lib --features julia search::queries::`
  `direnv exec . cargo clippy -p xiuxian-wendao --all-targets --all-features --message-format=short -- -D warnings`
  `direnv exec . git diff --check`

## Wendao Search Query Naming Convergence

- opened the next dedicated execution plan for the shared-query naming
  convergence slice at
  `.cache/agent/execplans/wendao-search-query-naming-convergence.md`
- re-ran the blueprint check against the active shared-query architecture
  blueprint at
  `.cache/agent/blueprints/wendao_search_shared_queries_adapters.md`
- synchronized package docs so the next bounded cleanup is explicit before
  code changes:
  canonical `src/search/queries/` names should converge away from legacy
  `Studio*` query-owner naming, while explicitly Studio-owned gateway
  transport/provider names stay unchanged
- landed the query-owned naming convergence under `src/search/queries/`:
  `SearchQueryService`
  `QueryCore`
  `GraphqlExecutionContext`
  `SqlQueryPayload`
  `GraphqlQueryPayload`
  `RestQueryRequest`
  `RestQueryPayload`
  `query_sql_payload`
  `query_graphql_payload`
  `query_rest_payload`
  `execute_sql_query`
- kept explicitly Studio-owned transport/provider names untouched, including
  `StudioSqlFlightRouteProvider` and `StudioFlightSqlService`
- confirmed focused validation is green:
  `direnv exec . cargo fmt -p xiuxian-wendao`
  `direnv exec . cargo nextest run -p xiuxian-wendao --lib --features julia search::queries::`
  `direnv exec . cargo nextest run -p xiuxian-wendao --bin wendao --features julia query`

## Wendao Queries CLI Adapter

- opened a dedicated execution plan for the next shared-queries slice at
  `.cache/agent/execplans/wendao-queries-cli-adapter.md`
- re-ran the blueprint check against the active Wendao migration blueprint and
  bounded this slice to the CLI adapter plus a shared SQL execution seam,
  without starting GraphQL or FlightSQL implementation work
- synchronized the search-queries architecture note and package README so the
  package docs now state the next concrete rule explicitly:
  `wendao query ...` must be a thin adapter over `search/queries/sql/`, not a
  second planner path and not a Flight-only wrapper
- queued focused validation on:
  `direnv exec . cargo fmt -p xiuxian-wendao`
  `direnv exec . cargo nextest run -p xiuxian-wendao --lib --features julia gateway::studio::search::queries::sql::tests::`
  `direnv exec . cargo nextest run -p xiuxian-wendao --bin wendao --features julia query`
  `direnv exec . cargo clippy -p xiuxian-wendao --all-targets --all-features --message-format=short -- -D warnings`
  `direnv exec . git diff --check`
- landed a transport-neutral SQL execution seam under
  `src/gateway/studio/search/queries/sql/execution/`, so Flight and CLI now
  share the same request-scoped DataFusion assembly and execution path
- added the first shared-query CLI adapter under
  `src/bin/wendao/{types/commands/query/,execute/query/}` with
  `wendao query sql --query ...`
- confirmed focused verification is green:
  `direnv exec . cargo nextest run -p xiuxian-wendao --lib --features julia gateway::studio::search::queries::sql::tests::`
  `direnv exec . cargo nextest run -p xiuxian-wendao --bin wendao --features julia query`
  `direnv exec . cargo clippy -p xiuxian-wendao --all-targets --all-features --message-format=short -- -D warnings`
  `direnv exec . git diff --check`

## Wendao Queries GraphQL Adapter

- opened a dedicated execution plan for the first GraphQL adapter slice at
  `.cache/agent/execplans/wendao-queries-graphql-adapter.md`
- re-ran the blueprint check before the dedicated query-adapter extraction
  blueprint existed; this earlier slice is now superseded by the active
  `.data/blueprints/wendao_search_query_adapter_extraction.md` lane
- corrected the slice definition after architecture review:
  the first GraphQL adapter must stay inside the Apache Arrow / DataFusion
  ecosystem as a ROAPI-style table-query frontend, not as a custom root-field
  DSL
- synchronized package docs so the architecture now records the intended
  GraphQL rule explicitly:
  GraphQL root fields map to SQL-visible tables or views and compile
  `filter`/`sort`/`limit`/`page` into the shared request-scoped
  SQL/DataFusion surface
- queued focused validation on:
  `direnv exec . cargo fmt -p xiuxian-wendao`
  `direnv exec . cargo nextest run -p xiuxian-wendao --lib --features julia search::queries::graphql::tests::`
  `direnv exec . cargo clippy -p xiuxian-wendao --all-targets --all-features --message-format=short -- -D warnings`
  `direnv exec . git diff --check`

## Wendao Queries FlightSQL Adapter

- opened a dedicated execution plan for the first FlightSQL adapter slice at
  `.cache/agent/execplans/wendao-queries-flightsql-adapter.md`
- re-ran the blueprint check before the dedicated shared-query extraction
  blueprint existed; this earlier FlightSQL slice is now superseded by the
  active `.data/blueprints/wendao_search_query_adapter_extraction.md` lane
- synchronized package docs so the FlightSQL rule is explicit:
  the adapter lives under `search/queries/flightsql/`, executes
  `CommandStatementQuery` through the shared request-scoped SQL/DataFusion
  surface, and stays separate from native Wendao business Flight routes
- bounded the first landed cut to:
  `CommandStatementQuery`
  `CommandGetSqlInfo`
  one dedicated FlightSQL server builder and binary
- landed the first FlightSQL adapter under
  `src/gateway/studio/search/queries/flightsql/`, with a shared-query service
  builder exported from `gateway::studio` and a standalone
  `wendao_search_flightsql_server` binary
- enabled the `arrow-flight` `flight-sql` feature in `xiuxian-wendao` so the
  adapter stays inside the Arrow/DataFusion ecosystem instead of introducing a
  sidecar protocol crate
- landed the GraphQL adapter as a real shared-query entrypoint under
  `src/gateway/studio/search/queries/graphql/`, with ROAPI-style table-query
  parsing/execution over the request-scoped SQL/DataFusion surface
- wired the adapter into a real non-test caller with
  `wendao query graphql --document ...`, so GraphQL is no longer test-only
  dead code and now shares the same query system as SQL and CLI
- confirmed focused and final verification is green:
  `direnv exec . cargo fmt -p xiuxian-wendao`
  `direnv exec . cargo nextest run -p xiuxian-wendao --lib --features julia gateway::studio::search::queries::flightsql::tests::`
  `direnv exec . cargo nextest run --no-tests pass -p xiuxian-wendao --bin wendao_search_flightsql_server --features julia`
  `direnv exec . cargo clippy -p xiuxian-wendao --all-targets --all-features --message-format=short -- -D warnings`
  `direnv exec . git diff --check`
- opened the next bounded execution plan for FlightSQL discovery metadata at
  `.cache/agent/execplans/wendao-queries-flightsql-discovery-metadata.md`
- created the governing shared-query blueprint for this multi-slice lane at
  `.cache/agent/blueprints/wendao_search_shared_queries_adapters.md`
- re-ran the blueprint check under the current AGENTS standard and corrected
  the lane governance:
  this multi-slice shared-query workstream now records an active blueprint
  instead of the earlier fallback-without-blueprint assumption
- synchronized package docs for the next bounded discovery slice:
  `CommandGetCatalogs`
  `CommandGetDbSchemas`
  `CommandGetTables`
  one logical catalog name `wendao`
  schema names derived from the shared SQL registration scope

## Wendao Search Query Wrapper Thinning

- opened a dedicated execution plan for the wrapper-thinning slice at
  `.cache/agent/execplans/wendao-search-query-wrapper-thinning.md`
- updated the active shared-query blueprint so `src/search/queries/` is now
  explicitly recorded as the canonical implementation owner, while
  `src/gateway/studio/search/queries/` is limited to a bridge seam
- synchronized package docs so the current rule is explicit:
  gateway-side query modules should be pure bridge seams or disappear, and
  the shared query implementation must stay under `src/search/queries/`
- moved the SQL provider implementation from
  `src/gateway/studio/search/queries/sql/provider/` into
  `src/search/queries/sql/provider/`, leaving the gateway SQL module as a
  bridge seam plus test-only compatibility exports
- cleaned the remaining gateway SQL bridge ordering so the thinned seam passes
  `clippy::items_after_test_module` under `--all-targets --all-features`
- confirmed final verification is green:
  `direnv exec . cargo fmt -p xiuxian-wendao`
  `direnv exec . cargo nextest run -p xiuxian-wendao --lib --features julia gateway::studio::search::queries::sql::tests::`
  `direnv exec . cargo nextest run -p xiuxian-wendao --lib --features julia gateway::studio::search::queries::flightsql::tests::`
  `direnv exec . cargo nextest run -p xiuxian-wendao --bin wendao --features julia query`
  `direnv exec . cargo nextest run --no-tests pass -p xiuxian-wendao --bin wendao_search_flightsql_server --features julia`
  `direnv exec . cargo clippy -p xiuxian-wendao --all-targets --all-features --message-format=short -- -D warnings`
  `direnv exec . git diff --check`

## Wendao Search GraphQL FlightSQL Wrapper Thinning

- opened the next dedicated execution plan for the remaining GraphQL and
  FlightSQL wrapper-thinning slice at
  `.cache/agent/execplans/wendao-search-graphql-flightsql-wrapper-thinning.md`
- extended the active shared-query blueprint so the next bounded slice is
  explicit:
  adapter-local GraphQL and FlightSQL tests should move into canonical
  `src/search/queries/{graphql,flightsql}/tests/`, and gateway wrappers should
  disappear unless a real stable bridge namespace is still needed
- synchronized package docs so the current rule is explicit:
  gateway-side `graphql` and `flightsql` query folders are no longer valid
  long-lived test homes once the canonical adapter tree under `src/search/`
  is stable
- moved the FlightSQL adapter tests into
  `src/search/queries/flightsql/tests/`, so the canonical adapter tree now owns
  the discovery, statement, and sql_info regression suite directly
- removed the gateway-side `graphql` and `flightsql` query wrapper modules
  after confirming they no longer had non-test callers
- cleaned one remaining unused top-level SQL bridge re-export that only
  surfaced once canonical GraphQL tests compiled against the slimmer gateway
  query tree
- confirmed final verification is green:
  `direnv exec . cargo fmt -p xiuxian-wendao`
  `direnv exec . cargo nextest run -p xiuxian-wendao --lib --features julia search::queries::graphql::tests::`
  `direnv exec . cargo nextest run -p xiuxian-wendao --lib --features julia search::queries::flightsql::tests::`
  `direnv exec . cargo nextest run -p xiuxian-wendao --bin wendao --features julia query`
  `direnv exec . cargo nextest run --no-tests pass -p xiuxian-wendao --bin wendao_search_flightsql_server --features julia`
  `direnv exec . cargo clippy -p xiuxian-wendao --all-targets --all-features --message-format=short -- -D warnings`
  `direnv exec . git diff --check`

## Wendao Search SQL Test Ownership Migration

- opened the next dedicated execution plan for the remaining SQL
  test-ownership slice at
  `.cache/agent/execplans/wendao-search-sql-test-ownership-migration.md`
- extended the active shared-query blueprint so the next bounded slice is
  explicit:
  SQL tests should move into canonical `src/search/queries/sql/tests/`, while
  `src/gateway/studio/search/queries/sql/` stays only as the remaining bridge
  seam still needed by production callers
- synchronized package docs so the current rule is explicit:
  gateway-side SQL tests are no longer the desired long-lived home once
  `src/search/queries/sql/` is the canonical adapter owner
- moved the SQL tests and fixtures into `src/search/queries/sql/tests/`, so
  the canonical SQL adapter tree now owns provider, discovery, execution, repo,
  local-symbol, and snapshot coverage directly
- deleted the old gateway `search/queries/` namespace after the moved SQL
  tests no longer referenced it and native Flight wiring was already importing
  the canonical SQL provider
- confirmed final verification is green:
  `direnv exec . cargo fmt -p xiuxian-wendao`
  `direnv exec . cargo nextest run -p xiuxian-wendao --lib --features julia search::queries::sql::tests::`
  `direnv exec . cargo nextest run -p xiuxian-wendao --lib --features julia gateway::studio::search::handlers::flight::tests::`
  `direnv exec . cargo nextest run -p xiuxian-wendao --bin wendao --features julia query`
  `direnv exec . cargo clippy -p xiuxian-wendao --all-targets --all-features --message-format=short -- -D warnings`
  `direnv exec . git diff --check`

## Wendao Search Query Core Extraction

- opened the next dedicated execution plan for the shared request-scoped
  query-core slice at
  `.cache/agent/execplans/wendao-search-query-core-extraction.md`
- extended the active shared-query blueprint so the next bounded slice is
  explicit:
  SQL, GraphQL, and FlightSQL should stop assembling the request-scoped query
  surface independently and instead consume one canonical query-core seam
- synchronized package docs so the current rule is explicit:
  request-scoped surface assembly belongs to one shared query-core owner under
  `src/search/queries/`, while adapters stay responsible only for protocol
  translation and result shaping
- landed the shared request-scoped query-core seam under
  `src/search/queries/core/`, so SQL, GraphQL, and FlightSQL now open one
  canonical `SearchEngineContext + SqlQuerySurface` owner instead of each
  calling the low-level SQL surface-registration helper directly
- kept adapter semantics unchanged while removing the duplicated request-scoped
  surface assembly from:
  `src/search/queries/sql/execution/service.rs`
  `src/search/queries/graphql/execution.rs`
  `src/search/queries/flightsql/service.rs`
- fixed one adjacent feature-gated bin compilation blocker in
  `src/link_graph/runtime_config/resolve/policy/retrieval/base.rs` so the
  `wendao` query bin gate could complete under `--features julia`
- confirmed final verification is green:
  `direnv exec . cargo fmt -p xiuxian-wendao`
  `direnv exec . cargo nextest run -p xiuxian-wendao --lib --features julia search::queries::sql::tests::`
  `direnv exec . cargo nextest run -p xiuxian-wendao --lib --features julia search::queries::graphql::tests::`
  `direnv exec . cargo nextest run -p xiuxian-wendao --lib --features julia search::queries::flightsql::tests::`
  `direnv exec . cargo nextest run -p xiuxian-wendao --bin wendao --features julia query`
  `direnv exec . cargo nextest run --no-tests pass -p xiuxian-wendao --bin wendao_search_flightsql_server --features julia`
  `direnv exec . cargo clippy -p xiuxian-wendao --all-targets --all-features --message-format=short -- -D warnings`
  `direnv exec . git diff --check`

## Wendao Search Query Service Extraction

- opened the next dedicated execution plan for the shared query-service slice
  at `.cache/agent/execplans/wendao-search-query-service-extraction.md`
- extended the active shared-query blueprint so the next bounded slice is
  explicit:
  CLI, SQL Flight, and FlightSQL should stop carrying raw
  `SearchPlaneService` ownership independently and instead consume one
  canonical `SearchQueryService` above the landed request-scoped query core
- synchronized package docs so the current rule is explicit:
  `src/search/queries/core/` now owns request-scoped core assembly, and the
  next ownership tightening is one shared query-service seam above that core
  for canonical adapters and CLI entrypoints
- landed `SearchQueryService` under `src/search/queries/core/` as the shared
  service owner above the request-scoped query core
- retargeted canonical SQL execution, SQL Flight, FlightSQL, GraphQL, and CLI
  query entrypoints so they no longer carry raw `SearchPlaneService`
  ownership independently
- kept adapter-local parsing and result shaping unchanged while reducing
  shared-query ownership to one canonical service seam
- confirmed final verification is green:
  `direnv exec . cargo fmt -p xiuxian-wendao`
  `direnv exec . cargo nextest run -p xiuxian-wendao --lib --features julia search::queries::sql::tests::`
  `direnv exec . cargo nextest run -p xiuxian-wendao --lib --features julia search::queries::graphql::tests::`
  `direnv exec . cargo nextest run -p xiuxian-wendao --lib --features julia search::queries::flightsql::tests::`
  `direnv exec . cargo nextest run -p xiuxian-wendao --bin wendao --features julia query`
  `direnv exec . cargo nextest run --no-tests pass -p xiuxian-wendao --bin wendao_search_flightsql_server --features julia`
  `direnv exec . cargo clippy -p xiuxian-wendao --all-targets --all-features --message-format=short -- -D warnings`
  `direnv exec . git diff --check`

## Wendao Queries REST Adapter

- opened the next dedicated execution plan for the first REST adapter slice at
  `.cache/agent/execplans/wendao-queries-rest-adapter.md`
- extended the active shared-query blueprint so the next bounded slice is
  explicit:
  `search/queries/rest/` should land as one thin REST-style adapter over the
  canonical `SearchQueryService`, and `wendao query rest` should prove that it
  is not a second planner path
- synchronized package docs so the current rule is explicit:
  the first REST slice should stay inside the canonical shared query tree and
  delegate into SQL or GraphQL through the already-landed query core and query
  service seams
- landed the first canonical REST adapter under
  `packages/rust/crates/xiuxian-wendao/src/search/queries/rest/`, keeping the
  request contract bounded to `Sql { query }` and `Graphql { document }`
  variants instead of widening native HTTP routes
- landed the CLI proof under
  `packages/rust/crates/xiuxian-wendao/src/bin/wendao/types/commands/query/`
  and
  `packages/rust/crates/xiuxian-wendao/src/bin/wendao/execute/query/`
  with `wendao query rest --payload ...`, so REST now consumes the same
  `SearchQueryService` as SQL and GraphQL instead of opening another planner
  path
- synchronized package docs for the landed state:
  `packages/rust/crates/xiuxian-wendao/README.md`
  `packages/rust/crates/xiuxian-wendao/docs/03_features/210_search_queries_architecture.md`
  `packages/rust/crates/xiuxian-wendao/docs/03_features/213_rest_query_surface.md`
  `packages/rust/crates/xiuxian-wendao/docs/index.md`
- confirmed final verification is green:
  `direnv exec . cargo fmt -p xiuxian-wendao`
  `direnv exec . cargo nextest run -p xiuxian-wendao --lib --features julia search::queries::rest::tests::`
  `direnv exec . cargo nextest run -p xiuxian-wendao --lib --features julia search::queries::sql::tests::`
  `direnv exec . cargo nextest run -p xiuxian-wendao --lib --features julia search::queries::graphql::tests::`
  `direnv exec . cargo nextest run -p xiuxian-wendao --bin wendao --features julia query`
  `direnv exec . cargo clippy -p xiuxian-wendao --all-targets --all-features --message-format=short -- -D warnings`
  `direnv exec . git diff --check`
- archived the completed ExecPlan at
  `.cache/agent/execplans/archives/wendao-queries-rest-adapter.md`

## Wendao Search Query Snapshot Contracts

- opened the next dedicated execution plan for mandatory shared-query snapshot
  contracts at
  `.cache/agent/execplans/archives/wendao-search-query-snapshot-contracts.md`
- extended the active shared-query blueprint so the current rule is explicit:
  every canonical adapter under `src/search/queries/*` must own snapshot-level
  regression coverage for its client-facing payload surface
- synchronized package docs so the next bounded rule is explicit:
  SQL snapshot coverage is the baseline, but GraphQL, FlightSQL, and REST must
  now match it and keep their snapshot suites beside the canonical adapter
  tests
- landed the missing canonical adapter snapshot suites under:
  `packages/rust/crates/xiuxian-wendao/src/search/queries/graphql/tests/snapshots.rs`
  `packages/rust/crates/xiuxian-wendao/src/search/queries/flightsql/tests/snapshots.rs`
  `packages/rust/crates/xiuxian-wendao/src/search/queries/rest/tests/snapshots.rs`
- refreshed snapshot baselines under
  `packages/rust/crates/xiuxian-wendao/tests/snapshots/gateway/studio/` for:
  `studio_graphql_query_surface_payload.snap`
  `studio_flightsql_query_surface_payload.snap`
  `studio_rest_query_surface_payload.snap`
- landed a canonical source-tree enforcement test at
  `packages/rust/crates/xiuxian-wendao/src/search/queries/tests/structure.rs`
  so `src/search/queries/{sql,graphql,flightsql,rest}/tests/snapshots.rs`
  remains mandatory instead of a convention
- synchronized package docs for the landed state:
  `packages/rust/crates/xiuxian-wendao/README.md`
  `packages/rust/crates/xiuxian-wendao/docs/03_features/210_search_queries_architecture.md`
  `packages/rust/crates/xiuxian-wendao/docs/03_features/211_graphql_query_surface.md`
  `packages/rust/crates/xiuxian-wendao/docs/03_features/212_flightsql_query_surface.md`
  `packages/rust/crates/xiuxian-wendao/docs/03_features/213_rest_query_surface.md`
- confirmed final verification is green:
  `direnv exec . cargo fmt -p xiuxian-wendao`
  `direnv exec . env INSTA_UPDATE=always cargo nextest run -p xiuxian-wendao --lib --features julia search::queries::`
  `direnv exec . cargo clippy -p xiuxian-wendao --all-targets --all-features --message-format=short -- -D warnings`
  `direnv exec . git diff --check`
- archived the completed ExecPlan at
  `.cache/agent/execplans/archives/wendao-search-query-snapshot-contracts.md`

## Wendao Search Query Test Support Extraction

- opened the next dedicated execution plan for shared query test-support
  extraction at
  `.cache/agent/execplans/archives/wendao-search-query-test-support-extraction.md`
- extended the active shared-query blueprint so the current rule is explicit:
  repeated shared-query corpus fixtures belong under
  `src/search/queries/tests/`, while protocol-specific decode helpers stay
  adapter-local
- synchronized package docs so the next bounded redundancy-reduction slice is
  explicit:
  GraphQL, REST, and FlightSQL should reuse one shared corpus-fixture seam
  instead of each carrying a private copy of the same search-plane setup
- landed the shared query test-support seam under
  `packages/rust/crates/xiuxian-wendao/src/search/queries/tests/fixtures.rs`
  so repeated corpus-fixture setup now has one canonical owner
- retargeted adapter-local fixtures to that seam:
  `packages/rust/crates/xiuxian-wendao/src/search/queries/graphql/tests/fixtures.rs`
  `packages/rust/crates/xiuxian-wendao/src/search/queries/rest/tests/fixtures.rs`
  `packages/rust/crates/xiuxian-wendao/src/search/queries/flightsql/tests/fixtures.rs`
- synchronized package docs for the landed state:
  `packages/rust/crates/xiuxian-wendao/README.md`
  `packages/rust/crates/xiuxian-wendao/docs/03_features/210_search_queries_architecture.md`
- confirmed final verification is green:
  `direnv exec . cargo fmt -p xiuxian-wendao`
  `direnv exec . cargo nextest run -p xiuxian-wendao --lib --features julia search::queries::`
  `direnv exec . cargo clippy -p xiuxian-wendao --all-targets --all-features --message-format=short -- -D warnings`
  `direnv exec . git diff --check`
- archived the completed ExecPlan at
  `.cache/agent/execplans/archives/wendao-search-query-test-support-extraction.md`

## Persistent Doc Hidden-Path Governance

- opened a dedicated execution plan for canonical-doc hidden-path governance at
  `.cache/agent/execplans/persistent-doc-hidden-path-governance.md`
- tightened `AGENTS.md` so canonical docs now explicitly forbid links to
  hidden tracking or runtime paths such as `.data/*`, `.cache/*`, and
  `.run/*`, while ExecPlans and GTD stay the allowed tracking surfaces for
  those references
- cleaned the active Wendao `core` / `runtime` / plugin migration docs so the
  package index, roadmap notes, and paired RFCs now point only to stable
  RFC/package-doc surfaces instead of direct `.cache/agent/blueprints/*` or
  `.cache/*/execplans/*` links
- cleaned the touched canonical research and feature docs so they no longer
  link local hidden-path paper copies from `.data/research/*`
- landed a `wendao.semantic_check` / docs-governance warning for canonical
  hidden-path links:
  `canonical_doc_hidden_path_link` now flags Markdown links and wikilinks to
  hidden workspace paths in canonical docs while leaving `docs/GTD` and
  similar tracking surfaces exempt
- queued focused validation on:
  `direnv exec . git diff --check`
  `direnv exec . rg -n "\\[\\[\\.data/|\\[\\[\\.cache/|\\]\\([^)]*\\.data/|\\]\\([^)]*\\.cache/" AGENTS.md packages/rust/crates/xiuxian-wendao/docs docs/rfcs docs/01_core/qianhuan/orchestration-spec.md docs/02_dev/standards/LINK_GRAPH_CODE_ADAPTATION.md docs/03_features/wendao-agentic-retrieval.md docs/03_features/wendao-living-brain.md docs/03_features/wendao-context-snapshot.md docs/04_chronicles/research/synapse-audit-calibration.md`
  `direnv exec . cargo test -p xiuxian-wendao docs_governance::tests::hidden_links`

## Blueprint Governance Alignment

- opened a dedicated execution plan for blueprint-governance alignment at
  `.cache/agent/execplans/blueprint-governance-alignment.md`
- verified that `AGENTS.md` still mandates the `blueprint -> ExecPlan -> docs`
  chain, but observed two active drifts:
  the Wendao core/runtime/plugin migration blueprint path was referenced widely
  while the file itself was absent, and `AGENTS.md` still implied one static
  active blueprint instead of task-scoped blueprint selection
- restored `.cache/agent/blueprints/wendao_arrow_plugin_core_runtime_migration.md`
  as the active blueprint for Wendao `core` / `runtime` / plugin-package
  migration, derived from the current RFC and roadmap ownership rules
- corrected `AGENTS.md` again after governance review:
  removed the hard-coded Wendao-specific migration reference and aligned the
  policy with the intended split where blueprint governance and templates live
  under `.agent/`, while generated blueprint files live under
  `.cache/agent/blueprints/`
- clarified the governance contract:
  the blueprint is the durable architecture rule set, while the ExecPlan is
  the task-local execution record that cites the governing blueprint path for
  one bounded implementation slice
- added the archive rule:
  active blueprint and ExecPlan files stay under
  `$PRJ_CACHE_HOME/agent/{blueprints,execplans}/`, completed ExecPlans move to
  their `archives/` subdirectory after validation, and blueprints move to
  `$PRJ_CACHE_HOME/agent/blueprints/archives/` only when the governed
  workstream is fully complete
- added `.agent/blueprints/_template.md` so blueprint generation now has a
  repository-local template alongside `.agent/PLANS.md` and
  `.agent/execplans/_template.md`
- updated the stale Wendao roadmap notes that still claimed
  `project_anchor_semantic_addressing.md` was absent in the current checkout:
  `402_repo_intelligence_mvp.md`
  `405_large_rust_modularization.md`
  `406_studio_search_plane.md`
- queued focused validation on:
  `direnv exec . git diff --check`
  `direnv exec . rg -n "not currently present|absent from this checkout|still absent" packages/rust/crates/xiuxian-wendao/docs/06_roadmap/402_repo_intelligence_mvp.md packages/rust/crates/xiuxian-wendao/docs/06_roadmap/405_large_rust_modularization.md packages/rust/crates/xiuxian-wendao/docs/06_roadmap/406_studio_search_plane.md AGENTS.md`
- confirmed `direnv exec . git diff --check` passes
- confirmed the targeted stale-absence scan now returns no matches, so the
  active Wendao roadmap notes no longer claim the semantic-addressing
  blueprint is missing

## Wendao Core Runtime Julia Docs

- opened a dedicated execution plan for the Wendao docs layering lane at
  `.cache/agent/execplans/wendao-core-runtime-julia-docs.md`
- used the existing roadmap context in
  `409_core_runtime_plugin_surface_inventory.md`,
  `412_core_runtime_plugin_program.md`,
  `415_m4_julia_externalization_package_list.md`, and
  `417_wendao_package_boundary_matrix.md` as the base contract for this slice
- added `packages/rust/crates/xiuxian-wendao/docs/01_core/103_package_layering.md`
  so `core`, `runtime`, `wendao`, gateway, parser, Arrow Flight, DataFusion,
  and plugin ownership now have one architecture-level explanation inside
  `wendao/docs`
- added `packages/rust/crates/xiuxian-wendao/docs/06_roadmap/418_julia_plugin_first_rollout.md`
  so the next execution lane is explicitly “plugin-first Julia”, not another
  host-crate expansion
- updated `packages/rust/crates/xiuxian-wendao/docs/index.md` to link the new
  architecture and roadmap notes
- fixed the immediate documentation rule for the next phase:
  `xiuxian-wendao-julia` should own thick Julia-specific implementation,
  `xiuxian-wendao-runtime` should own generic plugin loading and host wiring,
  and `xiuxian-wendao` should stay on domain semantics plus thin plugin
  consumption seams
- queued focused validation on:
  `direnv exec . git diff --check`

## Wendao Julia Plugin Ownership Slice

- opened a dedicated execution plan for the current Julia plugin ownership
  move at `.cache/agent/execplans/wendao-julia-plugin-ownership-slice.md`
- moved the Julia Arrow rerank exchange seam out of
  `xiuxian-wendao/analyzers/service/julia_transport/` and into the plugin
  crate under
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/rerank_exchange/`
- made `xiuxian-wendao/analyzers/service/julia_transport/mod.rs` a thin
  re-export seam over `xiuxian-wendao-core` schema contracts and the
  plugin-owned Julia rerank helpers
- updated host call sites so bootstrap and link-graph rerank validation import
  plugin-owned Julia registration or transport helpers directly instead of
  reaching through host-local Julia implementation modules
- synchronized the active plugin-first roadmap and both crate READMEs so the
  landed boundary now records that the Julia rerank exchange is plugin-owned
- queued focused validation on:
  `direnv exec . cargo test -p xiuxian-wendao-julia --lib rerank_exchange`
  `direnv exec . cargo test -p xiuxian-wendao --lib analyzers::service`
  `direnv exec . git diff --check`
- confirmed `direnv exec . cargo test -p xiuxian-wendao-julia --lib rerank_exchange`
  passes
- confirmed `direnv exec . cargo test -p xiuxian-wendao --lib analyzers::service`
  passes
- opened a second dedicated execution plan for the next Julia host-thinning
  move at `.cache/agent/execplans/wendao-julia-compat-wrapper-thinning.md`
- collapsed the host-owned Julia compatibility wrapper shell for the
  link-graph rerank lane:
  `link_graph/runtime_config/models/retrieval/julia_rerank/` now resolves
  directly to plugin-owned Julia compatibility types plus local compatibility
  aliases, and `link_graph/plugin_runtime/compat/` now re-exports the plugin
  helper directly
- deleted the redundant host forwarding files that previously wrapped the
  Julia compatibility types one by one
- synchronized `418_julia_plugin_first_rollout.md` so the roadmap now records
  the compatibility-wrapper thinning slice separately from the earlier Julia
  Arrow rerank-exchange migration
- opened a third dedicated execution plan for gateway flight-boundary thinning
  at `.cache/agent/execplans/wendao-gateway-flight-boundary-thinning.md`
- moved the Studio/search-plane Flight adapter implementation out of
  `link_graph/plugin_runtime/transport/server.rs` into
  `gateway/studio/search/handlers/flight/search_plane.rs`
- reduced `link_graph/plugin_runtime/transport/server.rs` to a thin re-export
  surface over the gateway-owned Flight adapter functions and provider type
- updated gateway-local repo-content search code to import
  `SearchPlaneRepoSearchFlightRouteProvider` from the gateway handler surface
  instead of routing through `link_graph::plugin_runtime`
- synchronized `418_julia_plugin_first_rollout.md` so the roadmap now records
  the gateway flight-boundary thinning slice alongside the previous Julia
  ownership moves
- opened a fourth dedicated execution plan for the remaining internal-caller
  retargeting step at
  `.cache/agent/execplans/wendao-gateway-flight-internal-caller-retargeting.md`
- switched `src/bin/wendao/execute/gateway/command.rs`,
  `src/bin/wendao_search_flight_server.rs`, and
  `src/bin/wendao_search_seed_sample.rs` to import the Studio Flight-service
  builder and sample-data bootstrap helper from a gateway-owned public
  `gateway::studio` surface instead of from `link_graph::plugin_runtime`, so
  the remaining host-internal gateway and standalone sample-server startup
  paths now follow the same boundary
- observed focused validation is currently blocked by unrelated crate-baseline
  compile failures outside the touched files:
  default-feature validation still stops in
  `gateway/studio/search/handlers/sql/registration/*`, and a
  `--no-default-features` probe stops in broader `gateway::studio` /
  `search_plane` feature-gating paths
- reran focused bin validation after moving the remaining internal callers to
  the gateway-owned public `gateway::studio` surface; both
  `direnv exec . cargo test -p xiuxian-wendao --bin wendao gateway::` and
  `direnv exec . cargo check -p xiuxian-wendao --bin wendao_search_flight_server --bin wendao_search_seed_sample`
  are back to green in the current worktree, and
  `direnv exec . cargo test -p xiuxian-wendao --lib gateway::studio::search::handlers::sql`
  also passes
- opened a fifth dedicated execution plan for retiring the now-unused legacy
  Flight re-export seam at
  `.cache/agent/execplans/wendao-plugin-runtime-flight-export-retirement.md`
- removed `link_graph/plugin_runtime/transport/server.rs` and the related
  Flight-helper re-exports from `link_graph/plugin_runtime/{transport/mod.rs,mod.rs}`
  now that all host-internal callers use the gateway-owned public
  `gateway::studio` surface directly
- opened a sixth dedicated execution plan for retiring the remaining
  plugin-runtime transport wrapper layer at
  `.cache/agent/execplans/wendao-plugin-runtime-transport-wrapper-retirement.md`
- switched the remaining transport negotiation call sites and tests to import
  `NegotiatedFlightTransportClient`, `NegotiatedTransportSelection`,
  `negotiate_flight_transport_client_from_bindings`, and
  `CANONICAL_PLUGIN_TRANSPORT_PREFERENCE_ORDER` from
  `xiuxian-wendao-runtime::transport` instead of from
  `link_graph::plugin_runtime`
- deleted `link_graph/plugin_runtime/transport/` entirely, so the shrinking
  `plugin_runtime` shell no longer wraps runtime transport negotiation or core
  transport types
- opened a seventh dedicated execution plan for retiring the plugin-runtime
  artifact wrapper layer at
  `.cache/agent/execplans/wendao-plugin-runtime-artifact-wrapper-retirement.md`
- moved the Wendao-specific plugin artifact resolve/render helpers to
  `link_graph/runtime_config/artifacts.rs`, and switched the zhenfa deployment
  export surface plus the Studio capability handler to import those helpers
  from `link_graph::runtime_config`
- deleted `link_graph/plugin_runtime/artifacts/` entirely, so the shrinking
  `plugin_runtime` shell no longer wraps artifact payload typing or
  runtime-config-backed TOML rendering
- opened an eighth dedicated execution plan for retiring the remaining
  `link_graph::plugin_runtime` namespace at
  `.cache/agent/execplans/wendao-plugin-runtime-module-retirement.md`
- switched the last rerank-binding call sites and tests to import
  `build_rerank_provider_binding` directly from
  `xiuxian-wendao-julia::compatibility::link_graph`
- moved the remaining plugin-runtime compatibility and direct artifact
  assertions onto the `link_graph::runtime_config` test surface
- deleted `link_graph/plugin_runtime/` entirely, so `xiuxian-wendao` no longer
  exposes a host-local plugin-runtime forwarding namespace at all
- queued focused validation on:
  `direnv exec . cargo test -p xiuxian-wendao --lib link_graph::runtime_config`
  `direnv exec . cargo test -p xiuxian-wendao --lib link_graph::index::search::plan::payload::quantum`
  `direnv exec . cargo test -p xiuxian-wendao --lib zhenfa_router::native::deployment`
  `direnv exec . cargo test -p xiuxian-wendao --lib gateway::studio::router::tests::config`
  `direnv exec . git diff --check`
- confirmed the full retirement validation is green:
  `direnv exec . cargo test -p xiuxian-wendao --lib link_graph::runtime_config`
  `direnv exec . cargo test -p xiuxian-wendao --lib link_graph::index::search::plan::payload::quantum`
  `direnv exec . cargo test -p xiuxian-wendao --lib zhenfa_router::native::deployment`
  `direnv exec . cargo test -p xiuxian-wendao --lib gateway::studio::router::tests::config`
  `direnv exec . git diff --check`
- opened a ninth dedicated execution plan for retiring the retrieval-level
  Julia forwarding shell at
  `.cache/agent/execplans/wendao-retrieval-julia-shell-retirement.md`
- removed `link_graph/runtime_config/models/retrieval/julia_rerank/` and
  switched retrieval-policy/runtime-config code to consume Julia-owned runtime
  and selector types directly from `xiuxian-wendao-julia`
- removed the host-local `LinkGraphCompat*` alias names from touched source
  tests and renamed the remaining test-only runtime-config helpers from
  `compat` to `julia`
- queued focused validation on:
  `direnv exec . cargo test -p xiuxian-wendao --lib link_graph::runtime_config`
  `direnv exec . cargo test -p xiuxian-wendao --lib link_graph::index::search::plan::payload::quantum`
  `direnv exec . git diff --check`

## Wendao Julia Integration Support Ownership

- opened a dedicated execution plan for the next plugin-first Julia slice at
  `.cache/agent/execplans/wendao-julia-integration-support-ownership.md`
- re-ran the blueprint check before implementation and selected the active
  governing blueprint
  `.cache/agent/blueprints/wendao_arrow_plugin_core_runtime_migration.md`
  because this slice is explicitly about moving the remaining Julia-specific
  integration-support ownership out of the host crate
- bounded the slice to the host integration wrapper
  `tests/integration/support/wendaoarrow_official_examples.rs` and the four
  planned-search integration tests that still import it
- synchronized the Julia plugin-first roadmap note so the package doc now
  records the queued integration-support ownership slice before code movement
- added `packages/rust/crates/xiuxian-wendao-julia/src/integration_support/`
  so the plugin crate now owns a bounded public support surface for
  WendaoArrow and WendaoAnalyzer official-example services
- moved the four host planned-search integration tests onto
  `xiuxian_wendao_julia::integration_support::*` and deleted the host-local
  wrapper file
  `packages/rust/crates/xiuxian-wendao/tests/integration/support/wendaoarrow_official_examples.rs`
- corrected the first implementation attempt after it leaked `plugin::test_support`
  into non-test builds and produced dead-code warnings; the final landed shape
  keeps `plugin::test_support` under `cfg(test)` and makes
  `integration_support` a self-contained public surface instead
- updated the surface inventory note
  `409_core_runtime_plugin_surface_inventory.md` so canonical docs no longer
  reference the deleted host wrapper path
- verified the landed slice with:
  `direnv exec . cargo fmt -p xiuxian-wendao-julia -p xiuxian-wendao`
  `direnv exec . cargo test -p xiuxian-wendao-julia --lib`
  `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate planned_search_julia_rerank_official_example::test_planned_search_payload_applies_official_stream_scoring_example --features julia -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate planned_search_julia_rerank_metadata_example::test_planned_search_payload_applies_official_stream_metadata_example --features julia -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate planned_search_wendaoanalyzer_linear_blend::test_planned_search_payload_applies_wendaoanalyzer_linear_blend_example --features julia -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate planned_search_wendaoanalyzer_similarity_only::test_planned_search_payload_applies_wendaoanalyzer_similarity_only_strategy --features julia -- --exact`
  `direnv exec . rg -n "wendaoarrow_official_examples" packages/rust/crates/xiuxian-wendao/src packages/rust/crates/xiuxian-wendao/tests packages/rust/crates/xiuxian-wendao-julia/src`
  `direnv exec . git diff --check`

## Wendao Julia Custom Service Ownership

- opened a dedicated execution plan for the next plugin-first Julia slice at
  `.cache/agent/execplans/wendao-julia-custom-service-ownership.md`
- re-ran the blueprint check before implementation and selected the active
  governing blueprint
  `.cache/agent/blueprints/wendao_arrow_plugin_core_runtime_migration.md`
  because this slice is the direct follow-up to the Julia integration-support
  migration
- bounded the slice to the remaining host-local custom scoring support files
  `tests/integration/support/wendaoarrow_common.rs` and
  `tests/integration/support/wendaoarrow_custom_service.rs`, plus the two
  Julia rerank tests that still import them
- synchronized the Julia plugin-first roadmap note so the package doc now
  records the queued custom-service ownership slice before code movement
- added `packages/rust/crates/xiuxian-wendao-julia/src/integration_support/custom_service.rs`
  and `packages/rust/crates/xiuxian-wendao-julia/src/integration_support/common.rs`
  so the plugin crate now owns both the custom scoring service helper and the
  shared Julia integration-service process utilities
- refactored `integration_support/official_examples.rs` to consume the shared
  `common` helpers, keeping the plugin-owned integration surface folderized by
  responsibility instead of growing one flat file
- switched `planned_search_julia_rerank.rs` and
  `planned_search_julia_rerank_vector_store.rs` to import
  `WendaoArrowScoreRow` and `spawn_wendaoarrow_custom_scoring_service` from
  `xiuxian_wendao_julia::integration_support`
- deleted the remaining host-local Julia support files
  `packages/rust/crates/xiuxian-wendao/tests/integration/support/wendaoarrow_common.rs`
  and
  `packages/rust/crates/xiuxian-wendao/tests/integration/support/wendaoarrow_custom_service.rs`
- verified the landed slice with:
  `direnv exec . cargo fmt -p xiuxian-wendao-julia -p xiuxian-wendao`
  `direnv exec . cargo test -p xiuxian-wendao-julia --lib`
  `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate planned_search_julia_rerank::test_planned_search_payload_applies_julia_rerank_scores --features julia -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate planned_search_julia_rerank_vector_store::test_planned_search_payload_with_query_vector_applies_julia_rerank_on_vector_store --features julia -- --exact`
  `direnv exec . rg -n "wendaoarrow_custom_service|wendaoarrow_common" packages/rust/crates/xiuxian-wendao/src packages/rust/crates/xiuxian-wendao/tests packages/rust/crates/xiuxian-wendao-julia/src`
  `direnv exec . git diff --check`
- confirmed the selector-surface retirement validation is green:
  `direnv exec . cargo test -p xiuxian-wendao --lib link_graph::runtime_config`
  `direnv exec . cargo test -p xiuxian-wendao --lib link_graph::index::search::plan::payload::quantum`
  `direnv exec . git diff --check`
- confirmed the retrieval-shell retirement validation is green:
  `direnv exec . cargo test -p xiuxian-wendao --lib link_graph::runtime_config`
  `direnv exec . cargo test -p xiuxian-wendao --lib link_graph::index::search::plan::payload::quantum`
  `direnv exec . git diff --check`
- opened a tenth dedicated execution plan for retiring the remaining Julia
  selector re-export surface at
  `.cache/agent/execplans/wendao-julia-selector-surface-retirement.md`
- removed the public `julia_deployment_artifact_selector` re-export from
  `xiuxian-wendao` and switched touched runtime-config internals to import
  Julia selector/runtime types directly from `xiuxian-wendao-julia`
- updated the package README and plugin-first roadmap so they no longer
  describe a surviving host re-export seam for Julia runtime records
- queued focused validation on:
  `direnv exec . cargo test -p xiuxian-wendao --lib link_graph::runtime_config`
  `direnv exec . cargo test -p xiuxian-wendao --lib link_graph::index::search::plan::payload::quantum`
  `direnv exec . git diff --check`

## Wendao Julia Runtime Config Ownership

- opened a dedicated execution plan for the next plugin-first Julia slice at
  `.cache/agent/execplans/wendao-julia-runtime-config-ownership.md`
- re-ran the blueprint check before implementation and selected the active
  governing blueprint
  `.cache/agent/blueprints/wendao_arrow_plugin_core_runtime_migration.md`
  because this slice keeps Julia-specific runtime-config semantics moving out
  of the host retrieval policy resolver
- bounded the slice to the host-local Julia helper in
  `src/link_graph/runtime_config/resolve/policy/retrieval/provider.rs`,
  the Julia rerank env constants in `runtime_config/constants.rs`, the plugin
  compatibility runtime surface in `xiuxian-wendao-julia`, and the focused
  runtime-config tests that prove config/env override behavior
- synchronized the Julia plugin-first roadmap note so the package doc now
  records the queued runtime-config ownership slice before code movement
- added `packages/rust/crates/xiuxian-wendao-julia/src/compatibility/link_graph/settings.rs`
  so the plugin crate now owns the Julia rerank settings keys, env constants,
  and normalization/application logic for
  `LinkGraphJuliaRerankRuntimeConfig`
- switched the host retrieval runtime resolver to call
  `LinkGraphJuliaRerankRuntimeConfig::resolve_with_settings(&settings)` and
  deleted the host-local
  `src/link_graph/runtime_config/resolve/policy/retrieval/provider.rs`
- deleted the Julia rerank env constants from
  `src/link_graph/runtime_config/constants.rs` and removed the now-dead
  `src/link_graph/runtime_config/settings/parse.rs` helper layer instead of
  keeping a compatibility shell
- added focused plugin compatibility coverage for config/env resolution and
  settings-over-env precedence without mutating process-global environment
- verified the landed slice with:
  `direnv exec . cargo fmt -p xiuxian-wendao-julia -p xiuxian-wendao`
  `direnv exec . cargo test -p xiuxian-wendao-julia --lib compatibility::link_graph`
  `direnv exec . cargo test -p xiuxian-wendao --lib link_graph::runtime_config`
  `direnv exec . rg -n "LINK_GRAPH_JULIA_RERANK_|apply_plugin_rerank_runtime_config_to_julia_runtime" packages/rust/crates/xiuxian-wendao/src packages/rust/crates/xiuxian-wendao-julia/src`
  `direnv exec . git diff --check`

## Wendao Julia Artifact Ownership

- opened a dedicated execution plan for the next plugin-first Julia slice at
  `.cache/agent/execplans/wendao-julia-artifact-ownership.md`
- re-ran the blueprint check before implementation and selected the active
  governing blueprint
  `.cache/agent/blueprints/wendao_arrow_plugin_core_runtime_migration.md`
  because this slice keeps Julia-specific deployment-artifact behavior moving
  out of the host runtime-config tree
- bounded the slice to
  `src/link_graph/runtime_config/artifacts.rs`,
  `src/link_graph/runtime_config.rs`,
  the Julia compatibility artifact surface in `xiuxian-wendao-julia`, and the
  focused runtime-config / compatibility tests that prove Julia deployment
  artifact payload and TOML rendering behavior
- synchronized the Julia plugin-first roadmap note so the package doc now
  records the queued artifact-ownership slice before code movement
- added Julia deployment-artifact helpers to
  `packages/rust/crates/xiuxian-wendao-julia/src/compatibility/link_graph/artifact.rs`
  so the plugin crate now owns artifact payload resolution, transport
  diagnostics, and TOML rendering for
  `LinkGraphJuliaRerankRuntimeConfig`
- switched host `src/link_graph/runtime_config/artifacts.rs` to thin selector
  dispatch that calls the plugin-owned Julia artifact helpers instead of
  keeping local Julia-specific implementation
- added focused plugin compatibility coverage for Julia artifact payload
  transport diagnostics and artifact TOML rendering
- verified the landed slice with:
  `direnv exec . cargo fmt -p xiuxian-wendao-julia -p xiuxian-wendao`
  `direnv exec . cargo test -p xiuxian-wendao-julia --lib compatibility::link_graph`
  `direnv exec . cargo test -p xiuxian-wendao --lib link_graph::runtime_config`
  `direnv exec . rg -n "attach_plugin_artifact_transport_diagnostics|resolve_link_graph_plugin_artifact_for_selector" packages/rust/crates/xiuxian-wendao/src packages/rust/crates/xiuxian-wendao-julia/src`
  `direnv exec . git diff --check`

## Wendao Julia Artifact Wrapper Retirement

- opened a dedicated execution plan for the next plugin-first Julia slice at
  `.cache/agent/execplans/wendao-julia-artifact-wrapper-retirement.md`
- re-ran the blueprint check before implementation and selected the active
  governing blueprint
  `.cache/agent/blueprints/wendao_arrow_plugin_core_runtime_migration.md`
  because this slice removes the last Julia-specific test-only wrapper surface
  left in the host runtime-config module
- bounded the slice to `src/link_graph/runtime_config.rs`,
  `src/link_graph/runtime_config/tests.rs`, and the roadmap/GTD tracking
  surfaces for this plugin-first Julia lane
- synchronized the Julia plugin-first roadmap note so the package doc now
  records the queued artifact-wrapper retirement slice before code movement
- removed the test-only host wrappers
  `resolve_link_graph_julia_deployment_artifact` and
  `export_link_graph_julia_deployment_artifact_toml` from
  `src/link_graph/runtime_config.rs`
- updated `src/link_graph/runtime_config/tests.rs` to consume the generic
  artifact-dispatch surface plus `LinkGraphJuliaDeploymentArtifact` directly,
  without introducing any new compatibility layer
- verified the landed slice with:
  `direnv exec . cargo fmt -p xiuxian-wendao`
  `direnv exec . cargo test -p xiuxian-wendao --lib link_graph::runtime_config`
  `direnv exec . rg -n "resolve_link_graph_julia_deployment_artifact|export_link_graph_julia_deployment_artifact_toml" packages/rust/crates/xiuxian-wendao/src/link_graph/runtime_config.rs packages/rust/crates/xiuxian-wendao/src/link_graph/runtime_config/tests.rs`
  `direnv exec . git diff --check`

## Wendao Julia Compatibility Proof Extraction

- opened a dedicated execution plan for the next plugin-first Julia slice at
  `.cache/agent/execplans/wendao-julia-compatibility-proof-extraction.md`
- re-ran the blueprint check before implementation and selected the active
  governing blueprint
  `.cache/agent/blueprints/wendao_arrow_plugin_core_runtime_migration.md`
  because this slice is about aligning test ownership with the already-moved
  Julia compatibility implementation
- bounded the slice to
  `src/link_graph/runtime_config/tests.rs`,
  `packages/rust/crates/xiuxian-wendao-julia/src/compatibility/link_graph/tests.rs`,
  and the roadmap/GTD tracking surfaces for this plugin-first Julia lane
- synchronized the Julia plugin-first roadmap note so the package doc now
  records the queued compatibility-proof extraction slice before code movement
- moved the pure Julia compatibility proofs for deployment-artifact TOML
  writing, deployment-artifact JSON writing, and rerank binding conversion into
  `packages/rust/crates/xiuxian-wendao-julia/src/compatibility/link_graph/tests.rs`
- deleted those three proofs from
  `packages/rust/crates/xiuxian-wendao/src/link_graph/runtime_config/tests.rs`,
  which now keeps only 7 host-owned end-to-end runtime-config tests
- verified the landed slice with:
  `direnv exec . cargo fmt -p xiuxian-wendao-julia -p xiuxian-wendao`
  `direnv exec . cargo test -p xiuxian-wendao-julia --lib compatibility::link_graph`
  `direnv exec . cargo test -p xiuxian-wendao --lib link_graph::runtime_config`
  `direnv exec . rg -n "test_julia_deployment_artifact_writes_toml_file|test_julia_deployment_artifact_writes_json_file|julia_rerank_runtime_converts_into_generic_binding" packages/rust/crates/xiuxian-wendao/src/link_graph/runtime_config/tests.rs packages/rust/crates/xiuxian-wendao-julia/src/compatibility/link_graph/tests.rs`
  `direnv exec . git diff --check`

## Wendao Julia Runtime Config Proof Thinning

- opened a dedicated execution plan for the next plugin-first Julia slice at
  `.cache/agent/execplans/wendao-julia-runtime-config-proof-thinning.md`
- re-ran the blueprint check before implementation and selected the active
  governing blueprint
  `.cache/agent/blueprints/wendao_arrow_plugin_core_runtime_migration.md`
  because this slice is about removing the last thick plugin-local proof from a
  host runtime integration test
- bounded the slice to
  `packages/rust/crates/xiuxian-wendao/src/link_graph/runtime_config/tests.rs`,
  `packages/rust/crates/xiuxian-wendao-julia/src/compatibility/link_graph/tests.rs`,
  and the roadmap/GTD tracking surfaces for this plugin-first Julia lane
- synchronized the Julia plugin-first roadmap note so the package doc now
  records the queued runtime-config proof-thinning slice before code movement
- moved the plugin-local descriptor, launch-manifest, deployment-artifact, and
  direct-binding assertions out of
  `test_retrieval_runtime_resolves_julia_rerank_config` and into the Julia
  compatibility test module
- kept the host runtime-config test focused on config resolution, score-weight
  resolution, generic rerank binding, and generic artifact dispatch, without
  changing any production runtime behavior
- verified the landed slice with:
  `direnv exec . cargo fmt -p xiuxian-wendao-julia -p xiuxian-wendao`
  `direnv exec . cargo test -p xiuxian-wendao-julia --lib compatibility::link_graph`
  `direnv exec . cargo test -p xiuxian-wendao --lib link_graph::runtime_config`
  `direnv exec . rg -n "provider_launch_descriptor|analyzer_launch_manifest|plugin_launch_spec|plugin_artifact_payload|rerank_provider_binding" packages/rust/crates/xiuxian-wendao/src/link_graph/runtime_config/tests.rs packages/rust/crates/xiuxian-wendao-julia/src/compatibility/link_graph/tests.rs`
  `direnv exec . git diff --check`

## Wendao Julia Host Runtime Integration Split

- opened a dedicated execution plan for the next plugin-first Julia slice at
  `.cache/agent/execplans/wendao-julia-host-runtime-integration-split.md`
- re-ran the blueprint check before implementation and selected the active
  governing blueprint
  `.cache/agent/blueprints/wendao_arrow_plugin_core_runtime_migration.md`
  because this slice is about keeping the remaining host test surface aligned
  with real host-owned runtime integration proof
- bounded the slice to
  `packages/rust/crates/xiuxian-wendao/src/link_graph/runtime_config/tests.rs`
  plus the roadmap/GTD tracking surfaces for this Julia plugin-first lane
- synchronized the Julia plugin-first roadmap note so the package doc now
  records the queued host-runtime-integration split before code movement
- added `configure_julia_rerank_runtime_fixture()` so the remaining host Julia
  runtime-config proofs share one local config setup helper
- split the old dense host test into:
  `test_retrieval_runtime_resolves_julia_rerank_config` for raw config
  resolution and
  `test_retrieval_runtime_projects_julia_rerank_host_helpers` for score
  weights, schema version, flight settings, and generic rerank binding
- kept the dedicated host artifact tests unchanged so they remain the owner of
  generic artifact-dispatch and render proof
- removed the touched-scope unused imports that were left behind by the test
  split
- validated the landed slice with:
  `direnv exec . cargo fmt -p xiuxian-wendao`
  `direnv exec . env CARGO_TARGET_DIR="$PRJ_CACHE_HOME/cargo-target/wendao-julia-host-runtime-integration-split" cargo test -p xiuxian-wendao --lib link_graph::runtime_config`
  `direnv exec . rg -n "test_retrieval_runtime_resolves_julia_rerank_config|test_retrieval_runtime_projects_julia_rerank_host_helpers|resolve_link_graph_rerank_score_weights|resolve_link_graph_rerank_schema_version|resolve_link_graph_rerank_flight_runtime_settings|resolve_link_graph_rerank_binding" packages/rust/crates/xiuxian-wendao/src/link_graph/runtime_config/tests.rs`
  `direnv exec . git diff --check`
- used an isolated `CARGO_TARGET_DIR` for the focused test because unrelated
  concurrent cargo jobs were holding the shared artifact directory lock under
  the default target path

## Wendao Julia OpenAPI Artifact Example Ownership

- opened a dedicated execution plan for the next plugin-first Julia slice at
  `.cache/agent/execplans/wendao-julia-openapi-artifact-example-ownership.md`
- re-ran the blueprint check against `.cache/agent/blueprints/` and found no active
  Wendao blueprint file in that tracking location, so this bounded slice is
  proceeding without a governing blueprint while still following the current
  plugin-first package roadmap
- bounded the slice to
  `packages/rust/crates/xiuxian-wendao-julia/src/compatibility/link_graph/`,
  `packages/rust/crates/xiuxian-wendao/src/gateway/openapi/document.rs`,
  `packages/rust/crates/xiuxian-wendao/resources/openapi/wendao_gateway.openapi.json`,
  and the roadmap/GTD tracking surfaces for this Julia plugin-first lane
- synchronized the Julia plugin-first roadmap note so the package doc now
  records the queued OpenAPI artifact-example ownership slice before code
  movement
- added plugin-owned OpenAPI example helpers in
  `packages/rust/crates/xiuxian-wendao-julia/src/compatibility/link_graph/openapi_examples.rs`
  for both the generic Julia plugin artifact and the legacy Julia deployment
  artifact contract
- re-exported those helpers through the Julia compatibility surface and added
  focused plugin tests to keep the example JSON and TOML payloads aligned with
  the existing deployment-artifact types
- rewired
  `packages/rust/crates/xiuxian-wendao/src/gateway/openapi/document.rs` so the
  host bundled OpenAPI assertions compare against plugin-owned example helpers
  instead of carrying hard-coded Julia payload strings
- updated
  `packages/rust/crates/xiuxian-wendao/resources/openapi/wendao_gateway.openapi.json`
  so the bundled resource now matches the plugin-owned launcher path
  `.data/WendaoAnalyzer.jl/scripts/run_analyzer_service.sh`
- aligned the bundled TOML example with the plugin serializer output by using
  the multiline `args` array layout emitted by
  `LinkGraphJuliaDeploymentArtifact::to_toml_string()`
- verified the landed slice with:
  `direnv exec . cargo fmt -p xiuxian-wendao-julia -p xiuxian-wendao`
  `direnv exec . env CARGO_TARGET_DIR="$PRJ_CACHE_HOME/cargo-target/wendao-julia-openapi-artifact-example-ownership" cargo test -p xiuxian-wendao-julia --lib compatibility::link_graph`
  `direnv exec . env CARGO_TARGET_DIR="$PRJ_CACHE_HOME/cargo-target/wendao-julia-openapi-artifact-example-ownership" cargo test -p xiuxian-wendao --lib gateway::openapi::document`
  `direnv exec . rg -n "julia_plugin_artifact_openapi_|julia_deployment_artifact_openapi_|WendaoAnalyzer\\.jl/scripts/run_analyzer_service\\.sh|artifact_schema_version = \\\\\\\"v1\\\\\\\"" packages/rust/crates/xiuxian-wendao-julia/src packages/rust/crates/xiuxian-wendao/src/gateway/openapi/document.rs packages/rust/crates/xiuxian-wendao/resources/openapi/wendao_gateway.openapi.json`
  `git diff --check`

## Wendao Julia Gateway Artifact Fixture Ownership

- opened a dedicated execution plan for the next plugin-first Julia slice at
  `.cache/agent/execplans/wendao-julia-gateway-artifact-fixture-ownership.md`
- re-ran the blueprint check against `.cache/agent/blueprints/` and confirmed there
  is still no active Wendao blueprint file for this bounded slice
- bounded the slice to
  `packages/rust/crates/xiuxian-wendao-julia/src/integration_support/`,
  `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/capabilities/deployment.rs`,
  `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/tests/config.rs`,
  and the roadmap/GTD tracking surfaces for this Julia plugin-first lane
- synchronized the Julia plugin-first roadmap note so the package doc now
  records the queued gateway-artifact fixture ownership slice before code
  movement
- added
  `packages/rust/crates/xiuxian-wendao-julia/src/integration_support/gateway_artifact.rs`
  so the Julia plugin now owns the Studio gateway artifact runtime-config TOML
  fixture, selector path helper, stable schema/base-url helpers, and expected
  TOML fragments
- re-exported those helpers through
  `packages/rust/crates/xiuxian-wendao-julia/src/integration_support/mod.rs`
  and added focused unit coverage for the new helper surface
- rewired
  `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/capabilities/deployment.rs`
  and
  `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/tests/config.rs`
  so the host gateway tests now consume plugin-owned Julia fixture helpers
  instead of repeating raw config snippets, selector literals, and stable TOML
  expectations inline
- landed a minimal validation unblocker in
  `packages/rust/crates/xiuxian-wendao/src/gateway/studio/search/queries/graphql/document.rs`
  by splitting the borrow lifetime and AST lifetime in the GraphQL document
  parser helper signatures; this was required for the focused host gateway test
  lanes to compile under the current worktree
- verified the landed slice with:
  `direnv exec . cargo fmt -p xiuxian-wendao-julia -p xiuxian-wendao`
  `direnv exec . cargo test -p xiuxian-wendao-julia --lib integration_support`
  `direnv exec . env CARGO_TARGET_DIR="$PRJ_CACHE_HOME/cargo-target/wendao-julia-gateway-artifact-fixture-ownership" cargo test -p xiuxian-wendao --lib gateway::studio::router::handlers::capabilities::deployment`
  `direnv exec . env CARGO_TARGET_DIR="$PRJ_CACHE_HOME/cargo-target/wendao-julia-gateway-artifact-fixture-ownership" cargo test -p xiuxian-wendao --lib gateway::studio::router::tests::config`
  `direnv exec . rg -n "julia_gateway_|JULIA_PLUGIN_ID|JULIA_DEPLOYMENT_ARTIFACT_ID|\\[link_graph\\.retrieval\\.julia_rerank\\]" packages/rust/crates/xiuxian-wendao-julia/src/integration_support packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/capabilities/deployment.rs packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/tests/config.rs`
  `git diff --check`

## Wendao Julia UI Artifact Fixture Ownership

- opened a dedicated execution plan for the next plugin-first Julia slice at
  `.cache/agent/execplans/wendao-julia-ui-artifact-fixture-ownership.md`
- re-ran the blueprint check against `.cache/agent/blueprints/` and confirmed there
  is still no active Wendao blueprint file for this bounded slice
- bounded the slice to
  `packages/rust/crates/xiuxian-wendao-julia/src/integration_support/`,
  `packages/rust/crates/xiuxian-wendao/src/gateway/studio/types/config.rs`,
  and the roadmap/GTD tracking surfaces for this Julia plugin-first lane
- synchronized the Julia plugin-first roadmap note so the package doc now
  records the queued UI-artifact fixture ownership slice before code movement
- extended
  `packages/rust/crates/xiuxian-wendao-julia/src/integration_support/gateway_artifact.rs`
  with a stable `PluginArtifactPayload` fixture helper dedicated to Studio UI
  artifact mapping tests
- re-exported that helper through
  `packages/rust/crates/xiuxian-wendao-julia/src/integration_support/mod.rs`
  and added focused unit coverage so the plugin-owned payload fixture keeps its
  stable Julia-specific shape
- rewired
  `packages/rust/crates/xiuxian-wendao/src/gateway/studio/types/config.rs` so
  the host `UiPluginArtifact` mapping test consumes the plugin-owned payload
  fixture and derives expected fields from it instead of constructing the Julia
  payload inline
- verified the landed slice with:
  `direnv exec . cargo fmt -p xiuxian-wendao-julia -p xiuxian-wendao`
  `direnv exec . env CARGO_TARGET_DIR="$PRJ_CACHE_HOME/cargo-target/wendao-julia-ui-artifact-fixture-ownership-plugin" cargo test -p xiuxian-wendao-julia --lib integration_support`
  `direnv exec . env CARGO_TARGET_DIR="$PRJ_CACHE_HOME/cargo-target/wendao-julia-ui-artifact-fixture-ownership" cargo test -p xiuxian-wendao --lib gateway::studio::types::config`
  `direnv exec . rg -n "julia_gateway_|PluginArtifactPayload|UiPluginArtifact|DEFAULT_JULIA_ANALYZER_LAUNCHER_PATH" packages/rust/crates/xiuxian-wendao-julia/src/integration_support packages/rust/crates/xiuxian-wendao/src/gateway/studio/types/config.rs`
  `git diff --check`

## Wendao SQL Information Schema Enablement

- continued the Studio DataFusion SQL capability lane from the existing
  request-scoped `wendao_sql_tables` and `wendao_sql_columns` catalogs
- added `handlers/sql/registration/session.rs` so the SQL provider now builds
  its request-scoped `SearchEngineContext` with DataFusion
  `information_schema` enabled instead of relying only on private Wendao
  catalogs
- kept the SQL surface read-only and request-scoped, so standard
  `information_schema.tables` and `information_schema.columns` introspection
  works without mutating the shared search-plane SQL session
- expanded the SQL app metadata payload with
  `supports_information_schema = true` so clients can feature-detect the
  standard SQL introspection surface
- added focused coverage in
  `handlers/sql/tests/information_schema.rs` for both
  `information_schema.tables` and `information_schema.columns`
- added `handlers/sql/registration/naming.rs` so local SQL-facing table names
  can stay stable even when the underlying engine table still uses
  epoch-publication naming
- switched the local reference-occurrence SQL query surface from the internal
  engine table name to the stable alias `reference_occurrence`, while keeping
  `engine_table_name` in the catalog as the explicit mapping back to the
  registered Parquet source
- extended the same SQL naming policy to repo-backed corpora, so repo
  publications now register under stable repo-scoped SQL aliases instead of
  publication-id engine names
- added focused repo SQL coverage for querying repo-content parquet through the
  stable alias and for verifying the repo-facing catalog row still preserves
  `engine_table_name`
- added `handlers/sql/registration/views/` so the request-scoped SQL provider
  now builds logical repo-backed views on top of the per-repo aliases instead
  of stopping at repo-specific table discovery
- exposed stable logical SQL view names `repo_content_chunk` and `repo_entity`
  for cross-repo querying, with a synthetic `repo_id` column injected at the
  view layer
- split repo SQL tests into
  `handlers/sql/tests/repo/{mod,content_chunk,entity}.rs` so repo content and
  repo entity query coverage no longer share one flat test file
- added `wendao_sql_view_sources` as a third request-scoped SQL discovery
  catalog so clients can inspect which stable repo aliases feed a logical SQL
  view, in stable `repo_id` order
- enriched `wendao_sql_tables` itself with `sql_object_kind` and `source_count`
  so clients can distinguish base tables from logical views and read view
  fan-in without joining a second catalog first
- enriched `wendao_sql_columns` with `source_column_name`, `sql_object_kind`,
  and `column_origin_kind` so clients can distinguish stored base-table
  columns from projected logical-view columns and synthetic columns such as
  the injected repo logical-view `repo_id`
- added `handlers/sql/registration/views/local.rs` so the SQL provider now
  also builds a stable `local_symbol` logical view on top of the currently
  readable local-symbol tables instead of requiring clients to discover and
  union partition table names themselves
- added focused coverage in `handlers/sql/tests/local_symbol/mod.rs` for
  querying the `local_symbol` logical view plus its table-catalog and
  view-source discoverability
- expanded repo SQL coverage so the lane now covers cross-repo logical-view
  querying, catalog discoverability for the `repo_logical` scope, and focused
  view-source catalog assertions for both `repo_content_chunk` and
  `repo_entity`
- expanded SQL app metadata again so it now reports `registered_view_count`
  alongside the existing table/column/view-source counts
- re-verified the SQL information-schema slice with:
  `direnv exec . cargo fmt -p xiuxian-wendao`
  `direnv exec . cargo nextest run -p xiuxian-wendao --lib --features julia gateway::studio::search::handlers::sql::tests`
  `direnv exec . cargo clippy -p xiuxian-wendao --all-targets --all-features --message-format=short -- -D warnings`
  `direnv exec . git diff --check`

## Wendao SQL Snapshot Coverage and Canonical Doc

- continued the same Studio DataFusion SQL lane from the active execution plan
  `.cache/agent/execplans/wendao-sql-query-catalog.md`
- added `handlers/sql/tests/snapshots.rs` so the SQL lane now keeps
  snapshot-level regression coverage instead of only field-by-field assertions
- normalized SQL snapshot payloads into JSON with decoded app metadata, batch
  schema descriptors, and row-oriented values, so discovery and logical-view
  drift can be audited without depending on Arrow-internal formatting
- added snapshot baselines under
  `packages/rust/crates/xiuxian-wendao/tests/snapshots/gateway/studio/` for:
  `studio_sql_query_surface_payload`
  `studio_sql_discovery_surface_payload`
- added the canonical feature doc
  `packages/rust/crates/xiuxian-wendao/docs/03_features/209_datafusion_sql_query_surface.md`
  so the DataFusion SQL query surface now has one stable documentation target
  covering request-scoped session assembly, stable tables/views, discovery
  catalogs, snapshot policy, and validation commands
- synchronized the package README and `packages/rust/crates/xiuxian-wendao/docs/index.md`
  so both the package surface and canonical docs index point to the new SQL
  doc and snapshot contract
- expanded the canonical SQL doc again so it now includes concrete query
  expressions for discovery, local corpora, repo logical views, query-writing
  rules, and prompt guidance for LLM-authored SQL instead of only describing
  the surface structurally
- verified the landed slice with:
  `direnv exec . cargo fmt -p xiuxian-wendao`
  `direnv exec . env INSTA_UPDATE=always cargo nextest run -p xiuxian-wendao --lib --features julia gateway::studio::search::handlers::sql::tests`
  `direnv exec . cargo clippy -p xiuxian-wendao --all-targets --all-features --message-format=short -- -D warnings`
  `direnv exec . git diff --check`

## Wendao Gateway Query SQL Convergence

- opened a dedicated execution plan for the first gateway-query-to-SQL
  convergence slice at
  `.cache/agent/execplans/wendao-gateway-query-sql-convergence.md`
- re-ran the blueprint check before touching implementation and selected the
  active governing blueprint
  `.cache/agent/blueprints/wendao_arrow_plugin_core_runtime_migration.md`
  because this slice is explicitly about keeping the gateway thin while moving
  query semantics into the Wendao-owned DataFusion lane
- bounded the first implementation slice to repo-content Flight filtering for
  `RepoSearchFlightRequest` instead of trying to fold every gateway route into
  SQL at once
- synchronized the canonical SQL feature doc so it now records which gateway
  query lanes are good SQL convergence targets and which semantic routes are
  intentionally out of scope
- landed the first repo-content convergence slice: `path_prefixes` and
  `filename_filters` now execute in the repo-content SQL/DataFusion scan,
  while `title_filters` and `tag_filters` intentionally remain in the
  gateway-owned semantic projection layer because the current SQL surface does
  not expose first-class `title` or `tags` columns
- validated the slice with focused `cargo nextest` on the repo-content query
  tests, search-plane publication tests, and Flight provider tests, then
  closed the final `cargo clippy -p xiuxian-wendao --all-targets --all-features -- -D warnings`
  gate
- opened the next bounded ExecPlan at
  `.cache/agent/execplans/wendao-gateway-query-semantic-convergence.md`
  to remove the remaining repo-content `title_filters` and `tag_filters` logic
  from the Flight gateway adapter while keeping the public Flight contract
  unchanged
- synchronized the package SQL feature doc so it now records this second
  gateway-thinning slice and its ownership target in
  `search_plane/repo_content_chunk/query/`
- landed the second bounded convergence slice: repo-content `title_filters`
  and `tag_filters` are now owned by the repo-content query lane instead of
  the Flight gateway adapter, while the public Flight request and batch schema
  stayed unchanged
- revalidated the second slice with focused `cargo nextest` on repo-content
  query tests, search-plane publication tests, and Flight provider tests, then
  reran `cargo clippy -p xiuxian-wendao --all-targets --all-features -- -D warnings`
  plus `git diff --check`
- opened a third dedicated execution plan for enriching the repo-content SQL
  surface at `.cache/agent/execplans/wendao-gateway-query-sql-columns.md`
- kept the same governing blueprint
  `.cache/agent/blueprints/wendao_arrow_plugin_core_runtime_migration.md`
  because this third slice is still about pushing repo-content query semantics
  into the Wendao-owned DataFusion lane while keeping the gateway thin
- synchronized the canonical SQL feature doc before implementation so the
  package-level contract recorded the planned logical-column expansion for
  repo-content
- landed the third bounded convergence slice: the stable
  `repo_content_chunk` logical view now exposes derived SQL-facing columns
  `title`, `doc_type`, `code_tag`, `file_tag`, `kind_tag`, and
  `language_tag`, and repo-content `title_filters` now execute in the SQL
  planner instead of as residual post-filtering
- kept `tag_filters` in the repo-content query lane rather than the stable SQL
  catalog because exact-match tags are still query-dependent semantics
- refreshed snapshot-level SQL regression coverage and revalidated the slice
  with:
  `direnv exec . cargo nextest run -p xiuxian-wendao --lib --features julia search_plane::repo_content_chunk::query::tests::`
  `direnv exec . cargo nextest run -p xiuxian-wendao --lib --features julia search_plane::service::tests::publication::search::`
  `direnv exec . cargo nextest run -p xiuxian-wendao --lib --features julia gateway::studio::search::handlers::flight::search_plane::tests::`
  `direnv exec . env INSTA_UPDATE=always cargo nextest run -p xiuxian-wendao --lib --features julia gateway::studio::search::handlers::sql::tests::snapshots::`
  `direnv exec . cargo clippy -p xiuxian-wendao --all-targets --all-features --message-format=short -- -D warnings`
  `direnv exec . git diff --check`
- opened a fourth dedicated execution plan for the repo-content SQL-safe
  tag-filter slice at
  `.cache/agent/execplans/wendao-gateway-query-sql-tag-filters.md`
- kept the same governing blueprint
  `.cache/agent/blueprints/wendao_arrow_plugin_core_runtime_migration.md`
  because this next slice still pushes repo-content query semantics into the
  Wendao-owned SQL planner while preserving a thin gateway
- synchronized the package SQL feature doc before implementation so it now
  records the next bounded split between SQL-safe stable tags and
  query-dependent exact-match tags
- opened a dedicated execution plan for the native-Flight plus shared
  `queries/` architecture slice at
  `.cache/agent/execplans/wendao-search-queries-architecture.md`
- kept the same governing blueprint
  `.cache/agent/blueprints/wendao_arrow_plugin_core_runtime_migration.md`
  because this structural slice is still about keeping the gateway thin and
  making DataFusion query semantics live under the Wendao-owned query lanes
- added a canonical feature doc
  `packages/rust/crates/xiuxian-wendao/docs/03_features/210_search_queries_architecture.md`
  so the target split is now explicit:
  native Flight stays the business protocol surface, while SQL, FlightSQL,
  GraphQL, REST-style query entrypoints, and the future CLI `query` command
  all consume one shared `queries/` system over the same DataFusion execution
  core
- synchronized the package README and docs index before implementation so the
  first physical slice can move the current SQL lane into an explicit
  `queries/sql/` namespace without ambiguity about the target end state
- corrected the first naming attempt before finalizing the slice:
  `query_frontends/` was rejected because it would have implied a
  frontend-only boundary, while the real target is one shared `queries/`
  system that CLI, backend, and future frontend adapters all consume
- landed the first structural slice:
  the SQL lane now lives under `src/gateway/studio/search/queries/sql/`,
  native Flight remains under `src/gateway/studio/search/handlers/flight/`,
  and the package docs now describe the shared `queries/` system explicitly
- validated the slice with:
  `direnv exec . cargo fmt -p xiuxian-wendao`
  `direnv exec . cargo nextest run -p xiuxian-wendao --lib --features julia gateway::studio::search::handlers::flight::tests::`
  `direnv exec . cargo nextest run -p xiuxian-wendao --lib --features julia gateway::studio::search::queries::sql::tests::`
  `direnv exec . cargo clippy -p xiuxian-wendao --all-targets --all-features --message-format=short -- -D warnings`
  `direnv exec . git diff --check`

## Wendao Julia Zhenfa Router Artifact Fixture Ownership

- reran the blueprint check for the next Julia plugin-first slice and then
  corrected the workflow gap by creating the active lane blueprint
  `.cache/agent/blueprints/wendao_julia_plugin_first_rollout.md`
- opened the dedicated execution plan at
  `.cache/agent/execplans/wendao-julia-zhenfa-router-artifact-fixture-ownership.md`
- bounded the slice to the remaining Julia-specific generic plugin-artifact
  fixture proof under `tests/unit/zhenfa_router/native/deployment.rs` and
  `tests/unit/zhenfa_router/rpc.rs`
- synchronized the package roadmap before implementation so the planned
  ownership move is explicit: `xiuxian-wendao-julia::integration_support`
  should own the Julia config fixture, selector/request fixture, and stable
  output fragments for these host `zhenfa_router` tests
- landed the slice:
  `packages/rust/crates/xiuxian-wendao-julia/src/integration_support/gateway_artifact.rs`
  now owns the extra `zhenfa_router` helpers for default strategy, JSON output
  fragments, and JSON-RPC params fixtures, while
  `packages/rust/crates/xiuxian-wendao/tests/unit/zhenfa_router/native/deployment.rs`
  and `packages/rust/crates/xiuxian-wendao/tests/unit/zhenfa_router/rpc.rs`
  consume those plugin-owned helpers instead of repeating Julia artifact
  details inline
- validated the landed slice with:
  `direnv exec . cargo fmt -p xiuxian-wendao-julia -p xiuxian-wendao`
  `direnv exec . env CARGO_TARGET_DIR="$PRJ_CACHE_HOME/cargo-target/wendao-julia-zhenfa-router-artifact-fixture-ownership-plugin" cargo test -p xiuxian-wendao-julia --lib integration_support`
  `direnv exec . env CARGO_TARGET_DIR="$PRJ_CACHE_HOME/cargo-target/wendao-julia-zhenfa-router-artifact-fixture-ownership-native" cargo test -p xiuxian-wendao --lib zhenfa_router::native::deployment`
  `direnv exec . env CARGO_TARGET_DIR="$PRJ_CACHE_HOME/cargo-target/wendao-julia-zhenfa-router-artifact-fixture-ownership-rpc" cargo test -p xiuxian-wendao --lib zhenfa_router::rpc`
  `direnv exec . rg -n "julia_gateway_artifact_|JULIA_PLUGIN_ID|JULIA_DEPLOYMENT_ARTIFACT_ID|DEFAULT_JULIA_RERANK_FLIGHT_ROUTE|DEFAULT_JULIA_ANALYZER_LAUNCHER_PATH" packages/rust/crates/xiuxian-wendao-julia/src/integration_support packages/rust/crates/xiuxian-wendao/tests/unit/zhenfa_router`
  `direnv exec . git diff --check`

## Blueprint Requirement Hardening

- corrected `AGENTS.md` so complex migration lanes now require both an active
  blueprint under `.cache/agent/blueprints/` and an active ExecPlan under
  `.cache/agent/execplans/`
- removed the old fallback wording that allowed ExecPlans to record
  a missing-blueprint fallback for a complex architecture or migration lane
- aligned the blueprint tracking path back to `.cache/agent/blueprints/` and updated
  the Triple-Sync workflow so the blueprint must be created first when the lane
  has no matching file yet
- created the missing active Wendao Julia plugin-first blueprint at
  `.cache/agent/blueprints/wendao_julia_plugin_first_rollout.md`
- retargeted the active Julia plugin-first ExecPlans so they now cite the new
  blueprint path instead of recording the old no-blueprint fallback

## Wendao Julia Planned-Search Config Fixture Ownership

- reran the blueprint check for the next Julia plugin-first slice and selected
  `.cache/agent/blueprints/wendao_julia_plugin_first_rollout.md`
- opened the dedicated execution plan at
  `.cache/agent/execplans/wendao-julia-planned-search-config-fixture-ownership.md`
- bounded the slice to the remaining inline Julia runtime-config TOML fixtures
  in `tests/integration/planned_search_julia_rerank.rs` and
  `tests/integration/planned_search_julia_rerank_vector_store.rs`
- synchronized the package roadmap before implementation so the planned move
  was explicit before code movement
- added plugin-owned planned-search runtime-config helpers in
  `packages/rust/crates/xiuxian-wendao-julia/src/integration_support/planned_search.rs`
  and re-exported them through
  `packages/rust/crates/xiuxian-wendao-julia/src/integration_support/mod.rs`
- rewired
  `packages/rust/crates/xiuxian-wendao/tests/integration/planned_search_julia_rerank.rs`
  and
  `packages/rust/crates/xiuxian-wendao/tests/integration/planned_search_julia_rerank_vector_store.rs`
  to consume the plugin-owned runtime-config helpers instead of inlining Julia
  rerank TOML fixtures
- verified the landed slice with:
  `direnv exec . cargo fmt -p xiuxian-wendao-julia -p xiuxian-wendao`
  `direnv exec . env CARGO_TARGET_DIR="$PRJ_CACHE_HOME/cargo-target/wendao-julia-planned-search-config-fixture-ownership-plugin" cargo test -p xiuxian-wendao-julia --lib integration_support`
  `direnv exec . env CARGO_TARGET_DIR="$PRJ_CACHE_HOME/cargo-target/wendao-julia-planned-search-config-fixture-ownership" cargo test -p xiuxian-wendao --test xiuxian-testing-gate planned_search_julia_rerank::test_planned_search_payload_applies_julia_rerank_scores --features julia -- --exact`
  `direnv exec . env CARGO_TARGET_DIR="$PRJ_CACHE_HOME/cargo-target/wendao-julia-planned-search-config-fixture-ownership" cargo test -p xiuxian-wendao --test xiuxian-testing-gate planned_search_julia_rerank_vector_store::test_planned_search_payload_with_query_vector_applies_julia_rerank_on_vector_store --features julia -- --exact`
  `direnv exec . rg -n "openai-compatible|vector-store|link_graph\\.retrieval\\.julia_rerank|DEFAULT_JULIA_RERANK_FLIGHT_ROUTE" packages/rust/crates/xiuxian-wendao-julia/src/integration_support packages/rust/crates/xiuxian-wendao/tests/integration/planned_search_julia_rerank.rs packages/rust/crates/xiuxian-wendao/tests/integration/planned_search_julia_rerank_vector_store.rs`
  `direnv exec . git diff --check`

## Wendao Julia Planned-Search Official Example Fixture Reuse

- reran the blueprint check for the next Julia plugin-first slice and selected
  `.cache/agent/blueprints/wendao_julia_plugin_first_rollout.md`
- opened the dedicated execution plan at
  `.cache/agent/execplans/wendao-julia-planned-search-official-example-fixture-reuse.md`
- bounded the slice to the repeated generic `vector-store` Julia runtime-config
  fixtures in
  `tests/integration/planned_search_julia_rerank_official_example.rs`,
  `tests/integration/planned_search_julia_rerank_metadata_example.rs`, and
  `tests/integration/planned_search_wendaoanalyzer_linear_blend.rs`
- synchronized the package roadmap before implementation so this reuse slice is
  explicit and does not accidentally absorb the separate
  `similarity_only` analyzer-strategy variant
- rewired the three host tests to consume the plugin-owned generic
  `vector-store` planned-search runtime-config helper, then ran the focused
  validation lane
- the plugin helper lane passed, and the
  `planned_search_wendaoanalyzer_linear_blend` host proof passed, but the other
  two host proofs exposed an unrelated compile blocker in
  `src/gateway/studio/search/queries/flightsql/*`
- after rereading `AGENTS.md`, paused further gateway-local blocker patching
  and treated that compiler failure as a boundary signal: the main SQL /
  `FlightSQL` implementations should not continue living under `gateway`

## Wendao Search Query Implementation Extraction From Gateway

- reread `AGENTS.md` and re-entered the lane under the stricter current rules:
  task-local research first, then docs/ExecPlan sync, then bounded
  implementation
- confirmed from the current `src/` layout and
  `docs/03_features/210_search_queries_architecture.md` that the intended
  shared-query home is `src/search/queries/`, while
  `gateway/studio/search/mod.rs` should remain an adapter-facing surface
- recorded the new architectural direction: SQL and `FlightSQL` query
  implementations should move into a src-owned `search/queries/` namespace,
  while `gateway/studio/search/*` should become a thin dispatch layer only
- opened the dedicated execution plan at
  `.cache/agent/execplans/wendao-search-query-implementation-extraction-from-gateway.md`
- completed the first bounded physical move by relocating the shared SQL
  execution and registration folders into `src/search/queries/sql/`
- kept `gateway/studio/search/queries/sql/` as a thin facade plus Flight
  provider, and redirected gateway SQL tests to consume registration constants
  from the src-owned shared-query surface instead of the gateway wrapper path
- clarified in the tracking record that `search_plane` is still the existing
  data-plane dependency surface, but it is not the destination namespace for
  shared query implementations
- reran the focused verification lane after the warning cleanup:
  `cargo fmt -p xiuxian-wendao`,
  `cargo test -p xiuxian-wendao --lib gateway::studio::search::queries::sql::tests::execution`,
  `cargo test -p xiuxian-wendao --lib gateway::studio::search::queries::flightsql::tests::catalogs`,
  `cargo test -p xiuxian-wendao --test xiuxian-testing-gate planned_search_julia_rerank_official_example::test_planned_search_payload_applies_official_stream_scoring_example --features julia -- --exact`,
  and
  `cargo test -p xiuxian-wendao --test xiuxian-testing-gate planned_search_julia_rerank_metadata_example::test_planned_search_payload_applies_official_stream_metadata_example --features julia -- --exact`
- the first bounded `search/queries/sql/` extraction slice is now landed; the
  remaining gateway extraction work can continue from this thinner seam

## Wendao Search FlightSQL Implementation Extraction From Gateway

- under the new `AGENTS.md` standard, created the active blueprint at
  `.data/blueprints/wendao_search_query_adapter_extraction.md` so this
  multi-slice query-adapter extraction lane has an explicit architectural
  contract
- opened the bounded execution record at
  `.cache/agent/execplans/wendao-search-flightsql-implementation-extraction-from-gateway.md`
- bounded the next slice to the `gateway/studio/search/queries/flightsql/`
  implementation subtree only; native business Flight handlers and unrelated
  query adapters stay out of scope
- the intended outcome for this slice is to move the main `FlightSQL`
  implementation into `src/search/queries/flightsql/` and leave gateway as a
  thin facade
- landed the physical move:
  `discovery/`, `metadata.rs`, `service.rs`, and `statement.rs` now live under
  `src/search/queries/flightsql/`
- reduced `gateway/studio/search/queries/flightsql/mod.rs` to a thin re-export
  seam and aligned `GraphQL` plus `FlightSQL` to consume the src-owned shared
  SQL surface under `search/queries/sql/`
- verification passed with
  `cargo fmt -p xiuxian-wendao`,
  `cargo test -p xiuxian-wendao --lib gateway::studio::search::queries::flightsql::tests::`,
  and
  `cargo check -p xiuxian-wendao --bin wendao_search_flightsql_server --features julia`

## Wendao Search GraphQL Implementation Extraction From Gateway

- the active query-adapter extraction blueprint now governs this next bounded
  slice too:
  `.data/blueprints/wendao_search_query_adapter_extraction.md`
- opened the bounded execution record at
  `.cache/agent/execplans/wendao-search-graphql-implementation-extraction-from-gateway.md`
- bounded the slice to `gateway/studio/search/queries/graphql/` only:
  implementation files plus feature-local tests move into
  `src/search/queries/graphql/`, while gateway becomes a thin facade
- landed the slice by adding the src-owned owner module
  `packages/rust/crates/xiuxian-wendao/src/search/queries/graphql/mod.rs`,
  retargeting the moved execution and test imports to
  `crate::search::queries::graphql::*`, and reducing
  `packages/rust/crates/xiuxian-wendao/src/gateway/studio/search/queries/graphql/mod.rs`
  to a pure re-export surface
- the `GraphQL` adapter now matches the already-landed SQL and `FlightSQL`
  ownership model under `src/search/queries/`
- verified the landed slice with:
  `direnv exec . cargo fmt -p xiuxian-wendao`
  `direnv exec . env CARGO_TARGET_DIR="$PRJ_CACHE_HOME/cargo-target/wendao-search-graphql-implementation-extraction-from-gateway" cargo test -p xiuxian-wendao --lib search::queries::graphql::tests::`
  `direnv exec . env CARGO_TARGET_DIR="$PRJ_CACHE_HOME/cargo-target/wendao-search-graphql-implementation-extraction-from-gateway" cargo check -p xiuxian-wendao --bin wendao --features julia`

## Wendao Search Adapter Search Plane Naming Clarification

- the active query-adapter extraction blueprint also governs this bounded
  naming-clarification slice:
  `.data/blueprints/wendao_search_query_adapter_extraction.md`
- opened the dedicated execution record at
  `.cache/agent/execplans/wendao-search-adapter-search-plane-naming-clarification.md`
- bounded the slice to thin adapter surfaces only:
  `search/queries/flightsql/` and `gateway/studio/search/handlers/flight/`
  may rename builder or module names, but the real `src/search_plane/`
  subsystem stays untouched
- the intended outcome is to remove `search_plane` from adapter names where it
  implies the wrong ownership boundary, so Studio query adapters and
  repo-search Flight facades read as adapters rather than as a destination
  namespace
- landed the rename without compatibility wrappers:
  `search/queries/flightsql/` now exports `build_studio_flightsql_service`,
  `gateway/studio/search/handlers/flight/repo_search.rs` replaces the old
  `search_plane.rs` facade file, and the public gateway builders now read as
  `build_repo_search_flight_service*` and `build_studio_flight_service*`
- updated the touched bins, tests, and re-export surfaces so they all consume
  the new Studio or repo-search names directly
- verified the landed slice with:
  `direnv exec . cargo fmt -p xiuxian-wendao`
  `direnv exec . env CARGO_TARGET_DIR="$PRJ_CACHE_HOME/cargo-target/wendao-search-adapter-search-plane-naming-clarification-flight" cargo test -p xiuxian-wendao --lib gateway::studio::search::handlers::flight::tests::`
  `direnv exec . env CARGO_TARGET_DIR="$PRJ_CACHE_HOME/cargo-target/wendao-search-adapter-search-plane-naming-clarification-flightsql" cargo test -p xiuxian-wendao --lib gateway::studio::search::queries::flightsql::tests::`
  `direnv exec . env CARGO_TARGET_DIR="$PRJ_CACHE_HOME/cargo-target/wendao-search-adapter-search-plane-naming-clarification-check" cargo check -p xiuxian-wendao --bin wendao_search_flight_server --bin wendao_search_flightsql_server --bin wendao --features julia`

## Wendao Repo Search Flight Provider Naming Clarification

- the active query-adapter extraction blueprint also governs this bounded
  provider-naming slice:
  `.data/blueprints/wendao_search_query_adapter_extraction.md`
- opened the dedicated execution record at
  `.cache/agent/execplans/wendao-repo-search-flight-provider-naming-clarification.md`
- bounded the slice to the public concrete provider and its outward-facing
  adapter wording:
  `SearchPlaneRepoSearchFlightRouteProvider` should become a Studio-owned name,
  while the real `src/search_plane/` subsystem remains untouched
- landed the bounded rename:
  `SearchPlaneRepoSearchFlightRouteProvider` is now
  `StudioRepoSearchFlightRouteProvider` across the touched gateway re-export
  seams and repo-search consumer path, without adding compatibility aliases
- updated the outward-facing adapter wording in
  `gateway/studio/search/handlers/flight/repo_search.rs` so the repo-search
  Flight errors no longer describe the adapter as `search-plane` owned
- verified the landed slice with the default Cargo target:
  `direnv exec . cargo fmt -p xiuxian-wendao`
  `direnv exec . cargo test -p xiuxian-wendao --lib gateway::studio::search::handlers::flight::tests::`
  `direnv exec . cargo test -p xiuxian-wendao --lib gateway::studio::search::handlers::code_search::search::repo_search`
  `direnv exec . cargo check -p xiuxian-wendao --bin wendao_search_flight_server --bin wendao_search_seed_sample --features julia`
  `direnv exec . git diff --check`

## Wendao Repo Search Flight Adapter Wording Cleanup

- the active query-adapter extraction blueprint also governs this bounded
  wording-cleanup slice:
  `.data/blueprints/wendao_search_query_adapter_extraction.md`
- opened the dedicated execution record at
  `.cache/agent/execplans/wendao-repo-search-flight-adapter-wording-cleanup.md`
- bounded the slice to adapter-facing wording inside
  `packages/rust/crates/xiuxian-wendao/src/gateway/studio/search/handlers/flight/repo_search.rs`
- landed the cleanup:
  `repo_search.rs` no longer uses adapter-facing `search-plane` wording in the
  Studio provider comments, focused test names, or test-only keyspace labels
- kept the real `SearchPlaneService` dependency, variable names, and runtime
  behavior unchanged
- verified the landed slice with the default Cargo target:
  `direnv exec . cargo fmt -p xiuxian-wendao`
  `direnv exec . cargo test -p xiuxian-wendao --lib gateway::studio::search::handlers::flight::repo_search`
  `direnv exec . cargo check -p xiuxian-wendao --bin wendao_search_flight_server --bin wendao_search_seed_sample --features julia`
  `direnv exec . git diff --check`
- this validation also exposed a separate adjacent warning lane in
  `packages/rust/crates/xiuxian-wendao/src/gateway/studio/search/queries/sql/mod.rs`;
  that warning closure should stay in its own bounded follow-up slice

## Wendao Gateway SQL Facade Warning Closure

- the active query-adapter extraction blueprint also governs this bounded
  warning-closure slice:
  `.data/blueprints/wendao_search_query_adapter_extraction.md`
- opened the dedicated execution record at
  `.cache/agent/execplans/wendao-gateway-sql-facade-warning-closure.md`
- bounded the slice to
  `packages/rust/crates/xiuxian-wendao/src/gateway/studio/search/queries/sql/mod.rs`
  and the adjacent gateway SQL tests that consume its facade re-exports
- landed the warning closure:
  the gateway SQL facade now gates `StudioSqlFlightMetadata` and
  `execute_sql_query` behind `#[cfg(test)]`, so those test-only facade
  re-exports no longer warn in default builds
- kept SQL ownership in `packages/rust/crates/xiuxian-wendao/src/search/queries/sql/`
  and left the thin gateway facade shape intact
- verified the landed slice with the default Cargo target:
  `direnv exec . cargo fmt -p xiuxian-wendao`
  `direnv exec . cargo test -p xiuxian-wendao --lib gateway::studio::search::queries::sql::tests::`
  `direnv exec . cargo check -p xiuxian-wendao --bin wendao_search_flight_server --bin wendao_search_seed_sample --features julia`
  `direnv exec . git diff --check`

## Wendao Shared Query Owner Consumption Cutover

- the active query-adapter extraction blueprint also governs this bounded
  owner-consumption cutover slice:
  `.data/blueprints/wendao_search_query_adapter_extraction.md`
- opened the dedicated execution record at
  `.cache/agent/execplans/wendao-shared-query-owner-consumption-cutover.md`
- bounded the slice to the shared-query visibility seam plus the three local
  consumers that still depend on `gateway::studio` re-exports:
  `src/search/mod.rs`, `src/search/queries/mod.rs`,
  `src/gateway/studio/mod.rs`,
  `src/bin/wendao/execute/query/sql.rs`,
  `src/bin/wendao/execute/query/graphql.rs`, and
  `src/bin/wendao_search_flightsql_server.rs`
- landed the owner cutover:
  the targeted bins now consume `search::queries::sql::query_sql_payload`,
  `search::queries::graphql::query_graphql_payload`, and
  `search::queries::flightsql::build_studio_flightsql_service` directly
- made `search::queries` the public owner namespace for those shared query
  adapters, added the missing owner-surface docs, and let the SQL owner export
  its payload surface directly
- retired the matching `gateway::studio` query re-exports and kept the
  remaining gateway query facade modules test-only where appropriate
- verified the landed slice with the default Cargo target:
  `direnv exec . cargo fmt -p xiuxian-wendao`
  `direnv exec . cargo test -p xiuxian-wendao --bin wendao query::`
  `direnv exec . cargo check -p xiuxian-wendao --bin wendao_search_flightsql_server --features julia`
  `direnv exec . cargo check -p xiuxian-wendao --bin wendao_search_flight_server --bin wendao_search_seed_sample --features julia`
  `direnv exec . git diff --check`

## Wendao Gateway Query Facade Test-Only Reduction

- the active query-adapter extraction blueprint also governs this bounded
  facade-reduction slice:
  `.data/blueprints/wendao_search_query_adapter_extraction.md`
- landed the slice and archived the dedicated execution record at
  `.cache/agent/execplans/archives/wendao-gateway-query-facade-test-only-reduction.md`
- bounded the slice to the last internal Flight service import plus the
  `gateway::studio::search::queries` module gate:
  `src/gateway/studio/search/handlers/flight/service.rs` and
  `src/gateway/studio/search/mod.rs`
- landed the test-only reduction:
  `flight/service.rs` now imports
  `search::queries::sql::provider::StudioSqlFlightRouteProvider` directly, and
  `search/mod.rs` gates `gateway::studio::search::queries` behind `#[cfg(test)]`
- confirmed the remaining `gateway::studio::search::queries::*` references are
  limited to the gateway SQL test tree
- verified the landed slice with the default Cargo target:
  `direnv exec . cargo fmt -p xiuxian-wendao`
  `direnv exec . cargo test -p xiuxian-wendao --lib gateway::studio::search::handlers::flight::tests::`
  `direnv exec . cargo test -p xiuxian-wendao --lib gateway::studio::search::queries::sql::tests::`
  `direnv exec . cargo check -p xiuxian-wendao --bin wendao_search_flight_server --bin wendao_search_seed_sample --features julia`
  `direnv exec . git diff --check`

## Wendao Julia Non-Feature Knowledge Flight Ungating

- the active Julia plugin-first blueprint governs this bounded independence
  slice:
  `.cache/agent/blueprints/wendao_julia_plugin_first_rollout.md`
- landed the slice and archived the dedicated execution record at
  `.cache/agent/execplans/archives/wendao-julia-nonfeature-knowledge-flight-ungating.md`
- bounded the slice to the incorrect host-side Julia feature gate around the
  plugin-agnostic knowledge intent Flight module:
  `src/gateway/studio/search/handlers/knowledge/intent/mod.rs`,
  `src/gateway/studio/search/handlers/knowledge/intent/flight.rs`,
  `src/gateway/studio/search/handlers/knowledge/search.rs`, and
  `src/gateway/studio/search/handlers/flight/provider.rs`
- landed the independence fix:
  `knowledge/intent/mod.rs` now keeps the generic `flight` module available to
  non-Julia host builds, so the touched gateway knowledge path no longer
  creates a false Julia coupling point
- verified the host compile now clears the targeted seam:
  `direnv exec . cargo check -p xiuxian-wendao --no-default-features --features zhenfa-router`
  now finishes successfully instead of failing at `knowledge::intent::flight`
- verified the landed slice with the default Cargo target:
  `direnv exec . cargo fmt -p xiuxian-wendao`
  `direnv exec . cargo test -p xiuxian-wendao --lib gateway::studio::search::handlers::flight::tests::`
  `direnv exec . git diff --check`
- note for follow-up slicing:
  the non-Julia compile probe still emits broad ambient warnings outside the
  touched seam, but the targeted Julia gate blocker is retired

## Wendao Julia Optional Host Dependency Cutover

- the active Julia plugin-first blueprint governs this bounded independence
  slice:
  `.cache/agent/blueprints/wendao_julia_plugin_first_rollout.md`
- opened the dedicated execution record at
  `.cache/agent/execplans/archives/wendao-julia-optional-host-dependency-cutover.md`
- bounded the slice to the Julia Cargo feature seam plus the remaining host
  runtime-config files that still import Julia types unconditionally:
  `packages/rust/crates/xiuxian-wendao/Cargo.toml`,
  `src/link_graph/runtime_config.rs`,
  `src/link_graph/runtime_config/artifacts.rs`,
  `src/link_graph/runtime_config/models/retrieval/policy.rs`, and
  `src/link_graph/runtime_config/resolve/policy/retrieval/base.rs`
- landed the host cutover by making `xiuxian-wendao-julia` optional in
  `packages/rust/crates/xiuxian-wendao/Cargo.toml` and binding it only to the
  host `julia` feature
- kept `xiuxian-wendao-runtime` transport support enabled through the host
  dependency so the generic Flight/DataFusion transport line remains available
  even when the Julia plugin crate is absent
- gated the touched host runtime-config imports and fallback branches so
  non-Julia host builds no longer require Julia-specific types
- also tightened the touched gateway/query facade modules so Julia- or
  test-only SQL/Flight provider helpers no longer leak new unused-import
  warnings into the non-Julia host build
- verified plugin independence directly:
  `direnv exec . cargo tree -p xiuxian-wendao --no-default-features --features zhenfa-router | rg 'xiuxian-wendao-julia'`
  returned no match
- verified the non-Julia host compile probe with:
  `direnv exec . cargo check -p xiuxian-wendao --no-default-features --features zhenfa-router`
  which passed while still surfacing broad ambient host warnings outside this
  slice
- revalidated the Julia-enabled host runtime-config lane with:
  `direnv exec . cargo test -p xiuxian-wendao --lib link_graph::runtime_config`
  which passed with `8 passed`
- rechecked formatting safety with:
  `direnv exec . cargo fmt -p xiuxian-wendao`
  `direnv exec . git diff --check`

## Wendao Runtime Transport Feature Realignment

- the active Julia plugin-first blueprint governs this bounded independence
  slice:
  `.cache/agent/blueprints/wendao_julia_plugin_first_rollout.md`
- landed the slice and archived the dedicated execution record at
  `.cache/agent/execplans/archives/wendao-runtime-transport-feature-realignment.md`
- bounded the slice to the runtime feature seam and its immediate consumers:
  `packages/rust/crates/xiuxian-wendao-runtime/Cargo.toml`,
  `packages/rust/crates/xiuxian-wendao-runtime/src/transport/`,
  `packages/rust/crates/xiuxian-wendao-runtime/src/tests/mod.rs`,
  `packages/rust/crates/xiuxian-wendao/Cargo.toml`,
  `packages/rust/crates/xiuxian-wendao-julia/Cargo.toml`, and
  `packages/rust/crates/xiuxian-wendao-runtime/README.md`
- landed the independence correction:
  the runtime-owned generic transport feature is now named `transport`, so the
  runtime seam no longer carries a Julia-plugin name
- switched the bounded runtime cfg gates, test gates, and runtime README
  verification commands from `feature = "julia"` to `feature = "transport"`
- updated both `xiuxian-wendao` and `xiuxian-wendao-julia` to consume the
  renamed runtime feature while keeping their own plugin feature name `julia`
- accepted one bounded compile-probe deviation in the `wendao` CLI:
  restored the missing `RestQueryArgs` re-export in
  `src/bin/wendao/types/commands/mod.rs` and `src/bin/wendao/types.rs` so the
  non-Julia host compile probe could complete again
- verified the runtime/host realignment with:
  `direnv exec . cargo fmt -p xiuxian-wendao-runtime -p xiuxian-wendao -p xiuxian-wendao-julia`
  `direnv exec . cargo test -p xiuxian-wendao-runtime --features transport`
  which passed with `183 passed`
  `direnv exec . cargo check -p xiuxian-wendao --no-default-features --features zhenfa-router`
  which passed while still surfacing broad ambient host warnings outside this
  slice
  `direnv exec . cargo test -p xiuxian-wendao --lib link_graph::runtime_config`
  which passed with `8 passed`
  `direnv exec . git diff --check`
- re-checked the naming cutover with:
  `rg -n 'features = \\[\"julia\"\\]|--features julia|feature = \"julia\"' packages/rust/crates/xiuxian-wendao-runtime packages/rust/crates/xiuxian-wendao/Cargo.toml packages/rust/crates/xiuxian-wendao-julia/Cargo.toml`
  which now returns only the intentional `xiuxian-ast` language-feature edges

## Wendao Studio Flight Julia Gate Retirement

- the active Julia plugin-first blueprint governs this bounded independence
  slice:
  `.cache/agent/blueprints/wendao_julia_plugin_first_rollout.md`
- the dedicated execution record for this slice is
  `.cache/agent/execplans/archives/wendao-studio-flight-julia-gate-retirement.md`
- kept the slice bounded to the generic Studio Flight builder/provider seam and its
  immediate consumers:
  `src/gateway/studio/mod.rs`,
  `src/gateway/studio/search/handlers/mod.rs`,
  `src/gateway/studio/search/handlers/flight/`,
  `src/search/queries/sql/provider/`,
  `src/bin/wendao_search_flight_server.rs`, and
  `src/bin/wendao/execute/gateway/command.rs`
- landed the independence correction:
  retired false `feature = "julia"` gates from the generic Studio Flight
  builder, SQL Flight provider, and the immediate generic provider re-export
  surfaces consumed by the Flight service, while leaving Julia-owned analyzer
  and rerank semantics untouched
- repointed the internal Flight service builder to owner-level `pub(crate)`
  provider surfaces from `analysis`, `graph`, `repo`, `search`, and `vfs`
  instead of importing private submodules directly
- rechecked the remaining `gateway/studio` and `wendao` search paths with:
  `rg -n 'feature = "julia"|any\\(test, feature = "julia"\\)|all\\(test, feature = "julia"\\)' packages/rust/crates/xiuxian-wendao/src/gateway/studio packages/rust/crates/xiuxian-wendao/src/search packages/rust/crates/xiuxian-wendao/src/bin/wendao packages/rust/crates/xiuxian-wendao/src/bin/wendao_search_flight_server.rs`
  which now leaves only the Flight test cfg and one unrelated dead-code attr
- verified the landed slice with:
  `direnv exec . cargo fmt -p xiuxian-wendao`
  `direnv exec . cargo test -p xiuxian-wendao --lib gateway::studio::search::handlers::flight::tests::`
  which passed with `24 passed`
  `direnv exec . cargo check -p xiuxian-wendao --no-default-features --features zhenfa-router`
  which passed
  `direnv exec . cargo check -p xiuxian-wendao --bin wendao_search_flight_server --no-default-features --features zhenfa-router`
  which passed
  `direnv exec . cargo check -p xiuxian-wendao --bin wendao --no-default-features --features zhenfa-router`
  which passed
  `direnv exec . git diff --check`

## Wendao Studio Flight Test Julia Gate Retirement

- the active Julia plugin-first blueprint governed this slice:
  `.cache/agent/blueprints/wendao_julia_plugin_first_rollout.md`
- the execution record for this landed slice is
  `.cache/agent/execplans/archives/wendao-studio-flight-test-julia-gate-retirement.md`
- retired the two remaining generic Flight test cfg gates from
  `#[cfg(all(test, feature = "julia"))]` to `#[cfg(test)]` in:
  `src/gateway/studio/search/handlers/flight/mod.rs` and
  `src/gateway/studio/search/handlers/flight/repo_search.rs`
- re-verified the focused Flight suites after the surrounding non-Julia
  blockers were cleared:
  `direnv exec . cargo test -p xiuxian-wendao --lib gateway::studio::search::handlers::flight::tests::`
  which passed with `24 passed`
  `direnv exec . cargo test -p xiuxian-wendao --lib gateway::studio::search::handlers::flight::repo_search::tests::`
  which passed with `19 passed`
  `direnv exec . cargo test -p xiuxian-wendao --no-default-features --features zhenfa-router --lib gateway::studio::search::handlers::flight::tests::`
  which passed with `19 passed`
  `direnv exec . cargo test -p xiuxian-wendao --no-default-features --features zhenfa-router --lib gateway::studio::search::handlers::flight::repo_search::tests::`
  which passed with `18 passed`
  `direnv exec . git diff --check`

## Wendao Julia-Dependent Host Lib-Test Gate Alignment

- the active Julia plugin-first blueprint governed this blocker slice:
  `.cache/agent/blueprints/wendao_julia_plugin_first_rollout.md`
- the execution record for this landed slice is
  `.cache/agent/execplans/archives/wendao-julia-dependent-host-libtest-gate-alignment.md`
- aligned the bounded host lib-test blockers with the optional Julia plugin
  seam in:
  `src/link_graph/runtime_config/tests.rs`,
  `src/gateway/openapi/document.rs`,
  `src/gateway/studio/types/config.rs`,
  `src/gateway/studio/router/handlers/capabilities/deployment.rs`,
  `src/gateway/studio/router/tests/config.rs`,
  `src/zhenfa_router/native/deployment.rs`, and
  `tests/unit/zhenfa_router/rpc.rs`
- gated Julia-only imports and proof cases on `feature = "julia"` while
  preserving generic host coverage in the non-Julia lane
- verified the slice by pushing the non-Julia host test probe past the old
  optional-Julia failures and down to the next bounded blocker:
  `direnv exec . cargo check -p xiuxian-wendao --tests --no-default-features --features zhenfa-router`
  which now narrows to Julia-dependent proofs in
  `tests/unit/link_graph_agentic/expansion.rs`
  `direnv exec . git diff --check`

## Wendao Studio Flight Julia-Dependent Analysis Test Partition

- the active Julia plugin-first blueprint governed this blocker slice:
  `.cache/agent/blueprints/wendao_julia_plugin_first_rollout.md`
- the execution record for this landed slice is
  `.cache/agent/execplans/archives/wendao-studio-flight-julia-dependent-analysis-test-partition.md`
- partitioned the true Julia-dependent Flight analysis proofs in:
  `src/gateway/studio/search/handlers/flight/tests/analysis.rs`,
  `src/gateway/studio/search/handlers/flight/tests/headers.rs`, and
  `src/gateway/studio/search/handlers/flight/repo_search.rs`
- kept the generic Flight route coverage visible in the non-Julia
  `zhenfa-router` lane and moved only the Julia-backed analysis proofs behind
  `feature = "julia"`
- verified the focused default and non-Julia Flight probes:
  `direnv exec . cargo test -p xiuxian-wendao --lib gateway::studio::search::handlers::flight::tests::`
  which passed with `24 passed`
  `direnv exec . cargo test -p xiuxian-wendao --lib gateway::studio::search::handlers::flight::repo_search::tests::`
  which passed with `19 passed`
  `direnv exec . cargo test -p xiuxian-wendao --no-default-features --features zhenfa-router --lib gateway::studio::search::handlers::flight::tests::`
  which passed with `19 passed`
  `direnv exec . cargo test -p xiuxian-wendao --no-default-features --features zhenfa-router --lib gateway::studio::search::handlers::flight::repo_search::tests::`
  which passed with `18 passed`
  `direnv exec . git diff --check`

## Wendao Link-Graph Agentic Julia Test Partition

- the active Julia plugin-first blueprint governed this blocker slice:
  `.cache/agent/blueprints/wendao_julia_plugin_first_rollout.md`
- the execution record for this landed slice is
  `.cache/agent/execplans/archives/wendao-link-graph-agentic-julia-test-partition.md`
- bounded the fix to the Julia-dependent proof cases inside:
  `tests/unit/link_graph_agentic/expansion.rs`
- gated the Julia graph-structural imports, helper functions, and two
  proof cases behind `feature = "julia"` while preserving the generic agentic
  plan-budget and candidate-narrowing tests in the non-Julia lane
- verified the landed slice with:
  `direnv exec . cargo fmt -p xiuxian-wendao`
  `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate link_graph_agentic::expansion --features julia`
  which passed with `4 passed`
  `direnv exec . cargo check -p xiuxian-wendao --tests --no-default-features --features zhenfa-router`
  which passed
  `direnv exec . git diff --check`

## Wendao Modelica Docs Integration File Gating

- the active plugin-boundary blueprint governs this warning-closure slice:
  `.cache/agent/blueprints/wendao_arrow_plugin_core_runtime_migration.md`
- the execution record for this landed slice is
  `.cache/agent/execplans/archives/wendao-modelica-docs-integration-file-gating.md`
- bounded the slice to the Modelica-only docs integration family:
  `tests/integration/docs_*.rs`
- added file-level `feature = "modelica"` gating to the bounded `docs_*`
  integration family so these plugin-specific test files disappear cleanly
  from the non-plugin lane instead of compiling as warning-only shells
- verified the landed slice with:
  `direnv exec . cargo fmt -p xiuxian-wendao`
  `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate --no-default-features --features zhenfa-router link_graph_agentic:: -- --nocapture`
  which passed with `7 passed`
  `direnv exec . cargo check -p xiuxian-wendao --test xiuxian-testing-gate --no-default-features --features zhenfa-router`
  and filtered the output with `rg "tests/integration/docs_"`, which returned
  no matches
  `direnv exec . git diff --check`

## Wendao Modelica Repo Integration Warning Closure

- the active plugin-boundary blueprint governed this warning-closure
  slice:
  `.cache/agent/blueprints/wendao_arrow_plugin_core_runtime_migration.md`
- the execution record for this landed slice is
  `.cache/agent/execplans/archives/wendao-modelica-repo-integration-warning-closure.md`
- bounded the cleanup to the mixed repo integration and support files
  that still expose Modelica-only imports in the non-plugin lane:
  `tests/integration/repo_overview.rs`
  `tests/integration/repo_projected_page.rs`
  `tests/integration/repo_projected_page_family_cluster.rs`
  `tests/integration/repo_projected_page_family_context.rs`
  `tests/integration/repo_projected_page_family_search.rs`
  `tests/integration/repo_projected_page_index_node.rs`
  `tests/integration/repo_projected_page_index_tree.rs`
  `tests/integration/repo_projected_page_navigation.rs`
  `tests/integration/repo_projected_page_navigation_search.rs`
  `tests/integration/repo_relations.rs`
  `tests/integration/repo_symbol_search.rs`
  `tests/support/repo_intelligence.rs`
  `tests/support/repo_fixture.rs`
- landed the import/helper gating cut:
  kept generic repo coverage visible while moving only the Modelica-only
  imports and helper surfaces behind `feature = "modelica"` so non-plugin
  warning probes stop compiling dead plugin setup code
- also gated the shared `create_sample_modelica_repo` support helper in both
  `tests/support/repo_intelligence.rs` and `tests/support/repo_fixture.rs`
- verified the landed slice with:
  `direnv exec . cargo fmt -p xiuxian-wendao`
  `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate --no-default-features --features zhenfa-router link_graph_agentic:: -- --nocapture`
  which passed with `7 passed`
  `direnv exec . cargo check -p xiuxian-wendao --test xiuxian-testing-gate --no-default-features --features zhenfa-router`
  and filtered the output with `rg "tests/integration/repo_|tests/support/repo_"`, which returned
  no matches
  `direnv exec . git diff --check`

## Wendao Builtin Plugin Self-Registration

- the active plugin-boundary blueprint governed this registration-ownership
  slice:
  `.cache/agent/blueprints/wendao_arrow_plugin_core_runtime_migration.md`
- the execution record for this landed slice is
  `.cache/agent/execplans/archives/wendao-builtin-plugin-self-registration.md`
- bounded the cleanup to builtin plugin bootstrap ownership only:
  `packages/rust/crates/xiuxian-wendao-core/src/repo_intelligence/`
  `packages/rust/crates/xiuxian-wendao/src/analyzers/service/bootstrap.rs`
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/entry.rs`
  `packages/rust/crates/xiuxian-wendao-modelica/src/plugin/entry.rs`
- landed the registration ownership cut:
  `xiuxian-wendao-core` now owns a generic builtin registrar contract,
  Julia and Modelica submit their registrars from plugin-owned code, and
  `xiuxian-wendao` bootstrap now iterates the generic registrar list instead
  of importing plugin crates directly
- also closed one touched warning revealed by host validation:
  `src/search/repo_search/mod.rs` no longer re-exports test-only helper
  functions that are unused in the non-plugin lane
- verified the landed slice with:
  `direnv exec . cargo fmt -p xiuxian-wendao-core -p xiuxian-wendao -p xiuxian-wendao-julia -p xiuxian-wendao-modelica`
  `direnv exec . cargo test -p xiuxian-wendao-core --lib repo_intelligence`
  which passed with `4 passed`
  `direnv exec . cargo test -p xiuxian-wendao-julia --lib`
  which passed with `94 passed`
  `direnv exec . cargo test -p xiuxian-wendao-modelica --lib`
  which passed with `15 passed`
  `direnv exec . cargo test -p xiuxian-wendao --lib analyzers::service::bootstrap`
  which passed with `1 passed`
  `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate repo_overview::builtin_registry_contains_julia_plugin --features julia,modelica -- --exact`
  which passed with `1 passed`
  `direnv exec . cargo check -p xiuxian-wendao --no-default-features --features zhenfa-router`
  which passed
  `direnv exec . git diff --check`

## Wendao Host Plugin Facade Thinning

- the active plugin-boundary blueprint governs this next bounded slice:
  `.cache/agent/blueprints/wendao_arrow_plugin_core_runtime_migration.md`
- the execution record for this landed slice is
  `.cache/agent/execplans/archives/wendao-host-plugin-facade-thinning.md`
- bounded the cleanup to host-owned plugin facade seams only:
  `packages/rust/crates/xiuxian-wendao/src/analyzers/languages/`
  `packages/rust/crates/xiuxian-wendao/src/analyzers/service/`
  `packages/rust/crates/xiuxian-wendao/src/analyzers/mod.rs`
  `packages/rust/crates/xiuxian-wendao/src/link_graph/index/search/quantum_fusion/`
  `packages/rust/crates/xiuxian-wendao/src/link_graph/index/search/plan/payload/quantum/`
  `packages/rust/crates/xiuxian-wendao/tests/unit/link_graph_agentic/expansion.rs`
- landed the host facade ownership cut:
  `xiuxian-wendao` no longer re-exports Julia or Modelica plugin registration
  APIs from `analyzers::languages`, no longer re-exports Julia-owned transport
  DTO or fetch helpers from `analyzers::service` or `analyzers`, and the
  bounded host and test call sites now import direct plugin-owned helpers from
  `xiuxian-wendao-julia`
- discovered and fixed the real linking dependency hidden behind the old
  facade:
  builtin plugin crates still need one host-private link anchor so their
  `inventory` registrars remain visible to `bootstrap_builtin_registry()`,
  but that anchor no longer widens the host public API
- verified the landed slice with:
  `direnv exec . cargo fmt -p xiuxian-wendao -p xiuxian-wendao-julia`
  `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate link_graph_agentic::expansion --features julia`
  which passed with `4 passed`
  `direnv exec . cargo test -p xiuxian-wendao --lib analyzers::service::bootstrap`
  which passed with `1 passed`
  `direnv exec . cargo check -p xiuxian-wendao --no-default-features --features zhenfa-router`
  which passed but exposed an existing warning front in
  `src/search/repo_search/buffered.rs` and `src/search/repo_search/mod.rs`
  outside this slice's bounded facade files
  `direnv exec . git diff --check`
