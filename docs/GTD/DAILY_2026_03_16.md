---
type: knowledge
metadata:
  title: "GTD: Sovereign Nexus (2026-03-16)"
---

# GTD: Sovereign Nexus (2026-03-16)

## 🌌 Core Features: DeepSeek OCR Metal Stabilization

### 1. [ACTIVE] Feature: DeepSeek Metal MoE Fast Backend

- **Task**: Continue the `metal_fast` MoE backend spike inside vendored `deepseek-ocr` without
  touching `candle-core`.
- **Status**: [ExecPlan Active] Explicit TOML-backed eager `metal_fast` probes now show the
  current overshoot more honestly: baseline at about `14.29 GB`, paired `skip_shared_experts`
  diagnostic at about `13.43 GB`. A behavior-preserving shared-native compute follow-up improved
  the best observed result to about `12.12 GB`, with a repeat at about `12.33 GB`; the native
  residual-add follow-up regressed and was reverted. A later token-major shared-input reuse probe
  also failed to beat the current head, still landing at about `12.15 GB`, and was reverted.
  A separate `lazy_shared_experts` load-spike experiment improved `load-only` to about
  `12.30 GB` from `12.72 GB`, but real `infer` regressed to about `12.58 GB`, so that policy
  was also removed. A later packed-weight-view follow-up on the same accepted head regressed to
  about `12.35 GB` and was also reverted. A more structural per-expert 2D carrier rewrite also
  failed to beat the current head, landing first at about `12.20 GB` and then spiking to about
  `13.14 GB`, so it was reverted as well. Re-stacking `moe_expert_f32_compute = false` on top of
  the current `metal_fast + shared_native` head also failed, still landing at about `12.22 GB`,
  and the transient profile was removed. Guarded PTY-backed trace runs are now reliable on the
  accepted head. The load-attribution landing is now fully validated: script tests are green,
  `deepseek-ocr-infer-deepseek --lib` is green again, and isolated real-Metal `--no-run` is
  green. The new guarded traces cross `weights_mmap.completed`,
  `deferred_moe_source.completed`, and `language.started`. Follow-up tracing pushed the `load`
  wall deeper again: it is no longer at layer start, attention, or MoE gate, but inside MoE
  expert linear materialization, with the latest representative widened `load` probe dying during
  `model.layers.5.mlp.experts.59.up_proj.weight`. The guarded `infer` trace still reaches
  `deepseek.language.transformer_layer.start` for `layer_idx=5` after completing layer `4` before
  the `13 GB` kill; a same-session infer v4 rerun was denied by capfox due to unrelated
  `cpu_overload`, so v3 remains the latest infer-side evidence. Shared experts remain the active
  bounded optimization target, but the current accepted head is still the simpler
  `shared_expert_f32_compute = false` branch.
- **Reference**: [[.cache/codex/execplans/deepseek-moe-fast-backend.md]]
- **Research Log**:
  [[packages/rust/crates/xiuxian-llm/docs/05_research/302_mistralrs_loading_patterns.md]]
- **Roadmap**:
  [[packages/rust/crates/xiuxian-llm/docs/06_roadmap/401_metal_stability.md]]

### 2. [ACTIVE] Feature: Xiuxian LLM Knowledge Base

- **Task**: Keep `xiuxian-llm/docs` aligned with every validated DeepSeek OCR Metal finding.
- **Status**: [In Sync] Research and roadmap docs updated with the latest guarded eager-probe
  result and next-step diagnosis.

## 🏗️ Architecture Sovereignty

- **Kernel Focus**: Model-side optimization first; `candle-core` remains out of scope.
- **Validation Rule**: Every `metal_fast` change must keep `infer-deepseek` tests green and must
  be checked with a guarded real Metal probe.
- **Evidence Path**: `.run/tmp/`
- **Current Head**: grouped-by-expert, load-time-prepacked `metal_fast` without slow-path routing
  staging for the routed path, plus `shared_expert_f32_compute = false`; the remaining gap is now
  about `0.12-0.33 GB`, and token-major shared-input reuse, `lazy_shared_experts`, the
  packed-weight-view experiment, the per-expert 2D carrier rewrite, and re-stacking routed native
  expert compute have all been rejected as non-winning follow-ups. The newest guarded diagnostics
  show that mmap is no longer the active unknown; the accepted-head overshoot is now attributable
  inside `LinearWeights::load` for MoE expert projections, with the current representative
  widened `load` trace dying during `model.layers.5.mlp.experts.59.up_proj.weight`.

## 🛡️ Active Audit: Wendao Clippy Hardening

### 1. [ACTIVE] Audit: `xiuxian-wendao` robustness and warning cleanup

- **Task**: Audit `packages/rust/crates/xiuxian-wendao`, harden brittle code paths, and clear
  crate-local warnings until `cargo clippy -p xiuxian-wendao --all-targets -- -D warnings`
  passes.
- **Status**: [ExecPlan Active] `cargo check -p xiuxian-wendao` is green; completed crate-local
  strict `clippy --no-deps` latest verified baseline is `170`. `sentinel.rs`,
  `code_observation.rs`, `audit_bridge.rs`, `agentic_nav.rs`, `audit/fix.rs`,
  `audit/fuzzy_suggest.rs`, `semantic_check.rs`, `link_graph/agentic/store/suggested.rs`, and
  `link_graph/agentic/store/decision.rs` are now all out of the completed error list. A later
  direct cached-profile lane, used because `direnv exec .` regressed with
  `undefined variable 'gateway_port'`, also cleared `gateway/studio/search.rs`, `storage_py.rs`,
  `zhenfa_router/native/forwarder.rs`, `zhenfa_router/native/audit/ccs.rs`,
  `zhenfa_router/native/remediation.rs`, `storage/crud.rs`, `graph/dedup/operations.rs`, and
  `link_graph/index/scoring/lexical/document.rs`; the latest comparable visible backlog on that
  lane is now `109` after `tests/unit/link_graph/parser/sections.rs` also dropped out. A later
  follow-up run also cleared `gateway/studio/vfs.rs` and its `gateway/studio/router.rs` call
  sites, but that specific run became non-comparable because upstream
  `deepseek-ocr-infer-deepseek` path-dependency failures stopped clippy early. Targeted isolated
  tests have passed for `code_observation` (`35`), `audit_bridge` (`25`), `agentic_nav` (`13`),
  `fix` (`34`), `fuzzy_suggest` (`17`), `semantic_check` (`23`), and `gateway` binary tests
  (`19`). `gateway start` now honors the explicit `--conf` path for port and webhook settings, and
  `cargo run -p xiuxian-wendao -- --conf packages/rust/crates/xiuxian-wendao/wendao.toml gateway
start` now reproduces a passing `curl -f http://localhost:9517/api/health` check. The next
  active hotspot wave is `tests/unit/link_graph/addressing/topology.rs`,
  `link_graph/index/build/cluster_finder.rs`, and `link_graph/index/build/assemble.rs`.
- **Reference**: [[.cache/codex/execplans/wendao-clippy-hardening.md]]
- **Blueprint**: [[.data/blueprints/project_anchor_semantic_addressing.md]]

## 🛠️ Active Ops: Wendao Gateway Readiness Hardening

### 1. [ACTIVE] Process: `wendao-gateway` process-compose readiness stabilization

- **Task**: Remove false `readiness check fail - exit status 7` outcomes from the
  `wendao-gateway` managed process by keeping `wendao.toml` as the dynamic config source and
  widening the readiness window to tolerate cold `cargo run` startup.
- **Status**: [ExecPlan Active] Implementation keeps `wendao.toml` as the runtime config source,
  keeps the probe port dynamic by reading `wendao.toml`, and widens the readiness window so
  `cargo run` cold compiles do not fail before the gateway binds `127.0.0.1:9517`. Static
  validation via `process-compose --dry-run --tui=false` now passes. An isolated managed startup
  also holds `wendao-gateway` in `Running / Not Ready` rather than failing readiness immediately;
  the remaining observed blocker on that lane was a shared cargo artifact lock, not the readiness
  probe itself.
- **Reference**: [[.cache/codex/execplans/wendao-gateway-readiness-hardening.md]]

## 🛡️ Active Audit: Qianji Clippy Hardening

### 1. [ACTIVE] Audit: `xiuxian-qianji` robustness and warning cleanup

- **Task**: Audit `packages/rust/crates/xiuxian-qianji`, harden brittle code paths, and clear
  crate-local warnings until
  `cargo clippy -p xiuxian-qianji --all-targets --all-features -- -D warnings` passes.
- **Status**: [ExecPlan Active] `xiuxian-qianji` now passes
  `cargo clippy -p xiuxian-qianji --all-targets --all-features --no-deps -- -D warnings`, and
  `cargo test -p xiuxian-qianji --all-features --tests` is green. A follow-up dependency wave
  also brought `xiuxian-zhenfa` fully green under `cargo clippy -p xiuxian-zhenfa --lib --tests
-- -D warnings` and `cargo test -p xiuxian-zhenfa --lib --tests`. The latest full strict
  qianji gate in `.run/qianji-full-clippy-pass7.log` is now blocked by `xiuxian-wendao` (`74`
  errors). A targeted `xiuxian-wendao` cleanup pass is underway; the latest crate-scoped strict
  snapshot in `.run/wendao-clippy-pass2.log` still reports `52` lib errors and `81` lib-test
  errors.
- **Reference**: [[.cache/codex/execplans/qianji-clippy-hardening-20260316.md]]
- **Blueprint**: [[.data/blueprints/qianji_autonomous_prototype.md]]
- **Conditional Blueprint**: [[.data/blueprints/project_anchor_semantic_addressing.md]]

## 🛡️ Active Audit: Xiuxian Testing Validation Hardening

### 1. [DONE] Audit: `xiuxian-testing` inline test gate unification

- **Task**: Repair the split validation path in `packages/rust/crates/xiuxian-testing` so inline
  `#[cfg(test)]` blocks participate in the shared external-test validation contract, then harden
  detection logic and test coverage.
- **Status**: [ExecPlan Complete] Unified gate landed: `validate_external_test_mounts` now checks
  both external `#[path]` mounts and inline `#[cfg(test)]` modules, the matcher covers more real
  module shapes, and nested path resolution is fixed. `xiuxian-zhenfa` externalized its `6`
  surfaced inline modules and remains green. `xiuxian-wendao` externalized its `14` surfaced
  inline modules and regained a green shared `xiuxian-testing` gate. The infrastructure
  hardening follow-up is now complete as well: `xiuxian-testing` exposes a reusable
  crate-policy assertion helper, scenario execution fails closed when a runner is missing,
  consumer-specific shared-crate tests are gone, and a lightweight workspace audit crate
  (`xiuxian-test-audit`) now enforces `zhenfa` and `wendao` policy without compiling those
  consumer crates. Targeted verification is green under
  `cargo test -p xiuxian-test-audit --test crate_policy -- --nocapture` and
  `cargo test -p xiuxian-testing --lib --tests`.
- **Reference**:
  [[.cache/codex/execplans/xiuxian-testing-inline-validation-hardening-20260316.md]]

---

_The branch is real; the budget wall is narrow._
