# Daily GTD - 2026-03-31

## Frontend: ZenSearch AST Lane Progressive Loading

- Opened a dedicated milestone ExecPlan:
  - `.cache/codex/execplans/zensearch_ast_lane_progressive_loading.md`
- Verified physical reality before implementation:
  - `useZenSearchPreview(...)` waited on one `loadZenSearchPreviewData(...)`
    batch before clearing `codeAstLoading`
  - `zenSearchPreviewLoaders.ts` grouped VFS, graph, code-AST, markdown, and
    Arrow enrichment into one `Promise.allSettled(...)`
  - the AST waterfall loading copy still said `Compiling AST anatomy view...`
- Landed progressive lane loading for ZenSearch preview:
  - added `loadZenSearchPreviewBaseData(...)`
  - added `loadZenSearchPreviewCodeAstData(...)`
  - added `loadZenSearchPreviewMarkdownData(...)`
  - kept `loadZenSearchPreviewData(...)` as the composed convenience surface
  - rewired `useZenSearchPreview(...)` so base, code-AST, and markdown lanes
    now publish state independently instead of waiting on one full batch
- Updated AST loading copy:
  - `Compiling AST anatomy view...` -> `Loading AST analysis...`
- Added a focused regression test proving AST lane completion can publish while
  the broader preview batch is still waiting on deferred VFS content.
- Validation:
  - `direnv exec . npm --prefix .data/wendao-frontend test -- --run src/components/ZenSearch/__tests__/useZenSearchPreview.test.tsx src/components/ZenSearch/__tests__/CodeAstAnatomyView.test.tsx src/components/SearchBar/__tests__/SearchBar.test.tsx src/App.test.tsx src/api/client.test.ts src/api/apiContracts.test.ts`
    -> passed
  - `direnv exec . git diff --check`
    -> passed

## Frontend: Markdown Bundle Split

- Opened a dedicated milestone ExecPlan:
  - `.cache/codex/execplans/frontend_markdown_bundle_split.md`
- Verified physical reality before implementation:
  - `MainViewContentPanel.tsx` still imported `../DirectReader` statically
  - `mainViewLazyPanels.ts` already exposed a lazy `DirectReader`
  - `scripts/rspack/chunk-policy.mjs` still grouped markdown, mermaid, KaTeX,
    and related heavy dependencies into one broad markdown-oriented policy
- Landed the markdown bundle split:
  - rewired
    `.data/wendao-frontend/src/components/panels/MainView/MainViewContentPanel.tsx`
    to mount `DirectReader` through `mainViewLazyPanels` with `Suspense`
  - split the old monolithic markdown policy into:
    - `markdownCore`
    - `mermaid`
    - `katex`
  - made those groups async-oriented so markdown-heavy runtime no longer
    leaks back into the initial entrypoint
  - updated focused panel and chunk-policy tests
- Validation:
  - `direnv exec . npm --prefix .data/wendao-frontend test -- --run src/build/rspackChunkPolicy.test.ts src/components/panels/MainView/MainViewContentPanel.test.tsx src/components/panels/MainView/MainView.test.tsx src/App.test.tsx src/api/apiContracts.test.ts src/api/clientFacade.test.ts src/api/client.test.ts`
    -> passed
  - `direnv exec . npm --prefix .data/wendao-frontend run build`
    -> passed
  - build audit result:
    - `Initial entry assets: react.js, vendors.js, main.js, vendors.css, main.css`
    - `Entrypoint total: 0.481 MiB / 3.624 MiB`
    - old oversized `markdown.js` is no longer part of the initial entrypoint
  - `direnv exec . git diff --check`
    -> passed

## Frontend: DirectReader Rich-Content Lazy Split

- Opened a dedicated milestone ExecPlan:
  - `.cache/codex/execplans/frontend_directreader_rich_content_lazy_split.md`
- Verified physical reality before implementation:
  - `DirectReader.tsx` still imported the markdown-heavy rich runtime directly
  - `DirectReaderRichContent.tsx` had already been introduced as the intended lazy seam
  - `DirectReader.test.tsx` still assumed synchronous rich rendering
- Landed the DirectReader rich-content lazy split stabilization:
  - kept rich markdown rendering behind `DirectReaderRichContent`
  - updated focused `DirectReader` rich-mode tests to wait for lazy rich hydration
  - preserved existing source-mode semantics and markdown rendering behavior
- Validation:
  - `direnv exec . npm --prefix .data/wendao-frontend test -- --run src/components/panels/DirectReader/DirectReader.test.tsx src/components/panels/DirectReader/MarkdownWaterfall.test.tsx src/components/panels/MainView/MainViewContentPanel.test.tsx src/components/panels/MainView/MainView.test.tsx src/build/rspackChunkPolicy.test.ts src/App.test.tsx`
    -> passed
  - `direnv exec . npm --prefix .data/wendao-frontend run build`
    -> passed
  - build audit result:
    - `Initial entry assets: react.js, vendors.js, main.js, vendors.css, main.css`
    - `Entrypoint total: 0.481 MiB / 3.624 MiB`
    - `DirectReader` rich markdown runtime stays behind the panel-local lazy seam without regressing the initial entrypoint
  - `direnv exec . git diff --check`
    -> pending final run

## PY-F95 Python Wendao Analyzer Real-Host V1

- Opened a dedicated real-host ExecPlan for the first bounded live-host slice:
  - `.cache/codex/execplans/python_wendao_analyzer_real_host_v1.md`
- Verified the physical seam before implementation:
  - `analyze_query(...)` already forwards `extra_metadata` through
    `WendaoTransportClient.read_query_table(...)`
  - the current deterministic baseline analyzer still expects rerank-shaped
    rows, so the first live-host slice should use an explicit repo-search
    analyzer instead of pretending the default baseline is already host-backed
- Landed the first real-host analyzer-package integration test:
  - `packages/python/xiuxian-wendao-analyzer/tests/test_transport_runtime.py`
  - boots `wendao_search_flight_server`
  - seeds one minimal search-plane fixture
  - calls `analyze_query(...)` with:
    - `repo_search_query()`
    - `extra_metadata=repo_search_metadata(...)`
    - an explicit repo-search analyzer
  - proves the analyzer package can consume a live Rust Flight host through
    `xiuxian-wendao-py` without rebuilding transport metadata
- Synced package docs:
  - `packages/python/xiuxian-wendao-analyzer/README.md`
  - now distinguishes:
    - landed explicit-analyzer real-host path
    - still-local default rerank-shaped baseline analyzer

## PY-F94 Python Transport vs Analyzer Package Boundary

- Audited `.data/WendaoArrow` and `.data/WendaoAnalyzer` as the clearest
  existing split between transport substrate and analyzer logic.
- Recorded that `xiuxian-wendao-py` should align with the `WendaoArrow` role:
  Arrow/Flight transport helpers, typed request/response contracts, and shared
  Python substrate for downstream analyzer authors.
- Recorded that a future sibling package such as `xiuxian-wendao-analyzer`
  should own Python-local analyzer strategies and scientific-ecosystem
  integration on top of `xiuxian-wendao-py`.
- Recorded that downstream users building custom analyzers over Arrow tables
  should depend directly on `xiuxian-wendao-py` instead of waiting for an
  official analyzer package.
- Opened a dedicated sibling-package RFC:
  - `docs/rfcs/2026-03-31-python-wendao-analyzer-package-rfc.md`
  - defines `xiuxian-wendao-analyzer` as the planned Python-side analogue of
    `WendaoAnalyzer`
- Extended that RFC with the first concrete package proposal:
  - minimal package layout
  - first public API
  - integration seam on top of `xiuxian-wendao-py`
  - transitional status for existing `analyzer.py` / `plugin.py` /
    `scaffold.py`
- Added the first explicit migration map:
  - substrate symbols that stay in `xiuxian-wendao-py`
  - compatibility-only analyzer helpers that may remain published
  - future analyzer-semantics ownership that should move to
    `xiuxian-wendao-analyzer`
- Added a v0 scaffold plan for the future sibling package:
  - which typed transport symbols it should reuse directly
  - which current compatibility helpers should not migrate yet
  - what the first test shape should prove
- Opened a dedicated implementation ExecPlan for the next slice:
  - `.cache/codex/execplans/python_wendao_analyzer_scaffold_v0.md`
  - bounded to planning the first scaffold, not creating the package yet
- Expanded that ExecPlan into milestone-level implementation checklists:
  - package skeleton
  - minimal analyzer surface
  - transport-backed integration
  - explicit deferred milestones beyond v0
- Landed Milestone 1 of that scaffold plan:
  - `packages/python/xiuxian-wendao-analyzer/pyproject.toml`
  - `packages/python/xiuxian-wendao-analyzer/README.md`
  - `packages/python/xiuxian-wendao-analyzer/src/xiuxian_wendao_analyzer/__init__.py`
  - `packages/python/xiuxian-wendao-analyzer/tests/test_smoke.py`
  - explicit local dependency on `../xiuxian-wendao-py`
- Validation:
  - `direnv exec . uv run pytest tests/test_smoke.py -q`
    in `packages/python/xiuxian-wendao-analyzer` -> `1 passed`
  - `direnv exec . python -m compileall src`
    in `packages/python/xiuxian-wendao-analyzer` -> passed
  - `direnv exec . git diff --check -- packages/python/xiuxian-wendao-analyzer/pyproject.toml packages/python/xiuxian-wendao-analyzer/README.md packages/python/xiuxian-wendao-analyzer/src/xiuxian_wendao_analyzer/__init__.py packages/python/xiuxian-wendao-analyzer/tests/test_smoke.py packages/python/README.md docs/GTD/DAILY_2026_03_31.md .cache/codex/execplans/python_wendao_analyzer_scaffold_v0.md`
    -> passed
- Landed Milestone 2 of that scaffold plan:
  - `config.py`
  - `strategies.py`
  - `runtime.py`
  - updated `__init__.py`
  - replaced the smoke-only test with focused config/runtime tests
- Validation:
  - `direnv exec . uv run pytest tests -q -k "config or strategy or analyze_table or analyze_rows"`
    in `packages/python/xiuxian-wendao-analyzer` -> `4 passed`
  - `direnv exec . python -m compileall src`
    in `packages/python/xiuxian-wendao-analyzer` -> passed
- Landed Milestone 3 of that scaffold plan:
  - `analyze_query(...)` transport-backed integration helper
  - focused transport-backed tests in
    `packages/python/xiuxian-wendao-analyzer/tests/test_transport_runtime.py`
- Validation:
  - `direnv exec . uv run pytest tests -q -k "config or strategy or analyze_table or analyze_rows or analyze_query or transport"`
    in `packages/python/xiuxian-wendao-analyzer` -> `6 passed`
  - `direnv exec . python -m compileall src`
    in `packages/python/xiuxian-wendao-analyzer` -> passed
- `xiuxian-wendao-analyzer` scaffold v0 is now operationally complete:
  - package skeleton
  - minimal analyzer surface
  - transport-backed query integration
  - real-host integration intentionally deferred to the next slice

## WendaoArrow Flight-Only Local Runtime

- Removed the package-local HTTP/IPC transport surface from
  `.data/WendaoArrow` so the local package runtime is now Flight-only.
- Dropped the following exported and implemented surfaces:
  - `build_handler`
  - `build_stream_handler`
  - `serve`
  - `serve_stream`
  - `decode_ipc`
  - `decode_stream`
  - `encode_ipc`
- Removed the local HTTP implementation files and the HTTP-only examples and
  scripts:
  - `src/server.jl`
  - `src/http_responses.jl`
  - `examples/passthrough_server.jl`
  - `examples/stream_passthrough_server.jl`
  - `examples/stream_scoring_server.jl`
  - `examples/stream_metadata_server.jl`
  - matching `run_*server.sh` wrappers
- Simplified `InterfaceConfig` to host/port only and moved the packaged default
  port to `DEFAULT_FLIGHT_PORT = 8815`.
- Removed `HTTP` from `.data/WendaoArrow/Project.toml`.
- Reworked package-local tests to match the Flight-only boundary:
  - removed `test/runtests/http_surface.jl`
  - replaced `test/runtests/ipc_contracts.jl` with
    `test/runtests/contract_helpers.jl`
  - kept local Flight, contract-helper, scoring/metadata, and config coverage
- Rewrote package docs so WendaoArrow is now documented as:
  - Flight-only locally
  - optional `gRPCServer` network binding
  - any non-Flight fallback owned by the host runtime, not by this package

## Verification

- `direnv exec . julia --project=.data/WendaoArrow -e 'using Pkg; Pkg.resolve(); Pkg.instantiate()'` -> passed
- `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/runtests.jl` -> passed
- `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/flight_grpcserver.jl` -> passed

## WendaoArrow Native Julia Flight Harness Simplification

- Reworked the native Julia cross-process Flight harness in
  `.data/WendaoArrow/test/flight_grpcserver/support.jl` to use upstream
  source-based `Arrow.Flight.doexchange(client, source; ...)` plus high-level
  `Arrow.Flight.table(...)` decoding.
- Removed the old manual request-channel `put!` loop and the success-path
  `collect(response)` workaround from the native Julia proof.
- This keeps the WendaoArrow native Julia client proof aligned with the
  upstream Arrow-owned Flight runtime contract instead of proving a custom raw
  protocol path in package-local test code.

## Verification

- `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/flight_grpcserver.jl` -> passed

## Arrow.jl Flight Runtime Contract Priorities

- Opened the next Arrow.jl Flight production workstream after the built-in
  logical-extension contract cleanup.
- Fixed the ranking by production importance:
  1. native `DoExchange` streaming decode without whole-stream collect
  2. one shared native Julia Flight runtime contract for client and server
  3. fewer batch/materialization/copy boundaries
  4. upstream schema/field metadata preservation as a core Arrow.jl ability
- Verified current physical reality before cutting code:
  - `src/flight/client/methods/data.jl` still exposes raw `Channel{FlightData}`
    transport wrappers
  - `src/flight/convert/streaming.jl` still routes `stream(...)` and
    `table(...)` through `_collect_messages(...)` plus full `IOBuffer`
    assembly
  - current `DoExchange` interop in
    `.data/arrow-julia/test/flight/pyarrow_interop/exchange_tests.jl` still
    proves correctness through the materialized `Arrow.Flight.table(...)`
    path
- Opened a dedicated ExecPlan:
  - `.cache/agent/execplans/arrow_julia_flight_runtime_contract.md`
- Immediate next 1-3 steps are now fixed:
  - add a native message-iterator/chunk seam in
    `src/flight/convert/streaming.jl`
  - rewire `DoExchange` tests to hit that streaming seam directly
  - add schema/field metadata preservation assertions on that same seam
- Landed the first production Flight runtime slice:
  - `src/flight/convert/streaming.jl` now exposes a native one-pass
    `FlightStream` that decodes `FlightData` directly into Arrow batches
    without rebuilding a whole IPC byte stream
  - `Arrow.Flight.stream(...)` is now the native decode path
  - `Arrow.Flight.table(...)` still intentionally uses the old materialized
    `streambytes(...)` path for this slice
- Rewired `DoExchange` interop to prove the response channel through native
  `Arrow.Flight.stream(...)` instead of collecting raw `FlightData` first.
- Added explicit metadata-preservation assertions on the payload plus
  external-schema path:
  - schema metadata survives
  - field metadata survives
- Verification:
  - `direnv exec . julia --project=.data/arrow-julia/test -e 'using Pkg; Pkg.instantiate(); using Test, Arrow; include(joinpath(pwd(), ".data/arrow-julia/test/flight.jl"))'`
    -> `Flight IPC conversion helpers | 36/36`,
    `Flight IPC schema separation | 6/6`,
    `Flight pyarrow interop | 19/19`
  - `direnv exec . julia --project=.data/arrow-julia/test -e 'using Pkg; Pkg.instantiate(); Pkg.test("Arrow")'`
    -> `Arrow | 66280/66280 | 3m29.4s`
- Perf artifact:
  - `.cache/agent/tmp/arrow-julia-flight-stream-bench.tsv`
- Perf result:
  - `native_full 0.055 ms / 0.015 MiB`
  - `legacy_full 0.108 ms / 4.024 MiB`
  - `native_payload_schema 0.061 ms / 0.017 MiB`
  - `legacy_payload_schema 0.102 ms / 4.026 MiB`
- Landed the follow-up Flight runtime slice on top of the native stream seam:
  - `Arrow.Flight.table(...)` now routes through the same native `FlightStream`
    contract instead of rebuilding a whole IPC byte stream first
  - multi-batch table aggregation now preserves field metadata instead of
    dropping it when columns become `ChainedVector`
- Expanded the contract coverage:
  - `DoExchange` interop now validates both native batch streaming and native
    table materialization
  - metadata-preservation tests now cover both `stream(payload; schema=...)`
    and `table(payload; schema=...)`
- Verification:
  - `direnv exec . julia --project=.data/arrow-julia/test -e 'using Pkg; Pkg.instantiate(); using Test, Arrow; include(joinpath(pwd(), ".data/arrow-julia/test/flight.jl"))'`
    -> `Flight IPC conversion helpers | 39/39`,
    `Flight pyarrow interop | 21/21`
  - `direnv exec . julia --project=.data/arrow-julia/test -e 'using Pkg; Pkg.instantiate(); Pkg.test("Arrow")'`
    -> `Arrow | 66280/66280 | 3m30.3s`
- Updated perf artifact:
  - `.cache/agent/tmp/arrow-julia-flight-stream-bench.tsv`
- Updated perf result:
  - `native_full 0.053 ms / 0.015 MiB`
  - `legacy_full 0.104 ms / 4.024 MiB`
  - `native_payload_schema 0.057 ms / 0.017 MiB`
  - `legacy_payload_schema 0.095 ms / 4.026 MiB`
  - `native_table_full 0.058 ms / 0.017 MiB`
  - `legacy_table_full 0.123 ms / 4.026 MiB`
  - `native_table_payload_schema 0.056 ms / 0.019 MiB`
  - `legacy_table_payload_schema 0.134 ms / 4.028 MiB`
- Next Flight priorities after the landed slice:
  - factor shared schema/dictionary/record-batch handling into one client/server
    runtime seam
  - add a second focused benchmark for direct response-channel decode once that
    shared runtime exists
- Landed the shared client/server Flight runtime slice:
  - `src/flight/convert/flightdata.jl` now owns a shared emitter seam and
    public `Arrow.Flight.putflightdata!(...)`
  - schema metadata and field metadata now default from the source
    table/batch when re-emitting Flight payloads
  - gRPCServer fixture `DoPut` now decodes requests through
    `Arrow.Flight.stream(request)` instead of collecting raw `FlightData`
  - gRPCServer fixture `DoExchange` now re-emits through the same native
    runtime contract with `Arrow.Flight.putflightdata!(response, Arrow.Flight.stream(request))`
  - `src/table.jl` now lets `TablePartitions` split metadata-wrapped
    `ChainedVector` columns correctly, so native Flight tables can partition
    and re-emit without dropping metadata
- Expanded contract coverage:
  - `Flight IPC conversion helpers` now lock metadata-preserving partition and
    `putflightdata!` roundtrip behavior
  - gRPCServer extension tests now validate native decode/re-emit plus
    metadata retention
- Verification:
  - `direnv exec . julia --project=.data/arrow-julia/test -e 'using Pkg; Pkg.instantiate(); using Test, Arrow; include(joinpath(pwd(), ".data/arrow-julia/test/flight.jl"))'`
    -> `Flight IPC conversion helpers | 46/46`,
    `Flight pyarrow interop | 21/21`
  - `direnv exec . julia --project=.data/arrow-julia/test -e 'using Pkg; Pkg.instantiate(); Pkg.test("Arrow")'`
    -> `Arrow | 66280/66280 | 3m33.1s`
- Updated perf artifact:
  - `.cache/agent/tmp/arrow-julia-flight-stream-bench.tsv`
- Updated perf result:
  - `native_full 0.061 ms / 0.015 MiB`
  - `legacy_full 0.108 ms / 4.024 MiB`
  - `native_payload_schema 0.058 ms / 0.017 MiB`
  - `legacy_payload_schema 0.09 ms / 4.026 MiB`
  - `native_table_full 0.055 ms / 0.017 MiB`
  - `legacy_table_full 0.149 ms / 4.026 MiB`
  - `native_table_payload_schema 0.054 ms / 0.019 MiB`
  - `legacy_table_payload_schema 0.111 ms / 4.028 MiB`
  - `native_reemit_stream 0.17 ms / 3.955 MiB`
  - `legacy_reemit_table 1.875 ms / 9.938 MiB`
- Landed the next Flight runtime slice on the client request side:
  - `src/flight/client/transport.jl` now owns a narrow `FlightAsyncRequest`
    wrapper so producer-task completion and gRPC request completion are
    awaited together
  - `src/flight/client/methods/data.jl` now exposes source-based
    `doput(client, source; ...)` and `doexchange(client, source; ...)`
    overloads that stream request-side `FlightData` through
    `Arrow.Flight.putflightdata!(...)`
  - pyarrow upload/exchange interop no longer hand-rolls
    `flightdata(...) + channel push` for those request paths
- Verification:
  - `direnv exec . julia --project=.data/arrow-julia/test -e 'using Pkg; Pkg.instantiate(); using Test, Arrow; include(joinpath(pwd(), ".data/arrow-julia/test/flight.jl"))'`
    -> `Flight IPC conversion helpers | 46/46`,
    `Flight pyarrow interop | 21/21`
  - `direnv exec . julia --project=.data/arrow-julia/test -e 'using Pkg; Pkg.instantiate(); Pkg.test("Arrow")'`
    -> `Arrow | 66280/66280 | 3m40.9s`
- Updated perf artifact:
  - `.cache/agent/tmp/arrow-julia-flight-stream-bench.tsv`
- Updated perf result:
  - `native_request_emit 1.621 ms / 3.808 MiB`
  - `legacy_request_emit 1.568 ms / 3.809 MiB`
  - `native_full 0.049 ms / 0.015 MiB`
  - `legacy_full 0.081 ms / 4.024 MiB`
  - `native_table_full 0.061 ms / 0.017 MiB`
  - `legacy_table_full 0.143 ms / 4.026 MiB`
  - `native_reemit_stream 0.222 ms / 3.955 MiB`
  - `legacy_reemit_table 2.057 ms / 9.938 MiB`
- Interpretation:
  - the new request-side source overloads are primarily a runtime-contract and
    streaming-surface improvement
  - they do not yet outperform the old hand-written request emit loop on the
    synthetic local channel benchmark
  - the clear production wins remain native response decode, native table
    materialization, and native re-emit with metadata retention
- Landed the next metadata-preservation slice on top of the Flight runtime
  work:
  - `src/table.jl` now owns a generic `MetadataVector`
  - `TablePartitions` understands metadata-preserving chained columns through
    that Arrow-owned wrapper
  - `src/flight/convert/streaming.jl` no longer needs a local
    `FlightMetadataVector`; native Flight table aggregation now reuses the
    upstream Arrow metadata wrapper
- Expanded contract coverage:
  - `Flight IPC conversion helpers` now also assert that partitioned native
    Flight tables preserve both table metadata and field metadata on every
    partition
- Verification:
  - `direnv exec . julia --project=.data/arrow-julia/test -e 'using Pkg; Pkg.instantiate(); using Test, Arrow; include(joinpath(pwd(), ".data/arrow-julia/test/flight.jl"))'`
    -> `Flight IPC conversion helpers | 49/49`,
    `Flight pyarrow interop | 29/29`
  - `direnv exec . julia --project=.data/arrow-julia/test -e 'using Pkg; Pkg.instantiate(); Pkg.test("Arrow")'`
    -> `Arrow | 66285/66285 | 3m42.3s`
- Updated perf artifact:
  - `.cache/agent/tmp/arrow-julia-flight-stream-bench.tsv`
- Updated perf result:
  - `native_request_emit 1.63 ms / 3.808 MiB`
  - `legacy_request_collect 1.636 ms / 3.807 MiB`
  - `legacy_request_emit 1.585 ms / 3.809 MiB`
  - `native_full 0.062 ms / 0.015 MiB`
  - `legacy_full 0.096 ms / 4.024 MiB`
  - `native_table_full 0.046 ms / 0.017 MiB`
  - `legacy_table_full 0.13 ms / 4.026 MiB`
  - `native_reemit_stream 0.223 ms / 3.955 MiB`
  - `legacy_reemit_table 1.982 ms / 9.938 MiB`
- Interpretation:
  - metadata preservation for partitioned native Flight tables is now an
    upstream Arrow capability
  - this removes a Flight-only wrapper seam without losing the performance
    gains from native decode/table/re-emit

## Arrow.jl Logical Extension Runtime Contract

- Continued the `arrow-julia` logical/extension contract centralization line
  without committing the current nested-repo worktree.
- Extended the Arrow.jl-owned runtime seam in
  `src/logicaltypes.jl` with built-in resolver hooks:
  - `_builtinextensionspec`
  - `_builtinextensionjuliatype`
- Registered Arrow.jl-owned built-in logical types on that runtime path so
  read-side compatibility and passthrough mapping no longer depend only on
  scattered `ArrowTypes.JuliaType(...)` definitions.
- Added focused runtime-contract regressions in
  `arrow-julia/test/runtests.jl` for:
  - canonical and legacy UUID resolution
  - built-in `Bool8`
  - canonical `arrow.timestamp_with_offset`
  - `arrow.opaque` passthrough
  - legacy `JuliaLang.ZonedDateTime`
- Verification:
  - `direnv exec . julia --project=.data/arrow-julia/src/ArrowTypes -e 'using Pkg; Pkg.test()'`
    -> `ArrowTypes | 190/190`
  - `direnv exec . julia --project=.data/arrow-julia/test -e 'using Pkg; Pkg.instantiate(); Pkg.test("Arrow")'`
    -> `Arrow | 66222/66222 | 3m46.2s`
- Perf artifacts captured:
  - `.cache/agent/tmp/arrow-julia-logical-builtin-writepaths.tsv`
  - `.cache/agent/tmp/arrow-julia-logical-builtin-writepaths-rerun.tsv`
  - `.cache/agent/tmp/arrow-julia-logical-builtin-throughput.tsv`
  - `.cache/agent/tmp/arrow-julia-logical-builtin-throughput-rerun.tsv`
  - `.cache/agent/tmp/arrow-julia-extension-contract-bench.tsv`
- Perf conclusion:
  - broad write/throughput harnesses were noisy across reruns
  - the changed surface is read-side logical contract resolution, not the main
    write hot path
  - the new extension-heavy benchmark showed the affected cases remain strong:
    - `uuid_canonical write 0.511 ms / read_copy 34.619 ms`
    - `bool8 write 0.064 ms / read_copy 0.010 ms`
    - `json_text write 1.520 ms / read_copy 0.651 ms`
    - `timestamp_with_offset write 0.140 ms / read_copy 2.291 ms`
- Continued the same slice by moving Arrow-owned built-in runtime registry
  methods out of `src/eltypes.jl` and into the new host file:
  - `.data/arrow-julia/src/logicaltypes_builtin.jl`
- The contract boundary is now:
  - `src/eltypes.jl` for storage/validation/trait declarations
  - `src/logicaltypes*.jl` for runtime extension resolution and alias mapping
- Re-ran formatter and package validation after the host-file move:
  - `julia -e 'using JuliaFormatter; format(".", verbose=true)'` in
    `.data/arrow-julia`
  - `direnv exec . julia --project=.data/arrow-julia/test -e 'using Pkg; Pkg.instantiate(); Pkg.test("Arrow")'`
    -> `Arrow | 66222/66222 | 3m38.1s`
- Extension-heavy rerun artifact:
  - `.cache/agent/tmp/arrow-julia-extension-contract-bench-hostfile.tsv`
- Rerun stayed in the same band:
  - `uuid_canonical write 0.585 ms / read_copy 33.184 ms`
  - `bool8 write 0.071 ms / read_copy 0.010 ms`
  - `json_text write 1.690 ms / read_copy 0.614 ms`
  - `timestamp_with_offset write 0.153 ms / read_copy 2.240 ms`

## xiuxian-daochang Discord and Telegram Runtime Bundle

- Closed the next bounded `xiuxian-daochang` crate-health slice in the live
  Discord/Telegram runtime path.
- Restored the live Discord channel constructor owner
  `new_with_partition_and_control_command_policy(...)`.
- Repaired Discord gateway/run loop ownership so the current join-handle and
  admission-snapshot telemetry shape are used consistently.
- Restored Telegram send-rate gate ownership, chunk-send helpers, and the
  `SessionGate` shared acquire/drop seam.
- Rebased the touched runtime/router files from `super::super::...` to
  explicit crate-owned imports.
- The `xiuxian-daochang --lib` compile front no longer stops in Discord
  runtime gateway/run ownership or Telegram send/runtime drift. It now starts
  deeper in:
  - `gateway/http/*`
  - `agent/injection/*`
  - `agent/native_tools/zhixing.rs`
  - `agent/zhenfa/bridge.rs`
  - Telegram ACL/settings
  - Telegram session-memory reply shaping
  - remaining test-support seams

## Verification

- `direnv exec . rustfmt <touched xiuxian-daochang Discord/Telegram runtime bundle>` -> passed
- `direnv exec . rg -n "super::super::|use super::\\*|wildcard_imports" <touched runtime bundle>` -> clean in the touched scope
- `direnv exec . cargo check -p xiuxian-daochang --lib --message-format short` -> bundle cleared; compile front moved into deeper `gateway/http/*`, `agent/injection/*`, ACL, reply-shaping, and test-support seams
- `direnv exec . git diff --check` -> passed

## Wendao Plugin RFC Scope Correction

- Corrected the active program position back to the Wendao plugin RFC.
- Recorded that recent `xiuxian-daochang` crate-health work is adjunct
  workspace remediation, not authoritative Wendao-plugin phase progress
  unless it directly unblocks a Wendao-owned gate.
- Opened `Phase 9: Core and Runtime Consumer Cutover`.
- Fixed the next governed move to `Phase-9 Stage A: Consumer Reality
Inventory Bundle`.

## Wendao Plugin RFC Phase-9 Stage-A Inventory

- Completed the authoritative `Phase-9 Stage A` consumer-reality inventory.
- Verified that live monolith-era `xiuxian-wendao` direct dependencies still
  exist in:
  - `xiuxian-qianji`
  - `xiuxian-zhixing`
  - `xiuxian-daochang`
  - `xiuxian-qianhuan` as an optional dependency
- Verified that `xiuxian-wendao-modelica` already uses
  `xiuxian-wendao-core` for production code, but still retains a monolith
  dev-dependency for integration tests.
- Verified that no surveyed sibling consumer crate currently imports
  `xiuxian_wendao_core::...` or `xiuxian_wendao_runtime::...` directly in
  Rust source.
- Identified the first bounded `Phase-9 Stage B` cutover candidate as the
  resource/VFS family:
  - `SkillVfsResolver`
  - `WendaoResourceUri`
  - `embedded_resource_text_from_wendao_uri`
  - `WendaoResourceRegistry`
- Recorded that this family is the best first cut because it is physically
  concentrated in Wendao-owned code and still re-exported from the monolith
  crate root.

## Wendao Plugin RFC Phase-9 Stage-B Resource/VFS Source Cutover

- Landed the first bounded `Phase-9 Stage B` consumer cutover slice.
- Rebased source consumers in:
  - `xiuxian-qianhuan`
  - `xiuxian-qianji`
  - `xiuxian-daochang`
    away from `xiuxian_wendao` crate-root imports for the resource/VFS family.
- The touched consumers now import from the owner seams instead:
  - `xiuxian_wendao::skill_vfs::*`
  - `xiuxian_wendao::enhancer::WendaoResourceRegistry`
- Verified that source-level root-qualified imports for
  `SkillVfsResolver`, `WendaoResourceUri`,
  `embedded_resource_text_from_wendao_uri`, and
  `WendaoResourceRegistry` are now gone across the touched source scope.
- Kept `Stage B` open because residual root-qualified imports for this family
  still remain in tests.

## Wendao Plugin RFC Phase-9 Stage-B Resource/VFS Test Cutover

- Landed the follow-up test-consumer slice for the same resource/VFS family.
- Rebased the remaining touched test imports in:
  - `xiuxian-daochang/tests/scenario_adversarial_evolution.rs`
  - `xiuxian-qianji/tests/integration/test_qianji_qianhuan_binding.rs`
  - `xiuxian-qianji/tests/integration/test_agenda_validation_pipeline.rs`
    from `xiuxian_wendao` crate-root imports onto
    `xiuxian_wendao::skill_vfs::embedded_resource_text_from_wendao_uri`.
- Verified that root-qualified imports for
  `SkillVfsResolver`, `WendaoResourceUri`,
  `embedded_resource_text_from_wendao_uri`, and
  `WendaoResourceRegistry` are now cleared across the touched
  `xiuxian-qianhuan` / `xiuxian-qianji` / `xiuxian-daochang`
  `src/` and `tests/` scope.

## Wendao Plugin RFC Phase-9 Stage-B Resource/VFS Internal Test Cutover

- Rebased the last remaining repo-internal root-qualified import for this
  family in `xiuxian-wendao/tests/unit/skill_vfs_resolver.rs` onto
  `xiuxian_wendao::skill_vfs::{SkillVfsError, SkillVfsResolver}`.
- Verified that root-qualified imports for
  `SkillVfsResolver`, `WendaoResourceUri`,
  `embedded_resource_text_from_wendao_uri`, and
  `WendaoResourceRegistry` are now cleared across the workspace `packages/**`
  Rust source and test scope.
- Recorded the corrected boundary:
  - resource/VFS consumer cutover remains `Phase-9 Stage B`
  - crate-root re-export contraction for this family belongs to `Phase-9 Stage C`
  - the next `Stage B` move should therefore be the next bounded consumer
    family, not premature re-export removal

## Wendao Plugin RFC Phase-9 Stage-B Ingress Spider Family Cutover

- Landed the next bounded `Phase-9 Stage B` consumer family after the
  resource/VFS slice: the ingress spider family.
- Rebased the touched `xiuxian-daochang` source and test consumers from
  root-qualified `xiuxian_wendao` imports onto the owner seam:
  - `xiuxian_wendao::ingress::SpiderWendaoBridge`
  - `xiuxian_wendao::ingress::SpiderPagePayload`
  - `xiuxian_wendao::ingress::canonical_web_uri`
- Verified that root-qualified imports for
  `SpiderWendaoBridge`, `SpiderPagePayload`, and `canonical_web_uri`
  are now cleared across the workspace `packages/**` Rust source and test
  scope.
- Recorded the bounded verification result:
  `xiuxian-daochang --test agent_suite --no-run` still fails, but the
  compile front is now deeper pre-existing `xiuxian-daochang` work rather
  than the touched ingress spider files.
- Kept `Stage B` open: the next move remains another bounded consumer family,
  not crate-root re-export contraction.

## Wendao Plugin RFC Phase-9 Stage-B Incremental Sync Policy Cutover

- Landed the next bounded `Phase-9 Stage B` consumer family after the ingress
  spider slice: `IncrementalSyncPolicy`.
- Rebased the touched consumers in:
  - `xiuxian-daochang/src/agent/bootstrap/hot_reload/prepared.rs`
  - `xiuxian-zhixing/src/heyi/types.rs`
  - `xiuxian-zhixing/tests/test_wendao_indexer.rs`
  - `xiuxian-wendao/tests/unit/sync/incremental_policy.rs`
    from root-qualified `xiuxian_wendao::IncrementalSyncPolicy` imports onto
    the owner seam `xiuxian_wendao::sync::IncrementalSyncPolicy`.
- In the same touched `xiuxian-daochang` file, replaced the local
  `super::super::...` imports with explicit `crate::agent::bootstrap::*`
  owner imports.
- Verified that root-qualified imports for `IncrementalSyncPolicy` are now
  cleared across the workspace `packages/**` Rust source and test scope.
- Verified the bounded consumer seam:
  - `direnv exec . cargo check -p xiuxian-zhixing --lib` passes
  - `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate --no-run` passes
  - `direnv exec . cargo check -p xiuxian-daochang --lib --message-format short`
    still fails, but only in deeper pre-existing `gateway/http`, `agent/injection`,
    Telegram reply-shaping, and test-support drift outside this family
  - `direnv exec . cargo test -p xiuxian-zhixing --test test_wendao_indexer --no-run`
    still fails on pre-existing `ZhixingIndexSummary` field drift in that test file,
    not on the `IncrementalSyncPolicy` import cutover
- Kept `Stage B` open: the next move remains another bounded consumer family,
  not crate-root re-export contraction.

## Wendao Plugin RFC Phase-9 Stage-B Zhixing Indexer Family Cutover

- Landed the next bounded `Phase-9 Stage B` consumer family after
  `IncrementalSyncPolicy`: `ZhixingWendaoIndexer / ZhixingIndexSummary`.
- Rebased the touched `xiuxian-zhixing` source and test consumers from
  root-qualified monolith imports onto the owner seam:
  - `xiuxian_wendao::skill_vfs::zhixing::ZhixingWendaoIndexer`
  - `xiuxian_wendao::skill_vfs::zhixing::ZhixingIndexSummary`
- Repaired the paired owner drift in
  `xiuxian-wendao/src/skill_vfs/zhixing/indexer/types.rs` by restoring the
  embedded skill-reference counters on `ZhixingIndexSummary` and wiring
  them into `index_all_domain_objects()`, so downstream tests no longer
  compile against fields the live indexer summary omits.
- Verified that root-qualified imports for
  `ZhixingWendaoIndexer` and `ZhixingIndexSummary` are now cleared across the
  workspace `packages/**` Rust source and test scope.
- Verified the bounded seam:
  - `direnv exec . cargo check -p xiuxian-zhixing --lib` passes
  - `direnv exec . cargo test -p xiuxian-zhixing --test test_wendao_indexer --no-run` passes
  - `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate --no-run` passes
  - `direnv exec . cargo test -p xiuxian-zhixing --tests --no-run` still fails,
    but only in deeper pre-existing `test_heyi` drift outside this family
  - `direnv exec . cargo check -p xiuxian-daochang --lib --message-format short`
    still fails, but only in deeper pre-existing compile front drift outside this family
- Kept `Stage B` open: the next move remains another bounded consumer family,
  not crate-root re-export contraction.

## Wendao Plugin RFC Phase-9 Stage-B Contract-Feedback Family Cutover

- Landed the next bounded `Phase-9 Stage B` consumer family after the
  Zhixing indexer slice: the `contract_feedback / knowledge_entry` family in
  `xiuxian-qianji`.
- Rebased the touched `xiuxian-qianji` source and test consumers from
  root-qualified monolith imports onto the owner seams:
  - `xiuxian_wendao::contract_feedback::WendaoContractFeedbackAdapter`
  - `xiuxian_wendao::storage::KnowledgeStorage`
  - `xiuxian_wendao::types::{KnowledgeCategory, KnowledgeEntry}`
- The cutover exposed one bounded host visibility seam:
  `xiuxian-wendao/src/lib.rs` now exports `pub mod types;` so the
  `xiuxian_wendao::types::*` owner path is physically reachable instead of
  only crate-root re-exported.
- Verified that root-qualified imports for
  `KnowledgeEntry`, `KnowledgeStorage`,
  `WendaoContractFeedbackAdapter`, and `KnowledgeCategory` are now cleared
  across the touched `xiuxian-qianji` `src/` and `tests/` scope.
- Verified the bounded seam:
  - `direnv exec . cargo check -p xiuxian-qianji --lib` passes
  - `direnv exec . cargo test -p xiuxian-qianji --tests --no-run` passes
  - `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate --no-run` passes
- Kept `Stage B` open: the next move remains another bounded consumer family,
  not crate-root re-export contraction.

## Wendao Plugin RFC Phase-9 Stage-B Graph Primitive Family Cutover

- Landed the next bounded `Phase-9 Stage B` consumer family after the
  `contract_feedback / knowledge_entry` slice: the graph primitive family in
  the touched `xiuxian-qianji` / `xiuxian-zhixing` scope.
- Rebased the touched consumers from root-qualified monolith imports onto the
  owner seams:
  - `xiuxian_wendao::entity::{Entity, EntityType, Relation, RelationType}`
  - `xiuxian_wendao::graph::KnowledgeGraph`
- The touched files are:
  - `xiuxian-qianji/src/executors/wendao_ingester/entity.rs`
  - `xiuxian-qianji/src/executors/wendao_ingester/persistence.rs`
  - `xiuxian-zhixing/src/heyi/tasks.rs`
  - `xiuxian-zhixing/src/heyi/reminders.rs`
  - `xiuxian-zhixing/tests/test_strict_teacher.rs`
  - `xiuxian-zhixing/tests/test_wendao_indexer.rs`
- The touched `test_strict_teacher.rs` seam also needed one bounded local
  drift repair:
  - replaced the dead `MockManifestation` import with a local
    `ManifestationInterface` stub
  - aligned the task-call site to the current
    `ZhixingHeyi::add_task(title, scheduled_at)` signature
- Verified that root-qualified imports for
  `Entity`, `EntityType`, `Relation`, `RelationType`, and root-braced
  `KnowledgeGraph` are now cleared across the touched scope; explicit
  `xiuxian_wendao::graph::KnowledgeGraph` owner imports remain by design.
- Verified the bounded seam:
  - `direnv exec . cargo check -p xiuxian-qianji --lib` passes
  - `direnv exec . cargo check -p xiuxian-zhixing --lib` passes
  - `direnv exec . cargo test -p xiuxian-zhixing --test test_strict_teacher --no-run` passes
  - `direnv exec . cargo test -p xiuxian-zhixing --test test_wendao_indexer --no-run` passes
- Kept `Stage B` open: the next move remains another bounded consumer family,
  not crate-root re-export contraction.

## Wendao Plugin RFC Phase-9 Stage-B Graph Primitive Residual Tail

- Continued the same graph primitive family without opening a new family.
- Rebased the residual `xiuxian-zhixing/tests/test_heyi.rs` tail from
  root-qualified `Entity` / `EntityType` imports onto
  `xiuxian_wendao::entity::{Entity, EntityType}` while keeping
  `xiuxian_wendao::graph::KnowledgeGraph` as the explicit owner import.
- In the same touched file, aligned the compile surface to the current live
  `ZhixingHeyi` APIs:
  - removed the stale third `add_task(...)` argument
  - replaced the dead `render_task_add_response_from_id(...)` test path with
    the current `render_reminder_notice_markdown(...)` surface
- Verified the residual tail compile seam:
  - `direnv exec . cargo test -p xiuxian-zhixing --test test_heyi --no-run` passes
- Also ran the full test to check behavioral reality:
  - `direnv exec . cargo test -p xiuxian-zhixing --test test_heyi` still fails
    on deeper pre-existing reminder/agenda/task behavior drift in that test
    surface, not on the owner-path cutover itself
- Kept `Stage B` open: the next move remains another bounded consumer family,
  not crate-root re-export contraction.

## Wendao Plugin RFC Phase-9 Stage-B Markdown Config Family Cutover

- Landed the next bounded `Phase-9 Stage B` consumer family after the graph
  primitive slice: `MarkdownConfigBlock / extract_markdown_config_blocks`.
- Added the minimum owner visibility seam in
  `xiuxian-wendao/src/enhancer/mod.rs`:
  - `pub mod markdown_config;`
- Rebased the touched consumer in
  `xiuxian-daochang/tests/agent/native_tools_zhixing.rs` from root-qualified
  monolith imports onto:
  - `xiuxian_wendao::enhancer::markdown_config::{MarkdownConfigBlock, extract_markdown_config_blocks}`
- Verified that root-qualified imports for
  `MarkdownConfigBlock` and `extract_markdown_config_blocks` are now cleared
  across the workspace `packages/**` Rust source and test scope.
- Verified the Wendao-owned seam:
  - `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate --no-run` passes
- Verified affected-package reality:
  - `direnv exec . cargo test -p xiuxian-daochang --test agent_suite --no-run --message-format short`
    still fails, but the compile front remains in deeper pre-existing
    `gateway/http`, `agent/injection`, Telegram ACL, and session-memory
    drift outside this markdown-config cutover
- Kept `Stage B` open: the next move remains another bounded consumer family,
  not crate-root re-export contraction.

## PY-F71 Real-Host `wendao.toml` Rerank Weight Smoke

- Rewired `wendao_search_flight_server` so it binds a local or project-root
  `wendao.toml` into the existing runtime-config override path before
  resolving rerank score weights.
- Extended the Python real-host helper with an explicit `cwd` override so the
  integration smoke can launch the binary inside a temporary workspace that
  owns its own `wendao.toml`.
- Added a real-host Python smoke proving
  `link_graph.retrieval.julia_rerank.vector_weight` and
  `link_graph.retrieval.julia_rerank.similarity_weight`
  change rerank ordering and `final_score` through the live
  `wendao_search_flight_server` path, not only through resolver tests.
- Added an explicit conflict contract:
  - `wendao.toml > env`
  - the Python real-host conflict smoke now asserts that file-backed rerank
    weights override conflicting env values on the live host path.
- Synced the successor RFC so this precedence is now a governed contract rule,
  not just an implementation detail or test-local assumption.

## Verification

- `direnv exec . sh -lc 'export CARGO_TARGET_DIR="$PRJ_ROOT/.cache/pyflight-f56-target"; cargo build -p xiuxian-wendao --features julia --bin wendao_search_flight_server'` -> passed
- `direnv exec . env WENDAO_SEARCH_SERVER_BINARY="/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-f56-target/debug/wendao_search_flight_server" WENDAO_SEARCH_SEED_BINARY="/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-f56-target/debug/wendao_search_seed_sample" uv run pytest packages/python/xiuxian-wendao-py/tests/test_transport.py -q -k "wendao_toml_rerank_weight_policy_via_wendao_search_flight_server"` -> `1 passed`
- `direnv exec . env WENDAO_SEARCH_SERVER_BINARY="/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-f56-target/debug/wendao_search_flight_server" WENDAO_SEARCH_SEED_BINARY="/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-f56-target/debug/wendao_search_seed_sample" uv run pytest packages/python/xiuxian-wendao-py/tests/test_transport.py -q -k "prefers_wendao_toml_over_env_for_rerank_weights_via_wendao_search_flight_server"` -> `1 passed`

## PY-F72 Real-Host `wendao.toml` Schema-Version Wiring

- Added a narrow public helper,
  `resolve_link_graph_rerank_schema_version()`, so the live
  `xiuxian-wendao` host can resolve rerank-side Flight schema version from
  retrieval runtime config without exposing the broader internal resolver
  surface.
- Rewired `wendao_search_flight_server` so it now:
  - binds local or project-root `wendao.toml`
  - resolves `link_graph.retrieval.julia_rerank.schema_version`
  - falls back to `"v2"` when neither CLI nor config sets a value
- Replaced the fragile blank positional schema-version sentinel with an
  explicit `--schema-version=<value>` CLI override, so config-backed startup
  no longer depends on positional-argument gaps.
- Added a real-host Python smoke proving workspace `wendao.toml` can change
  the live host schema expectation and still serve typed repo-search reads
  end to end on `wendao_search_flight_server`.
- Added the corresponding precedence smoke for schema-version policy:
  - `--schema-version=<value> > wendao.toml > default`
  - the real host now proves explicit CLI schema pinning overrides a
    conflicting workspace config file.
- Propagated the same explicit `--schema-version=<value>` pattern to the
  runtime-owned `wendao_flight_server` binary, so both current Flight hosts
  now accept the same schema-pin mechanism.
- Added a direct Python smoke proving the runtime-owned binary accepts CLI
  schema pinning on its typed repo-search read path.
- Consolidated the active precedence rules into one runtime host settings
  contract, now recorded in
  [wendao-flight-runtime-host-settings-contract.md](../contracts/wendao-flight-runtime-host-settings-contract.md).
- Promoted one additional runtime-owned knob through the same checklist:
  `rerank_dimension` on `wendao_flight_server`, with precedence
  `explicit CLI override > positional arg > default`.
- Added a direct Python smoke proving the runtime-owned binary accepts
  `--rerank-dimension=<n>` and changes live rerank-route validation behavior.

## Verification

- `direnv exec . sh -lc 'export CARGO_TARGET_DIR="$PRJ_ROOT/.cache/pyflight-f56-target"; cargo test -p xiuxian-wendao --features julia test_retrieval_runtime_resolves_julia_rerank_config -- --nocapture'` -> `1 passed`
- `direnv exec . sh -lc 'export WENDAO_SEARCH_SERVER_BINARY="$PRJ_ROOT/.cache/pyflight-f56-target/debug/wendao_search_flight_server"; export WENDAO_SEARCH_SEED_BINARY="$PRJ_ROOT/.cache/pyflight-f56-target/debug/wendao_search_seed_sample"; uv run pytest packages/python/xiuxian-wendao-py/tests/test_transport.py -q -k "reads_schema_version_from_wendao_toml_via_wendao_search_flight_server"'` -> `1 passed`
- `direnv exec . sh -lc 'export WENDAO_SEARCH_SERVER_BINARY="$PRJ_ROOT/.cache/pyflight-f56-target/debug/wendao_search_flight_server"; export WENDAO_SEARCH_SEED_BINARY="$PRJ_ROOT/.cache/pyflight-f56-target/debug/wendao_search_seed_sample"; uv run pytest packages/python/xiuxian-wendao-py/tests/test_transport.py -q -k "schema_version_from_wendao_toml_via_wendao_search_flight_server or prefers_cli_schema_version_over_wendao_toml_via_wendao_search_flight_server"'` -> `2 passed`
- `direnv exec . sh -lc 'export CARGO_TARGET_DIR="$PRJ_ROOT/.cache/pyflight-rust-contract-target"; cargo build -p xiuxian-wendao-runtime --features julia --bin wendao_flight_server'` -> passed
- `direnv exec . sh -lc 'export WENDAO_MOCK_SERVER_BINARY="$PRJ_ROOT/.cache/pyflight-rust-contract-target/debug/wendao_flight_server"; uv run pytest packages/python/xiuxian-wendao-py/tests/test_transport.py -q -k "prefers_cli_schema_version_via_runtime_wendao_flight_server"'` -> `1 passed`

## PY-F70 Wendao Runtime-Config Rerank Weight Wiring

- Added a narrow public helper on the `xiuxian-wendao` side,
  `resolve_link_graph_rerank_score_weights()`, instead of exposing the whole
  internal retrieval-policy resolver surface.
- Rewired `wendao_search_flight_server` so rerank scorer weights now resolve
  from Wendao retrieval runtime config first, then fall back to the existing
  env seam.
- This means
  `link_graph.retrieval.julia_rerank.vector_weight` and
  `link_graph.retrieval.julia_rerank.similarity_weight`
  now drive the same Flight rerank behavior as the ad hoc host env vars.

## Verification

- `direnv exec . sh -lc 'export CARGO_TARGET_DIR="$PRJ_ROOT/.cache/pyflight-f56-target"; cargo test -p xiuxian-wendao --features julia test_retrieval_runtime_resolves_julia_rerank_config -- --nocapture'` -> `1 passed`
- `direnv exec . sh -lc 'export CARGO_TARGET_DIR="$PRJ_ROOT/.cache/pyflight-f56-target"; cargo build -p xiuxian-wendao --features julia --bin wendao_search_flight_server'` -> passed

## PY-F69 Runtime-Owned Rerank Weight Policy

- Lifted the rerank scorer weights out of hardcoded transport logic by
  introducing shared Rust-owned `RerankScoreWeights`.
- Kept the stable default blend at `0.4 / 0.6`, but added explicit
  `new_with_weights(...)` seams on the runtime rerank handler and Flight
  service so host binaries can override policy without changing the Flight
  wire contract.
- Rewired `wendao_flight_server` and `wendao_search_flight_server` to read:
  - `WENDAO_RERANK_VECTOR_WEIGHT`
  - `WENDAO_RERANK_SEMANTIC_WEIGHT`
- Upgraded `wendao_flight_server`'s static repo-search batch to the current
  stable repo-search response schema so the runtime binary no longer lags the
  published Python contract.
- Added a real-host Python smoke proving the runtime weight policy changes
  rerank ordering and `final_score` through `wendao_search_flight_server`.

## Verification

- `direnv exec . sh -lc 'export CARGO_TARGET_DIR="$PRJ_ROOT/.cache/pyflight-rust-contract-target"; cargo test -p xiuxian-wendao-runtime --features julia query_contract -- --nocapture'` -> `29 passed`
- `direnv exec . sh -lc 'export CARGO_TARGET_DIR="$PRJ_ROOT/.cache/pyflight-rust-contract-target"; cargo test -p xiuxian-wendao-runtime --features julia rerank_route_handler -- --nocapture'` -> `3 passed`
- `direnv exec . sh -lc 'export CARGO_TARGET_DIR="$PRJ_ROOT/.cache/pyflight-f56-target"; cargo test -p xiuxian-wendao --features julia build_search_plane_flight_service_accepts_runtime_search_plane_provider -- --nocapture'` -> `1 passed`
- `direnv exec . sh -lc 'export CARGO_TARGET_DIR="$PRJ_ROOT/.cache/pyflight-rust-contract-target"; cargo build -p xiuxian-wendao-runtime --features julia --bin wendao_flight_server'` -> passed
- `direnv exec . sh -lc 'export CARGO_TARGET_DIR="$PRJ_ROOT/.cache/pyflight-f56-target"; cargo build -p xiuxian-wendao --features julia --bin wendao_search_flight_server --bin wendao_search_seed_sample'` -> passed
- `direnv exec . env WENDAO_SEARCH_SERVER_BINARY="/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-f56-target/debug/wendao_search_flight_server" WENDAO_SEARCH_SEED_BINARY="/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-f56-target/debug/wendao_search_seed_sample" uv run pytest packages/python/xiuxian-wendao-py/tests/test_transport.py -q -k "runtime_rerank_weight_policy_via_wendao_search_flight_server or exchanges_rerank_rows_via_wendao_search_flight_server"` -> `2 passed`

## PY-F68 Rerank Score-Component Response Contract

- Extended the stable rerank response contract with:
  - `vector_score`
  - `semantic_score`
- Rewired the shared Rust scorer so the response path now carries the same
  explainable score components that feed `final_score`.
- Rewired the runtime-owned rerank response builder, the runtime Flight
  service, and Python typed parsing so `WendaoRerankResultRow` now exposes:
  - `vector_score`
  - `semantic_score`
  - `final_score`
  - `rank`
- Tightened Python and Rust validation so stale 3-column rerank responses are
  now rejected.

## Verification

- `direnv exec . uv run pytest packages/python/xiuxian-wendao-py/tests/test_query.py packages/python/xiuxian-wendao-py/tests/test_transport.py -q` -> `52 passed`
- `direnv exec . sh -lc 'export CARGO_TARGET_DIR="$PRJ_ROOT/.cache/pyflight-rust-contract-target"; cargo test -p xiuxian-wendao-runtime --features julia query_contract -- --nocapture'` -> `26 passed`

## Repo-Search Response Stop Conditions

- Audited the remaining obvious repo-search response candidates before adding
  more stable columns.
- Confirmed on the current real repo-content host that:
  - `doc_type` is effectively constant at `file`
  - `hierarchical_uri` is not populated
- Recorded the successor-RFC stop condition that low-information, empty, or
  reconstructible response fields must not be promoted into the stable Flight
  contract only for surface symmetry.
- This keeps the next `Q2/Q3` slice focused on fields with real analyzer value
  instead of widening the response schema mechanically.

## Repo-Search Response Closure

- Marked the current repo-search response contract as operationally sufficient
  for the real repo-content host.
- The retained stable response surface is now:
  - `doc_id`
  - `path`
  - `title`
  - `best_section`
  - `match_reason`
  - `navigation_path`
  - `navigation_category`
  - `navigation_line`
  - `navigation_line_end`
  - `hierarchy`
  - `tags`
  - `score`
  - `language`
- Future repo-search response growth should require new analyzer-facing value
  on the real host, not simple parity with broader `SearchHit` fields.
- The next semantic-expansion priority should shift toward rerank/runtime
  behavior unless a new repo-search field proves concrete value.

## PY-F67 Repo-Search Hierarchy Response Contract

- Extended the stable repo-search response contract with one backend-owned
  list field, `hierarchy`.
- Rewired both Rust Flight hosts so repo-search rows now publish path-derived
  hierarchy segments directly instead of forcing Python analyzers to split
  `path` and reconstruct hierarchy on their own.
- Rewired Python typed parsing so `WendaoRepoSearchResultRow` now exposes
  `hierarchy: tuple[str, ...]`.
- Tightened repo-search validation and live query smokes so both the runtime
  mock server and the real `xiuxian-wendao` host now reject stale response
  shapes and prove stable hierarchy-bearing rows.

## Verification

- `direnv exec . uv run pytest packages/python/xiuxian-wendao-py/tests/test_query.py packages/python/xiuxian-wendao-py/tests/test_transport.py -q` -> `52 passed`
- `direnv exec . sh -lc 'export CARGO_TARGET_DIR="$PRJ_ROOT/.cache/pyflight-rust-contract-target"; cargo test -p xiuxian-wendao-runtime --features julia query_contract -- --nocapture'` -> `26 passed`
- `direnv exec . sh -lc 'export CARGO_TARGET_DIR="$PRJ_ROOT/.cache/pyflight-f56-target"; cargo test -p xiuxian-wendao --features julia link_graph::plugin_runtime::transport::server::tests:: -- --nocapture'` -> `14 passed`

## Python Arrow Flight RFC Audit

- Audited the active Python Arrow/Flight work against
  [2026-03-29-python-arrow-flight-boundary-rfc.md](../rfcs/2026-03-29-python-arrow-flight-boundary-rfc.md).
- Confirmed that `PY-P1` through `PY-P4` are now operationally complete enough
  that current work is no longer boundary-removal work.
- Marked the boundary RFC as implemented and added explicit completion notes so
  future work does not keep accreting under the wrong planning surface.
- Added the successor RFC
  [2026-03-31-wendao-flight-query-contract-evolution-rfc.md](../rfcs/2026-03-31-wendao-flight-query-contract-evolution-rfc.md)
  for active repo-search and rerank contract evolution.
- Rebased the active ExecPlan
  [python-arrow-flight-redesign.md](../../.cache/codex/execplans/python-arrow-flight-redesign.md)
  onto that successor RFC.

## Verification

- `direnv exec . git diff --check` -> passed

## Arrow.jl Milestone 2 Canonical Low-Risk Extensions

- Milestone 1 was committed in the nested repo as `3bc1d80`
  (`Add canonical Arrow core type support`).
- Started the Milestone 2 working slice with a fresh ExecPlan at
  `/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/agent/execplans/arrow_julia_type_alignment_m2_low_risk_extensions.md`.
- Scoped the next production-alignment targets to:
  - `arrow.bool8`
  - `arrow.json`
  - `arrow.opaque`
- Locked the implementation order to start with `arrow.bool8`, then move to
  `arrow.json`, then `arrow.opaque`, with perf gates after each landed change
  instead of relying on a single end-of-slice sweep.
- Landed the low-risk canonical surfaces:
  - `Arrow.Bool8`
  - `Arrow.JSONText{String}`
  - `Arrow.opaquemetadata(type_name, vendor_name)`
- Kept production defaults unchanged:
  - plain Julia `Bool` still writes Arrow packed-bit bool
  - plain strings still write normal UTF-8 string columns
  - `arrow.opaque` remains explicit metadata, not implicit Julia semantics
- Added canonical roundtrip coverage for bool8/json/opaque.
- Added a targeted extension benchmark at
  `/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/agent/benchmarks/arrow_julia_m2_canonical_extension_bench.jl`.
- Tightened the explicit bool8 path with a contiguous `writearray` fast path so
  `Vector{Bool8}` writes near raw `Int8` cost instead of paying generic
  conversion overhead.
- Committed the nested repo slice as `bd7e881`
  (`Add canonical low-risk Arrow extensions`).

## Verification

- `direnv exec . julia --project=.data/arrow-julia/src/ArrowTypes -e 'using Pkg; Pkg.test()'` -> `ArrowTypes | 190/190`
- `direnv exec . julia --project=.data/arrow-julia/test /tmp/arrow_m2_smoke.jl` -> `m2-smoke-ok`
- `direnv exec . julia --project=.data/arrow-julia/test -e 'using Pkg; Pkg.instantiate(); Pkg.test("Arrow")'` -> `Arrow | 66172/66172 | 3m41.6s`
- `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_m2_canonical_extension_bench.jl > .cache/agent/tmp/arrow-julia-m2-canonical-extension-bench.tsv`
- `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_writepaths_bench.jl > .cache/agent/tmp/arrow-julia-m2-writepaths.tsv`
- `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_throughput_matrix.jl > .cache/agent/tmp/arrow-julia-m2-throughput.tsv`
- `direnv exec . git -C .data/arrow-julia diff --check` -> pass

## Arrow.jl Milestone 3 REE Read Support

- Started a dedicated ExecPlan at
  `/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/agent/execplans/arrow_julia_type_alignment_m3_ree_read_support.md`
  for a narrow Milestone 3 slice.
- Kept scope intentionally small:
  - add Run-End Encoded read support
  - keep Run-End Encoded write paths explicitly unsupported
  - defer tensors, variant, and GeoArrow work
- Verified the fixture
  `/Users/guangtao/projects/xiuxian-artisan-workshop/.data/arrow-julia/test/run_end_encoded_small.arrow`
  independently with pyarrow before locking the test oracle.
- Added a new read-only `Arrow.RunEndEncoded` vector implementation and wired
  it through `juliaeltype` and table/stream build dispatch.
- Replaced the old REE rejection tests with:
  - successful `Arrow.Table` load
  - successful `Arrow.Stream` load
  - fixture-value verification against `["a", "a", "b", "b", "b"]`
  - explicit write rejection verification
- Updated package docs to state the new contract precisely:
  - REE read supported
  - REE write still rejected
- Added a dedicated REE read benchmark at
  `/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/agent/benchmarks/arrow_julia_m3_ree_read_bench.jl`
  and archived the first baseline.

## Verification

- `direnv exec . uv run python - <<'PY' ... ipc.open_file('.data/arrow-julia/test/run_end_encoded_small.arrow') ...` -> schema `run_end_encoded<run_ends: int16, values: string>`, values `['a', 'a', 'b', 'b', 'b']`
- `direnv exec . julia --project=.data/arrow-julia/test -e 'using Pkg; Pkg.instantiate(); using Arrow, Test; ...'` -> `ree-smoke-ok`
- `direnv exec . julia --project=.data/arrow-julia/src/ArrowTypes -e 'using Pkg; Pkg.test()'` -> `ArrowTypes | 190/190`
- `direnv exec . julia --project=.data/arrow-julia/test -e 'using Pkg; Pkg.instantiate(); Pkg.test("Arrow")'` -> `Arrow | 66178/66178 | 3m19.8s`
- `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_writepaths_bench.jl > .cache/agent/tmp/arrow-julia-m3-writepaths.tsv`
- `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_throughput_matrix.jl > .cache/agent/tmp/arrow-julia-m3-throughput.tsv`
- `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_m3_ree_read_bench.jl > .cache/agent/tmp/arrow-julia-m3-ree-read.tsv`
- `direnv exec . git -C .data/arrow-julia diff --check` -> passed

## Arrow.jl Milestone 4 Canonical Advanced Passthrough

- Started a dedicated ExecPlan at
  `/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/agent/execplans/arrow_julia_type_alignment_m4_canonical_passthrough.md`.
- Scoped the slice to the narrowest production-safe alignment for advanced
  canonical extension names:
  - `arrow.parquet.variant`
  - `arrow.fixed_shape_tensor`
  - `arrow.variable_shape_tensor`
- Kept this slice read-side only:
  - recognize the canonical extension names
  - return the underlying storage types
  - avoid the generic unsupported-extension warning path
  - do not add automatic writer semantics or tensor IPC support
- Added coverage proving:
  - canonical variant metadata reads back as the storage `NamedTuple`
  - canonical fixed-shape tensor metadata reads back as fixed-size-list storage
  - canonical variable-shape tensor metadata reads back as list storage
- Updated package docs to distinguish canonical passthrough recognition from
  full semantic implementation.
- The first perf pass was noisier than expected for untouched write-path cases,
  so a same-day rerun was taken before accepting the gate. The rerun returned
  to the Milestone 3 baseline band.

## Verification

- `direnv exec . julia --project=.data/arrow-julia/src/ArrowTypes -e 'using Pkg; Pkg.test()'` -> `ArrowTypes | 190/190`
- `direnv exec . julia --project=.data/arrow-julia/test -e 'using Pkg; Pkg.instantiate(); using Arrow, Test, Logging; ...'` -> `m4-smoke-ok`
- `direnv exec . julia --project=.data/arrow-julia/test -e 'using Pkg; Pkg.instantiate(); Pkg.test("Arrow")'` -> `Arrow | 66190/66190 | 3m28.6s`
- `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_writepaths_bench.jl > .cache/agent/tmp/arrow-julia-m4-writepaths.tsv`
- `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_throughput_matrix.jl > .cache/agent/tmp/arrow-julia-m4-throughput.tsv`
- `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_writepaths_bench.jl > .cache/agent/tmp/arrow-julia-m4-writepaths-rerun.tsv`
- `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_throughput_matrix.jl > .cache/agent/tmp/arrow-julia-m4-throughput-rerun.tsv`
- `direnv exec . git -C .data/arrow-julia diff --check` -> passed

## Arrow.jl Milestone 5 Tensor Message Boundary

- Started a dedicated ExecPlan at
  `/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/agent/execplans/arrow_julia_type_alignment_m5_tensor_boundary.md`.
- Scoped the slice strictly to message-boundary behavior:
  - recognize IPC message header values `4` and `5`
  - reject tensor and sparse tensor messages precisely
  - do not add tensor payload semantics or writer support
- Added placeholder FlatBuffers table types for `Tensor` and `SparseTensor`
  and wired them into `Meta.MessageHeader`.
- Tightened both table and stream loading so tensor/sparse tensor messages now
  fail with:
  - `Arrow.TENSOR_UNSUPPORTED`
  - `Arrow.SPARSE_TENSOR_UNSUPPORTED`
- Added synthetic regression coverage by patching a valid schema message into
  tensor/sparse tensor variants, and fixed the helper to match the runtime IPC
  parser's zero-based FlatBuffers offsets.
- Updated package docs to state the tensor/sparse tensor boundary explicitly.

## Verification

- `direnv exec . julia --project=.data/arrow-julia/test -e 'using Pkg; Pkg.instantiate(); using Arrow, Test; ...'` -> `m5-smoke-ok`
- `direnv exec . julia --project=.data/arrow-julia/test -e 'using Pkg; Pkg.instantiate(); Pkg.test("Arrow")'` -> `Arrow | 66196/66196 | 3m36.8s`
- `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_writepaths_bench.jl > .cache/agent/tmp/arrow-julia-m5-writepaths.tsv`
- `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_throughput_matrix.jl > .cache/agent/tmp/arrow-julia-m5-throughput.tsv`
- Accepted serial perf reruns after discarding the first parallel run as noisy:
  - `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_writepaths_bench.jl > .cache/agent/tmp/arrow-julia-m5-writepaths-rerun.tsv`
  - `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_throughput_matrix.jl > .cache/agent/tmp/arrow-julia-m5-throughput-rerun.tsv`
- `direnv exec . git -C .data/arrow-julia diff --check` -> passed

## xiuxian-daochang React Loop Remediation

- Closed the bounded `xiuxian-daochang`
  `agent/turn_execution/react_loop/*` remediation bundle.
- Replaced wildcard and deep relative imports in the touched react-loop scope
  with explicit crate-owned imports.
- Aligned the live react-loop tool-dispatch calls with current runtime
  signatures and removed the stale `turn_ctx.session_id()` usage.
- Mounted `agent/persistence/turn_store` again so the react loop can see the
  live append-turn owner seam.
- Current compile-front owners after this slice:
  - `packages/rust/crates/xiuxian-daochang/src/agent/persistence/turn_store/*`
  - `packages/rust/crates/xiuxian-daochang/src/agent/session_context/window_ops/*`
  - `packages/rust/crates/xiuxian-daochang/src/session/bounded_store/window_ops/*`

## Verification

- `direnv exec . rustfmt /Users/guangtao/projects/xiuxian-artisan-workshop/packages/rust/crates/xiuxian-daochang/src/agent/turn_execution/react_loop/mod.rs /Users/guangtao/projects/xiuxian-artisan-workshop/packages/rust/crates/xiuxian-daochang/src/agent/turn_execution/react_loop/types.rs /Users/guangtao/projects/xiuxian-artisan-workshop/packages/rust/crates/xiuxian-daochang/src/agent/turn_execution/react_loop/messages.rs /Users/guangtao/projects/xiuxian-artisan-workshop/packages/rust/crates/xiuxian-daochang/src/agent/turn_execution/react_loop/conversation.rs /Users/guangtao/projects/xiuxian-artisan-workshop/packages/rust/crates/xiuxian-daochang/src/agent/turn_execution/react_loop/memory_recall/execution.rs /Users/guangtao/projects/xiuxian-artisan-workshop/packages/rust/crates/xiuxian-daochang/src/agent/turn_execution/react_loop/memory_recall/plan.rs /Users/guangtao/projects/xiuxian-artisan-workshop/packages/rust/crates/xiuxian-daochang/src/agent/turn_execution/react_loop/memory_recall/observability.rs /Users/guangtao/projects/xiuxian-artisan-workshop/packages/rust/crates/xiuxian-daochang/src/agent/persistence/mod.rs`
- `direnv exec . rg -n "super::super::|use super::\\*|wildcard_imports|session_id\\(\\)" /Users/guangtao/projects/xiuxian-artisan-workshop/packages/rust/crates/xiuxian-daochang/src/agent/turn_execution/react_loop -g '*.rs'`
- `direnv exec . cargo check -p xiuxian-daochang --lib --message-format short`

## xiuxian-daochang Agent Session Alignment Remediation

- Closed the bounded `Agent` / session-alignment remediation bundle.
- Restored the live `Agent` owner fields for session reset, admission,
  tool-registry, and embedding-runtime ownership.
- Rewired
  [agent/bootstrap/memory.rs](/Users/guangtao/projects/xiuxian-artisan-workshop/packages/rust/crates/xiuxian-daochang/src/agent/bootstrap/memory.rs)
  so the current crate-owned seams initialize those fields again.
- Restored the live recall-bias wrappers in
  [feedback.rs](/Users/guangtao/projects/xiuxian-artisan-workshop/packages/rust/crates/xiuxian-daochang/src/agent/feedback.rs)
  and the live reset/resume/backup wrappers in
  [backup.rs](/Users/guangtao/projects/xiuxian-artisan-workshop/packages/rust/crates/xiuxian-daochang/src/agent/session_context/backup.rs).
- Kept the touched scope on `crate::...` imports instead of deep relative
  paths.
- Current compile-front owners after this slice:
  - `packages/rust/crates/xiuxian-daochang/src/agent/tool_startup.rs`
  - `packages/rust/crates/xiuxian-daochang/src/session/redis_backend/executor.rs`
  - `packages/rust/crates/xiuxian-daochang/src/test_support/memory_metrics.rs`

## Verification

- `direnv exec . cargo check -p xiuxian-daochang --lib --message-format short`
- `direnv exec . rg -n "super::super::|use super::\\*|wildcard_imports" /Users/guangtao/projects/xiuxian-artisan-workshop/packages/rust/crates/xiuxian-daochang/src/agent/mod.rs /Users/guangtao/projects/xiuxian-artisan-workshop/packages/rust/crates/xiuxian-daochang/src/agent/bootstrap/memory.rs /Users/guangtao/projects/xiuxian-artisan-workshop/packages/rust/crates/xiuxian-daochang/src/agent/feedback.rs /Users/guangtao/projects/xiuxian-artisan-workshop/packages/rust/crates/xiuxian-daochang/src/agent/session_context/backup.rs /Users/guangtao/projects/xiuxian-artisan-workshop/packages/rust/crates/xiuxian-daochang/src/agent/tool_dispatch/dispatch.rs /Users/guangtao/projects/xiuxian-artisan-workshop/packages/rust/crates/xiuxian-daochang/src/agent/embedding_runtime.rs`
- `direnv exec . git diff --check`

## xiuxian-daochang Tool Startup + Redis Executor + Memory Metrics Remediation

- Closed the next bounded compile-front bundle after the `Agent` /
  session-alignment slice.
- Retained the startup connect config for diagnostics in
  [tool_startup.rs](/Users/guangtao/projects/xiuxian-artisan-workshop/packages/rust/crates/xiuxian-daochang/src/agent/tool_startup.rs)
  while cloning the value passed into the live tool-pool connector.
- Aligned
  [executor.rs](/Users/guangtao/projects/xiuxian-artisan-workshop/packages/rust/crates/xiuxian-daochang/src/session/redis_backend/executor.rs)
  to the current `RwLock` connection owner instead of the stale `lock()`
  path.
- Moved the memory-recall metric helper methods onto
  [memory_recall_metrics.rs](/Users/guangtao/projects/xiuxian-artisan-workshop/packages/rust/crates/xiuxian-daochang/src/agent/memory_recall_metrics.rs)
  and retired the dead
  [memory_metrics.rs](/Users/guangtao/projects/xiuxian-artisan-workshop/packages/rust/crates/xiuxian-daochang/src/test_support/memory_metrics.rs)
  test-support shim.
- Current compile-front owners after this slice:
  - `packages/rust/crates/xiuxian-daochang/src/channels/discord/runtime/managed/handlers/command_dispatch/session/budget.rs`
  - `packages/rust/crates/xiuxian-daochang/src/tool_runtime/bridge.rs`
  - `packages/rust/crates/xiuxian-daochang/src/llm/*`
  - deeper Discord/Telegram runtime drift

## Verification

- `direnv exec . rustfmt /Users/guangtao/projects/xiuxian-artisan-workshop/packages/rust/crates/xiuxian-daochang/src/agent/tool_startup.rs /Users/guangtao/projects/xiuxian-artisan-workshop/packages/rust/crates/xiuxian-daochang/src/session/redis_backend/executor.rs /Users/guangtao/projects/xiuxian-artisan-workshop/packages/rust/crates/xiuxian-daochang/src/agent/memory_recall_metrics.rs /Users/guangtao/projects/xiuxian-artisan-workshop/packages/rust/crates/xiuxian-daochang/src/test_support/mod.rs`
- `direnv exec . rg -n "super::super::|use super::\\*|wildcard_imports" /Users/guangtao/projects/xiuxian-artisan-workshop/packages/rust/crates/xiuxian-daochang/src/agent/tool_startup.rs /Users/guangtao/projects/xiuxian-artisan-workshop/packages/rust/crates/xiuxian-daochang/src/session/redis_backend/executor.rs /Users/guangtao/projects/xiuxian-artisan-workshop/packages/rust/crates/xiuxian-daochang/src/agent/memory_recall_metrics.rs /Users/guangtao/projects/xiuxian-artisan-workshop/packages/rust/crates/xiuxian-daochang/src/test_support/mod.rs`
- `direnv exec . cargo check -p xiuxian-daochang --lib --message-format short`
- `direnv exec . git diff --check`

## Xiuxian-Daochang Budget Bridge and LLM Bundle

- Rebound the touched Discord budget command handler to explicit
  `crate::...` owner imports in
  [budget.rs](/Users/guangtao/projects/xiuxian-artisan-workshop/packages/rust/crates/xiuxian-daochang/src/channels/discord/runtime/managed/handlers/command_dispatch/session/budget.rs)
  and fixed the touched snapshot formatter calls to pass references.
- Aligned
  [bridge.rs](/Users/guangtao/projects/xiuxian-artisan-workshop/packages/rust/crates/xiuxian-daochang/src/tool_runtime/bridge.rs)
  to the live `reqwest` and touched JSON-RPC owner seams by keeping connect
  diagnostics on a cloned config, removing the stale `bytes_stream()` usage,
  and fixing the touched closure-capture drift.
- Restored the touched `llm/*` surface to the live shape in
  [chat.rs](/Users/guangtao/projects/xiuxian-artisan-workshop/packages/rust/crates/xiuxian-daochang/src/llm/client/chat.rs),
  [init.rs](/Users/guangtao/projects/xiuxian-artisan-workshop/packages/rust/crates/xiuxian-daochang/src/llm/client/init.rs),
  and
  [litellm_ocr.rs](/Users/guangtao/projects/xiuxian-artisan-workshop/packages/rust/crates/xiuxian-daochang/src/llm/compat/litellm_ocr.rs)
  by restoring `max_tokens` and explicit
  `DeepseekRuntime::RemoteHttp` handling.
- Current compile-front owners after this slice:
  - `packages/rust/crates/xiuxian-daochang/src/channels/discord/runtime/gateway/*`
  - `packages/rust/crates/xiuxian-daochang/src/channels/discord/runtime/run/*`
  - `packages/rust/crates/xiuxian-daochang/src/channels/telegram/channel/send_api/*`
  - `packages/rust/crates/xiuxian-daochang/src/channels/telegram/channel/send_text/*`
  - `packages/rust/crates/xiuxian-daochang/src/channels/telegram/runtime/*`

## Verification

- `direnv exec . rustfmt /Users/guangtao/projects/xiuxian-artisan-workshop/packages/rust/crates/xiuxian-daochang/src/channels/discord/runtime/managed/handlers/command_dispatch/session/budget.rs /Users/guangtao/projects/xiuxian-artisan-workshop/packages/rust/crates/xiuxian-daochang/src/tool_runtime/bridge.rs /Users/guangtao/projects/xiuxian-artisan-workshop/packages/rust/crates/xiuxian-daochang/src/llm/client/chat.rs /Users/guangtao/projects/xiuxian-artisan-workshop/packages/rust/crates/xiuxian-daochang/src/llm/client/init.rs /Users/guangtao/projects/xiuxian-artisan-workshop/packages/rust/crates/xiuxian-daochang/src/llm/compat/litellm_ocr.rs`
- `direnv exec . rg -n "super::super::|use super::\\*|wildcard_imports" /Users/guangtao/projects/xiuxian-artisan-workshop/packages/rust/crates/xiuxian-daochang/src/channels/discord/runtime/managed/handlers/command_dispatch/session/budget.rs /Users/guangtao/projects/xiuxian-artisan-workshop/packages/rust/crates/xiuxian-daochang/src/tool_runtime/bridge.rs /Users/guangtao/projects/xiuxian-artisan-workshop/packages/rust/crates/xiuxian-daochang/src/llm/client/chat.rs /Users/guangtao/projects/xiuxian-artisan-workshop/packages/rust/crates/xiuxian-daochang/src/llm/client/init.rs /Users/guangtao/projects/xiuxian-artisan-workshop/packages/rust/crates/xiuxian-daochang/src/llm/compat/litellm_ocr.rs`
- `direnv exec . cargo check -p xiuxian-daochang --lib --message-format short`
- `direnv exec . git diff --check`

## Arrow.jl Milestone 1 Canonical Core Perf Follow-Up

- Re-validated the Milestone 1 functional slice on the dirty `flight`
  worktree rather than changing scope again.
- Confirmed that the broad write-path harness was noisier than the earlier
  Milestone 0 archive and should not be treated as the only perf signal.
- Added a detached baseline worktree at commit `83cc25b` under
  `/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/agent/tmp/arrow-julia-m0-baseline`
  to run same-day A/B measurements on the same machine.
- Added a focused benchmark harness at
  `/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/agent/benchmarks/arrow_julia_binary_nested_focus_bench.jl`
  to isolate the two blocking cases: `offset_binary` and `offset_nested_list`.
- Same-day focused A/B result:
  - `offset_binary` is effectively flat within noise between current Milestone 1
    and `83cc25b`
  - `offset_nested_list` remains marginally slower, roughly in the `5-7%`
    band across focused reruns
- Current conclusion:
  - the remaining perf question was isolated to single-partition direct
    `tobuffer` on top-level non-string nested lists
  - narrowing that eligibility back to the legacy writer closed the perf gate
  - Milestone 1 is now commit-ready as a standalone slice

## Verification

- `direnv exec . julia --project=.data/arrow-julia/src/ArrowTypes -e 'using Pkg; Pkg.test()'` -> `ArrowTypes | 190/190`
- `direnv exec . julia --project=.data/arrow-julia/test -e 'using Pkg; Pkg.instantiate(); Pkg.test("Arrow")'` -> `Arrow | 66158/66158 | 102m41.6s`
- `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_writepaths_bench.jl > .cache/agent/tmp/arrow-julia-m1-writepaths-rerun3.tsv`
- `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_throughput_matrix.jl > .cache/agent/tmp/arrow-julia-m1-throughput-rerun2.tsv`
- `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_binary_nested_focus_bench.jl > .cache/agent/tmp/arrow-julia-binary-nested-focus-current.tsv`
- `direnv exec . julia --project=.cache/agent/tmp/arrow-julia-m0-baseline/test -e 'using Pkg; Pkg.develop(PackageSpec(path=\"/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/agent/tmp/arrow-julia-m0-baseline\")); Pkg.develop(PackageSpec(path=\"/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/agent/tmp/arrow-julia-m0-baseline/src/ArrowTypes\")); Pkg.instantiate()'`
- `direnv exec . julia --project=.cache/agent/tmp/arrow-julia-m0-baseline/test .cache/agent/benchmarks/arrow_julia_binary_nested_focus_bench.jl > .cache/agent/tmp/arrow-julia-binary-nested-focus-baseline.tsv`
- `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_binary_nested_focus_bench.jl > .cache/agent/tmp/arrow-julia-binary-nested-focus-current-rerun.tsv`
- `direnv exec . julia --project=.cache/agent/tmp/arrow-julia-m0-baseline/test .cache/agent/benchmarks/arrow_julia_binary_nested_focus_bench.jl > .cache/agent/tmp/arrow-julia-binary-nested-focus-baseline-rerun.tsv`
- `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_nested_list_direct_legacy_ab.jl > .cache/agent/tmp/arrow-julia-nested-list-direct-legacy-ab.tsv`
- `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_nested_list_direct_legacy_ab.jl > .cache/agent/tmp/arrow-julia-nested-list-direct-legacy-ab-postpatch.tsv`
- `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_writepaths_bench.jl > .cache/agent/tmp/arrow-julia-m1-writepaths-postpatch-rerun.tsv`
- `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_throughput_matrix.jl > .cache/agent/tmp/arrow-julia-m1-throughput-postpatch-rerun.tsv`
- `direnv exec . git -C .data/arrow-julia diff --check` -> pass

## PY-F58 Real-Host Repo-Search Tag Filter Contract

- Extended the stable repo-search request contract with
  `x-wendao-repo-search-tag-filters`.
- Rewired the Rust Flight host and the real `xiuxian-wendao` provider so
  typed repo-search requests can carry optional tag filters end to end.
- Rewired the Python typed request surface so
  `repo_search_request(..., tag_filters=(...))` emits sorted stable metadata
  and rejects blank tag filters.
- Tightened the real `wendao_search_flight_server` smoke so one persisted repo
  corpus now proves:
  - `lang:markdown` narrows `alpha` to `README.md`
  - `lang:rust` excludes that row

## Verification

- `direnv exec . uv run pytest /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/xiuxian-wendao-py/tests/test_query.py /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/xiuxian-wendao-py/tests/test_transport.py -q -k "repo_search_request or fetches_repo_search_info_with_typed_request"` -> `11 passed`
- `direnv exec . cargo test -p xiuxian-wendao-runtime --features julia query_contract -- --nocapture` -> `25 passed`
- `direnv exec . env CARGO_TARGET_DIR=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-f56-target cargo test -p xiuxian-wendao --features julia link_graph::plugin_runtime::transport::server::tests:: -- --nocapture` -> `11 passed`
- `direnv exec . env WENDAO_SEARCH_SERVER_BINARY=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-f56-target/debug/wendao_search_flight_server WENDAO_SEARCH_SEED_BINARY=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-f56-target/debug/wendao_search_seed_sample uv run pytest /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/xiuxian-wendao-py/tests/test_transport.py -q -k wendao_search_flight_server` -> `4 passed`

## PY-F59 Real-Host Repo-Search Filename Filter Contract

- Extended the stable repo-search request contract with
  `x-wendao-repo-search-filename-filters`.
- Rewired the Rust Flight host and the real `xiuxian-wendao` provider so
  typed repo-search requests can carry optional filename filters end to end.
- Rewired the Python typed request surface so
  `repo_search_request(..., filename_filters=(...))` emits sorted stable
  metadata and rejects blank filename filters.
- Tightened the provider and real-host semantics to case-insensitive exact
  filename matching instead of broad substring matching.
- Tightened the real `wendao_search_flight_server` smoke so one persisted repo
  corpus now proves:
  - `readme.md` narrows `alpha` to `README.md`
  - unrelated file names are not pulled in by substring coincidence

## Verification

- `direnv exec . uv run pytest /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/xiuxian-wendao-py/tests/test_query.py /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/xiuxian-wendao-py/tests/test_transport.py -q -k "repo_search_request or fetches_repo_search_info_with_typed_request"` -> `13 passed`
- `direnv exec . cargo test -p xiuxian-wendao-runtime --features julia query_contract -- --nocapture` -> `26 passed`
- `direnv exec . env CARGO_TARGET_DIR=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-f56-target cargo test -p xiuxian-wendao --features julia link_graph::plugin_runtime::transport::server::tests:: -- --nocapture` -> `12 passed`
- `direnv exec . env WENDAO_SEARCH_SERVER_BINARY=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-f56-target/debug/wendao_search_flight_server WENDAO_SEARCH_SEED_BINARY=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-f56-target/debug/wendao_search_seed_sample uv run pytest /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/xiuxian-wendao-py/tests/test_transport.py -q -k wendao_search_flight_server` -> `4 passed`

## PY-F60 Real-Host Repo-Search Exact-Match Tag Semantics

- Reused the existing tag-filter contract instead of introducing another
  request header.
- Rewired repo-content search hits so backend exact matches now publish
  `match:exact` in the stable tag set.
- Tightened provider and real-host coverage so the same persisted corpus now
  proves:
  - `repo_search_request(..., tag_filters=("match:exact",))`
  - narrows `searchonlytoken` to `src/search.rs`

## Verification

- `direnv exec . env CARGO_TARGET_DIR=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-f56-target cargo test -p xiuxian-wendao --features julia link_graph::plugin_runtime::transport::server::tests:: -- --nocapture` -> `13 passed`
- `direnv exec . env CARGO_TARGET_DIR=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-f56-target cargo build -p xiuxian-wendao --features julia --bin wendao_search_flight_server --bin wendao_search_seed_sample` -> passed
- `direnv exec . env WENDAO_SEARCH_SERVER_BINARY=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-f56-target/debug/wendao_search_flight_server WENDAO_SEARCH_SEED_BINARY=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-f56-target/debug/wendao_search_seed_sample uv run pytest /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/xiuxian-wendao-py/tests/test_transport.py -q -k wendao_search_flight_server` -> `4 passed`

## Arrow.jl Flight App Metadata Emit Surface

- Extended the upstream native Julia Flight emit surface so batch-wise
  `app_metadata` is now owned by `arrow-julia`, not by raw
  `Protocol.FlightData` construction in downstream packages.
- Landed explicit `app_metadata=...` support on:
  - `Arrow.Flight.flightdata(...)`
  - `Arrow.Flight.putflightdata!(...)`
  - source-based `Arrow.Flight.doexchange(...)`
- Kept the decode side aligned with the already-landed
  `include_app_metadata=true` surface on `Arrow.Flight.stream(...)` /
  `Arrow.Flight.table(...)`.
- Added local Flight IPC conversion coverage for explicit `app_metadata`
  emission and arity mismatch failure.
- Added real `pyarrow.flight` `DoExchange` interop coverage proving native
  Julia source-based `doexchange(...; app_metadata=...)` can send batch-wise
  metadata that the Python reference server echoes back.

## Verification

- `direnv exec . julia --project=.data/arrow-julia/test -e 'using Pkg; Pkg.instantiate(); using Test, Arrow, Tables, gRPCClient; include(".data/arrow-julia/test/flight/ipc_conversion.jl")'` -> `Flight IPC conversion helpers | 59/59`
- `direnv exec . julia --project=.data/arrow-julia/test -e 'using Pkg; Pkg.instantiate(); using Test, Arrow, Tables, Dates, UUIDs, gRPCClient; include(".data/arrow-julia/test/flight.jl")'` -> `Flight pyarrow interop | 35/35`

## PY-F81 Search-Host Rerank-Dimension Precedence

- Tightened the SearchPlane-backed real host coverage so `wendao_search_flight_server`
  now has a direct Python smoke for rerank-dimension precedence.
- Locked the host behavior:
  `explicit CLI override > positional arg > default`.
- The smoke starts the host with a conflicting positional rerank dimension and
  explicit `--rerank-dimension=<n>`, then proves a 4-dimensional rerank request
  succeeds on the live host path.

## WendaoArrow Retrieval Mode Typed Enum + Flight Test Modularization

- Promoted metadata `retrieval_mode` onto the typed Julia enum path alongside
  `cache_backend`, `cache_scope`, and `ranking_strategy`.
- Added native-Julia and `pyarrow.flight` coverage proving that
  `retrieval_mode` now round-trips as
  `WendaoArrow.LinkGraphRetrievalMode` on the native side and as primitive
  storage plus `JuliaLang.Enum` field metadata on the Python side.
- Added a dedicated bad-response Flight example for invalid outbound
  `retrieval_mode` data and matching remote invalid-argument regressions.
- Split the cross-process Flight regression matrix out of the monolithic
  `test/flight_grpcserver.jl` file into focused include files under
  `.data/WendaoArrow/test/flight_grpcserver/`.
- Split the package-local regression matrix out of the monolithic
  `test/runtests.jl` file into focused include files under
  `.data/WendaoArrow/test/runtests/`.
- Split the contract helper implementation out of the monolithic
  `src/contracts.jl` file into focused source files under
  `.data/WendaoArrow/src/contracts/`.
- Repaired the split test harness root-path assumptions, fixed three
  mechanically truncated child-file tails, and raised the native Julia test
  RPC deadline in the harness to `120` seconds so cold temporary Flight
  environments stop timing out during metadata round-trips.

## Verification

- `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/runtests.jl` -> passed
- `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/flight_grpcserver.jl` -> passed
- `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/flight_grpcserver.jl` -> passed
- `direnv exec . git diff --check -- .data/WendaoArrow docs/GTD/DAILY_2026_03_31.md .cache/codex/execplans/wendaoarrow_arrow_flight_adaptation.md` -> passed
- `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/flight_grpcserver.jl` -> passed

## Arrow.jl Enum Logical-Type Contract Hardening

- Hardened the `JuliaLang.Enum` trait path in
  `.data/arrow-julia/src/ArrowTypes/src/ArrowTypes.jl` so enum serialization no
  longer routes through `Int(...)`; legal `UInt64`-backed enums above
  `typemax(Int)` now serialize through their real basetype.
- Tightened enum read-side resolution so `labels=` is enforced as contract:
  mismatched, malformed, or missing label metadata now refuses local enum
  resolution and falls back to raw storage semantics instead of silently
  accepting drift.
- Added focused ArrowTypes and package regressions for:
  - wide unsigned enum basetypes
  - reordered-but-equivalent label metadata
  - mismatched and malformed labels
  - package-level raw-storage fallback under mismatched enum metadata
- Recorded a narrow enum perf artifact at
  `.cache/agent/tmp/arrow-julia-enum-contract-bench.tsv`; current medians are:
  - `wide_enum write = 0.071 ms, read = 0.092 ms`
  - `wide_enum_missing write = 0.075 ms, read = 0.093 ms`

## Verification

- `direnv exec . julia --project=.data/arrow-julia/src/ArrowTypes -e 'using Pkg; Pkg.test()'` -> `ArrowTypes | 203/203`
- `direnv exec . julia --project=.data/arrow-julia/test -e 'using Pkg; Pkg.instantiate(); Pkg.test("Arrow")'` -> `Arrow | 66233/66233 | 3m25.8s`
- `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_enum_contract_bench.jl` -> passed

## Arrow.jl Built-In Logical Runtime Shim Centralization

- Tightened the metadata-bearing built-in logical read path so package-level
  contract tests now directly assert `ArrowTypes.JuliaType(Val(...), storageT,
metadata)` for:
  - `arrow.bool8`
  - `arrow.json`
  - `arrow.opaque`
  - `arrow.timestamp_with_offset`
  - `arrow.parquet.variant`
  - `arrow.fixed_shape_tensor`
  - `arrow.variable_shape_tensor`
- Tried a generic public fallback on
  `ArrowTypes.JuliaType(::Val{name}, storageT, metadata)` and rejected it after
  it created an ambiguity with user-defined 3-argument logical-type
  registrations such as the existing `CustomStruct2` test seam.
- Landed the production-safe version instead:
  public `ArrowTypes.JuliaType` methods remain explicit in
  `.data/arrow-julia/src/eltypes.jl`, but their implementations now delegate to
  the Arrow-owned built-in registry in
  `.data/arrow-julia/src/logicaltypes_builtin.jl`.
- Updated extension-focused perf evidence:
  - artifact: `.cache/agent/tmp/arrow-julia-extension-contract-bench-shim.tsv`
  - `uuid_canonical write = 0.455 ms, read_copy = 31.596 ms`
  - `bool8 write = 0.067 ms, read_copy = 0.012 ms`
  - `json_text write = 1.406 ms, read_copy = 0.603 ms`
  - `timestamp_with_offset write = 0.144 ms, read_copy = 2.229 ms`

## Verification

- `direnv exec . julia --project=.data/arrow-julia/test -e 'using Pkg; Pkg.instantiate(); Pkg.test("Arrow")'` -> `Arrow | 66240/66240 | 3m30.1s`
- `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_extension_contract_bench.jl` -> passed

## Arrow.jl Built-In Logical Write-Side Shim Centralization

- Applied the same centralization strategy to built-in write-side logical
  registration without widening the public trait surface.
- Added Arrow-owned internal helpers:
  - `_builtinarrowtype`
  - `_builtintoarrow`
  - `_builtinarrowname`
- Registered built-in write mapping in
  `.data/arrow-julia/src/logicaltypes_builtin.jl` for:
  - `Bool8`
  - `JSONText{S}`
  - `ZonedDateTime`
  - `TimestampWithOffset{U}` name emission
- Kept public `ArrowTypes.ArrowType`, `ArrowTypes.toarrow`, and
  `ArrowTypes.arrowname` definitions explicit in `.data/arrow-julia/src/eltypes.jl`,
  but reduced them to thin shims into the Arrow-owned built-in helper layer.
- Added direct runtime contract regressions in
  `.data/arrow-julia/test/runtests.jl` for the new helper entrypoints.

## Verification

- `direnv exec . julia --project=.data/arrow-julia/test -e 'using Pkg; Pkg.instantiate(); Pkg.test("Arrow")'` -> `Arrow | 66250/66250 | 3m38.3s`
- `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_extension_contract_bench.jl` -> artifact `.cache/agent/tmp/arrow-julia-extension-contract-bench-write-shim.tsv`
  - `uuid_canonical write = 0.648 ms, read_copy = 33.276 ms`
  - `bool8 write = 0.063 ms, read_copy = 0.010 ms`
  - `json_text write = 1.627 ms, read_copy = 0.625 ms`
  - `timestamp_with_offset write = 0.150 ms, read_copy = 2.177 ms`

## PY-F61 Real-Host Exact-Match Ranking Preference

- Expanded the persisted repo-search fixture corpus with one mixed-case exact
  token row and one lowercase-only folded-match row.
- Tightened provider and real-host coverage so the same typed repo-search
  route now proves exact-case matches outrank folded-only matches.
- The real host now proves:
  - `repo_search_request("CamelBridgeToken", limit=1)`
  - returns `docs/CamelBridge.md`
  - ahead of the lowercase-only `src/camelbridge.rs`

## Verification

- `direnv exec . env CARGO_TARGET_DIR=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-f56-target cargo test -p xiuxian-wendao --features julia link_graph::plugin_runtime::transport::server::tests:: -- --nocapture` -> `14 passed`
- `direnv exec . env CARGO_TARGET_DIR=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-f56-target cargo build -p xiuxian-wendao --features julia --bin wendao_search_flight_server --bin wendao_search_seed_sample` -> passed
- `direnv exec . env WENDAO_SEARCH_SERVER_BINARY=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-f56-target/debug/wendao_search_flight_server WENDAO_SEARCH_SEED_BINARY=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-f56-target/debug/wendao_search_seed_sample uv run pytest /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/xiuxian-wendao-py/tests/test_transport.py -q -k wendao_search_flight_server` -> `4 passed`

## WENDAOARROW Flight Response App Metadata

- Added a packaged `flight_response(table; app_metadata=...)` seam in
  `WendaoArrow` so analyzer processors can emit batch-wise `FlightData`
  sideband metadata without assembling raw protocol messages.
- Added a dedicated Flight-only example/startup path for response
  `app_metadata` and proved it locally plus cross-process to both
  `pyarrow.flight` and native Julia clients.
- Hardened the split `WendaoArrow` pyarrow cross-process harness with a narrow
  retry helper for transient `FlightUnavailableError` /
  `Can't assign requested address` startup noise, without changing runtime
  behavior.

## Verification

- `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/runtests.jl` -> passed
- `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/flight_grpcserver.jl` -> passed

## Arrow Metadata Overlay Sinking

- Added upstream `Arrow.withmetadata(...)` to `arrow-julia` as a lightweight
  schema/field metadata overlay helper for Tables.jl-compatible sources.
- Covered that helper with an Arrow roundtrip regression proving merged schema
  metadata and merged column metadata survive `Arrow.tobuffer(...)` /
  `Arrow.Table(...)`.
- Rewired `WendaoArrow` scoring normalization and metadata example paths to use
  `Arrow.withmetadata(...)` instead of the package-local metadata carrier
  structs.
- Removed the local metadata carrier implementation from
  `.data/WendaoArrow/src/contracts/support.jl`, so downstream now keeps only the
  business contract layer instead of owning generic Arrow metadata plumbing.

## Verification

- `direnv exec . julia --project=.data/arrow-julia/test -e 'using Pkg; Pkg.instantiate(); Pkg.develop(PackageSpec(path=".data/arrow-julia")); Pkg.develop(PackageSpec(path=".data/arrow-julia/src/ArrowTypes")); Pkg.test("Arrow")'` -> `Arrow | 66285/66285 | 3m43.6s`
- `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/runtests.jl` -> passed
- `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/flight_grpcserver.jl` -> passed

## Arrow Flight High-Level app_metadata Surface

- Extended the native Julia high-level Flight decode surface so
  `Arrow.Flight.stream(...; include_app_metadata=true)` now yields per-batch
  `(table, app_metadata)` records instead of forcing callers back to raw
  `Protocol.FlightData`.
- Extended `Arrow.Flight.table(...; include_app_metadata=true)` so the
  materialized result now returns the aggregated table plus the ordered batch
  `app_metadata` vector.
- Covered the new opt-in surface in local IPC conversion tests and in the real
  `pyarrow.flight` `DoExchange` exchange interop path, proving that high-level
  Julia decode can now see `exchange:0` / `exchange:1` without manually walking
  raw Flight messages.

## Verification

- `direnv exec . julia --project=.data/arrow-julia/test -e 'using Pkg; Pkg.instantiate(); using Test, Arrow, Tables, gRPCClient; include(".data/arrow-julia/test/flight.jl")'` -> passed

## WendaoArrow Native Flight Hot-Path Alignment

- Reworked the packaged WendaoArrow Flight service seam so request handling no
  longer starts by whole-stream `collect(...)`.
- The Flight adapter now peeks only the first inbound `FlightData` message to
  recover descriptor/logging context, then passes the primed message iterator
  straight into the upstream Arrow Flight stream/table decode path.
- Response emission now writes directly through
  `Arrow.Flight.putflightdata!(response, ...)` instead of materializing an
  intermediate `Vector{FlightData}` first.
- Updated the package README and transport profile docs to show the stream-first
  Flight example without `collect(stream)` and to document the new hot path.
- While validating this slice, fixed a mechanical parse error in
  `.data/arrow-julia/src/flight/client/methods/data.jl` so the vendored Arrow
  dependency precompiles again.

## Verification

- `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/runtests.jl` -> passed
- `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/flight_grpcserver.jl` -> passed

## WendaoArrow Additive Response Field Metadata Preservation

- Added an internal metadata-wrapping merge path so packaged examples can
  attach field metadata onto existing table-like responses without losing any
  processor-owned schema metadata or existing field metadata.
- Rewired the packaged metadata example path to stamp additive response
  columns such as `trace_id`, `attempt_count`, `cache_backend`, and
  `ranking_strategy` with `semantic.role` field metadata before shared
  normalization runs.
- Tightened local HTTP coverage and cross-process Flight coverage so both
  `pyarrow.flight` and native Julia now prove that those additive response
  columns keep their field metadata after normalization and remote decode.

## Verification

- `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/runtests.jl` -> passed

## WendaoArrow Response Column Metadata Preservation

- Reworked `normalize_scoring_response(...)` so packaged scoring-style
  normalization preserves processor-owned schema metadata and field metadata
  instead of rebuilding plain output columns that discard field-level
  annotations.
- Added local package proofs covering:
  - direct helper preservation of schema and column metadata
  - HTTP Arrow IPC response preservation after normalization
  - package-local Flight `DoExchange` preservation after normalization
- Extended the dedicated cross-process schema-metadata Flight startup example
  so both `pyarrow.flight` and native Julia now prove:
  - additive response schema metadata survives transport
  - `wendao.schema_version` is still force-normalized to `v1`
  - field metadata on `analyzer_score` / `final_score` survives packaged
    scoring normalization and remote decode

## Verification

- `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/runtests.jl` -> passed

## WendaoArrow Request Contract Settlement

- Settled the packaged WendaoArrow request contract docs to match the current
  implementation:
  - `doc_id` and `vector_score` remain the only required request columns
  - `embedding` and `query_embedding` are now documented as recommended
    upstream vector columns instead of WendaoArrow-core required columns
  - empty IPC/Flight request payloads remain explicitly rejected
- Added `require_unique_string_column(...)` to the shared contract helpers and
  wired the packaged scoring and metadata example processors through one shared
  request-side normalization path for `doc_id` and `vector_score`.
- The shared request-side path now rejects duplicate `doc_id` values across the
  full streamed request batch set instead of only relying on per-batch column
  shape checks.
- Extended package-local and cross-process Flight regressions so both the
  scoring and metadata example servers now prove duplicate-`doc_id`
  request failures as explicit `INVALID_ARGUMENT(3)` contract errors.
- Refreshed `.data/WendaoArrow/Manifest.toml` via `Pkg.resolve()` /
  `Pkg.instantiate()` so the vendored Arrow dependency graph now includes the
  required `JSON3` entry and local package tests run cleanly again.

## Verification

- `direnv exec . julia --project=.data/WendaoArrow -e 'using Pkg; Pkg.resolve(); Pkg.instantiate()'` -> updated `.data/WendaoArrow/Manifest.toml` with `JSON3` and precompiled `WendaoArrow`
- `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/runtests.jl` -> passed
- `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/flight_grpcserver.jl` -> passed

## WendaoArrow Response Schema Metadata Preservation

- Reworked WendaoArrow response encoding so HTTP IPC and Flight `DoExchange`
  no longer replace processor-provided schema metadata with a bare
  `wendao.schema_version` map.
- Response encoding now preserves additive analyzer-owned schema metadata keys
  and force-writes `wendao.schema_version = v1` on top.
- Added package-local HTTP and local Flight regressions proving that a
  processor can return an `Arrow.Table` carrying custom schema metadata and
  still get that metadata back after WendaoArrow transport encoding.
- Added a dedicated cross-process Flight startup example and regression proving
  the same behavior for both `pyarrow.flight` and native Julia clients.

## Verification

- `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/runtests.jl` -> passed
- `direnv exec . julia --project=.data/WendaoArrow .data/WendaoArrow/test/flight_grpcserver.jl` -> passed

## Arrow.jl Logical Extension Contract Follow-up

- Continued the Arrow-owned logical/extension runtime contract work in
  `.data/arrow-julia`.
- Tightened the UUID built-in helper so the Arrow-owned write-side contract no
  longer depends on `ArrowTypes.toarrow(::UUID)`; helper conversion now casts
  directly from `uuid.value`.
- Added runtime-contract regressions that assert helper/public trait
  consistency for `UUID`.
- Explicitly rejected a stricter design that overwrote the public
  `ArrowTypes.ArrowType/toarrow/arrowname/JuliaType` UUID methods from
  `Arrow`; that version failed package precompilation with method-overwrite
  errors and was rolled back.

## Verification

- `direnv exec . julia --project=.data/arrow-julia/test -e 'using Pkg; Pkg.instantiate(); using Arrow; println("arrow-load-ok")'` -> `arrow-load-ok`
- `direnv exec . julia --project=.data/arrow-julia/test -e 'using Pkg; Pkg.instantiate(); Pkg.test("Arrow")'` -> `Arrow | 66258/66258 | 3m46.4s`
- `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_extension_contract_bench.jl > .cache/agent/tmp/arrow-julia-extension-contract-bench-uuid-helper.tsv`
- `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_writepaths_bench.jl > .cache/agent/tmp/arrow-julia-writepaths-uuid-helper.tsv`

## Arrow.jl TimestampWithOffset Contract Shim

- Continued the Arrow-owned logical contract work with a safe
  `TimestampWithOffset{U}` write-side shim.
- Added Arrow-owned `_builtinarrowtype` and `_builtintoarrow` helpers for
  `TimestampWithOffset{U}` in `.data/arrow-julia/src/logicaltypes_builtin.jl`.
- Kept the physical layout unchanged by using the same
  `NamedTuple{(:timestamp, :offset_minutes), ...}` storage shape that the
  generic struct path already serialized.
- Added direct runtime-contract regressions that assert helper/public trait
  consistency for `TimestampWithOffset{U}`.

## Verification

- `direnv exec . julia --project=.data/arrow-julia/test -e 'using Pkg; Pkg.instantiate(); Pkg.test("Arrow")'` -> `Arrow | 66262/66262 | 3m35.1s`
- `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_extension_contract_bench.jl > .cache/agent/tmp/arrow-julia-extension-contract-bench-ts-shim.tsv`
- `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_extension_contract_bench.jl > .cache/agent/tmp/arrow-julia-extension-contract-bench-ts-shim-rerun.tsv`
- `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_writepaths_bench.jl > .cache/agent/tmp/arrow-julia-writepaths-ts-shim.tsv`

## Arrow.jl Built-in Read Contract Shim

- Continued the Arrow-owned logical contract centralization on the read/default
  side.
- Added internal helpers in `.data/arrow-julia/src/logicaltypes.jl` and
  `.data/arrow-julia/src/logicaltypes_builtin.jl` for:
  - `_builtinfromarrow`
  - `_builtinfromarrowstruct`
  - `_builtindefault`
- Moved built-in implementations for `Bool8`, `JSONText{S}`,
  `ZonedDateTime`, and `TimestampWithOffset{U}` to the Arrow-owned helper
  registry.
- Kept public `ArrowTypes.fromarrow`, `ArrowTypes.fromarrowstruct`, and
  `ArrowTypes.default` as thin shims in `.data/arrow-julia/src/eltypes.jl`.

## Verification

- `direnv exec . julia --project=.data/arrow-julia/test -e 'using Pkg; Pkg.instantiate(); Pkg.test("Arrow")'` -> `Arrow | 66271/66271 | 3m24.3s`
- `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_extension_contract_bench.jl > .cache/agent/tmp/arrow-julia-extension-contract-bench-read-shim.tsv`
- `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_writepaths_bench.jl > .cache/agent/tmp/arrow-julia-writepaths-read-shim.tsv`

## Arrow.jl Built-in Validation Dispatch Shim

- Continued the Arrow-owned logical contract centralization on canonical
  validation dispatch.
- Added `_validatebuiltinextension(spec, field)` in
  `.data/arrow-julia/src/logicaltypes.jl`.
- Moved canonical passthrough validation routing for:
  - `arrow.parquet.variant`
  - `arrow.fixed_shape_tensor`
  - `arrow.variable_shape_tensor`
    into `.data/arrow-julia/src/logicaltypes_builtin.jl`.
- Removed the ad hoc `_validatecanonicalpassthrough(...)` branch from
  `.data/arrow-julia/src/eltypes.jl`.

## Verification

- `direnv exec . julia --project=.data/arrow-julia/test -e 'using Pkg; Pkg.instantiate(); Pkg.test("Arrow")'` -> `Arrow | 66271/66271 | 3m39.2s`
- `.cache/agent/tmp/arrow-julia-extension-contract-bench-validation-dispatch.tsv`
- `.cache/agent/tmp/arrow-julia-writepaths-validation-dispatch.tsv`

## Arrow.jl Metadata Builder Shim

- Continued the same contract line by moving canonical metadata-builder logic
  into the Arrow-owned logical helper registry.
- Added internal helpers in:
  - `.data/arrow-julia/src/logicaltypes.jl`
  - `.data/arrow-julia/src/logicaltypes_builtin.jl`
    for:
  - `_builtinopaquemetadata`
  - `_builtinvariantmetadata`
  - `_builtinfixedshapetensormetadata`
  - `_builtinvariableshapetensormetadata`
- Kept the public API in `.data/arrow-julia/src/eltypes.jl` stable as thin
  shims:
  - `opaquemetadata(...)`
  - `variantmetadata()`
  - `fixedshapetensormetadata(...)`
  - `variableshapetensormetadata(...)`
- Added direct runtime-contract regressions in
  `.data/arrow-julia/test/runtests.jl` asserting public/helper equivalence.

## Verification

- `direnv exec . julia --project=.data/arrow-julia/test -e 'using Pkg; Pkg.instantiate(); Pkg.test("Arrow")'` -> `Arrow | 66275/66275 | 3m28.5s`
- `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_extension_contract_bench.jl > .cache/agent/tmp/arrow-julia-extension-contract-bench-metadata-shim.tsv`
- `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_writepaths_bench.jl > .cache/agent/tmp/arrow-julia-writepaths-metadata-shim.tsv`
- Key perf readouts:
  - `uuid_canonical write 0.582 ms / read_copy 32.473 ms`
  - `bool8 write 0.068 ms / read_copy 0.009 ms`
  - `json_text write 1.701 ms / read_copy 0.604 ms`
  - `timestamp_with_offset write 0.148 ms / read_copy 2.164 ms`
  - `offset_string 1.467 ms`
  - `offset_binary 2.078 ms`
  - `offset_nested_list 0.845 ms`
  - `offset_map 1.954 ms`

## Arrow.jl Built-in Alias Resolution Shim

- Continued the Arrow-owned logical contract centralization on metadata-less
  built-in alias resolution.
- Added `_builtinextensionjuliatype(::Val{name}, storageT)` in
  `.data/arrow-julia/src/logicaltypes.jl` as the no-metadata Arrow-owned
  helper entrypoint.
- Rebased the public `ArrowTypes.JuliaType` shims in
  `.data/arrow-julia/src/eltypes.jl` for:
  - `JuliaLang.ZonedDateTime-UTC`
  - `JuliaLang.ZonedDateTime`
    so they now delegate through the Arrow-owned helper instead of returning
    target types directly.
- Added direct runtime-contract regressions in
  `.data/arrow-julia/test/runtests.jl` for both helper and public-trait
  alias resolution.

## Verification

- `julia -e 'using JuliaFormatter; format([".data/arrow-julia/src/logicaltypes.jl",".data/arrow-julia/src/eltypes.jl",".data/arrow-julia/test/runtests.jl"], verbose=true)'`
- `direnv exec . julia --project=.data/arrow-julia/test -e 'using Pkg; Pkg.instantiate(); Pkg.test("Arrow")'` -> `Arrow | 66279/66279 | 3m28.2s`
- `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_extension_contract_bench.jl > .cache/agent/tmp/arrow-julia-extension-contract-bench-alias-shim.tsv`
- Key perf readouts:
  - `uuid_canonical write 0.491 ms / read_copy 33.364 ms`
  - `bool8 write 0.058 ms / read_copy 0.009 ms`
  - `json_text write 1.742 ms / read_copy 0.651 ms`
  - `timestamp_with_offset write 0.143 ms / read_copy 2.147 ms`

## Arrow.jl Local-Zoned Read Shim

- Continued the same Arrow-owned logical contract line on the last remaining
  built-in legacy read seam.
- Moved the `LocalZonedDateTime` legacy `fromarrow(::Timestamp)` conversion
  body into `.data/arrow-julia/src/logicaltypes_builtin.jl` via
  `_builtinfromarrow`.
- Kept the public `ArrowTypes.fromarrow(::Type{LocalZonedDateTime}, ...)`
  surface in `.data/arrow-julia/src/eltypes.jl` as a thin shim.
- Added a direct runtime-contract regression in
  `.data/arrow-julia/test/runtests.jl` asserting helper/public shim
  equivalence on a legacy local-zoned timestamp payload.

## Verification

- `julia -e 'using JuliaFormatter; format([".data/arrow-julia/src/logicaltypes_builtin.jl",".data/arrow-julia/src/eltypes.jl",".data/arrow-julia/test/runtests.jl"], verbose=true)'`
- `direnv exec . julia --project=.data/arrow-julia/test -e 'using Pkg; Pkg.instantiate(); Pkg.test("Arrow")'` -> `Arrow | 66280/66280 | 3m40.5s`
- `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_extension_contract_bench.jl > .cache/agent/tmp/arrow-julia-extension-contract-bench-localzdt-shim.tsv`
- Key perf readouts:
  - `uuid_canonical write 0.579 ms / read_copy 32.846 ms`
  - `bool8 write 0.072 ms / read_copy 0.013 ms`
  - `json_text write 1.892 ms / read_copy 0.634 ms`
  - `timestamp_with_offset write 0.146 ms / read_copy 2.278 ms`

## Arrow.jl Flight Logical Contract Coverage

- Extended the Arrow-owned logical contract line into Flight IPC roundtrip
  coverage.
- Broadened `.data/arrow-julia/test/flight/ipc_conversion.jl` so built-in
  logical types roundtrip through:
  - `Arrow.Flight.flightdata(...)`
  - `Arrow.Flight.stream(...)`
  - `Arrow.Flight.table(...)`
- Added direct Flight coverage for:
  - `arrow.uuid`
  - `arrow.bool8`
  - `arrow.json`
  - `arrow.timestamp_with_offset`
- Assertions now lock both extension metadata preservation and logical value
  roundtrip through the Flight IPC helpers.

## Verification

- `julia -e 'using JuliaFormatter; format([".data/arrow-julia/test/flight/ipc_conversion.jl"], verbose=true)'`
- `direnv exec . julia --project=.data/arrow-julia/test -e 'using Pkg; Pkg.instantiate(); Pkg.test("Arrow")'` -> `Arrow | 66280/66280 | 3m46.0s`
- `Flight IPC conversion helpers | 31/31`
- `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_extension_contract_bench.jl > .cache/agent/tmp/arrow-julia-extension-contract-bench-flight-contract.tsv`
- Key perf readouts:
  - `uuid_canonical write 0.567 ms / read_copy 33.458 ms`
  - `bool8 write 0.065 ms / read_copy 0.009 ms`
  - `json_text write 1.798 ms / read_copy 0.630 ms`
  - `timestamp_with_offset write 0.149 ms / read_copy 2.175 ms`

## PY-F87 Rerank Top-K Zero-Value Host Contract

- Added Python helper coverage proving typed rerank requests reject negative
  `top_k` values before Flight dispatch.
- Added raw-header smokes proving both:
  - `wendao_flight_server`
  - `wendao_search_flight_server`
    reject `x-wendao-rerank-top-k=0` with the live invalid-argument error path.

## PY-F88 Rerank Top-K Upper-Bound Contract

- Added runtime-owned rerank handler coverage proving `top_k` behaves as an
  upper bound rather than an exact row-count request.
- Added live host smokes proving both:
  - `wendao_flight_server`
  - `wendao_search_flight_server`
    preserve the full ranked result when `top_k` exceeds the available candidate
    count.

## PY-F89 Rerank Top-K Blank-Header Contract

- Added shared Rust parser coverage proving blank
  `x-wendao-rerank-top-k` is rejected as an invalid value.
- Added raw-header smokes proving both:
  - `wendao_flight_server`
  - `wendao_search_flight_server`
    reject blank `top_k` metadata on the live invalid-argument path.

## PY-F90 Rerank Top-K Exact-Bound Contract

- Added runtime-owned rerank handler coverage proving `top_k == result_count`
  preserves the full ranked result.
- Added live host smokes proving both:
  - `wendao_flight_server`
  - `wendao_search_flight_server`
    preserve all ranked rows when `top_k` exactly matches the available
    candidate count.

## PY-F91 Typed Rerank Top-K Metadata Omission Contract

- Added typed metadata coverage proving `rerank_request_metadata(...)` omits
  `x-wendao-rerank-top-k` entirely when `top_k` is not requested, instead of
  serializing an empty or placeholder value.

## PY-F92 Arrow Flight Support-Matrix Audit

- Added an explicit `xiuxian-wendao-py` Arrow Flight support matrix to the
  package README.
- Recorded the current implementation baseline in the successor RFC so the
  query-contract line stops drifting into already-closed `top_k` or
  repo-search transport hardening work.

## PY-F93 Python Package Positioning Clarification

- Clarified in package docs and the successor RFC that `xiuxian-wendao-py`
  remains a typed Python access layer for Rust-owned Wendao Flight contracts.
- Clarified that `rerank` support in this package means typed access to the
  Rust-owned `/rerank/flight` route, not Python-local ownership of rerank
  semantics or scoring runtime behavior.

## Phase-9 Stage-B Residual Graph Primitive Tail

- Landed the next bounded `Phase-9 Stage B` slice on the residual
  `xiuxian-daochang` test-consumer graph-primitive tail.
- Rebased these touched test files onto owner seams:
  - `packages/rust/crates/xiuxian-daochang/tests/agent/native_tools_zhixing.rs`
  - `packages/rust/crates/xiuxian-daochang/tests/agent/native_tools_zhixing_e2e.rs`
  - `packages/rust/crates/xiuxian-daochang/tests/agent/native_tools_web.rs`
- The touched scope now uses:
  - `xiuxian_wendao::entity::{Entity, EntityType}`
  - `xiuxian_wendao::graph::KnowledgeGraph`
- This keeps the rollout on the Wendao-plugin RFC mainline and avoids opening
  a broad `LinkGraphIndex` cut.

## Verification

- `direnv exec . rustfmt /Users/guangtao/projects/xiuxian-artisan-workshop/packages/rust/crates/xiuxian-daochang/tests/agent/native_tools_zhixing.rs /Users/guangtao/projects/xiuxian-artisan-workshop/packages/rust/crates/xiuxian-daochang/tests/agent/native_tools_zhixing_e2e.rs /Users/guangtao/projects/xiuxian-artisan-workshop/packages/rust/crates/xiuxian-daochang/tests/agent/native_tools_web.rs` -> passed
- `direnv exec . rg -n "use xiuxian_wendao::\\{[^\\n]*(Entity|EntityType)|use xiuxian_wendao::KnowledgeGraph" packages/rust/crates/xiuxian-daochang/tests/agent/native_tools_zhixing.rs packages/rust/crates/xiuxian-daochang/tests/agent/native_tools_zhixing_e2e.rs packages/rust/crates/xiuxian-daochang/tests/agent/native_tools_web.rs` -> no matches
- `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate --no-run` -> passed
- `direnv exec . cargo test -p xiuxian-daochang --test agent_suite --no-run --message-format short` -> still fails in deeper pre-existing `gateway/http`, `agent/injection`, `agent/native_tools/zhixing`, `agent/zhenfa/bridge`, Telegram ACL, and session-memory drift outside this slice

## Phase-9 Stage-B Frontmatter and URI Discovery Family

- Landed the next bounded `Phase-9 Stage B` consumer family in
  `xiuxian-qianji`.
- Rebased these touched consumers onto owner seams:
  - `packages/rust/crates/xiuxian-qianji/src/executors/annotation/persona_markdown.rs`
  - `packages/rust/crates/xiuxian-qianji/src/scheduler/preflight/query.rs`
- The touched scope now uses:
  - `xiuxian_wendao::enhancer::parse_frontmatter`
  - `xiuxian_wendao::skill_vfs::embedded_discover_canonical_uris`
- This keeps `Phase 9 Stage B` on small consumer families and avoids opening
  a broad `LinkGraphIndex` cut.

## Verification

- `direnv exec . rustfmt /Users/guangtao/projects/xiuxian-artisan-workshop/packages/rust/crates/xiuxian-qianji/src/executors/annotation/persona_markdown.rs /Users/guangtao/projects/xiuxian-artisan-workshop/packages/rust/crates/xiuxian-qianji/src/scheduler/preflight/query.rs` -> passed
- `direnv exec . rg -n "use xiuxian_wendao::(parse_frontmatter|embedded_discover_canonical_uris)\\b|use xiuxian_wendao::\\{[^\\n]*(parse_frontmatter|embedded_discover_canonical_uris)" packages/rust/crates/xiuxian-qianji packages/rust/crates/xiuxian-qianhuan packages/rust/crates/xiuxian-zhixing packages/rust/crates/xiuxian-daochang -g '*.rs'` -> no matches
- `direnv exec . cargo check -p xiuxian-qianji --lib` -> passed
- `direnv exec . cargo test -p xiuxian-qianji --tests --no-run` -> passed

## Phase-9 Stage-B Link-Graph Search Type Family

- Landed the next bounded `Phase-9 Stage B` consumer family in
  `xiuxian-zhixing`.
- Rebased the touched consumer onto the owner seam:
  - `packages/rust/crates/xiuxian-zhixing/src/heyi/agenda_render.rs`
- The touched scope now uses:
  - `xiuxian_wendao::link_graph::{LinkGraphHit, LinkGraphSearchOptions}`
- `LinkGraphIndex` was intentionally left untouched so this slice stays off a
  broad `LinkGraphIndex` migration.

## Verification

- `direnv exec . rustfmt /Users/guangtao/projects/xiuxian-artisan-workshop/packages/rust/crates/xiuxian-zhixing/src/heyi/agenda_render.rs` -> passed
- `direnv exec . rg -n "use xiuxian_wendao::(LinkGraphHit|LinkGraphSearchOptions)\\b|use xiuxian_wendao::\\{[^\\n]*(LinkGraphHit|LinkGraphSearchOptions)" packages/rust/crates/xiuxian-qianji packages/rust/crates/xiuxian-qianhuan packages/rust/crates/xiuxian-zhixing packages/rust/crates/xiuxian-daochang -g '*.rs'` -> no matches
- `direnv exec . cargo check -p xiuxian-zhixing --lib` -> passed
- `direnv exec . cargo test -p xiuxian-zhixing --tests --no-run` -> still fails in pre-existing `packages/rust/crates/xiuxian-zhixing/tests/test_storage_markdown.rs` crate-path drift, not in this owner-path cutover

## Phase-9 Stage-B Wendao Search Tool Family

- Landed the next bounded `Phase-9 Stage B` consumer family across sibling
  tests.
- Rebased these touched consumers onto the owner seam:
  - `packages/rust/crates/xiuxian-qianhuan/tests/test_zhenfa_native_tools.rs`
  - `packages/rust/crates/xiuxian-daochang/tests/scenario_adversarial_evolution.rs`
- The touched scope now uses:
  - `xiuxian_wendao::zhenfa_router::WendaoSearchTool`
- `LinkGraphIndex` was intentionally left untouched so this slice stays off a
  broad `LinkGraphIndex` migration.

## Verification

- `direnv exec . rustfmt /Users/guangtao/projects/xiuxian-artisan-workshop/packages/rust/crates/xiuxian-qianhuan/tests/test_zhenfa_native_tools.rs /Users/guangtao/projects/xiuxian-artisan-workshop/packages/rust/crates/xiuxian-daochang/tests/scenario_adversarial_evolution.rs` -> passed
- `direnv exec . rg -n "use xiuxian_wendao::\\{[^\\n]*WendaoSearchTool|use xiuxian_wendao::WendaoSearchTool\\b" packages/rust/crates/xiuxian-qianji packages/rust/crates/xiuxian-qianhuan packages/rust/crates/xiuxian-zhixing packages/rust/crates/xiuxian-daochang -g '*.rs'` -> no matches
- `direnv exec . cargo test -p xiuxian-qianhuan --test test_zhenfa_native_tools --features zhenfa-router --no-run` -> passed
- `direnv exec . cargo test -p xiuxian-daochang --test scenario_adversarial_evolution --no-run --message-format short` -> still fails in deeper pre-existing `gateway/http`, `agent/injection`, `agent/native_tools/zhixing`, `agent/zhenfa/bridge`, Telegram ACL, and session-memory drift outside this slice

## Phase-9 Stage-B Residual Resource VFS Test Tail

- Landed the next bounded `Phase-9 Stage B` slice on the residual
  `xiuxian-zhixing` resource/VFS test tail.
- Rebased these touched tests onto owner seams:
  - `packages/rust/crates/xiuxian-zhixing/tests/test_forge_skill_resources.rs`
  - `packages/rust/crates/xiuxian-zhixing/tests/test_wendao_skill_resources.rs`
- The touched scope now uses:
  - `xiuxian_wendao::enhancer::WendaoResourceRegistry`
  - `xiuxian_wendao::skill_vfs::{...}`
- This finishes another small Stage-B family without opening a broad
  `LinkGraphIndex` cut.

## Verification

- `direnv exec . rustfmt /Users/guangtao/projects/xiuxian-artisan-workshop/packages/rust/crates/xiuxian-zhixing/tests/test_forge_skill_resources.rs /Users/guangtao/projects/xiuxian-artisan-workshop/packages/rust/crates/xiuxian-zhixing/tests/test_wendao_skill_resources.rs` -> passed
- `direnv exec . rg -n "use xiuxian_wendao::\\{[^\\n]*(WendaoResourceRegistry|ZHIXING_SKILL_DOC_PATH|build_embedded_wendao_registry|embedded_resource_text|embedded_resource_text_from_wendao_uri|embedded_skill_links_for_id|embedded_skill_links_for_reference_type|embedded_skill_links_index|embedded_skill_markdown)|use xiuxian_wendao::(WendaoResourceRegistry|ZHIXING_SKILL_DOC_PATH|build_embedded_wendao_registry|embedded_resource_text|embedded_resource_text_from_wendao_uri|embedded_skill_links_for_id|embedded_skill_links_for_reference_type|embedded_skill_links_index|embedded_skill_markdown)\\b" packages/rust/crates/xiuxian-qianji packages/rust/crates/xiuxian-qianhuan packages/rust/crates/xiuxian-zhixing packages/rust/crates/xiuxian-daochang -g '*.rs'` -> no matches
- `direnv exec . cargo test -p xiuxian-zhixing --test test_forge_skill_resources --no-run` -> passed
- `direnv exec . cargo test -p xiuxian-zhixing --test test_wendao_skill_resources --no-run` -> passed

## Phase-9 Stage-B Test-Only LinkGraphIndex Leaf

- Landed the next bounded `Phase-9 Stage B` slice on a test-only
  `LinkGraphIndex` leaf.
- Rebased these touched native-search scenario consumers onto the owner seam:
  - `packages/rust/crates/xiuxian-qianhuan/tests/test_zhenfa_native_tools.rs`
  - `packages/rust/crates/xiuxian-daochang/tests/scenario_adversarial_evolution.rs`
- The touched scope now uses:
  - `xiuxian_wendao::link_graph::LinkGraphIndex`
- This was intentionally kept test-only and does not open a broad
  `LinkGraphIndex` migration across app/runtime surfaces.

## Verification

- `direnv exec . rustfmt /Users/guangtao/projects/xiuxian-artisan-workshop/packages/rust/crates/xiuxian-qianhuan/tests/test_zhenfa_native_tools.rs /Users/guangtao/projects/xiuxian-artisan-workshop/packages/rust/crates/xiuxian-daochang/tests/scenario_adversarial_evolution.rs` -> passed
- `direnv exec . rg -n "use xiuxian_wendao::LinkGraphIndex\\b|use xiuxian_wendao::\\{[^\\n]*LinkGraphIndex" packages/rust/crates/xiuxian-qianhuan/tests/test_zhenfa_native_tools.rs packages/rust/crates/xiuxian-daochang/tests/scenario_adversarial_evolution.rs` -> no matches
- `direnv exec . cargo test -p xiuxian-qianhuan --test test_zhenfa_native_tools --features zhenfa-router --no-run` -> passed
- `direnv exec . cargo test -p xiuxian-daochang --test scenario_adversarial_evolution --no-run --message-format short` -> still fails in deeper pre-existing `gateway/http`, `agent/injection`, `agent/native_tools/zhixing`, `agent/zhenfa/bridge`, Telegram ACL, and session-memory drift outside this slice

## Phase-9 Stage-B Residual Qianji LinkGraphIndex Leaf

- Landed the next bounded `Phase-9 Stage B` slice on a residual
  `xiuxian-qianji` integration-test `LinkGraphIndex` leaf.
- Rebased these touched files onto the owner seam:
  - `packages/rust/crates/xiuxian-qianji/tests/integration/test_qianji_qianhuan_binding.rs`
  - `packages/rust/crates/xiuxian-qianji/tests/integration/test_agenda_validation_pipeline.rs`
  - `packages/rust/crates/xiuxian-qianji/tests/integration/test_qianji_trinity_integration.rs`
- The touched scope now uses:
  - `xiuxian_wendao::link_graph::LinkGraphIndex`
- This is intentionally recorded as a touched leaf, not a family-complete
  `LinkGraphIndex` migration:
  - these files are not wired as active `[[test]]` targets in
    `packages/rust/crates/xiuxian-qianji/Cargo.toml`
  - residual root-qualified `LinkGraphIndex` imports still exist elsewhere in
    `xiuxian-qianji`

## Verification

- `direnv exec . rustfmt /Users/guangtao/projects/xiuxian-artisan-workshop/packages/rust/crates/xiuxian-qianji/tests/integration/test_qianji_qianhuan_binding.rs /Users/guangtao/projects/xiuxian-artisan-workshop/packages/rust/crates/xiuxian-qianji/tests/integration/test_agenda_validation_pipeline.rs /Users/guangtao/projects/xiuxian-artisan-workshop/packages/rust/crates/xiuxian-qianji/tests/integration/test_qianji_trinity_integration.rs` -> passed
- `direnv exec . rg -n "use xiuxian_wendao::LinkGraphIndex\\b|use xiuxian_wendao::\\{[^\\n]*LinkGraphIndex" /Users/guangtao/projects/xiuxian-artisan-workshop/packages/rust/crates/xiuxian-qianji/tests/integration/test_qianji_qianhuan_binding.rs /Users/guangtao/projects/xiuxian-artisan-workshop/packages/rust/crates/xiuxian-qianji/tests/integration/test_agenda_validation_pipeline.rs /Users/guangtao/projects/xiuxian-artisan-workshop/packages/rust/crates/xiuxian-qianji/tests/integration/test_qianji_trinity_integration.rs` -> no matches
- `direnv exec . cargo test -p xiuxian-qianji --tests --no-run` -> passed

## Phase-9 Stage-B Qianji App/Runtime Boot Surfaces

- Landed the next bounded `Phase-9 Stage B` slice on the
  `xiuxian-qianji` app/runtime boot surfaces.
- Rebased these touched files onto the owner seam:
  - `packages/rust/crates/xiuxian-qianji/src/app/qianji_app.rs`
  - `packages/rust/crates/xiuxian-qianji/src/app/build.rs`
  - `packages/rust/crates/xiuxian-qianji/src/bootcamp/model.rs`
  - `packages/rust/crates/xiuxian-qianji/src/bootcamp/runtime.rs`
  - `packages/rust/crates/xiuxian-qianji/src/bin/qianji.rs`
  - `packages/rust/crates/xiuxian-qianji/src/python_module/llm_bridge.rs`
- The touched scope now uses:
  - `xiuxian_wendao::link_graph::LinkGraphIndex`
- This keeps `Stage B` on a bounded source bundle and still does not open a
  broad `LinkGraphIndex` migration across app/runtime surfaces.

## Verification

- `direnv exec . rustfmt /Users/guangtao/projects/xiuxian-artisan-workshop/packages/rust/crates/xiuxian-qianji/src/app/qianji_app.rs /Users/guangtao/projects/xiuxian-artisan-workshop/packages/rust/crates/xiuxian-qianji/src/app/build.rs /Users/guangtao/projects/xiuxian-artisan-workshop/packages/rust/crates/xiuxian-qianji/src/bootcamp/model.rs /Users/guangtao/projects/xiuxian-artisan-workshop/packages/rust/crates/xiuxian-qianji/src/bootcamp/runtime.rs /Users/guangtao/projects/xiuxian-artisan-workshop/packages/rust/crates/xiuxian-qianji/src/bin/qianji.rs /Users/guangtao/projects/xiuxian-artisan-workshop/packages/rust/crates/xiuxian-qianji/src/python_module/llm_bridge.rs` -> passed
- `direnv exec . rg -n "use xiuxian_wendao::LinkGraphIndex\\b|use xiuxian_wendao::\\{[^\\n]*LinkGraphIndex" /Users/guangtao/projects/xiuxian-artisan-workshop/packages/rust/crates/xiuxian-qianji/src/app/qianji_app.rs /Users/guangtao/projects/xiuxian-artisan-workshop/packages/rust/crates/xiuxian-qianji/src/app/build.rs /Users/guangtao/projects/xiuxian-artisan-workshop/packages/rust/crates/xiuxian-qianji/src/bootcamp/model.rs /Users/guangtao/projects/xiuxian-artisan-workshop/packages/rust/crates/xiuxian-qianji/src/bootcamp/runtime.rs /Users/guangtao/projects/xiuxian-artisan-workshop/packages/rust/crates/xiuxian-qianji/src/bin/qianji.rs /Users/guangtao/projects/xiuxian-artisan-workshop/packages/rust/crates/xiuxian-qianji/src/python_module/llm_bridge.rs` -> no matches
- `direnv exec . cargo check -p xiuxian-qianji --lib` -> passed
- `direnv exec . cargo check -p xiuxian-qianji --bin qianji --features llm` -> passed
- `direnv exec . cargo check -p xiuxian-qianji --lib --features pyo3,llm` -> passed

## PY-F82 Runtime-Host Contract Status Table

- Added a contract-status table to the shared runtime host settings contract.
- Marked promoted knobs separately from implementation-only knobs.
- Made that table normative from the successor RFC so future runtime settings
  do not get treated as governed precedence by implication alone.

## PY-F83 Explicit Non-Promoted Host Inputs

- Explicitly marked `bind_addr`, `repo_id`, `project_root`/cwd, and
  `WENDAO_BOOTSTRAP_SAMPLE_REPO` as implementation-only inputs.
- Recorded the rule that host bring-up and fixture controls must stay out of
  governed precedence unless they acquire real shared runtime-policy meaning.

## PY-F84 Runtime-Host Promotion Closure

- Audited the remaining exposed host inputs after the shared host-settings
  helper consolidation.
- Recorded that there is no additional high-value promotion candidate beyond
  rerank weights, schema version, and rerank dimension.
- Marked the current runtime host-settings contract as provisionally closed
  until a new knob gains real shared runtime-policy semantics.

## PY-F85 Rerank Top-K Request Contract

- Added `x-wendao-rerank-top-k` as a stable rerank request header.
- Rewired the runtime-owned rerank handler to truncate scored outputs after
  ranking when `top_k` is present.
- Extended Python typed rerank helpers with `top_k=...` so callers do not need
  manual Flight metadata assembly.
- Proved the new behavior on both:
  - `wendao_flight_server`
  - `wendao_search_flight_server`

## PY-F86 Rerank Top-K Error Surface Contract

- Added Rust parser tests proving the live rerank route rejects:
  - zero `x-wendao-rerank-top-k`
  - non-numeric `x-wendao-rerank-top-k`
- Added raw `exchange_query_table(...)` Python smokes proving both:
  - `wendao_flight_server`
  - `wendao_search_flight_server`
    reject malformed `top_k` headers with the live invalid-argument error path.

## Arrow M6 Advanced Canonical Validation

- Landed the Milestone 6 advanced canonical validation slice in the nested
  `arrow-julia` repo.
- Added explicit helper metadata APIs for advanced canonical passthrough
  extensions:
  - `Arrow.variantmetadata()`
  - `Arrow.fixedshapetensormetadata(...)`
  - `Arrow.variableshapetensormetadata(...)`
- Tightened canonical read-side validation so:
  - `arrow.fixed_shape_tensor` validates canonical JSON metadata and
    `FixedSizeList` storage shape
  - `arrow.variable_shape_tensor` validates canonical JSON metadata and the
    required `Struct(data, shape)` storage contract
  - `arrow.parquet.variant` now explicitly rejects non-empty canonical
    metadata instead of passing malformed values through silently
- Kept all three advanced canonical extensions as storage passthrough types on
  the read path; this slice did not add higher-level semantic interpretation
  or automatic writer surfaces.
- Recorded the milestone in:
  - `/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/agent/execplans/arrow_julia_type_alignment_m6_advanced_validation.md`

## Verification

- `direnv exec . julia --project=.data/arrow-julia/src/ArrowTypes -e 'using Pkg; Pkg.test()'` -> `ArrowTypes | 190/190`
- `direnv exec . julia --project=.data/arrow-julia/test -e 'using Pkg; Pkg.instantiate(); Pkg.test("Arrow")'` -> `Arrow | 66212/66212 | 3m46.7s`
- `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_writepaths_bench.jl` -> reruns archived under:
  - `/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/agent/tmp/arrow-julia-m6-writepaths.tsv`
  - `/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/agent/tmp/arrow-julia-m6-writepaths-rerun2.tsv`
  - `/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/agent/tmp/arrow-julia-m6-writepaths-rerun3.tsv`
  - `/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/agent/tmp/arrow-julia-m6-writepaths-rerun4.tsv`
- `direnv exec . julia --project=.data/arrow-julia/test .cache/agent/benchmarks/arrow_julia_throughput_matrix.jl` -> `/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/agent/tmp/arrow-julia-m6-throughput-rerun.tsv`
- Same-machine M5 local comparison rerun archived at `/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/agent/tmp/arrow-julia-m5-writepaths-currentab-local.tsv`
- Perf note:
  - `offset_string`, `offset_nested_list`, and `offset_map` remained at or
    better than the same-machine M5 local rerun
  - `offset_binary` measured about `+10.6%` slower in one local microbench
    comparison, but the larger throughput rerun stayed near the accepted M5
    baseline (`100k binary 1.404 ms` vs `1.569 ms`, `500k binary 11.676 ms`
    vs `11.495 ms`)

## Arrow Next Contract Direction

- Locked the next production direction for `arrow-julia`: the package itself is
  the host for logical type and extension type registration plus contract.
- Recorded that this contract spans:
  - extension type mechanism
  - logical metadata encode/decode
  - read/write/IPC/Flight roundtrip
  - generic Julia type mapping
  - required `ArrowTypes` trait hooks
- Recorded the concrete trait surface in
  `/Users/guangtao/projects/xiuxian-artisan-workshop/.data/arrow-julia/src/ArrowTypes/src/ArrowTypes.jl`
  around:
  - `ArrowType`
  - `toarrow`
  - `arrowname`
  - `arrowmetadata`
  - `JuliaType`
  - `fromarrow`
- Created the next active ExecPlan:
  - `/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/agent/execplans/arrow_julia_logical_extension_contract.md`

## Arrow Contract Centralization Slice

- Landed the first concrete slice under the logical/extension contract plan.
- Added a new Arrow.jl-owned internal layer in:
  - `/Users/guangtao/projects/xiuxian-artisan-workshop/.data/arrow-julia/src/logicaltypes.jl`
- Centralized runtime contract touchpoints for:
  - extension metadata keys
  - write-side extension metadata emission
  - read-side extension metadata decoding and Julia type resolution
- Kept the public trait surface in
  `/Users/guangtao/projects/xiuxian-artisan-workshop/.data/arrow-julia/src/ArrowTypes/src/ArrowTypes.jl`,
  but made Arrow.jl itself the runtime host for the contract seam.
- Added a focused Flight IPC regression in
  `/Users/guangtao/projects/xiuxian-artisan-workshop/.data/arrow-julia/test/flight/ipc_conversion.jl`
  to prove extension metadata survives `Arrow.Flight.flightdata(...)` and
  `Arrow.Flight.table(...)`.

## Verification

- `direnv exec . julia --project=.data/arrow-julia/test -e 'using Pkg; Pkg.instantiate(); using Test, Arrow; include(".data/arrow-julia/test/flight/ipc_conversion.jl")'` -> `Flight IPC conversion helpers | 25/25`
- `direnv exec . julia --project=.data/arrow-julia/test -e 'using Pkg; Pkg.instantiate(); Pkg.test("Arrow")'` -> `Arrow | 66212/66212 | 3m55.3s`
- Serial write-path benchmark:
  - `/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/agent/tmp/arrow-julia-logical-contract-writepaths-final.tsv`
- Serial throughput benchmark:
  - `/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/agent/tmp/arrow-julia-logical-contract-throughput-final.tsv`
- Acceptance rerun for throughput noise:
  - `/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/agent/tmp/arrow-julia-logical-contract-throughput-rerun3.tsv`
- Same-machine HEAD baseline worktree artifacts:
  - `/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/agent/tmp/arrow-julia-contract-base-writepaths.tsv`
  - `/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/agent/tmp/arrow-julia-contract-base-throughput.tsv`
- Perf note:
  - the first parallel perf run was discarded
  - final serial writepaths landed near or better than the same-machine HEAD
    baseline for `offset_string`, `offset_binary`, `offset_nested_list`, and
    `offset_map`
  - throughput needed a second serial rerun to settle binary/map noise; the
    acceptance rerun brought `500k map` back near baseline and `500k binary`
    back to the accepted M6 band

## PY-F63 Repo-Search Best-Section Response Contract

- Extended the stable repo-search response contract with one read-side evidence
  column, `best_section`.
- Rewired both Rust Flight hosts to publish that column on the repo-search
  route, and rewired Python typed parsing so `WendaoRepoSearchResultRow`
  exposes it directly.
- Tightened repo-search tests so Python now rejects old 5-column tables and the
  live repo-search smokes assert snippet-bearing rows on both the runtime mock
  server and the real `xiuxian-wendao` host.

## Verification

- `direnv exec . env WENDAO_MOCK_SERVER_BINARY="$PRJ_ROOT/.cache/pyflight-rust-contract-target/debug/examples/mock_flight_exchange_server" WENDAO_SEARCH_SERVER_BINARY="$PRJ_ROOT/.cache/pyflight-f56-target/debug/wendao_search_flight_server" WENDAO_SEARCH_SEED_BINARY="$PRJ_ROOT/.cache/pyflight-f56-target/debug/wendao_search_seed_sample" uv run pytest /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/xiuxian-wendao-py/tests/test_query.py /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/xiuxian-wendao-py/tests/test_transport.py -q` -> `41 passed, 11 skipped`
- `direnv exec . env CARGO_TARGET_DIR="$PRJ_ROOT/.cache/pyflight-f56-target" cargo test -p xiuxian-wendao --features julia link_graph::plugin_runtime::transport::server::tests:: -- --nocapture` -> `14 passed`
- `direnv exec . git diff --check` -> passed

## PY-F65 Repo-Search Match-Reason Response Contract

- Extended the stable repo-search response contract with one backend-owned
  scalar field, `match_reason`.
- Rewired both Rust Flight hosts so repo-search rows now publish backend match
  provenance directly instead of forcing Python analyzers to infer it from
  tags or scores.
- Rewired Python typed parsing so `WendaoRepoSearchResultRow` now exposes
  `match_reason: str`.
- Tightened repo-search tests so:
  - stale 7-column tables are rejected
  - mock-host rows expose stable `repo_content_search`
  - real-host rows expose backend-owned provenance on the live
    `xiuxian-wendao` host

## Verification

- `direnv exec . uv run pytest packages/python/xiuxian-wendao-py/tests/test_query.py packages/python/xiuxian-wendao-py/tests/test_transport.py -q` -> `52 passed`
- `direnv exec . env CARGO_TARGET_DIR="$PRJ_ROOT/.cache/pyflight-f56-target" cargo test -p xiuxian-wendao --features julia link_graph::plugin_runtime::transport::server::tests:: -- --nocapture` -> `14 passed`

## PY-F66 Repo-Search Navigation Response Contract

- Extended the stable repo-search response contract with backend-owned
  navigation evidence fields:
  - `navigation_path`
  - `navigation_category`
  - `navigation_line`
  - `navigation_line_end`
- Rewired both Rust Flight hosts so repo-search rows now expose a stable
  editor landing point instead of forcing Python analyzers to reconstruct jump
  targets from `path` plus snippet text.
- Rewired Python typed parsing so `WendaoRepoSearchResultRow` now exposes the
  same navigation fields directly.
- Tightened repo-search tests so:
  - stale 8-column tables are rejected
  - mock-host rows expose stable `repo_code` navigation data
  - real-host rows expose backend-owned file/line landing points on the live
    `xiuxian-wendao` host
- Fixed the real-host test helper so it resolves the current
  `wendao_search_flight_server` from `.cache/pyflight-f56-target` instead of
  a stale target dir.

## Verification

- `direnv exec . uv run pytest packages/python/xiuxian-wendao-py/tests/test_query.py packages/python/xiuxian-wendao-py/tests/test_transport.py -q` -> `52 passed`

## PY-F64 Repo-Search Tags Response Contract

- Extended the stable repo-search response contract with one backend-owned list
  field, `tags`.
- Rewired both Rust Flight hosts so repo-search responses now publish a real
  `List<Utf8>` Arrow column instead of forcing Python consumers to reconstruct
  tag semantics from filters.
- Rewired Python typed parsing so `WendaoRepoSearchResultRow` now exposes
  `tags: tuple[str, ...]`.
- Tightened repo-search tests so:
  - stale 6-column tables are rejected
  - mock-host rows expose stable `code/file/lang:*` tags
  - real-host rows expose backend semantics such as `match:exact` and language
    tags on the live `xiuxian-wendao` host

## Verification

- `direnv exec . env WENDAO_MOCK_SERVER_BINARY="$PRJ_ROOT/.cache/pyflight-rust-contract-target/debug/examples/mock_flight_exchange_server" WENDAO_SEARCH_SERVER_BINARY="$PRJ_ROOT/.cache/pyflight-f56-target/debug/wendao_search_flight_server" WENDAO_SEARCH_SEED_BINARY="$PRJ_ROOT/.cache/pyflight-f56-target/debug/wendao_search_seed_sample" uv run pytest /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/xiuxian-wendao-py/tests/test_query.py /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/xiuxian-wendao-py/tests/test_transport.py -q` -> `41 passed, 11 skipped`
- `direnv exec . env CARGO_TARGET_DIR="$PRJ_ROOT/.cache/pyflight-f56-target" cargo test -p xiuxian-wendao --features julia link_graph::plugin_runtime::transport::server::tests:: -- --nocapture` -> `14 passed`
- `direnv exec . git diff --check` -> passed

## PY-F62 Real-Host Exact-Match Score Preference

- Tightened the mixed exact-vs-folded repo-search fixture so the same query can
  return both rows under one typed Flight request.
- Tightened provider and real-host coverage so the repo-search `score` column
  now has an explicit semantic assertion:
  - exact-case row first
  - exact-case row score greater than folded-only row score
- The real host now proves:
  - `repo_search_request("CamelBridgeToken", limit=2)`
  - returns `docs/CamelBridge.md` before `src/camelbridge.rs`
  - and `score[0] > score[1]`

## Verification

- `direnv exec . env CARGO_TARGET_DIR=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-f56-target cargo test -p xiuxian-wendao --features julia link_graph::plugin_runtime::transport::server::tests:: -- --nocapture` -> `14 passed`
- `direnv exec . env CARGO_TARGET_DIR=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-f56-target cargo build -p xiuxian-wendao --features julia --bin wendao_search_flight_server --bin wendao_search_seed_sample` -> passed
- `direnv exec . env WENDAO_SEARCH_SERVER_BINARY=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-f56-target/debug/wendao_search_flight_server WENDAO_SEARCH_SEED_BINARY=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/pyflight-f56-target/debug/wendao_search_seed_sample uv run pytest /Users/guangtao/projects/xiuxian-artisan-workshop/packages/python/xiuxian-wendao-py/tests/test_transport.py -q -k wendao_search_flight_server` -> `4 passed`
