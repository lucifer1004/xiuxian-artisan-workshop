# Daily GTD Log - 2026-03-26

### [DONE] Feature: WendaoAnalyzer first standalone package

- **Objective**: move from transport-only Julia examples to the first reusable
  analyzer package on top of the completed WendaoArrow `v1` contract.
- **Update**:
  - created `.data/WendaoAnalyzer` as a standalone Julia package with
    `Project.toml`, `src/`, `test/`, `examples/`, `scripts/`, and docs
  - added `LinearBlendAnalyzer` plus `analyze_table(...)`,
    `analyze_stream(...)`, `build_table_processor(...)`, and
    `build_stream_processor(...)`
  - kept the package analyzer-only by removing the external `Tables.jl`
    dependency and using a local columnar adapter over property access
  - added runnable table-first and stream-first analyzer servers that locally
    develop `.data/WendaoArrow` into the example environment
  - updated `.data/WendaoArrow/README.md` and the Arrow contract doc to point
    at the new analyzer package boundary
- **Validation Executed**:
  - `direnv exec . ls -la .data/WendaoAnalyzer .data/WendaoAnalyzer/src .data/WendaoAnalyzer/test .data/WendaoAnalyzer/examples .data/WendaoAnalyzer/scripts`
  - `direnv exec . julia --project=.data/WendaoAnalyzer -e 'using WendaoAnalyzer; println("wendaoanalyzer-load-ok")'`
  - `direnv exec . julia --project=.data/WendaoAnalyzer -e 'using Pkg; Pkg.test()'`

### [DONE] Feature: WendaoAnalyzer live HTTP Arrow IPC e2e

- **Objective**: close the remaining gap between the new standalone analyzer
  package and the Rust transport harness by validating a real analyzer-owned
  HTTP Arrow IPC server.
- **Update**:
  - extended `xiuxian-wendao-julia` official-example test support so it can
    resolve and boot `.data/WendaoAnalyzer` scripts in addition to
    `.data/WendaoArrow` scripts
  - added a live transport test for
    `run_stream_linear_blend_server.sh` that verifies real `doc_id`,
    `analyzer_score`, `final_score`, and additive `ranking_reason` values
  - changed the analyzer example servers to load local `WendaoArrow` through
    `LOAD_PATH` instead of `Pkg.develop(...)`, removing runtime package-manager
    mutation and making startup fast enough for test harness health checks
  - updated `.data/WendaoAnalyzer/README.md` to record the new live e2e surface
- **Validation Executed**:
  - `direnv exec . cargo test -p xiuxian-wendao-julia real_wendaoanalyzer_linear_blend_roundtrip_emits_expected_scores --lib`
  - `direnv exec . cargo test -p xiuxian-wendao-julia transport --lib`
  - `direnv exec . julia --project=.data/WendaoAnalyzer -e 'using Pkg; Pkg.test()'`
  - `direnv exec . git diff --check`

### [DONE] Feature: WendaoAnalyzer main planned-search integration

- **Objective**: push the new analyzer package from Rust transport coverage into
  the `xiuxian-wendao` planned-search Julia rerank integration surface.
- **Update**:
  - extended `xiuxian-wendao` integration support so test harnesses can resolve
    and boot `.data/WendaoAnalyzer` scripts in addition to `.data/WendaoArrow`
    scripts
  - added
    `packages/rust/crates/xiuxian-wendao/tests/integration/planned_search_wendaoanalyzer_linear_blend.rs`
    and wired it into `xiuxian-testing-gate.rs`
  - validated that planned-search vector-store Julia rerank can target the
    analyzer-owned `run_stream_linear_blend_server.sh` surface and still apply
    remote rerank results successfully
  - updated `packages/rust/crates/xiuxian-wendao/README.md` and
    `.data/WendaoAnalyzer/README.md` to record the new main-crate integration
- **Validation Executed**:
  - `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate test_planned_search_payload_applies_wendaoanalyzer_linear_blend_example`
  - `direnv exec . cargo check -p xiuxian-wendao --tests`

### [DONE] Feature: WendaoAnalyzer strategy surface

- **Objective**: evolve `WendaoAnalyzer` from one fixed demo analyzer into a
  small, typed, configurable strategy surface without changing the WendaoArrow
  contract.
- **Update**:
  - added `AbstractAnalyzer`, `AnalyzerConfig`, and `build_analyzer(...)`
  - kept `LinearBlendAnalyzer` as the default backward-compatible strategy
  - added built-in `SimilarityOnlyAnalyzer` and `VectorScoreAnalyzer`
  - updated scoring and processor builders to work against the typed analyzer
    surface while preserving the existing `doc_id / analyzer_score /
final_score / ranking_reason` response shape
  - expanded `.data/WendaoAnalyzer/test/runtests.jl` with strategy selection and
    alternate-strategy contract-shape coverage
  - updated `.data/WendaoAnalyzer/README.md` and package spec to document the
    new strategy surface
- **Validation Executed**:
  - `direnv exec . julia --project=.data/WendaoAnalyzer -e 'using Pkg; Pkg.test()'`
  - `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate test_planned_search_payload_applies_wendaoanalyzer_linear_blend_example`
  - `direnv exec . git diff --check`

### [DONE] Feature: WendaoAnalyzer runtime strategy selection

- **Objective**: push the new typed strategy surface into the runnable analyzer
  example layer so operators can choose strategy by flags or TOML without
  changing the WendaoArrow contract.
- **Update**:
  - added `AnalyzerRuntimeConfig`, `split_runtime_args(...)`,
    `analyzer_runtime_from_args(...)`, and `load_analyzer_runtime_config(...)`
  - kept analyzer-only arguments local to `WendaoAnalyzer`, while forwarding the
    remaining flags to `WendaoArrow.config_from_args(...)`
  - updated the example servers so they now accept analyzer runtime selection
    through `--analyzer-strategy`, `--vector-weight`,
    `--similarity-weight`, and `--analyzer-config`
  - added `.data/WendaoAnalyzer/config/analyzer.example.toml`
  - expanded package tests with runtime-arg splitting and TOML config loading
  - updated `README.md` and package spec to document the runtime selection layer
- **Validation Executed**:
  - `direnv exec . julia --project=.data/WendaoAnalyzer -e 'using Pkg; Pkg.test()'`
  - `direnv exec . cargo test -p xiuxian-wendao-julia real_wendaoanalyzer_linear_blend_roundtrip_emits_expected_scores --lib`
  - `direnv exec . git diff --check`

### [DONE] Feature: WendaoAnalyzer service contract consolidation

- **Objective**: turn the current analyzer-owned launch seam into one explicit
  service contract shared by Julia example entrypoints, Rust integration
  helpers, and package documentation.
- **Update**:
  - added `AnalyzerServiceContract` and
    `analyzer_service_contract_from_args(...)` to `.data/WendaoAnalyzer`
  - rewired the table and stream example servers to consume the shared Julia
    service-contract parser instead of duplicating arg splitting inline
  - renamed the Rust integration helper surface to
    `WendaoAnalyzerServiceContract`, added `analyzer_config_path`, and allowed
    passthrough `wendaoarrow_args`
  - documented the two-part contract explicitly: analyzer-owned flags stay in
    `WendaoAnalyzer`, while transport flags continue to belong to
    `WendaoArrow`
- **Validation Executed**:
  - `direnv exec . julia --project=.data/WendaoAnalyzer -e 'using Pkg; Pkg.test()'`
  - `direnv exec . cargo test -p xiuxian-wendao test_retrieval_runtime_resolves_julia_rerank_config --lib`
  - `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate test_planned_search_payload_applies_wendaoanalyzer_similarity_only_strategy`
  - `direnv exec . cargo check -p xiuxian-wendao --tests`
  - `direnv exec . git diff --check`

### [DONE] Feature: WendaoAnalyzer strategy selection from Rust runtime config

- **Objective**: let the Rust Julia rerank config surface express analyzer-owned
  strategy preferences without changing the WendaoArrow transport contract.
- **Update**:
  - extended `LinkGraphJuliaRerankRuntimeConfig` with additive
    `analyzer_strategy`, `vector_weight`, and `similarity_weight` fields
  - extended the runtime resolver and unit test coverage so
    `link_graph.retrieval.julia_rerank.*` now parses those fields from config
  - extended the analyzer-owned integration support harness so tests can launch
    the analyzer server with strategy-specific flags
  - added
    `planned_search_wendaoanalyzer_similarity_only.rs` to prove a non-default
    `similarity_only` strategy can be expressed through Rust-owned config and
    reach the main planned-search rerank path
  - updated Rust and Julia package docs to record the new config seam
- **Validation Executed**:
  - `direnv exec . cargo test -p xiuxian-wendao test_retrieval_runtime_resolves_julia_rerank_config --lib`
  - `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate test_planned_search_payload_applies_wendaoanalyzer_similarity_only_strategy`
  - `direnv exec . cargo check -p xiuxian-wendao --tests`
  - `direnv exec . git diff --check`

### [DONE] Feature: WendaoArrow contract column constants

- **Objective**: stop repeating WendaoArrow request/response column-name
  literals across `xiuxian-wendao` and `xiuxian-wendao-julia`.
- **Update**:
  - added canonical Julia contract column constants to
    `packages/rust/crates/xiuxian-wendao/src/analyzers/service/julia_transport.rs`
  - re-exported those constants from `analyzers::service` and
    `analyzers` so downstream crates can import the contract surface without
    enabling the `julia` feature
  - rewired `xiuxian-wendao-julia` response validation and metadata loopback
    fixtures to consume the shared constants instead of repeating
    `doc_id` / `analyzer_score` / `final_score` / `vector_score`
  - updated `packages/rust/crates/xiuxian-wendao/README.md` to record the new
    contract surface
- **Validation Executed**:
  - `direnv exec . cargo check -p xiuxian-wendao --lib`
  - `direnv exec . cargo test -p xiuxian-wendao-julia real_wendaoarrow_metadata_example_roundtrip_decodes_trace_id_column --lib`

### [DONE] Feature: Arrow transport metadata helper extraction

- **Objective**: stop rebuilding Arrow schemas by hand whenever a caller needs
  to attach request metadata before a WendaoArrow transport roundtrip.
- **Update**:
  - added `attach_record_batch_metadata(...)` to
    `packages/rust/crates/xiuxian-vector/src/arrow_transport/codec.rs` and
    re-exported it from the crate root
  - added canonical metadata key constants plus
    `attach_record_batch_trace_id(...)` so callers no longer need to repeat the
    `trace_id` / `wendao.schema_version` strings
  - rewired the `xiuxian-vector` transport tests to use the helper for
    metadata-bearing sample batches
  - rewired `xiuxian-wendao` Julia rerank request metadata attachment and the
    `xiuxian-wendao-julia` metadata loopback fixture to consume the shared
    helper instead of open-coding `Schema::new_with_metadata(...)`
  - documented the helper in `packages/rust/crates/xiuxian-vector/README.md`
- **Validation Executed**:
  - `direnv exec . cargo test -p xiuxian-vector attach_record_batch_metadata_merges_existing_entries --lib`
  - `direnv exec . cargo test -p xiuxian-vector transport_client_preserves_request_schema_metadata --lib`
  - `direnv exec . cargo test -p xiuxian-wendao-julia real_wendaoarrow_metadata_example_roundtrip_decodes_trace_id_column --lib`
  - `direnv exec . cargo check -p xiuxian-wendao --lib`

### [DONE] Feature: Frontend API client UI config transport split

- **Objective**: reduce the size and concern load of `.data/wendao-frontend/src/api/client.ts`
  by extracting the stateful UI config sync/retry and capabilities cache
  surface into one dedicated helper module.
- **Changes**:
  - added `.data/wendao-frontend/src/api/uiConfigTransport.ts`
  - added `.data/wendao-frontend/src/api/uiConfigTransport.test.ts`
  - rewired `.data/wendao-frontend/src/api/client.ts`
    so UI config sync/retry and capabilities cache now consume the extracted
    stateful helper surface
- **Validation**:
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm test -- src/api/uiConfigTransport.test.ts src/api/client.test.ts src/components/SearchBar/__tests__/searchExecution.test.ts src/components/SearchBar/__tests__/SearchBar.test.tsx'`
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm run build'`

### [DONE] Feature: Frontend API client response transport split

- **Objective**: reduce the size and concern load of `.data/wendao-frontend/src/api/client.ts`
  by extracting the shared response contract surface (`ApiClientError`, JSON
  response parsing, and binary response parsing) into one dedicated helper
  module.
- **Changes**:
  - added `.data/wendao-frontend/src/api/responseTransport.ts`
  - added `.data/wendao-frontend/src/api/responseTransport.test.ts`
  - rewired `.data/wendao-frontend/src/api/client.ts`
    so shared response handling now consumes the extracted helper surface
- **Validation**:
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm test -- src/api/responseTransport.test.ts src/api/client.test.ts src/components/SearchBar/__tests__/searchExecution.test.ts src/components/SearchBar/__tests__/SearchBar.test.tsx'`
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm run build'`

### [DONE] Feature: Frontend API client workspace transport split

- **Objective**: reduce the size and concern load of `.data/wendao-frontend/src/api/client.ts`
  by extracting the foundational health, VFS, and graph gateway fetch surface
  into one dedicated transport module.
- **Changes**:
  - added `.data/wendao-frontend/src/api/workspaceTransport.ts`
  - added `.data/wendao-frontend/src/api/workspaceTransport.test.ts`
  - rewired `.data/wendao-frontend/src/api/client.ts`
    so the health, VFS, and graph API methods now consume the extracted
    workspace transport helper
- **Validation**:
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm test -- src/api/workspaceTransport.test.ts src/api/client.test.ts src/components/SearchBar/__tests__/searchExecution.test.ts src/components/SearchBar/__tests__/SearchBar.test.tsx'`
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm run build'`

### [DONE] Feature: Frontend API client document transport split

- **Objective**: reduce the size and concern load of `.data/wendao-frontend/src/api/client.ts`
  by extracting the remaining autocomplete, projected-page-index, and
  refine-doc gateway fetch surface into one dedicated transport module.
- **Changes**:
  - added `.data/wendao-frontend/src/api/documentTransport.ts`
  - added `.data/wendao-frontend/src/api/documentTransport.test.ts`
  - rewired `.data/wendao-frontend/src/api/client.ts`
    so autocomplete, projected-page-index, and refine-doc API methods now
    consume the extracted document transport helper
- **Validation**:
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm test -- src/api/documentTransport.test.ts src/api/client.test.ts src/components/SearchBar/__tests__/searchExecution.test.ts src/components/SearchBar/__tests__/SearchBar.test.tsx'`
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm run build'`

### [DONE] Feature: Frontend API client contract surface split

- **Objective**: reduce the size and concern load of `.data/wendao-frontend/src/api/client.ts`
  by extracting the remaining public repo/UI contract interfaces into one
  dedicated module.
- **Changes**:
  - added `.data/wendao-frontend/src/api/apiContracts.ts`
  - added `.data/wendao-frontend/src/api/apiContracts.test.ts`
  - rewired `.data/wendao-frontend/src/api/client.ts`
    so repo/UI public contracts now consume the extracted contract module
- **Validation**:
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm test -- src/api/apiContracts.test.ts src/api/client.test.ts src/components/SearchBar/__tests__/searchExecution.test.ts src/components/SearchBar/__tests__/SearchBar.test.tsx'`
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm run build'`

### [DONE] Feature: Frontend API client facade/runtime split

- **Objective**: reduce the size and concern load of `.data/wendao-frontend/src/api/client.ts`
  by turning it into a pure public facade and moving runtime wiring into a
  dedicated module.
- **Changes**:
  - added `.data/wendao-frontend/src/api/clientRuntime.ts`
  - added `.data/wendao-frontend/src/api/clientFacade.test.ts`
  - rewired `.data/wendao-frontend/src/api/client.ts`
    so it now exposes the public facade only and delegates runtime wiring to
    the extracted runtime module
- **Validation**:
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm test -- src/api/clientFacade.test.ts src/api/client.test.ts src/components/SearchBar/__tests__/searchExecution.test.ts src/components/SearchBar/__tests__/SearchBar.test.tsx'`
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm run build'`

### [DONE] Feature: Frontend API client internal type cutover

- **Objective**: reduce internal coupling in `.data/wendao-frontend/src/api/`
  by removing helper-module type backreferences to the public facade.
- **Changes**:
  - rewired `.data/wendao-frontend/src/api/uiConfigTransport.ts`
    to consume `UiCapabilities` from `.data/wendao-frontend/src/api/apiContracts.ts`
  - rewired `.data/wendao-frontend/src/api/repoResponseNormalizers.ts`
    to consume repo contract types from `.data/wendao-frontend/src/api/apiContracts.ts`
  - extended `.data/wendao-frontend/src/api/apiContracts.test.ts`
    with explicit backlink-contract coverage
- **Validation**:
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm test -- src/api/apiContracts.test.ts src/api/uiConfigTransport.test.ts src/api/repoResponseNormalizers.test.ts src/api/clientFacade.test.ts src/api/client.test.ts src/components/SearchBar/__tests__/searchExecution.test.ts src/components/SearchBar/__tests__/SearchBar.test.tsx'`
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm run build'`

### [DONE] Feature: Frontend API index barrel expansion

- **Objective**: promote `.data/wendao-frontend/src/api/index.ts` into the
  stable public API barrel instead of leaving it as a trivial client forwarder.
- **Changes**:
  - expanded `.data/wendao-frontend/src/api/index.ts`
    so it now re-exports the facade plus the contract/error surface
  - rewired `.data/wendao-frontend/src/api/client.test.ts`
    to consume the barrel directly
  - rewired `.data/wendao-frontend/src/api/clientFacade.test.ts`
    to consume the barrel directly
- **Validation**:
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm test -- src/api/clientFacade.test.ts src/api/client.test.ts src/components/SearchBar/__tests__/searchExecution.test.ts src/components/SearchBar/__tests__/SearchBar.test.tsx'`
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm run build'`

### [DONE] Feature: Frontend SearchBar controller presentation split

- **Objective**: reduce the concern load of
  `.data/wendao-frontend/src/components/SearchBar/useSearchBarController.tsx`
  by extracting data-flow, interaction, and view-model assembly into one
  dedicated presentation hook.
- **Changes**:
  - added `.data/wendao-frontend/src/components/SearchBar/useSearchBarControllerPresentation.ts`
  - rewired `.data/wendao-frontend/src/components/SearchBar/useSearchBarController.tsx`
    so it now focuses on controller state hooks, repo-slice wiring, reset
    behavior, and final result assembly
  - updated `.data/wendao-frontend/src/components/SearchBar/__tests__/useSearchBarController.test.tsx`
    so the controller regression now reflects the current module graph
- **Validation**:
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm test -- src/components/SearchBar/__tests__/useSearchBarController.test.tsx src/components/SearchBar/__tests__/searchExecution.test.ts src/components/SearchBar/__tests__/SearchBar.test.tsx'`
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm run build'`

### [DONE] Feature: Frontend API client analysis transport split

- **Objective**: reduce the size and concern load of `.data/wendao-frontend/src/api/client.ts`
  by extracting markdown/code-ast analysis fetch path composition into one
  dedicated transport module.
- **Changes**:
  - added `.data/wendao-frontend/src/api/analysisTransport.ts`
  - added `.data/wendao-frontend/src/api/analysisTransport.test.ts`
  - updated `.data/wendao-frontend/src/api/client.test.ts`
    with explicit markdown analysis endpoint coverage
  - rewired `.data/wendao-frontend/src/api/client.ts`
    so analysis API methods now consume the extracted analysis transport helper
- **Validation**:
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm test -- src/api/analysisTransport.test.ts src/api/client.test.ts src/components/SearchBar/__tests__/searchExecution.test.ts src/components/SearchBar/__tests__/SearchBar.test.tsx'`
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm run build'`

### [DONE] Feature: Frontend API client repo transport split

- **Objective**: reduce the size and concern load of `.data/wendao-frontend/src/api/client.ts`
  by extracting the repeated repo endpoint fetch wrapper seam into one dedicated
  transport module.
- **Changes**:
  - added `.data/wendao-frontend/src/api/repoTransport.ts`
  - added `.data/wendao-frontend/src/api/repoTransport.test.ts`
  - rewired `.data/wendao-frontend/src/api/client.ts`
    so repo API methods now consume the extracted repo transport helper
- **Validation**:
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm test -- src/api/repoTransport.test.ts src/api/repoResponseNormalizers.test.ts src/api/client.test.ts src/components/SearchBar/__tests__/searchExecution.test.ts src/components/SearchBar/__tests__/SearchBar.test.tsx'`
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm run build'`

### [DONE] Feature: Frontend API client repo normalizer split

- **Objective**: reduce the size and concern load of `.data/wendao-frontend/src/api/client.ts`
  by extracting the repo-search wire-type and response-normalizer surface into
  one dedicated module.
- **Changes**:
  - added `.data/wendao-frontend/src/api/repoResponseNormalizers.ts`
  - added `.data/wendao-frontend/src/api/repoResponseNormalizers.test.ts`
  - rewired `.data/wendao-frontend/src/api/client.ts`
    so repo API methods now consume the extracted normalizer module
- **Validation**:
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm test -- src/api/repoResponseNormalizers.test.ts src/api/client.test.ts src/components/SearchBar/__tests__/searchExecution.test.ts src/components/SearchBar/__tests__/SearchBar.test.tsx'`
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm run build'`

### [DONE] Feature: Frontend SearchBar code-mode helper split

- **Objective**: reduce the concern load of
  `.data/wendao-frontend/src/components/SearchBar/searchExecutionCodeMode.ts`
  by extracting repo-aware settled-result resolution, fallback selection, and
  standalone code-search shaping into one dedicated helper module.
- **Changes**:
  - added `.data/wendao-frontend/src/components/SearchBar/searchExecutionCodeModeHelpers.ts`
  - added `.data/wendao-frontend/src/components/SearchBar/__tests__/searchExecutionCodeModeHelpers.test.ts`
  - rewired `.data/wendao-frontend/src/components/SearchBar/searchExecutionCodeMode.ts`
    so it now keeps only repo fan-out orchestration and route selection
- **Validation**:
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm test -- src/components/SearchBar/__tests__/searchExecutionCodeModeHelpers.test.ts src/components/SearchBar/__tests__/searchExecutionCodeMode.test.ts src/components/SearchBar/__tests__/searchExecutionAllModeHelpers.test.ts src/components/SearchBar/__tests__/searchExecutionAllMode.test.ts src/components/SearchBar/__tests__/searchExecutionSimpleModes.test.ts src/components/SearchBar/__tests__/searchExecution.test.ts src/components/SearchBar/__tests__/SearchBar.test.tsx'`
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm run build'`

### [DONE] Feature: Frontend SearchBar all-mode helper split

- **Objective**: reduce the concern load of
  `.data/wendao-frontend/src/components/SearchBar/searchExecutionAllMode.ts`
  by extracting fallback response builders and merged result/meta shaping into
  one dedicated helper module.
- **Changes**:
  - added `.data/wendao-frontend/src/components/SearchBar/searchExecutionAllModeHelpers.ts`
  - added `.data/wendao-frontend/src/components/SearchBar/__tests__/searchExecutionAllModeHelpers.test.ts`
  - rewired `.data/wendao-frontend/src/components/SearchBar/searchExecutionAllMode.ts`
    so it now keeps only fan-out orchestration and settled-lane resolution
- **Validation**:
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm test -- src/components/SearchBar/__tests__/searchExecutionAllModeHelpers.test.ts src/components/SearchBar/__tests__/searchExecutionAllMode.test.ts src/components/SearchBar/__tests__/searchExecutionSimpleModes.test.ts src/components/SearchBar/__tests__/searchExecutionCodeMode.test.ts src/components/SearchBar/__tests__/searchExecution.test.ts src/components/SearchBar/__tests__/SearchBar.test.tsx'`
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm run build'`

### [DONE] Feature: Frontend SearchBar execution contract split

- **Objective**: remove the remaining shared execution contract from
  `.data/wendao-frontend/src/components/SearchBar/searchExecution.ts`
  so helper modules depend on a dedicated type surface instead of the main
  router file.
- **Changes**:
  - added `.data/wendao-frontend/src/components/SearchBar/searchExecutionTypes.ts`
  - rewired `.data/wendao-frontend/src/components/SearchBar/searchExecution.ts`
  - rewired `searchExecutionAllMode.ts`, `searchExecutionCodeMode.ts`, and
    `searchExecutionSimpleModes.ts` to import `SearchExecutionOutcome` from the
    dedicated contract file
- **Validation**:
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm test -- src/components/SearchBar/__tests__/searchExecutionSimpleModes.test.ts src/components/SearchBar/__tests__/searchExecutionCodeMode.test.ts src/components/SearchBar/__tests__/searchExecutionAllMode.test.ts src/components/SearchBar/__tests__/searchExecution.test.ts src/components/SearchBar/__tests__/SearchBar.test.tsx'`
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm run build'`

### [DONE] Feature: Frontend SearchBar simple-mode execution split

- **Objective**: reduce the concern load of `.data/wendao-frontend/src/components/SearchBar/searchExecution.ts`
  by extracting the remaining reference/attachment/AST/symbol/knowledge
  responders into one dedicated simple-mode module.
- **Changes**:
  - added `.data/wendao-frontend/src/components/SearchBar/searchExecutionSimpleModes.ts`
  - added `.data/wendao-frontend/src/components/SearchBar/__tests__/searchExecutionSimpleModes.test.ts`
  - rewired `.data/wendao-frontend/src/components/SearchBar/searchExecution.ts`
    so only `all` and `code` stay special-cased inline and all other modes
    flow through the extracted simple-mode execution seam
- **Validation**:
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm test -- src/components/SearchBar/__tests__/searchExecutionSimpleModes.test.ts src/components/SearchBar/__tests__/searchExecutionCodeMode.test.ts src/components/SearchBar/__tests__/searchExecutionAllMode.test.ts src/components/SearchBar/__tests__/searchExecution.test.ts src/components/SearchBar/__tests__/SearchBar.test.tsx'`
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm run build'`

### [DONE] Feature: Frontend SearchBar code-mode execution split

- **Objective**: reduce the concern load of `.data/wendao-frontend/src/components/SearchBar/searchExecution.ts`
  by extracting the repo-aware code-mode branch into one dedicated module.
- **Update**:
  - added `.data/wendao-frontend/src/components/SearchBar/searchExecutionCodeMode.ts`
  - added `.data/wendao-frontend/src/components/SearchBar/__tests__/searchExecutionCodeMode.test.ts`
  - rewired `.data/wendao-frontend/src/components/SearchBar/searchExecution.ts`
    to delegate `mode === "code"` to the extracted helper
  - updated `.data/wendao-frontend/README.md` and the active ExecPlan to record
    the extracted code-mode execution seam
- **Validation Executed**:
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm test -- src/components/SearchBar/__tests__/searchExecutionCodeMode.test.ts src/components/SearchBar/__tests__/searchExecutionAllMode.test.ts src/components/SearchBar/__tests__/searchExecution.test.ts src/components/SearchBar/__tests__/SearchBar.test.tsx'`
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm run build'`

### [DONE] Feature: Frontend SearchBar all-mode execution split

- **Objective**: reduce the concern load of `.data/wendao-frontend/src/components/SearchBar/searchExecution.ts`
  by extracting the self-contained all-mode aggregation branch into one
  dedicated module.
- **Update**:
  - added `.data/wendao-frontend/src/components/SearchBar/searchExecutionAllMode.ts`
    for all-mode lane fan-out, fallback shaping, dedupe, and merged meta
    assembly
  - added `.data/wendao-frontend/src/components/SearchBar/__tests__/searchExecutionAllMode.test.ts`
  - rewired `.data/wendao-frontend/src/components/SearchBar/searchExecution.ts`
    to delegate `mode === "all"` to the extracted helper
  - updated `.data/wendao-frontend/README.md` and the active ExecPlan to record
    the extracted all-mode execution seam
- **Validation Executed**:
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm test -- src/components/SearchBar/__tests__/searchExecutionAllMode.test.ts src/components/SearchBar/__tests__/searchExecution.test.ts src/components/SearchBar/__tests__/SearchBar.test.tsx'`
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm run build'`

### [DONE] Feature: Wendao RFC Phase 1 query core and vector-heavy retrieval split

- **Objective**: land the first Wendao RFC implementation slice by adding a
  new internal `query_core` module in `xiuxian-wendao` and extracting reusable
  retrieval batch helpers into `xiuxian-vector`.
- **Update**:
  - created an ExecPlan at
    `.cache/codex/execplans/wendao_rfc_phase1_operator_skeleton.md`
  - added `xiuxian-vector::query_support` for retrieval-row shaping and payload
    projection helpers
  - added `xiuxian-wendao::query_core` with typed operator requests,
    execution context, explain telemetry, and thin adapters to the existing
    search-plane and link-graph paths
  - added a thin query-core service facade so handlers no longer need to
    hand-assemble execution context for the first landed callers
  - rewired repo-scoped `repo_content` and `repo_entity` code search to route
    through query-core relations before decoding back into legacy `SearchHit`
    payloads
  - rewired Studio `graph_neighbors` and `node_neighbors` to route through the
    query-core graph path while preserving existing response shapes
  - query-core explain now feeds Search Plane status for repo code search and
    emits compact internal summaries for graph handlers
  - query-core backend naming is now API-first rather than transition-first:
    `LinkGraphBackend` and `SearchPlaneBackend` replace the old `legacy-*`
    explain labels in code and tests
  - repo-wide code-search fanout now reuses one repo-scoped
    `search_repo_code_hits(...)` entrypoint, so the task scheduler only owns
    concurrency and timeout orchestration instead of open-coding entity/content
    fallback
  - repo publication gating and entity-first/content-fallback policy can now be
    expressed through one query-core service-level code-query entrypoint,
    reducing the amount of repo-lane policy that still lives above query-core
  - repo-scoped content/entity relation helpers now run through
    `vector_search -> column_mask -> payload_fetch`, so the narrow-phase and
    payload-phase operators are now exercised by real internal callers instead
    of only unit tests
  - added direct query-core service regressions for repo code-search corpus
    selection, so entity-preferred routing and content fallback are now pinned
    below the handler layer too
  - added `query_core::graph` as the query-core-native graph projection layer,
    with `WendaoGraphProjection` and
    `query_graph_neighbors_projection(...)`, so graph handlers no longer parse
    Arrow relation columns directly
  - repo-analysis query-search tests now also keep Studio snapshot baselines
    for repo-entity fast-path payloads, covering module, symbol, and example
    response shapes under
    `packages/rust/crates/xiuxian-wendao/tests/snapshots/gateway/studio/`
  - the RFC execution model is now explicitly milestone-based: M1 query-core
    foundation, M2 first internal caller migration, and M3 regression baseline
    are treated as complete; M4 unified repo query surface is now complete too
  - removed the gateway-local repo-analysis Search Plane helper seam; the
    repo-entity fast path for `module-search`, `symbol-search`, and
    `example-search` now routes through typed `query_core::service` entrypoints
    before analyzer artifact fallback
  - started Phase 2 with a bounded repo-analysis orchestration slice and
    recorded it in
    `.cache/codex/execplans/wendao_rfc_phase2_repo_analysis_orchestration.md`
  - extracted the repeated repo-analysis cache + fast-path + analyzer-fallback
    control flow into
    `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/repo/analysis/search/service.rs`,
    so module, symbol, and example handlers now stay thin
  - moved the typed repo-analysis search entrypoints into that shared service
    too, so route files now delegate to
    `run_repo_module_search(...)`, `run_repo_symbol_search(...)`, and
    `run_repo_example_search(...)` instead of carrying route-local fallback
    builder closures
  - added result snapshots under `tests/snapshots/wendao/` for query-core
    repo-code routing, query-core graph projection, and typed repo-entity
    query outputs
  - added targeted tests for retrieval helpers, vector-search adapter,
    graph-neighbor adapter, column masking, and payload projection
- **Validation Executed**:
  - `direnv exec . cargo check -p xiuxian-vector`
  - `direnv exec . cargo test -p xiuxian-wendao --lib query_core`
  - `direnv exec . cargo test -p xiuxian-wendao --lib graph_neighbors`
  - `direnv exec . cargo check -p xiuxian-wendao`
  - `direnv exec . cargo test -p xiuxian-wendao --lib repo_gateway_search`
  - `direnv exec . cargo test -p xiuxian-wendao --lib cached_repo_search_result_reuses_hot_query_payload`
  - `direnv exec . cargo test -p xiuxian-wendao --lib repo_entity_query_core_`

### [DONE] Feature: Wendao RFC Phase 2 repo-analysis typed service consolidation

- **Objective**: finish the first bounded Phase 2 repo-analysis milestone by
  collapsing module, symbol, and example route entrypoints onto one shared
  typed service boundary.
- **Update**:
  - advanced
    `.cache/codex/execplans/wendao_rfc_phase2_repo_analysis_orchestration.md`
    with landed `P2-M2`
  - moved the typed repo-analysis route-facing entry contract fully into
    `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/repo/analysis/search/service.rs`
  - rewired `module.rs`, `symbol.rs`, and `example.rs` so they now act as thin
    route adapters over `run_repo_module_search(...)`,
    `run_repo_symbol_search(...)`, and `run_repo_example_search(...)`
  - removed the remaining route-local fallback-builder closures from those
    repo-analysis handlers
  - lowered the shared service one step further by introducing a typed
    repo-analysis search contract for query construction, query-core fast-path
    dispatch, and analyzer-artifact fallback binding, so module, symbol, and
    example now differ by contract metadata rather than duplicated builder
    wiring
- **Validation Executed**:
  - `direnv exec . cargo check -p xiuxian-wendao`
  - `direnv exec . cargo test -p xiuxian-wendao --lib repo_gateway_search`
  - `direnv exec . cargo test -p xiuxian-wendao --lib repo_entity_query_core_`

### [DONE] Feature: Wendao RFC Phase 2 analyzer fallback contract downshift

- **Objective**: move repo-analysis fallback contract ownership out of the
  gateway shared service and into `analyzers::service::search`, so the gateway
  layer only composes query-core fast-path dispatch with analyzer-owned
  fallback contracts.
- **Update**:
  - added
    `packages/rust/crates/xiuxian-wendao/src/analyzers/service/search/contracts.rs`
    for analyzer-owned module/symbol/example fallback contracts
  - moved query construction, query-text access, query-limit access, fuzzy
    options, and artifact-backed result binding for repo-analysis fallback into
    `module_fallback_contract()`, `symbol_fallback_contract()`, and
    `example_fallback_contract()`
  - rewired
    `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/repo/analysis/search/service.rs`
    so the shared gateway service now composes analyzer-owned fallback
    contracts with query-core fast paths and gateway error mapping
  - removed the gateway-local typed query trait and query-builder helpers that
    were only supporting the previous higher-level fallback binding
- **Validation Executed**:
  - `direnv exec . cargo check -p xiuxian-wendao`
  - `direnv exec . cargo test -p xiuxian-wendao --lib repo_entity_query_core_`
  - `direnv exec . cargo test -p xiuxian-wendao --lib repo_gateway_search`
  - `direnv exec . cargo test -p xiuxian-wendao --lib analyzers_service_search`

### [DONE] Feature: Wendao RFC Phase 2 query-core fast-path contract downshift

- **Objective**: move repo-analysis fast-path contract ownership out of the
  gateway shared service and into `query_core`, so the gateway layer composes
  query-core-owned fast-path contracts with analyzer-owned fallback contracts.
- **Update**:
  - added typed repo-entity fast-path contracts to
    `packages/rust/crates/xiuxian-wendao/src/query_core/service.rs`
    via `RepoEntityTypedResultsContract<T>`,
    `query_repo_entity_results_if_published(...)`, and typed contract builders
    for module, symbol, and example result surfaces
  - rewired the existing query-core module/symbol/example repo-entity helpers
    to delegate through those contracts instead of maintaining three separate
    typed execution paths
  - rewired
    `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/handlers/repo/analysis/search/service.rs`
    so the shared gateway service now composes query-core-owned fast-path
    contracts with analyzer-owned fallback contracts and gateway error mapping
  - removed the gateway-local repo module/symbol/example fast-path helper
    functions that previously duplicated that contract layer
- **Validation Executed**:
  - `direnv exec . cargo check -p xiuxian-wendao`
  - `direnv exec . cargo test -p xiuxian-wendao --lib repo_entity_query_core_`
  - `direnv exec . cargo test -p xiuxian-wendao --lib repo_gateway_search`
  - `direnv exec . cargo test -p xiuxian-wendao --lib query_core`
  - `direnv exec . cargo test -p xiuxian-wendao --lib repo_entity_search_plane_query_returns_none_when_publication_is_not_ready`
  - `direnv exec . cargo test -p xiuxian-wendao --lib repo_entity_search_plane_query_preserves_error_code_mapping`
  - `direnv exec . cargo test -p xiuxian-wendao --lib query_repo_code_relation_prefers_repo_entity_corpus`
  - `direnv exec . cargo test -p xiuxian-wendao --lib query_repo_code_relation_falls_back_to_repo_content_when_entity_lane_is_disabled`
  - `direnv exec . cargo test -p xiuxian-wendao --lib query_graph_neighbors_projection_returns_nodes_and_links`
  - `direnv exec . cargo test -p xiuxian-wendao --lib typed_repo_entity_search_reconstructs_module_symbol_and_example_results`
  - `direnv exec . cargo check -p xiuxian-wendao`
  - `direnv exec . cargo test -p xiuxian-vector --lib query_support`
  - `direnv exec . cargo test -p xiuxian-wendao --lib query_core`
  - `direnv exec . cargo test -p xiuxian-wendao --lib repo_entity`
  - `direnv exec . cargo test -p xiuxian-wendao --lib graph_neighbors`
  - `direnv exec . cargo test -p xiuxian-wendao --lib indexing`
  - `direnv exec . cargo test -p xiuxian-wendao --lib budget`
  - `direnv exec . cargo test -p xiuxian-wendao --lib repo_content`
  - `direnv exec . cargo test -p xiuxian-wendao-julia --lib transport`

### [DONE] Feature: Frontend API client search transport split

- **Objective**: reduce the size and concern load of `.data/wendao-frontend/src/api/client.ts`
  by extracting the repeated Arrow-backed search transport seam into one
  dedicated helper module.
- **Update**:
  - added `.data/wendao-frontend/src/api/searchTransport.ts` for trimmed search
    param normalization, Arrow-backed search URL construction, and delegated
    JSON-plus-Arrow transport calls
  - added `.data/wendao-frontend/src/api/searchTransport.test.ts`
  - rewired knowledge, attachment, AST, reference, and symbol search methods in
    `.data/wendao-frontend/src/api/client.ts` to use the shared search
    transport helper
  - updated `.data/wendao-frontend/README.md` and the active ExecPlan to record
    the extracted search transport seam
- **Validation Executed**:
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm test -- src/api/searchTransport.test.ts src/api/client.test.ts src/components/SearchBar/__tests__/searchExecution.test.ts src/components/SearchBar/__tests__/SearchBar.test.tsx'`
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm run build'`

### [DONE] Feature: Frontend build-size compatibility-layer retirement

- **Objective**: retire the remaining `check-build-size-lib.mjs`
  compatibility layer now that the frontend build helper surface is fully
  normalized around `scripts/build/index.mjs`.
- **Update**:
  - verified that `check-build-size-lib.mjs` had no active direct code
    consumers and only one README mention remained
  - removed `.data/wendao-frontend/scripts/build/check-build-size-lib.mjs`
  - updated `.data/wendao-frontend/README.md` and the active ExecPlan to record
    that `scripts/build/index.mjs` is now the sole active build helper surface
- **Validation Executed**:
  - `direnv exec . bash -lc 'rg -n "check-build-size-lib\\.mjs" /Users/guangtao/projects/xiuxian-artisan-workshop/.data/wendao-frontend'`
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm test -- src/build/checkBuildSize.test.ts src/build/checkBuildSizeModelRuntime.test.ts'`
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm run build'`

### [DONE] Feature: Frontend build-size model/runtime split

- **Objective**: split the normalized build-size helper by concern so the
  frontend build helper surface distinguishes pure asset/budget logic from
  runtime filesystem execution.
- **Update**:
  - added `.data/wendao-frontend/scripts/build/check-build-size-model.mjs`
  - added `.data/wendao-frontend/scripts/build/check-build-size-runtime.mjs`
  - rewired `.data/wendao-frontend/scripts/build/index.mjs` to export the split
    model/runtime surface directly
  - reduced `.data/wendao-frontend/scripts/build/check-build-size-lib.mjs` to a
    compatibility re-export
  - added focused regression coverage in
    `.data/wendao-frontend/src/build/checkBuildSizeModelRuntime.test.ts`
  - updated `.data/wendao-frontend/README.md` and the active ExecPlan to record
    the split model/runtime helper layout
- **Validation Executed**:
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm test -- src/build/checkBuildSize.test.ts src/build/checkBuildSizeModelRuntime.test.ts'`
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm run build'`

### [DONE] Feature: Frontend build helper barrel

- **Objective**: add a small normalized `scripts/build` barrel so the build
  helper side mirrors the `scripts/rspack/*` helper surface and the CLI no
  longer depends on a concrete implementation filename.
- **Update**:
  - added `.data/wendao-frontend/scripts/build/index.mjs`
  - rewired `.data/wendao-frontend/scripts/check-build-size.mjs` to consume the
    build helper barrel
  - rewired `.data/wendao-frontend/src/build/checkBuildSize.test.ts` to consume
    the build helper barrel
  - updated `.data/wendao-frontend/README.md` and the active ExecPlan to record
    the normalized `scripts/build/*` helper surface
- **Validation Executed**:
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm test -- src/build/checkBuildSize.test.ts'`
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm run build'`

### [DONE] Feature: Frontend build-size CLI normalization

- **Objective**: normalize the remaining standalone build-size script so the
  frontend build helper surface keeps one stable CLI entrypoint while moving
  reusable implementation into a dedicated module.
- **Update**:
  - added `.data/wendao-frontend/scripts/build/check-build-size-lib.mjs` for
    the reusable asset extraction, budget evaluation, and build report logic
  - reduced `.data/wendao-frontend/scripts/check-build-size.mjs` to a thin CLI
    entrypoint that re-exports the shared implementation
  - extended `.data/wendao-frontend/src/build/checkBuildSize.test.ts` to pin
    the CLI-to-implementation bridge
  - updated `.data/wendao-frontend/README.md` and the active ExecPlan to record
    the normalized `scripts/build/*` plus `scripts/rspack/*` helper layout
- **Validation Executed**:
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm test -- src/build/checkBuildSize.test.ts'`
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm run build'`

### [DONE] Feature: Planned-search integration against official WendaoArrow metadata example

- **Objective**: verify that the `xiuxian-wendao` planned-search Julia rerank
  path remains healthy when the official WendaoArrow metadata example returns
  additive columns derived from request schema metadata.
- **Update**:
  - added `spawn_wendaoarrow_stream_metadata_service()` to the official
    example support helper
  - added `planned_search_julia_rerank_metadata_example.rs` as a focused
    vector-store runtime integration that targets
    `.data/WendaoArrow/scripts/run_stream_metadata_server.sh`
  - extended Julia response decoding plus `LinkGraphJuliaRerankTelemetry` so
    optional additive `trace_id` response columns are materialized into
    `julia_rerank.trace_ids`
  - attached a stable request schema metadata `trace_id` at the planned-search
    Julia rerank request boundary so the official metadata example can roundtrip
    context back into telemetry without changing the Arrow column contract
  - registered the new integration in `tests/xiuxian-testing-gate.rs`
  - updated `packages/rust/crates/xiuxian-wendao/README.md` to record that the
    main crate now validates both the official stream scoring and metadata
    examples
- **Validation Executed**:
  - `direnv exec . cargo check -p xiuxian-wendao --lib`
  - `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate test_planned_search_payload_applies_official_stream_metadata_example`
  - `direnv exec . git diff --check`

### [DONE] Feature: xiuxian-wendao-julia metadata-aware real WendaoArrow loopback

- **Objective**: verify that the Rust-side Julia transport can roundtrip a
  metadata-bearing Arrow IPC request through the official
  `.data/WendaoArrow` metadata example and decode additive response columns.
- **Update**:
  - added `spawn_real_wendaoarrow_metadata_service(...)` to
    `src/plugin/test_support/official_examples.rs`
  - added a transport test that writes request schema metadata with `trace_id`,
    sends it through the production `xiuxian_vector::ArrowTransportClient` to
    `.data/WendaoArrow/scripts/run_stream_metadata_server.sh`, and asserts the
    decoded response contains a `trace_id` column with the expected values
  - added test-only `reqwest` and `arrow-ipc` dependencies so the test can
    send a manual metadata-bearing Arrow IPC payload without changing the
    production transport API
  - updated `packages/rust/crates/xiuxian-wendao-julia/README.md` to record
    the new metadata-aware real loopback
- **Validation Executed**:
  - `direnv exec . cargo test -p xiuxian-wendao-julia process_julia_arrow_batches_against_real_wendaoarrow_service --lib`
  - `direnv exec . cargo test -p xiuxian-wendao-julia real_wendaoarrow_metadata_example_roundtrip_decodes_trace_id_column --lib`
  - `direnv exec . cargo check -p xiuxian-wendao-julia --lib`
  - `direnv exec . git diff --check`

### [DONE] Feature: Arrow transport request-metadata preservation contract

- **Objective**: pin the generic `xiuxian-vector` Arrow-over-HTTP substrate so
  request-side Arrow schema metadata is preserved across a real
  `ArrowTransportClient` roundtrip.
- **Update**:
  - added `transport_client_preserves_request_schema_metadata` to
    `src/arrow_transport/tests.rs`
  - the new test sends a `RecordBatch` whose schema metadata contains
    `trace_id`, verifies the server sees that metadata on decode, and asserts
    the echoed response batch still carries the same metadata after the client
    decode path
  - updated `packages/rust/crates/xiuxian-vector/README.md` to record that
    additive request metadata is now part of the transport verification surface
- **Validation Executed**:
  - `direnv exec . cargo test -p xiuxian-vector transport_client_preserves_request_schema_metadata --lib`
  - `direnv exec . cargo check -p xiuxian-vector --lib`

### [DONE] Feature: Frontend Rspack shim removal

- **Objective**: remove the retired root-level frontend Rspack helper shim
  files now that the repository has fully converged on the normalized
  `scripts/rspack/*` helper tree.
- **Update**:
  - verified that the repo-wide shim-path scan only found GTD history
    references and no active code consumers
  - removed the six root-level helper shim files under
    `.data/wendao-frontend/scripts/`
  - added focused regression coverage in
    `.data/wendao-frontend/src/build/rspackShimRemoval.test.ts` to keep the
    root-level shim paths absent and the normalized helper tree present
  - updated `.data/wendao-frontend/README.md` and the active ExecPlan to record
    that `scripts/rspack/*` is now the only active helper surface
- **Validation Executed**:
  - `direnv exec . bash -lc 'rg -n "\\.data/wendao-frontend/scripts/(build-size-budgets\\.mjs|rspack-chunk-policy\\.mjs|rspack-build-environment|rspack-build-profile|rspack-module-rules|rspack-core-surface)|scripts/(build-size-budgets\\.mjs|rspack-chunk-policy\\.mjs|rspack-build-environment|rspack-build-profile|rspack-module-rules|rspack-core-surface)" /Users/guangtao/projects/xiuxian-artisan-workshop'`
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm test -- src/build/checkBuildSize.test.ts src/build/rspackChunkPolicy.test.ts src/build/rspackBuildEnvironment.test.ts src/build/rspackBuildProfile.test.ts src/build/rspackModuleRules.test.ts src/build/rspackCoreSurface.test.ts src/build/rspackHelperBarrel.test.ts src/build/rspackShimRemoval.test.ts'`
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm run build'`

### [DONE] Feature: Frontend Rspack shim consumer cutover status sync

- **Objective**: close the remaining documentation drift after the normalized
  `scripts/rspack/*` helper tree landed, so the frontend build-tooling source
  of truth is explicit and the root-level helper files are documented only as
  compatibility shims.
- **Update**:
  - verified that internal frontend build-tooling consumers no longer import
    the root-level Rspack helper shim paths
  - updated `.data/wendao-frontend/README.md` to point all active helper
    references at the normalized `scripts/rspack/*` tree
  - updated the active ExecPlan to record that the consumer cutover was already
    complete in code and this slice only closed the stale docs/plan state
- **Validation Executed**:
  - `direnv exec . bash -lc 'rg -n "scripts/(build-size-budgets\\.mjs|rspack-chunk-policy\\.mjs|rspack-build-environment|rspack-build-profile|rspack-module-rules|rspack-core-surface)" .data/wendao-frontend'`
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm test -- src/build/checkBuildSize.test.ts src/build/rspackChunkPolicy.test.ts src/build/rspackBuildEnvironment.test.ts src/build/rspackBuildProfile.test.ts src/build/rspackModuleRules.test.ts src/build/rspackCoreSurface.test.ts src/build/rspackHelperBarrel.test.ts'`
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm run build'`

### [DONE] Feature: WendaoArrow stream metadata example

- **Objective**: add one official example that demonstrates how an analyzer can
  read request schema metadata from `Arrow.Stream` batches and surface that
  context as additive response data.
- **Update**:
  - added `.data/WendaoArrow/examples/stream_metadata_server.jl` with a
    metadata-aware stream processor that reads `trace_id` from batch schema
    metadata and emits it as an optional response column
  - added `.data/WendaoArrow/scripts/run_stream_metadata_server.sh`
  - extended `.data/WendaoArrow/test/runtests.jl` with a stream metadata test
    that writes custom Arrow metadata and validates `schema_metadata(...)`
    through the stream decode path
  - updated `.data/WendaoArrow/README.md` and the HTTP interface doc to record
    the metadata-aware example and the intended usage pattern
- **Validation Executed**:
  - `direnv exec . julia --project=.data/WendaoArrow -e 'using Pkg; Pkg.test()'`
  - `direnv exec . git diff --check`

### [DONE] Feature: xiuxian-wendao-julia test support split by official example boundary

- **Objective**: align `xiuxian-wendao-julia` test support with the same
  boundary used in `xiuxian-wendao`, so the transport crate no longer keeps
  official WendaoArrow service helpers inline inside `transport.rs`.
- **Update**:
  - added `src/plugin/test_support/common.rs` for shared child/path helpers
  - added `src/plugin/test_support/official_examples.rs` for the real
    `run_stream_scoring_server.sh` loopback helper
  - registered the new `test_support` module in `src/plugin/mod.rs`
  - reduced `src/plugin/transport.rs` test code to transport assertions and
    moved official-example process wiring out of the file
  - updated `packages/rust/crates/xiuxian-wendao-julia/README.md` to record
    the new test-support layout
- **Validation Executed**:
  - `direnv exec . cargo test -p xiuxian-wendao-julia process_julia_arrow_batches_against_real_wendaoarrow_service --lib`
  - `direnv exec . cargo check -p xiuxian-wendao-julia --lib`
  - `direnv exec . git diff --check`

### [DONE] Feature: WendaoArrow integration helper split by test semantics

- **Objective**: make the Julia rerank integration layer explicit about which
  tests validate private custom scoring behavior and which tests validate the
  package-owned official stream scoring example.
- **Update**:
  - renamed the custom helper to
    `spawn_wendaoarrow_custom_scoring_service(...)` so score-map driven tests
    are visibly distinct from the official example path
  - updated the OpenAI-compatible and vector-store rerank integration tests to
    call the custom helper by name
  - documented the split in `packages/rust/crates/xiuxian-wendao/README.md`
- **Validation Executed**:
  - `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate test_planned_search_payload_applies_julia_rerank_scores`
  - `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate test_planned_search_payload_with_query_vector_applies_julia_rerank_on_vector_store`
  - `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate test_planned_search_payload_applies_official_stream_scoring_example`
  - `direnv exec . git diff --check`

### [DONE] Feature: Planned-search integration against official WendaoArrow stream scoring example

- **Objective**: validate that the `xiuxian-wendao` planned-search runtime can
  call the package-owned `.data/WendaoArrow` stream scoring example directly,
  instead of only relying on custom per-test Julia processors.
- **Update**:
  - added `spawn_wendaoarrow_stream_scoring_service()` in the integration
    support helper so `xiuxian-wendao` tests can launch
    `.data/WendaoArrow/scripts/run_stream_scoring_server.sh`
  - added `planned_search_julia_rerank_official_example.rs` as a focused
    vector-store runtime integration that asserts Julia rerank applies and that
    `saliency_score == vector_score` after the official stream scoring example
    runs
  - registered the new integration in `tests/xiuxian-testing-gate.rs`
  - updated `packages/rust/crates/xiuxian-wendao/README.md` to record that the
    main crate now validates the official WendaoArrow example surface
- **Validation Executed**:
  - `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate test_planned_search_payload_applies_official_stream_scoring_example`
  - `direnv exec . git diff --check`

### [DONE] Feature: Rust loopback targets WendaoArrow stream scoring example

- **Objective**: make the Rust-side real WendaoArrow loopback test hit the
  package's official stream scoring example, so the e2e fixture validates the
  same entrypoint future Julia analyzers are expected to copy or extend.
- **Update**:
  - updated `xiuxian-wendao-julia` real loopback helper to launch
    `.data/WendaoArrow/scripts/run_stream_scoring_server.sh` instead of an
    inline Julia processor snippet
  - aligned the Rust-side assertions with the stream scoring example's actual
    contract semantics: `analyzer_score == vector_score` and
    `final_score == vector_score`
  - updated `packages/rust/crates/xiuxian-wendao-julia/README.md` to record
    that the real loopback now targets the package-owned stream scoring example
- **Validation Executed**:
  - `direnv exec . cargo test -p xiuxian-wendao-julia process_julia_arrow_batches_against_real_wendaoarrow_service --lib`
  - `direnv exec . cargo check -p xiuxian-wendao-julia --lib`
  - `direnv exec . git diff --check`

### [DONE] Feature: WendaoArrow stream scoring example

- **Objective**: add one contract-shaped stream example that returns
  `doc_id / analyzer_score / final_score`, so future Julia analyzer packages
  can start from a real WendaoArrow `v1` response shape instead of a transport
  passthrough.
- **Update**:
  - added `.data/WendaoArrow/examples/stream_scoring_server.jl` as a minimal
    stream-first scoring server that iterates over `Arrow.Stream` batches and
    emits WendaoArrow `v1` response columns
  - added `.data/WendaoArrow/scripts/run_stream_scoring_server.sh` so the
    scoring example can be launched with the same config and flag flow as the
    other package examples
  - updated `.data/WendaoArrow/README.md` and the HTTP interface doc to record
    the new stream scoring example and its intended role as the analyzer-facing
    bootstrap example
- **Validation Executed**:
  - `direnv exec . julia --project=.data/WendaoArrow -e 'using Pkg; Pkg.test()'`
  - `direnv exec . git diff --check`

### [DONE] Feature: WendaoArrow stream passthrough example

- **Objective**: add one concrete stream-first example so future Julia
  analyzers can start from a real `Arrow.Stream` HTTP processor instead of
  reverse-engineering the lower-level helper surface.
- **Update**:
  - added `.data/WendaoArrow/examples/stream_passthrough_server.jl` as the
    stream-first companion to the existing table-first passthrough example
  - added `.data/WendaoArrow/scripts/run_stream_passthrough_server.sh` so the
    example can be launched with the same config and flag flow as the existing
    table-first server
  - updated `.data/WendaoArrow/README.md` and the HTTP interface doc to
    distinguish the table-first and stream-first handler surfaces explicitly
- **Validation Executed**:
  - `direnv exec . julia --project=.data/WendaoArrow -e 'using Pkg; Pkg.test()'`
  - `direnv exec . git diff --check`

### [DONE] Feature: WendaoArrow stream-ready decode helpers

- **Objective**: add low-risk Arrow.jl API upgrades to WendaoArrow so the
  interface package can prepare for future batch-wise processing without
  changing the current `processor(table)` HTTP contract.
- **Update**:
  - updated `.data/WendaoArrow/src/codec.jl` to make `convert = true`
    explicit on Arrow decode paths, matching the actual Arrow.jl stable API
  - added `decode_stream(body)` as the stream-first companion to
    `decode_ipc(body)`, exposing `Arrow.Stream` for future large-batch
    processors
  - added `build_stream_handler(processor)` and `serve_stream(processor)` so
    stream-first processors can opt into HTTP composition without changing the
    existing table-first `build_handler(processor)` / `serve(processor)` path
  - added `schema_metadata(table)` so WendaoArrow owns metadata access instead
    of forcing callers to use `Arrow.getmetadata(...)` directly
  - verified that the new stream helper does not change the current handler
    shape: HTTP routing remains table-first and `processor(input_table)` is
    unchanged in this iteration
- **Validation Executed**:
  - `direnv exec . julia --project=.data/WendaoArrow -e 'using Pkg; Pkg.test()'`
  - `direnv exec . cargo test -p xiuxian-wendao-julia process_julia_arrow_batches_against_real_wendaoarrow_service --lib`

### [DONE] Feature: Frontend build-size regression check

- **Objective**: add one post-build guardrail so the warning-free frontend
  bundle state fails closed if any emitted JS/CSS asset or the initial
  entrypoint payload regresses past the current Rspack size budgets.
- **Update**:
  - added `.data/wendao-frontend/scripts/check-build-size.mjs` with pure helper
    exports for `dist/index.html` asset extraction and budget evaluation, plus
    a CLI entrypoint that measures emitted JS/CSS assets and fails on budget
    regressions
  - added `.data/wendao-frontend/scripts/build-size-budgets.mjs` so the
    Rspack warning thresholds and the post-build gate now read one shared
    budget source instead of duplicating the values
  - added focused unit coverage in
    `.data/wendao-frontend/src/build/checkBuildSize.test.ts`
  - updated `.data/wendao-frontend/package.json` so `npm run build` now runs
    the size gate immediately after `rspack build`
  - updated `.data/wendao-frontend/README.md` to record the new build guardrail
- **Validation Executed**:
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm test -- src/build/checkBuildSize.test.ts'`
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm run build'`

### [DONE] Feature: Frontend shared Rspack chunk policy

- **Objective**: centralize the frontend split-chunk cache-group and async
  vendor naming rules so bundle topology policy no longer lives inline inside
  `rspack.config.ts`.
- **Update**:
  - added `.data/wendao-frontend/scripts/rspack-chunk-policy.mjs` with one
    shared cache-group factory plus async chunk-name sanitization helpers
  - rewired `.data/wendao-frontend/rspack.config.ts` to consume that shared
    policy module instead of keeping the cache-group monolith inline
  - added focused unit coverage in
    `.data/wendao-frontend/src/build/rspackChunkPolicy.test.ts`
  - updated `.data/wendao-frontend/README.md` to record the new shared policy
    source
- **Validation Executed**:
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm test -- src/build/checkBuildSize.test.ts src/build/rspackChunkPolicy.test.ts'`
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm run build'`

### [DONE] Feature: Frontend shared Rspack build environment

- **Objective**: centralize frontend gateway-target parsing, plugin assembly,
  and dev-server proxy construction so `rspack.config.ts` no longer owns those
  environment details inline.
- **Update**:
  - added `.data/wendao-frontend/scripts/rspack-build-environment.ts` with
    shared helpers for gateway bind normalization, TOML gateway resolution,
    plugin construction, and development proxy config assembly
  - rewired `.data/wendao-frontend/rspack.config.ts` to consume the shared
    build-environment helpers
  - added focused unit coverage in
    `.data/wendao-frontend/src/build/rspackBuildEnvironment.test.ts`
  - updated `.data/wendao-frontend/README.md` to record the new build helper
    module
- **Validation Executed**:
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm test -- src/build/checkBuildSize.test.ts src/build/rspackChunkPolicy.test.ts src/build/rspackBuildEnvironment.test.ts'`
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm run build'`

### [DONE] Feature: Frontend shared Rspack build profile

- **Objective**: centralize frontend browser targets, minimizer assembly, and
  shared performance/experiments profile so `rspack.config.ts` no longer owns
  that build profile inline.
- **Update**:
  - added `.data/wendao-frontend/scripts/rspack-build-profile.ts` with shared
    browser targets, minimizer factory, and performance/experiments helpers
  - rewired `.data/wendao-frontend/rspack.config.ts` to consume the shared
    build-profile helpers
  - added focused unit coverage in
    `.data/wendao-frontend/src/build/rspackBuildProfile.test.ts`
  - updated `.data/wendao-frontend/README.md` to record the new build-profile
    helper module
- **Validation Executed**:
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm test -- src/build/checkBuildSize.test.ts src/build/rspackChunkPolicy.test.ts src/build/rspackBuildEnvironment.test.ts src/build/rspackBuildProfile.test.ts'`
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm run build'`

### [DONE] Feature: Frontend shared Rspack module rules

- **Objective**: centralize frontend asset-resource and SWC loader rules so
  `rspack.config.ts` no longer owns the `module.rules` block inline.
- **Update**:
  - added `.data/wendao-frontend/scripts/rspack-module-rules.ts` with shared
    SVG asset, BPMN/GLB/GLTF asset-resource, and SWC loader rule helpers
  - rewired `.data/wendao-frontend/rspack.config.ts` to consume the shared
    module-rules helper
  - added focused unit coverage in
    `.data/wendao-frontend/src/build/rspackModuleRules.test.ts`
  - updated `.data/wendao-frontend/README.md` to record the new module-rules
    helper module
- **Validation Executed**:
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm test -- src/build/checkBuildSize.test.ts src/build/rspackChunkPolicy.test.ts src/build/rspackBuildEnvironment.test.ts src/build/rspackBuildProfile.test.ts src/build/rspackModuleRules.test.ts'`
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm run build'`

### [DONE] Feature: Frontend shared Rspack core surface

- **Objective**: centralize frontend static `entry`, `output`, and `resolve`
  config so `rspack.config.ts` no longer owns that static assembly inline.
- **Update**:
  - added `.data/wendao-frontend/scripts/rspack-core-surface.ts` with shared
    entry, output, and resolve helpers
  - rewired `.data/wendao-frontend/rspack.config.ts` to consume the shared
    core-surface helper
  - added focused unit coverage in
    `.data/wendao-frontend/src/build/rspackCoreSurface.test.ts`
  - updated `.data/wendao-frontend/README.md` to record the new core-surface
    helper module
- **Validation Executed**:
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm test -- src/build/checkBuildSize.test.ts src/build/rspackChunkPolicy.test.ts src/build/rspackBuildEnvironment.test.ts src/build/rspackBuildProfile.test.ts src/build/rspackModuleRules.test.ts src/build/rspackCoreSurface.test.ts'`
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm run build'`

### [DONE] Feature: Frontend shared Rspack helper barrel

- **Objective**: aggregate the extracted Rspack helper modules behind one
  shared barrel so `rspack.config.ts` consumes one coherent build-tooling
  surface instead of a flat helper import list.
- **Update**:
  - added `.data/wendao-frontend/scripts/rspack/index.ts` as the shared helper
    barrel over budgets, chunk policy, build environment, build profile, module
    rules, and core surface helpers
  - rewired `.data/wendao-frontend/rspack.config.ts` to import from the helper
    barrel
  - added focused unit coverage in
    `.data/wendao-frontend/src/build/rspackHelperBarrel.test.ts`
  - updated `.data/wendao-frontend/README.md` to record the new helper-barrel
    path
- **Validation Executed**:
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm test -- src/build/checkBuildSize.test.ts src/build/rspackChunkPolicy.test.ts src/build/rspackBuildEnvironment.test.ts src/build/rspackBuildProfile.test.ts src/build/rspackModuleRules.test.ts src/build/rspackCoreSurface.test.ts src/build/rspackHelperBarrel.test.ts'`
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm run build'`

### [DONE] Feature: Frontend normalized Rspack helper tree

- **Objective**: normalize the extracted Rspack helper implementation files
  under `scripts/rspack/*` while preserving the current build behavior and
  leaving root-level compatibility shims in place.
- **Update**:
  - moved the concrete helper implementations into the normalized
    `.data/wendao-frontend/scripts/rspack/*` tree
  - updated the helper barrel, build-size checker, and focused tests to consume
    the normalized helper paths directly
  - left the old root-level helper files in place as thin compatibility shims
  - updated `.data/wendao-frontend/README.md` to record the normalized helper
    tree
- **Validation Executed**:
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm test -- src/build/checkBuildSize.test.ts src/build/rspackChunkPolicy.test.ts src/build/rspackBuildEnvironment.test.ts src/build/rspackBuildProfile.test.ts src/build/rspackModuleRules.test.ts src/build/rspackCoreSurface.test.ts src/build/rspackHelperBarrel.test.ts'`
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm run build'`

### [DONE] Feature: WendaoArrow schema metadata duplicate

- **Objective**: harden the WendaoArrow `v1` contract by duplicating the schema
  version inside successful Arrow IPC payload metadata, while preserving the
  existing HTTP header as the authoritative transport-level signal.
- **Update**:
  - updated `.data/WendaoArrow/src/codec.jl` so successful Arrow IPC responses
    now write `wendao.schema_version = v1` into Arrow schema metadata
  - extended `.data/WendaoArrow/test/runtests.jl` to assert the metadata is
    present after decode, alongside the existing HTTP header checks
  - kept the Rust side unchanged: `x-wendao-schema-version` remains the
    required runtime contract header, and metadata stays as an Arrow-native
    duplicate for post-decode inspection
  - synchronized `.data/WendaoArrow` docs so README and interface specs now
    describe the header-first plus metadata-duplicate contract precisely
- **Validation Executed**:
  - `direnv exec . julia --project=.data/WendaoArrow -e 'using Pkg; Pkg.test()'`
  - `direnv exec . cargo test -p xiuxian-wendao-julia process_julia_arrow_batches_against_real_wendaoarrow_service --lib`

### [DONE] Feature: Wendao Julia Arrow score adapter in core service layer

- **Objective**: add one explicit async service-layer adapter in `xiuxian-wendao`
  that can call the repository-configured Julia Arrow transport and materialize
  the validated response into typed Rust score rows, without mutating the
  current synchronous analyzer plugin trait.
- **Update**:
  - added `analyzers/service/julia_transport.rs` with
    `fetch_julia_arrow_score_rows_for_repository(...)` and
    `decode_julia_arrow_score_rows(...)`
  - introduced `JuliaArrowScoreRow` as the typed `doc_id /
analyzer_score / final_score` materialization surface for future gateway or
    reranking integration
  - exported the new adapter from `analyzers::service` and the crate-level
    `analyzers` public surface behind the existing `julia` feature gate
  - added focused unit tests for response decoding and one loopback HTTP
    roundtrip against repository-configured Julia transport settings
  - repaired one Arrow-57 follow-up break in
    `gateway/studio/router/retrieval_arrow.rs` by switching test/runtime IPC
    imports to `arrow::ipc`
- **Validation Executed**:
  - `direnv exec . cargo test -p xiuxian-wendao julia_arrow_score_rows --lib`
  - `direnv exec . cargo check -p xiuxian-wendao --lib`
  - `direnv exec . git diff --check`

### [DONE] Feature: WendaoArrow request-batch assembly prerequisites

- **Objective**: complete the missing Rust-side prerequisites for WendaoArrow
  `v1` request assembly by adding one vector-store embedding fetch surface and
  one typed request-batch builder.
- **Update**:
  - added `VectorStore::fetch_embeddings_by_ids(...)` in `xiuxian-vector` so
    Rust can recover candidate embeddings by `doc_id` without widening
    `VectorSearchResult`
  - added `JuliaArrowRequestRow` and
    `build_julia_arrow_request_batch(...)` in `xiuxian-wendao` to emit
    `doc_id`, `vector_score`, `embedding`, and repeated `query_embedding`
    columns for WendaoArrow `v1`
  - added `VectorStoreSemanticIgnition::build_julia_rerank_request_batch(...)`
    so the link-graph semantic ignition path can now turn
    `QuantumAnchorHit + query_vector` into a transport-ready WendaoArrow batch
  - repaired a small Studio search export/visibility break while running this
    verification pass
- **Validation Executed**:
  - `direnv exec . cargo test -p xiuxian-vector fetch_embeddings_by_ids --lib`
  - `direnv exec . cargo test -p xiuxian-wendao julia_transport --lib`
  - `direnv exec . cargo test -p xiuxian-wendao build_julia_rerank_request_batch_uses_anchor_ids_as_request_doc_ids --lib`
  - `direnv exec . cargo check -p xiuxian-vector --lib`
  - `direnv exec . cargo check -p xiuxian-wendao --lib`
  - `direnv exec . git diff --check`

### [DONE] Feature: OpenAI-compatible Julia request-batch assembly

- **Objective**: let the OpenAI-compatible semantic ignition backend assemble
  the same WendaoArrow `v1` request batch used by the vector-store backend, so
  link-graph retrieval can reuse one Julia rerank contract even when the
  effective query vector must be derived from `query_text`.
- **Update**:
  - added `OpenAiCompatibleSemanticIgnition::resolve_query_vector(...)` to
    normalize the effective query vector from either an explicit
    `query_vector` or an OpenAI-compatible embedding call
  - added
    `OpenAiCompatibleSemanticIgnition::build_julia_rerank_request_batch(...)`
    to fetch anchor embeddings from the vector store and emit the canonical
    WendaoArrow `v1` request shape
  - added `OpenAiCompatibleJuliaRequestError` so query-vector resolution,
    missing embeddings, vector-store failures, and Arrow batch construction are
    surfaced as typed failures instead of stringly-typed transport errors
  - verified both the explicit-query-vector path and the missing-query-signal
    rejection path with focused unit tests
- **Validation Executed**:
  - `direnv exec . cargo fmt -p xiuxian-wendao`
  - `direnv exec . cargo test -p xiuxian-wendao build_julia_rerank_request_batch_uses_explicit_query_vector --lib`
  - `direnv exec . cargo test -p xiuxian-wendao build_julia_rerank_request_batch_rejects_missing_query_signal --lib`

### [DONE] Feature: Link-graph Julia rerank runtime config scaffold

- **Objective**: add one runtime-owned configuration surface for future
  WendaoArrow post-processing in link-graph retrieval, without yet inserting
  the remote Julia rerank call into the live quantum-fusion ranking path.
- **Update**:
  - added `link_graph.retrieval.julia_rerank.*` runtime parsing for
    `base_url`, `route`, `health_route`, `schema_version`, and `timeout_secs`
  - extended `LinkGraphRetrievalPolicyRuntimeConfig` with
    `LinkGraphJuliaRerankRuntimeConfig`
  - extended `LinkGraphPlannedSearchPayload` with a separate
    `julia_rerank` telemetry slot so Julia transport state does not get folded
    into `semantic_ignition`
  - repaired one existing attachment test visibility break while re-running the
    `xiuxian-wendao` lib test target, and updated one payload test fixture to
    include the new telemetry field
- **Validation Executed**:
  - `direnv exec . cargo fmt -p xiuxian-wendao`
  - `direnv exec . cargo test -p xiuxian-wendao test_retrieval_runtime_resolves_julia_rerank_config --lib`
  - `direnv exec . cargo test -p xiuxian-wendao test_retrieval_runtime_resolves_semantic_ignition_config --lib`
  - `direnv exec . cargo check -p xiuxian-wendao --lib`

### [DONE] Feature: Link-graph OpenAI runtime Julia rerank wiring

- **Objective**: connect the OpenAI-compatible link-graph runtime path to the
  WendaoArrow transport so Julia rerank can run as an optional post-processing
  step over emitted `quantum_contexts`, while keeping the original Rust scores
  available as the fail-closed fallback.
- **Update**:
  - wired `link_graph/index/search/plan/payload/quantum.rs` to build an
    `ArrowTransportClient` from `link_graph.retrieval.julia_rerank`
  - added optional OpenAI runtime rerank execution that:
    builds a WendaoArrow `v1` request batch, sends it over Arrow IPC, validates
    the Julia response contract, decodes score rows, and overwrites
    `QuantumContext.saliency_score` with Julia `final_score`
  - kept `vector_score` and `topology_score` unchanged so Rust-side scoring
    observability remains intact
  - made the runtime fail closed: any Julia build/transport/decode/contract
    failure now records `julia_rerank` telemetry and preserves the original
    quantum-fusion ordering
  - explicitly reports the current vector-store backend limitation through
    `julia_rerank` telemetry because the runtime path still does not retain an
    effective query vector there
  - added focused unit tests for the saliency override/re-sort helper and the
    runtime transport-config builder
- **Validation Executed**:
  - `direnv exec . cargo fmt -p xiuxian-wendao`
  - `direnv exec . cargo test -p xiuxian-wendao apply_julia_rerank_scores_overwrites_saliency_and_resorts_contexts --lib`
  - `direnv exec . cargo test -p xiuxian-wendao build_julia_rerank_transport_client_honors_runtime_overrides --lib`
  - `direnv exec . cargo check -p xiuxian-wendao --lib`

### [DONE] Feature: Planned-search Julia rerank loopback integration test

- **Objective**: verify the whole planned-search runtime path can traverse
  OpenAI-compatible semantic ignition, WendaoArrow transport, Julia rerank
  contract validation, and final `quantum_contexts` resorting under one real
  loopback test.
- **Update**:
  - added `tests/integration/planned_search_julia_rerank.rs`
  - the test boots one local Axum server that serves both `/v1/embeddings` and
    `/arrow-ipc`, seeds a temporary vector store, configures
    `link_graph.retrieval.semantic_ignition` plus
    `link_graph.retrieval.julia_rerank`, and executes the real
    `LinkGraphIndex::search_planned_payload_with_agentic(...)` flow
  - verified that Julia `final_score` rewrites the emitted
    `QuantumContext.saliency_score`, reorders the contexts, and records
    successful `julia_rerank` telemetry
  - wired the new integration test into `tests/xiuxian-testing-gate.rs`
- **Validation Executed**:
  - `direnv exec . cargo fmt -p xiuxian-wendao`
  - `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate test_planned_search_payload_applies_julia_rerank_scores`
  - `direnv exec . cargo check -p xiuxian-wendao --tests`

### [DONE] Feature: Vector-store Julia rerank query-vector carry-through

- **Objective**: let the planned-search runtime retain one optional
  precomputed `query_vector` so the vector-store semantic ignition backend can
  participate in the same Julia rerank transport path as the
  OpenAI-compatible backend.
- **Update**:
  - added a non-serialized `query_vector` carry slot on
    `LinkGraphPlannedSearchPayload`
  - threaded optional query-vector input through the planned-search sync path,
    async path, and runtime bridge via
    `search_planned_payload_with_agentic_query_vector(...)` and
    `search_planned_payload_with_agentic_async_with_query_vector(...)`
  - updated `quantum.rs` so the vector-store runtime backend now uses the
    retained query vector for both semantic ignition and Julia rerank request
    assembly
  - added `tests/integration/planned_search_julia_rerank_vector_store.rs` and
    wired it into `tests/xiuxian-testing-gate.rs`
  - verified the vector-store backend can now roundtrip through WendaoArrow and
    reorder `quantum_contexts` when a caller supplies a precomputed query
    vector
- **Validation Executed**:
  - `direnv exec . cargo fmt -p xiuxian-wendao`
  - `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate test_planned_search_payload_with_query_vector_applies_julia_rerank_on_vector_store`
  - `direnv exec . cargo check -p xiuxian-wendao --tests`

### [DONE] Feature: Zhenfa router query-vector request surface

- **Objective**: expose the planned-search Julia rerank query-vector path at
  the external `zhenfa_router` request boundary so HTTP and RPC callers can
  supply a precomputed embedding without changing the serialized
  `LinkGraphPlannedSearchPayload` contract.
- **Update**:
  - extended `WendaoSearchRequest` and `WendaoSearchPlannedRequest` with an
    optional `query_vector`
  - updated the HTTP planned-search path to call
    `search_planned_payload_with_agentic_async_with_query_vector(...)`
  - updated the RPC execution path to route caller-provided vectors into
    `search_planned_payload_with_agentic_query_vector(...)`
  - kept `LinkGraphPlannedSearchPayload.query_vector` runtime-only with
    `#[serde(skip)]`, so the external payload schema remains unchanged
  - added unit coverage for `WendaoSearchRequest` query-vector deserialization
- **Validation Executed**:
  - `direnv exec . cargo fmt -p xiuxian-wendao`
  - `direnv exec . cargo test -p xiuxian-wendao wendao_search_request_deserializes_query_vector --lib`
  - `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate test_planned_search_payload_with_query_vector_applies_julia_rerank_on_vector_store`
  - `direnv exec . cargo check -p xiuxian-wendao --tests`

### [DONE] Feature: Native `wendao.search` query-vector forwarding

- **Objective**: extend the native Zhenfa `wendao.search` tool surface so
  direct tool callers can pass a precomputed `query_vector` into the same
  planned-search runtime path already used by HTTP and RPC routing.
- **Update**:
  - extended `WendaoSearchArgs` with optional `query_vector`
  - updated native `wendao_search(...)` execution to call
    `search_planned_payload_with_agentic_query_vector(...)` when the vector is
    present
  - added a focused native-tool unit test for argument deserialization
  - added one `xiuxian-daochang` bridge test to verify native tool invocation
    still succeeds when `query_vector` is present in the tool arguments
- **Validation Executed**:
  - `direnv exec . cargo test -p xiuxian-wendao wendao_search_args_deserialize_query_vector --lib`
  - `direnv exec . cargo test -p xiuxian-daochang call_tool_dispatches_wendao_search_with_query_vector_natively`

### [DONE] Feature: Real WendaoArrow HTTP Arrow IPC loopback

- **Objective**: verify `xiuxian-wendao`'s Julia transport can speak to the
  actual `.data/WendaoArrow` Julia package over HTTP Arrow IPC, instead of
  only validating against Rust-side mock Axum handlers.
- **Update**:
  - added a real loopback test in
    `xiuxian-wendao-julia/src/plugin/transport.rs` that spawns a Julia
    `WendaoArrow` service from `.data/WendaoArrow`
  - the test waits on the real `/health` probe, sends Arrow IPC through
    `ArrowTransportClient`, and validates the returned
    `doc_id / analyzer_score / final_score` batches
  - fixed two compile-boundary issues uncovered while running the real loopback:
    `xiuxian-wendao` now gates the repository-scoped Julia fetch adapter behind
    `feature = "julia"`, and `xiuxian-wendao-julia` now re-exports
    `JULIA_ARROW_RESPONSE_SCHEMA_VERSION` through its public plugin surface
- **Validation Executed**:
  - `direnv exec . cargo test -p xiuxian-wendao-julia process_julia_arrow_batches_against_real_wendaoarrow_service --lib`
  - `direnv exec . git diff --check`

### [DONE] Feature: Real WendaoArrow planned-search rerank integration

- **Objective**: move `xiuxian-wendao`'s Julia rerank integration tests off
  Rust-side Arrow mocks and onto the real `.data/WendaoArrow` Julia service so
  the planned-search runtime verifies the actual gateway-to-Julia transport.
- **Update**:
  - added `tests/integration/support/wendaoarrow_real_service.rs` to spawn the
    real `.data/WendaoArrow` package with deterministic score mappings for test
    docs
  - rewired `planned_search_julia_rerank.rs` to keep only the embedding mock
    while sending the rerank Arrow IPC request to the real Julia service
  - rewired `planned_search_julia_rerank_vector_store.rs` to use the same real
    Julia service helper for the vector-store backend path
  - re-exported `JULIA_ARROW_RESPONSE_SCHEMA_VERSION` through
    `xiuxian-wendao::analyzers::languages` to eliminate the warning exposed by
    the shared Julia plugin source include
- **Validation Executed**:
  - `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate test_planned_search_payload_applies_julia_rerank_scores`
  - `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate test_planned_search_payload_with_query_vector_applies_julia_rerank_on_vector_store`

### [DONE] Feature: WendaoArrow schema-version response header

- **Objective**: make `.data/WendaoArrow` actively declare the `v1` contract on
  every HTTP response so the Rust gateway can rely on an explicit protocol
  header instead of a best-effort optional response hint.
- **Update**:
  - added `DEFAULT_SCHEMA_VERSION = "v1"` and
    `SCHEMA_VERSION_HEADER = "x-wendao-schema-version"` to the Julia package
  - updated health, success, invalid-request, and processor-failure responses
    to emit the schema-version header
  - extended Julia tests to assert the header on both Arrow and JSON response
    paths
  - synchronized the README and HTTP/schema-contract docs to treat the
    response header as part of the live WendaoArrow transport behavior
- **Validation Executed**:
  - `direnv exec . julia --project=.data/WendaoArrow -e 'using Pkg; Pkg.test()'`
  - `direnv exec . cargo test -p xiuxian-wendao-julia process_julia_arrow_batches_against_real_wendaoarrow_service --lib`

### [DONE] Feature: Strict Arrow transport response-header enforcement

- **Objective**: upgrade the Rust Arrow-over-HTTP client from soft schema
  negotiation to strict protocol enforcement so a missing
  `x-wendao-schema-version` is treated as a transport error.
- **Update**:
  - changed `xiuxian-vector::ArrowTransportClient` to require a matching
    `x-wendao-schema-version` header on both `/health` and Arrow IPC responses
  - added dedicated regression coverage for missing health and missing Arrow
    response schema headers
  - synchronized `xiuxian-vector` docs to document the stricter response-header
    requirement
- **Validation Executed**:
  - `direnv exec . cargo test -p xiuxian-vector transport_client_checks_health_endpoint --lib`
  - `direnv exec . cargo test -p xiuxian-vector transport_client_rejects_missing_health_schema_version --lib`
  - `direnv exec . cargo test -p xiuxian-vector transport_client_rejects_missing_response_schema_version --lib`

### [IN-PROGRESS] Feature: Wendao whole search-plane DataFusion refactor blueprint refinement

- **Objective**: tighten the whole-search-plane DataFusion cutover plan so the
  first implementation slices stay compatible with current HTTP contracts and
  current corpus boundaries.
- **Update**:
  - accepted mandatory two-stage lazy materialization for DataFusion corpus
    queries: narrow filter/ranking scan first, then Top-K payload hydration
  - accepted selective statistics-aware pruning as a Parquet writer/query-plan
    rule for contract-safe scalar quality columns rather than as a universal
    query assumption
  - explicitly deferred cross-corpus join views until after single-corpus query
    parity is landed
  - explicitly deferred DataFusion runtime priority groups and memory-pool
    partitioning; current service semaphores and request budgets remain the
    first-line scheduling controls during cutover
  - landed the first real corpus cutover on `repo_entity`: the service now
    owns a shared `SearchEngineContext`, repo-entity publication writes export
    Parquet beside the Lance table and record `storage_format = parquet`, and
    repo-entity query plus Top-K hydration now read through DataFusion instead
    of Lance FTS
  - landed the second corpus cutover on `repo_content_chunk`: publication
    writes now export Parquet beside the Lance table, repo-content publications
    now record `storage_format = parquet`, and repo-content search now reads
    narrow projected columns through DataFusion instead of Lance FTS/scan
  - landed the third corpus cutover on `knowledge_section`: local epoch writes
    now export Parquet beside the Lance table, local epoch queries now require
    the Parquet artifact, and knowledge search now runs as a two-stage
    DataFusion read with narrow ranking columns first and `hit_json` hydration
    by `id` second
  - landed the fourth corpus cutover on `attachment`: local epoch writes now
    export Parquet beside the Lance table, attachment search now reads narrow
    ranking columns through DataFusion and hydrates `hit_json` by `id`, and the
    internal rerank source enum is now reduced to the scan path that this
    refactor actually emits
- **Validation Executed**:
  - `direnv exec . git diff --check`
  - `CARGO_HOME=.cache/codex/cargo_home_repo_entity CARGO_TARGET_DIR=.cache/codex/target_datafusion_repo_entity_iso direnv exec . cargo check -p xiuxian-vector`
  - `CARGO_HOME=.cache/codex/cargo_home_repo_entity CARGO_TARGET_DIR=.cache/codex/target_datafusion_repo_entity_iso direnv exec . cargo check -p xiuxian-wendao --lib`
  - `direnv exec . cargo test -p xiuxian-vector --test test_search_engine -- --nocapture`
  - `direnv exec . cargo check -p xiuxian-wendao --lib`
  - `direnv exec . cargo nextest run -p xiuxian-wendao -E 'test(repo_content_chunk_incremental_refresh_reuses_unchanged_rows) | test(plan_repo_content_chunk_build_only_rewrites_changed_files) | test(plan_repo_content_chunk_build_reuses_table_for_revision_only_refresh) | test(trim_best_by_path_keeps_top_ranked_paths) | test(retained_window_scales_with_limit)'`
  - `direnv exec . cargo nextest run -p xiuxian-wendao -E 'test(knowledge_section_incremental_refresh_reuses_unchanged_rows) | test(plan_knowledge_section_build_only_reparses_changed_notes) | test(trim_best_by_path_keeps_highest_ranked_hits) | test(retained_window_scales_with_limit)'`
  - `direnv exec . cargo nextest run -p xiuxian-wendao -E 'test(attachment_incremental_refresh_reuses_unchanged_rows) | test(plan_attachment_build_only_reparses_changed_notes) | test(trim_candidates_keeps_highest_ranked_attachment_hits) | test(retained_window_scales_with_limit) | test(attachment_query_reads_hits_from_published_epoch) | test(streaming_rerank_telemetry_tracks_batches_and_trims)'`

### [DONE] Feature: Arrow IPC search-hit transport for Zen search

- **Objective**: move the hottest Zen left-pane search-result lane onto Arrow IPC without widening or replacing the existing JSON `SearchResponse` metadata contract.
- **Update**:
  - added backend Arrow encoding for `SearchHit[]` in `gateway/studio/search/handlers/knowledge/intent/arrow.rs`
  - added `/api/search/intent/hits-arrow`, reusing the same `SearchQuery` contract as `/api/search/intent`
  - extracted frontend Arrow search-hit decoding into `.data/wendao-frontend/src/api/arrowSearchIpc.ts`
  - updated `.data/wendao-frontend/src/api/client.ts` so `api.searchKnowledge()` fetches JSON metadata and Arrow hits in parallel, then overlays Arrow-decoded hits onto the JSON response when the Arrow sidecar succeeds
  - kept nested hit fields such as `tags`, `hierarchy`, `implicitBacklinks`, `implicitBacklinkItems`, and `navigationTarget` stable through Arrow by encoding them as per-row JSON strings inside the Arrow record batch
  - repaired the focused Rust roundtrip test import scope and relaxed the frontend fetch-order assertion so gateway config bootstrap traffic does not make the Arrow transport test flaky
- **Validation Executed**:
  - `direnv exec . cargo test -p xiuxian-wendao search_hit_arrow --lib`
  - `direnv exec . cargo test -p xiuxian-wendao gateway::studio::search::handlers::knowledge:: --lib`
  - `direnv exec . cargo check -p xiuxian-wendao --lib --keep-going`
  - `direnv exec . bash -lc 'cd /Users/guangtao/projects/xiuxian-artisan-workshop/.data/wendao-frontend && npm test -- src/api/client.test.ts src/components/SearchBar/__tests__/searchExecution.test.ts src/components/SearchBar/__tests__/searchResultNormalization.test.ts'`
  - `direnv exec . bash -lc 'cd /Users/guangtao/projects/xiuxian-artisan-workshop/.data/wendao-frontend && npm run build'`

### [DONE] Feature: Arrow local result-view assembly for SearchBar

- **Objective**: move the hottest client-side SearchBar list assembly path onto Arrow-backed local tables without changing the existing `SearchResult[]` consumer contract.
- **Update**:
  - added `.data/wendao-frontend/src/components/SearchBar/arrowSearchResultView.ts` as the local Arrow view model for row indexes, identity, category, path, score, and code-filter columns
  - rewired `.data/wendao-frontend/src/components/SearchBar/searchResultSections.ts` so `getVisibleResults(...)` now performs scope filtering, code-filter matching, dedupe, and sort through the Arrow-backed local view
  - added `.data/wendao-frontend/src/components/SearchBar/__tests__/searchResultSections.test.ts` to pin dedupe, code-filter, and path-sort behavior through the Arrow path
  - updated `.data/wendao-frontend/src/components/SearchBar/__tests__/SearchBar.test.tsx` mock coverage to include the current Zen preview analysis/Arrow helper methods, so SearchBar integration tests remain aligned with the live API surface
- **Validation Executed**:
  - `direnv exec . bash -lc 'cd /Users/guangtao/projects/xiuxian-artisan-workshop/.data/wendao-frontend && npm test -- src/components/SearchBar/__tests__/searchResultSections.test.ts src/components/SearchBar/__tests__/SearchBar.test.tsx src/components/SearchBar/__tests__/searchExecution.test.ts src/api/client.test.ts'`
  - `direnv exec . bash -lc 'cd /Users/guangtao/projects/xiuxian-artisan-workshop/.data/wendao-frontend && npm run build'`

### [DONE] Feature: Arrow local catalog and section assembly for SearchBar

- **Objective**: continue the same local Arrow cutover so SearchBar code-filter facets and visible section grouping stop re-scanning plain object arrays after the visible result slice has already been columnarized.
- **Update**:
  - extended `.data/wendao-frontend/src/components/SearchBar/arrowSearchResultView.ts` with Arrow-backed code-filter catalog extraction for `repo`, `kind`, and normalized `path` facets while still keeping language suggestions sourced from gateway capabilities
  - rewired `.data/wendao-frontend/src/components/SearchBar/useCodeFilterCatalog.ts` onto that shared Arrow local view instead of manually scanning raw `SearchResult[]`
  - added `getVisibleSearchView(...)` in `.data/wendao-frontend/src/components/SearchBar/searchResultSections.ts` so visible results and visible sections now derive from the same Arrow-backed slice
  - rewired `.data/wendao-frontend/src/components/SearchBar/useSearchDerivedState.ts` to consume the combined visible-result plus visible-section helper instead of running separate passes
  - extended focused coverage in `.data/wendao-frontend/src/components/SearchBar/__tests__/useCodeFilterCatalog.test.ts` and `.data/wendao-frontend/src/components/SearchBar/__tests__/searchResultSections.test.ts`
- **Validation Executed**:
  - `direnv exec . bash -lc 'cd /Users/guangtao/projects/xiuxian-artisan-workshop/.data/wendao-frontend && npm test -- src/components/SearchBar/__tests__/searchResultSections.test.ts src/components/SearchBar/__tests__/useCodeFilterCatalog.test.ts src/components/SearchBar/__tests__/SearchBar.test.tsx src/components/SearchBar/__tests__/searchExecution.test.ts src/api/client.test.ts'`
  - `direnv exec . bash -lc 'cd /Users/guangtao/projects/xiuxian-artisan-workshop/.data/wendao-frontend && npm run build'`

### [DONE] Feature: Arrow IPC symbol-hit transport for dedicated symbol search

- **Objective**: extend the Arrow search transport pattern to dedicated symbol-mode search without widening the existing `SymbolSearchResponse` metadata contract.

### [DONE] Feature: Arrow IPC AST-hit transport for dedicated AST search

- **Objective**: extend the Arrow search transport pattern to dedicated AST-mode search without widening the existing `AstSearchResponse` metadata contract.
- **Update**:
  - added backend Arrow encoding for `AstSearchHit[]` in `gateway/studio/search/handlers/ast.rs`
  - added `/api/search/ast/hits-arrow`, reusing the same AST query parameters as `/api/search/ast`
  - extended `.data/wendao-frontend/src/api/arrowSearchIpc.ts` with Arrow decoding for markdown-aware AST hit fields such as `nodeKind`, `ownerTitle`, and `navigationTargetJson`
  - updated `.data/wendao-frontend/src/api/client.ts` so `api.searchAst()` fetches JSON metadata and Arrow hits in parallel, then overlays the Arrow-decoded hits onto the unchanged JSON response when the Arrow sidecar succeeds
  - kept the existing JSON `AstSearchResponse` contract intact so AST-mode SearchBar flows benefit from Arrow transport without a wider public response surface
- **Validation Executed**:
  - `direnv exec . cargo fmt -p xiuxian-wendao`
  - `direnv exec . cargo test -p xiuxian-wendao search_ast_arrow --lib`
  - `direnv exec . cargo test -p xiuxian-wendao gateway::studio::search::handlers::ast --lib`
  - `direnv exec . cargo check -p xiuxian-wendao --lib --keep-going`
  - `direnv exec . bash -lc 'cd /Users/guangtao/projects/xiuxian-artisan-workshop/.data/wendao-frontend && npm test -- src/api/client.test.ts src/components/SearchBar/__tests__/searchExecution.test.ts src/components/SearchBar/__tests__/SearchBar.test.tsx'`
  - `direnv exec . bash -lc 'cd /Users/guangtao/projects/xiuxian-artisan-workshop/.data/wendao-frontend && npm run build'`

### [DONE] Feature: Shared Arrow transport primitives for Studio search

- **Objective**: collapse repeated Arrow IPC encode/decode and JSON-overlay plumbing across knowledge, symbol, reference, and AST search without changing any public endpoint contract.
- **Update**:
  - added `gateway/studio/search/handlers/arrow_transport.rs` as the shared backend helper for Arrow IPC batch emission, JSON field encoding, and response header wrapping
  - rewired the knowledge, symbol, reference, and AST Arrow handlers onto that helper while keeping each endpoint’s schema definition local
  - added `.data/wendao-frontend/src/api/arrowSearchOverlay.ts` as the shared frontend helper for parallel JSON metadata + Arrow hit overlay
  - rewired `api.searchKnowledge()`, `api.searchSymbols()`, `api.searchReferences()`, and `api.searchAst()` onto the shared overlay helper
  - reduced `.data/wendao-frontend/src/api/arrowSearchIpc.ts` to one shared Arrow row-walk primitive plus the four endpoint-specific row mappers
  - added direct frontend coverage for the shared overlay helper fallback/success paths
- **Validation Executed**:
  - `direnv exec . cargo fmt -p xiuxian-wendao`
  - `CARGO_TARGET_DIR=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/codex/target_arrow_search_primitives direnv exec . cargo test -p xiuxian-wendao arrow_roundtrip --lib`
  - `CARGO_TARGET_DIR=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/codex/target_arrow_search_primitives direnv exec . cargo check -p xiuxian-wendao --lib --keep-going`
  - `direnv exec . bash -lc 'cd /Users/guangtao/projects/xiuxian-artisan-workshop/.data/wendao-frontend && npm test -- src/api/arrowSearchOverlay.test.ts src/api/client.test.ts src/components/SearchBar/__tests__/searchExecution.test.ts src/components/SearchBar/__tests__/SearchBar.test.tsx'`
  - `direnv exec . bash -lc 'cd /Users/guangtao/projects/xiuxian-artisan-workshop/.data/wendao-frontend && npm run build'`

### [DONE] Feature: Arrow IPC attachment-hit transport for dedicated attachment search

- **Objective**: extend the Arrow search transport pattern to dedicated attachment-mode search without widening the existing `AttachmentSearchResponse` metadata contract.
- **Update**:
  - added backend Arrow encoding for `AttachmentSearchHit[]` in `gateway/studio/search/handlers/attachments.rs`
  - added `/api/search/attachments/hits-arrow`, reusing the same attachment query parameters as `/api/search/attachments`
  - extended `.data/wendao-frontend/src/api/arrowSearchIpc.ts` with Arrow decoding for attachment hit fields such as `name`, `sourceTitle`, `navigationTargetJson`, and `visionSnippet`
  - updated `.data/wendao-frontend/src/api/client.ts` so `api.searchAttachments()` fetches JSON metadata and Arrow hits in parallel, then overlays the Arrow-decoded hits onto the unchanged JSON response when the Arrow sidecar succeeds
  - kept the existing JSON `AttachmentSearchResponse` contract intact so attachment-mode SearchBar flows benefit from Arrow transport without a wider public response surface
  - added direct attachment transport coverage in `.data/wendao-frontend/src/api/arrowSearchIpc.test.ts` and `tests/unit/gateway/studio/search.rs`, so this slice is no longer relying only on client overlay coverage
- **Validation Executed**:
  - `direnv exec . cargo fmt -p xiuxian-wendao`
  - `CARGO_TARGET_DIR=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/codex/target_arrow_attachment_transport direnv exec . cargo test -p xiuxian-wendao search_attachment_arrow --lib`
  - `CARGO_TARGET_DIR=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/codex/target_arrow_attachment_transport direnv exec . cargo test -p xiuxian-wendao search_attachments_hits_arrow_returns_arrow_payload`
  - `CARGO_TARGET_DIR=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/codex/target_arrow_attachment_transport direnv exec . cargo check -p xiuxian-wendao --lib --keep-going`
  - `direnv exec . bash -lc 'cd /Users/guangtao/projects/xiuxian-artisan-workshop/.data/wendao-frontend && npm test -- src/api/arrowSearchIpc.test.ts src/api/client.test.ts src/components/SearchBar/__tests__/searchExecution.test.ts src/components/SearchBar/__tests__/SearchBar.test.tsx'`
  - `direnv exec . bash -lc 'cd /Users/guangtao/projects/xiuxian-artisan-workshop/.data/wendao-frontend && npm run build'`

### [DONE] Feature: Lazy-load workspace App from Studio bootstrap

- **Objective**: remove the remaining `main` entrypoint size warning by moving the
  full workspace app behind the existing gateway/bootstrap shell instead of
  widening the Rspack performance budget.
- **Update**:
  - replaced the static `App` import in `.data/wendao-frontend/src/StudioBootstrap.tsx` with `React.lazy(() => import('./App'))`
  - reused the existing blank bootstrap surface as the suspense fallback so the
    startup flow stays visually stable while the workspace app chunk loads
  - kept the gateway bootstrap sequencing unchanged: health, UI config sync, and
    capabilities probing still complete before the workspace app is rendered
- **Validation Executed**:
  - `direnv exec . bash -lc 'cd /Users/guangtao/projects/xiuxian-artisan-workshop/.data/wendao-frontend && npm test -- src/StudioBootstrap.test.tsx'`
  - `direnv exec . bash -lc 'cd /Users/guangtao/projects/xiuxian-artisan-workshop/.data/wendao-frontend && npm run build'`

### [DONE] Feature: Split Shiki into core and per-language async chunks

- **Objective**: remove the remaining oversized async `shiki.js` warning without dropping syntax highlighting or loosening the frontend asset budget.
- **Update**:
  - added explicit per-language and per-theme chunk names in `.data/wendao-frontend/src/components/code-syntax/shikiHighlighter.ts`
  - narrowed the Rspack Shiki cache group in `.data/wendao-frontend/rspack.config.ts` to `@shikijs/core`, `engine-oniguruma`, and the small shared runtime packages only
  - kept Shiki languages and themes lazy so they load on demand instead of being collapsed into one monolithic async asset
- **Validation Executed**:
  - `direnv exec . bash -lc 'cd /Users/guangtao/projects/xiuxian-artisan-workshop/.data/wendao-frontend && npm test -- src/components/code-syntax/CodeSyntaxHighlighter.test.tsx src/components/code-syntax/index.test.ts'`
  - `direnv exec . bash -lc 'cd /Users/guangtao/projects/xiuxian-artisan-workshop/.data/wendao-frontend && npm run build'`
  - added backend Arrow encoding for `SymbolSearchHit[]` in `packages/rust/crates/xiuxian-wendao/src/gateway/studio/search/handlers/symbols.rs`
  - added `/api/search/symbols/hits-arrow`, reusing the same `SymbolSearchQuery` contract as `/api/search/symbols`
  - extended `.data/wendao-frontend/src/api/arrowSearchIpc.ts` with Arrow decoding for `SymbolSearchHit`
  - updated `.data/wendao-frontend/src/api/client.ts` so `api.searchSymbols()` now fetches JSON symbol metadata and Arrow symbol hits in parallel, then overlays Arrow-decoded hits onto the JSON response when the Arrow sidecar succeeds
  - added focused frontend coverage in `.data/wendao-frontend/src/api/client.test.ts` for the symbol Arrow overlay path
- **Validation Executed**:
  - `direnv exec . cargo fmt -p xiuxian-wendao`
  - `direnv exec . cargo test -p xiuxian-wendao search_symbol_arrow --lib`
  - `direnv exec . cargo test -p xiuxian-wendao gateway::studio::search::handlers::symbols --lib`
  - `direnv exec . cargo check -p xiuxian-wendao --lib --keep-going`
  - `direnv exec . bash -lc 'cd /Users/guangtao/projects/xiuxian-artisan-workshop/.data/wendao-frontend && npm test -- src/api/client.test.ts src/components/SearchBar/__tests__/searchExecution.test.ts src/components/SearchBar/__tests__/SearchBar.test.tsx'`
  - `direnv exec . bash -lc 'cd /Users/guangtao/projects/xiuxian-artisan-workshop/.data/wendao-frontend && npm run build'`

### [DONE] Feature: Arrow IPC reference-hit transport for dedicated reference search

- **Objective**: extend the Arrow search transport pattern to dedicated reference-mode search without widening the existing `ReferenceSearchResponse` metadata contract.
- **Update**:
  - added backend Arrow encoding for `ReferenceSearchHit[]` in `packages/rust/crates/xiuxian-wendao/src/gateway/studio/search/handlers/references.rs`
  - added `/api/search/references/hits-arrow`, reusing the same `ReferenceSearchQuery` contract as `/api/search/references`
  - extended `.data/wendao-frontend/src/api/arrowSearchIpc.ts` with Arrow decoding for `ReferenceSearchHit`
  - updated `.data/wendao-frontend/src/api/client.ts` so `api.searchReferences()` now fetches JSON reference metadata and Arrow reference hits in parallel, then overlays Arrow-decoded hits onto the JSON response when the Arrow sidecar succeeds
  - added focused frontend coverage in `.data/wendao-frontend/src/api/client.test.ts` for the reference Arrow overlay path
- **Validation Executed**:
  - `direnv exec . cargo fmt -p xiuxian-wendao`
  - `direnv exec . cargo test -p xiuxian-wendao search_reference_arrow --lib`
  - `direnv exec . cargo test -p xiuxian-wendao gateway::studio::search::handlers::references --lib`
  - `direnv exec . cargo check -p xiuxian-wendao --lib --keep-going`
  - `direnv exec . bash -lc 'cd /Users/guangtao/projects/xiuxian-artisan-workshop/.data/wendao-frontend && npm test -- src/api/client.test.ts src/components/SearchBar/__tests__/searchExecution.test.ts src/components/SearchBar/__tests__/SearchBar.test.tsx'`
  - `direnv exec . bash -lc 'cd /Users/guangtao/projects/xiuxian-artisan-workshop/.data/wendao-frontend && npm run build'`

### [DONE] Feature: Wendao config and Tantivy helper modularization

- **Objective**: continue the Rust modularization sweep by splitting remaining mixed-responsibility Wendao modules into folder-first layouts without changing public surfaces.
- **Update**:
  - split `src/analyzers/config.rs` into `src/analyzers/config/` with dedicated types, TOML schema, parse, load, and tests modules
  - split `src/search/tantivy/index.rs` into `src/search/tantivy/index/` with dedicated core, exact, prefix, fuzzy, and helper modules
  - split `src/analyzers/service/helpers/tests.rs` into `src/analyzers/service/helpers/tests/` with fixture and themed assertion modules
  - removed stale TOML re-exports and dead helpers, and kept module imports crate-rooted where possible
- **Validation Executed**:
  - `direnv exec . cargo fmt -p xiuxian-wendao`
- `direnv exec . cargo test -p xiuxian-wendao analyzers::config::tests:: --lib`
- `direnv exec . cargo test -p xiuxian-wendao gateway::studio::router::config::tests:: --lib`
- `direnv exec . cargo test -p xiuxian-wendao search::tantivy::tests:: --lib`
- `direnv exec . cargo test -p xiuxian-wendao analyzers::service::helpers::tests:: --lib`
- `direnv exec . git diff --check`

- `search_plane/reference_occurrence/query.rs` was split into `search_plane/reference_occurrence/query/` with dedicated search, candidates, decode, and tests modules; the next target is `search_plane/repo_entity/build/tests.rs`

### [DONE] Feature: Wendao Arrow ownership alignment with Lance-facing Arrow-57 paths

- **Objective**: remove accidental Arrow-58 usage from LanceDB-facing Wendao and vector-store paths while keeping DataFusion and HTTP transport layers on their engine-side Arrow surface.
- **Update**:
  - restored mixed `plugins = ["julia", { id = "julia", arrow_transport = { ... } }]` parsing in `xiuxian-wendao` analyzer and Studio config loaders
  - recovered `xiuxian-wendao-julia` package documentation for the Julia transport plugin boundary
  - re-exported Lance-native `ListArray` and builders from `xiuxian-vector` so Wendao repo-entity hydration and batch construction no longer downcast Arrow-58 arrays into Lance record batches
  - aligned `link_graph` PPR traversal visibility with the current `related_ppr_compute` ownership boundary
  - verified the remaining direct `arrow::...` imports are isolated to DataFusion/search-engine, quantum fusion, and Arrow-over-HTTP transport codepaths
- **Validation Executed**:
  - `direnv exec . cargo test -p xiuxian-wendao load_repo_intelligence_config --lib`
  - `direnv exec . cargo test -p xiuxian-wendao repo_entity_batches_encode_structured_columns --lib`
  - `direnv exec . cargo test -p xiuxian-wendao-julia transport --lib`
  - `direnv exec . cargo check -p xiuxian-vector --lib`
  - `direnv exec . cargo check -p xiuxian-wendao --lib`
  - `direnv exec . cargo check -p xiuxian-wendao-julia --lib`
  - `direnv exec . git diff --check`

### [DONE] Feature: WendaoArrow baseline schema contract

- **Objective**: fix the Rust-to-Julia Arrow boundary around one explicit request and response contract before analyzer-specific packages start adding fields.
- **Update**:
  - added `.data/WendaoArrow/docs/01_core/wendao_arrow/architecture/arrow-schema-contract.md`
  - defined the `v1` contract version, required request columns, required response columns, null semantics, and response matching semantics
  - documented the preferred schema version carrier as `x-wendao-schema-version` with optional Arrow schema metadata duplication
  - linked the new contract from `SPEC.md`, `http-arrow-interface.md`, and `VECTOR_SUBSYSTEM.md`
- **Validation Executed**:
  - `direnv exec . git diff --check`

### [DONE] Feature: Rust-side Arrow schema-version wiring

- **Objective**: turn the WendaoArrow `v1` contract into a runtime constraint on the Rust side without over-coupling the generic Arrow transport to analyzer-specific response semantics.
- **Update**:
  - added `schema_version` support to `xiuxian-vector::ArrowTransportConfig` with default `v1`
  - wired `ArrowTransportClient` to send `x-wendao-schema-version` and reject mismatched response headers when present
  - added Julia-plugin-level `validate_julia_arrow_response_batches` so analyzer response validation lives in `xiuxian-wendao-julia`, not the generic transport substrate
  - exported the Julia response contract version constant for future gateway call sites
- **Validation Executed**:
  - `direnv exec . cargo test -p xiuxian-vector arrow_transport:: --lib`
  - `direnv exec . cargo test -p xiuxian-wendao-julia transport --lib`
  - `direnv exec . git diff --check`

### [DONE] Feature: Julia transport runtime wrapper

- **Objective**: create one reusable call site that performs the Julia Arrow HTTP roundtrip and contract validation together, so future gateway integration does not have to reassemble transport and response checks manually.
- **Update**:
  - added `process_julia_arrow_batches(...)` to `xiuxian-wendao-julia`
  - the wrapper now delegates to `ArrowTransportClient::process_batches(...)` and immediately enforces the `v1` Julia response contract
  - added end-to-end HTTP loopback tests for valid and invalid remote Julia responses
- **Validation Executed**:
  - `direnv exec . cargo test -p xiuxian-wendao-julia transport --lib`
  - `direnv exec . git diff --check`

### [DONE] Feature: Code AST anatomy deep modularization

- **Objective**: continue shrinking the AST anatomy projection layer so retrieval plumbing, projection primitives, signature parsing, block assembly, and symbol grouping no longer accumulate inside one large module.
- **Update**:
  - extracted shared projection primitives into `src/components/ZenSearch/StructuredDashboard/codeAstProjectionShared.ts`
  - extracted signature snippet/parsing helpers into `src/components/ZenSearch/StructuredDashboard/codeAstSignatureHelpers.ts`
  - extracted logic-block assembly into `src/components/ZenSearch/StructuredDashboard/codeAstBlockHelpers.ts`
  - extracted symbol and pivot-anchor grouping into `src/components/ZenSearch/StructuredDashboard/codeAstSymbolHelpers.ts`
  - kept `codeAstAnatomy.ts` focused on orchestrating the AST anatomy model
- **Validation Executed**:
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm test -- src/components/ZenSearch/__tests__/CodeAstAnatomyView.test.tsx src/components/ZenSearch/StructuredDashboard/__tests__/StructuredIntelligenceDashboard.test.tsx src/components/ZenSearch/StructuredDashboard/__tests__/StructuredCodeInspector.test.tsx src/components/ZenSearch/__tests__/ZenSearchPreviewPane.test.tsx'`
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm run build'`

### [DONE] Feature: Structured dashboard shell modularization

- **Objective**: keep the right-pane structured dashboard readable by extracting inline formatting, focus resolution, and reusable renderer helpers out of the main Zen orchestration component.
- **Update**:
  - extracted shared path compression, side badge formatting, layer navigation constants, and focused-anchor resolution into `src/components/ZenSearch/StructuredDashboard/structuredDashboardShared.ts`
  - extracted reusable chip/list/fragment/metadata/outline renderers into `src/components/ZenSearch/StructuredDashboard/structuredDashboardRenderers.tsx`
  - added `src/components/ZenSearch/StructuredDashboard/__tests__/structuredDashboardShared.test.ts` to pin path compaction and anchor-side resolution behavior
  - kept `StructuredIntelligenceDashboard.tsx` focused on Zen preview orchestration and slot composition
- **Validation Executed**:
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm test -- src/components/ZenSearch/StructuredDashboard/__tests__/structuredDashboardShared.test.ts src/components/ZenSearch/StructuredDashboard/__tests__/StructuredIntelligenceDashboard.test.tsx src/components/ZenSearch/StructuredDashboard/__tests__/StructuredCodeInspector.test.tsx src/components/ZenSearch/__tests__/CodeAstAnatomyView.test.tsx src/components/ZenSearch/__tests__/ZenSearchPreviewPane.test.tsx'`
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm run build'`

### [DONE] Feature: AST waterfall presentational slice modularization

- **Objective**: keep the AST waterfall renderer maintainable by extracting the large JSX body in `CodeAstAnatomyView.tsx` into focused presentational slices without changing retrieval atom behavior.
- **Update**:
  - extracted the file prelude, declaration lane, logic-block lane, and symbol/anchor lane into `src/components/ZenSearch/codeAstAnatomySections.tsx`
  - kept `CodeAstAnatomyView.tsx` focused on preview-state wiring, derived-model setup, and high-level composition
  - added `src/components/ZenSearch/__tests__/codeAstAnatomySections.test.tsx` as a smoke test for the extracted slices and their pivot actions
- **Validation Executed**:
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm test -- src/components/ZenSearch/__tests__/codeAstAnatomySections.test.tsx src/components/ZenSearch/__tests__/CodeAstAnatomyView.test.tsx src/components/ZenSearch/StructuredDashboard/__tests__/StructuredIntelligenceDashboard.test.tsx src/components/ZenSearch/StructuredDashboard/__tests__/StructuredCodeInspector.test.tsx src/components/ZenSearch/__tests__/ZenSearchPreviewPane.test.tsx'`
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm run build'`

### [DONE] Feature: Zen preview bridge modularization

- **Objective**: keep the right-pane preview shell readable by separating preview-surface detection from the markdown and structured bridge render paths in `ZenSearchPreviewEntity.tsx`.
- **Update**:
  - extracted markdown preview detection into `src/components/ZenSearch/zenSearchPreviewSurface.ts`
  - extracted markdown and structured preview bridge bodies into `src/components/ZenSearch/zenSearchPreviewBridges.tsx`
  - added `src/components/ZenSearch/__tests__/zenSearchPreviewSurface.test.ts` to pin markdown preview detection behavior
  - kept `ZenSearchPreviewEntity.tsx` focused on preview routing
- **Validation Executed**:
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm test -- src/components/ZenSearch/__tests__/zenSearchPreviewSurface.test.ts src/components/ZenSearch/__tests__/ZenSearchPreviewEntity.test.tsx src/components/ZenSearch/__tests__/ZenSearchPreviewPane.test.tsx src/components/panels/DirectReader/MarkdownWaterfall.test.tsx src/components/ZenSearch/StructuredDashboard/__tests__/StructuredIntelligenceDashboard.test.tsx'`
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm run build'`

### [DONE] Feature: Zen preview loader modularization

- **Objective**: keep the shared Zen preview hook maintainable by separating preview-path planning and parallel retrieval execution from `useZenSearchPreview.ts`.
- **Update**:
  - extracted preview load planning and retrieval execution into `src/components/ZenSearch/zenSearchPreviewLoaders.ts`
  - kept `useZenSearchPreview.ts` focused on preview identity, cancellation, and state orchestration
  - added `src/components/ZenSearch/__tests__/zenSearchPreviewLoaders.test.ts` to pin normalized path planning and code/markdown eligibility decisions
- **Validation Executed**:
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm test -- src/components/ZenSearch/__tests__/zenSearchPreviewLoaders.test.ts src/components/ZenSearch/__tests__/useZenSearchPreview.test.tsx src/components/ZenSearch/__tests__/ZenSearchPreviewEntity.test.tsx src/components/ZenSearch/__tests__/ZenSearchPreviewPane.test.tsx src/components/panels/DirectReader/MarkdownWaterfall.test.tsx'`
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm run build'`

### [DONE] Feature: Zen preview pane/shell controller-view split

- **Objective**: keep the top-level preview controller thin by separating pane framing and placeholder/entity routing from `ZenSearchPreviewPane.tsx` and `ZenSearchPreviewShell.tsx`.
- **Update**:
  - extracted the rendered pane frame into `src/components/ZenSearch/ZenSearchPreviewPaneView.tsx`
  - extracted placeholder-vs-entity routing into `src/components/ZenSearch/zenSearchPreviewShellContent.tsx`
  - kept `ZenSearchPreviewPane.tsx` focused on hook wiring and kept `ZenSearchPreviewShell.tsx` focused on the shell container
  - added `src/components/ZenSearch/__tests__/zenSearchPreviewShellContent.test.tsx` to pin placeholder/entity routing
- **Validation Executed**:
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm test -- src/components/ZenSearch/__tests__/zenSearchPreviewShellContent.test.tsx src/components/ZenSearch/__tests__/ZenSearchPreviewPane.test.tsx src/components/ZenSearch/__tests__/ZenSearchPreviewEntity.test.tsx src/components/ZenSearch/__tests__/ZenSearchPreviewShell.test.tsx src/components/ZenSearch/__tests__/zenSearchPreviewSurface.test.ts src/components/ZenSearch/__tests__/useZenSearchPreview.test.tsx'`
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm run build'`

### [DONE] Feature: Arrow-backed retrieval lookup for Zen structured previews

- **Objective**: move the hottest frontend retrieval-atom query paths onto `apache-arrow` so Zen markdown and code waterfalls share one columnar local lookup layer instead of per-surface `Map` and array-scan logic.
- **Update**:
  - added `apache-arrow` to `.data/wendao-frontend`
  - added `src/utils/arrowRetrievalLookup.ts` with shared owner, owner/surface, semantic-type, and line-range lookup helpers over Arrow tables
  - rewired `codeAstRetrievalHelpers.ts` and `codeAstBlockHelpers.ts` to resolve backend-issued code AST atoms through the Arrow lookup instead of raw `Map.get(...)`
  - rewired `markdownWaterfallModel.ts` and `markdownWaterfallComponents.tsx` to resolve markdown section and rich-slot atoms through the Arrow lookup instead of repeated array scans
  - added focused regression coverage in `src/utils/__tests__/arrowRetrievalLookup.test.ts`
- **Validation Executed**:
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm test -- src/utils/__tests__/arrowRetrievalLookup.test.ts src/components/ZenSearch/__tests__/CodeAstAnatomyView.test.tsx src/components/panels/DirectReader/MarkdownWaterfall.test.tsx src/components/ZenSearch/__tests__/useZenSearchPreview.test.tsx src/components/ZenSearch/__tests__/zenSearchPreviewLoaders.test.ts'`
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm run build'`

### [DONE] Feature: Arrow IPC retrieval transport for Zen preview

- **Objective**: extend Arrow from local lookup tables to the network edge by delivering shared `RetrievalChunk[]` payloads over Arrow IPC while keeping the existing markdown/code analysis JSON responses stable.
- **Update**:
  - added `src/gateway/studio/router/retrieval_arrow.rs` to encode shared retrieval chunks as Arrow IPC streams in `xiuxian-wendao`
  - added `/api/analysis/markdown/retrieval-arrow` and `/api/analysis/code-ast/retrieval-arrow` sidecar endpoints
  - added `.data/wendao-frontend/src/api/arrowRetrievalIpc.ts` for Arrow IPC decode on the frontend
  - added client methods for markdown/code Arrow retrieval loading and wired `zenSearchPreviewLoaders.ts` to overlay Arrow-decoded chunks onto the existing JSON analysis payloads when available
  - added focused client and Zen preview regression coverage for the new Arrow transport path
- **Validation Executed**:
  - `direnv exec . cargo test -p xiuxian-wendao retrieval_arrow --lib`
  - `direnv exec . cargo test -p xiuxian-wendao gateway::studio::router::tests::code_ast --lib`
  - `direnv exec . cargo check -p xiuxian-wendao --lib --keep-going`
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm test -- src/api/client.test.ts src/components/ZenSearch/__tests__/useZenSearchPreview.test.tsx src/components/ZenSearch/__tests__/zenSearchPreviewLoaders.test.ts src/components/ZenSearch/__tests__/CodeAstAnatomyView.test.tsx src/components/panels/DirectReader/MarkdownWaterfall.test.tsx'`
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm run build'`

### [DONE] Feature: DataFusion cutover for reference-occurrence search

- **Objective**: move `reference_occurrence` off the old Lance scan path and onto the same local-epoch Parquet + DataFusion two-stage execution model already used by the other converted local corpora.
- **Update**:
  - changed `reference_occurrence` local epoch writes to export Parquet beside the Lance table
  - narrowed stage-1 projected columns to `id`, path/position, and `line_text`, then moved payload hydration to a second `id` lookup against the Parquet-backed engine table
  - kept the existing exact `name_folded` filter semantics and telemetry source while removing active-epoch query dependence on Lance table scanning
  - extended build/query regressions to verify the Parquet artifact exists and the published-epoch query path still returns the same payload shape
- **Validation Executed**:
  - `direnv exec . cargo check -p xiuxian-wendao --lib`
  - `direnv exec . cargo nextest run -p xiuxian-wendao -E 'test(reference_occurrence_query_reads_hits_from_published_epoch) | test(reference_occurrence_incremental_refresh_reuses_unchanged_rows) | test(plan_reference_occurrence_build_only_reparses_changed_files) | test(fingerprint_projects_changes_when_scanned_file_metadata_changes)'`
  - `direnv exec . git diff --check`

### [DONE] Feature: DataFusion cutover for local-symbol search

- **Objective**: move the last remaining local corpus, `local_symbol`, off the Lance scan path and onto the partition-aware Parquet + DataFusion two-stage execution model without collapsing the existing per-project partition layout.
- **Update**:
  - bumped `local_symbol` schema version to `3` so old Lance-only active epochs are rebuilt instead of being read by the new query path
  - changed each partition table write to emit a sibling parquet artifact and added shared `local_table_parquet_path(...)` helpers for partition-aware reads
  - rewired `search_local_symbols` and `autocomplete_local_symbols` to register each active partition parquet into the shared search engine, run narrow stage-1 scans across all active partition tables, and hydrate payloads by `(table_name, id)` in stage-2
  - extended build/query regressions to verify partition parquet artifacts exist after refresh and that published-epoch search plus autocomplete still behave correctly across multi-partition reads
- **Validation Executed**:
  - `direnv exec . cargo check -p xiuxian-wendao --lib`
  - `direnv exec . cargo nextest run -p xiuxian-wendao -E 'test(local_symbol_query_reads_hits_from_published_epoch) | test(local_symbol_query_can_rerank_across_multiple_tables) | test(local_symbol_autocomplete_reads_suggestions_from_published_epoch) | test(local_symbol_incremental_refresh_reuses_unchanged_rows) | test(local_symbol_build_writes_partitioned_epoch_tables_for_multiple_scopes) | test(plan_local_symbol_build_only_reparses_changed_files)'`
  - `direnv exec . git diff --check`

### [DONE] Feature: Gateway accept-loop overload hardening

- **Objective**: reduce transport-level `NetworkError` windows by hardening the gateway listener and turning studio-route overload into explicit HTTP backpressure instead of intermittent TCP refusal.
- **Update**:
  - reproduced the failure locally: `127.0.0.1:9517` stayed in `LISTEN`, but direct `curl` health probes intermittently failed with immediate connect refusal while the gateway process remained alive
  - observed large server send queues plus multiple frontend `rspack-node` connections to `9517`, which indicates accept/send pressure rather than a clean process crash
  - changed `gateway start` to bind through `tokio::net::TcpSocket` with an env-configurable listen backlog instead of the default `TcpListener::bind(...)`
  - added a studio-only overload layer with load shedding plus a global concurrency cap, so hot route pressure degrades to `503 GATEWAY_OVERLOADED` instead of starving the listener as easily
  - kept `/api/health`, `/api/stats`, and `/api/notify` outside that shedder so supervisor probes still have a lightweight path during studio-route saturation
- **Validation Executed**:
  - `direnv exec . cargo check -p xiuxian-wendao --bin wendao`
  - `direnv exec . cargo nextest run -p xiuxian-wendao -E 'test(test_gateway_server_bind) | test(test_gateway_listen_backlog_defaults_when_env_missing) | test(test_gateway_listen_backlog_accepts_positive_override) | test(test_gateway_listen_backlog_clamps_invalid_override) | test(test_gateway_studio_concurrency_limit_defaults_from_parallelism) | test(test_gateway_studio_concurrency_limit_accepts_positive_override) | test(test_gateway_studio_concurrency_limit_ignores_invalid_override) | test(test_health_endpoint_reports_process_id_header) | test(test_health_endpoint_accepts_owned_pidfile) | test(test_health_endpoint_rejects_mismatched_pidfile)'`

### [DONE] Feature: Remove dead Lance secondary-index builds after DataFusion cutover

- **Objective**: reduce gateway startup publication pressure now that all six search-plane corpora read from Parquet/DataFusion and no longer consume legacy Lance secondary indices.
- **Update**:
  - confirmed the runtime hypothesis remained consistent with publish-time CPU pressure rather than process churn: successful health probes kept returning the same gateway pid while mirrored stderr stayed panic-free
  - added an explicit corpus-level policy stating the DataFusion-cutover corpora no longer require legacy Lance secondary indices
  - removed post-write inverted/scalar index creation from `repo_entity`, `repo_content_chunk`, `knowledge_section`, `attachment`, `reference_occurrence`, and `local_symbol`, so publication now stops after table mutation plus parquet export instead of also building unused Lance read-side indices
  - narrowed touched schema helpers and re-exports so the cleanup stayed warning-free after those legacy hooks disappeared
- **Validation Executed**:
  - `direnv exec . cargo check -p xiuxian-wendao --lib`
  - `direnv exec . cargo nextest run -p xiuxian-wendao -E 'test(datafusion_cutover_corpora_no_longer_require_legacy_lance_indices) | test(repo_entity_incremental_refresh_reuses_unchanged_rows) | test(repo_content_chunk_incremental_refresh_reuses_unchanged_rows) | test(knowledge_section_incremental_refresh_reuses_unchanged_rows) | test(attachment_incremental_refresh_reuses_unchanged_rows) | test(reference_occurrence_incremental_refresh_reuses_unchanged_rows) | test(local_symbol_incremental_refresh_reuses_unchanged_rows) | test(local_symbol_build_writes_partitioned_epoch_tables_for_multiple_scopes)'`
  - `direnv exec . git diff --check`

### [DONE] Feature: Repo-backed publications go Parquet-only

- **Objective**: remove the remaining repo-backed Lance write pressure from gateway startup by making `repo_entity` and `repo_content_chunk` publish directly to parquet, while keeping repo search contracts unchanged.
- **Update**:
  - added a shared repo-publication parquet helper that rewrites repo publications from the previous parquet artifact plus changed Arrow batches, instead of cloning and mutating a Lance table
  - changed `repo_entity` and `repo_content_chunk` publish paths so `ReplaceAll` and `CloneAndMutate` now emit parquet directly, and `RefreshPublication` now inspects the existing parquet artifact instead of re-exporting from Lance
  - changed repo publication metadata recording to accept explicit parquet publication input, so repo manifests no longer depend on `TableInfo` from a Lance dataset when the publication format is parquet
  - changed maintenance prewarm to prefer registered parquet tables for both repo-backed and local corpora, with Lance scan fallback only when a parquet artifact is absent
  - disabled repo compaction scheduling for parquet publications, since the repo-backed read path no longer consumes Lance fragments
  - extended regression coverage to prove repo-backed publications no longer create `.lance` tables and that repo gateway / code-search paths still read the published parquet-backed corpora correctly
- **Validation Executed**:
  - `direnv exec . cargo check -p xiuxian-wendao --lib`
  - `direnv exec . cargo nextest run -p xiuxian-wendao -E 'test(repo_entity_incremental_refresh_reuses_unchanged_rows) | test(repo_content_chunk_incremental_refresh_reuses_unchanged_rows) | test(repo_publication_runs_prewarm_via_maintenance_task_and_releases_repo_slot) | test(repo_publication_does_not_skip_prewarm_when_slot_is_already_claimed) | test(repo_publication_keeps_compaction_disabled_for_parquet_publications) | test(repo_module_search_endpoint_returns_module_payload) | test(repo_symbol_search_endpoint_returns_symbol_payload) | test(repo_example_search_endpoint_returns_example_payload) | test(build_code_search_response_returns_repo_entity_hits_from_search_plane)'`
  - `direnv exec . git diff --check`

### [DONE] Feature: Gateway studio backpressure tightening

- **Objective**: reduce same-process `ECONNREFUSED` windows during heavy startup/indexing pressure by rejecting heavy studio traffic earlier instead of letting the listener and send queues overfill.
- **Update**:
  - reran direct health probes after restart and confirmed the failure mode stayed the same: the gateway kept the same process id while `curl http://127.0.0.1:9517/api/health` intermittently failed with immediate connect refusal
  - confirmed the latest restart no longer emitted fresh repo-backed Lance publication writes, so the next bottleneck was no longer legacy Lance mutation but the gateway's own acceptance/backpressure behavior under active indexing
  - tightened the default studio concurrency limit from `24 x available_parallelism` to `4 x available_parallelism`, with the clamp narrowed from `64..512` down to `32..128`
  - added `XIUXIAN_WENDAO_GATEWAY_STUDIO_REQUEST_TIMEOUT_SECS` with a default of `15s` and a `5..60s` clamp, so long studio requests now fail as `504 GATEWAY_TIMEOUT` instead of remaining in flight indefinitely
  - kept `/api/health`, `/api/stats`, and `/api/notify` outside that shedder/timeout envelope so lightweight process probes stay on the cheapest route
- **Validation Executed**:
  - `direnv exec . cargo check -p xiuxian-wendao --bin wendao`
  - `direnv exec . cargo nextest run -p xiuxian-wendao -E 'test(test_gateway_server_bind) | test(test_gateway_listen_backlog_defaults_when_env_missing) | test(test_gateway_listen_backlog_accepts_positive_override) | test(test_gateway_listen_backlog_clamps_invalid_override) | test(test_gateway_studio_concurrency_limit_defaults_from_parallelism) | test(test_gateway_studio_concurrency_limit_accepts_positive_override) | test(test_gateway_studio_concurrency_limit_ignores_invalid_override) | test(test_gateway_studio_concurrency_limit_clamps_large_override) | test(test_gateway_studio_request_timeout_defaults_when_env_missing) | test(test_gateway_studio_request_timeout_accepts_positive_override) | test(test_gateway_studio_request_timeout_clamps_invalid_override) | test(test_health_endpoint_reports_process_id_header) | test(test_health_endpoint_accepts_owned_pidfile) | test(test_health_endpoint_rejects_mismatched_pidfile)'`
  - `direnv exec . git diff --check`

### [DONE] Feature: Frontend dev-proxy root-cause fix for gateway `NetworkError`

- **Objective**: remove the real source of the observed browser-side `NetworkError when attempting to fetch resource` in local development instead of continuing to tune gateway pressure heuristics.
- **Update**:
  - reproduced a split signal after the latest gateway hardening: a reused single HTTP connection to `/api/health` stayed `30/30` green against one stable gateway pid, while repeated new-connection probes still failed intermittently with client-side `Can't assign requested address`
  - confirmed the latest gateway stderr no longer showed fresh repo-backed Lance publication writes, which ruled out the old repo publication path as the dominant remaining fault
  - traced the frontend dev stack to the Rspack proxy in `.data/wendao-frontend/scripts/rspack-build-environment.ts`
  - confirmed the underlying `http-proxy` default is `outgoing.agent = options.agent || false`, and when no agent is provided it forces `connection: close`; that means the current dev proxy was intentionally creating a fresh upstream socket for every `/api/*` request instead of reusing keep-alive connections
  - patched the Rspack dev proxy to use an explicit pooled `node:http.Agent` with keep-alive enabled plus bounded proxy/request timeouts, so `rspack-node` reuses gateway connections instead of driving avoidable local port churn
  - added focused frontend tests that pin the proxy agent pool shape and the updated dev-server proxy surface
- **Validation Executed**:
  - `direnv exec . bash -lc 'cd .data/wendao-frontend && npm run test -- src/build/rspackBuildEnvironment.test.ts'`
  - `direnv exec . git diff --check`

### [DONE] Feature: WendaoArrow typed Rust schema builders

- **Objective**: centralize the WendaoArrow `v1` Arrow request/response schema shape so Rust fixtures and validators stop rebuilding the contract from repeated field literals.
- **Update**:
  - promoted the Julia contract column-name constants in `analyzers::service::julia_transport` into an unconditional shared surface
  - added `julia_arrow_request_schema(...)` and `julia_arrow_response_schema(...)` so request builders, response fixtures, and downstream Julia transport tests can share one typed Arrow schema definition
  - fixed the public import boundary so the schema builders remain available even when `xiuxian-wendao` is compiled without the `julia` feature, while keeping request-row and decode helpers feature-gated
  - updated crate docs to describe the typed schema builder surface alongside the existing column-name constants
  - grouped WendaoArrow contract fixtures in both `xiuxian-wendao` and `xiuxian-wendao-julia` test modules under local `fixtures` submodules, so valid contract-shaped batches and explicit negative fixtures are separated by intent
  - moved the reusable `xiuxian-wendao-julia` contract-shaped test batches into `src/plugin/test_support/contract.rs`, so `plugin/transport.rs` now focuses on transport assertions and official-example process orchestration
  - moved the `xiuxian-wendao` Julia transport fixture batches into `src/analyzers/service/julia_transport_tests.rs`, so the main transport unit tests no longer carry an embedded fixture submodule
  - restored the missing `gateway/studio/router/handlers/repo/analysis/search/tests.rs` from the current `HEAD` baseline so `xiuxian-wendao` lib tests compile again while the WendaoArrow contract work continues
  - finished the milestone audit by removing the remaining repo-analysis fast-path lifetime mismatch: `run_repo_analysis_search` now accepts owned `String` inputs for the fast path, and `CachedRepositoryAnalysis` is cloneable where the fallback cache path needs to preserve ownership
- **Validation Executed**:
  - `direnv exec . cargo test -p xiuxian-wendao julia_arrow_request_schema_uses_contract_columns --lib`
  - `direnv exec . cargo test -p xiuxian-wendao-julia real_wendaoarrow_metadata_example_roundtrip_decodes_trace_id_column --lib`
  - `direnv exec . cargo check -p xiuxian-wendao --lib`
  - `direnv exec . cargo test -p xiuxian-wendao-julia process_julia_arrow_batches_against_real_wendaoarrow_service --lib`
  - `direnv exec . cargo test -p xiuxian-wendao decode_julia_arrow_score_rows_rejects_missing_columns --lib`
  - `direnv exec . cargo test -p xiuxian-wendao-julia transport --lib`
  - `direnv exec . julia --project=.data/WendaoArrow -e 'using Pkg; Pkg.test()'`
  - `direnv exec . cargo check -p xiuxian-vector --lib`
  - `direnv exec . cargo check -p xiuxian-wendao-julia --lib`
  - `direnv exec . git diff --check`

### [DONE] Feature: Repo import-search surface on the shared Phase-2 repo-analysis boundary

- **Objective**: land `repo/import-search` as a first-class repo-analysis query
  surface on the shared Wendao-owned Phase-2 orchestration boundary.
- **Update**:
  - added `repo/import-search` to Studio routing and OpenAPI route inventory
  - added `RepoImportSearchApiQuery` plus `required_import_search_filters(...)`
    so the route layer now rejects requests that omit both `package` and
    `module`
  - added `run_repo_import_search(...)` to the shared repo-analysis search
    service and wired it to the analyzer-owned `import_fallback_contract(...)`
  - normalized the touched import-search analyzer surface to `crate::...`
    imports instead of `use super::super`
  - added gateway-side request validation tests for missing `repo` and missing
    import filters
  - accepted snapshot baselines for analyzer import search output and the
    gateway-facing `repo/import-search` payload
- **Validation Executed**:
  - `direnv exec . cargo test -p xiuxian-wendao import_search_requires_ --lib`
  - `direnv exec . cargo test -p xiuxian-wendao import_search_snapshot_matches_package_and_module_filters --lib`
  - `direnv exec . cargo test -p xiuxian-wendao repo_import_search_payload_snapshot --lib`
  - `direnv exec . cargo check -p xiuxian-wendao`

### [DONE] Feature: Repo import-search cache identity hardening

- **Objective**: remove the degenerate `repo/import-search` cache-key shape so
  combined `package + module` filters keep distinct identity while staying
  inside the formal Phase-2 fallback-contract path.
- **Update**:
  - added
    `analyzers::service::search::contracts::canonical_import_query_text(...)`
    as the canonical import query-identity function
  - rewired `import_fallback_contract(...)` so cache identity now preserves
    both `package` and `module`, with stable wildcard placeholders when one
    side is absent
  - removed the route-local `repo_import_cache_query(...)` helper and routed
    `run_repo_import_search(...)` through the same fallback-only orchestration
    contract used by the other shared repo-analysis lanes
  - added targeted unit coverage for canonical import query identity and
    gateway-side import cache identity
  - cleaned up the touched import boundary so `cargo check` is warning-free
- **Validation Executed**:
  - `direnv exec . cargo test -p xiuxian-wendao canonical_import_query_text_ --lib`
  - `direnv exec . cargo test -p xiuxian-wendao import_search_cache_identity_uses_both_filters --lib`
  - `direnv exec . cargo test -p xiuxian-wendao import_search_requires_ --lib`
  - `direnv exec . cargo test -p xiuxian-wendao import_search_snapshot_matches_package_and_module_filters --lib`
  - `direnv exec . cargo test -p xiuxian-wendao repo_import_search_payload_snapshot --lib`
  - `direnv exec . cargo check -p xiuxian-wendao`

### [DONE] Feature: Repo import-search repo-entity fast path

- **Objective**: promote `repo/import-search` from a fallback-only Phase-2 lane
  into a repo-entity-backed fast path while preserving the analyzer fallback
  path for unpublished repositories.
- **Update**:
  - extended repo-entity row materialization so repo publication now includes
    import entities alongside modules, symbols, and examples
  - encoded import rows on the existing repo-entity column surface and added
    import-aware candidate scoring plus typed `ImportSearchResult`
    reconstruction in `search_plane::repo_entity::query`
  - added
    `query_core::query_repo_entity_import_results_if_published(...)` as the
    publication-gated typed import fast path
  - rewired `run_repo_import_search(...)` so it prefers repo-entity
    publication when ready and only falls back to analyzer-owned import search
    when publication is absent
  - accepted a new query-core import payload snapshot and added a gateway
    regression that proves import search can succeed from repo-entity
    publication alone, without repo-config fallback
- **Validation Executed**:
  - `direnv exec . cargo check -p xiuxian-wendao`
  - `direnv exec . cargo test -p xiuxian-wendao typed_repo_entity_search_reconstructs_module_symbol_and_example_results --lib`
  - `direnv exec . cargo test -p xiuxian-wendao repo_entity_query_core_import_payload_snapshot --lib`
  - `direnv exec . cargo test -p xiuxian-wendao repo_import_search_payload_snapshot --lib`
  - `direnv exec . cargo test -p xiuxian-wendao repo_import_search_uses_repo_entity_fast_path_when_publication_ready --lib`
  - `direnv exec . git diff --check`

### [DONE] Feature: WendaoAnalyzer generic service descriptor

- **Objective**: promote the analyzer launch surface from named example scripts
  to one generic service descriptor with explicit service mode.
- **Update**:
  - added `AnalyzerServiceDescriptor` and
    `analyzer_service_descriptor_from_args(...)` to `.data/WendaoAnalyzer`
  - added generic analyzer entrypoint
    `.data/WendaoAnalyzer/examples/analyzer_service.jl` and launcher
    `.data/WendaoAnalyzer/scripts/run_analyzer_service.sh`
  - kept the named stream/table scripts as compatibility wrappers over the new
    generic launcher
  - extended Rust integration support so `WendaoAnalyzerServiceContract`
    carries `service_mode` and launches the generic analyzer-service script
- **Validation Executed**:
  - `direnv exec . julia --project=.data/WendaoAnalyzer -e 'using Pkg; Pkg.test()'`
  - `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate test_planned_search_payload_applies_wendaoanalyzer_linear_blend_example`
  - `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate test_planned_search_payload_applies_wendaoanalyzer_similarity_only_strategy`
  - `direnv exec . cargo check -p xiuxian-wendao --tests`
  - `direnv exec . git diff --check`

### [DONE] Feature: WendaoAnalyzer runtime descriptor assembly

- **Objective**: make the analyzer service descriptor expressible from Rust
  runtime config instead of only hand-built inside test helpers.
- **Update**:
  - added `service_mode` and `analyzer_config_path` to
    `link_graph.retrieval.julia_rerank`
  - added `LinkGraphJuliaAnalyzerServiceDescriptor` plus
    `LinkGraphJuliaRerankRuntimeConfig::analyzer_service_descriptor()`
  - updated integration support so Rust tests can derive
    `WendaoAnalyzerServiceContract` from runtime-shaped config
  - added
    `.data/WendaoAnalyzer/config/link_graph_julia_rerank.example.toml`
    as a Rust-side sample descriptor
- **Validation Executed**:
  - `direnv exec . cargo test -p xiuxian-wendao test_retrieval_runtime_resolves_julia_rerank_config --lib`
  - `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate test_planned_search_payload_applies_wendaoanalyzer_similarity_only_strategy`
  - `direnv exec . cargo check -p xiuxian-wendao --tests`
  - `direnv exec . julia --project=.data/WendaoAnalyzer -e 'using Pkg; Pkg.test()'`
  - `direnv exec . git diff --check`
