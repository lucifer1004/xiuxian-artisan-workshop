# Daily Log - 2026-04-09

## Wendao Code Parser Modelica Grouped Import Guard

- continued the active native parser blueprint at
  `.cache/agent/blueprints/wendao_code_parser_flight_native_language_parsers.md`
  and advanced the active ExecPlan at
  `.cache/agent/execplans/wendao-code-parser-modelica-grouped-import-guard.md`
- landed a parser-owned grouped-import preflight guard in
  `.data/WendaoCodeParser.jl`, so unsupported Modelica grouped imports now
  return deterministic parser error rows with source location and statement
  context before the `OMParser.jl` bridge is called
- kept the slice bounded on purpose: named, qualified, and unqualified
  imports remain parser-owned supported shapes, while grouped imports are
  still rejected instead of being partially parsed or widened into Rust-side
  reconstruction
- synchronized package docs and mounted shared-service regressions so
  `.data/WendaoSearch.jl` now proves grouped imports fail without aborting the
  live Arrow Flight child
- focused validation for this slice is green:
  `direnv exec . julia --project=.data/WendaoCodeParser.jl --color=yes -e 'using Test, Tables, WendaoCodeParser; include(".data/WendaoCodeParser.jl/test/support/mod.jl"); include(".data/WendaoCodeParser.jl/test/cases/modelica_dependencies.jl"); include(".data/WendaoCodeParser.jl/test/cases/modelica_import_forms.jl"); include(".data/WendaoCodeParser.jl/test/cases/modelica_grouped_imports.jl"); include(".data/WendaoCodeParser.jl/test/cases/flight_modelica_import_forms.jl"); include(".data/WendaoCodeParser.jl/test/cases/flight_modelica_grouped_imports.jl")'`
  `direnv exec . julia --project=.data/WendaoSearch.jl --color=yes -e 'using Test, WendaoSearch; include(".data/WendaoSearch.jl/test/support/mod.jl"); include(".data/WendaoSearch.jl/test/integration/live_modelica_grouped_imports.jl")'`
  `direnv exec . git -C .data/WendaoCodeParser.jl diff --check`
  `direnv exec . git -C .data/WendaoSearch.jl diff --check`

## Testing-Gate Consumer Follow-Up Wave 35

- continued the active testing-gate blueprint at
  `.cache/agent/blueprints/testing_gate_full_policy_alignment.md` and advanced
  the active ExecPlan at
  `.cache/agent/execplans/testing-gate-auto-remediation-wave1.md`
- closed `xiuxian-wendao-runtime` as the next bounded consumer milestone:
  `src/lib.rs` now mounts the shared source-side gate, `tests/unit_test.rs`
  is the explicit root harness target, and `tests/unit/lib_policy.rs` owns the
  gate body for `cargo test --lib`
- externalized the remaining source-owned suites from `src/tests/**`,
  `src/transport/query_contract/tests/**`,
  `src/transport/plugin_arrow_exchange/tests.rs`,
  `src/config/test_support.rs`, and the last inline `#[cfg(test)]` blocks
  under `src/{artifacts,settings,transport,config}/**` onto canonical
  `tests/unit/...` mounts
- closed the real default-profile drift exposed by the stronger lane without
  widening the crate target surface: the existing
  `src/bin/wendao_flight_server.rs` now gates its transport-only
  implementation inside the source file, so `cargo test --test unit_test`
  stays green without introducing new Cargo target metadata
- focused validation for this slice is green:
  `direnv exec . cargo fmt --all`
  `direnv exec . cargo test -p xiuxian-wendao-runtime --lib`
  `direnv exec . cargo test -p xiuxian-wendao-runtime --lib xiuxian_test_policy_harness::enforce_crate_test_policy_harness -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao-runtime --test unit_test enforce_crate_test_policy_harness -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao-runtime --all-features`
  `direnv exec . cargo clippy -p xiuxian-wendao-runtime --lib --tests --all-features -- -D warnings`

## Wendao DuckDB Repo-Backed Gateway Read Engine

- continued the active bounded DuckDB blueprint at
  `.cache/agent/blueprints/wendao_duckdb_bounded_analytics.md` and closed the
  bounded ExecPlan at
  `.cache/agent/execplans/archives/wendao-duckdb-repo-gateway-read-engine.md`
- landed a first gateway-facing DuckDB cutover for repo-backed published
  Parquet reads without changing external Flight contracts:
  `src/duckdb/parquet.rs` now provides a bounded `ParquetQueryEngine` seam
  that selects DuckDB when `search.duckdb.enabled` is true in a `duckdb`
  build and otherwise preserves the current DataFusion fallback
- routed repo-backed `repo_entity` and `repo_content_chunk` query paths
  through the new Parquet query-engine seam, so these gateway-facing reads no
  longer depend directly on the shared `SearchEngineContext` path when DuckDB
  is enabled
- kept the slice bounded on purpose: local-corpus Lance writers, non-repo
  gateway handlers, and broader shared-query-core replacement remain future
  work under the same RFC and blueprint
- added focused DuckDB coverage for both engine selection and repo-backed
  gateway correctness, then verified the crate in both build modes:
  `direnv exec . cargo test -p xiuxian-wendao --lib duckdb::tests::configured_parquet_query_engine_uses_duckdb_when_enabled --features duckdb -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao --lib search::service::tests::publication::search::search_repo_entities_reads_hits_from_published_table_with_duckdb_repo_query_engine --features duckdb -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao --lib search::service::tests::publication::search::search_repo_content_chunks_with_filters_applies_sql_native_repo_filters_with_duckdb_repo_query_engine --features duckdb -- --exact`
  `direnv exec . cargo check -p xiuxian-wendao --features duckdb`
  `direnv exec . cargo check -p xiuxian-wendao`

## Wendao DuckDB Local Symbol Read Engine

- continued the same bounded DuckDB blueprint at
  `.cache/agent/blueprints/wendao_duckdb_bounded_analytics.md` and closed the
  bounded ExecPlan at
  `.cache/agent/execplans/archives/wendao-duckdb-local-symbol-read-engine.md`
- landed the next local-corpus gateway-facing DuckDB cutover for published
  `local_symbol` parquet reads: active-epoch table registration, search,
  autocomplete, and payload hydration now all reuse the bounded
  `ParquetQueryEngine` seam instead of reading directly from
  `SearchEngineContext`
- kept the slice bounded on purpose: Lance-backed local-symbol build
  ownership is still unchanged, and the unified in-memory symbol index behind
  `/search/symbols` remains a separate migration question
- focused validation for this slice is green:
  `direnv exec . cargo test -p xiuxian-wendao --lib search::local_symbol::query --features duckdb --quiet`
  `direnv exec . cargo test -p xiuxian-wendao --lib duckdb::tests::configured_parquet_query_engine_uses_duckdb_when_enabled --features duckdb -- --exact`
  `direnv exec . cargo check -p xiuxian-wendao --features duckdb`
  `direnv exec . cargo check -p xiuxian-wendao`
  `direnv exec . cargo clippy -p xiuxian-wendao --all-targets --features duckdb -- -D warnings`

## Wendao DuckDB Reference Occurrence Read Engine

- continued the same bounded DuckDB blueprint at
  `.cache/agent/blueprints/wendao_duckdb_bounded_analytics.md` and closed the
  bounded ExecPlan at
  `.cache/agent/execplans/archives/wendao-duckdb-reference-occurrence-read-engine.md`
- landed the next local-corpus gateway-facing DuckDB cutover for the
  published `reference_occurrence` lane behind `/search/references`: the
  active-epoch parquet file now registers through the bounded
  `ParquetQueryEngine`, and both the stage-one scan and payload hydration path
  no longer read directly from `SearchEngineContext`
- tightened the lane-specific SQL builder so engine-facing identifiers such as
  `column` are quoted consistently, keeping the same published parquet read
  path valid in both DataFusion and DuckDB
- kept the slice bounded on purpose: Lance-backed reference-occurrence build
  ownership remains unchanged, and other gateway handlers still belong to
  later DuckDB migration slices
- focused validation for this slice is green:
  `direnv exec . cargo test -p xiuxian-wendao --lib search::reference_occurrence::query --features duckdb --quiet`
  `direnv exec . cargo test -p xiuxian-wendao --lib duckdb::tests::configured_parquet_query_engine_uses_duckdb_when_enabled --features duckdb -- --exact`
  `direnv exec . cargo check -p xiuxian-wendao --features duckdb`
  `direnv exec . cargo check -p xiuxian-wendao`
  `direnv exec . cargo clippy -p xiuxian-wendao --all-targets --features duckdb -- -D warnings`

## Wendao DuckDB Attachment Read Engine

- continued the same bounded DuckDB blueprint at
  `.cache/agent/blueprints/wendao_duckdb_bounded_analytics.md` and closed the
  bounded ExecPlan at
  `.cache/agent/execplans/archives/wendao-duckdb-attachment-read-engine.md`
- landed the next local-corpus gateway-facing DuckDB cutover for the
  published `attachment` lane behind `/search/attachments`: the active-epoch
  parquet file now registers through the bounded `ParquetQueryEngine`, and
  both the stage-one scan and payload hydration path no longer read directly
  from `SearchEngineContext`
- tightened the lane-specific SQL builder so engine-facing identifiers and
  table names are quoted consistently, keeping the same published parquet read
  path valid in both DataFusion and DuckDB
- kept the slice bounded on purpose: Lance-backed attachment build ownership
  remains unchanged, and other gateway handlers still belong to later DuckDB
  migration slices
- focused validation for this slice is green:
  `direnv exec . cargo test -p xiuxian-wendao --lib search::attachment::query --features duckdb --quiet`
  `direnv exec . cargo test -p xiuxian-wendao --lib duckdb::tests::configured_parquet_query_engine_uses_duckdb_when_enabled --features duckdb -- --exact`
  `direnv exec . cargo check -p xiuxian-wendao --features duckdb`
  `direnv exec . cargo check -p xiuxian-wendao`
  `direnv exec . cargo clippy -p xiuxian-wendao --all-targets --features duckdb -- -D warnings`

## Wendao DuckDB Knowledge Read Engine

- continued the same bounded DuckDB blueprint at
  `.cache/agent/blueprints/wendao_duckdb_bounded_analytics.md` and closed the
  bounded ExecPlan at
  `.cache/agent/execplans/archives/wendao-duckdb-knowledge-read-engine.md`
- landed the next local-corpus gateway-facing DuckDB cutover for the
  published `knowledge_section` lane behind the gateway knowledge search path:
  the active-epoch parquet file now registers through the bounded
  `ParquetQueryEngine`, and both the stage-one scan and payload hydration path
  no longer read directly from `SearchEngineContext`
- tightened the lane-specific SQL builder so engine-facing identifiers and
  table names are quoted consistently, keeping the same published parquet read
  path valid in both DataFusion and DuckDB
- kept the slice bounded on purpose: knowledge intent/source merge
  orchestration and Lance-backed knowledge build ownership remain unchanged,
  and other gateway handlers still belong to later DuckDB migration slices
- focused validation for this slice is green:
  `direnv exec . cargo test -p xiuxian-wendao --lib search::knowledge_section::query --features duckdb --quiet`
  `direnv exec . cargo test -p xiuxian-wendao --lib duckdb::tests::configured_parquet_query_engine_uses_duckdb_when_enabled --features duckdb -- --exact`
  `direnv exec . cargo check -p xiuxian-wendao --features duckdb`
  `direnv exec . cargo check -p xiuxian-wendao`
  `direnv exec . cargo clippy -p xiuxian-wendao --all-targets --features duckdb -- -D warnings`

## Wendao DuckDB Local Symbol Build Owner Cutover

- continued the same bounded DuckDB blueprint at
  `.cache/agent/blueprints/wendao_duckdb_bounded_analytics.md` and closed the
  bounded ExecPlan at
  `.cache/agent/execplans/archives/wendao-duckdb-local-symbol-build-owner-cutover.md`
- landed the next bounded storage-owner slice for `local_symbol`: the corpus
  build owner now rewrites published partition tables directly to Parquet
  instead of cloning and mutating Lance tables, so the already-migrated
  local-symbol query and gateway routes keep reading the same published
  contract without fresh `local_symbol` `.lance` tables being created
- tightened local epoch discovery to be Parquet-first with legacy `.lance`
  fallback and removed `local_symbol` from local Lance compaction scheduling,
  which keeps the bounded writer cut consistent with the already-landed
  DuckDB-fed read lanes
- tightened local-symbol test fixtures to publish through the same bounded
  Parquet write path and added focused build assertions that fresh
  `local_symbol` builds leave no `.lance` tables behind
- focused validation for this slice is green:
  `direnv exec . cargo test -p xiuxian-wendao --lib search::local_symbol::build --features duckdb --quiet`
  `direnv exec . cargo test -p xiuxian-wendao --lib search::local_symbol::query --features duckdb --quiet`
  `direnv exec . cargo test -p xiuxian-wendao --lib search_symbols --features duckdb --quiet`
  `direnv exec . cargo test -p xiuxian-wendao --lib gateway::studio::search::handlers::flight::tests::provider::studio_search_flight_provider_dispatches_symbol_route --features duckdb -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao --lib duckdb::tests::configured_parquet_query_engine_uses_duckdb_when_enabled --features duckdb -- --exact`
  `direnv exec . cargo check -p xiuxian-wendao --features duckdb`
  `direnv exec . cargo check -p xiuxian-wendao`
  `direnv exec . cargo clippy -p xiuxian-wendao --all-targets --features duckdb -- -D warnings`

## Wendao DuckDB Reference Occurrence Build Owner Cutover

- continued the same bounded DuckDB blueprint at
  `.cache/agent/blueprints/wendao_duckdb_bounded_analytics.md` and closed the
  bounded ExecPlan at
  `.cache/agent/execplans/archives/wendao-duckdb-reference-occurrence-build-owner-cutover.md`
- landed the next bounded storage-owner slice for `reference_occurrence`: the
  corpus build owner now rewrites the published table directly to Parquet
  instead of cloning and mutating a Lance table, so the already-landed
  `/search/references` DuckDB read lane keeps the same published contract
- removed `reference_occurrence` from local Lance compaction ownership so the
  maintenance state now matches the new Parquet-only build owner
- tightened the `reference_occurrence` query fixture to publish through the
  same published Parquet shape and added a focused build assertion that fresh
  `reference_occurrence` builds leave no `.lance` tables behind
- focused validation for this slice is green:
  `direnv exec . cargo test -p xiuxian-wendao --lib search::reference_occurrence::build --features duckdb --quiet`
  `direnv exec . cargo test -p xiuxian-wendao --lib search::reference_occurrence::query --features duckdb --quiet`
  `direnv exec . cargo test -p xiuxian-wendao --lib duckdb::tests::configured_parquet_query_engine_uses_duckdb_when_enabled --features duckdb -- --exact`
  `direnv exec . cargo check -p xiuxian-wendao --features duckdb`
  `direnv exec . cargo check -p xiuxian-wendao`
  `direnv exec . cargo clippy -p xiuxian-wendao --all-targets --features duckdb -- -D warnings`

## Testing-Gate Consumer Follow-Up Wave 36

- continued the active testing-gate blueprint at
  `.cache/agent/blueprints/testing_gate_full_policy_alignment.md` and advanced
  the active ExecPlan at
  `.cache/agent/execplans/testing-gate-auto-remediation-wave1.md`
- closed `xiuxian-daochang` as the next bounded consumer milestone:
  `src/lib.rs` now mounts the shared source-side gate,
  `tests/unit/lib_policy.rs` owns the `cargo test --lib` policy body, and
  `tests/unit_test.rs` is the explicit root harness target
- moved the former root-scattered `tests/*.rs` surface under canonical
  `tests/unit/**`, added the root module aggregator there, and externalized
  the remaining source-owned inline suites from
  `src/jobs/manager/types.rs`,
  `src/agent/tool_dispatch/helpers.rs`,
  `src/agent/bootstrap/native_tools.rs`,
  `src/agent/system_prompt_injection_state.rs`,
  `src/runtime_agent_factory/tools.rs`,
  `src/channels/telegram/channel/markdown/escape.rs`,
  `src/channels/telegram/runtime/dispatch/preview.rs`, and
  `src/tool_runtime/bridge.rs`
- while validating the stronger lane, closed the real harness drift inside the
  migrated suite:
  `tests/unit/agent_suite.rs` and `tests/unit/channels_telegram_media.rs` now
  mount their shared support trees with explicit `#[path = ...]`, the
  `session_redis` root-compatibility harness now exposes the minimum root
  aliases required by the migrated source includes, and the moved
  `system_prompt_injection_state` roundtrip test now matches the live
  `Option` contract
- validation also surfaced two upstream `xiuxian-wendao` compile drifts in the
  new `ParquetQueryEngine` seam; this wave closed them in place by switching
  the stale `ensure_parquet_table_registered(..., &[])` and
  `sql_batches(...)` call sites to the current bounded API and by making
  `ParquetQueryEngine::configured(...)` feature-aware so the non-`duckdb`
  clippy lane no longer trips `unnecessary_wraps`
- focused gate validation for this slice is green:
  `direnv exec . cargo test -p xiuxian-daochang --lib xiuxian_test_policy_harness::enforce_crate_test_policy_harness -- --exact`
  `direnv exec . cargo test -p xiuxian-daochang --test unit_test enforce_crate_test_policy_harness -- --exact`
  `direnv exec . cargo test -p xiuxian-daochang --test unit_test unit::channels_telegram_media::telegram_media_group_uses_send_media_group_for_multi_url_payload -- --exact`
  `direnv exec . cargo test -p xiuxian-daochang --test unit_test unit::agent_session_context_unit::tests::enforce_session_reset_policy_resets_stale_unbounded_session_and_injects_notice -- --exact`
- strict clippy for `xiuxian-daochang` is still blocked by broader pre-existing
  crate-local debt outside the harness migration, most visibly in
  `src/agent/feedback.rs`,
  `src/channels/telegram/channel/acl/settings.rs`,
  `src/tool_runtime/bridge.rs`,
  `src/test_support/bootstrap.rs`, and
  `src/session/summary.rs`

## Testing-Gate Consumer Follow-Up Wave 37

- continued the same testing-gate blueprint at
  `.cache/agent/blueprints/testing_gate_full_policy_alignment.md` and the same
  active ExecPlan at
  `.cache/agent/execplans/testing-gate-auto-remediation-wave1.md`, but stayed
  strictly inside one post-gate clippy cluster instead of widening the
  consumer surface again
- closed the first bounded strict-clippy convergence slice inside
  `xiuxian-daochang` by fixing the remaining diagnostics in
  `src/agent/feedback.rs` and
  `src/test_support/{bootstrap,memory_recall_metrics,reflection}.rs`
- that slice added the missing `# Errors` contract to `clear_session`,
  converted no-op async helpers back to synchronous helpers, switched
  `Option` guards in the recall-feedback path to the direct `?` form, removed
  the precision-loss casts in recall metrics and feedback logging, marked the
  pure bootstrap helpers as `#[must_use]`, and changed the small
  `ReflectiveRuntime::stage` getter to pass `self` by value instead of by
  reference
- focused validation for this clippy slice is green:
  `direnv exec . cargo test -p xiuxian-daochang --test unit_test agent_memory_recall_metrics::`
  `direnv exec . cargo test -p xiuxian-daochang --test unit_test agent_reflection_unit::`
  `direnv exec . cargo test -p xiuxian-daochang --test unit_test unit::session_summary::clear_session_removes_summary_segments -- --exact`
- a filtered strict-clippy rerun for this exact cluster now returns no matches,
  so the failure surface has moved forward to the next real debt owners:
  `tests/unit/agent/system_prompt_injection_state/mod.rs`,
  `src/agent/memory_recall_metrics.rs`,
  `src/agent/reflection/lifecycle.rs`, and the broader telegram/discord
  runtime cluster

## Testing-Gate Consumer Follow-Up Wave 38

- continued the same bounded `xiuxian-daochang` post-gate lane and closed the
  next agent-side strict-clippy cluster in
  `src/agent/memory_recall_metrics.rs`,
  `src/agent/reflection/lifecycle.rs`, and
  `tests/unit/agent/system_prompt_injection_state/mod.rs`
- that slice removed the remaining precision-loss conversion in
  `memory_recall_metrics`, changed the tiny reflection lifecycle getter to
  pass `self` by value, and simplified the source-backed XML test literal so
  the moved suite no longer trips `needless_raw_string_hashes`
- focused validation for this slice is green:
  `direnv exec . cargo test -p xiuxian-daochang --test unit_test agent_memory_recall_metrics::`
  `direnv exec . cargo test -p xiuxian-daochang --test unit_test agent_reflection_unit::`
  `direnv exec . cargo test -p xiuxian-daochang --lib agent::system_prompt_injection_state::tests::normalization_counts_qa_entries_and_renders_canonical_xml -- --exact`
- a fresh filtered strict-clippy rerun for those exact owners now returns no
  matches, and the crate-level head surface has moved fully into the next
  real runtime/channel debt group:
  `src/channels/telegram/channel/acl/settings.rs`,
  `tests/unit/agent/bootstrap/native_tools.rs`,
  `src/agent/tool_dispatch/helpers.rs`,
  `src/channels/discord/runtime/managed/replies/memory/runtime_status/json.rs`,
  and the broader telegram channel cluster

## Testing-Gate Consumer Follow-Up Wave 39

- continued the same bounded `xiuxian-daochang` post-gate lane and closed the
  next runtime/helper strict-clippy cluster under
  `tests/unit/agent/bootstrap/native_tools.rs`,
  `src/agent/tool_dispatch/helpers.rs`,
  `src/channels/discord/runtime/managed/replies/memory/runtime_status/json.rs`,
  `src/channels/discord/runtime/managed/replies/memory/snapshot/{available,not_found}.rs`,
  and `src/channels/discord/send.rs`
- that slice removed the remaining `assigning_clones` / pass-by-value lint in
  the tool-dispatch and discord runtime-status helpers, made the
  runtime-status JSON formatter borrow the snapshot instead of consuming it,
  added `#[must_use]` to the discord message splitter, and aligned the
  source-backed native-tools fixtures with the live internal-authority
  contract by using `SKILL.md` frontmatter `tags` plus valid TOML manifests
- the same slice also corrected one stale migrated assertion:
  `agenda.view` remains present as a base native tool even when the matching
  alias mount is rejected, so the suite now checks that the unauthorized alias
  is skipped without pretending the base tool disappears
- focused validation for this slice is green:
  `direnv exec . cargo test -p xiuxian-daochang --lib agent::tool_dispatch::helpers::tests::llm_tool_definitions_maps_description_and_schema -- --exact`
  `direnv exec . cargo test -p xiuxian-daochang --test unit_test unit::channels_discord_send::split_message_for_discord_handles_zero_limit -- --exact`
  `direnv exec . cargo test -p xiuxian-daochang --lib agent::bootstrap::native_tools::tests::blocks_alias_mount_on_ghost_links -- --exact`
  `direnv exec . cargo test -p xiuxian-daochang --lib agent::bootstrap::native_tools::tests::mounts_authorized_aliases_and_skips_unauthorized -- --exact`
  `direnv exec . cargo clippy -p xiuxian-daochang --lib --tests --message-format short -- -D warnings 2>&1 | rg '^packages/rust/crates/xiuxian-daochang/(tests/unit/agent/bootstrap/native_tools\\.rs|src/agent/tool_dispatch/helpers\\.rs|src/channels/discord/runtime/managed/replies/memory/runtime_status/json\\.rs|src/channels/discord/runtime/managed/replies/memory/snapshot/(available|not_found)\\.rs|src/channels/discord/send\\.rs)'` (no matches; `rg` exited 1)
- one extra telegram-runtime proof attempted during the slice still fails on a
  pre-existing behavior path outside this cluster:
  `unit::channels_telegram_runtime_unit::tests::session_memory::runtime_handle_inbound_session_memory_without_snapshot_reports_json`
  still forwards into the foreground queue instead of staying in the JSON
  reply lane

## Testing-Gate Consumer Follow-Up Wave 40

- continued the same bounded `xiuxian-daochang` post-gate lane and closed the
  next telegram channel utility strict-clippy cluster under
  `src/channels/telegram/channel/acl/settings.rs`,
  `src/channels/telegram/channel/acl_overrides.rs`,
  `src/channels/telegram/channel/chunking.rs`,
  `src/channels/telegram/channel/error.rs`,
  `src/channels/telegram/channel/group_policy.rs`,
  `src/channels/telegram/channel/listen.rs`,
  `src/channels/telegram/channel/markdown/markdown_v2.rs`,
  `src/channels/telegram/channel/outbound_text.rs`,
  `src/channels/telegram/channel/recipient_admin.rs`, and
  `src/channels/telegram/channel/send_api/{media,request}.rs`
- that slice split the overgrown ACL and polling helpers into smaller
  stages, derived the default group-policy mode instead of hand-writing the
  impl, replaced `map(...).unwrap_or_else(...)` / clone-assignment patterns
  with the stricter forms Clippy expects, switched `TelegramApiError` to a
  borrowed reqwest adapter, and tightened the Markdown/text extraction helpers
  without changing their public surface
- the decisive outcome is that this entire telegram utility cluster no longer
  appears on the crate-level strict-clippy surface; the head surface has moved
  forward to `session_admin_persistence`, `trait_impl`, and the telegram
  runtime loop-control owners instead
- focused validation for this slice is green:
  `direnv exec . cargo test -p xiuxian-daochang --test unit_test unit::channels_telegram_chunking::split_message_prefers_markdown_ast_block_boundary_even_when_newline_is_early -- --exact`
  `direnv exec . cargo test -p xiuxian-daochang --test unit_test unit::channels_telegram_polling::telegram_listen_respects_retry_after_on_rate_limit -- --exact`
  `direnv exec . cargo test -p xiuxian-daochang --test unit_test unit::channels_telegram_tool_result_render::telegram_send_extracts_markdown_from_json_content_string -- --exact`
  `direnv exec . cargo test -p xiuxian-daochang --test unit_test unit::channels_telegram::telegram_send_retries_on_rate_limit_and_succeeds -- --exact`
  `direnv exec . cargo clippy -p xiuxian-daochang --lib --tests --message-format short -- -D warnings 2>&1 | rg '^packages/rust/crates/xiuxian-daochang/src/channels/telegram/channel/(acl/settings|acl_overrides|chunking|error|group_policy|listen|markdown/markdown_v2|outbound_text|recipient_admin|send_api/media|send_api/request)\\.rs'` (no matches; `rg` exited 1)
- validation also exposed two pre-existing behavior drifts that remain outside
  this lint slice and should be handled separately:
  `unit::telegram_acl_overrides::{telegram_acl_overrides_build_from_structured_acl,telegram_acl_overrides_reject_invalid_control_rule_with_field_path}`
  currently see empty ACL overrides from the YAML fixture path, and
  `unit::channels_telegram::telegram_send_falls_back_to_html_on_markdown_api_error_with_http_200`
  currently emits a trailing newline in the fallback HTML payload

## Wendao DuckDB Intent Composition Proof

- continued the active DuckDB bounded-analytics blueprint at
  `.cache/agent/blueprints/wendao_duckdb_bounded_analytics.md` and opened a
  bounded ExecPlan at
  `.cache/agent/execplans/wendao-duckdb-intent-composition-proof.md`, but kept
  the slice explicitly scoped to `/search/intent` composition proof instead of
  widening into a new read-engine cutover
- landed additive internal transport metadata for the gateway intent route in
  `src/gateway/studio/search/handlers/knowledge/intent/`, so the route now
  records source-lane query-engine labels for `knowledge_section`,
  `local_symbol`, and repo-intent while preserving the existing repo-content
  Flight transport marker
- kept the public contract unchanged on purpose: `/search/intent` still
  aggregates already-migrated source lanes, and this slice did not change
  response shape, cache shape, or merge behavior
- added focused proofs in
  `tests/unit/gateway/studio/search/handlers/intent.rs` and
  `tests/unit/gateway/studio/search/handlers/knowledge/intent/flight.rs` that
  verify DuckDB-fed local and repo source composition through both the direct
  handler metadata path and the Flight-backed route
- focused validation is green so far:
- focused validation for the landed slice is green:
  `direnv exec . cargo test -p xiuxian-wendao --lib gateway::studio::search::handlers::tests::intent::load_intent_search_response_reports_duckdb_local_source_query_engines --features duckdb -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao --lib gateway::studio::search::handlers::tests::intent::load_intent_search_response_reports_duckdb_repo_query_engine --features duckdb -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao --lib gateway::studio::search::handlers::knowledge::intent::flight::tests::studio_intent_flight_provider_reads_local_duckdb_fed_hits --features duckdb -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao --lib duckdb::tests::configured_parquet_query_engine_uses_duckdb_when_enabled --features duckdb -- --exact`
  `direnv exec . cargo check -p xiuxian-wendao --features duckdb`
  `direnv exec . cargo check -p xiuxian-wendao`
  `direnv exec . cargo clippy -p xiuxian-wendao --all-targets --features duckdb -- -D warnings`
- closed the ExecPlan for this slice and archived it at
  `.cache/agent/execplans/archives/wendao-duckdb-intent-composition-proof.md`

## Wendao DuckDB Symbols Read Engine

- continued the active DuckDB bounded-analytics blueprint at
  `.cache/agent/blueprints/wendao_duckdb_bounded_analytics.md` and opened a
  bounded ExecPlan at
  `.cache/agent/execplans/wendao-duckdb-symbols-read-engine.md`, keeping the
  slice explicitly scoped to `/search/symbols` rather than widening into
  local-corpus writer removal
- landed the `/search/symbols` read-engine cutover in
  `src/gateway/studio/search/handlers/symbols/`, so the route now starts and
  reads from the published `local_symbol` search plane instead of querying
  `UnifiedSymbolIndex::search_unified(...)`
- kept the public contract stable on purpose: the handler still returns the
  existing `SymbolSearchHit` payload, keeps the partial response shape while
  no published local-symbol epoch exists, and filters the broader
  `local_symbol` corpus back down to code-symbol results so the old route
  semantics remain bounded
- updated focused gateway and Flight fixtures in
  `tests/unit/gateway/studio/search.rs` and
  `tests/unit/gateway/studio/search/handlers/flight/{fixtures,provider}.rs`
  so the symbol route now proves DuckDB-fed reads without warming the old
  in-memory symbol index
- focused validation for the landed slice is green:
  `direnv exec . cargo test -p xiuxian-wendao --lib search_symbols --features duckdb --quiet`
  `direnv exec . cargo test -p xiuxian-wendao --lib gateway::studio::search::handlers::flight::tests::provider::studio_search_flight_provider_dispatches_symbol_route --features duckdb -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao --lib duckdb::tests::configured_parquet_query_engine_uses_duckdb_when_enabled --features duckdb -- --exact`
  `direnv exec . cargo check -p xiuxian-wendao --features duckdb`
  `direnv exec . cargo check -p xiuxian-wendao`
  `direnv exec . cargo clippy -p xiuxian-wendao --all-targets --features duckdb -- -D warnings`
- broad filtered `gateway::studio::search` test execution was attempted during
  this slice, but it still surfaces the pre-existing repo-search bootstrap
  fixture failures under
  `gateway::studio::search::handlers::flight::repo_search::tests::{bootstrap_sample_repo_search_content_publishes_queryable_rows,bootstrap_sample_repo_search_content_respects_query_and_limit}`;
  those failures are outside this `/search/symbols` cut and were not used as
  the landing gate
- closed the ExecPlan for this slice and archived it at
  `.cache/agent/execplans/archives/wendao-duckdb-symbols-read-engine.md`

## Wendao DuckDB Attachment Build Owner Cutover

- continued the active DuckDB bounded-analytics blueprint at
  `.cache/agent/blueprints/wendao_duckdb_bounded_analytics.md` and opened a
  bounded ExecPlan at
  `.cache/agent/execplans/wendao-duckdb-attachment-build-owner-cutover.md`,
  keeping the slice explicitly scoped to `attachment` build ownership instead
  of widening into the remaining local-corpus writers
- landed the `attachment` build-owner cutover in
  `src/search/attachment/build/write.rs`, so the corpus now rewrites its
  published table directly to Parquet through the bounded
  `local_publication_parquet` helper instead of cloning and mutating a Lance
  table before exporting
- removed `attachment` from local Lance compaction ownership in
  `src/search/corpus.rs`, and tightened focused build/query fixtures so this
  corpus now proves the same published read contract without leaving behind
  fresh `attachment` `.lance` tables
- focused validation for the landed slice is green:
  `direnv exec . cargo test -p xiuxian-wendao --lib search::attachment::build --features duckdb --quiet`
  `direnv exec . cargo test -p xiuxian-wendao --lib search::attachment::query --features duckdb --quiet`
  `direnv exec . cargo test -p xiuxian-wendao --lib duckdb::tests::configured_parquet_query_engine_uses_duckdb_when_enabled --features duckdb -- --exact`
  `direnv exec . cargo check -p xiuxian-wendao --features duckdb`
  `direnv exec . cargo check -p xiuxian-wendao`
  `direnv exec . cargo clippy -p xiuxian-wendao --all-targets --features duckdb -- -D warnings`
- closed the ExecPlan for this slice and archived it at
  `.cache/agent/execplans/archives/wendao-duckdb-attachment-build-owner-cutover.md`

## Wendao DuckDB Knowledge Build Owner Cutover

- continued the active DuckDB bounded-analytics blueprint at
  `.cache/agent/blueprints/wendao_duckdb_bounded_analytics.md` and opened a
  bounded ExecPlan at
  `.cache/agent/execplans/wendao-duckdb-knowledge-build-owner-cutover.md`,
  keeping the slice explicitly scoped to `knowledge_section` build ownership
  instead of widening into workflow or gateway orchestration changes
- landed the `knowledge_section` build-owner cutover in
  `src/search/knowledge_section/build/write.rs`, so the corpus now rewrites
  its published table directly to Parquet through the bounded
  `local_publication_parquet` helper instead of cloning and mutating a Lance
  table before exporting
- removed `knowledge_section` from local Lance compaction ownership in
  `src/search/corpus.rs`, added a no-fresh-`.lance` proof in
  `tests/unit/search/knowledge_section/build/mod.rs`, and reduced the old
  `staged_mutation` helper surface to test-only because no production writer
  still uses it
- focused validation for the landed slice is green:
  `direnv exec . cargo test -p xiuxian-wendao --lib search::knowledge_section::build --features duckdb --quiet`
  `direnv exec . cargo test -p xiuxian-wendao --lib search::knowledge_section::query --features duckdb --quiet`
  `direnv exec . cargo test -p xiuxian-wendao --lib duckdb::tests::configured_parquet_query_engine_uses_duckdb_when_enabled --features duckdb -- --exact`
  `direnv exec . cargo check -p xiuxian-wendao --features duckdb`
  `direnv exec . cargo check -p xiuxian-wendao`
  `direnv exec . cargo clippy -p xiuxian-wendao --all-targets --features duckdb -- -D warnings`
- closed the ExecPlan for this slice and archived it at
  `.cache/agent/execplans/archives/wendao-duckdb-knowledge-build-owner-cutover.md`

## Wendao DuckDB Status Diagnostics Rollup

- continued the active DuckDB bounded-analytics blueprint at
  `.cache/agent/blueprints/wendao_duckdb_bounded_analytics.md` and opened a
  bounded ExecPlan at
  `.cache/agent/execplans/wendao-duckdb-status-diagnostics-rollup.md`,
  keeping the slice explicitly scoped to the Studio search-index status route
  instead of widening into broader repo/runtime analytics or Qianji
- landed the bounded diagnostics helper in
  `src/gateway/studio/types/search_index/diagnostics.rs`, so the Studio
  search-index status route now computes top-level totals, phase counts,
  `compactionPending`, and the aggregate maintenance summary through the local
  relation-engine seam instead of pure ad-hoc Rust traversal
- kept the public `SearchIndexStatusResponse` contract stable on purpose:
  per-corpus rows, status reason, query telemetry summary, and repo-read
  pressure still follow the existing Rust path, and the route falls back to
  the existing Rust summary if local diagnostics execution fails
- wired the route through
  `src/gateway/studio/router/state/graph.rs` and added focused proof in
  `tests/unit/gateway/studio/types/search_index/diagnostics.rs`, while the
  existing gateway route proof in `tests/integration/studio_search_index_api.rs`
  now serves as the route-level bounded diagnostics check through the
  `xiuxian-testing-gate` harness
- focused validation for the landed slice is green:
  `direnv exec . cargo test -p xiuxian-wendao --lib gateway::studio::types::search_index --features duckdb --quiet`
  `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate --features "zhenfa-router duckdb" studio_search_index_api::search_index_status_endpoint_returns_idle_corpora_snapshot -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao --lib duckdb::tests::configured_parquet_query_engine_uses_duckdb_when_enabled --features duckdb -- --exact`
  `direnv exec . cargo check -p xiuxian-wendao --features duckdb`
  `direnv exec . cargo check -p xiuxian-wendao`
  `direnv exec . cargo clippy -p xiuxian-wendao --all-targets --features duckdb -- -D warnings`
- closed the ExecPlan for this slice and archived it at
  `.cache/agent/execplans/archives/wendao-duckdb-status-diagnostics-rollup.md`

## Wendao DuckDB Local Parquet-Only Epoch Discovery

- continued the active DuckDB bounded-analytics blueprint at
  `.cache/agent/blueprints/wendao_duckdb_bounded_analytics.md` and opened a
  bounded ExecPlan at
  `.cache/agent/execplans/wendao-duckdb-local-parquet-only-epoch-discovery.md`,
  keeping the slice explicitly scoped to local epoch discovery and local
  prewarm instead of widening into repo maintenance or vector-store changes
- landed the Parquet-only discovery tightening in
  `src/search/service/core/construction/paths.rs`, so local epoch discovery
  now ignores legacy `.lance` artifacts and only observes Parquet
  publications for local search-plane corpora
- landed the matching prewarm tightening in
  `src/search/service/core/maintenance/local.rs`, so local prewarm now
  rejects missing Parquet publications with `TableNotFound` instead of
  falling back to opening a local store scan
- added focused proof in
  `tests/unit/search/service/core/construction/mod.rs` and
  `tests/unit/search/service/maintenance.rs`, proving both that stale local
  `.lance` directories are ignored and that missing local Parquet epochs no
  longer pass silently through prewarm
- focused validation for the landed slice is green:
  `direnv exec . cargo test -p xiuxian-wendao --lib construction --features duckdb --quiet`
  `direnv exec . cargo test -p xiuxian-wendao --lib search::service::tests::maintenance::prewarm_epoch_table_rejects_missing_local_parquet_publication --features duckdb -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao --lib search::local_symbol::build --features duckdb --quiet`
  `direnv exec . cargo check -p xiuxian-wendao --features duckdb`
  `direnv exec . cargo check -p xiuxian-wendao`
  `direnv exec . cargo clippy -p xiuxian-wendao --all-targets --features duckdb -- -D warnings`
- closed the ExecPlan for this slice and archived it at
  `.cache/agent/execplans/archives/wendao-duckdb-local-parquet-only-epoch-discovery.md`

## Wendao DuckDB Retire Local Compaction Runtime

- continued the active DuckDB bounded-analytics blueprint at
  `.cache/agent/blueprints/wendao_duckdb_bounded_analytics.md` and opened a
  bounded ExecPlan at
  `.cache/agent/execplans/wendao-duckdb-retire-local-compaction-runtime.md`,
  keeping the slice explicitly scoped to dead local maintenance runtime
  retirement instead of widening into repo maintenance or public status
  schema changes
- retired the dead local compaction runtime in
  `src/search/service/core/{types,status/compaction.rs,status/runtime.rs,maintenance/local.rs}`
  so Wendao no longer ships a local compaction queue or worker state for
  search-plane corpora, while `publish_ready_and_maintain(...)` now performs
  a pure local publish step
- removed the matching dead coordinator and test debt in
  `src/search/coordinator/{mod.rs,maintenance.rs,types.rs}` and
  `tests/unit/search/service/**`, replacing local queue-era proofs with
  bounded assertions that local compaction metadata stays idle while the
  repo-backed compaction status path still exists
- focused validation for the landed slice is green:
  `direnv exec . cargo test -p xiuxian-wendao --lib maintenance --features duckdb --quiet`
  `direnv exec . cargo test -p xiuxian-wendao --lib core::status --features duckdb --quiet`
  `direnv exec . cargo test -p xiuxian-wendao --lib status::maintenance --features duckdb --quiet`
  `direnv exec . cargo test -p xiuxian-wendao --lib coordinator --features duckdb --quiet`
  `direnv exec . cargo check -p xiuxian-wendao --features duckdb`
  `direnv exec . cargo check -p xiuxian-wendao`
  `direnv exec . cargo clippy -p xiuxian-wendao --all-targets --features duckdb -- -D warnings`
- closed the ExecPlan for this slice and archived it at
  `.cache/agent/execplans/archives/wendao-duckdb-retire-local-compaction-runtime.md`
