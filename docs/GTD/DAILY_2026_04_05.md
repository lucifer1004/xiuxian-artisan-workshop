# Daily Log - 2026-04-05

## Wendao Parser Namespace Baseline Recovery

- re-checked the active parser namespace blueprint at
  `.cache/agent/blueprints/wendao_parser_core_namespace.md`
- kept the active execution plan open at
  `.cache/agent/execplans/wendao-parser-root-namespace-migration.md`
- scoped the current bounded slice to parser namespace baseline recovery only:
  repair the remaining `src/parsers/markdown/*` private `link_graph`
  imports and restore the canonical `LinkGraphIndex::extract_lineage`
  integration so crate-level validation can reach the currently blocked
  quantum rerank proof again
- tightened the slice per current steering: remove the temporary
  `link_graph::parser` compatibility idea, keep the migration direct, and
  make `packages/rust/crates/xiuxian-wendao/src/parsers/markdown/` the only
  parser implementation home in code
- retired `packages/rust/crates/xiuxian-wendao/src/link_graph/parser.rs`,
  migrated active crate and test callers to
  `crate::parsers::markdown::*` / `xiuxian_wendao::parsers::markdown::*`, and
  moved parser unit fixtures to
  `packages/rust/crates/xiuxian-wendao/tests/unit/parsers/markdown/`
- aligned the moved parser code away from private
  `link_graph::{models,index}` imports toward public
  `crate::link_graph::*` re-exports, which cleared the parser-namespace
  compile blocker that was preventing crate-level validation from reaching
  the existing `LinkGraphIndex::extract_lineage` baseline
- synchronized package docs in
  `packages/rust/crates/xiuxian-wendao/docs/01_core/103_package_layering.md`
  and `packages/rust/crates/xiuxian-wendao/docs/06_roadmap/405_large_rust_modularization.md`
  so they now describe `src/parsers/{markdown,...}` as the canonical parser
  home without a parallel `link_graph` parser namespace
- verified the bounded parser slice with:
  `direnv exec . cargo fmt -p xiuxian-wendao`
  `direnv exec . cargo test -p xiuxian-wendao canonical_parser_namespace_parses_workflow_fixture --lib`
  `direnv exec . cargo test -p xiuxian-wendao parsers::markdown:: --lib`
  `direnv exec . cargo check -p xiuxian-wendao --lib`
- attempted the crate-level done gate:
  `direnv exec . cargo clippy -p xiuxian-wendao --all-targets --all-features -- -D warnings`
  which is still blocked by unrelated existing `xiuxian-wendao-julia`
  findings in
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/capability_manifest.rs`
  and
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/graph_structural_projection/mod.rs`,
  not by parser-namespace regression in `xiuxian-wendao`
- `cargo check -p xiuxian-wendao --lib` is green for this slice and now
  advances past the parser-namespace blocker, but the current worktree
  baseline still emits two pre-existing warnings in
  `packages/rust/crates/xiuxian-wendao/src/link_graph/index/lookup.rs`:
  dead-code for `HierarchicalHit` and the unused `hierarchical_hit` method
- archived the completed execution plan at
  `.cache/agent/execplans/archives/wendao-parser-root-namespace-migration.md`

## Wendao Parser Frontmatter Consolidation

- re-used the active parser namespace blueprint at
  `.cache/agent/blueprints/wendao_parser_core_namespace.md`
- opened the new bounded execution plan at
  `.cache/agent/execplans/wendao-parser-frontmatter-consolidation.md`
- created
  `packages/rust/crates/xiuxian-wendao/src/parsers/markdown/frontmatter/`
  so parser-owned `mod.rs`, `api.rs`, and `types.rs` now own semantic
  markdown frontmatter parsing plus the `NoteFrontmatter` contract
- retired
  `packages/rust/crates/xiuxian-wendao/src/enhancer/frontmatter.rs`
  and retargeted `enhancer`, `skill_vfs`, and pybinding consumers to
  `crate::parsers::markdown::{parse_frontmatter, NoteFrontmatter}`
- moved frontmatter unit coverage to
  `packages/rust/crates/xiuxian-wendao/tests/unit/parsers/markdown/frontmatter.rs`
  and kept `enhancer` tests focused on note-enhancement behavior
- synchronized package docs in
  `packages/rust/crates/xiuxian-wendao/docs/01_core/103_package_layering.md`
  and `packages/rust/crates/xiuxian-wendao/docs/06_roadmap/405_large_rust_modularization.md`
  so they now record parser-owned frontmatter as part of the canonical
  `src/parsers/{markdown,...}` shape
- verified the bounded frontmatter slice with:
  `direnv exec . cargo fmt -p xiuxian-wendao`
  `direnv exec . cargo test -p xiuxian-wendao parse_frontmatter --lib`
  `direnv exec . cargo test -p xiuxian-wendao enhance_note --lib`
  `direnv exec . cargo test -p xiuxian-wendao parsers::markdown:: --lib`
  `direnv exec . cargo check -p xiuxian-wendao --lib`
  `direnv exec . git diff --check -- packages/rust/crates/xiuxian-wendao docs/GTD/DAILY_2026_04_05.md .cache/agent/blueprints/wendao_parser_core_namespace.md .cache/agent/execplans/archives/wendao-parser-frontmatter-consolidation.md`
- attempted the done gate:
  `direnv exec . cargo clippy -p xiuxian-wendao --all-targets --all-features -- -D warnings`
  which is still blocked by unrelated existing `xiuxian-wendao-julia`
  findings in
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/capability_manifest.rs`,
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/graph_structural_projection/topology.rs`,
  and
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/graph_structural_projection/topology_builders.rs`
- archived the completed execution plan at
  `.cache/agent/execplans/archives/wendao-parser-frontmatter-consolidation.md`

## Wendao Parser Zhixing Task Projection Migration

- re-used the active parser namespace blueprint at
  `.cache/agent/blueprints/wendao_parser_core_namespace.md`
- opened the new bounded execution plan at
  `.cache/agent/execplans/wendao-parser-zhixing-task-projection-migration.md`
- created
  `packages/rust/crates/xiuxian-wendao/src/parsers/zhixing/tasks/`
  so zhixing task-line parsing, inline metadata splitting, and task identity
  normalization now live under the canonical parser namespace
- retired
  `packages/rust/crates/xiuxian-wendao/src/skill_vfs/zhixing/indexer/parse/`
  and retargeted `skill_vfs::zhixing::indexer::tasks` plus
  `skill_vfs::zhixing::indexer::stats` to
  `crate::parsers::zhixing::tasks::*`
- added parser-owned unit coverage in
  `packages/rust/crates/xiuxian-wendao/tests/unit/parsers/zhixing/tasks.rs`
  and a focused consumer regression in
  `packages/rust/crates/xiuxian-wendao/src/skill_vfs/zhixing/indexer/stats.rs`
- synchronized package docs in
  `packages/rust/crates/xiuxian-wendao/docs/01_core/103_package_layering.md`
  and `packages/rust/crates/xiuxian-wendao/docs/06_roadmap/405_large_rust_modularization.md`
  so they now record `src/parsers/zhixing/tasks/` as parser-owned zhixing
  task parsing instead of a `skill_vfs`-local parser subtree
- verified the bounded zhixing parser slice with:
  `direnv exec . cargo fmt -p xiuxian-wendao`
  `direnv exec . cargo test -p xiuxian-wendao zhixing --lib`
  `direnv exec . cargo check -p xiuxian-wendao --lib`
  `direnv exec . git diff --check -- packages/rust/crates/xiuxian-wendao docs/GTD/DAILY_2026_04_05.md .cache/agent/blueprints/wendao_parser_core_namespace.md .cache/agent/execplans/archives/wendao-parser-zhixing-task-projection-migration.md`
- attempted the done gate:
  `direnv exec . cargo clippy -p xiuxian-wendao --all-targets --all-features -- -D warnings`
  which is still blocked by unrelated existing `xiuxian-wendao-julia`
  findings in
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/capability_manifest.rs`,
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/graph_structural_projection/topology.rs`,
  and
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/graph_structural_projection/topology_builders.rs`
- archived the completed execution plan at
  `.cache/agent/execplans/archives/wendao-parser-zhixing-task-projection-migration.md`

## Wendao Parser Link-Graph Query Migration

- re-used the active parser namespace blueprint at
  `.cache/agent/blueprints/wendao_parser_core_namespace.md`
- opened the new bounded execution plan at
  `.cache/agent/execplans/wendao-parser-link-graph-query-migration.md`
- created
  `packages/rust/crates/xiuxian-wendao/src/parsers/link_graph/query/`
  so link-graph search-query parsing, directive scanning, query-option merge,
  and parser-local helpers now live under the canonical parser namespace
- retired
  `packages/rust/crates/xiuxian-wendao/src/link_graph/query/`
  and retargeted link-graph search planning plus crate-root exports to
  `crate::parsers::link_graph::query::{parse_search_query, ParsedLinkGraphQuery}`
- added parser-owned unit coverage in
  `packages/rust/crates/xiuxian-wendao/tests/unit/parsers/link_graph/query.rs`
- synchronized package docs in
  `packages/rust/crates/xiuxian-wendao/docs/01_core/103_package_layering.md`
  and `packages/rust/crates/xiuxian-wendao/docs/06_roadmap/405_large_rust_modularization.md`
  so they now record `src/parsers/link_graph/query/` as the canonical
  link-graph query parser ownership boundary
- synchronized the parser namespace blueprint in
  `.cache/agent/blueprints/wendao_parser_core_namespace.md`
  so the link-graph query slice now points at
  `crate::parsers::link_graph::query::*`
- verified the bounded link-graph parser slice with:
  `direnv exec . cargo fmt -p xiuxian-wendao`
  `direnv exec . cargo test -p xiuxian-wendao parse_search_query --lib`
  `direnv exec . cargo test -p xiuxian-wendao link_graph::index::search:: --lib`
  `direnv exec . cargo test -p xiuxian-wendao parsers::link_graph::query:: --lib`
  `direnv exec . cargo check -p xiuxian-wendao --lib`
  `direnv exec . git diff --check -- packages/rust/crates/xiuxian-wendao docs/GTD/DAILY_2026_04_05.md .cache/agent/blueprints/wendao_parser_core_namespace.md .cache/agent/execplans/archives/wendao-parser-link-graph-query-migration.md`
- attempted the done gate:
  `direnv exec . cargo clippy -p xiuxian-wendao --all-targets --all-features -- -D warnings`
  which is still blocked by unrelated existing `xiuxian-wendao-julia`
  findings in
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/capability_manifest.rs`,
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/graph_structural_projection/topology.rs`,
  and
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/graph_structural_projection/topology_builders.rs`
- archived the completed execution plan at
  `.cache/agent/execplans/archives/wendao-parser-link-graph-query-migration.md`

## Wendao Parser Cargo Dependency Migration

- re-used the active parser namespace blueprint at
  `.cache/agent/blueprints/wendao_parser_core_namespace.md`
- opened the new bounded execution plan at
  `.cache/agent/execplans/wendao-parser-cargo-dependency-migration.md`
- created
  `packages/rust/crates/xiuxian-wendao/src/parsers/cargo/dependencies/`
  so Cargo.toml dependency parsing now lives under the canonical parser
  namespace with parser-owned `api.rs`, `regex.rs`, and `types.rs`
- retired
  `packages/rust/crates/xiuxian-wendao/src/dependency_indexer/cargo/`
  and removed the old `dependency_indexer` Cargo parser exports so the direct
  canonical import path is now
  `crate::parsers::cargo::dependencies::{parse_cargo_dependencies, CargoDependency}`
- moved parser-owned unit coverage to
  `packages/rust/crates/xiuxian-wendao/tests/unit/parsers/cargo/dependencies.rs`
  and kept the migration direct without a compatibility shim
- synchronized package docs in
  `packages/rust/crates/xiuxian-wendao/docs/01_core/103_package_layering.md`
  and `packages/rust/crates/xiuxian-wendao/docs/06_roadmap/405_large_rust_modularization.md`
  so they now record `src/parsers/cargo/dependencies/` as the canonical
  Cargo manifest parser ownership boundary
- synchronized the parser namespace blueprint in
  `.cache/agent/blueprints/wendao_parser_core_namespace.md`
  so the next parser-family slices now explicitly include the Cargo
  dependency parser move in the governed sequence
- verified the bounded Cargo parser slice with:
  `direnv exec . cargo fmt -p xiuxian-wendao`
  `direnv exec . cargo test -p xiuxian-wendao parsers::cargo::dependencies:: --lib`
  `direnv exec . cargo check -p xiuxian-wendao --lib`
  `direnv exec . git diff --check -- packages/rust/crates/xiuxian-wendao docs/GTD/DAILY_2026_04_05.md .cache/agent/blueprints/wendao_parser_core_namespace.md .cache/agent/execplans/archives/wendao-parser-cargo-dependency-migration.md`
- attempted the done gate:
  `direnv exec . cargo clippy -p xiuxian-wendao --all-targets --all-features -- -D warnings`
  which is still blocked by unrelated existing `xiuxian-wendao-julia`
  findings in
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/capability_manifest.rs`,
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/graph_structural_projection/topology.rs`,
  and
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/graph_structural_projection/topology_builders.rs`
- archived the completed execution plan at
  `.cache/agent/execplans/archives/wendao-parser-cargo-dependency-migration.md`

## Wendao Parser Repo Code Query Migration

- re-used the active parser namespace blueprint at
  `.cache/agent/blueprints/wendao_parser_core_namespace.md`
- opened the new bounded execution plan at
  `.cache/agent/execplans/wendao-parser-repo-code-query-migration.md`
- created
  `packages/rust/crates/xiuxian-wendao/src/parsers/search/repo_code_query/`
  so repo-code search query parsing now lives under the canonical parser
  namespace with parser-owned `api.rs` and `types.rs`
- retired
  `packages/rust/crates/xiuxian-wendao/src/search/repo_search/query.rs`
  and retargeted `repo_search` consumers directly to
  `crate::parsers::search::repo_code_query::parse_repo_code_search_query`
- moved parser-focused assertions out of the gateway search test bundle into
  `packages/rust/crates/xiuxian-wendao/tests/unit/parsers/search/repo_code_query.rs`
  so parser ownership and test ownership now match
- synchronized package docs in
  `packages/rust/crates/xiuxian-wendao/docs/01_core/103_package_layering.md`
  and `packages/rust/crates/xiuxian-wendao/docs/06_roadmap/405_large_rust_modularization.md`
  so they now record `src/parsers/search/repo_code_query/` as the canonical
  repo-code search query parser ownership boundary
- synchronized the parser namespace blueprint in
  `.cache/agent/blueprints/wendao_parser_core_namespace.md`
  so the next parser-family slices now include the repo-code query move in the
  governed sequence
- verified the bounded repo-code query parser slice with:
  `direnv exec . cargo fmt -p xiuxian-wendao`
  `direnv exec . cargo test -p xiuxian-wendao parsers::search::repo_code_query:: --lib`
  `direnv exec . cargo test -p xiuxian-wendao parse_repo_code_search_query --lib`
  `direnv exec . cargo check -p xiuxian-wendao --lib`
  `direnv exec . git diff --check -- packages/rust/crates/xiuxian-wendao docs/GTD/DAILY_2026_04_05.md .cache/agent/blueprints/wendao_parser_core_namespace.md .cache/agent/execplans/archives/wendao-parser-repo-code-query-migration.md`
- attempted the done gate:
  `direnv exec . cargo clippy -p xiuxian-wendao --all-targets --all-features -- -D warnings`
  which is still blocked by unrelated existing `xiuxian-wendao-julia`
  findings in
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/capability_manifest.rs`,
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/graph_structural_projection/topology.rs`,
  and
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/graph_structural_projection/topology_builders.rs`
- archived the completed execution plan at
  `.cache/agent/execplans/archives/wendao-parser-repo-code-query-migration.md`

## Wendao Parser Graph Persistence Migration

- re-used the active parser namespace blueprint at
  `.cache/agent/blueprints/wendao_parser_core_namespace.md`
- opened the new bounded execution plan at
  `.cache/agent/execplans/wendao-parser-graph-persistence-migration.md`
- created
  `packages/rust/crates/xiuxian-wendao/src/parsers/graph/persistence/`
  so graph JSON dict parsing now lives under the canonical parser namespace
  with parser-owned `api.rs`, `entity.rs`, and `relation.rs`
- retired
  `packages/rust/crates/xiuxian-wendao/src/graph/persistence/parse.rs`
  and retargeted graph save/load directly to
  `crate::parsers::graph::persistence::{entity_from_dict, relation_from_dict}`
- moved parser-focused graph dict assertions into
  `packages/rust/crates/xiuxian-wendao/tests/unit/parsers/graph/persistence.rs`
  and attached one focused save/load regression to
  `packages/rust/crates/xiuxian-wendao/src/graph/persistence/save_load.rs`
  through
  `packages/rust/crates/xiuxian-wendao/tests/unit/graph/persistence_save_load.rs`
- synchronized package docs in
  `packages/rust/crates/xiuxian-wendao/docs/01_core/103_package_layering.md`
  and `packages/rust/crates/xiuxian-wendao/docs/06_roadmap/405_large_rust_modularization.md`
  so they now record `src/parsers/graph/persistence/` as the canonical graph
  persistence parser ownership boundary
- synchronized the parser namespace blueprint in
  `.cache/agent/blueprints/wendao_parser_core_namespace.md`
  so the governed parser matrix now includes graph persistence and the next
  likely follow-up is lane closeout audit rather than another obvious parser
  family move
- physically cleaned leftover empty retired directories under
  `packages/rust/crates/xiuxian-wendao/src/link_graph/query/` and
  `packages/rust/crates/xiuxian-wendao/src/skill_vfs/zhixing/indexer/parse/`
- verified the bounded graph persistence parser slice with:
  `direnv exec . cargo fmt -p xiuxian-wendao`
  `direnv exec . cargo test -p xiuxian-wendao parsers::graph::persistence:: --lib`
  `direnv exec . cargo test -p xiuxian-wendao test_save_and_load_graph --lib`
  `direnv exec . cargo check -p xiuxian-wendao --lib`
  `direnv exec . git diff --check -- packages/rust/crates/xiuxian-wendao docs/GTD/DAILY_2026_04_05.md .cache/agent/blueprints/wendao_parser_core_namespace.md .cache/agent/execplans/archives/wendao-parser-graph-persistence-migration.md`
- attempted the done gate:
  `direnv exec . cargo clippy -p xiuxian-wendao --all-targets --all-features -- -D warnings`
  which is still blocked by unrelated existing `xiuxian-wendao-julia`
  findings in
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/capability_manifest.rs`,
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/graph_structural_projection/topology.rs`,
  and
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/graph_structural_projection/topology_builders.rs`
- archived the completed execution plan at
  `.cache/agent/execplans/archives/wendao-parser-graph-persistence-migration.md`

## Wendao Parser Closeout Audit

- re-used the parser namespace blueprint and opened the bounded closeout
  execution plan at
  `.cache/agent/execplans/wendao-parser-closeout-audit.md`
- hardened
  `packages/rust/crates/xiuxian-wendao/src/parsers/graph/persistence/relation.rs`
  so relation-type parsing moved under the canonical parser namespace, with
  parser-owned coverage expanded in
  `packages/rust/crates/xiuxian-wendao/tests/unit/parsers/graph/persistence.rs`
- created the stable parser docs namespace under
  `packages/rust/crates/xiuxian-wendao/docs/02_parser/`
  with `index.md` and `architecture.md`, so parser ownership boundaries and
  parsing strategy now have a dedicated package-doc home
- synchronized
  `packages/rust/crates/xiuxian-wendao/docs/index.md`,
  `packages/rust/crates/xiuxian-wendao/docs/01_core/103_package_layering.md`,
  and
  `packages/rust/crates/xiuxian-wendao/docs/06_roadmap/405_large_rust_modularization.md`
  to point at the dedicated parser docs and to record that the remaining
  `parse.rs`, `query.rs`, and `document.rs` helpers outside `src/parsers/`
  are adapter-local or subsystem-local by design
- updated the parser blueprint in
  `.cache/agent/blueprints/wendao_parser_core_namespace.md`
  so the stable reference set now includes `docs/02_parser/` and the closeout
  outcome records the dedicated parser docs home
- verified the closeout slice with:
  `direnv exec . cargo fmt -p xiuxian-wendao`
  `direnv exec . cargo test -p xiuxian-wendao parsers::graph::persistence:: --lib`
  `direnv exec . cargo check -p xiuxian-wendao --lib`
  `direnv exec . git diff --check -- packages/rust/crates/xiuxian-wendao docs/GTD/DAILY_2026_04_05.md .cache/agent/blueprints/archives/wendao_parser_core_namespace.md .cache/agent/execplans/archives/wendao-parser-closeout-audit.md`
- archived the parser blueprint at
  `.cache/agent/blueprints/archives/wendao_parser_core_namespace.md`
- archived the completed execution plan at
  `.cache/agent/execplans/archives/wendao-parser-closeout-audit.md`

## Wendao Parser Structural Relation Semantics

- opened the active relation-semantics blueprint at
  `.cache/agent/blueprints/wendao_parser_relation_semantics.md`
  and the bounded execution plan at
  `.cache/agent/execplans/wendao-parser-structural-relation-semantics.md`
- corrected the parser contract so ordinary `[[...]]` links are treated as
  structural graph edges, while typed relation semantics are reserved for
  explicit metadata owners such as property drawers
- updated
  `packages/rust/crates/xiuxian-wendao/src/enhancer/relations.rs`
  and
  `packages/rust/crates/xiuxian-wendao/src/enhancer/types.rs`
  so note enhancement no longer invents semantic relation labels for plain
  wiki links, skill paths, or frontmatter tags
- narrowed
  `packages/rust/crates/xiuxian-wendao/src/parsers/graph/persistence/relation.rs`
  and
  `packages/rust/crates/xiuxian-wendao/src/pybindings/graph_py/parsers.rs`
  to persisted relation-token decoding only, preserving wiki-link-shaped
  labels such as `[[notes/design]]` and `[[assets/logo.png]]` instead of
  promoting them to semantic enums
- synchronized
  `packages/rust/crates/xiuxian-wendao/docs/02_parser/index.md`
  and
  `packages/rust/crates/xiuxian-wendao/docs/02_parser/architecture.md`
  to document the structural-link-first rule, attachment classification by
  suffix, and explicit typed semantics via property drawers or equivalent
  metadata
- verified the bounded slice with:
  `direnv exec . cargo test -p xiuxian-wendao enhancer:: --lib`
  `direnv exec . cargo test -p xiuxian-wendao parsers::graph::persistence:: --lib`
  `direnv exec . cargo test -p xiuxian-wendao test_save_and_load_graph --lib`
  `direnv exec . cargo check -p xiuxian-wendao --lib`
  `direnv exec . git diff --check -- packages/rust/crates/xiuxian-wendao docs/GTD/DAILY_2026_04_05.md .cache/agent/blueprints/wendao_parser_relation_semantics.md .cache/agent/execplans/wendao-parser-structural-relation-semantics.md`
- attempted the done gate:
  `direnv exec . cargo clippy -p xiuxian-wendao --all-targets --all-features -- -D warnings`
  which is still blocked by unrelated existing `xiuxian-wendao-julia`
  findings in
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/capability_manifest.rs`,
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/graph_structural_projection/topology.rs`,
  and
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/graph_structural_projection/topology_builders.rs`
- archived the completed execution plan at
  `.cache/agent/execplans/archives/wendao-parser-structural-relation-semantics.md`

## Wendao Julia Graph-Structural Projection Modularization

- physically re-verified that
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/graph_structural_projection.rs`
  has grown to `3069` lines and now mixes query DTOs, pair helpers,
  generic-topology staging, keyword-overlap staging, request-row builders,
  normalization utilities, and tests in one file
- opened the active execution plan for the next bounded slice at
  `.cache/agent/execplans/wendao-julia-graph-structural-projection-modularization.md`
- scoped the slice to a feature-folder refactor only: split
  `graph_structural_projection.rs` into a `graph_structural_projection/`
  module tree with interface-only `mod.rs`, preserve the current live
  contract, and keep the just-added raw connected-pair collection helper
  inside the modularized projection surface
- landed the feature-folder split under
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/graph_structural_projection/`
  with interface-only `mod.rs` plus responsibility modules for `core`,
  `topology`, `pair`, `overlap`, `rows`, `*_builders`, `support`, and
  `tests`
- preserved the raw connected-pair collection helper inside the modularized
  generic-topology staging surface and kept the public graph-structural
  exports stable through `xiuxian-wendao-julia`
- re-verified the bounded slice:
  `direnv exec . cargo test -p xiuxian-wendao-julia graph_structural_projection --lib`
  `direnv exec . cargo test -p xiuxian-wendao-julia graph_structural_exchange --lib`
  `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate generic_topology_fetch_helper`
  `direnv exec . cargo check -p xiuxian-wendao --features julia --test xiuxian-testing-gate`
  `direnv exec . julia --project=.data/WendaoSearch.jl -e 'using Pkg; Pkg.test()'`
- archived the completed execution plan at
  `.cache/agent/execplans/archives/wendao-julia-graph-structural-projection-modularization.md`

## Wendao Julia Raw Connected Pair Collection Fetch

- re-checked the active mixed-graph retrieval blueprint at
  `.cache/agent/blueprints/wendao_search_mixed_graph_retrieval.md`
  with the Julia plugin-first rollout blueprint as a bounded secondary
  reference:
  `.cache/agent/blueprints/wendao_julia_plugin_first_rollout.md`
- opened the active execution plan for the next bounded slice at
  `.cache/agent/execplans/wendao-julia-raw-connected-pair-collection-fetch.md`
- scoped the slice to one remaining host-local assembly step only: keep the
  current `WendaoSearch.jl` live contract unchanged, but move the host-side
  `raw connected pairs -> generic-topology candidate collection` assembly into
  a plugin-owned fetch seam above the current raw connected-pair helper
- completed that slice as part of the projection feature-folder refactor and
  archived the execution plan at
  `.cache/agent/execplans/archives/wendao-julia-raw-connected-pair-collection-fetch.md`

## Wendao Julia Generic Topology Multi-Candidate Live

- re-checked the active mixed-graph retrieval blueprint at
  `.cache/agent/blueprints/wendao_search_mixed_graph_retrieval.md`
  with the Julia plugin-first rollout blueprint as a bounded secondary
  reference:
  `.cache/agent/blueprints/wendao_julia_plugin_first_rollout.md`
- opened the active execution plan for the next bounded slice at
  `.cache/agent/execplans/wendao-julia-generic-topology-multi-candidate-live.md`
- scoped the slice to one live-proof extension only: keep the current
  `WendaoSearch.jl` contract unchanged, but prove that the generic-topology
  rerank seam can carry multiple candidate collections in one batch, with the
  host proof deriving more than one connected pair collection from a real
  `LinkGraphAgenticExpansionPlan`
- landed one plugin-owned multi-candidate generic-topology live proof in
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/graph_structural_exchange.rs`
  against the real `WendaoSearch.jl --mode solver_demo` multi-route service,
  so one repository downcall now returns more than one generic-topology
  candidate row in a single batch
- landed one host-through-language-seam live proof in
  `packages/rust/crates/xiuxian-wendao/tests/unit/link_graph_agentic/expansion.rs`
  that derives two connected pair collections from a real
  `LinkGraphAgenticExpansionPlan` and downcalls them through the same manifest
  discovery seam without changing the `WendaoSearch.jl` contract
- re-verified the bounded slice:
  `direnv exec . cargo test -p xiuxian-wendao-julia graph_structural_exchange --lib`
  `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate generic_topology_fetch_helper`
  `direnv exec . cargo check -p xiuxian-wendao --features julia --test xiuxian-testing-gate`
  `direnv exec . julia --project=.data/WendaoSearch.jl -e 'using Pkg; Pkg.test()'`
  `direnv exec . git diff --check -- ...`
- archived the completed execution plan at
  `.cache/agent/execplans/archives/wendao-julia-generic-topology-multi-candidate-live.md`

## Wendao Julia Raw Connected Pair Tuple Helper

- re-checked the active mixed-graph retrieval blueprint at
  `.cache/agent/blueprints/wendao_search_mixed_graph_retrieval.md`
  with the Julia plugin-first rollout blueprint as a bounded secondary
  reference:
  `.cache/agent/blueprints/wendao_julia_plugin_first_rollout.md`
- opened the active execution plan for the next bounded slice at
  `.cache/agent/execplans/wendao-julia-raw-connected-pair-tuple-helper.md`
- scoped the slice to one remaining host-local glue step only: keep the
  current `WendaoSearch.jl` live contract unchanged, but move the
  `left_id/right_id/semantic_score -> GraphStructuralRawConnectedPairInputs`
  normalization into a plugin-owned helper above the raw connected-pair
  collection seam
- landed one plugin-owned raw tuple helper under
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/graph_structural_projection/topology.rs`,
  wired it through the plugin and builtin re-export surfaces, and updated the
  plugin plus host generic-topology proofs to consume raw tuple collections
  directly instead of manually constructing
  `GraphStructuralRawConnectedPairInputs`
- externalized the remaining
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/graph_structural_exchange.rs`
  inline tests into
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/graph_structural_exchange_tests.rs`
  and
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/graph_structural_exchange_generic_topology_tests.rs`
  using `#[cfg(test)] #[path = "..."]`, so the production exchange module now
  stays implementation-only while keeping the live proof matrix intact
- re-verified the bounded slice:
  `direnv exec . cargo test -p xiuxian-wendao-julia graph_structural_projection --lib`
  `direnv exec . cargo test -p xiuxian-wendao-julia graph_structural_exchange --lib`
  `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate generic_topology_fetch_helper`
  `direnv exec . cargo check -p xiuxian-wendao --features julia --test xiuxian-testing-gate`
  `direnv exec . julia --project=.data/WendaoSearch.jl -e 'using Pkg; Pkg.test()'`
  `direnv exec . git diff --check -- ...`

## Wendao Julia Raw Connected Pair Live

- re-checked the active mixed-graph retrieval blueprint at
  `.cache/agent/blueprints/wendao_search_mixed_graph_retrieval.md`
  with the Julia plugin-first rollout blueprint as a bounded secondary
  reference:
  `.cache/agent/blueprints/wendao_julia_plugin_first_rollout.md`
- opened the active execution plan for the next bounded slice at
  `.cache/agent/execplans/wendao-julia-raw-connected-pair-live.md`
- scoped the slice to one remaining host-local glue step only: keep the
  current `WendaoSearch.jl` live contract unchanged, but move the host-side
  `LinkGraphAgenticCandidatePair -> scored pair DTO` mapping into a
  plugin-owned raw connected-pair helper above the generic topology seam
- landed one raw connected-pair staging seam under
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/graph_structural_projection.rs`,
  wired it through the plugin and builtin re-export surfaces, and updated both
  plugin-owned live proofs plus the host-through-language-seam proof in
  `packages/rust/crates/xiuxian-wendao/tests/unit/link_graph_agentic/expansion.rs`
- the bounded generic-topology live path now derives candidate semantic score
  inside `xiuxian-wendao-julia` from raw connected pairs as well, so the host
  proof no longer maps `LinkGraphAgenticCandidatePair` into scored pair DTOs
  before the generic-topology downcall
- re-verified the bounded slice:
  `direnv exec . cargo test -p xiuxian-wendao-julia graph_structural_projection --lib`
  `direnv exec . cargo test -p xiuxian-wendao-julia graph_structural_exchange --lib`
  `direnv exec . julia --project=.data/WendaoSearch.jl -e 'using Pkg; Pkg.test()'`
  `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate generic_topology_fetch_helper`
  `direnv exec . cargo check -p xiuxian-wendao --features julia --test xiuxian-testing-gate`
  `direnv exec . git diff --check -- ...`
- archived the completed execution plan at
  `.cache/agent/execplans/archives/wendao-julia-raw-connected-pair-live.md`

## Wendao Julia Raw Pair Collection Generic Topology

- re-checked the active mixed-graph retrieval blueprint at
  `.cache/agent/blueprints/wendao_search_mixed_graph_retrieval.md`
  with the Julia plugin-first rollout blueprint as a bounded secondary
  reference:
  `.cache/agent/blueprints/wendao_julia_plugin_first_rollout.md`
- opened the active execution plan for the next bounded slice at
  `.cache/agent/execplans/wendao-julia-raw-pair-collection-generic-topology.md`
- scoped the slice to one staging gap only: keep the current
  `WendaoSearch.jl` service contract unchanged, but move the host-side
  `pair -> GraphStructuralPairCandidateInputs + semantic score reduction`
  glue into a plugin-owned raw pair-collection helper above the generic
  topology seam
- landed one scored pair-collection staging seam under
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/graph_structural_projection.rs`,
  wired it through the plugin and builtin re-export surfaces, and updated both
  plugin-owned live proofs plus the host-through-language-seam proof in
  `packages/rust/crates/xiuxian-wendao/tests/unit/link_graph_agentic/expansion.rs`
- the bounded generic-topology live path now derives candidate semantic score
  inside `xiuxian-wendao-julia` from the raw pair collection, so the host
  proof no longer manually averages connected-pair priorities or manually maps
  them into `GraphStructuralPairCandidateInputs`
- a pre-existing `quantum_fusion` lifetime blocker in
  `packages/rust/crates/xiuxian-wendao/src/link_graph/index/search/quantum_fusion/{openai_ignition,vector_ignition}.rs`
  had to be resolved before the host validation gate could compile again; the
  fix was bounded to removing one borrowed projection from an async return path
  and making the vector-side projection helper synchronous
- re-verified the bounded slice:
  `direnv exec . cargo test -p xiuxian-wendao-julia graph_structural_projection --lib`
  `direnv exec . cargo test -p xiuxian-wendao-julia graph_structural_exchange --lib`
  `direnv exec . julia --project=.data/WendaoSearch.jl -e 'using Pkg; Pkg.test()'`
  `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate generic_topology_fetch_helper`
  `direnv exec . cargo check -p xiuxian-wendao --features julia --test xiuxian-testing-gate`
  `direnv exec . git diff --check -- ...`
- archived the completed execution plan at
  `.cache/agent/execplans/archives/wendao-julia-raw-pair-collection-generic-topology.md`

## Wendao Julia Generic Topology Pair Collection Live

- re-checked the active mixed-graph retrieval blueprint at
  `.cache/agent/blueprints/wendao_search_mixed_graph_retrieval.md`
  with the Julia plugin-first rollout blueprint as a bounded secondary
  reference:
  `.cache/agent/blueprints/wendao_julia_plugin_first_rollout.md`
- opened the active execution plan for the next bounded slice at
  `.cache/agent/execplans/wendao-julia-generic-topology-pair-collection-live.md`
- scoped the slice to one realism step only: keep the current
  `WendaoSearch.jl` explicit-edge live contract unchanged, but let
  `xiuxian-wendao-julia` promote one connected
  `LinkGraphAgenticExpansionPlan` pair collection into the existing generic
  topology helper seam instead of hand-built node and edge arrays
- landed plugin-owned pair-collection staging helpers under
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/graph_structural_projection.rs`,
  reused that seam in
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/graph_structural_exchange.rs`,
  and updated the host-through-language-seam proof in
  `packages/rust/crates/xiuxian-wendao/tests/unit/link_graph_agentic/expansion.rs`
- the bounded live proof now derives a real connected pair collection from
  `LinkGraphAgenticExpansionPlan`, promotes it into the generic explicit-edge
  request path, and confirms `solver_demo` returns real node ids such as
  `notes/a` instead of synthetic `node-1`
- re-verified the bounded slice:
  `direnv exec . cargo test -p xiuxian-wendao-julia graph_structural_projection --lib`
  `direnv exec . cargo test -p xiuxian-wendao-julia graph_structural_exchange --lib`
  `direnv exec . julia --project=.data/WendaoSearch.jl -e 'using Pkg; Pkg.test()'`
  `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate generic_topology_fetch_helper`
  `direnv exec . cargo check -p xiuxian-wendao --features julia --test xiuxian-testing-gate`
  `direnv exec . git diff --check -- ...`
- archived the completed execution plan at
  `.cache/agent/execplans/archives/wendao-julia-generic-topology-pair-collection-live.md`

## Wendao Julia Generic Topology Live Proof

- re-checked the active mixed-graph retrieval blueprint at
  `.cache/agent/blueprints/wendao_search_mixed_graph_retrieval.md`
  with the Julia plugin-first rollout blueprint as a bounded secondary
  reference:
  `.cache/agent/blueprints/wendao_julia_plugin_first_rollout.md`
- opened the active execution plan for the next bounded slice at
  `.cache/agent/execplans/wendao-julia-generic-topology-live-proof.md`
- scoped the slice to one additive realism gap only: keep the current
  `WendaoSearch.jl` explicit-edge live contract untouched, but let
  `xiuxian-wendao-julia` stage and fetch one non-pair candidate topology
  without collapsing it into the existing pair-specific helper stack
- landed plugin-owned generic topology staging and fetch helpers under
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/{graph_structural_projection,graph_structural_exchange}.rs`,
  plus the linked builtin re-export seam and one host-through-language-seam
  proof in
  `packages/rust/crates/xiuxian-wendao/tests/unit/link_graph_agentic/expansion.rs`
- the new proof uses a three-node chain with two explicit edges against
  `WendaoSearch.jl --mode solver_demo`, so the live path is now proven above
  the pair normalization layer
- re-verified the bounded slice:
  `direnv exec . cargo test -p xiuxian-wendao-julia graph_structural_projection --lib`
  `direnv exec . cargo test -p xiuxian-wendao-julia graph_structural_exchange --lib`
  `direnv exec . cargo test -p xiuxian-wendao-builtin graph_structural --lib`
  `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate generic_topology_fetch_helper`
- closed the final validation gate:
  `direnv exec . julia --project=.data/WendaoSearch.jl -e 'using Pkg; Pkg.test()'`
  `direnv exec . cargo check -p xiuxian-wendao --features julia --test xiuxian-testing-gate`
  `direnv exec . git diff --check -- ...`
- archived the completed execution plan at
  `.cache/agent/execplans/archives/wendao-julia-generic-topology-live-proof.md`

## WendaoSearch Explicit Edge Endpoints Live Contract

- re-checked the active mixed-graph retrieval blueprint at
  `.cache/agent/blueprints/wendao_search_mixed_graph_retrieval.md`
- physically re-verified the current contract gap before implementation:
  `.data/WendaoSearch.jl` and `xiuxian-wendao-julia` still carry
  `candidate_edge_kinds` but not explicit edge endpoint columns
- opened the active execution plan for the next bounded slice at
  `.cache/agent/execplans/wendao-search-explicit-edge-endpoints-live-contract.md`
- scoped the new slice to a narrow staged-contract widening only:
  add `candidate_edge_sources` and `candidate_edge_destinations`,
  propagate them through Julia and Rust graph-structural request surfaces, and
  then let `solver_demo` consume explicit request topology instead of
  projecting a path from node order
- landed the explicit edge-endpoint widening across:
  `.data/WendaoSearch.jl/src/structural_search/types.jl`,
  `.data/WendaoSearch.jl/src/contracts/{schema,rows}.jl`,
  `.data/WendaoSearch.jl/src/service/{solver_demo,warmup}.jl`,
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/{graph_structural,graph_structural_exchange,graph_structural_projection}.rs`,
  and the linked builtin plus host-through-language-seam test surfaces
- `solver_demo` now builds the bounded candidate graph from explicit request
  edges instead of projecting a simple path from node order
- re-verified the bounded slice:
  `direnv exec . julia --project=.data/WendaoSearch.jl -e 'using Pkg; Pkg.test()'`
  `direnv exec . cargo test -p xiuxian-wendao-julia graph_structural_exchange --lib`
  `direnv exec . cargo test -p xiuxian-wendao-julia graph_structural_projection --lib`
  `direnv exec . cargo test -p xiuxian-wendao-builtin graph_structural --lib`
  `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate host_uses_julia_graph_structural_fetch_helper`
  `direnv exec . cargo check -p xiuxian-wendao --features julia --test xiuxian-testing-gate`
- archived the completed execution plan at
  `.cache/agent/execplans/archives/wendao-search-explicit-edge-endpoints-live-contract.md`

## WendaoSearch Solver Demo Live Service

- re-checked the active mixed-graph retrieval blueprint at
  `.cache/agent/blueprints/wendao_search_mixed_graph_retrieval.md`
- verified the current `WendaoSearch.jl` live baseline is green before opening
  the next solver slice:
  `direnv exec . julia --project=.data/WendaoSearch.jl -e 'using Pkg; Pkg.test()'`
  `direnv exec . cargo test -p xiuxian-wendao-julia graph_structural_exchange --lib`
  `direnv exec . cargo test -p xiuxian-wendao-julia capability_manifest --lib`
- opened the active execution plan for the next bounded slice at
  `.cache/agent/execplans/wendao-search-solver-demo-live-service.md`
- scoped the next implementation lane to a new `solver_demo` service mode so
  `WendaoSearch.jl` can expose one real `GadgetSearch`-backed structural
  rerank live path without disturbing the existing `demo` or
  `contract_probe` modes
- landed `.data/WendaoSearch.jl/src/service/solver_demo.jl` and wired
  `solver_demo` through the service descriptor, runtime dispatch, example
  service entrypoint, and dedicated flight-env startup path
- kept the live boundary explicit: the current request contract still lacks
  edge endpoints, so `solver_demo` projects candidate node ids and edge kinds
  onto a simple path topology before running a real bounded
  `GadgetSearch.RydbergModel` solve
- extended package regression coverage for:
  service-mode parsing, in-process solver service helpers, warmup, and one
  cross-process `solver_demo` live regression
- re-verified the full package and the new live solver lane:
  `direnv exec . julia --project=.data/WendaoSearch.jl -e 'using Pkg; Pkg.test()'`
- re-verified the new live solver lane directly through the child-service test
  harness:
  `direnv exec . julia --project=.data/WendaoSearch.jl -e 'using WendaoSearch; include(joinpath(pwd(), ".data", "WendaoSearch.jl", "test", "support", "live_service.jl")); with_wendaosearch_live_service(route_name = :structural_rerank, service_mode = :solver_demo, probe_profile = :exact) do port, _; client = WendaoSearch.WendaoArrow.Arrow.Flight.Client("grpc://127.0.0.1:$(port)"); query = QueryGraph([QueryAnchor(:semantic, "graph query")]; max_layers = 2); candidate = CandidateSubgraph("candidate-live-solver", ["node-a", "node-b"]; edge_kinds = [:depends_on]); request = StructuralRerankRequest("probe-query", query, candidate; retrieval_layer = 1, semantic_score = 0.9, dependency_score = 0.4, keyword_score = 0.2, tag_score = 0.1); table = WendaoSearch.WendaoArrow.flight_exchange_table(client, structural_rerank_exchange_request([request])); cols = WendaoSearch.WendaoArrow.Tables.columntable(table); meta = WendaoSearch.WendaoArrow.schema_metadata(table); println(cols.candidate_id[1], "|", cols.pin_assignment[1][1], "|", cols.feasible[1], "|", cols.structural_score[1], "|", cols.final_score[1], "|", meta["wendao_search.service_mode"]); end'`
  which returned
  `candidate-live-solver|node-a|true|0.47|0.63|solver_demo`
- archived the completed execution plan at
  `.cache/agent/execplans/archives/wendao-search-solver-demo-live-service.md`

## Wendao Julia Graph-Structural Solver Demo Live Smoke

- re-checked the active Julia plugin-first rollout blueprint at
  `.cache/agent/blueprints/wendao_julia_plugin_first_rollout.md`
  with the active mixed-graph retrieval blueprint as a bounded secondary
  reference:
  `.cache/agent/blueprints/wendao_search_mixed_graph_retrieval.md`
- opened the active execution plan for the next bounded slice at
  `.cache/agent/execplans/wendao-julia-graph-structural-solver-demo-live-smoke.md`
- scoped the slice to plugin-owned live smoke only: prove that
  `xiuxian-wendao-julia` can downcall the new `WendaoSearch.jl --mode solver_demo`
  structural-rerank service through both explicit transport config and
  capability-manifest discovery, without widening the current staged Rust
  graph-structural request contract
- landed plugin-owned real-service launch helpers for both single-route and
  same-port multi-route `solver_demo` services under
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/test_support/official_examples.rs`
- landed two bounded Rust live-smoke regressions in
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/graph_structural_exchange.rs`
  covering:
  explicit `graph_structural_transport` against a real
  `WendaoSearch.jl --mode solver_demo --route-name structural_rerank` child
  service, and manifest-discovery fallback against a same-port multi-route
  `WendaoSearch.jl --mode solver_demo` child service
- re-verified the bounded Rust slice:
  `direnv exec . cargo test -p xiuxian-wendao-julia graph_structural_exchange --lib`
  `direnv exec . cargo test -p xiuxian-wendao-julia capability_manifest --lib`
  `direnv exec . cargo check -p xiuxian-wendao-julia --lib`
- archived the completed execution plan at
  `.cache/agent/execplans/archives/wendao-julia-graph-structural-solver-demo-live-smoke.md`

## Wendao Host Graph-Structural Solver Demo Live Smoke

- re-checked the active Julia plugin-first rollout blueprint at
  `.cache/agent/blueprints/wendao_julia_plugin_first_rollout.md`
  with the active mixed-graph retrieval blueprint as a bounded secondary
  reference:
  `.cache/agent/blueprints/wendao_search_mixed_graph_retrieval.md`
- opened the active execution plan for the next bounded host slice at
  `.cache/agent/execplans/wendao-host-graph-structural-solver-demo-live-smoke.md`
- scoped the slice to host-through-language-seam live smoke only: prove that
  `xiuxian-wendao` can consume the existing Julia-owned graph-structural fetch
  helper against `WendaoSearch.jl --mode solver_demo` through
  `xiuxian_wendao::analyzers::languages`, without adding a host-local
  structural transport adapter or widening the staged request contract
- lifted the official `WendaoSearch` `solver_demo` launchers into the public
  Julia integration-support surface and linked builtin test-support, so host
  tests can consume the same official child-service seam without importing the
  plugin crate directly
- landed two bounded host-through-language-seam live-smoke regressions in
  `packages/rust/crates/xiuxian-wendao/tests/unit/link_graph_agentic/expansion.rs`
  covering:
  explicit `graph_structural_transport` against a real single-route
  `WendaoSearch.jl --mode solver_demo` child service, and
  manifest-discovery fallback against a real same-port multi-route
  `WendaoSearch.jl --mode solver_demo` child service
- kept the host boundary thin: the new live smoke uses the existing
  Julia-owned graph-structural fetch helper through
  `xiuxian_wendao::analyzers::languages`, and no new production structural
  transport adapter landed in `xiuxian-wendao`
- re-verified the bounded Rust slice:
  `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate host_uses_julia_graph_structural_fetch_helper`
  `direnv exec . cargo test -p xiuxian-wendao-builtin graph_structural --lib`
  `direnv exec . cargo check -p xiuxian-wendao --features julia --test xiuxian-testing-gate`
- archived the completed execution plan at
  `.cache/agent/execplans/archives/wendao-host-graph-structural-solver-demo-live-smoke.md`

## WendaoSearch Solver Demo Constraint Filter Live

- re-checked the active mixed-graph retrieval blueprint at
  `.cache/agent/blueprints/wendao_search_mixed_graph_retrieval.md`
  with the Julia plugin-first rollout blueprint as a bounded secondary
  reference:
  `.cache/agent/blueprints/wendao_julia_plugin_first_rollout.md`
- opened the active execution plan for the next bounded slice at
  `.cache/agent/execplans/wendao-search-solver-demo-constraint-filter-live.md`
- scoped the slice to one behavioral gap only: `WendaoSearch.jl --mode solver_demo`
  still routed `constraint_filter` through demo logic even though the explicit
  edge-endpoint request contract and live transport were already landed
- landed one solver-backed `constraint_filter` path in
  `.data/WendaoSearch.jl/src/service/solver_demo.jl` and wired runtime dispatch
  so `solver_demo` now serves both current graph-structural routes
- extended Julia package coverage with package-local and cross-process live
  solver-demo filter regressions, while also correcting the remaining
  projected-topology wording to explicit request topology
- extended `xiuxian-wendao-julia` plugin live smoke with:
  one explicit `/graph/structural/filter` solver-demo downcall and one
  manifest-discovered same-port multi-route solver-demo filter downcall
- extended host-through-builtin proof with one manifest-discovered
  solver-demo `constraint_filter` downcall, still through the thin Julia-owned
  graph-structural seam and without adding a host-local transport wrapper
- re-verified the bounded slice:
  `direnv exec . julia --project=.data/WendaoSearch.jl -e 'using Pkg; Pkg.test()'`
  `direnv exec . cargo test -p xiuxian-wendao-julia graph_structural_exchange --lib`
  `direnv exec . cargo test -p xiuxian-wendao-builtin graph_structural --lib`
  `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate host_uses_julia_graph_structural`
  `direnv exec . cargo check -p xiuxian-wendao --features julia --test xiuxian-testing-gate`
- archived the completed execution plan at
  `.cache/agent/execplans/archives/wendao-search-solver-demo-constraint-filter-live.md`

## Wendao Julia Generic Topology Live Proof

- re-checked the active mixed-graph retrieval blueprint at
  `.cache/agent/blueprints/wendao_search_mixed_graph_retrieval.md`
  with the Julia plugin-first rollout blueprint as a bounded secondary
  reference:
  `.cache/agent/blueprints/wendao_julia_plugin_first_rollout.md`
- opened the active execution plan for the next bounded slice at
  `.cache/agent/execplans/wendao-julia-generic-topology-live-proof.md`
- scoped the next implementation lane to one realism gap above the live
  service boundary: `WendaoSearch.jl` already supports explicit-edge topology,
  but high-level `xiuxian-wendao-julia` convenience helpers are still centered
  on two-node pair candidates

## Wendao Frontend Live Gateway Contract Realign

- re-ran the active gateway-downpressure blueprint check against
  `.cache/agent/blueprints/wendao_search_gateway_downpressure.md`
- opened the active execution plan for this slice at
  `.cache/agent/execplans/wendao-frontend-live-gateway-contract-realign.md`
- traced the restarted `127.0.0.1:9517` live gateway failures down to two
  concrete frontend contract issues:
  projected page-index tree decoding treated `UInt64` `rootCount` values as
  unsupported, and the refine-doc live proof spent its budget on a heavier
  symbol-discovery chain than the route under test
- updated `.data/wendao-frontend/src/api/arrowDocumentIpc.ts` so the document
  IPC decoder accepts safe `bigint` values from Arrow `UInt64` columns, then
  added a matching bigint regression in
  `.data/wendao-frontend/src/api/arrowDocumentIpc.test.ts`
- updated `.data/wendao-frontend/src/api/liveGateway.test.ts` so the refine-doc
  live contract resolves `symbol_id` through `/api/repo/symbol-search` and no
  longer depends on the slower repo-search plus knowledge-search fallback chain
- confirmed the bounded frontend regression set is green:
  `direnv exec . bash -lc 'cd .data/wendao-frontend && npm test -- --run src/api/arrowDocumentIpc.test.ts src/api/liveGateway.test.ts'`
- confirmed the restarted live gateway contract is green end-to-end:
  `direnv exec . bash -lc 'cd .data/wendao-frontend && RUN_LIVE_GATEWAY_TEST=1 npm run test:live-gateway'`
  now passes `11/11`, including the projected page-index tree and refine-doc
  same-origin Flight proofs

## Wendao Frontend Hotspot Perf Trace

- re-checked the active gateway-downpressure blueprint at
  `.cache/agent/blueprints/wendao_search_gateway_downpressure.md`
- resumed the active execution plan for this bounded perf-artifact slice at
  `.cache/agent/execplans/wendao-frontend-hotspot-perf-trace.md`
- re-ran the bounded hotspot and live gateway validation set against the
  restarted local `127.0.0.1:9517` gateway:
  `direnv exec . bash -lc 'cd .data/wendao-frontend && npm run test:hotspot-perf'`
  `direnv exec . bash -lc 'cd .data/wendao-frontend && RUN_LIVE_GATEWAY_TEST=1 npm run test:live-gateway'`
  `direnv exec . bash -lc 'cd .data/wendao-frontend && RUN_LIVE_GATEWAY_TEST=1 ./node_modules/.bin/vitest run src/api/liveRepoSearchGateway.test.ts src/api/liveCodeSearchGateway.test.ts'`
  `direnv exec . bash -lc 'cd .data/wendao-frontend && RUN_LIVE_GATEWAY_TEST=1 npm run test:live-flight-perf'`
- confirmed the rerun is green end-to-end:
  hotspot suite `10/10` files and `65/65` tests,
  live gateway suite `11/11`,
  live repo/code suites `4/4`,
  live Flight perf suite `3/3`
- recorded the latest warm-steady checkpoint in package docs:
  `.data/wendao-frontend/docs/05_research/308_live_flight_search_perf_report.md`
  now captures the restarted-gateway rerun at `1.95ms` average, `2.39ms` P95,
  with `GetFlightInfo` at `0.72ms`, `DoGet` at `0.65ms`, and the refreshed
  hotspot artifact still carrying `19` scenario records
- synchronized the package overview in `.data/wendao-frontend/README.md`
  so the current live/perf checkpoint matches the report instead of the older
  `2.49ms` / `5.17ms` snapshot
- archived the completed execution plan at
  `.cache/agent/execplans/archives/wendao-frontend-hotspot-perf-trace.md`

## Wendao Frontend API Contract TS Closure

- opened a frontend-specific API contract blueprint at
  `.cache/agent/blueprints/wendao_frontend_api_contract_ts_closure.md`
  so the remaining strict TypeScript cleanup no longer hangs off an unrelated
  gateway/search ownership blueprint
- synchronized package docs before implementation by recording the preferred
  strict TypeScript command in `.data/wendao-frontend/README.md` and narrowing
  this slice to `src/api/*` contract owner plus Arrow IPC payload typing work
- opened the active execution plan for this slice at
  `.cache/agent/execplans/wendao-frontend-api-contract-ts-closure.md`
- planned scope: clear the touched API contract owner drift
  (`bindings.ts` vs `apiContracts.ts`) and the Arrow IPC `ArrayBuffer`/`Uint8Array`
  typing mismatch without widening into unrelated StructuredDashboard or
  build-tooling debt in the same change
- moved handwritten API contract ownership back under
  `.data/wendao-frontend/src/api/apiContracts.ts` for refine-doc and repo
  status/overview/sync surfaces, then rewired the touched API consumers so
  `bindings.ts` no longer acts as the owner for handwritten studio contracts
- introduced `.data/wendao-frontend/src/api/arrowIpcPayload.ts` and broadened
  the touched Arrow IPC decoders so the API lane accepts both `ArrayBuffer`
  and `Uint8Array` payloads used by tests and runtime helpers
- closed the touched `src/api/*` strict TypeScript failures and re-ran the live
  gateway contract after the API cleanup:
  `direnv exec . bash -lc 'cd .data/wendao-frontend && npm test -- --run src/api/arrowDocumentIpc.test.ts src/api/arrowSearchIpc.test.ts src/api/arrowGraphIpc.test.ts src/api/arrowWorkspaceIpc.test.ts src/api/flightProjectedPageIndexTransport.test.ts src/api/flightRefineEntityDocTransport.test.ts src/api/apiContracts.test.ts src/api/uiConfigTransport.test.ts src/api/flightAnalysisTransport.test.ts src/api/clientFacade.test.ts src/api/liveFlightSearchPerf.test.ts'`
  `direnv exec . bash -lc 'cd .data/wendao-frontend && RUN_LIVE_GATEWAY_TEST=1 npm run test:live-gateway'`
- confirmed the remaining `./node_modules/.bin/tsc --noEmit --pretty false`
  frontier is now outside the bounded API slice:
  `scripts/rspack/*`, broader app/search surfaces, FileTree, DirectReader,
  GraphView, and ZenSearch or StructuredDashboard debt still remain, but the
  touched `src/api/*` cluster no longer appears in the compiler output

## Wendao Frontend Build Tooling TS Declarations

- opened a dedicated build-tooling blueprint at
  `.cache/agent/blueprints/wendao_frontend_build_tooling_ts_closure.md`
  so the next strict TypeScript slice does not widen the API-contract lane
- synchronized package docs before implementation by recording the next bounded
  frontier in `.data/wendao-frontend/README.md`:
  `scripts/rspack/*.mjs`, `scripts/build/*.mjs`, and `src/build/*`
- opened the active execution plan for this slice at
  `.cache/agent/execplans/wendao-frontend-build-tooling-ts-declarations.md`
- planned scope: publish precise colocated declarations for the stable build
  helper `.mjs` surface and clear the current TS7016 build-tooling cluster
  without widening into app/search or ZenSearch debt
- landed precise colocated `.d.mts` declarations for the stable build helper
  surface under `.data/wendao-frontend/scripts/rspack/`,
  `.data/wendao-frontend/scripts/build/`, and the top-level CLI shims so TS no
  longer falls back to missing-declaration `any` for those `.mjs` imports
- narrowed the runtime helper typings in
  `.data/wendao-frontend/scripts/rspack/build-environment.ts`,
  `.data/wendao-frontend/scripts/rspack/build-profile.ts`, and
  `.data/wendao-frontend/scripts/rspack/module-rules.ts` so the build-tooling
  tests consume the actual helper surface instead of wider Rspack unions
- cleaned the remaining local build-tooling strict errors in
  `src/build/mermaidProviderBakeoff.test.ts` and the `rspack` helper tests
- confirmed the bounded build-tooling regression set is green:
  `direnv exec . bash -lc 'cd .data/wendao-frontend && npm test -- --run src/build/mermaidProviderBakeoff.test.ts src/build/rspackBuildEnvironment.test.ts src/build/rspackBuildProfile.test.ts src/build/rspackChunkPolicy.test.ts src/build/rspackModuleRules.test.ts src/build/checkBuildSize.test.ts src/build/checkBuildSizeModelRuntime.test.ts src/build/mermaidBundleReport.test.ts src/build/mermaidBundleReportCli.test.ts src/build/mermaidBundleReportModelRuntime.test.ts src/build/mermaidProviderManifest.test.ts src/build/rspackHelperBarrel.test.ts'`
- confirmed the strict compiler frontier moved beyond build-tooling:
  `direnv exec . bash -lc 'cd .data/wendao-frontend && ./node_modules/.bin/tsc --noEmit --pretty false'`
  no longer reports `scripts/rspack/*.mjs`, `scripts/build/*.mjs`, or
  `src/build/*`; the remaining frontier is now broader app and UI debt such as
  `src/App*`, `DiagramWindow`, `DirectReader`, `FileTree`, `SearchBar`, and
  ZenSearch or StructuredDashboard
- archived the completed execution plan at
  `.cache/agent/execplans/archives/wendao-frontend-build-tooling-ts-declarations.md`
  and the completed blueprint at
  `.cache/agent/blueprints/archives/wendao_frontend_build_tooling_ts_closure.md`

## Wendao Frontend App Shell TS Closure

- opened an app-shell-specific blueprint at
  `.cache/agent/blueprints/wendao_frontend_app_shell_ts_closure.md`
  so the next strict TypeScript slice stays bounded to `App.tsx` and
  `App.test.tsx`
- synchronized package docs before implementation by recording the app-shell
  frontier in `.data/wendao-frontend/README.md`
- opened the active execution plan for this slice at
  `.cache/agent/execplans/wendao-frontend-app-shell-ts-closure.md`
- planned scope: clear the current Zustand setter misuse and the touched
  over-narrow selection callback typings in `App.test.tsx` without widening
  into `DiagramWindow`, `DirectReader`, `FileTree`, `SearchBar`, or ZenSearch
- aligned `.data/wendao-frontend/src/App.tsx` with the real editor-store
  boolean setter contract instead of using a React updater closure
- rewired the touched callback proofs in
  `.data/wendao-frontend/src/App.test.tsx` onto the shared
  `SearchSelection` and `MainViewGraphSelection` types so the tests no longer
  carry narrower local callback contracts
- confirmed the bounded app-shell proof is green:
  `direnv exec . bash -lc 'cd .data/wendao-frontend && npm test -- --run src/App.test.tsx'`
- confirmed the strict compiler frontier moved past the app shell:
  `direnv exec . bash -lc 'cd .data/wendao-frontend && ./node_modules/.bin/tsc --noEmit --pretty false'`
  no longer reports `src/App.tsx` or `src/App.test.tsx`
- archived the completed execution plan at
  `.cache/agent/execplans/archives/wendao-frontend-app-shell-ts-closure.md`
  and the completed blueprint at
  `.cache/agent/blueprints/archives/wendao_frontend_app_shell_ts_closure.md`

## Wendao Frontend DiagramWindow TS Closure

- opened a DiagramWindow-specific blueprint at
  `.cache/agent/blueprints/wendao_frontend_diagram_window_ts_closure.md`
  so the next strict TypeScript slice stayed bounded to the current
  `mermaidUnsupported` copy-contract drift
- synchronized package docs before implementation by recording the new
  `DiagramWindow` frontier in `.data/wendao-frontend/README.md`
- opened the active execution plan for this slice at
  `.cache/agent/execplans/wendao-frontend-diagram-window-ts-closure.md`
- planned scope: propagate the shared `mermaidUnsupported` copy field through
  `DiagramWindow` builders, component props, and the touched test fixtures
  without widening into `DirectReader`, `FileTree`, `SearchBar`, or ZenSearch
- updated
  `.data/wendao-frontend/src/components/panels/DiagramWindow/diagramWindowViewModel.ts`
  so `buildDiagramWindowWorkspaceCopy(...)` now forwards the shared
  `mermaidUnsupported` field into the workspace payload
- updated
  `.data/wendao-frontend/src/components/panels/DiagramWindow/DiagramWindow.tsx`
  so the view-model hook receives the full copy payload including
  `mermaidUnsupported`
- aligned the touched test fixtures in
  `.data/wendao-frontend/src/components/panels/DiagramWindow/__tests__/diagramWindowViewModel.test.ts`
  and
  `.data/wendao-frontend/src/components/panels/DiagramWindow/__tests__/useDiagramWindowViewModel.test.tsx`
  with the shared copy contract
- confirmed the bounded DiagramWindow proof is green:
  `direnv exec . bash -lc 'cd .data/wendao-frontend && npm test -- --run src/components/panels/DiagramWindow/__tests__/diagramWindowViewModel.test.ts src/components/panels/DiagramWindow/__tests__/useDiagramWindowViewModel.test.tsx'`
- confirmed the strict compiler frontier moved past the touched DiagramWindow
  files:
  `direnv exec . bash -lc 'cd .data/wendao-frontend && ./node_modules/.bin/tsc --noEmit --pretty false'`
  no longer reports `src/components/panels/DiagramWindow/*`
- archived the completed execution plan at
  `.cache/agent/execplans/archives/wendao-frontend-diagram-window-ts-closure.md`
  and the completed blueprint at
  `.cache/agent/blueprints/archives/wendao_frontend_diagram_window_ts_closure.md`

## Wendao Frontend Render Hygiene TS Closure

- opened a render-hygiene-specific blueprint at
  `.cache/agent/blueprints/wendao_frontend_render_hygiene_ts_closure.md`
  so the next strict TypeScript slice stayed bounded to the current
  top-of-stack `CosmicBackground` and code-syntax hygiene errors
- synchronized package docs before implementation by recording the new
  top-of-stack render frontier in `.data/wendao-frontend/README.md`
- opened the active execution plan for this slice at
  `.cache/agent/execplans/wendao-frontend-render-hygiene-ts-closure.md`
- planned scope: remove the stale unused `React` import in
  `CodeSyntaxHighlighter.test.tsx` and align `CosmicBackground`'s `Canvas dpr`
  literal with the mutable tuple expected by `@react-three/fiber`, without
  widening into the larger `DirectReader`, `FileTree`, `SearchBar`, or
  ZenSearch clusters
- removed the unused `React` import from
  `.data/wendao-frontend/src/components/code-syntax/CodeSyntaxHighlighter.test.tsx`
- updated `.data/wendao-frontend/src/components/CosmicBackground.tsx` so the
  shared `Canvas dpr` literal is explicitly typed as `[number, number]` instead
  of a readonly tuple
- confirmed the bounded render-surface proof is green:
  `direnv exec . bash -lc 'cd .data/wendao-frontend && npm test -- --run src/components/code-syntax/CodeSyntaxHighlighter.test.tsx src/components/__tests__/CosmicBackground.r3f.test.tsx'`
- confirmed the strict compiler frontier moved past the touched render files:
  `direnv exec . bash -lc 'cd .data/wendao-frontend && ./node_modules/.bin/tsc --noEmit --pretty false'`
  no longer reports `CodeSyntaxHighlighter.test.tsx` or `CosmicBackground.tsx`
  and now starts at `src/components/panels/DirectReader/*`
- archived the completed execution plan at
  `.cache/agent/execplans/archives/wendao-frontend-render-hygiene-ts-closure.md`
  and the completed blueprint at
  `.cache/agent/blueprints/archives/wendao_frontend_render_hygiene_ts_closure.md`

## Wendao Frontend DirectReader TS Closure

- opened a DirectReader-specific blueprint at
  `.cache/agent/blueprints/wendao_frontend_direct_reader_ts_closure.md`
  so the next strict TypeScript lane stayed bounded to the current
  top-of-stack `DirectReader` frontier
- opened the first bounded execution plan for the render/parser seam at
  `.cache/agent/execplans/wendao-frontend-direct-reader-render-ts-closure.md`
- planned scope for the first DirectReader slice: export and consume
  `parseBiLink(...)` from its owner module, align the source-line scroll helper
  handoff with `Element | undefined`, and make the markdown plugin lists
  compatible with `ReactMarkdown`
- updated
  `.data/wendao-frontend/src/components/panels/DirectReader/markdownWaterfallBiLinks.ts`
  so `parseBiLink(...)` is exported from its owner module
- updated
  `.data/wendao-frontend/src/components/panels/DirectReader/DirectReader.tsx`
  so the source-line scroll helper receives `targetLine ?? undefined` and the
  component consumes the owner-exported `parseBiLink(...)`
- updated
  `.data/wendao-frontend/src/components/panels/DirectReader/DirectReaderRichContent.tsx`
  and
  `.data/wendao-frontend/src/components/panels/DirectReader/MarkdownWaterfallSectionBody.tsx`
  so the markdown plugin lists are typed as `PluggableList` instead of readonly
  arrays that drift from `ReactMarkdown`
- confirmed the bounded render/parser proof is green:
  `direnv exec . bash -lc 'cd .data/wendao-frontend && npm test -- --run src/components/panels/DirectReader/DirectReader.test.tsx src/components/panels/DirectReader/MarkdownWaterfall.test.tsx'`
- confirmed the strict compiler frontier moved deeper into the DirectReader
  waterfall-model seam after the first slice
- opened the second bounded execution plan for the waterfall-model seam at
  `.cache/agent/execplans/wendao-frontend-direct-reader-waterfall-model-ts-closure.md`
- planned scope for the second DirectReader slice: align
  `AnalysisNodeKind` with the observed markdown-analysis observation nodes and
  narrow local markdown AST children so frontmatter and heading handling use
  the actual parsed node shapes
- updated `.data/wendao-frontend/src/api/bindings.ts` so `AnalysisNodeKind`
  now includes `observation`, matching the active markdown-analysis retrieval
  surface and the DirectReader fixtures already used in the frontend
- updated
  `.data/wendao-frontend/src/components/panels/DirectReader/markdownWaterfallModel.ts`
  so the parser keeps `Root` ownership but narrows local `rootChildren` through
  `MarkdownFrontmatterAstNode` and `MarkdownHeadingAstNode` aliases before
  frontmatter and heading logic reads `toml` or `depth`
- confirmed the bounded waterfall-model proof is green:
  `direnv exec . bash -lc 'cd .data/wendao-frontend && npm test -- --run src/components/panels/DirectReader/MarkdownWaterfall.test.tsx'`
- confirmed the strict compiler frontier moved past DirectReader completely:
  `direnv exec . bash -lc 'cd .data/wendao-frontend && ./node_modules/.bin/tsc --noEmit --pretty false'`
  no longer reports `src/components/panels/DirectReader/*` and now starts at
  `src/components/panels/FileTree/*`
- archived the completed execution plans at
  `.cache/agent/execplans/archives/wendao-frontend-direct-reader-render-ts-closure.md`
  and
  `.cache/agent/execplans/archives/wendao-frontend-direct-reader-waterfall-model-ts-closure.md`
- archived the completed blueprint at
  `.cache/agent/blueprints/archives/wendao_frontend_direct_reader_ts_closure.md`

## Wendao Frontend FileTree TS Closure

- opened a FileTree-specific blueprint at
  `.cache/agent/blueprints/wendao_frontend_file_tree_ts_closure.md`
  so the next strict TypeScript lane stays bounded to the current top-of-stack
  `FileTree` frontier
- synchronized package docs by keeping `.data/wendao-frontend/README.md`
  aligned to the new `FileTree` frontier before implementation
- opened the active execution plan for the first bounded slice at
  `.cache/agent/execplans/wendao-frontend-file-tree-jsx-status-ts-closure.md`
- planned scope: close the current JSX return-type namespace drift, stale
  unused `React` value imports, repo-index status helper `toSorted` and
  implicit-`any` drift, and the touched repo-index mock shape mismatch in
  `src/components/panels/FileTree/__tests__/FileTree.test.tsx`
- updated
  `.data/wendao-frontend/src/components/panels/FileTree/FileTreeContent.tsx`,
  `.data/wendao-frontend/src/components/panels/FileTree/FileTreeNodes.tsx`,
  `.data/wendao-frontend/src/components/panels/FileTree/FileTreeToolbar.tsx`,
  and `.data/wendao-frontend/src/components/panels/FileTree/TreeNode.tsx`
  so the panel aligns with the automatic JSX runtime instead of using legacy
  `JSX.Element` return annotations or stale value imports
- updated
  `.data/wendao-frontend/src/components/panels/FileTree/repoIndexStatus.ts`
  and `.data/wendao-frontend/src/components/panels/FileTree/treeModel.ts` so
  the local helper surface uses explicitly typed stable array sorting instead
  of `toSorted(...)` and implicit-`any` callbacks
- updated
  `.data/wendao-frontend/src/components/panels/FileTree/__tests__/FileTree.test.tsx`
  so the touched repo-index mock payload now includes the current concurrency
  fields required by `RepoIndexStatusResponse`
- confirmed the bounded `FileTree` proof is green:
  `direnv exec . bash -lc 'cd .data/wendao-frontend && npm test -- --run src/components/panels/FileTree/__tests__/FileTree.test.tsx src/components/panels/FileTree/repoIndexStatus.test.ts'`
- confirmed the strict compiler frontier moved past `FileTree` completely:
  `direnv exec . bash -lc 'cd .data/wendao-frontend && ./node_modules/.bin/tsc --noEmit --pretty false'`
  no longer reports `src/components/panels/FileTree/*`; the next frontier now
  starts at `GraphView`, then flows into `MainView`, `PropertyEditor`,
  `VfsSidebar`, and repo-diagnostics surfaces
- archived the completed execution plan at
  `.cache/agent/execplans/archives/wendao-frontend-file-tree-jsx-status-ts-closure.md`
- archived the completed blueprint at
  `.cache/agent/blueprints/archives/wendao_frontend_file_tree_ts_closure.md`

## Wendao Frontend GraphView TS Closure

- opened a GraphView-specific blueprint at
  `.cache/agent/blueprints/wendao_frontend_graph_view_ts_closure.md`
  so the next strict TypeScript lane stays bounded to the current top-of-stack
  `GraphView` frontier
- synchronized package docs by keeping `.data/wendao-frontend/README.md`
  aligned to the new `GraphView` frontier before implementation
- opened the active execution plan for the first bounded slice at
  `.cache/agent/execplans/wendao-frontend-graph-view-stage-summary-ts-closure.md`
- planned scope: remove the stale unused `React` import in
  `src/components/panels/GraphView/__tests__/GraphSVG.test.tsx` and replace
  the local `toSorted(...)` and implicit-`any` stage-summary drift in
  `src/components/panels/GraphView/GraphView.tsx`
- removed the stale value `React` import from
  `.data/wendao-frontend/src/components/panels/GraphView/__tests__/GraphSVG.test.tsx`
- updated `.data/wendao-frontend/src/components/panels/GraphView/GraphView.tsx`
  so the stage-summary layer bucketing uses explicit stable sorting instead of
  `toSorted(...)` and implicit-`any` tuple destructuring
- confirmed the bounded `GraphView` proof is green:
  `direnv exec . bash -lc 'cd .data/wendao-frontend && npm test -- --run src/components/panels/GraphView/__tests__/GraphSVG.test.tsx src/components/panels/GraphView/__tests__/GraphView.test.tsx'`
- confirmed the strict compiler frontier moved past `GraphView` completely:
  `direnv exec . bash -lc 'cd .data/wendao-frontend && ./node_modules/.bin/tsc --noEmit --pretty false'`
  no longer reports `src/components/panels/GraphView/*`; the next frontier now
  starts at `MainView`, then flows into `PropertyEditor`, `VfsSidebar`, and
  repo-diagnostics surfaces
- archived the completed execution plan at
  `.cache/agent/execplans/archives/wendao-frontend-graph-view-stage-summary-ts-closure.md`
- archived the completed blueprint at
  `.cache/agent/blueprints/archives/wendao_frontend_graph_view_ts_closure.md`

## Wendao Frontend MainView TS Closure

- opened a MainView-specific blueprint at
  `.cache/agent/blueprints/wendao_frontend_main_view_ts_closure.md`
  so the next strict TypeScript lane stays bounded to the current top-of-stack
  `MainView` frontier
- synchronized package docs by keeping `.data/wendao-frontend/README.md`
  aligned to the new `MainView` frontier before implementation
- opened the active execution plan for the first bounded slice at
  `.cache/agent/execplans/wendao-frontend-main-view-selected-file-ts-closure.md`
- planned scope: add the missing `category` field to the touched
  `MainViewSelectedFile` fixtures, replace the local `toSorted(...)` usage in
  `mainViewLazyPanels.test.ts` and `mainViewTabPanels.test.tsx`, and narrow the
  `selectedFile.content` handoff in `MainViewDiagramPanel.tsx`
- updated the touched `MainView` test fixtures so `MainViewSelectedFile`
  carries the required `category` field in
  `MainViewContentPanel.test.tsx`, `MainViewDiagramPanel.test.tsx`, and
  `MainViewReferencesPanel.test.tsx`
- updated `mainViewLazyPanels.test.ts` and `mainViewTabPanels.test.tsx` so the
  local key-order assertions use explicit stable `sort()` instead of
  `toSorted(...)`
- updated `.data/wendao-frontend/src/components/panels/MainView/MainViewDiagramPanel.tsx`
  so selected content is narrowed through an explicit early-return before
  passing it to `DiagramWindow`
- confirmed the bounded `MainView` proof is green:
  `direnv exec . bash -lc 'cd .data/wendao-frontend && npm test -- --run src/components/panels/MainView/MainViewContentPanel.test.tsx src/components/panels/MainView/MainViewDiagramPanel.test.tsx src/components/panels/MainView/MainViewReferencesPanel.test.tsx src/components/panels/MainView/mainViewLazyPanels.test.ts src/components/panels/MainView/mainViewTabPanels.test.tsx'`
- confirmed the strict compiler frontier moved past `MainView` completely:
  `direnv exec . bash -lc 'cd .data/wendao-frontend && ./node_modules/.bin/tsc --noEmit --pretty false'`
  no longer reports `src/components/panels/MainView/*`; the next frontier now
  starts at `PropertyEditor`, then flows into `VfsSidebar` and
  repo-diagnostics surfaces
- archived the completed execution plan at
  `.cache/agent/execplans/archives/wendao-frontend-main-view-selected-file-ts-closure.md`
- archived the completed blueprint at
  `.cache/agent/blueprints/archives/wendao_frontend_main_view_ts_closure.md`

## Wendao Frontend PropertyEditor TS Closure

- opened a PropertyEditor-specific blueprint at
  `.cache/agent/blueprints/wendao_frontend_property_editor_ts_closure.md`
  so the next strict TypeScript lane stays bounded to the current top-of-stack
  `PropertyEditor` frontier
- synchronized package docs by keeping `.data/wendao-frontend/README.md`
  aligned to the new `PropertyEditor` frontier before implementation
- opened the active execution plan for the first bounded slice at
  `.cache/agent/execplans/wendao-frontend-property-editor-graph-summary-ts-closure.md`
- planned scope: replace the local `toSorted(...)` usage and add explicit
  callback typing through the graph-summary aggregation chain in
  `src/components/panels/PropertyEditor/PropertyEditor.tsx`
- updated `.data/wendao-frontend/src/components/panels/PropertyEditor/PropertyEditor.tsx`
  so the graph-summary layer aggregation uses explicit typed sorting, mapping,
  reduction, lookup, and focus selection instead of `toSorted(...)` and
  implicit callback parameters
- confirmed the bounded `PropertyEditor` proof is green:
  `direnv exec . bash -lc 'cd .data/wendao-frontend && npm test -- --run src/components/panels/PropertyEditor/__tests__/PropertyEditor.test.tsx'`
- confirmed the strict compiler frontier moved past `PropertyEditor`
  completely:
  `direnv exec . bash -lc 'cd .data/wendao-frontend && ./node_modules/.bin/tsc --noEmit --pretty false'`
  no longer reports `src/components/panels/PropertyEditor/*`; the next
  frontier now starts at `VfsSidebar`, then flows into repo-diagnostics
  surfaces
- archived the completed execution plan at
  `.cache/agent/execplans/archives/wendao-frontend-property-editor-graph-summary-ts-closure.md`
- archived the completed blueprint at
  `.cache/agent/blueprints/archives/wendao_frontend_property_editor_ts_closure.md`

## Wendao Frontend VfsSidebar TS Closure

- opened a VfsSidebar-specific blueprint at
  `.cache/agent/blueprints/wendao_frontend_vfs_sidebar_ts_closure.md`
  so the next strict TypeScript lane stays bounded to the current top-of-stack
  `VfsSidebar` frontier
- synchronized package docs by keeping `.data/wendao-frontend/README.md`
  aligned to the new `VfsSidebar` frontier before implementation
- opened the active execution plan for the first bounded slice at
  `.cache/agent/execplans/wendao-frontend-vfs-sidebar-sort-ts-closure.md`
- planned scope: remove the stale `React` import in
  `src/components/panels/VfsSidebar/__tests__/VfsSidebar.test.tsx`, replace
  the local `toSorted(...)` usage, and add explicit callback typing through the
  touched sort and map chain in `src/components/panels/VfsSidebar/VfsSidebar.tsx`
- updated `src/components/panels/VfsSidebar/__tests__/VfsSidebar.test.tsx` so
  the touched test no longer carries a stale runtime `React` import
- updated `.data/wendao-frontend/src/components/panels/VfsSidebar/VfsSidebar.tsx`
  so the local child and root-entry ordering uses explicit stable sorting and
  typed callbacks instead of `toSorted(...)`
- confirmed the bounded `VfsSidebar` proof is green:
  `direnv exec . bash -lc 'cd .data/wendao-frontend && npm test -- --run src/components/panels/VfsSidebar/__tests__/VfsSidebar.test.tsx'`
- confirmed the strict compiler frontier moved past `VfsSidebar` completely:
  `direnv exec . bash -lc 'cd .data/wendao-frontend && ./node_modules/.bin/tsc --noEmit --pretty false'`
  no longer reports `src/components/panels/VfsSidebar/*`; the next frontier
  now starts at repo diagnostics
- archived the completed execution plan at
  `.cache/agent/execplans/archives/wendao-frontend-vfs-sidebar-sort-ts-closure.md`
- archived the completed blueprint at
  `.cache/agent/blueprints/archives/wendao_frontend_vfs_sidebar_ts_closure.md`

## Wendao Frontend Repo Diagnostics TS Closure

- opened a repo-diagnostics-specific blueprint at
  `.cache/agent/blueprints/wendao_frontend_repo_diagnostics_ts_closure.md`
  so the next strict TypeScript lane stays bounded to the current top-of-stack
  repo-diagnostics frontier
- synchronized package docs by keeping `.data/wendao-frontend/README.md`
  aligned to the new repo-diagnostics frontier before implementation
- opened the active execution plan for the first bounded slice at
  `.cache/agent/execplans/wendao-frontend-repo-diagnostics-jsx-ts-closure.md`
- planned scope: remove the stale runtime `React` import and replace the
  legacy `JSX.Element` annotations in
  `src/components/repoDiagnostics/RepoDiagnosticsDrawer.tsx`
- updated `.data/wendao-frontend/src/components/repoDiagnostics/RepoDiagnosticsDrawer.tsx`
  so the drawer uses hook plus type-only React imports and `ReactElement`
  return annotations instead of the legacy runtime import and `JSX.Element`
  namespace
- confirmed the existing repo-diagnostics feature proof remains green:
  `direnv exec . bash -lc 'cd .data/wendao-frontend && npm test -- --run src/components/repoDiagnostics/__tests__/state.test.ts'`
- discovered that strict `tsc --noEmit --pretty false` now moves deeper than
  the initial drawer-only scope:
  `src/components/repoDiagnostics/state.ts`,
  `src/components/RepoDiagnosticsPage.tsx`, and
  `src/components/RepoDiagnosticsPage.test.tsx` still report broader
  repo-diagnostics drift, and the compiler frontier continues beyond that into
  `SearchBar`, `ZenSearch`, controllers, and other shared surfaces
- opened the follow-on bounded execution plan for the deeper repo-diagnostics
  slice at
  `.cache/agent/execplans/wendao-frontend-repo-diagnostics-state-page-ts-closure.md`
- planned scope: replace local `toSorted(...)` and implicit callback typing in
  `src/components/repoDiagnostics/state.ts` and
  `src/components/RepoDiagnosticsPage.tsx`, then align the touched page tests
  in `src/components/RepoDiagnosticsPage.test.tsx` with the current contract
- updated `.data/wendao-frontend/src/components/repoDiagnostics/state.ts`,
  `.data/wendao-frontend/src/components/RepoDiagnosticsPage.tsx`, and
  `.data/wendao-frontend/src/components/RepoDiagnosticsPage.test.tsx` so the
  repo-diagnostics surface now uses stable `sort(...)`, explicit response
  contracts, `ReactElement`, and a typed selected-failure context instead of
  leaking nullable machine keys through the failure-only actions
- confirmed the bounded repo-diagnostics proof is green:
  `direnv exec . bash -lc 'cd .data/wendao-frontend && npm test -- --run src/components/repoDiagnostics/__tests__/state.test.ts src/components/RepoDiagnosticsPage.test.tsx'`
- confirmed the strict compiler frontier moved past repo diagnostics
  completely:
  `direnv exec . bash -lc 'cd .data/wendao-frontend && ./node_modules/.bin/tsc --noEmit --pretty false'`
  no longer reports `src/components/repoDiagnostics/*` or
  `src/components/RepoDiagnosticsPage*`; the next frontier now starts at
  `SearchBar`, then flows into `ZenSearch`, controllers, and shared surfaces
- archived the completed execution plans at
  `.cache/agent/execplans/archives/wendao-frontend-repo-diagnostics-jsx-ts-closure.md`
  and
  `.cache/agent/execplans/archives/wendao-frontend-repo-diagnostics-state-page-ts-closure.md`
- archived the completed blueprint at
  `.cache/agent/blueprints/archives/wendao_frontend_repo_diagnostics_ts_closure.md`

## Wendao Runtime Plugin Arrow Request-Batch Builder Extraction

- re-ran the active plugin-core-runtime blueprint check against
  `.cache/agent/blueprints/wendao_arrow_plugin_core_runtime_migration.md`
- synchronized package docs before implementation so the next bounded slice is
  explicit:
  anchor-hit plus embedding to `WendaoArrow` request-batch assembly should move
  into the runtime-owned `plugin_arrow_exchange` seam, while host quantum
  ignition keeps query-vector resolution and vector-store retrieval ownership
- opened the active execution plan for this slice at
  `.cache/agent/execplans/wendao-runtime-plugin-arrow-request-batch-builder-extraction.md`
- landed a runtime-owned scored-candidate request helper in
  `packages/rust/crates/xiuxian-wendao-runtime/src/transport/plugin_arrow_exchange/request.rs`
  so runtime now owns candidate-plus-embedding to `WendaoArrow` request-batch
  materialization
- rewired
  `packages/rust/crates/xiuxian-wendao/src/link_graph/index/search/quantum_fusion/openai_ignition.rs`
  and
  `packages/rust/crates/xiuxian-wendao/src/link_graph/index/search/quantum_fusion/vector_ignition.rs`
  so host ignition adapters no longer assemble `PluginArrowRequestRow`
  locally; they now resolve query signal plus embeddings, then delegate the
  request-shaped assembly to runtime
- added runtime-side proof for candidate-id doc shaping and missing-embedding
  failure in
  `packages/rust/crates/xiuxian-wendao-runtime/src/transport/plugin_arrow_exchange/tests.rs`
- confirmed focused validation is green on the default cargo target:
  `direnv exec . cargo fmt -p xiuxian-wendao-runtime -p xiuxian-wendao`
  `direnv exec . cargo test -p xiuxian-wendao-runtime --features transport plugin_arrow_exchange`
  `direnv exec . cargo test -p xiuxian-wendao --lib link_graph::index::search::quantum_fusion`
  `direnv exec . cargo test -p xiuxian-wendao --lib link_graph::index::search::plan::payload::quantum`
  `direnv exec . cargo check -p xiuxian-wendao --no-default-features --features julia,zhenfa-router`
  `direnv exec . git diff --check`
- archived the completed execution plan at
  `.cache/agent/execplans/archives/wendao-runtime-plugin-arrow-request-batch-builder-extraction.md`

## Wendao Runtime Plugin Arrow Request Metadata Extraction

- re-ran the active plugin-core-runtime blueprint check against
  `.cache/agent/blueprints/wendao_arrow_plugin_core_runtime_migration.md`
- synchronized package docs before implementation so the next bounded slice is
  explicit:
  `WendaoArrow` request-batch metadata attachment for `trace_id` and schema
  version should move into the runtime-owned `plugin_arrow_exchange` seam,
  while host `rerank.rs` keeps domain telemetry and score-application
  ownership
- opened the active execution plan for this slice at
  `.cache/agent/execplans/wendao-runtime-plugin-arrow-request-metadata-extraction.md`
- landed a runtime-owned metadata helper surface under
  `packages/rust/crates/xiuxian-wendao-runtime/src/transport/plugin_arrow_exchange/metadata.rs`
  and re-exported it through the `plugin_arrow_exchange` interface seam
- rewired
  `packages/rust/crates/xiuxian-wendao/src/link_graph/index/search/plan/payload/quantum/rerank.rs`
  so host rerank wrappers no longer call `attach_record_batch_metadata`
  directly; request trace-id normalization and schema metadata attachment now
  delegate to runtime
- moved metadata-shaping proof from host quantum tests into runtime transport
  tests, leaving host quantum proof focused on telemetry and score application
- confirmed focused validation is green on the default cargo target:
  `direnv exec . cargo fmt -p xiuxian-wendao-runtime -p xiuxian-wendao`
  `direnv exec . cargo test -p xiuxian-wendao-runtime --features transport plugin_arrow_exchange`
  `direnv exec . cargo test -p xiuxian-wendao --lib link_graph::index::search::plan::payload::quantum`
  `direnv exec . cargo check -p xiuxian-wendao --no-default-features --features julia,zhenfa-router`
  `direnv exec . git diff --check`
- archived the completed execution plan at
  `.cache/agent/execplans/archives/wendao-runtime-plugin-arrow-request-metadata-extraction.md`

## Wendao Quantum Fusion Plugin Request Wrapper Ownership Cutover

- re-ran the active plugin-core-runtime blueprint check against
  `.cache/agent/blueprints/wendao_arrow_plugin_core_runtime_migration.md`
- synchronized package docs before implementation so the next bounded slice is
  explicit:
  backend-specific plugin rerank request wrapper logic should live in the
  owning `quantum_fusion` ignition adapters, not in
  `link_graph/index/search/plan/payload/quantum/rerank.rs`, and the no-longer
  used `build_julia_rerank_request_batch` compatibility shims should retire
- opened the active execution plan for this slice at
  `.cache/agent/execplans/wendao-quantum-fusion-plugin-request-wrapper-ownership-cutover.md`
- landed the bounded owner cut so backend-specific plugin request wrapper logic
  now lives with the semantic-ignition owners in
  `link_graph/index/search/quantum_fusion/openai_ignition.rs` and
  `link_graph/index/search/quantum_fusion/vector_ignition.rs`, not in host
  `link_graph/index/search/plan/payload/quantum/rerank.rs`
- removed the dead Julia-named compatibility request-builder shims from the
  ignition adapters and deleted the now-unused host-local wrapper functions
- closed the touched-scope warning that surfaced during validation by removing
  the leftover unused `RecordBatch` import from host `quantum/rerank.rs`
- confirmed focused validation is green on the default cargo target:
  `direnv exec . cargo fmt -p xiuxian-wendao`
  `direnv exec . cargo test -p xiuxian-wendao --lib link_graph::index::search::quantum_fusion --message-format=short`
  `direnv exec . cargo test -p xiuxian-wendao --lib link_graph::index::search::plan::payload::quantum --message-format=short`
  `direnv exec . cargo check -p xiuxian-wendao --no-default-features --features julia,zhenfa-router --message-format=short`
  `direnv exec . git diff --check`
- archived the completed execution plan at
  `.cache/agent/execplans/archives/wendao-quantum-fusion-plugin-request-wrapper-ownership-cutover.md`

## Wendao Runtime Plugin Arrow Provider-Aware Trace Id Cutover

- re-ran the active plugin-core-runtime blueprint check against
  `.cache/agent/blueprints/wendao_arrow_plugin_core_runtime_migration.md`
- synchronized package docs before implementation so the next bounded slice is
  explicit: the runtime-owned plugin arrow request trace-id helper should not
  hardcode `julia-rerank:*`; it should build one provider-aware generic trace
  id so runtime stays plugin-agnostic while host ignition adapters thread the
  negotiated provider id through
- opened the active execution plan for this slice at
  `.cache/agent/execplans/wendao-runtime-plugin-arrow-provider-aware-trace-id-cutover.md`
- landed the bounded runtime cut so the plugin arrow request trace-id helper no
  longer hardcodes `julia-rerank:*`; it now emits a generic provider-aware
  trace id and the ignition owners thread the negotiated provider id through
- updated the focused proof surface so runtime transport tests,
  `quantum_fusion` ignition tests, and the Julia metadata integration probe all
  assert the provider-aware trace-id shape
- confirmed focused validation is green on the default cargo target:
  `direnv exec . cargo fmt -p xiuxian-wendao-runtime -p xiuxian-wendao`
  `direnv exec . cargo test -p xiuxian-wendao-runtime --features transport plugin_arrow_exchange`
  `direnv exec . cargo test -p xiuxian-wendao --lib link_graph::index::search::quantum_fusion`
  `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate planned_search_julia_rerank_metadata_example::test_planned_search_payload_applies_official_stream_metadata_example --features julia -- --exact`
  `direnv exec . cargo check -p xiuxian-wendao --no-default-features --features julia,zhenfa-router`
  `direnv exec . git diff --check`
- archived the completed execution plan at
  `.cache/agent/execplans/archives/wendao-runtime-plugin-arrow-provider-aware-trace-id-cutover.md`

## Wendao Runtime Plugin Arrow Metadata-Aware Request Shaping Extraction

- re-ran the active plugin-core-runtime blueprint check against
  `.cache/agent/blueprints/wendao_arrow_plugin_core_runtime_migration.md`
- synchronized package docs before implementation so the next bounded slice is
  explicit: `quantum_fusion/openai_ignition.rs` and
  `quantum_fusion/vector_ignition.rs` still duplicate the same runtime-owned
  “build request batch from scored candidates plus provider-aware metadata”
  shaping, so that composition should move down into
  `xiuxian-wendao-runtime`
- opened the active execution plan for this slice at
  `.cache/agent/execplans/wendao-runtime-plugin-arrow-metadata-aware-request-shaping-extraction.md`
- landed the bounded runtime cut so `xiuxian-wendao-runtime` now owns one
  combined helper for “request batch from scored candidates plus provider-aware
  metadata”, instead of making both ignition owners repeat that composition
- rewired `quantum_fusion/openai_ignition.rs` and
  `quantum_fusion/vector_ignition.rs` so they keep only owner-local query
  vector and embedding resolution while runtime owns the shared request-shaping
  composition
- closed the touched-scope warning surfaced during validation by moving the
  `VectorStoreSemanticIgnition::build_plugin_rerank_request_batch` doc comment
  back onto the public method after inserting the private embedding helper
- confirmed focused validation is green on the default cargo target:
  `direnv exec . cargo fmt -p xiuxian-wendao-runtime -p xiuxian-wendao`
  `direnv exec . cargo test -p xiuxian-wendao-runtime --features transport plugin_arrow_exchange`
  `direnv exec . cargo test -p xiuxian-wendao --lib link_graph::index::search::quantum_fusion`
  `direnv exec . cargo check -p xiuxian-wendao --no-default-features --features julia,zhenfa-router`
  `direnv exec . git diff --check`
- archived the completed execution plan at
  `.cache/agent/execplans/archives/wendao-runtime-plugin-arrow-metadata-aware-request-shaping-extraction.md`

## Wendao Runtime Plugin Arrow Candidate Projection Extraction

- re-ran the active plugin-core-runtime blueprint check against
  `.cache/agent/blueprints/wendao_arrow_plugin_core_runtime_migration.md`
- synchronized package docs before implementation so the next bounded slice is
  explicit: `quantum_fusion/openai_ignition.rs` and
  `quantum_fusion/vector_ignition.rs` still duplicate the same
  `anchors -> doc_ids + PluginArrowScoredCandidate` projection, and that
  generic shaping should move into `xiuxian-wendao-runtime`
- opened the active execution plan for this slice at
  `.cache/agent/execplans/wendao-runtime-plugin-arrow-candidate-projection-extraction.md`
- landed the bounded runtime cut so `xiuxian-wendao-runtime` now owns one
  generic projection helper that materializes `doc_ids` plus
  `PluginArrowScoredCandidate` rows from doc-score pairs
- rewired `quantum_fusion/openai_ignition.rs` and
  `quantum_fusion/vector_ignition.rs` to consume that runtime helper instead of
  hand-rolling the same anchor-to-candidate projection
- closed the touched-scope issues surfaced during validation:
  explicit anchor-tied lifetimes were added to the private helper returns, and
  the leftover unused `PluginArrowCandidateProjection` import in
  `openai_ignition.rs` was removed
- confirmed focused validation is green on the default cargo target:
  `direnv exec . cargo fmt -p xiuxian-wendao-runtime -p xiuxian-wendao`
  `direnv exec . cargo test -p xiuxian-wendao-runtime --features transport plugin_arrow_exchange`
  `direnv exec . cargo test -p xiuxian-wendao --lib link_graph::index::search::quantum_fusion`
  `direnv exec . cargo check -p xiuxian-wendao --no-default-features --features julia,zhenfa-router`
  `direnv exec . git diff --check`
- archived the completed execution plan at
  `.cache/agent/execplans/archives/wendao-runtime-plugin-arrow-candidate-projection-extraction.md`

## Wendao Runtime Plugin Arrow Vector-Store Request Row Preparation Extraction

- re-ran the active plugin-core-runtime blueprint check against
  `.cache/agent/blueprints/wendao_arrow_plugin_core_runtime_migration.md`
- synchronized package docs before implementation so the next bounded seam is
  explicit: `quantum_fusion/openai_ignition.rs` and
  `quantum_fusion/vector_ignition.rs` still duplicate the same
  `doc-score pairs -> fetch embeddings from xiuxian-vector -> PluginArrowRequestRow`
  preparation step before batch shaping
- opened the active execution plan for this slice at
  `.cache/agent/execplans/wendao-runtime-plugin-arrow-vector-store-request-row-preparation-extraction.md`
- landed the bounded runtime cut so `xiuxian-wendao-runtime` now owns one
  vector-store-backed helper seam that materializes `PluginArrowRequestRow`
  values and metadata-bearing request batches directly from doc-score pairs
- rewired `quantum_fusion/openai_ignition.rs` and
  `quantum_fusion/vector_ignition.rs` to delegate request-row preparation and
  batch shaping into runtime, leaving only query-vector resolution and
  owner-specific error mapping in the ignition owners
- confirmed the duplicated host-local embedding lookup is gone:
  `fetch_embeddings_by_ids(...)` now only matches in
  `packages/rust/crates/xiuxian-wendao-runtime/src/transport/plugin_arrow_exchange/prepare.rs`
- closed the touched-scope issue surfaced by the new runtime proof: the first
  missing-embedding test incorrectly used an empty table and hit the
  vector-store error path, so the regression was tightened to a seeded table
  missing the requested doc id
- confirmed focused validation is green on the default cargo target:
  `direnv exec . cargo fmt -p xiuxian-wendao-runtime -p xiuxian-wendao`
  `direnv exec . cargo test -p xiuxian-wendao-runtime --features transport plugin_arrow_exchange`
  `direnv exec . cargo test -p xiuxian-wendao --lib link_graph::index::search::quantum_fusion`
  `direnv exec . cargo check -p xiuxian-wendao --no-default-features --features julia,zhenfa-router`
  `direnv exec . git diff --check`
- archived the completed execution plan at
  `.cache/agent/execplans/archives/wendao-runtime-plugin-arrow-vector-store-request-row-preparation-extraction.md`

## Wendao Quantum Plugin Rerank Host Orchestration Deduplication

- re-ran the active plugin-core-runtime blueprint check against
  `.cache/agent/blueprints/wendao_arrow_plugin_core_runtime_migration.md`
- synchronized package docs before implementation so the next bounded seam is
  explicit: `quantum/rerank.rs` still duplicates the same
  `QuantumContext -> QuantumAnchorHit` staging plus request-batch
  error/roundtrip/telemetry orchestration across the vector-store and
  openai-compatible plugin rerank paths
- opened the active execution plan for this slice at
  `.cache/agent/execplans/wendao-quantum-plugin-rerank-host-orchestration-deduplication.md`
- implemented the bounded host-local cut in
  `packages/rust/crates/xiuxian-wendao/src/link_graph/index/search/plan/payload/quantum/rerank.rs`
  by adding one anchor-staging helper and one shared roundtrip-orchestration
  helper, then rewiring both plugin rerank entrypoints to consume them
- added one focused regression in
  `packages/rust/crates/xiuxian-wendao/src/link_graph/index/search/plan/payload/quantum/tests.rs`
  that proves the new anchor-staging helper preserves `anchor_id` and
  `vector_score`
- confirmed the duplicated inline staging/build-error path is now collapsed
  behind the new helpers; the old repeated `contexts -> QuantumAnchorHit`
  mapping and repeated `build_plugin_rerank_telemetry(None, false, 0, ...)`
  no longer appear at the two entrypoints
- kept the baseline recovery inside this same lane: moved the canonical
  semantic-lineage helper back under `link_graph/index/lookup.rs`, removed the
  parser-owned `LinkGraphIndex` impl from
  `src/parsers/markdown/links/normalize.rs`, fixed the `PageIndexNode`
  namespace import in `link_graph/index/page_indices.rs`, and added a focused
  page-index lineage regression in
  `packages/rust/crates/xiuxian-wendao/tests/unit/link_graph/index.rs`
- confirmed focused validation is green again on the default cargo target:
  `direnv exec . cargo fmt -p xiuxian-wendao`
  `direnv exec . cargo test -p xiuxian-wendao parsers::markdown:: --lib`
  `direnv exec . cargo test -p xiuxian-wendao --lib link_graph::index::tests::test_page_index_lineage_helpers_follow_canonical_owner_surface`
  `direnv exec . cargo test -p xiuxian-wendao --lib link_graph::index::search::plan::payload::quantum`
  `direnv exec . cargo test -p xiuxian-wendao --lib link_graph::index::search::quantum_fusion`
  `direnv exec . cargo check -p xiuxian-wendao --no-default-features --features julia,zhenfa-router`
  `direnv exec . git diff --check`
- archived the completed execution plan at
  `.cache/agent/execplans/archives/wendao-quantum-plugin-rerank-host-orchestration-deduplication.md`

## Wendao Host Agentic Collection Test Support

- re-checked the active mixed-graph retrieval blueprint at
  `.cache/agent/blueprints/wendao_search_mixed_graph_retrieval.md`
  with the Julia plugin-first rollout blueprint as a bounded secondary
  reference:
  `.cache/agent/blueprints/wendao_julia_plugin_first_rollout.md`
- opened the active execution plan for the next bounded slice at
  `.cache/agent/execplans/wendao-host-agentic-collection-test-support.md`
- scoped the slice to one host-test modularization step only: keep the
  current `WendaoSearch.jl` live contract unchanged, but move
  `LinkGraphAgenticExpansionPlan -> connected pair collections` extraction out
  of `packages/rust/crates/xiuxian-wendao/tests/unit/link_graph_agentic/expansion.rs`
  and into dedicated host test-support
- landed the helper extraction under
  `packages/rust/crates/xiuxian-wendao/tests/unit/link_graph_agentic/expansion_support.rs`
  and rewired `expansion.rs` to consume that shared host test-support instead
  of carrying the collection-selection logic inline
- re-verified the bounded host and live slices:
  `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate generic_topology_fetch_helper`
  `direnv exec . cargo check -p xiuxian-wendao --features julia --test xiuxian-testing-gate`
  `direnv exec . cargo test -p xiuxian-wendao-julia graph_structural_exchange --lib`
  `direnv exec . julia --project=.data/WendaoSearch.jl -e 'using Pkg; Pkg.test()'`
  `direnv exec . git diff --check -- ...`
- archived the completed execution plan at
  `.cache/agent/execplans/archives/wendao-host-agentic-collection-test-support.md`

## Wendao Host Generic Topology Live Harness Thinning

- re-checked the active mixed-graph retrieval blueprint at
  `.cache/agent/blueprints/wendao_search_mixed_graph_retrieval.md`
  with the Julia plugin-first rollout blueprint as a bounded secondary
  reference:
  `.cache/agent/blueprints/wendao_julia_plugin_first_rollout.md`
- opened the active execution plan for the next bounded slice at
  `.cache/agent/execplans/wendao-host-generic-topology-live-harness-thinning.md`
- scoped the slice to one host-test live-harness thinning step only: keep the
  current `WendaoSearch.jl` solver-demo live contract unchanged, but move the
  repeated generic-topology manifest repository setup, shared query-context
  setup, and baseline solver-demo row assertions out of
  `packages/rust/crates/xiuxian-wendao/tests/unit/link_graph_agentic/expansion.rs`
  and into dedicated host test-support
- landed the next host test-support layer in
  `packages/rust/crates/xiuxian-wendao/tests/unit/link_graph_agentic/expansion_support.rs`
  so `expansion.rs` now consumes a shared generic-topology manifest fetch
  helper and shared baseline solver-demo row assertions, leaving only test
  intent plus pin-specific assertions inline
- propagated `GraphStructuralRerankScoreRow` through the linked builtin test
  support re-export seam so the new host support helper can stay on the
  builtin-owned Julia surface instead of reaching around it
- re-verified the bounded host and live slices:
  `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate generic_topology_fetch_helper`
  `direnv exec . cargo test -p xiuxian-wendao-julia graph_structural_exchange --lib`
  `direnv exec . cargo check -p xiuxian-wendao --features julia --test xiuxian-testing-gate`
  `direnv exec . julia --project=.data/WendaoSearch.jl -e 'using Pkg; Pkg.test()'`
  `direnv exec . git diff --check -- ...`
- archived the completed execution plan at
  `.cache/agent/execplans/archives/wendao-host-generic-topology-live-harness-thinning.md`

## Wendao Host Seed-Centered Generic Topology Batch

- re-checked the active mixed-graph retrieval blueprint at
  `.cache/agent/blueprints/wendao_search_mixed_graph_retrieval.md`
  with the Julia plugin-first rollout blueprint as a bounded secondary
  reference:
  `.cache/agent/blueprints/wendao_julia_plugin_first_rollout.md`
- opened the active execution plan for the next bounded slice at
  `.cache/agent/execplans/wendao-host-seed-centered-generic-topology-batch.md`
- scoped the slice to one host-test mixed-graph promotion step only: derive
  seed-centered generic-topology candidates from a real
  `LinkGraphAgenticExpansionPlan` and send them through the existing
  manifest-discovered `WendaoSearch.jl --mode solver_demo` live route without
  changing the Julia contract
- landed the next host test-support layer in
  `packages/rust/crates/xiuxian-wendao/tests/unit/link_graph_agentic/expansion_support.rs`
  so the host proof can now derive seed-centered candidate batches from a real
  expansion plan instead of stopping at connected-pair collections
- added one bounded host live proof in
  `packages/rust/crates/xiuxian-wendao/tests/unit/link_graph_agentic/expansion.rs`
  that sends those seed-centered plan-derived candidates through the same
  manifest-discovered generic-topology `solver_demo` route and verifies the
  returned `5 nodes / 4 explicit edges` explanation shape
- re-verified the bounded host and live slices:
  `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate generic_topology_fetch_helper`
  `direnv exec . cargo test -p xiuxian-wendao-julia graph_structural_exchange --lib`
  `direnv exec . cargo check -p xiuxian-wendao --features julia --test xiuxian-testing-gate`
  `direnv exec . julia --project=.data/WendaoSearch.jl -e 'using Pkg; Pkg.test()'`
  `direnv exec . git diff --check -- ...`
- archived the completed execution plan at
  `.cache/agent/execplans/archives/wendao-host-seed-centered-generic-topology-batch.md`

## Wendao Host Worker Partition Generic Topology Batch

- re-checked the active mixed-graph retrieval blueprint at
  `.cache/agent/blueprints/wendao_search_mixed_graph_retrieval.md`
  with the Julia plugin-first rollout blueprint as a bounded secondary
  reference:
  `.cache/agent/blueprints/wendao_julia_plugin_first_rollout.md`
- opened the active execution plan for the next bounded slice at
  `.cache/agent/execplans/wendao-host-worker-partition-generic-topology-batch.md`
- scoped the slice to one host-test mixed-graph promotion step only: derive
  worker-partition generic-topology candidates from a real
  `LinkGraphAgenticExpansionPlan` and send them through the existing
  manifest-discovered `WendaoSearch.jl --mode solver_demo` live route without
  changing the Julia contract
- landed worker-partition generic-topology host test-support in
  `packages/rust/crates/xiuxian-wendao/tests/unit/link_graph_agentic/expansion_support.rs`,
  so the host live proof can now derive plan-shaped candidates directly from
  real `LinkGraphAgenticWorkerPlan` partitions instead of only from
  seed-centered groups
- landed one bounded host live proof in
  `packages/rust/crates/xiuxian-wendao/tests/unit/link_graph_agentic/expansion.rs`
  that sends those worker-partition plan-derived candidates through the same
  manifest-discovered `WendaoSearch.jl --mode solver_demo` generic-topology
  route and accepts mixed feasible and infeasible solver rows within the same
  batch while still requiring at least one feasible result
- verification:
  `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate generic_topology_fetch_helper`
- verification:
  `direnv exec . cargo test -p xiuxian-wendao-julia graph_structural_exchange --lib`
- verification:
  `direnv exec . cargo check -p xiuxian-wendao --features julia --test xiuxian-testing-gate`
- verification:
  `direnv exec . julia --project=.data/WendaoSearch.jl -e 'using Pkg; Pkg.test()'`
- archived ExecPlan:
  `.cache/agent/execplans/archives/wendao-host-worker-partition-generic-topology-batch.md`

## Wendao Host Plan-Aware Generic Topology Batch

- re-checked the active mixed-graph retrieval blueprint at
  `.cache/agent/blueprints/wendao_search_mixed_graph_retrieval.md`
  with the Julia plugin-first rollout blueprint as a bounded secondary
  reference:
  `.cache/agent/blueprints/wendao_julia_plugin_first_rollout.md`
- opened the active execution plan for the next bounded slice at
  `.cache/agent/execplans/wendao-host-plan-aware-generic-topology-batch.md`
- scoped the slice to one host-test mixed-graph promotion step only: derive
  one generic-topology batch query context from the real
  `LinkGraphAgenticExpansionPlan` and selected worker seed metadata, then send
  the resulting worker-partition batch through the existing manifest-discovered
  `WendaoSearch.jl --mode solver_demo` live route without changing the Julia
  contract
- landed one dedicated host support module in
  `packages/rust/crates/xiuxian-wendao/tests/unit/link_graph_agentic/expansion_plan_batch_support.rs`
  that derives batch-level generic-topology query anchors from the real plan
  query and selected worker seed metadata instead of hard-coding host query
  context inside the fetch helper
- landed one external host test module in
  `packages/rust/crates/xiuxian-wendao/tests/unit/link_graph_agentic/expansion_plan_batch_tests.rs`
  through `#[cfg(feature = "julia")] #[path = "..."]`, so the new bounded
  plan-aware live proof does not further inflate `expansion.rs`
- landed one bounded host live proof that sends that plan-aware worker-partition
  batch through the same manifest-discovered `WendaoSearch.jl --mode solver_demo`
  generic-topology route and verifies the returned rows still preserve the same
  mixed feasible/infeasible batch semantics as the lower-level worker proof
- verification:
  `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate generic_topology_fetch_helper`
- verification:
  `direnv exec . cargo test -p xiuxian-wendao-julia graph_structural_exchange --lib`
- verification:
  `direnv exec . cargo check -p xiuxian-wendao --features julia --test xiuxian-testing-gate`
- verification:
  `direnv exec . julia --project=.data/WendaoSearch.jl -e 'using Pkg; Pkg.test()'`
- archived ExecPlan:
  `.cache/agent/execplans/archives/wendao-host-plan-aware-generic-topology-batch.md`

## Wendao Host Plan-Aware Worker Score Batch

- re-checked the active mixed-graph retrieval blueprint at
  `.cache/agent/blueprints/wendao_search_mixed_graph_retrieval.md`
  with the Julia plugin-first rollout blueprint as a bounded secondary
  reference:
  `.cache/agent/blueprints/wendao_julia_plugin_first_rollout.md`
- opened the active execution plan for the next bounded slice at
  `.cache/agent/execplans/wendao-host-plan-aware-worker-score-batch.md`
- scoped the slice to one host-test staging step only: derive worker-batch
  dependency, keyword, and tag scores from the real plan-aware batch semantics,
  validate those staged request columns, and then reuse the same batch against
  the existing manifest-discovered `WendaoSearch.jl --mode solver_demo` live
  route without changing the Julia contract
- landed the host-side plan-aware worker score staging in
  `packages/rust/crates/xiuxian-wendao/tests/unit/link_graph_agentic/expansion_plan_batch_support.rs`,
  so the bounded host fixture now derives `dependency_score`,
  `keyword_score`, and `tag_score` from real worker partitions plus batch
  anchors instead of fixed constants
- landed the bounded external host proof in
  `packages/rust/crates/xiuxian-wendao/tests/unit/link_graph_agentic/expansion_plan_batch_tests.rs`,
  and it now validates the staged Arrow request-batch score columns before
  reusing the same batch against the manifest-discovered
  `WendaoSearch.jl --mode solver_demo` live route
- re-verified the bounded live baselines:
  `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate generic_topology_fetch_helper`
  `direnv exec . cargo test -p xiuxian-wendao-julia graph_structural_exchange --lib`
  `direnv exec . cargo check -p xiuxian-wendao --features julia --test xiuxian-testing-gate`
  `direnv exec . julia --project=.data/WendaoSearch.jl -e 'using Pkg; Pkg.test()'`
- archived the completed slice plan at
  `.cache/agent/execplans/archives/wendao-host-plan-aware-worker-score-batch.md`

## Wendao Host Plan-Aware Semantic Score Batch

- re-checked the active mixed-graph retrieval blueprint at
  `.cache/agent/blueprints/wendao_search_mixed_graph_retrieval.md`
  with the Julia plugin-first rollout blueprint as a bounded secondary
  reference:
  `.cache/agent/blueprints/wendao_julia_plugin_first_rollout.md`
- opened the active execution plan for the next bounded slice at
  `.cache/agent/execplans/wendao-host-plan-aware-semantic-score-batch.md`
- scoped the slice to one host-test staging step only: derive and validate the
  staged `semantic_score` request column from real plan-aware worker-partition
  pair semantics before reusing the same batch against the existing
  manifest-discovered `WendaoSearch.jl --mode solver_demo` live route without
  changing the Julia contract
- landed the host-side semantic-score staging update in
  `packages/rust/crates/xiuxian-wendao/tests/unit/link_graph_agentic/expansion_plan_batch_support.rs`,
  so the bounded host fixture now records the expected staged
  `semantic_score` for each plan-aware worker-partition candidate directly
  from real worker pair priorities
- landed the bounded external host proof update in
  `packages/rust/crates/xiuxian-wendao/tests/unit/link_graph_agentic/expansion_plan_batch_tests.rs`,
  and it now validates the outgoing Arrow `semantic_score` request column
  alongside the already-realized dependency, keyword, and tag score columns
  before reusing the same batch against the manifest-discovered
  `WendaoSearch.jl --mode solver_demo` live route
- re-verified the bounded live baselines:
  `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate generic_topology_fetch_helper`
  `direnv exec . cargo test -p xiuxian-wendao-julia graph_structural_exchange --lib`
  `direnv exec . cargo check -p xiuxian-wendao --features julia --test xiuxian-testing-gate`
  `direnv exec . julia --project=.data/WendaoSearch.jl -e 'using Pkg; Pkg.test()'`
- archived the completed slice plan at
  `.cache/agent/execplans/archives/wendao-host-plan-aware-semantic-score-batch.md`

## Wendao Host Plan-Aware Query Column Batch

- re-checked the active mixed-graph retrieval blueprint at
  `.cache/agent/blueprints/wendao_search_mixed_graph_retrieval.md`
  with the Julia plugin-first rollout blueprint as a bounded secondary
  reference:
  `.cache/agent/blueprints/wendao_julia_plugin_first_rollout.md`
- opened the active execution plan for the next bounded slice at
  `.cache/agent/execplans/wendao-host-plan-aware-query-column-batch.md`
- scoped the slice to one host-test staging step only: validate the staged
  `query_id`, `retrieval_layer`, `query_max_layers`, `anchor_planes`,
  `anchor_values`, and `edge_constraint_kinds` request columns from the
  existing plan-aware batch fixture before reusing the same batch against the
  manifest-discovered `WendaoSearch.jl --mode solver_demo` live route without
  changing the Julia contract
- landed the host-side query-column staging update in
  `packages/rust/crates/xiuxian-wendao/tests/unit/link_graph_agentic/expansion_plan_batch_support.rs`,
  so the bounded plan-aware batch fixture now records the expected retrieval
  layer, query max layers, anchor plane ordering, anchor values, and edge
  constraint kinds alongside the existing score expectations
- landed the bounded external host proof update in
  `packages/rust/crates/xiuxian-wendao/tests/unit/link_graph_agentic/expansion_plan_batch_tests.rs`,
  and it now validates the outgoing Arrow `query_id`,
  `retrieval_layer`, `query_max_layers`, `anchor_planes`,
  `anchor_values`, and `edge_constraint_kinds` request columns before
  reusing the same batch against the manifest-discovered
  `WendaoSearch.jl --mode solver_demo` live route
- re-exported `GRAPH_STRUCTURAL_EDGE_CONSTRAINT_KINDS_COLUMN` through the
  linked builtin test-support seam so the host proof continues to consume the
  Julia-owned contract through `xiuxian-wendao-builtin` instead of reaching
  into `xiuxian-wendao-julia` directly
- re-verified the bounded live baselines:
  `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate generic_topology_fetch_helper`
  `direnv exec . cargo test -p xiuxian-wendao-julia graph_structural_exchange --lib`
  `direnv exec . cargo check -p xiuxian-wendao --features julia --test xiuxian-testing-gate`
  `direnv exec . julia --project=.data/WendaoSearch.jl -e 'using Pkg; Pkg.test()'`
- archived the completed slice plan at
  `.cache/agent/execplans/archives/wendao-host-plan-aware-query-column-batch.md`

## Wendao Host Plan-Aware Topology Column Batch

- re-checked the active mixed-graph retrieval blueprint at
  `.cache/agent/blueprints/wendao_search_mixed_graph_retrieval.md`
  with the Julia plugin-first rollout blueprint as a bounded secondary
  reference:
  `.cache/agent/blueprints/wendao_julia_plugin_first_rollout.md`
- opened the active execution plan for the next bounded slice at
  `.cache/agent/execplans/wendao-host-plan-aware-topology-column-batch.md`
- scoped the slice to one host-test staging step only: validate the staged
  `candidate_node_ids`, `candidate_edge_sources`,
  `candidate_edge_destinations`, and `candidate_edge_kinds` request columns
  from the existing plan-aware batch fixture before reusing the same batch
  against the manifest-discovered `WendaoSearch.jl --mode solver_demo` live
  route without changing the Julia contract
- landed the host-side topology-column staging update in
  `packages/rust/crates/xiuxian-wendao/tests/unit/link_graph_agentic/expansion_plan_batch_support.rs`,
  so the bounded plan-aware batch fixture now records the expected normalized
  candidate node ids, edge sources, edge destinations, and edge kinds for each
  worker-partition generic-topology candidate
- landed the bounded external host proof update in
  `packages/rust/crates/xiuxian-wendao/tests/unit/link_graph_agentic/expansion_plan_batch_tests.rs`,
  and it now validates the outgoing Arrow `candidate_node_ids`,
  `candidate_edge_sources`, `candidate_edge_destinations`, and
  `candidate_edge_kinds` request columns before reusing the same batch against
  the manifest-discovered `WendaoSearch.jl --mode solver_demo` live route
- re-verified the bounded live baselines:
  `direnv exec . cargo test -p xiuxian-wendao --test xiuxian-testing-gate generic_topology_fetch_helper`
  `direnv exec . cargo test -p xiuxian-wendao-julia graph_structural_exchange --lib`
  `direnv exec . cargo check -p xiuxian-wendao --features julia --test xiuxian-testing-gate`
  `direnv exec . julia --project=.data/WendaoSearch.jl -e 'using Pkg; Pkg.test()'`
- archived the completed slice plan at
  `.cache/agent/execplans/archives/wendao-host-plan-aware-topology-column-batch.md`

## Wendao Host Plan-Aware Generic-Topology Filter Batch

- re-checked the active mixed-graph retrieval blueprint at
  `.cache/agent/blueprints/wendao_search_mixed_graph_retrieval.md`
  with the Julia plugin-first rollout blueprint as a bounded secondary
  reference:
  `.cache/agent/blueprints/wendao_julia_plugin_first_rollout.md`
- opened the active execution plan for the next bounded slice at
  `.cache/agent/execplans/wendao-host-plan-aware-generic-topology-filter-batch.md`
- scoped the slice to one live promotion step only: add a Julia-owned
  generic-topology `constraint_filter` helper above raw connected-pair
  collections, then prove the same plan-aware worker-partition batch against
  manifest-discovered `WendaoSearch.jl --mode solver_demo` while explicitly
  validating staged `constraint_kind` and `required_boundary_size`
