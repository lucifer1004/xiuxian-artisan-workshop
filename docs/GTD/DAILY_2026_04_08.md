# Daily Log - 2026-04-08

## WendaoCodeParser Julia SyntaxNode Alignment and Modularization

- continued the active native-parser blueprint at
  `.cache/agent/blueprints/wendao_code_parser_flight_native_language_parsers.md`
  and advanced the bounded ExecPlan at
  `.cache/agent/execplans/wendao-code-parser-julia-syntaxnode-alignment.md`
- replaced the old Julia `Expr`-style collector inside
  `.data/WendaoCodeParser.jl` with a `JuliaSyntax.SyntaxNode` collector so the
  parser now derives module, import, symbol, docstring, and include data from
  the native syntax tree instead of from lowered expression heuristics
- split the Julia parser owner into focused files under
  `.data/WendaoCodeParser.jl/src/parsers/julia/`:
  `state.jl`, `summary.jl`, `emit.jl`, `syntax.jl`, and `collect.jl`, so the
  parser lane no longer grows back into a single large source file
- kept the parser-only contract explicit while strengthening Julia semantics:
  native rows now preserve `@reexport using`, module docstrings, symbol
  docstrings, first-line function signatures, and parser-owned line spans
- removed the weak mixed-semantics Julia docstring `line_end` heuristic:
  docstring rows now carry doc-literal spans and target-declaration spans as
  separate native Arrow fields, so the search side does not need to infer one
  from the other
- kept parser-package tests modular by introducing
  `.data/WendaoCodeParser.jl/test/support/mod.jl` and
  `.data/WendaoCodeParser.jl/test/cases/mod.jl`, so
  `.data/WendaoCodeParser.jl/test/runtests.jl` stays as a thin runner
- tightened the shared-service regression in `.data/WendaoSearch.jl` so the
  mounted `julia_ast_query` route now asserts native line-span propagation over
  the Arrow Flight boundary instead of only matching `name` and `node_kind`
- focused validation for this slice is green:
  `direnv exec . julia --project=.data/WendaoCodeParser.jl --color=yes .data/WendaoCodeParser.jl/test/runtests.jl`
  `direnv exec . julia --project=.data/WendaoSearch.jl --color=yes -e 'using Pkg; Pkg.test()'`

## WendaoCodeParser Parser Module Folderization and AST Alignment

- continued the active native-parser blueprint at
  `.cache/agent/blueprints/wendao_code_parser_flight_native_language_parsers.md`
  and advanced the bounded ExecPlan at
  `.cache/agent/execplans/wendao-code-parser-parser-module-folderization.md`
- established a package-local `docs/` tree under `.data/WendaoCodeParser.jl`
  so parser migration notes are no longer trapped in the package README alone;
  the initial canonical docs surface now includes `docs/index.md`,
  `docs/contracts/README.md`, and
  `docs/contracts/parser_ast_alignment.md`
- clarified in the package README and parser contract docs that
  `WendaoCodeParser.jl` is parser-side only; Rust or host-side Flight client
  linkage stays in the consuming search, graph, and runtime layers during the
  `xiuxian-ast` replacement
- split `.data/WendaoCodeParser.jl/src/parsers/` into language-owned module
  folders so Julia parsing now lives under `src/parsers/julia/` and Modelica
  parsing now lives under `src/parsers/modelica/`
- tightened the parser/search seam by promoting shared state collectors into
  the language parser owners, so `src/search/julia.jl` and
  `src/search/modelica.jl` consume parser-owned collection surfaces instead of
  reaching into monolithic files
- aligned documentation-node semantics across languages:
  Julia docstrings stay queryable as `docstring` AST nodes, and Modelica
  lexer/comment documentation is now normalized before it becomes a
  `documentation` summary item or AST node
- kept the external Arrow Flight surface stable in this slice: route names are
  unchanged, mounted `WendaoSearch.jl` live regressions still pass, and the
  parser package docs now record the current `v3` native Arrow row contract
- advanced the next parser-only milestone inside `.data/WendaoCodeParser.jl`:
  Modelica native summary rows now carry visibility, owner, line-span,
  qualifier, and equation detail directly in native Arrow columns, and the
  parser package test runner has been split into `test/support/` plus
  bounded `test/cases/` files so `test/runtests.jl` does not keep growing
- kept the parser layout from re-bloating during the same lane:
  `src/parsers/modelica/backend.jl` is now narrowed to the `OMParser.jl`
  native bridge, while `src/parsers/modelica/collect.jl` owns Modelica state
  collection, cache lifecycle, expression normalization, and AST materialized
  rows
- tightened Modelica component-detail parity in the native Arrow summary
  contract: symbol rows now carry `component_kind`, `default_value`, and
  `unit` detail derived directly from `OMParser.jl` modification state
- focused validation for this slice is green:
  `direnv exec . julia --project=.data/WendaoCodeParser.jl --color=yes .data/WendaoCodeParser.jl/test/runtests.jl`
  `direnv exec . julia --project=.data/WendaoSearch.jl --color=yes -e 'using Pkg; Pkg.test()'`

## WendaoSearch Native Arrow Flight AST Code Search Report

- tightened the active AST code-search lane so the mounted
  `julia_ast_query` and `modelica_ast_query` routes now use native Arrow
  request and response columns instead of `query_json` / `payload_json`
  transport blobs
- kept the slice bounded to AST routes: summary routes still keep their
  previous JSON payload shape, while the AST benchmark and mounted live
  regressions now audit typed Arrow columns directly
- added the package-owned benchmark entrypoint
  `scripts/benchmark_ast_code_search.jl` and recorded the current native Arrow
  Flight measurements in
  `.data/WendaoSearch.jl/docs/04_chronicles/research/2026-04-08-native-arrow-flight-ast-code-search-performance-report.md`
- synchronized `.data/WendaoSearch.jl/README.md` and
  `.data/WendaoSearch.jl/docs/testing/README.md` so the package docs now point
  to the native AST benchmark lane explicitly
- removed the root Modelica tail-latency cause in `.data/WendaoCodeParser.jl`:
  repeated same-source AST queries now reuse a bounded in-process
  `ModelicaCollectionState` cache instead of reparsing through `OMParser` on
  every request, and the backend now also caches the `parseString` symbol
- added direct parser-package regression coverage for Modelica cache hits and
  invalidation so the hot-path optimization stays under test outside the live
  child harness
- refreshed the mounted native Arrow benchmark evidence after the fix:
  the previous `modelica_ast_query` `20`-sample run measured
  `median_ms=5.887` / `max_ms=38.498`, while the post-fix runs measured
  `median_ms=3.439` / `max_ms=19.933` over `20` samples and
  `median_ms=3.261` / `max_ms=4.515` over `50` samples
- focused validation for this slice is green:
  `direnv exec . julia --project=.data/WendaoCodeParser.jl --color=yes .data/WendaoCodeParser.jl/test/runtests.jl`
  `direnv exec . julia --project=.data/WendaoSearch.jl --color=yes -e 'using Pkg; Pkg.resolve(); Pkg.instantiate()'`
  `direnv exec . julia --project=.data/WendaoSearch.jl --color=yes -e 'using Pkg; Pkg.test()'`
  `direnv exec . julia --project=.data/WendaoSearch.jl --color=yes .data/WendaoSearch.jl/scripts/benchmark_ast_code_search.jl --host 127.0.0.1 --port 49872 --route-names julia_ast_query,modelica_ast_query --fixture-scale 32 --samples 10 --node-kind function --name-contains foo --limit 5`
  `direnv exec . julia --project=.data/WendaoSearch.jl --color=yes .data/WendaoSearch.jl/scripts/benchmark_ast_code_search.jl --host 127.0.0.1 --port 49872 --route-names julia_ast_query,modelica_ast_query --fixture-scale 32 --samples 20 --node-kind function --name-contains foo --limit 5`
  `direnv exec . julia --project=.data/WendaoSearch.jl --color=yes .data/WendaoSearch.jl/scripts/benchmark_ast_code_search.jl --host 127.0.0.1 --port 49873 --route-names julia_ast_query,modelica_ast_query --fixture-scale 32 --samples 20 --node-kind function --name-contains foo --limit 5`
  `direnv exec . julia --project=.data/WendaoSearch.jl --color=yes .data/WendaoSearch.jl/scripts/benchmark_ast_code_search.jl --host 127.0.0.1 --port 49873 --route-names modelica_ast_query --fixture-scale 32 --samples 50 --node-kind function --name-contains foo --limit 5`

## Qianji Workdir Check Follow-up Output Surface

- continued the active Flowhub graph-contract blueprint at
  `.cache/agent/blueprints/qianji_flowhub_planning_source_model.md` and
  advanced the bounded ExecPlan at
  `.cache/agent/execplans/qianji-workdir-check-followup-output-surface.md`
- promoted the existing workdir follow-up retrieval guidance into the
  user-facing bounded workdir check surface:
  `render_workdir_check_markdown(...)` now appends `## Follow-up Query` on
  failure, so `qianji check --dir <workdir>` exposes a bounded skeleton SQL
  repair query directly
- kept the slice bounded: successful workdir check output is unchanged, and
  Flowhub root/module plus scenario check surfaces still keep their previous
  markdown contract
- synchronized
  `packages/rust/crates/xiuxian-qianji/README.md`,
  `packages/rust/crates/xiuxian-qianji/docs/00_mocs/20260315141000-qianji-orchestrator-moc.md`,
  and the active blueprint so the user-facing follow-up output is recorded in
  the same Flowhub graph-contract lane
- focused validation for this slice is green:
  `direnv exec . cargo fmt --all`
  `direnv exec . cargo test -p xiuxian-qianji check_workdir_render_includes_follow_up_query_on_failure --test workdir_contracts -- --exact`
  `direnv exec . cargo test -p xiuxian-qianji check_workdir_accepts_valid_surface --test workdir_contracts -- --exact`
  `direnv exec . cargo test -p xiuxian-qianji --features llm --bin qianji`
  `direnv exec . cargo check -p xiuxian-qianji --tests`
  `direnv exec . git diff --check -- packages/rust/crates/xiuxian-qianji/src/workdir packages/rust/crates/xiuxian-qianji/tests/integration/workdir_contracts.rs packages/rust/crates/xiuxian-qianji/tests/unit/bin/qianji.rs packages/rust/crates/xiuxian-qianji/README.md packages/rust/crates/xiuxian-qianji/docs/00_mocs/20260315141000-qianji-orchestrator-moc.md .cache/agent/blueprints/qianji_flowhub_planning_source_model.md .cache/agent/execplans/qianji-workdir-check-followup-output-surface.md docs/GTD/DAILY_2026_04_08.md`

## Qianji Flowhub Materialize Follow-up Query Diagnostics

- continued the active Flowhub graph-contract blueprint at
  `.cache/agent/blueprints/qianji_flowhub_planning_source_model.md` and
  advanced the bounded ExecPlan at
  `.cache/agent/execplans/qianji-flowhub-materialize-followup-query-diagnostics.md`
- connected the existing workdir follow-up query seam to one internal caller:
  `materialize_flowhub_scenario_workdir(...)` now appends bounded SQL repair
  guidance when the generated work surface fails compact workdir validation
- kept the slice bounded: no `qianji show` / `qianji check` output change, no
  new CLI verb, and no widening beyond materialize-time workdir validation
  failure
- synchronized
  `packages/rust/crates/xiuxian-qianji/README.md`,
  `packages/rust/crates/xiuxian-qianji/docs/00_mocs/20260315141000-qianji-orchestrator-moc.md`,
  and the active blueprint so the materialize lane now records the
  repair-oriented follow-up query guidance explicitly
- focused validation for this slice is green:
  `direnv exec . cargo fmt --all`
  `direnv exec . cargo test -p xiuxian-qianji materialize_flowhub_scenario_reports_follow_up_query_for_invalid_generated_surface --test flowhub_materialize -- --exact`
  `direnv exec . cargo test -p xiuxian-qianji materialize_flowhub_scenario_generates_compact_work_surface --test flowhub_materialize -- --exact`
  `direnv exec . cargo check -p xiuxian-qianji --tests`
  `direnv exec . git diff --check -- packages/rust/crates/xiuxian-qianji/src/flowhub/materialize packages/rust/crates/xiuxian-qianji/src/workdir packages/rust/crates/xiuxian-qianji/tests/integration/flowhub_materialize.rs packages/rust/crates/xiuxian-qianji/README.md packages/rust/crates/xiuxian-qianji/docs/00_mocs/20260315141000-qianji-orchestrator-moc.md .cache/agent/blueprints/qianji_flowhub_planning_source_model.md .cache/agent/execplans/qianji-flowhub-materialize-followup-query-diagnostics.md docs/GTD/DAILY_2026_04_08.md`

## Qianji Workdir Check Follow-up Query Surface

- continued the active Flowhub graph-contract blueprint at
  `.cache/agent/blueprints/qianji_flowhub_planning_source_model.md` and
  advanced the bounded ExecPlan at
  `.cache/agent/execplans/qianji-workdir-check-followup-query-surface.md`
- extended the existing workdir retrieval lane with a repair-oriented follow-up
  surface: failing `check_workdir(...)` reports now carry explicit bounded
  `blueprint/` / `plan/` follow-up hints, and `xiuxian-qianji` now exposes
  `build_workdir_check_follow_up_query(...)` plus
  `query_workdir_check_follow_up_payload(...)` over the existing Wendao SQL
  retrieval seam
- kept the slice bounded: no CLI changes, no new global query registration,
  and the default follow-up query stays on `skeleton`-oriented markdown
  retrieval only
- synchronized
  `packages/rust/crates/xiuxian-qianji/README.md`,
  `packages/rust/crates/xiuxian-qianji/docs/00_mocs/20260315141000-qianji-orchestrator-moc.md`,
  and the active blueprint so the workdir retrieval lane now records the
  diagnostic-follow-up seam explicitly
- focused validation for this slice is green:
  `direnv exec . cargo fmt --all`
  `direnv exec . cargo test -p xiuxian-qianji workdir_check_follow_up_query_stays_surface_bounded --test workdir_contracts -- --exact`
  `direnv exec . cargo test -p xiuxian-qianji workdir_check_follow_up_query_returns_surface_bounded_payload --test workdir_contracts -- --exact`
  `direnv exec . cargo test -p xiuxian-qianji valid_workdir_has_no_follow_up_payload --test workdir_contracts -- --exact`
  `direnv exec . cargo test -p xiuxian-qianji workdir_query_surface_returns_sql_payload --test workdir_contracts -- --exact`
  `direnv exec . cargo check -p xiuxian-qianji --tests`
  `direnv exec . git diff --check -- packages/rust/crates/xiuxian-qianji/src/workdir packages/rust/crates/xiuxian-qianji/src/lib.rs packages/rust/crates/xiuxian-qianji/tests/integration/workdir_contracts.rs packages/rust/crates/xiuxian-qianji/README.md packages/rust/crates/xiuxian-qianji/docs/00_mocs/20260315141000-qianji-orchestrator-moc.md .cache/agent/blueprints/qianji_flowhub_planning_source_model.md .cache/agent/execplans/qianji-workdir-check-followup-query-surface.md docs/GTD/DAILY_2026_04_08.md`

## Wendao Bounded Work Markdown SQL Surface

- continued the active Flowhub graph-contract blueprint at
  `.cache/agent/blueprints/qianji_flowhub_planning_source_model.md` and
  advanced the bounded ExecPlan at
  `.cache/agent/execplans/wendao-bounded-work-markdown-sql-surface.md`
- added a dedicated feature-folder surface under
  `packages/rust/crates/xiuxian-wendao/src/search/queries/sql/bounded_work_markdown/`
  so Wendao now has a first code-backed helper that scans only `blueprint/`
  and `plan/`, builds the RFC-minimum `markdown` rows (`path`, `surface`,
  `heading_path`, `title`, `level`, `skeleton`, `body`), and registers them
  into a local DataFusion `markdown` table
- kept this slice intentionally bounded: the new helper is now a local SQL
  registration surface in `xiuxian-wendao`, but it is not yet wired into the
  global SQL surface collector and does not introduce any new CLI
- synchronized `packages/rust/crates/xiuxian-wendao/README.md` and
  `packages/rust/crates/xiuxian-qianji/docs/00_mocs/20260315141000-qianji-orchestrator-moc.md`
  so the package docs now record that exact bounded-work retrieval is no
  longer RFC-only
- focused validation for this slice is green:
  `direnv exec . cargo fmt --all`
  `direnv exec . cargo test -p xiuxian-wendao search::queries::sql::bounded_work_markdown::tests::registers_bounded_work_markdown_rows_into_sql_surface --lib -- --exact`
  `direnv exec . cargo check -p xiuxian-wendao --tests`
  `direnv exec . git diff --check -- packages/rust/crates/xiuxian-wendao/src/search/queries/sql/bounded_work_markdown packages/rust/crates/xiuxian-wendao/src/search/queries/sql/mod.rs packages/rust/crates/xiuxian-wendao/tests/unit/search/queries/sql/bounded_work_markdown.rs packages/rust/crates/xiuxian-wendao/README.md packages/rust/crates/xiuxian-qianji/docs/00_mocs/20260315141000-qianji-orchestrator-moc.md .cache/agent/blueprints/qianji_flowhub_planning_source_model.md .cache/agent/execplans/wendao-bounded-work-markdown-sql-surface.md docs/GTD/DAILY_2026_04_08.md`

## Wendao Bounded Work Markdown Bootstrap Surface

- continued the active Flowhub graph-contract blueprint at
  `.cache/agent/blueprints/qianji_flowhub_planning_source_model.md` and
  advanced the bounded ExecPlan at
  `.cache/agent/execplans/wendao-bounded-work-markdown-bootstrap-surface.md`
- extended the new bounded-work markdown SQL surface with one opt-in bootstrap
  helper, so `xiuxian-wendao` can now create a fresh query engine and
  register the local `markdown` table for `blueprint/` and `plan/` in one step
  without widening into the global SQL collector
- kept the slice narrow: the existing local registration helper remains, the
  global collector is unchanged, and the new proof only adds one focused
  mounted unit test for the bootstrap path
- synchronized
  `packages/rust/crates/xiuxian-wendao/README.md`,
  `packages/rust/crates/xiuxian-qianji/docs/00_mocs/20260315141000-qianji-orchestrator-moc.md`,
  and the active blueprint so the retrieval lane now records both the local
  helper and the opt-in bootstrap seam
- focused validation for this slice is green:
  `direnv exec . cargo fmt --all`
  `direnv exec . cargo test -p xiuxian-wendao search::queries::sql::bounded_work_markdown::tests::registers_bounded_work_markdown_rows_into_sql_surface --lib -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao search::queries::sql::bounded_work_markdown::tests::bootstraps_bounded_work_markdown_query_engine --lib -- --exact`
  `direnv exec . cargo check -p xiuxian-wendao --tests`
  `direnv exec . git diff --check -- packages/rust/crates/xiuxian-wendao/src/search/queries/sql/bounded_work_markdown packages/rust/crates/xiuxian-wendao/src/search/queries/sql/mod.rs packages/rust/crates/xiuxian-wendao/src/search/queries/sql/registration/session.rs packages/rust/crates/xiuxian-wendao/src/search/queries/sql/registration/mod.rs packages/rust/crates/xiuxian-wendao/tests/unit/search/queries/sql/bounded_work_markdown.rs packages/rust/crates/xiuxian-wendao/README.md packages/rust/crates/xiuxian-qianji/docs/00_mocs/20260315141000-qianji-orchestrator-moc.md .cache/agent/blueprints/qianji_flowhub_planning_source_model.md .cache/agent/execplans/wendao-bounded-work-markdown-bootstrap-surface.md docs/GTD/DAILY_2026_04_08.md`

## Qianji Workdir Bounded Markdown Query Surface

- continued the active Flowhub graph-contract blueprint at
  `.cache/agent/blueprints/qianji_flowhub_planning_source_model.md` and
  advanced the bounded ExecPlan at
  `.cache/agent/execplans/qianji-workdir-bounded-markdown-query-surface.md`
- extended the bounded-work retrieval lane in two thin steps:
  `xiuxian-wendao` now exposes a stable
  `query_bounded_work_markdown_payload(...)` helper over the local `markdown`
  table, and `xiuxian-qianji` now mirrors it through
  `query_workdir_markdown_payload(...)` under the `workdir` library surface
- kept the slice bounded: no new CLI verb, no `show/check` behavior change,
  and no widening into the global Wendao SQL collector
- synchronized
  `packages/rust/crates/xiuxian-wendao/README.md`,
  `packages/rust/crates/xiuxian-qianji/README.md`,
  `packages/rust/crates/xiuxian-qianji/docs/00_mocs/20260315141000-qianji-orchestrator-moc.md`,
  and the active blueprint so the retrieval lane now records the workdir-facing
  query seam explicitly
- focused validation for this slice is green:
  `direnv exec . cargo fmt --all`
  `direnv exec . cargo test -p xiuxian-wendao search::queries::sql::bounded_work_markdown::tests::queries_bounded_work_markdown_payload --lib -- --exact`
  `direnv exec . cargo test -p xiuxian-qianji workdir_query_surface_returns_sql_payload --test workdir_contracts -- --exact`
  `direnv exec . cargo check -p xiuxian-wendao --tests`
  `direnv exec . cargo check -p xiuxian-qianji --tests`
  `direnv exec . git diff --check -- packages/rust/crates/xiuxian-wendao/src/search/queries/sql/bounded_work_markdown packages/rust/crates/xiuxian-wendao/src/search/queries/sql/execution/mod.rs packages/rust/crates/xiuxian-wendao/src/search/queries/sql/execution/result.rs packages/rust/crates/xiuxian-wendao/src/search/queries/sql/mod.rs packages/rust/crates/xiuxian-wendao/README.md packages/rust/crates/xiuxian-qianji/src/workdir packages/rust/crates/xiuxian-qianji/src/lib.rs packages/rust/crates/xiuxian-qianji/tests/integration/workdir_contracts.rs packages/rust/crates/xiuxian-qianji/README.md packages/rust/crates/xiuxian-qianji/docs/00_mocs/20260315141000-qianji-orchestrator-moc.md .cache/agent/blueprints/qianji_flowhub_planning_source_model.md .cache/agent/execplans/qianji-workdir-bounded-markdown-query-surface.md docs/GTD/DAILY_2026_04_08.md`

## Config Core Macro Precedence Dedup

- continued the active config-core runtime-resolution blueprint at
  `.cache/agent/blueprints/config_core_shared_runtime_resolution.md` and
  closed the bounded ExecPlan at
  `.cache/agent/execplans/archives/config-core-macro-precedence-dedup.md`
- added the small shared `xiuxian_config_core::first_some!` macro so the
  config-core rollout now removes repeated typed `Option` precedence chains
  instead of only moving parsing semantics into helper functions
- rewired
  `packages/rust/crates/xiuxian-qianji/src/runtime_config/resolve/wendao.rs`
  to use the shared precedence macro and switched
  `packages/rust/crates/xiuxian-qianji/src/runtime_config/env_vars.rs` to the
  existing `xiuxian_macros::env_non_empty!` helper, so the macro package now
  actually removes duplicated env-read boilerplate in the active runtime-config
  lane
- recovered the current `xiuxian-qianji` done-gate blockers that surfaced
  while validating this slice by finishing the missing `FlowhubRoot*`
  re-export in `packages/rust/crates/xiuxian-qianji/src/contracts/mod.rs`,
  moving test-only cache-home helpers out of the production `qianji` bin body,
  and clearing a `clippy::similar_names` violation in
  `packages/rust/crates/xiuxian-qianji/src/flowhub/check.rs`
- synchronized
  `packages/rust/crates/xiuxian-qianji/docs/20_specs/2026-04-07-qianji-runtime-config-layering.md`
  so the package spec now states why the earlier config-core push reduced
  semantic drift first and macro-driven call-site shrink second
- focused validation for this slice is green:
  `direnv exec . cargo test -p xiuxian-config-core`
  `direnv exec . cargo test -p xiuxian-qianji runtime_config:: --lib --features llm`
  `direnv exec . cargo test -p xiuxian-qianji --test runtime_config --features llm`
  `direnv exec . cargo test -p xiuxian-qianji --bin qianji --features llm`
  `direnv exec . cargo clippy -p xiuxian-config-core --all-targets -- -D warnings`
  `direnv exec . cargo clippy -p xiuxian-qianji --bin qianji --lib --tests --features llm -- -D warnings`
  `direnv exec . cargo fmt --all`
  `direnv exec . git diff --check -- ...`

## Qianji Project Root Runtime Dedup

- continued the active config-core runtime-resolution blueprint at
  `.cache/agent/blueprints/config_core_shared_runtime_resolution.md` and
  closed the bounded ExecPlan at
  `.cache/agent/execplans/archives/qianji-project-root-runtime-dedup.md`
- retired the remaining direct `PRJ_ROOT` parsing branches in
  `packages/rust/crates/xiuxian-qianji/src/bootcamp/runtime.rs`,
  `packages/rust/crates/xiuxian-qianji/src/scheduler/preflight/wendao_uri.rs`,
  and
  `packages/rust/crates/xiuxian-qianji/src/executors/wendao_refresh/input.rs`
  so those runtime owners now consume the shared Qianji pathing seam instead
  of each reading `std::env::var("PRJ_ROOT")`
- extended
  `packages/rust/crates/xiuxian-qianji/src/runtime_config/pathing.rs` with the
  shared process-root and process-env-path helpers, and synchronized
  `packages/rust/crates/xiuxian-qianji/docs/20_specs/2026-04-07-qianji-runtime-config-layering.md`
  so the Qianji runtime-config note now records the shared path-resolution
  ownership rule
- focused validation for this slice is:
  `direnv exec . cargo test -p xiuxian-qianji runtime_config:: --lib --features llm`
  `direnv exec . cargo test -p xiuxian-qianji scheduler::preflight:: --lib --features llm`
  `direnv exec . cargo test -p xiuxian-qianji --bin qianji --features llm`
  `direnv exec . cargo test -p xiuxian-qianji --test flowhub_scenario_show_check --features llm`
  `direnv exec . cargo clippy -p xiuxian-qianji --lib --tests --features llm -- -D warnings`
  `direnv exec . cargo fmt --all`
  `direnv exec . git diff --check -- ...`

## Qianji Cache Home Runtime Dedup

- continued the active config-core runtime-resolution blueprint at
  `.cache/agent/blueprints/config_core_shared_runtime_resolution.md` and
  closed the bounded ExecPlan at
  `.cache/agent/execplans/archives/qianji-cache-home-runtime-dedup.md`
- added a proper shared `resolve_cache_home_from_value(...)` surface in
  `packages/rust/crates/xiuxian-config-core/` and rewired
  `packages/rust/crates/xiuxian-qianji/src/bin/qianji.rs` so contract-feedback
  storage no longer reads `PRJ_CACHE_HOME` directly in the bin owner
- preserved the Qianji-specific safety rule that foreign absolute cache-home
  overrides fall back to the workspace-owned `.cache`, and synchronized the
  package note in
  `packages/rust/crates/xiuxian-qianji/docs/20_specs/2026-04-07-qianji-runtime-config-layering.md`
- focused validation for this slice is:
  `direnv exec . cargo test -p xiuxian-config-core`
  `direnv exec . cargo test -p xiuxian-qianji --bin qianji --features llm`
  `direnv exec . cargo clippy -p xiuxian-config-core --all-targets -- -D warnings`
  `direnv exec . cargo clippy -p xiuxian-qianji --lib --tests --features llm -- -D warnings`
  `direnv exec . cargo fmt --all`
  `direnv exec . git diff --check -- ...`

## Wendao Gateway Runtime Knobs TOML-First

- continued the active config-core runtime-resolution blueprint at
  `.cache/agent/blueprints/config_core_shared_runtime_resolution.md` and
  closed the bounded ExecPlan at
  `.cache/agent/execplans/archives/wendao-gateway-runtime-knobs-toml-first.md`
- retired the remaining env-only gateway runtime knob parsing in
  `packages/rust/crates/xiuxian-wendao/src/bin/wendao/execute/gateway/command.rs`,
  so gateway listen backlog, Studio concurrency limit, and Studio request
  timeout now resolve `wendao.toml` first and only fall back to
  `XIUXIAN_WENDAO_GATEWAY_*` env overrides when the TOML keys are absent
- extended the typed gateway config owner in
  `packages/rust/crates/xiuxian-wendao/src/bin/wendao/execute/gateway/config.rs`
  with `[gateway.runtime]` projection and documented the canonical keyspace in
  `packages/rust/crates/xiuxian-wendao/resources/config/wendao.toml` plus
  `packages/rust/crates/xiuxian-wendao/docs/03_features/214_config_import_overlay.md`
- added focused bin/unit coverage for TOML precedence, env fallback, clamp
  behavior, and overlay import parsing of `gateway.runtime.*`
- focused validation for this slice is green:
  `direnv exec . cargo fmt --all`
  `direnv exec . cargo test -p xiuxian-wendao gateway::studio::router::tests::config:: --lib`
  `direnv exec . cargo test -p xiuxian-wendao --bin wendao`
  `direnv exec . cargo clippy -p xiuxian-wendao --bin wendao --lib --tests -- -D warnings`

## Wendao Gateway Webhook Config Hygiene

- continued the active config-core runtime-resolution blueprint at
  `.cache/agent/blueprints/config_core_shared_runtime_resolution.md` and
  opened the bounded ExecPlan at
  `.cache/agent/execplans/wendao-gateway-webhook-config-hygiene.md`
- retired the remaining raw webhook env parsing in
  `packages/rust/crates/xiuxian-wendao/src/bin/wendao/execute/gateway/config.rs`,
  so webhook fallback now stays on the same trim-aware TOML-first lane as the
  other gateway runtime owners instead of reading `WENDAO_WEBHOOK_*` with raw
  `std::env::var`
- added a lookup seam and focused bin coverage for trimmed env values, blank
  env rejection, and `wendao.toml` precedence over webhook env fallback in
  `packages/rust/crates/xiuxian-wendao/tests/unit/bin/wendao/execute/gateway.rs`
- synchronized the package feature note in
  `packages/rust/crates/xiuxian-wendao/docs/03_features/214_config_import_overlay.md`
  so gateway webhook precedence is documented next to the other gateway
  runtime knobs
- focused validation for this slice is:
  `direnv exec . cargo test -p xiuxian-wendao --bin wendao`
  `direnv exec . cargo clippy -p xiuxian-wendao --bin wendao --lib --tests -- -D warnings`
  `direnv exec . cargo fmt --all`
  `direnv exec . git diff --check -- ...`

## Wendao Gateway Notify Status Config Consistency

- continued the active config-core runtime-resolution blueprint at
  `.cache/agent/blueprints/config_core_shared_runtime_resolution.md` and
  opened the bounded ExecPlan at
  `.cache/agent/execplans/wendao-gateway-notify-status-config-consistency.md`
- moved effective webhook status ownership into shared gateway state, so
  `packages/rust/crates/xiuxian-wendao/src/bin/wendao/execute/gateway/status.rs`
  now reports the startup-resolved webhook URL instead of re-reading
  `WENDAO_WEBHOOK_URL` from the live process environment
- extended
  `packages/rust/crates/xiuxian-wendao/src/gateway/studio/router/state/`
  with a bounded `webhook_url` state field and wired the gateway startup path
  in `packages/rust/crates/xiuxian-wendao/src/bin/wendao/execute/gateway/command.rs`
  to pass the effective URL chosen by the existing TOML-first webhook
  resolution logic
- updated notify-status tests to lock both configured and unconfigured webhook
  payloads, and patched the affected `GatewayState` fixtures to keep the new
  state field explicit
- synchronized
  `packages/rust/crates/xiuxian-wendao/docs/03_features/214_config_import_overlay.md`
  so the feature doc now states that `/api/notify/status` reflects the
  effective startup config rather than raw env
- focused validation for this slice is:
  `direnv exec . cargo test -p xiuxian-wendao --bin wendao`
  `direnv exec . cargo clippy -p xiuxian-wendao --bin wendao --lib --tests -- -D warnings`
  `direnv exec . cargo fmt --all`
  `direnv exec . git diff --check -- ...`

## Wendao Gateway Project Root Resolution Dedup

- continued the active config-core runtime-resolution blueprint at
  `.cache/agent/blueprints/config_core_shared_runtime_resolution.md` and
  opened the bounded ExecPlan at
  `.cache/agent/execplans/wendao-gateway-project-root-resolution-dedup.md`
- retired the remaining hand-rolled `PRJ_ROOT` lookup in
  `packages/rust/crates/xiuxian-wendao/src/bin/wendao/execute/gateway/config.rs`,
  so gateway config discovery now consumes
  `xiuxian-config-core::resolve_project_root()` instead of owning a separate
  env-path branch
- added focused config-bin coverage proving relative `PRJ_ROOT` values still
  resolve the shared `$PRJ_ROOT/wendao.toml` contract through the
  config-core helper path
- synchronized
  `packages/rust/crates/xiuxian-wendao/docs/03_features/214_config_import_overlay.md`
  so the package note now records that gateway config discovery is on the
  shared project-root helper lane
- focused validation for this slice is:
  `direnv exec . cargo test -p xiuxian-wendao --bin wendao`
  `direnv exec . cargo clippy -p xiuxian-wendao --bin wendao --lib --tests -- -D warnings`
  `direnv exec . cargo fmt --all`
  `direnv exec . git diff --check -- ...`

## Testing Gate Harness And Explanation Hardening

- continued the active testing-gate blueprint at
  `.cache/agent/blueprints/testing_gate_full_policy_alignment.md` and advanced
  the remediation wave tracked in
  `.cache/agent/execplans/testing-gate-auto-remediation-wave1.md`
- hardened `packages/rust/crates/xiuxian-testing/` so full crate policy now
  rejects both inline `#[cfg(test)] mod ... { ... }` blocks and
  `#[cfg(test)] mod ...;` declarations that remain in `src/` without an
  external `#[path]` mount into `tests/unit/...`
- tightened `tests/xiuxian-testings-rules.toml` semantics so every
  `allowed_root_files` and `allowed_directories` entry must carry an
  explanation, keeping the file as a narrowly justified exception surface
  under `tests/` instead of a silent root whitelist
- remediated `packages/rust/crates/xiuxian-vector` as the first bounded
  consumer crate in the wave: replaced the broad root allowlist with explicit
  harness entrypoints, migrated snapshots to
  `tests/integration/snapshots/`, and fixed the real stale-dataset cache bugs
  exposed by the stronger harness validation in
  `src/ops/admin_impl/index_ops.rs`,
  `src/ops/admin_impl/guards.rs`, and
  `src/ops/writer_impl/ingest_ops.rs`
- synchronized
  `packages/rust/crates/xiuxian-testing/docs/01_core/101_contract_testing_kernel.md`
  so the kernel doc now records the canonical root harness shape, the
  explanation requirement for rules TOML exceptions, and the required
  `#[cfg(test)] #[path = "..."]` externalization pattern
- closed the next self-hosting follow-up inside
  `packages/rust/crates/xiuxian-testing` itself: `src/lib.rs` now mounts the
  shared source-side harness for `cargo test --lib`, remaining inline test
  blocks in `src/external_test.rs` and `src/scenario.rs` moved to
  `tests/unit/`, `src/performance/tests.rs` was retired, and the old scattered
  root contract targets were consolidated behind `tests/integration_test.rs`
- fixed a real gate false positive in the same self-hosting slice by teaching
  the external-test validator to ignore rustdoc/comment `#[path = "..."]`
  examples, then locked that behavior with focused unit coverage
- focused validation for this slice is green:
  `direnv exec . cargo test -p xiuxian-testing`
  `direnv exec . cargo test -p xiuxian-vector invalidate_cached_table_drops_stale_dataset_handles --lib -- --nocapture`
  `direnv exec . cargo test -p xiuxian-vector --test integration_test lance_fts::test_streaming_fts_projection_does_not_autoproject_score_column -- --nocapture`
  `direnv exec . cargo test -p xiuxian-testing --lib`
  `direnv exec . cargo test -p xiuxian-testing --test integration_test`
  `direnv exec . cargo clippy -p xiuxian-testing --lib --tests -- -D warnings`
  `direnv exec . cargo fmt --all`
  `direnv exec . git diff --check -- ...`
  `direnv exec . cargo test -p xiuxian-vector --test integration_test partitioning::test_add_documents_partitioned_groups_by_column -- --nocapture`
  `direnv exec . cargo test -p xiuxian-vector --lib --test xiuxian-testing-gate --test unit_test --test integration_test --test performance_test`

## Wendao Julia Testing Gate Wiring

- continued the active testing-gate blueprint at
  `.cache/agent/blueprints/testing_gate_full_policy_alignment.md` and advanced
  the bounded remediation wave tracked in
  `.cache/agent/execplans/testing-gate-auto-remediation-wave1.md`
- moved `packages/rust/crates/xiuxian-wendao-julia` off the legacy
  `tests/xiuxian-testing-gate.rs` target and onto the canonical split shape:
  `src/lib.rs -> tests/unit/lib_policy.rs` for `cargo test --lib`, plus
  `tests/unit_test.rs` as the root harness target
- promoted `xiuxian-testing` from `dev-dependencies` to normal dependencies in
  `packages/rust/crates/xiuxian-wendao-julia/Cargo.toml` because the shared
  source harness now expands inside the library target
- normalized the crate-local helper mounts by moving
  `tests/support/plugin/**` to `tests/unit/plugin/**` and
  `tests/support/memory.rs` to `tests/unit/memory/mod.rs`, then externalized
  the smaller remaining source-owned `mod tests;` seams from
  `src/compatibility/link_graph/` and `src/plugin/rerank_exchange/`
- after the wiring landed, both `cargo test --lib` and
  `cargo test --test unit_test` now fail on the real remaining debt only:
  large inline `#[cfg(test)]` blocks across `src/integration_support/`,
  `src/memory/`, and `src/plugin/`
- focused validation for this slice is:
  `direnv exec . cargo fmt --all`
  `direnv exec . env CARGO_TARGET_DIR=.cache/cargo-target/testing-gate-xiuxian-wendao-julia cargo test -p xiuxian-wendao-julia --lib -- --list | rg 'compatibility::link_graph::tests::selectors_keep_stable_julia_ids|plugin::rerank_exchange::tests::julia_arrow_request_schema_uses_contract_columns|xiuxian_test_policy_harness::enforce_crate_test_policy_harness'`
  `direnv exec . env CARGO_TARGET_DIR=.cache/cargo-target/testing-gate-xiuxian-wendao-julia cargo test -p xiuxian-wendao-julia --lib compatibility::link_graph::tests::selectors_keep_stable_julia_ids -- --exact`
  `direnv exec . env CARGO_TARGET_DIR=.cache/cargo-target/testing-gate-xiuxian-wendao-julia cargo test -p xiuxian-wendao-julia --lib plugin::rerank_exchange::tests::julia_arrow_request_schema_uses_contract_columns -- --exact`
  `direnv exec . env CARGO_TARGET_DIR=.cache/cargo-target/testing-gate-xiuxian-wendao-julia cargo test -p xiuxian-wendao-julia --lib xiuxian_test_policy_harness::enforce_crate_test_policy_harness -- --exact` (expected policy failure on remaining inline-test debt)
  `direnv exec . env CARGO_TARGET_DIR=.cache/cargo-target/testing-gate-xiuxian-wendao-julia cargo test -p xiuxian-wendao-julia --test unit_test enforce_crate_test_policy_harness -- --exact` (expected policy failure on remaining inline-test debt)
  `direnv exec . env CARGO_TARGET_DIR=.cache/cargo-target/testing-gate-xiuxian-wendao-julia cargo clippy -p xiuxian-wendao-julia --lib --tests -- -D warnings`
  `direnv exec . git diff --check -- ...`

## Wendao Julia Inline Test Externalization Wave 2

- continued the active testing-gate blueprint at
  `.cache/agent/blueprints/testing_gate_full_policy_alignment.md` and advanced
  the bounded remediation wave tracked in
  `.cache/agent/execplans/testing-gate-auto-remediation-wave1.md`
- externalized the next smallest `xiuxian-wendao-julia` inline-test clusters:
  all three `src/integration_support/*` inline suites plus the four
  `src/memory/downcall/*` suites now mount canonical
  `tests/unit/integration_support/*` and `tests/unit/memory/downcall/*`
  files instead of keeping `#[cfg(test)] mod tests { ... }` bodies in `src/`
- confirmed the harness lane is still behaving correctly after the second
  slice: both `cargo test --lib` and `cargo test --test unit_test` continue to
  fail on the real remaining debt only, which is now reduced to the larger
  inline suites in the main `src/memory/` and `src/plugin/` trees
- focused validation for this slice is:
  `direnv exec . cargo fmt --all`
  `direnv exec . env CARGO_TARGET_DIR=.cache/cargo-target/testing-gate-xiuxian-wendao-julia cargo test -p xiuxian-wendao-julia --lib custom_service_cache_relative_dir_stays_relative`
  `direnv exec . env CARGO_TARGET_DIR=.cache/cargo-target/testing-gate-xiuxian-wendao-julia cargo test -p xiuxian-wendao-julia --lib planned_search_`
  `direnv exec . env CARGO_TARGET_DIR=.cache/cargo-target/testing-gate-xiuxian-wendao-julia cargo test -p xiuxian-wendao-julia --lib gateway_artifact_`
  `direnv exec . env CARGO_TARGET_DIR=.cache/cargo-target/testing-gate-xiuxian-wendao-julia cargo test -p xiuxian-wendao-julia --lib fetch_gate_score_recommendation_rows_from_evidence_roundtrips`
  `direnv exec . env CARGO_TARGET_DIR=.cache/cargo-target/testing-gate-xiuxian-wendao-julia cargo test -p xiuxian-wendao-julia --lib fetch_episodic_recall_score_rows_from_projection_roundtrips`
  `direnv exec . env CARGO_TARGET_DIR=.cache/cargo-target/testing-gate-xiuxian-wendao-julia cargo test -p xiuxian-wendao-julia --lib fetch_calibration_artifact_rows_from_inputs_roundtrips`
  `direnv exec . env CARGO_TARGET_DIR=.cache/cargo-target/testing-gate-xiuxian-wendao-julia cargo test -p xiuxian-wendao-julia --lib fetch_plan_tuning_advice_rows_from_inputs_roundtrips`
  `direnv exec . env CARGO_TARGET_DIR=.cache/cargo-target/testing-gate-xiuxian-wendao-julia cargo test -p xiuxian-wendao-julia --lib xiuxian_test_policy_harness::enforce_crate_test_policy_harness -- --exact` (expected policy failure on remaining inline-test debt)
  `direnv exec . env CARGO_TARGET_DIR=.cache/cargo-target/testing-gate-xiuxian-wendao-julia cargo test -p xiuxian-wendao-julia --test unit_test enforce_crate_test_policy_harness -- --exact` (expected policy failure on remaining inline-test debt)
  `direnv exec . env CARGO_TARGET_DIR=.cache/cargo-target/testing-gate-xiuxian-wendao-julia cargo clippy -p xiuxian-wendao-julia --lib --tests -- -D warnings`
  `direnv exec . git diff --check -- ...`

## Wendao Julia Inline Test Externalization Wave 3

- continued the active testing-gate blueprint at
  `.cache/agent/blueprints/testing_gate_full_policy_alignment.md` and advanced
  the bounded remediation wave tracked in
  `.cache/agent/execplans/testing-gate-auto-remediation-wave1.md`
- externalized the next small `xiuxian-wendao-julia` main-memory seams:
  `src/memory/runtime.rs`, `src/memory/profile.rs`, and
  `src/memory/transport/{client,process}.rs` now mount canonical
  `tests/unit/memory/...` files instead of keeping inline `#[cfg(test)]`
  modules in `src/`
- after this slice, the remaining gate surface is reduced to:
  `src/memory/transport/fetch.rs`, `src/memory/host/*`, the larger
  `src/memory/{gate_score,manifest,episodic_recall,calibration,plan_tuning}.rs`
  modules, and the large `src/plugin/*` suites
- focused validation for this slice is:
  `direnv exec . cargo fmt --all`
  `direnv exec . env CARGO_TARGET_DIR=.cache/cargo-target/testing-gate-xiuxian-wendao-julia cargo test -p xiuxian-wendao-julia --lib build_memory_julia_compute_bindings_`
  `direnv exec . env CARGO_TARGET_DIR=.cache/cargo-target/testing-gate-xiuxian-wendao-julia cargo test -p xiuxian-wendao-julia --lib build_memory_julia_compute_binding_rejects_invalid_runtime_values`
  `direnv exec . env CARGO_TARGET_DIR=.cache/cargo-target/testing-gate-xiuxian-wendao-julia cargo test -p xiuxian-wendao-julia --lib parse_memory_julia_compute_profile_`
  `direnv exec . env CARGO_TARGET_DIR=.cache/cargo-target/testing-gate-xiuxian-wendao-julia cargo test -p xiuxian-wendao-julia --lib memory_julia_compute_profile_contract_exposes_family_metadata`
  `direnv exec . env CARGO_TARGET_DIR=.cache/cargo-target/testing-gate-xiuxian-wendao-julia cargo test -p xiuxian-wendao-julia --lib build_memory_julia_compute_flight_transport_client_`
  `direnv exec . env CARGO_TARGET_DIR=.cache/cargo-target/testing-gate-xiuxian-wendao-julia cargo test -p xiuxian-wendao-julia --lib validate_memory_julia_compute_`
  `direnv exec . env CARGO_TARGET_DIR=.cache/cargo-target/testing-gate-xiuxian-wendao-julia cargo test -p xiuxian-wendao-julia --lib xiuxian_test_policy_harness::enforce_crate_test_policy_harness -- --exact` (expected policy failure on remaining inline-test debt)
  `direnv exec . env CARGO_TARGET_DIR=.cache/cargo-target/testing-gate-xiuxian-wendao-julia cargo test -p xiuxian-wendao-julia --test unit_test enforce_crate_test_policy_harness -- --exact` (expected policy failure on remaining inline-test debt)
  `direnv exec . env CARGO_TARGET_DIR=.cache/cargo-target/testing-gate-xiuxian-wendao-julia cargo clippy -p xiuxian-wendao-julia --lib --tests -- -D warnings`
  `direnv exec . git diff --check -- ...`

## Wendao Julia Inline Test Externalization Wave 4

- continued the active testing-gate blueprint at
  `.cache/agent/blueprints/testing_gate_full_policy_alignment.md` and advanced
  the bounded remediation wave tracked in
  `.cache/agent/execplans/testing-gate-auto-remediation-wave1.md`
- externalized `src/memory/transport/fetch.rs` onto the canonical
  `tests/unit/memory/transport/fetch.rs` mount instead of keeping the inline
  `#[cfg(test)] mod tests { ... }` body in `src/`
- after this slice, the remaining gate surface is reduced to:
  `src/memory/host/*`, the larger
  `src/memory/{gate_score,manifest,episodic_recall,calibration,plan_tuning}.rs`
  modules, and the large `src/plugin/*` suites
- focused validation for this slice is:
  `direnv exec . cargo fmt --all`
  `direnv exec . env CARGO_TARGET_DIR=.cache/cargo-target/testing-gate-xiuxian-wendao-julia cargo test -p xiuxian-wendao-julia --lib fetch_memory_julia_`
  `direnv exec . env CARGO_TARGET_DIR=.cache/cargo-target/testing-gate-xiuxian-wendao-julia cargo test -p xiuxian-wendao-julia --lib xiuxian_test_policy_harness::enforce_crate_test_policy_harness -- --exact` (expected policy failure on remaining inline-test debt)
  `direnv exec . env CARGO_TARGET_DIR=.cache/cargo-target/testing-gate-xiuxian-wendao-julia cargo test -p xiuxian-wendao-julia --test unit_test enforce_crate_test_policy_harness -- --exact` (expected policy failure on remaining inline-test debt)
  `direnv exec . env CARGO_TARGET_DIR=.cache/cargo-target/testing-gate-xiuxian-wendao-julia cargo clippy -p xiuxian-wendao-julia --lib --tests -- -D warnings`
  `direnv exec . git diff --check -- ...`

## Wendao Julia Inline Test Externalization Wave 5

- continued the active testing-gate blueprint at
  `.cache/agent/blueprints/testing_gate_full_policy_alignment.md` and advanced
  the bounded remediation wave tracked in
  `.cache/agent/execplans/testing-gate-auto-remediation-wave1.md`
- externalized the full `src/memory/host/*` subtree onto canonical
  `tests/unit/memory/host/*` mounts instead of keeping inline
  `#[cfg(test)] mod tests { ... }` bodies in `src/`
- after this slice, the remaining gate surface is reduced to:
  `src/memory/{gate_score,manifest,episodic_recall,calibration,plan_tuning}.rs`
  modules and the large `src/plugin/*` suites
- focused validation for this slice is:
  `direnv exec . cargo fmt --all`
  `direnv exec . env CARGO_TARGET_DIR=.cache/cargo-target/testing-gate-xiuxian-wendao-julia cargo test -p xiuxian-wendao-julia --lib build_memory_gate_score_`
  `direnv exec . env CARGO_TARGET_DIR=.cache/cargo-target/testing-gate-xiuxian-wendao-julia cargo test -p xiuxian-wendao-julia --lib build_episodic_recall_`
  `direnv exec . env CARGO_TARGET_DIR=.cache/cargo-target/testing-gate-xiuxian-wendao-julia cargo test -p xiuxian-wendao-julia --lib build_memory_calibration_`
  `direnv exec . env CARGO_TARGET_DIR=.cache/cargo-target/testing-gate-xiuxian-wendao-julia cargo test -p xiuxian-wendao-julia --lib build_memory_plan_tuning_`
  `direnv exec . env CARGO_TARGET_DIR=.cache/cargo-target/testing-gate-xiuxian-wendao-julia cargo test -p xiuxian-wendao-julia --lib xiuxian_test_policy_harness::enforce_crate_test_policy_harness -- --exact` (expected policy failure on remaining inline-test debt)
  `direnv exec . env CARGO_TARGET_DIR=.cache/cargo-target/testing-gate-xiuxian-wendao-julia cargo test -p xiuxian-wendao-julia --test unit_test enforce_crate_test_policy_harness -- --exact` (expected policy failure on remaining inline-test debt)
  `direnv exec . env CARGO_TARGET_DIR=.cache/cargo-target/testing-gate-xiuxian-wendao-julia cargo clippy -p xiuxian-wendao-julia --lib --tests -- -D warnings`
  `direnv exec . git diff --check -- ...`

## Wendao Julia Inline Test Externalization Wave 6

- continued the active testing-gate blueprint at
  `.cache/agent/blueprints/testing_gate_full_policy_alignment.md` and advanced
  the bounded remediation wave tracked in
  `.cache/agent/execplans/testing-gate-auto-remediation-wave1.md`
- externalized the remaining large `src/memory/*.rs` inline suites onto
  canonical `tests/unit/memory/{gate_score,manifest,episodic_recall,calibration,plan_tuning}.rs`
  mounts
- after this slice, the remaining gate surface is reduced to the four large
  `src/plugin/*` suites only
- focused validation for this slice is:
  `direnv exec . cargo fmt --all`
  `direnv exec . env CARGO_TARGET_DIR=.cache/cargo-target/testing-gate-xiuxian-wendao-julia cargo test -p xiuxian-wendao-julia --lib build_memory_julia_gate_score_`
  `direnv exec . env CARGO_TARGET_DIR=.cache/cargo-target/testing-gate-xiuxian-wendao-julia cargo test -p xiuxian-wendao-julia --lib build_memory_julia_compute_manifest_`
  `direnv exec . env CARGO_TARGET_DIR=.cache/cargo-target/testing-gate-xiuxian-wendao-julia cargo test -p xiuxian-wendao-julia --lib build_memory_julia_episodic_recall_`
  `direnv exec . env CARGO_TARGET_DIR=.cache/cargo-target/testing-gate-xiuxian-wendao-julia cargo test -p xiuxian-wendao-julia --lib build_memory_julia_calibration_`
  `direnv exec . env CARGO_TARGET_DIR=.cache/cargo-target/testing-gate-xiuxian-wendao-julia cargo test -p xiuxian-wendao-julia --lib build_memory_julia_plan_tuning_`
  `direnv exec . env CARGO_TARGET_DIR=.cache/cargo-target/testing-gate-xiuxian-wendao-julia cargo test -p xiuxian-wendao-julia --lib xiuxian_test_policy_harness::enforce_crate_test_policy_harness -- --exact` (expected policy failure on remaining inline-test debt)
  `direnv exec . env CARGO_TARGET_DIR=.cache/cargo-target/testing-gate-xiuxian-wendao-julia cargo test -p xiuxian-wendao-julia --test unit_test enforce_crate_test_policy_harness -- --exact` (expected policy failure on remaining inline-test debt)
  `direnv exec . env CARGO_TARGET_DIR=.cache/cargo-target/testing-gate-xiuxian-wendao-julia cargo clippy -p xiuxian-wendao-julia --lib --tests -- -D warnings`
  `direnv exec . git diff --check -- ...`

## Wendao Julia Inline Test Externalization Wave 7

- continued the active testing-gate blueprint at
  `.cache/agent/blueprints/testing_gate_full_policy_alignment.md` and advanced
  the bounded remediation wave tracked in
  `.cache/agent/execplans/testing-gate-auto-remediation-wave1.md`
- externalized the final large `src/plugin/*` inline suites onto canonical
  `tests/unit/plugin/{graph_structural,capability_manifest,transport,graph_structural_transport}.rs`
  mounts
- removed the last source-resident plugin-only `#[cfg(test)]` helper from
  `src/plugin/capability_manifest.rs`; the helper now lives inside the
  externalized unit file instead of `src/`
- after this slice, `packages/rust/crates/xiuxian-wendao-julia/` no longer has
  crate-test-policy debt and both shared gate entry points pass
- focused validation for this slice is:
  `direnv exec . cargo fmt --all`
  `direnv exec . env CARGO_TARGET_DIR=.cache/cargo-target/testing-gate-xiuxian-wendao-julia cargo test -p xiuxian-wendao-julia --lib capability_manifest_build_client_returns_none_without_config`
  `direnv exec . env CARGO_TARGET_DIR=.cache/cargo-target/testing-gate-xiuxian-wendao-julia cargo test -p xiuxian-wendao-julia --lib graph_structural_route_staging_resolves_canonical_paths`
  `direnv exec . env CARGO_TARGET_DIR=.cache/cargo-target/testing-gate-xiuxian-wendao-julia cargo test -p xiuxian-wendao-julia --lib build_julia_flight_transport_client_returns_none_without_inline_config`
  `direnv exec . env CARGO_TARGET_DIR=.cache/cargo-target/testing-gate-xiuxian-wendao-julia cargo test -p xiuxian-wendao-julia --lib build_graph_structural_flight_transport_client_returns_none_without_config`
  `direnv exec . env CARGO_TARGET_DIR=.cache/cargo-target/testing-gate-xiuxian-wendao-julia cargo test -p xiuxian-wendao-julia --lib xiuxian_test_policy_harness::enforce_crate_test_policy_harness -- --exact`
  `direnv exec . env CARGO_TARGET_DIR=.cache/cargo-target/testing-gate-xiuxian-wendao-julia cargo test -p xiuxian-wendao-julia --test unit_test enforce_crate_test_policy_harness -- --exact`
  `direnv exec . env CARGO_TARGET_DIR=.cache/cargo-target/testing-gate-xiuxian-wendao-julia cargo clippy -p xiuxian-wendao-julia --lib --tests -- -D warnings`
  `direnv exec . git diff --check -- ...`

## Vector Target-Local Testing Harness

- continued the active testing-gate blueprint at
  `.cache/agent/blueprints/testing_gate_full_policy_alignment.md` and advanced
  the remediation wave tracked in
  `.cache/agent/execplans/testing-gate-auto-remediation-wave1.md`
- retired the standalone
  `packages/rust/crates/xiuxian-vector/tests/xiuxian-testing-gate.rs` entry
  and moved `packages/rust/crates/xiuxian-vector/tests/integration_test.rs`,
  `packages/rust/crates/xiuxian-vector/tests/unit_test.rs`, and
  `packages/rust/crates/xiuxian-vector/tests/performance_test.rs` onto the
  canonical target-local harness shape with
  `xiuxian_testing::crate_test_policy_harness!()`
- confirmed that the source-backed unit files already externalize correctly via
  `#[cfg(test)] #[path = "..."] mod tests;`, so this slice stayed bounded to
  harness ownership instead of reopening the vector source externalization lane
- synchronized
  `packages/rust/crates/xiuxian-testing/docs/01_core/101_contract_testing_kernel.md`
  so the kernel doc now states that default crate test entrypoints should own
  the shared harness directly and that `xiuxian-testing-gate.rs` is only a
  migration surface
- focused validation for this slice is:
  `direnv exec . cargo test -p xiuxian-vector --test unit_test -- --list`
  `direnv exec . cargo test -p xiuxian-vector --test unit_test enforce_crate_test_policy_harness -- --exact`
  `direnv exec . cargo test -p xiuxian-vector --test integration_test -- --list`
  `direnv exec . cargo test -p xiuxian-vector --test integration_test enforce_crate_test_policy_harness -- --exact`
  `direnv exec . cargo test -p xiuxian-vector --test performance_test enforce_crate_test_policy_harness -- --exact`

## Testing Gate Source Harness Follow-Up

- continued the active testing-gate blueprint at
  `.cache/agent/blueprints/testing_gate_full_policy_alignment.md` and advanced
  the remediation wave tracked in
  `.cache/agent/execplans/testing-gate-auto-remediation-wave1.md`
- extended `packages/rust/crates/xiuxian-testing/src/lib.rs` and
  `packages/rust/crates/xiuxian-testing/src/policy.rs` with a new
  `crate_test_policy_source_harness!(...)` lane plus validator coverage, so
  crates that already externalize source-backed unit tests can keep the gate
  body out of `src/` while still enforcing it during `cargo test --lib`
- proved that lane in `packages/rust/crates/xiuxian-vector/src/lib.rs` by
  mounting `tests/unit/lib_policy.rs`; `cargo test -p xiuxian-vector --lib`
  now lists and runs
  `xiuxian_test_policy_harness::enforce_crate_test_policy_harness`
- promoted `packages/rust/crates/xiuxian-vector/Cargo.toml` to use
  `xiuxian-testing` as a normal dependency instead of a dev-dependency because
  the source-side harness expands inside `src/lib.rs`
- externalized the small inline utility/validation test modules inside
  `packages/rust/crates/xiuxian-testing/src/utils.rs` and
  `packages/rust/crates/xiuxian-testing/src/validation.rs` into
  `packages/rust/crates/xiuxian-testing/tests/unit/`
- synchronized
  `packages/rust/crates/xiuxian-testing/docs/01_core/101_contract_testing_kernel.md`
  so the kernel doc now records the canonical source-backed `--lib` harness
  shape in addition to the target-local `tests/*.rs` harness rule
- focused validation for this slice is:
  `direnv exec . cargo fmt --all`
  `direnv exec . cargo test -p xiuxian-testing --lib`
  `direnv exec . cargo clippy -p xiuxian-testing --lib --tests -- -D warnings`
  `direnv exec . cargo test -p xiuxian-vector --lib -- --list | rg 'enforce_crate_test_policy_harness|xiuxian_test_policy_harness'`
  `direnv exec . cargo test -p xiuxian-vector --lib xiuxian_test_policy_harness::enforce_crate_test_policy_harness -- --exact`
  `direnv exec . cargo test -p xiuxian-vector --test unit_test enforce_crate_test_policy_harness -- --exact`
  `direnv exec . cargo clippy -p xiuxian-vector --lib -- -D warnings`
  `direnv exec . cargo clippy -p xiuxian-vector --lib --tests -- -D warnings` blocked by pre-existing lint debt in
  `packages/rust/crates/xiuxian-vector/tests/integration/maintenance.rs`,
  `packages/rust/crates/xiuxian-vector/tests/unit/string_match.rs`,
  `packages/rust/crates/xiuxian-vector/tests/integration/columnar_tables.rs`,
  and
  `packages/rust/crates/xiuxian-vector/tests/integration/search_engine.rs`

## Git Repo Testing Gate Remediation

- continued the active testing-gate blueprint at
  `.cache/agent/blueprints/testing_gate_full_policy_alignment.md` and advanced
  the remediation wave tracked in
  `.cache/agent/execplans/testing-gate-auto-remediation-wave1.md`
- remediated `packages/rust/crates/xiuxian-git-repo` off the legacy
  `tests/xiuxian-testing-gate.rs` target and onto the canonical split shape:
  `src/lib.rs` now mounts `tests/unit/lib_policy.rs` for `cargo test --lib`,
  `tests/unit_test.rs` owns the root harness target, and
  `src/backend/gix/mod.rs` externalizes its source-backed tests into
  `tests/unit/backend/gix/mod.rs`
- promoted `packages/rust/crates/xiuxian-git-repo/Cargo.toml` to use
  `xiuxian-testing` as a normal dependency because the source-side harness now
  expands inside `src/lib.rs`
- while validating the stronger gate, recovered the newly exposed `gix`
  fixture drift in `tests/unit/backend/gix/mod.rs` and retired broad
  `expect()` usage across `tests/support/mod.rs`, `tests/unit/diff.rs`,
  `tests/unit/locks.rs`, and `tests/unit/materialization.rs` so strict clippy
  stays green on the remediated crate
- synchronized
  `packages/rust/crates/xiuxian-testing/docs/01_core/101_contract_testing_kernel.md`
  so the kernel doc now records `xiuxian-git-repo` as a real consumer proof of
  the source-backed `--lib` harness pattern
- focused validation for this slice is green:
  `direnv exec . cargo fmt --all`
  `direnv exec . env CARGO_TARGET_DIR=.cache/cargo-target/testing-gate-xiuxian-git-repo cargo test -p xiuxian-git-repo --lib`
  `direnv exec . env CARGO_TARGET_DIR=.cache/cargo-target/testing-gate-xiuxian-git-repo cargo test -p xiuxian-git-repo --test unit_test`
  `direnv exec . env CARGO_TARGET_DIR=.cache/cargo-target/testing-gate-xiuxian-git-repo cargo clippy -p xiuxian-git-repo --lib --tests -- -D warnings`
  `git diff --check -- packages/rust/crates/xiuxian-git-repo/Cargo.toml packages/rust/crates/xiuxian-git-repo/src/lib.rs packages/rust/crates/xiuxian-git-repo/src/backend/gix/mod.rs packages/rust/crates/xiuxian-git-repo/tests`

## Qianji Flowhub RFC 0004 Compact Validation And Flowchart Alignment

- continued the active planning-source blueprint at
  `.cache/agent/blueprints/qianji_flowhub_planning_source_model.md` and opened
  the bounded ExecPlan at
  `.cache/agent/execplans/qianji-flowhub-rfc-compact-validation-flowchart-alignment.md`
- published the draft at
  `packages/rust/crates/xiuxian-qianji/docs/rfcs/2026-04-08-compact-validation-flowchart-alignment-rfc.md`
  so the compact `[plan]` plus `[check]` contract, minimum
  `flowchart_alignment`, and the default `qianji show --dir` /
  `qianji check --dir` behavior now live on a dedicated canonical RFC surface
- synchronized `packages/rust/crates/xiuxian-qianji/README.md`, the Qianji
  MOC, and the governing planning-source blueprint so package docs now point
  at the new compact-validation RFC alongside the broader planning-source and
  retrieval RFCs
- focused validation for this slice is green:
  `direnv exec . git diff --check -- .cache/agent/blueprints/qianji_flowhub_planning_source_model.md .cache/agent/execplans/qianji-flowhub-rfc-compact-validation-flowchart-alignment.md packages/rust/crates/xiuxian-qianji/docs/rfcs/2026-04-08-compact-validation-flowchart-alignment-rfc.md packages/rust/crates/xiuxian-qianji/README.md packages/rust/crates/xiuxian-qianji/docs/00_mocs/20260315141000-qianji-orchestrator-moc.md docs/GTD/DAILY_2026_04_08.md`

## Qianji Flowhub RFC 0005 Markdown Skeleton Minimal Rules

- continued the active planning-source blueprint at
  `.cache/agent/blueprints/qianji_flowhub_planning_source_model.md` and opened
  the bounded ExecPlan at
  `.cache/agent/execplans/qianji-flowhub-rfc-markdown-skeleton-minimal-rules.md`
- published the draft at
  `packages/rust/crates/xiuxian-qianji/docs/rfcs/2026-04-08-markdown-skeleton-minimal-rules-rfc.md`
  so the minimum preservation, omission, and Codex read-order rules for
  `markdown.skeleton` now live on their own canonical RFC surface
- synchronized `packages/rust/crates/xiuxian-qianji/README.md`, the Qianji
  MOC, and the governing planning-source blueprint so package docs now point
  at the skeleton RFC alongside the broader planning-source, retrieval, and
  compact-validation RFCs
- focused validation for this slice is:
  `direnv exec . git diff --check -- .cache/agent/blueprints/qianji_flowhub_planning_source_model.md .cache/agent/execplans/qianji-flowhub-rfc-markdown-skeleton-minimal-rules.md packages/rust/crates/xiuxian-qianji/docs/rfcs/2026-04-08-markdown-skeleton-minimal-rules-rfc.md packages/rust/crates/xiuxian-qianji/README.md packages/rust/crates/xiuxian-qianji/docs/00_mocs/20260315141000-qianji-orchestrator-moc.md docs/GTD/DAILY_2026_04_08.md`

## Qianji Flowhub RFC 0006 Markdown Row Segmentation Minimal Rules

- continued the active planning-source blueprint at
  `.cache/agent/blueprints/qianji_flowhub_planning_source_model.md` and opened
  the bounded ExecPlan at
  `.cache/agent/execplans/qianji-flowhub-rfc-markdown-row-segmentation-minimal-rules.md`
- published the draft at
  `packages/rust/crates/xiuxian-qianji/docs/rfcs/2026-04-08-markdown-row-segmentation-minimal-rules-rfc.md`
  so the stable row model, minimum split rules, and anti-fragmentation
  constraints for the `markdown` retrieval surface now live on their own
  canonical RFC surface
- synchronized `packages/rust/crates/xiuxian-qianji/README.md`, the Qianji
  MOC, and the governing planning-source blueprint so package docs now point
  at the row-segmentation RFC alongside the broader planning-source,
  retrieval, compact-validation, and skeleton RFCs
- focused validation for this slice is:
  `direnv exec . git diff --check -- .cache/agent/blueprints/qianji_flowhub_planning_source_model.md .cache/agent/execplans/qianji-flowhub-rfc-markdown-row-segmentation-minimal-rules.md packages/rust/crates/xiuxian-qianji/docs/rfcs/2026-04-08-markdown-row-segmentation-minimal-rules-rfc.md packages/rust/crates/xiuxian-qianji/README.md packages/rust/crates/xiuxian-qianji/docs/00_mocs/20260315141000-qianji-orchestrator-moc.md docs/GTD/DAILY_2026_04_08.md`

## Qianji Flowhub RFC 0007 Flowchart Backbone Minimal Rules

- continued the active planning-source blueprint at
  `.cache/agent/blueprints/qianji_flowhub_planning_source_model.md` and opened
  the bounded ExecPlan at
  `.cache/agent/execplans/qianji-flowhub-rfc-flowchart-backbone-minimal-rules.md`
- published the draft at
  `packages/rust/crates/xiuxian-qianji/docs/rfcs/2026-04-08-flowchart-backbone-minimal-rules-rfc.md`
  so the minimum visible `flowchart.mmd` backbone contract, accepted
  compression, and obvious-conflict rejection rules now live on their own
  canonical RFC surface
- synchronized `packages/rust/crates/xiuxian-qianji/README.md`, the Qianji
  MOC, and the governing planning-source blueprint so package docs now point
  at the flowchart-backbone RFC alongside the broader planning-source,
  retrieval, compact-validation, skeleton, and row-segmentation RFCs
- focused validation for this slice is:
  `direnv exec . git diff --check -- .cache/agent/blueprints/qianji_flowhub_planning_source_model.md .cache/agent/execplans/qianji-flowhub-rfc-flowchart-backbone-minimal-rules.md packages/rust/crates/xiuxian-qianji/docs/rfcs/2026-04-08-flowchart-backbone-minimal-rules-rfc.md packages/rust/crates/xiuxian-qianji/README.md packages/rust/crates/xiuxian-qianji/docs/00_mocs/20260315141000-qianji-orchestrator-moc.md docs/GTD/DAILY_2026_04_08.md`

## Qianji Flowhub Root Contract And Top-Level Rust Correction

- continued the active planning-source blueprint at
  `.cache/agent/blueprints/qianji_flowhub_planning_source_model.md` and kept
  the bounded ExecPlan open at
  `.cache/agent/execplans/qianji-flowhub-top-level-rust-graph-decouple.md`
- corrected the real `qianji-flowhub/` graph shape so `rust/` is a top-level
  node next to `coding/`, `blueprint/`, and `plan/`, while `rust/` alone owns
  the nested constraint nodes `style/`, `engineering-requirement/`, and
  `policy/`
- replaced the old root `[check]` filesystem list with a reusable
  qianji.toml-rooted `[contract]` surface, so the Flowhub root and composite
  modules now declare `contract.register` plus `contract.required` and
  `qianji check` evaluates those contracts instead of relying on a script or a
  hand-expanded path list
- updated the Flowhub runtime helpers in `xiuxian-qianji` so root/module
  `show` and `check` honor the new `[contract]` namespace, top-level module
  ownership comes from the root contract, and repeated validation of shared
  nodes is deduplicated during guard evaluation
- synchronized `qianji-flowhub/README.md`, the package README, the Qianji MOC,
  and the active blueprint so the docs now describe root contract ownership,
  qianji.toml-only node anchors, and the corrected `coding` / `rust` split
- focused validation for this slice is:
  `direnv exec . cargo fmt --all`
  `direnv exec . cargo check -p xiuxian-qianji --features llm --tests`
  `direnv exec . cargo test -p xiuxian-qianji --test flowhub_contracts`
  `direnv exec . cargo test -p xiuxian-qianji --test flowhub_manifest_contracts`
  `direnv exec . cargo test -p xiuxian-qianji --test flowhub_scenario_show_check`
  `direnv exec . cargo test -p xiuxian-qianji --test flowhub_materialize`
  `direnv exec . cargo test -p xiuxian-qianji --features llm --bin qianji`
  `direnv exec . cargo run -p xiuxian-qianji --features llm --bin qianji -- show --dir qianji-flowhub`
  `direnv exec . cargo run -p xiuxian-qianji --features llm --bin qianji -- check --dir qianji-flowhub`
  `git -C qianji-flowhub diff --check`
  `direnv exec . git diff --check -- ...`

## Qianji Flowhub RFC 0008 Heading Path Minimal Conventions

- continued the active planning-source blueprint at
  `.cache/agent/blueprints/qianji_flowhub_planning_source_model.md` and opened
  the bounded ExecPlan at
  `.cache/agent/execplans/qianji-flowhub-rfc-heading-path-minimal-conventions.md`
- published the draft at
  `packages/rust/crates/xiuxian-qianji/docs/rfcs/2026-04-08-heading-path-minimal-conventions-rfc.md`
  so the stable external `heading_path` separator, ancestry order, root-row
  convention, and relation to `path` plus `title` now live on their own
  canonical RFC surface
- synchronized `packages/rust/crates/xiuxian-qianji/README.md`, the Qianji
  MOC, and the governing planning-source blueprint so package docs now point
  at the heading-path RFC alongside the broader planning-source, retrieval,
  compact-validation, skeleton, row-segmentation, and flowchart-backbone RFCs
- focused validation for this slice is:
  `direnv exec . git diff --check -- .cache/agent/blueprints/qianji_flowhub_planning_source_model.md .cache/agent/execplans/qianji-flowhub-rfc-heading-path-minimal-conventions.md packages/rust/crates/xiuxian-qianji/docs/rfcs/2026-04-08-heading-path-minimal-conventions-rfc.md packages/rust/crates/xiuxian-qianji/README.md packages/rust/crates/xiuxian-qianji/docs/00_mocs/20260315141000-qianji-orchestrator-moc.md docs/GTD/DAILY_2026_04_08.md`

## Qianji Workdir Show And Check Code

- continued the active planning-source blueprint at
  `.cache/agent/blueprints/qianji_flowhub_planning_source_model.md` and opened
  the bounded ExecPlan at
  `.cache/agent/execplans/qianji-flowhub-workdir-show-check-code.md`
- corrected the internal naming drift away from `plan_workdir` and landed a
  dedicated `workdir` feature surface in
  `packages/rust/crates/xiuxian-qianji/src/contracts/workdir/` plus
  `packages/rust/crates/xiuxian-qianji/src/workdir/`, covering compact
  root-manifest parse/load/validate, first-order surface rendering, and
  structural `flowchart.mmd` checks for bounded work surfaces
- wired `packages/rust/crates/xiuxian-qianji/src/bin/qianji.rs` so the binary
  now accepts `show --dir <workdir>` and `check --dir <workdir>` without
  widening into materialize or Wendao retrieval work
- added focused contract coverage in
  `packages/rust/crates/xiuxian-qianji/tests/workdir_contracts.rs` and
  extended `qianji` binary tests to cover the new CLI argument parsing surface
- synchronized `packages/rust/crates/xiuxian-qianji/README.md`, the Qianji
  MOC, and the governing planning-source blueprint so package docs now state
  that the first bounded `show/check` implementation is present in code

## Qianji Workdir CLI Hardening

- continued the active planning-source blueprint at
  `.cache/agent/blueprints/qianji_flowhub_planning_source_model.md` and opened
  the bounded ExecPlan at
  `.cache/agent/execplans/qianji-flowhub-workdir-cli-hardening.md`
- refactored `packages/rust/crates/xiuxian-qianji/src/bin/qianji.rs` so the
  new `show/check` path now runs through a pure helper that returns rendered
  output plus exit semantics instead of forcing tests to reason through direct
  process exit behavior
- extended the `qianji` binary tests to assert real rendered `show` output and
  blocking invalid-`check` exit code behavior using temp bounded-workdir
  fixtures
- synchronized `packages/rust/crates/xiuxian-qianji/README.md`, the Qianji
  MOC, and the governing planning-source blueprint so package docs now reflect
  that the bounded `show/check` path is test-hardened at the CLI layer

## Qianji Flowhub CLI Flowhub Bridge

- continued the active planning-source blueprint at
  `.cache/agent/blueprints/qianji_flowhub_planning_source_model.md` and opened
  the bounded ExecPlan at
  `.cache/agent/execplans/qianji-flowhub-cli-flowhub-bridge.md`
- added a dedicated `flowhub` runtime surface in
  `packages/rust/crates/xiuxian-qianji/src/flowhub/` plus a shallow
  `workdir` detector in `packages/rust/crates/xiuxian-qianji/src/workdir/`,
  so `qianji show --dir <path>` and `qianji check --dir <path>` now
  auto-detect bounded work surfaces versus real `qianji-flowhub` roots or
  module directories without widening into materialize
- extended `packages/rust/crates/xiuxian-qianji/src/bin/qianji.rs` and added
  `packages/rust/crates/xiuxian-qianji/tests/flowhub_contracts.rs`, so the
  CLI and library surfaces now cover real Flowhub root summaries, real root
  validation, and invalid module diagnostics alongside the existing workdir
  path
- verified the real library surface with
  `direnv exec . cargo run -p xiuxian-qianji --features llm --bin qianji -- show --dir qianji-flowhub`
  and
  `direnv exec . cargo run -p xiuxian-qianji --features llm --bin qianji -- check --dir qianji-flowhub`,
  which rendered 10 discovered modules and a passing Flowhub validation report

## Qianji Flowhub Materialize Workdir Core

- continued the active planning-source blueprint at
  `.cache/agent/blueprints/qianji_flowhub_planning_source_model.md` and opened
  the bounded ExecPlan at
  `.cache/agent/execplans/qianji-flowhub-materialize-workdir-core.md`
- added a modular `planning_source/materialize/` runtime in
  `packages/rust/crates/xiuxian-qianji/src/planning_source/materialize/`,
  so a real Flowhub scenario manifest can now materialize into a compact
  bounded work surface without adding a new CLI verb
- the new materialize core copies visible leaf-module templates into alias
  directories, generates the root `qianji.toml` plus root `flowchart.mmd`,
  and then validates the generated result through the existing
  `check_workdir` gate before returning success
- added focused coverage in
  `packages/rust/crates/xiuxian-qianji/tests/planning_source_materialize.rs`,
  proving that the real `coding_rust_blueprint_plan` scenario fixture
  materializes into a valid work surface with `blueprint/`, `plan/`,
  `qianji.toml`, and `flowchart.mmd`, while non-empty output targets are
  rejected

## Qianji PRJ Root Anchor Cleanup

- continued the active planning-source blueprint at
  `.cache/agent/blueprints/qianji_flowhub_planning_source_model.md` and opened
  the bounded ExecPlan at
  `.cache/agent/execplans/qianji-prj-root-anchor-cleanup.md`
- replaced the touched `xiuxian-qianji` test-local workspace root scanners with
  the shared `xiuxian-config-core::resolve_project_root` helper so the lane now
  anchors on `PRJ_ROOT` first instead of crate-local ancestor guessing
- kept the command surface unchanged: this slice only tightened path anchoring
  under the existing `show/check/materialize` coverage
- re-verified the touched scope with focused `xiuxian-qianji` tests, the
  `qianji` bin tests, and `cargo check --tests`

## Qianji Flowhub Namespace Cleanup

- continued the active planning-source blueprint at
  `.cache/agent/blueprints/qianji_flowhub_planning_source_model.md` and opened
  the bounded ExecPlan at
  `.cache/agent/execplans/qianji-flowhub-namespace-cleanup.md`
- removed the stale `planning_source` runtime and contract namespace from
  `xiuxian-qianji`, moving scenario detection, scenario `show/check`,
  manifest parse/load/resolve, and materialize code under
  `packages/rust/crates/xiuxian-qianji/src/flowhub/`
- renamed the contract validation types to `FlowhubValidation*`, renamed the
  focused test crates to `flowhub_*`, and moved the scenario fixtures from
  `tests/fixtures/planning_source/` to `tests/fixtures/flowhub/`
- removed all touched `#![allow(missing_docs)]` usage and replaced it with
  real crate-level test docs so the renamed test crates pass under the
  workspace `missing_docs` warning policy
- re-verified the touched scope with:
  `direnv exec . cargo fmt --all`
  `direnv exec . cargo check -p xiuxian-qianji --features llm --tests`
  `direnv exec . cargo test -p xiuxian-qianji --test flowhub_contracts --test flowhub_manifest_contracts --test flowhub_materialize --test flowhub_scenario_show_check --test workdir_contracts`
  `direnv exec . cargo test -p xiuxian-qianji --features llm --bin qianji`
  `direnv exec . cargo run -p xiuxian-qianji --features llm --bin qianji -- show --dir qianji-flowhub`
  `direnv exec . cargo run -p xiuxian-qianji --features llm --bin qianji -- check --dir qianji-flowhub`

## Qianji Flowhub RFC Guard Graph and Done Gate

- continued the active planning-source blueprint at
  `.cache/agent/blueprints/qianji_flowhub_planning_source_model.md` and opened
  the bounded ExecPlan at
  `.cache/agent/execplans/qianji-flowhub-rfc-guard-graph-done-gate.md`
- extended
  `packages/rust/crates/xiuxian-qianji/docs/rfcs/2026-04-07-qianji-flowhub-planning-source-model-rfc.md`
  so the main Flowhub planning RFC now defines scenario-as-guard-graph,
  bounded evaluation over `<plan-workdir>`, done-gate acceptance semantics,
  and blocked-vs-failed diagnostics for `qianji check`
- synchronized `packages/rust/crates/xiuxian-qianji/README.md`, the Qianji
  MOC, and the active blueprint so the package entry surfaces now reflect the
  new guard-graph and done-gate language
- focused validation for this docs-only slice is:
  `direnv exec . git diff --check -- .cache/agent/blueprints/qianji_flowhub_planning_source_model.md .cache/agent/execplans/qianji-flowhub-rfc-guard-graph-done-gate.md packages/rust/crates/xiuxian-qianji/docs/rfcs/2026-04-07-qianji-flowhub-planning-source-model-rfc.md packages/rust/crates/xiuxian-qianji/README.md packages/rust/crates/xiuxian-qianji/docs/00_mocs/20260315141000-qianji-orchestrator-moc.md docs/GTD/DAILY_2026_04_08.md`

## Qianji Flowhub Qianji.toml Validation Anchor

- continued the active planning-source blueprint at
  `.cache/agent/blueprints/qianji_flowhub_planning_source_model.md` and opened
  the bounded ExecPlan at
  `.cache/agent/execplans/qianji-flowhub-qianjitoml-validation-anchor.md`
- corrected the remaining `qianji-flowhub/README.md` drift so Flowhub no
  longer points to any `scripts/check_layout.sh` flow; the README now states
  that module-root `qianji.toml` files are the validation anchors and
  `qianji check --dir ...` is the only normative layout-check surface
- removed the ghost `scripts/check_layout.sh` index entry from the
  `qianji-flowhub` submodule state so the submodule no longer carries a
  half-deleted script surface
- updated the README fixture reference from
  `tests/fixtures/planning_source/...` to `tests/fixtures/flowhub/...`
- focused validation for this slice is:
  `direnv exec . cargo run -p xiuxian-qianji --features llm --bin qianji -- show --dir qianji-flowhub`
  `direnv exec . cargo run -p xiuxian-qianji --features llm --bin qianji -- check --dir qianji-flowhub`
  `direnv exec . git diff --check -- .cache/agent/execplans/qianji-flowhub-qianjitoml-validation-anchor.md qianji-flowhub/README.md docs/GTD/DAILY_2026_04_08.md`

## Qianji Flowhub Hierarchical Root Cleanup

- continued the active planning-source blueprint at
  `.cache/agent/blueprints/qianji_flowhub_planning_source_model.md` and opened
  the bounded ExecPlan at
  `.cache/agent/execplans/qianji-flowhub-hierarchical-root-cleanup.md`
- removed the remaining flat root modules from `qianji-flowhub/`, so the real
  Flowhub root now consists only of the hierarchical demo tree:
  `coding/`, `rust/`, `rust/{style,engineering-requirement,policy}/`,
  `blueprint/`, and `plan/`
- synchronized `qianji-flowhub/README.md` so the root library surface no
  longer describes `common-review`, `common-implement`, or
  `rust-borrow-check` as active root modules
- focused validation for this slice is:
  `direnv exec . cargo run -p xiuxian-qianji --features llm --bin qianji -- show --dir qianji-flowhub`
  `direnv exec . cargo run -p xiuxian-qianji --features llm --bin qianji -- check --dir qianji-flowhub`
  `direnv exec . git diff --check -- .cache/agent/execplans/qianji-flowhub-hierarchical-root-cleanup.md qianji-flowhub/README.md docs/GTD/DAILY_2026_04_08.md`

## Qianji Flowhub Qianji.toml-Only Node Graph

- continued the active planning-source blueprint at
  `.cache/agent/blueprints/qianji_flowhub_planning_source_model.md` and opened
  the bounded ExecPlan at
  `.cache/agent/execplans/qianji-flowhub-qianjitoml-only-node-graph.md`
- reduced the real `qianji-flowhub/` tree to qianji.toml-only nodes, so
  `blueprint/`, `plan/`, `rust/style/`,
  `rust/engineering-requirement/`, and `rust/policy/` now keep
  only their root manifests while composite nodes continue to anchor child
  existence checks inside their own `qianji.toml`
- updated `xiuxian-qianji` Flowhub contracts and scenario `show/check` so real
  Flowhub roots are treated as node graphs instead of checked-in
  `template/validation` bundles; template-bearing materialize proofs now run
  only against test-created fixture roots
- synchronized `qianji-flowhub/README.md`,
  `packages/rust/crates/xiuxian-qianji/README.md`, the Qianji MOC, and the
  active planning-source blueprint so docs now describe the live Flowhub
  contract as qianji.toml-only node anchors
- focused validation for this slice is:
  `direnv exec . cargo fmt --all`
  `direnv exec . cargo test -p xiuxian-qianji --test flowhub_contracts`
  `direnv exec . cargo test -p xiuxian-qianji --test flowhub_manifest_contracts`
  `direnv exec . cargo test -p xiuxian-qianji --test flowhub_scenario_show_check`
  `direnv exec . cargo test -p xiuxian-qianji --test flowhub_materialize`
  `direnv exec . cargo test -p xiuxian-qianji --features llm --bin qianji`
  `direnv exec . cargo run -p xiuxian-qianji --features llm --bin qianji -- show --dir qianji-flowhub`
  `direnv exec . cargo run -p xiuxian-qianji --features llm --bin qianji -- check --dir qianji-flowhub`
  `direnv exec . git diff --check -- .cache/agent/execplans/qianji-flowhub-qianjitoml-only-node-graph.md .cache/agent/blueprints/qianji_flowhub_planning_source_model.md qianji-flowhub/README.md packages/rust/crates/xiuxian-qianji/README.md packages/rust/crates/xiuxian-qianji/docs/00_mocs/20260315141000-qianji-orchestrator-moc.md docs/GTD/DAILY_2026_04_08.md`

## Qianji Flowhub Contract Structure Tightening

- continued the active planning-source blueprint at
  `.cache/agent/blueprints/qianji_flowhub_planning_source_model.md` and opened
  the bounded ExecPlan at
  `.cache/agent/execplans/qianji-flowhub-contract-structure-tightening.md`
- tightened Flowhub contract evaluation so `[contract].register` is now also
  the allowlist for child graph-node directories; undeclared child
  directories under a leaf or composite node are now structural drift
- removed the stale live directories `qianji-flowhub/rust/style/`,
  `qianji-flowhub/rust/engineering-requirement/`, and
  `qianji-flowhub/rust/policy/`, leaving the live Flowhub root at the
  top-level node anchors `coding/`, `rust/`, `blueprint/`, and `plan/`
- updated `qianji-flowhub/README.md`, the package README, the Qianji MOC, and
  the active blueprint so canonical docs no longer describe `rust` as a live
  composite directory tree
- updated Flowhub scenario and CLI coverage so `rust` is now rendered as a
  visible leaf surface instead of a hidden composite alias
- focused validation for this slice is:
  `direnv exec . cargo fmt --all`
  `direnv exec . cargo check -p xiuxian-qianji --features llm --tests`
  `direnv exec . cargo test -p xiuxian-qianji --features llm --test flowhub_contracts --test flowhub_manifest_contracts --test flowhub_scenario_show_check --test flowhub_materialize --bin qianji`
  `direnv exec . cargo run -p xiuxian-qianji --features llm --bin qianji -- show --dir qianji-flowhub`
  `direnv exec . cargo run -p xiuxian-qianji --features llm --bin qianji -- check --dir qianji-flowhub`
  `git -C qianji-flowhub diff --check`
  `direnv exec . git diff --check -- .cache/agent/blueprints/qianji_flowhub_planning_source_model.md .cache/agent/execplans/qianji-flowhub-contract-structure-tightening.md qianji-flowhub/README.md packages/rust/crates/xiuxian-qianji/src/flowhub/check.rs packages/rust/crates/xiuxian-qianji/src/bin/qianji.rs packages/rust/crates/xiuxian-qianji/tests/flowhub_contracts.rs packages/rust/crates/xiuxian-qianji/tests/flowhub_materialize.rs packages/rust/crates/xiuxian-qianji/tests/flowhub_scenario_show_check.rs packages/rust/crates/xiuxian-qianji/README.md packages/rust/crates/xiuxian-qianji/docs/00_mocs/20260315141000-qianji-orchestrator-moc.md docs/GTD/DAILY_2026_04_08.md`

## Qianji Flowhub RFC Contract Syntax Formalization

- continued the active planning-source blueprint at
  `.cache/agent/blueprints/qianji_flowhub_planning_source_model.md` and opened
  the bounded ExecPlan at
  `.cache/agent/execplans/qianji-flowhub-rfc-contract-syntax-formalization.md`
- replaced the old `template/validation/[[validation]]`-first wording in the
  main Flowhub planning RFC with the contract ordering the code now follows:
  root and composite structure are anchored by `[contract]`, while
  `[[validation]]` is an optional secondary rule surface
- formalized the minimal root and module contract syntax in
  `packages/rust/crates/xiuxian-qianji/docs/rfcs/2026-04-07-qianji-flowhub-planning-source-model-rfc.md`,
  including:
  `contract.register` as the child-directory allowlist,
  `contract.required` as the required filesystem surface list,
  and `*/...` expansion semantics at the Flowhub root
- synchronized the package README, Qianji MOC, and active blueprint so those
  surfaces now describe the same contract ordering as the main RFC
- focused validation for this slice is:
  `direnv exec . git diff --check -- .cache/agent/blueprints/qianji_flowhub_planning_source_model.md .cache/agent/execplans/qianji-flowhub-rfc-contract-syntax-formalization.md packages/rust/crates/xiuxian-qianji/docs/rfcs/2026-04-07-qianji-flowhub-planning-source-model-rfc.md packages/rust/crates/xiuxian-qianji/README.md packages/rust/crates/xiuxian-qianji/docs/00_mocs/20260315141000-qianji-orchestrator-moc.md docs/GTD/DAILY_2026_04_08.md`

## Qianji Flowhub RFC Contract Grammar and Drift Semantics

- continued the active planning-source blueprint at
  `.cache/agent/blueprints/qianji_flowhub_planning_source_model.md` and reused
  the bounded ExecPlan at
  `.cache/agent/execplans/qianji-flowhub-rfc-contract-syntax-formalization.md`
- extended the main Flowhub planning RFC so `[contract]` now has an explicit
  grammar section covering:
  relative-only `contract.register`,
  duplicate rejection,
  invalid segment rejection,
  relative `contract.required`,
  and the current `*/...` expansion rule
- added explicit drift semantics to the main RFC so canonical docs now match
  code: missing required paths, empty glob matches, and undeclared child
  directories all block `qianji check`
- synchronized the package README, Qianji MOC, and active blueprint so those
  surfaces now describe the same grammar and drift semantics
- focused validation for this slice is:
  `direnv exec . git diff --check -- .cache/agent/blueprints/qianji_flowhub_planning_source_model.md .cache/agent/execplans/qianji-flowhub-rfc-contract-syntax-formalization.md packages/rust/crates/xiuxian-qianji/docs/rfcs/2026-04-07-qianji-flowhub-planning-source-model-rfc.md packages/rust/crates/xiuxian-qianji/README.md packages/rust/crates/xiuxian-qianji/docs/00_mocs/20260315141000-qianji-orchestrator-moc.md docs/GTD/DAILY_2026_04_08.md`

## Qianji Flowhub RFC Contract Diagnostics Catalogue

- continued the active planning-source blueprint at
  `.cache/agent/blueprints/qianji_flowhub_planning_source_model.md` and opened
  the bounded ExecPlan at
  `.cache/agent/execplans/qianji-flowhub-rfc-contract-diagnostics-catalogue.md`
- extended the main Flowhub planning RFC so contract failures now have a
  documented minimum diagnostics catalogue:
  invalid root or module contract,
  missing required path,
  missing required glob matches,
  unregistered top-level directory drift,
  and unregistered child-directory drift
- fixed the minimum markdown output shape in both docs and tests:
  title, location, problem, why-it-blocks, and fix
- strengthened `packages/rust/crates/xiuxian-qianji/tests/flowhub_contracts.rs`
  so current contract failure cases assert the common markdown diagnostic
  skeleton
- synchronized the package README, Qianji MOC, and active blueprint so they
  describe the same contract-diagnostic surface
- focused validation for this slice is:
  `direnv exec . cargo test -p xiuxian-qianji --features llm --test flowhub_contracts`
  `direnv exec . git diff --check -- .cache/agent/blueprints/qianji_flowhub_planning_source_model.md .cache/agent/execplans/qianji-flowhub-rfc-contract-diagnostics-catalogue.md packages/rust/crates/xiuxian-qianji/tests/flowhub_contracts.rs packages/rust/crates/xiuxian-qianji/docs/rfcs/2026-04-07-qianji-flowhub-planning-source-model-rfc.md packages/rust/crates/xiuxian-qianji/README.md packages/rust/crates/xiuxian-qianji/docs/00_mocs/20260315141000-qianji-orchestrator-moc.md docs/GTD/DAILY_2026_04_08.md`

## Qianji Shared Check Markdown Diagnostics

- continued the active planning-source blueprint at
  `.cache/agent/blueprints/qianji_flowhub_planning_source_model.md` and opened
  the bounded ExecPlan at
  `.cache/agent/execplans/qianji-shared-check-markdown-diagnostics.md`
- added a shared internal diagnostics renderer under
  `packages/rust/crates/xiuxian-qianji/src/diagnostics/` and routed Flowhub
  root/module checks, Flowhub scenario checks, and bounded work-surface
  checks through the same markdown rendering skeleton
- strengthened focused tests in
  `packages/rust/crates/xiuxian-qianji/tests/flowhub_contracts.rs`,
  `packages/rust/crates/xiuxian-qianji/tests/flowhub_scenario_show_check.rs`,
  and `packages/rust/crates/xiuxian-qianji/tests/workdir_contracts.rs`
  so failing checks explicitly assert the shared markdown shape:
  title, location, problem, why-it-blocks, and fix
- extended the main Flowhub planning RFC, package README, Qianji MOC, and
  active blueprint so canonical docs now state that the same markdown
  diagnostics contract should hold across all supported `qianji check`
  surfaces
- focused validation for this slice is:
  `direnv exec . cargo fmt --all`
  `direnv exec . cargo test -p xiuxian-qianji --features llm --lib --test flowhub_contracts --test flowhub_scenario_show_check --test workdir_contracts`
  `direnv exec . cargo test -p xiuxian-qianji --features llm --bin qianji`
  `direnv exec . git diff --check -- .cache/agent/blueprints/qianji_flowhub_planning_source_model.md .cache/agent/execplans/qianji-shared-check-markdown-diagnostics.md packages/rust/crates/xiuxian-qianji/src/diagnostics/mod.rs packages/rust/crates/xiuxian-qianji/src/diagnostics/markdown.rs packages/rust/crates/xiuxian-qianji/src/flowhub/check.rs packages/rust/crates/xiuxian-qianji/src/flowhub/scenario/check.rs packages/rust/crates/xiuxian-qianji/src/workdir/check.rs packages/rust/crates/xiuxian-qianji/tests/flowhub_contracts.rs packages/rust/crates/xiuxian-qianji/tests/flowhub_scenario_show_check.rs packages/rust/crates/xiuxian-qianji/tests/workdir_contracts.rs packages/rust/crates/xiuxian-qianji/docs/rfcs/2026-04-07-qianji-flowhub-planning-source-model-rfc.md packages/rust/crates/xiuxian-qianji/README.md packages/rust/crates/xiuxian-qianji/docs/00_mocs/20260315141000-qianji-orchestrator-moc.md docs/GTD/DAILY_2026_04_08.md`

## Qianji Shared Show Markdown Surface

- continued the active planning-source blueprint at
  `.cache/agent/blueprints/qianji_flowhub_planning_source_model.md` and opened
  the bounded ExecPlan at
  `.cache/agent/execplans/qianji-shared-show-markdown-surface.md`
- replaced the old `src/diagnostics/` naming with a neutral
  `packages/rust/crates/xiuxian-qianji/src/markdown/` shared renderer surface
  so both `qianji check` and `qianji show` now use one internal markdown
  namespace
- routed Flowhub root/module previews, Flowhub scenario previews, and bounded
  work-surface previews through one shared markdown show renderer with the
  same external skeleton:
  one H1 title, metadata lines, and H2 sections for first-order surfaces
- strengthened focused tests in
  `packages/rust/crates/xiuxian-qianji/tests/flowhub_contracts.rs`,
  `packages/rust/crates/xiuxian-qianji/tests/flowhub_scenario_show_check.rs`,
  and `packages/rust/crates/xiuxian-qianji/tests/workdir_contracts.rs`
  so successful `show` renders assert the common markdown shape
- synchronized the main Flowhub RFC, package README, Qianji MOC, and active
  blueprint so canonical docs now describe the shared `qianji show` contract
  and the default `qianji show --dir <plan-workdir>` command form
- focused validation for this slice is:
  `direnv exec . cargo fmt --all`
  `direnv exec . cargo test -p xiuxian-qianji --features llm --lib --test flowhub_contracts --test flowhub_scenario_show_check --test workdir_contracts --bin qianji`
  `direnv exec . cargo run -p xiuxian-qianji --features llm --bin qianji -- show --dir qianji-flowhub`
  `direnv exec . cargo run -p xiuxian-qianji --features llm --bin qianji -- show --dir packages/rust/crates/xiuxian-qianji/tests/fixtures/flowhub/coding_rust_blueprint_plan`
  `direnv exec . git diff --check -- .cache/agent/blueprints/qianji_flowhub_planning_source_model.md .cache/agent/execplans/qianji-shared-show-markdown-surface.md packages/rust/crates/xiuxian-qianji/src/lib.rs packages/rust/crates/xiuxian-qianji/src/markdown/mod.rs packages/rust/crates/xiuxian-qianji/src/markdown/check.rs packages/rust/crates/xiuxian-qianji/src/markdown/show.rs packages/rust/crates/xiuxian-qianji/src/flowhub/show.rs packages/rust/crates/xiuxian-qianji/src/flowhub/scenario/show.rs packages/rust/crates/xiuxian-qianji/src/workdir/show.rs packages/rust/crates/xiuxian-qianji/src/flowhub/check.rs packages/rust/crates/xiuxian-qianji/src/flowhub/scenario/check.rs packages/rust/crates/xiuxian-qianji/src/workdir/check.rs packages/rust/crates/xiuxian-qianji/tests/flowhub_contracts.rs packages/rust/crates/xiuxian-qianji/tests/flowhub_scenario_show_check.rs packages/rust/crates/xiuxian-qianji/tests/workdir_contracts.rs packages/rust/crates/xiuxian-qianji/docs/rfcs/2026-04-07-qianji-flowhub-planning-source-model-rfc.md packages/rust/crates/xiuxian-qianji/README.md packages/rust/crates/xiuxian-qianji/docs/00_mocs/20260315141000-qianji-orchestrator-moc.md docs/GTD/DAILY_2026_04_08.md`

## Qianji Flowhub Plan Scenario-Case Mermaid

- continued the active planning-source blueprint at
  `.cache/agent/blueprints/qianji_flowhub_planning_source_model.md` and
  updated the bounded ExecPlan at
  `.cache/agent/execplans/qianji-flowhub-plan-scenario-case-mmd.md`
- added the live scenario-case graph
  `qianji-flowhub/plan/codex-plan.mmd` and contracted it through
  `qianji-flowhub/plan/qianji.toml`
- switched the Mermaid scenario-case parser under
  `packages/rust/crates/xiuxian-qianji/src/flowhub/mermaid/` onto
  `merman-core` so `qianji check` now parses immediate node-owned `.mmd`
  files through a mature dependency, classifies labels matching live Flowhub
  module names as graph-module nodes, and rejects malformed or uncontracted
  scenario-case graphs
- tightened scenario-case graph validation so a valid Mermaid assembly must
  derive `merimind_graph_name` from the owning filename stem, cover every
  registered Flowhub module node required by the root contract, and keep one
  connected module backbone; disconnected or incomplete module backbones now
  fail `qianji check`
- tightened the same Mermaid lane again so undeclared graph-node labels now
  fail validation explicitly, and the live `codex-plan.mmd` graph was reduced
  to the current real module set plus guard/process nodes
- updated `qianji show --dir "$PRJ_ROOT/qianji-flowhub/plan"` so the
  Scenario Cases section now renders explicit `Graph name` plus `Path`
  fields for each owned Mermaid case
- updated `qianji show --dir "$PRJ_ROOT/qianji-flowhub/plan"` so the `plan`
  node exposes its owned scenario cases directly in the shared markdown show
  surface
- synchronized the Flowhub README, package README, Qianji MOC, main Flowhub
  RFC, active blueprint, and ExecPlan so the scenario-case `.mmd` contract is
  documented consistently
- focused validation for this slice is currently:
  `direnv exec . cargo fmt --all`
  `direnv exec . cargo run -p xiuxian-qianji --features llm --bin qianji -- show --dir qianji-flowhub/plan`
  `direnv exec . cargo run -p xiuxian-qianji --features llm --bin qianji -- check --dir qianji-flowhub`
- full `direnv exec . cargo test -p xiuxian-qianji --features llm --test flowhub_contracts`
  is currently blocked by pre-existing external-test-policy debt in unrelated
  files under `packages/rust/crates/xiuxian-qianji/src/`

## Wendao Code Parser Native Flight Provider Slice

- opened the new parser-migration blueprint at
  `.cache/agent/blueprints/wendao_code_parser_flight_native_language_parsers.md`
  and advanced the bounded provider ExecPlan at
  `.cache/agent/execplans/wendao-code-parser-flight-native-ast-slice1.md`
- scaffolded `.data/WendaoCodeParser.jl` as the native Julia and Modelica
  parser provider over `WendaoArrow`, including request and response contracts,
  `src/search/` AST-query routes, `JuliaSyntax.jl`-backed Julia summary
  extraction, and the initial provider shell for Modelica routes
- mounted `WendaoCodeParser.jl` into `.data/WendaoSearch.jl` as an opt-in
  same-port live service surface, so `scripts/run_search_service.jl` can now
  serve Search routes and parser routes together through
  `--code-parser-route-name` or `--code-parser-route-names`
- synchronized `.data/WendaoCodeParser.jl/README.md`,
  `.data/WendaoSearch.jl/README.md`, and
  `.data/WendaoSearch.jl/docs/testing/README.md` so package docs now record the
  mounted parser surface, current route names, and the native parser contract
- follow-up on the same slice: replaced the earlier Modelica
  backend-unavailable placeholder with an `OMParser.jl` official source-build
  fallback (`lib/parser -> autoconf -> ./configure -> make`), real Modelica
  summary plus AST-query normalization, and a mounted child-service regression
  that now passes once the live-test harness stops forcing
  `JULIA_LOAD_PATH=@:@stdlib`
- focused validation for this slice is:
  `direnv exec . julia --project=.data/WendaoCodeParser.jl --color=yes .data/WendaoCodeParser.jl/test/runtests.jl`
  `direnv exec . julia --project=.data/WendaoSearch.jl -e 'using Pkg; Pkg.resolve(); Pkg.instantiate()'`
  `direnv exec . julia --project=.data/WendaoSearch.jl -e 'using WendaoSearch; include(".data/WendaoSearch.jl/test/support/live_service.jl"); with_wendaosearch_live_service(route_name = :capability_manifest, code_parser_route_names = [:julia_ast_query, :modelica_file_summary], service_mode = :demo, probe_profile = :exact) do port, _; client = WendaoSearch.WendaoArrow.Arrow.Flight.Client("grpc://127.0.0.1:$(port)"); julia_request = WendaoSearch.WendaoCodeParser.parser_exchange_request(WendaoSearch.WendaoCodeParser.JULIA_AST_QUERY_ROUTE, [WendaoSearch.WendaoCodeParser.ParserRequest("live-julia", "Demo.jl", "module Demo\nfoo(x)=x\nexport foo\nend\n"; query_json = "{\"node_kind\":\"function\",\"name_contains\":\"fo\",\"limit\":5}")]); julia_table = WendaoSearch.WendaoArrow.flight_exchange_table(client, julia_request); julia_columns = WendaoSearch.WendaoArrow.Tables.columntable(julia_table); @assert julia_columns.success == [true]; modelica_request = WendaoSearch.WendaoCodeParser.parser_exchange_request(WendaoSearch.WendaoCodeParser.MODELICA_FILE_SUMMARY_ROUTE, [WendaoSearch.WendaoCodeParser.ParserRequest("live-modelica", "Demo.mo", "model Demo\n  Real x;\nend Demo;\n")]); modelica_table = WendaoSearch.WendaoArrow.flight_exchange_table(client, modelica_request); modelica_columns = WendaoSearch.WendaoArrow.Tables.columntable(modelica_table); @assert modelica_columns.success == [true]; end'`

## Qianji Test-Policy Source Externalization

- opened the bounded ExecPlan at
  `.cache/agent/execplans/qianji-test-policy-source-externalization.md`
  under the active Flowhub blueprint
  `.cache/agent/blueprints/qianji_flowhub_planning_source_model.md`
- mounted the shared source harness in
  `packages/rust/crates/xiuxian-qianji/src/lib.rs` and added
  `packages/rust/crates/xiuxian-qianji/tests/unit/lib_policy.rs`
- externalized the previously inline source test modules from
  `packages/rust/crates/xiuxian-qianji/src/bin/qianji.rs` and
  `packages/rust/crates/xiuxian-qianji/src/contract_feedback/rest_docs.rs`
  into `tests/unit/bin/qianji.rs` and
  `tests/unit/contract_feedback/rest_docs.rs`
- synchronized `packages/rust/crates/xiuxian-qianji/README.md` and
  `packages/rust/crates/xiuxian-qianji/docs/00_mocs/20260315141000-qianji-orchestrator-moc.md`
  so package docs now record that the harness debt for this slice is reduced
  to the remaining inline source tests under `src/executors/` and
  `src/sovereign/`
- focused validation for this slice is:
  `direnv exec . cargo fmt --all`
  `direnv exec . cargo test -p xiuxian-qianji --features llm --test flowhub_contracts`
  `direnv exec . cargo test -p xiuxian-qianji --features llm --bin qianji`
  `direnv exec . cargo test -p xiuxian-qianji --features llm openapi_file_rest_docs_rule_pack_collects_yaml_document --lib`
  `direnv exec . cargo check -p xiuxian-qianji --features llm --tests`
  `direnv exec . git diff --check -- packages/rust/crates/xiuxian-qianji/src/lib.rs packages/rust/crates/xiuxian-qianji/src/bin/qianji.rs packages/rust/crates/xiuxian-qianji/src/contract_feedback/rest_docs.rs packages/rust/crates/xiuxian-qianji/tests/unit/lib_policy.rs packages/rust/crates/xiuxian-qianji/tests/unit/bin/qianji.rs packages/rust/crates/xiuxian-qianji/tests/unit/contract_feedback/rest_docs.rs`
- current remaining blocker is explicit:
  `direnv exec . cargo test -p xiuxian-qianji --features llm --test flowhub_contracts`
  still fails `enforce_crate_test_policy_harness`, but only for the
  still-inline test modules under `packages/rust/crates/xiuxian-qianji/src/executors/`
  and `packages/rust/crates/xiuxian-qianji/src/sovereign/`

## Qianji Test-Policy Executors Externalization

- opened the bounded ExecPlan at
  `.cache/agent/execplans/qianji-test-policy-executors-externalization.md`
  under the active Flowhub blueprint
  `.cache/agent/blueprints/qianji_flowhub_planning_source_model.md`
- externalized the remaining inline source test modules under
  `packages/rust/crates/xiuxian-qianji/src/executors/` into
  `packages/rust/crates/xiuxian-qianji/tests/unit/executors/`
- replaced the inline `#[cfg(test)]` modules in:
  `src/executors/router.rs`
  `src/executors/formal_audit/native.rs`
  `src/executors/llm/mechanism.rs`
  `src/executors/llm/streaming.rs`
  `src/executors/annotation/persona_markdown.rs`
  with path-mounted unit modules
- synchronized package docs so the current crate-test-policy status now states
  that executor debt is cleared and the remaining inline source-test debt lives
  only under `packages/rust/crates/xiuxian-qianji/src/sovereign/`
- focused validation for this slice is:
  `direnv exec . cargo fmt --all`
  `direnv exec . cargo test -p xiuxian-qianji --features llm executors --lib`
  `direnv exec . cargo test -p xiuxian-qianji --features llm --test flowhub_contracts`
  `direnv exec . cargo check -p xiuxian-qianji --features llm --tests`
  `direnv exec . git diff --check -- packages/rust/crates/xiuxian-qianji/src/executors/router.rs packages/rust/crates/xiuxian-qianji/src/executors/formal_audit/native.rs packages/rust/crates/xiuxian-qianji/src/executors/llm/mechanism.rs packages/rust/crates/xiuxian-qianji/src/executors/annotation/persona_markdown.rs packages/rust/crates/xiuxian-qianji/src/executors/llm/streaming.rs packages/rust/crates/xiuxian-qianji/tests/unit/executors/router.rs packages/rust/crates/xiuxian-qianji/tests/unit/executors/formal_audit/native.rs packages/rust/crates/xiuxian-qianji/tests/unit/executors/llm/mechanism.rs packages/rust/crates/xiuxian-qianji/tests/unit/executors/annotation/persona_markdown.rs packages/rust/crates/xiuxian-qianji/tests/unit/executors/llm/streaming.rs .cache/agent/execplans/qianji-test-policy-executors-externalization.md`
- decisive outcome:
  `direnv exec . cargo test -p xiuxian-qianji --features llm --test flowhub_contracts`
  still fails `enforce_crate_test_policy_harness`, but its report now contains
  only the remaining inline source-test modules under
  `packages/rust/crates/xiuxian-qianji/src/sovereign/`

## Qianji Test-Policy Sovereign Externalization

- opened the bounded ExecPlan at
  `.cache/agent/execplans/qianji-test-policy-sovereign-externalization.md`
  under the active Flowhub blueprint
  `.cache/agent/blueprints/qianji_flowhub_planning_source_model.md`
- externalized the remaining inline source test modules under
  `packages/rust/crates/xiuxian-qianji/src/sovereign/` into
  `packages/rust/crates/xiuxian-qianji/tests/unit/sovereign/`
- replaced the inline `#[cfg(test)]` modules in:
  `src/sovereign/contract_feedback_sink.rs`
  `src/sovereign/wendao_adapter.rs`
  `src/sovereign/thought_aggregator.rs`
  `src/sovereign/wendao_sink.rs`
  `src/sovereign/artifact_observer.rs`
  with path-mounted unit modules
- synchronized package docs so the current crate-test-policy status now states
  that the bounded cleanup lane is complete and the shared harness for
  `packages/rust/crates/xiuxian-qianji/` passes again
- focused validation for this slice is:
  `direnv exec . cargo fmt --all`
  `direnv exec . cargo test -p xiuxian-qianji --features llm sovereign --lib`
  `direnv exec . cargo test -p xiuxian-qianji --features llm --test flowhub_contracts`
  `direnv exec . cargo check -p xiuxian-qianji --features llm --tests`
- decisive outcome:
  `direnv exec . cargo test -p xiuxian-qianji --features llm --test flowhub_contracts`
  now passes fully, including `enforce_crate_test_policy_harness`

## Qianji Flowhub RFC Canonical Naming Realignment

- opened the bounded ExecPlan at
  `.cache/agent/execplans/qianji-flowhub-rfc-canonical-flowhub-renaming.md`
  under the active Flowhub blueprint
  `.cache/agent/blueprints/qianji_flowhub_planning_source_model.md`
- renamed the main canonical Flowhub RFC from
  `packages/rust/crates/xiuxian-qianji/docs/rfcs/2026-04-07-qianji-flowhub-planning-source-model-rfc.md`
  to
  `packages/rust/crates/xiuxian-qianji/docs/rfcs/2026-04-07-qianji-flowhub-graph-contract-model-rfc.md`
  and retitled it to match the live Flowhub graph-contract model
- synchronized the package README, the Qianji MOC, and the active blueprint so
  live canonical references now point at the new RFC path and title
- deliberately kept the hidden active blueprint file path unchanged for
  continuity across existing active GTD and ExecPlan references, while
  updating its internal title and governing RFC reference
- focused validation for this slice is:
  `direnv exec . cargo run -p xiuxian-qianji --features llm --bin qianji -- show --dir qianji-flowhub`
  `direnv exec . cargo run -p xiuxian-qianji --features llm --bin qianji -- check --dir qianji-flowhub`
  `direnv exec . git diff --check -- packages/rust/crates/xiuxian-qianji/docs/rfcs/2026-04-07-qianji-flowhub-graph-contract-model-rfc.md packages/rust/crates/xiuxian-qianji/README.md packages/rust/crates/xiuxian-qianji/docs/00_mocs/20260315141000-qianji-orchestrator-moc.md .cache/agent/blueprints/qianji_flowhub_planning_source_model.md .cache/agent/execplans/qianji-flowhub-rfc-canonical-flowhub-renaming.md docs/GTD/DAILY_2026_04_08.md`

## Wendao Test-Policy Harness Wiring

- continued the active testing-gate remediation wave for
  `packages/rust/crates/xiuxian-wendao/`
- mounted the shared source harness in
  `packages/rust/crates/xiuxian-wendao/src/lib.rs`, added
  `packages/rust/crates/xiuxian-wendao/tests/unit/lib_policy.rs`, and added
  `packages/rust/crates/xiuxian-wendao/tests/unit_test.rs` as the explicit root
  harness target
- externalized the first bounded inline-test cluster into canonical
  `tests/unit/...` files:
  `src/analyzers/service/bootstrap.rs`
  `src/gateway/studio/types/collection.rs`
  `src/link_graph/stats_cache/runtime.rs`
  `src/skill_vfs/zhixing/indexer/stats.rs`
  `src/gateway/studio/router/handlers/repo/shared/repository.rs`
- focused validation for this slice is:
  `direnv exec . cargo fmt --all`
  `direnv exec . cargo test -p xiuxian-wendao --lib analyzers::service::bootstrap::tests::bootstrap_builtin_registry_registers_modelica_plugin -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao --lib gateway::studio::types::collection::tests::studio_type_collection_exports_generic_plugin_artifact_types_only -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao --lib link_graph::stats_cache::runtime::tests::normalize_stats_cache_key_prefix_falls_back_for_blank_input -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao --lib skill_vfs::zhixing::indexer::stats::tests::count_agenda_statuses_tracks_open_and_done_tasks -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao --lib gateway::studio::router::handlers::repo::shared::repository::tests::repository_uses_managed_remote_source_only_when_url_is_present -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao --test unit_test --no-run`
- decisive outcome:
  both `direnv exec . cargo test -p xiuxian-wendao --lib xiuxian_test_policy_harness::enforce_crate_test_policy_harness -- --exact`
  and
  `direnv exec . cargo test -p xiuxian-wendao --test unit_test enforce_crate_test_policy_harness -- --exact`
  now execute the shared gate and fail only on real remaining debt under
  `src/**` plus the non-standard `tests/support` mounts in
  `src/analyzers/service/projection/tests.rs`
- current clippy blockers are explicit and pre-existing:
  `packages/rust/crates/xiuxian-wendao/src/search/queries/sql/bounded_work_markdown/register.rs`
  still hits `clippy::doc_markdown`, and
  `packages/rust/crates/xiuxian-wendao/tests/unit/search/queries/sql/bounded_work_markdown.rs`
  still hits repeated `clippy::expect_used`

## Wendao Test-Policy Follow-Up Slice 2

- continued the bounded `packages/rust/crates/xiuxian-wendao/` cleanup wave
  without widening into the larger gateway or link-graph suites
- externalized the inline test modules in:
  `src/valkey_common.rs`
  `src/link_graph/agentic/store/common.rs`
  onto canonical `tests/unit/...` mounts
- moved the `src/analyzers/service/projection/` test module fully out of
  `src/` by path-mounting
  `tests/unit/analyzers/service/projection/mod.rs` from
  `src/analyzers/service/projection/mod.rs`, deleting the old
  `src/analyzers/service/projection/tests.rs`, and localizing the fixture-only
  `sample_projection_analysis(...)` helper under the new unit-test support file
- focused validation for this slice is:
  `direnv exec . cargo fmt --all`
  `direnv exec . cargo test -p xiuxian-wendao --lib valkey_common::tests::normalize_key_prefix_falls_back_for_blank_input -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao --lib link_graph::agentic::store::common::tests::redis_client_preserves_agentic_error_context -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao --lib analyzers::service::projection::tests::repo_projected_pages_wraps_projection_fixture -- --exact`
  `direnv exec . cargo clippy -p xiuxian-wendao --lib --tests -- -D warnings`
- decisive outcome:
  both
  `direnv exec . cargo test -p xiuxian-wendao --lib xiuxian_test_policy_harness::enforce_crate_test_policy_harness -- --exact`
  and
  `direnv exec . cargo test -p xiuxian-wendao --test unit_test enforce_crate_test_policy_harness -- --exact`
  still fail as expected, but their live failure surface no longer includes
  `src/valkey_common.rs`,
  `src/link_graph/agentic/store/common.rs`, or the old
  `src/analyzers/service/projection/tests.rs`
- the full `clippy` gate is still blocked only by pre-existing debt under
  `src/search/queries/sql/bounded_work_markdown/register.rs`
  (`clippy::doc_markdown`) and
  `tests/unit/search/queries/sql/bounded_work_markdown.rs`
  (`clippy::expect_used` and `clippy::uninlined_format_args`)

## Wendao Test-Policy Follow-Up Slice 3

- continued the bounded `packages/rust/crates/xiuxian-wendao/` cleanup wave by
  staying inside the next two `link_graph` clusters reported by the shared
  gate
- externalized the inline test modules in:
  `src/link_graph/runtime_config/tests.rs`
  `src/link_graph/index/build/graphmem.rs`
  `src/link_graph/index/build/cache/storage.rs`
  `src/link_graph/stats_cache/valkey.rs`
  onto canonical `tests/unit/...` mounts
- converted the next path-only `link_graph` shim surfaces to direct canonical
  mounts:
  `src/link_graph/saliency/store/write/mod.rs`
  `src/link_graph/context_snapshot/mod.rs`
  `src/link_graph/addressing/mod.rs`
  `src/link_graph/index/ppr/kernel/mod.rs`
  `src/link_graph/index/search/plan/payload/quantum/mod.rs`
- collapsed the old `link_graph/addressing` prelude shim into the canonical
  `tests/unit/link_graph/addressing/mod.rs` file so the source module no longer
  needs an extra forwarding layer
- focused validation for this slice is:
  `direnv exec . cargo fmt --all`
  `direnv exec . cargo test -p xiuxian-wendao --lib link_graph::runtime_config::tests::test_agentic_runtime_resolves_override_values -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao --lib link_graph::index::build::graphmem::tests::redis_client_preserves_graphmem_error_context -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao --lib link_graph::index::build::cache::storage::tests::redis_client_preserves_index_cache_error_context -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao --lib link_graph::stats_cache::valkey::tests::redis_client_preserves_stats_cache_error_context -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao --lib link_graph::saliency::store::write::tests::higher_rank_neighbors_receive_lower_weight -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao --lib link_graph::context_snapshot::tests::snapshot_redis_client_preserves_snapshot_error_context -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao --lib link_graph::addressing::tests::test_address_parse_id -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao --lib link_graph::index::ppr::kernel::tests::run_kernel_iterations_conserves_probability_mass -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao --features julia --lib link_graph::index::search::plan::payload::quantum::tests::collect_plugin_rerank_trace_ids_deduplicates_non_empty_values -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao --lib xiuxian_test_policy_harness::enforce_crate_test_policy_harness -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao --test unit_test enforce_crate_test_policy_harness -- --exact`
- decisive outcome:
  the shared failure surface no longer includes
  `src/link_graph/runtime_config.rs`,
  `src/link_graph/index/build/graphmem.rs`,
  `src/link_graph/index/build/cache/storage.rs`,
  `src/link_graph/stats_cache/valkey.rs`,
  `src/link_graph/saliency/store/write/mod.rs`,
  `src/link_graph/context_snapshot/mod.rs`,
  `src/link_graph/addressing/mod.rs`,
  `src/link_graph/index/ppr/kernel/mod.rs`, or
  `src/link_graph/index/search/plan/payload/quantum/mod.rs`
- the next remaining `link_graph` debt is now concentrated in
  `src/link_graph/index/search/quantum_fusion/*`
  instead of the lower-level runtime, cache, saliency, or context modules

## Wendao Test-Policy Follow-Up Slice 4

- continued the bounded `packages/rust/crates/xiuxian-wendao/` cleanup wave by
  clearing the next `link_graph/index/search/quantum_fusion` cluster reported
  by the shared gate
- externalized the inline source tests in:
  `src/link_graph/index/search/quantum_fusion/vector_ignition.rs`
  `src/link_graph/index/search/quantum_fusion/openai_ignition.rs`
- converted the path-only shim in
  `src/link_graph/index/search/quantum_fusion/orchestrate/mod.rs`
  to a direct canonical mount and deleted the old
  `src/link_graph/index/search/quantum_fusion/orchestrate/tests.rs`
- focused validation for this slice is:
  `direnv exec . cargo fmt --all`
  `direnv exec . cargo test -p xiuxian-wendao --features julia --lib link_graph::index::search::quantum_fusion::vector_ignition::tests::build_plugin_rerank_request_batch_with_metadata_uses_anchor_ids_as_request_doc_ids -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao --features julia --lib link_graph::index::search::quantum_fusion::openai_ignition::tests::build_plugin_rerank_request_batch_rejects_missing_query_signal -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao --lib link_graph::index::search::quantum_fusion::orchestrate::tests::extract_saliency_scores_reads_expected_values -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao --lib xiuxian_test_policy_harness::enforce_crate_test_policy_harness -- --exact`
- decisive outcome:
  the shared failure surface no longer includes
  `src/link_graph/index/search/quantum_fusion/vector_ignition.rs`,
  `src/link_graph/index/search/quantum_fusion/openai_ignition.rs`, or
  `src/link_graph/index/search/quantum_fusion/orchestrate/mod.rs`
  so the remaining debt has moved out of `link_graph` and into the broader
  analyzer/search/gateway surfaces

## Wendao Test-Policy Follow-Up Slice 5

- continued the bounded `packages/rust/crates/xiuxian-wendao/` cleanup wave by
  staying inside the next analyzer cluster reported by the shared gate
- externalized the source-backed test modules in:
  `src/analyzers/cache/valkey/mod.rs`
  `src/analyzers/cache/mod.rs`
  `src/analyzers/config/mod.rs`
  onto canonical `tests/unit/analyzers/...` mounts
- kept the slice intentionally narrow: no service-layer analyzer suites, no
  search/gateway externalization, and no production behavior changes outside
  the `#[cfg(test)]` mount points
- focused validation for this slice is:
  `direnv exec . cargo fmt --all`
  `direnv exec . cargo test -p xiuxian-wendao --lib analyzers::cache::valkey::tests::runtime_resolution_prefers_toml_values_over_env -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao --lib analyzers::cache::tests::repository_search_query_cache_isolated_by_endpoint_and_filter -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao --lib analyzers::config::tests::load_repo_intelligence_config_reads_overlay_importing_base -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao --lib xiuxian_test_policy_harness::enforce_crate_test_policy_harness -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao --test unit_test enforce_crate_test_policy_harness -- --exact`
- decisive outcome:
  both shared harness entrypoints still fail only on real remaining debt, but
  the live failure surface no longer includes
  `src/analyzers/cache/valkey/mod.rs`,
  `src/analyzers/cache/mod.rs`, or
  `src/analyzers/config/mod.rs`
  so the next analyzer debt is now concentrated in the service/query/projection
  branches instead of the cache/config mounts

## Wendao Test-Policy Follow-Up Slice 6

- continued the bounded `packages/rust/crates/xiuxian-wendao/` cleanup wave by
  staying inside the next analyzer service/search cluster reported by the
  shared gate
- externalized the source-backed test modules in:
  `src/analyzers/service/search/mod.rs`
  `src/analyzers/service/search/ranking/mod.rs`
  `src/analyzers/service/search/contracts.rs`
  onto canonical `tests/unit/analyzers/service/search/...` mounts
- kept the slice narrow: no analyzer service root/module migration, no gateway
  or search-plane changes, and no production behavior updates outside the
  `#[cfg(test)]` mount points
- focused validation for this slice is:
  `direnv exec . rustfmt --edition 2024 packages/rust/crates/xiuxian-wendao/src/analyzers/service/search/mod.rs packages/rust/crates/xiuxian-wendao/src/analyzers/service/search/ranking/mod.rs packages/rust/crates/xiuxian-wendao/src/analyzers/service/search/contracts.rs packages/rust/crates/xiuxian-wendao/tests/unit/analyzers/service/search/mod.rs packages/rust/crates/xiuxian-wendao/tests/unit/analyzers/service/search/ranking/mod.rs packages/rust/crates/xiuxian-wendao/tests/unit/analyzers/service/search/contracts.rs`
  `direnv exec . cargo test -p xiuxian-wendao --lib analyzers::service::search::tests::module_search_uses_shared_tantivy_fuzzy_index_for_typos -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao --lib analyzers::service::search::ranking::tests::ranked_module_matches_empty_query_keeps_input_order -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao --lib analyzers::service::search::contracts::tests::canonical_import_query_text_preserves_package_and_module_identity -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao --lib xiuxian_test_policy_harness::enforce_crate_test_policy_harness -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao --test unit_test enforce_crate_test_policy_harness -- --exact`
- decisive outcome:
  both shared harness entrypoints still fail only on real remaining debt, but
  the live failure surface no longer includes
  `src/analyzers/service/search/mod.rs`,
  `src/analyzers/service/search/ranking/mod.rs`, or
  `src/analyzers/service/search/contracts.rs`
  so the next analyzer debt is now concentrated in the service root/helpers and
  projection/query planner branches
- repository-wide `cargo fmt --all` is currently blocked outside this slice by
  the existing missing test mount at
  `packages/rust/crates/xiuxian-qianji/src/markdown/qianhuan.rs`

## Wendao Test-Policy Follow-Up Slice 7

- continued the bounded `packages/rust/crates/xiuxian-wendao/` cleanup wave by
  staying inside the next analyzer service-root/planner cluster reported by the
  shared gate
- externalized the source-backed test modules in:
  `src/analyzers/service/mod.rs`
  `src/analyzers/service/helpers/mod.rs`
  `src/analyzers/service/projection/planner/mod.rs`
  `src/analyzers/query/docs/planner/mod.rs`
  onto canonical `tests/unit/analyzers/...` mounts
- kept the slice narrow: no `analyzers/projection/*` migration, no gateway
  suites, and no runtime behavior change outside the `#[cfg(test)]` mount
  points
- focused validation for this slice is:
  `direnv exec . rustfmt --edition 2024 packages/rust/crates/xiuxian-wendao/src/analyzers/service/mod.rs packages/rust/crates/xiuxian-wendao/src/analyzers/service/helpers/mod.rs packages/rust/crates/xiuxian-wendao/src/analyzers/service/projection/planner/mod.rs packages/rust/crates/xiuxian-wendao/src/analyzers/query/docs/planner/mod.rs packages/rust/crates/xiuxian-wendao/tests/unit/analyzers/service/mod.rs packages/rust/crates/xiuxian-wendao/tests/unit/analyzers/service/helpers/mod.rs packages/rust/crates/xiuxian-wendao/tests/unit/analyzers/service/helpers/contracts.rs packages/rust/crates/xiuxian-wendao/tests/unit/analyzers/service/helpers/fixtures.rs packages/rust/crates/xiuxian-wendao/tests/unit/analyzers/service/helpers/projection.rs packages/rust/crates/xiuxian-wendao/tests/unit/analyzers/service/helpers/ranking.rs packages/rust/crates/xiuxian-wendao/tests/unit/analyzers/service/helpers/relations.rs packages/rust/crates/xiuxian-wendao/tests/unit/analyzers/service/helpers/scope.rs packages/rust/crates/xiuxian-wendao/tests/unit/analyzers/service/projection/planner/mod.rs packages/rust/crates/xiuxian-wendao/tests/unit/analyzers/query/docs/planner/mod.rs`
  `direnv exec . cargo test -p xiuxian-wendao --lib analyzers::service::tests::merge_repository_record_prefers_overlay_metadata -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao --lib analyzers::service::helpers::tests::contracts::relation_labels_and_uris_remain_stable -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao --lib analyzers::service::projection::planner::tests::normalize_planner_search_text_trims_and_lowercases -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao --lib analyzers::query::docs::planner::tests::workset_quota_hint_roundtrip -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao --lib xiuxian_test_policy_harness::enforce_crate_test_policy_harness -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao --test unit_test enforce_crate_test_policy_harness -- --exact`
- decisive outcome:
  both shared harness entrypoints still fail only on real remaining debt, but
  the live failure surface no longer includes
  `src/analyzers/service/mod.rs`,
  `src/analyzers/service/helpers/mod.rs`,
  `src/analyzers/service/projection/planner/mod.rs`, or
  `src/analyzers/query/docs/planner/mod.rs`
  so the next analyzer debt is now pushed into the remaining standalone inline
  suites and the separate `analyzers/projection/*` branch
- repository-wide `cargo fmt --all` remains blocked outside this slice by the
  existing missing test mount at
  `packages/rust/crates/xiuxian-qianji/src/markdown/qianhuan.rs`

## WendaoSearch Mounted OMParser Live-Child Contract

- continued the active Julia search/code-parser integration lane under
  `.data/WendaoSearch.jl` and `.data/WendaoCodeParser.jl`
- confirmed that the earlier upstream `OMParser.jl` PR remains useful for the
  build/bootstrap lane, release assets, and CI coverage, but does not by
  itself solve the mounted live-child startup contract
- locked the current workspace to
  `https://github.com/tao3k/OMParser.jl@853c28d294339a611eaf60b08841ffb55b127db1`
  so local `WendaoCodeParser.jl` and `WendaoSearch.jl` no longer float on
  `OpenModelica/OMParser.jl@master` while the bootstrap fixes wait for
  upstream consumption
- documented the actual runtime root cause in package docs:
  the current `OMParser.jl` native bridge still resolves `Absyn`,
  `ImmutableList`, and `MetaModelica` from `Main`, so mounted Modelica routes
  now rely on the downstream `WendaoCodeParser.jl` runtime shim before first
  parse
- synchronized
  `.data/WendaoCodeParser.jl/README.md`,
  `.data/WendaoSearch.jl/README.md`,
  `.data/WendaoSearch.jl/docs/testing/README.md`,
  and this GTD entry so the upstream/downstream boundary is recorded in one
  place
- focused validation for this slice is:
  `direnv exec . julia --project=.data/WendaoCodeParser.jl --color=yes .data/WendaoCodeParser.jl/test/runtests.jl`
  `direnv exec . julia --project=.data/WendaoSearch.jl --color=yes -e 'using Pkg; Pkg.test()'`
  `git -C .data/WendaoSearch.jl diff --check`
  `git -C .data/WendaoCodeParser.jl diff --check`

## Qianji Qianhuan Shared Markdown Template Catalog

- continued the active Flowhub graph-contract blueprint at
  `.cache/agent/blueprints/qianji_flowhub_planning_source_model.md` and
  recorded the bounded ExecPlan at
  `.cache/agent/execplans/qianji-qianhuan-shared-markdown-template-catalog.md`
- deduplicated the `xiuxian-qianji` control-plane markdown renderer bootstrap
  path by introducing one shared embedded `qianhuan` template catalog under
  `packages/rust/crates/xiuxian-qianji/src/markdown/qianhuan.rs`
- rewired
  `packages/rust/crates/xiuxian-qianji/src/markdown/show.rs`,
  `packages/rust/crates/xiuxian-qianji/src/markdown/check.rs`,
  `packages/rust/crates/xiuxian-qianji/src/flowhub/show.rs`, and
  `packages/rust/crates/xiuxian-qianji/src/flowhub/scenario/show.rs`
  through that shared helper while keeping the existing `show` / `check`
  markdown output contract unchanged
- added focused unit coverage for the helper in
  `packages/rust/crates/xiuxian-qianji/tests/unit/markdown/qianhuan.rs`
  and kept the existing Flowhub and scenario output regressions green
- synchronized
  `packages/rust/crates/xiuxian-qianji/README.md` and
  `packages/rust/crates/xiuxian-qianji/docs/00_mocs/20260315141000-qianji-orchestrator-moc.md`
  so package docs now record the shared qianhuan-catalog ownership
- focused validation for this slice is:
  `direnv exec . cargo fmt --all`
  `direnv exec . cargo test -p xiuxian-qianji markdown::qianhuan::tests::render_text_uses_embedded_template_catalog --lib -- --exact`
  `direnv exec . cargo test -p xiuxian-qianji markdown::show::tests::render_show_surface_uses_shared_shape --lib -- --exact`
  `direnv exec . cargo test -p xiuxian-qianji markdown::check::tests::render_validation_failed_uses_shared_shape --lib -- --exact`
  `direnv exec . cargo test -p xiuxian-qianji flowhub::show::tests::flowhub_root_module_section_lines_render_with_qianhuan_template --lib -- --exact`
  `direnv exec . cargo test -p xiuxian-qianji flowhub::scenario::show::tests::scenario_flowchart_section_lines_render_with_qianhuan_template --lib -- --exact`
  `direnv exec . cargo check -p xiuxian-qianji --tests`
  `direnv exec . cargo run -p xiuxian-qianji --features llm --bin qianji -- show --dir qianji-flowhub`
  `direnv exec . cargo run -p xiuxian-qianji --features llm --bin qianji -- show --dir packages/rust/crates/xiuxian-qianji/tests/fixtures/flowhub/coding_rust_blueprint_plan`
  `direnv exec . git diff --check -- packages/rust/crates/xiuxian-qianji/src/markdown/mod.rs packages/rust/crates/xiuxian-qianji/src/markdown/qianhuan.rs packages/rust/crates/xiuxian-qianji/src/markdown/show.rs packages/rust/crates/xiuxian-qianji/src/markdown/check.rs packages/rust/crates/xiuxian-qianji/src/flowhub/show.rs packages/rust/crates/xiuxian-qianji/src/flowhub/scenario/show.rs packages/rust/crates/xiuxian-qianji/tests/unit/markdown/qianhuan.rs packages/rust/crates/xiuxian-qianji/README.md packages/rust/crates/xiuxian-qianji/docs/00_mocs/20260315141000-qianji-orchestrator-moc.md docs/GTD/DAILY_2026_04_08.md`

## Qianhuan Embedded Template Catalog Promotion

- continued the same Flowhub graph-contract blueprint at
  `.cache/agent/blueprints/qianji_flowhub_planning_source_model.md` and
  recorded the bounded ExecPlan at
  `.cache/agent/execplans/qianhuan-embedded-template-catalog-promotion.md`
- promoted the lazy embedded-template catalog helper out of
  `xiuxian-qianji` and into
  `packages/rust/crates/xiuxian-qianhuan/src/manifestation/catalog.rs` as
  the public `EmbeddedManifestationTemplateCatalog`
- exported that helper from `xiuxian-qianhuan/src/lib.rs`, removed the local
  `packages/rust/crates/xiuxian-qianji/src/markdown/qianhuan.rs` wrapper, and
  rewired the `qianji` show/check/Flowhub/scenario control-plane renderers to
  consume the shared `qianhuan` surface directly
- synchronized
  `packages/rust/crates/xiuxian-qianhuan/README.md`,
  `packages/rust/crates/xiuxian-qianji/README.md`, and
  `packages/rust/crates/xiuxian-qianji/docs/00_mocs/20260315141000-qianji-orchestrator-moc.md`
  so package docs now record the helper at the owning crate boundary
- focused validation for this slice is:
  `direnv exec . cargo fmt --all`
  `direnv exec . cargo test -p xiuxian-qianhuan embedded_template_catalog_renders_text --test test_embedded_template_catalog -- --exact`
  `direnv exec . cargo test -p xiuxian-qianhuan embedded_template_catalog_renders_lines --test test_embedded_template_catalog -- --exact`
  `direnv exec . cargo test -p xiuxian-qianji markdown::show::tests::render_show_surface_uses_shared_shape --lib -- --exact`
  `direnv exec . cargo test -p xiuxian-qianji flowhub::show::tests::flowhub_root_module_section_lines_render_with_qianhuan_template --lib -- --exact`
  `direnv exec . cargo test -p xiuxian-qianji flowhub::scenario::show::tests::scenario_flowchart_section_lines_render_with_qianhuan_template --lib -- --exact`
  `direnv exec . cargo check -p xiuxian-qianji --tests`
  `direnv exec . cargo run -p xiuxian-qianji --features llm --bin qianji -- show --dir qianji-flowhub`
  `direnv exec . cargo run -p xiuxian-qianji --features llm --bin qianji -- check --dir qianji-flowhub`
  `direnv exec . git diff --check -- packages/rust/crates/xiuxian-qianhuan/src/manifestation/catalog.rs packages/rust/crates/xiuxian-qianhuan/src/manifestation/mod.rs packages/rust/crates/xiuxian-qianhuan/src/lib.rs packages/rust/crates/xiuxian-qianhuan/tests/test_embedded_template_catalog.rs packages/rust/crates/xiuxian-qianhuan/README.md packages/rust/crates/xiuxian-qianji/src/markdown/mod.rs packages/rust/crates/xiuxian-qianji/src/markdown/show.rs packages/rust/crates/xiuxian-qianji/src/markdown/check.rs packages/rust/crates/xiuxian-qianji/src/flowhub/show.rs packages/rust/crates/xiuxian-qianji/src/flowhub/scenario/show.rs packages/rust/crates/xiuxian-qianji/README.md packages/rust/crates/xiuxian-qianji/docs/00_mocs/20260315141000-qianji-orchestrator-moc.md docs/GTD/DAILY_2026_04_08.md`

## Qianji Control-Plane Template Files

- continued the same Flowhub graph-contract blueprint at
  `.cache/agent/blueprints/qianji_flowhub_planning_source_model.md` and
  recorded the bounded ExecPlan at
  `.cache/agent/execplans/qianji-control-plane-template-files.md`
- moved the qianji control-plane `.md.j2` text out of Rust const bodies and
  into checked-in resource files under
  `packages/rust/crates/xiuxian-qianji/resources/templates/control_plane/`
- rewired the shared markdown surface and Flowhub show/scenario renderers to
  `include_str!` those template files while keeping the existing payload shape
  and live output contract unchanged
- preserved the no-trailing-newline output contract by joining rendered lines
  back in the qianji callers instead of changing the shared qianhuan catalog
  behavior
- synchronized
  `packages/rust/crates/xiuxian-qianji/README.md` and
  `packages/rust/crates/xiuxian-qianji/docs/00_mocs/20260315141000-qianji-orchestrator-moc.md`
  so package docs now point to the checked-in template-file surface
- focused validation for this slice is:
  `direnv exec . cargo fmt --all`
  `direnv exec . cargo test -p xiuxian-qianji markdown::show::tests::render_show_surface_uses_shared_shape --lib -- --exact`
  `direnv exec . cargo test -p xiuxian-qianji markdown::check::tests::render_validation_failed_uses_shared_shape --lib -- --exact`
  `direnv exec . cargo test -p xiuxian-qianji flowhub::show::tests::flowhub_root_module_section_lines_render_with_qianhuan_template --lib -- --exact`
  `direnv exec . cargo test -p xiuxian-qianji flowhub::scenario::show::tests::scenario_flowchart_section_lines_render_with_qianhuan_template --lib -- --exact`
  `direnv exec . cargo check -p xiuxian-qianji --tests`
  `direnv exec . cargo run -p xiuxian-qianji --features llm --bin qianji -- show --dir qianji-flowhub`
  `direnv exec . cargo run -p xiuxian-qianji --features llm --bin qianji -- check --dir qianji-flowhub`
  `direnv exec . git diff --check -- packages/rust/crates/xiuxian-qianji/resources/templates/control_plane packages/rust/crates/xiuxian-qianji/src/markdown/show.rs packages/rust/crates/xiuxian-qianji/src/markdown/check.rs packages/rust/crates/xiuxian-qianji/src/flowhub/show.rs packages/rust/crates/xiuxian-qianji/src/flowhub/scenario/show.rs packages/rust/crates/xiuxian-qianji/README.md packages/rust/crates/xiuxian-qianji/docs/00_mocs/20260315141000-qianji-orchestrator-moc.md docs/GTD/DAILY_2026_04_08.md`

## Wendao Test-Policy Follow-Up Slice 8

- continued the active testing-gate remediation lane governed by
  `.cache/agent/execplans/testing-gate-auto-remediation-wave1.md`
- externalized the remaining standalone analyzer inline suites in
  `packages/rust/crates/xiuxian-wendao/src/analyzers/saliency.rs` and
  `packages/rust/crates/xiuxian-wendao/src/analyzers/skeptic.rs` into the
  canonical `packages/rust/crates/xiuxian-wendao/tests/unit/analyzers/`
  surface
- updated both source files to canonical `#[cfg(test)] #[path = "..."]`
  mounts, removing the last two standalone analyzer test bodies from `src/`
- the shared `xiuxian-testing-gate` proof now no longer reports those two
  analyzer files; the next analyzer debt has moved forward to the
  `analyzers/projection/*` branch
- focused validation for this slice is:
  `direnv exec . rustfmt --edition 2024 packages/rust/crates/xiuxian-wendao/src/analyzers/saliency.rs packages/rust/crates/xiuxian-wendao/src/analyzers/skeptic.rs packages/rust/crates/xiuxian-wendao/tests/unit/analyzers/saliency.rs packages/rust/crates/xiuxian-wendao/tests/unit/analyzers/skeptic.rs`
  `direnv exec . cargo test -p xiuxian-wendao --lib analyzers::saliency::tests::test_compute_repository_saliency_basic -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao --lib analyzers::skeptic::tests::test_audit_symbols_verified -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao --lib analyzers::skeptic::tests::test_audit_symbols_unverified -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao --lib xiuxian_test_policy_harness::enforce_crate_test_policy_harness -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao --test unit_test enforce_crate_test_policy_harness -- --exact`
  `direnv exec . cargo clippy -p xiuxian-wendao --lib --tests -- -D warnings`
  `direnv exec . git diff --check -- packages/rust/crates/xiuxian-wendao packages/rust/crates/xiuxian-testing/docs/01_core/101_contract_testing_kernel.md docs/GTD/DAILY_2026_04_08.md .cache/agent/execplans/testing-gate-auto-remediation-wave1.md`

## Wendao Test-Policy Follow-Up Slice 9

- continued the same active remediation lane and cleared the next
  `analyzers/projection` cluster exposed by the shared gate
- externalized the source-side shims in
  `packages/rust/crates/xiuxian-wendao/src/analyzers/projection/search/mod.rs`
  and
  `packages/rust/crates/xiuxian-wendao/src/analyzers/projection/builder/mod.rs`
  into canonical `tests/unit/analyzers/projection/.../mod.rs` targets, then
  deleted the old `src/**/tests.rs` files
- the shared `xiuxian-testing-gate` proof now no longer reports those two
  projection modules; the next remaining debt has moved out of analyzer
  projection and starts at parser / memory-julia surfaces
- focused validation for this slice is:
  `direnv exec . rustfmt --edition 2024 packages/rust/crates/xiuxian-wendao/src/analyzers/projection/search/mod.rs packages/rust/crates/xiuxian-wendao/src/analyzers/projection/builder/mod.rs packages/rust/crates/xiuxian-wendao/tests/unit/analyzers/projection/search/mod.rs packages/rust/crates/xiuxian-wendao/tests/unit/analyzers/projection/builder/mod.rs`
  `direnv exec . cargo test -p xiuxian-wendao --lib analyzers::projection::search::tests::projected_page_search_uses_shared_tantivy_fuzzy_index_for_typos -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao --lib analyzers::projection::builder::tests::doc_projection_kind_honors_reference_format_without_symbol_targets -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao --lib analyzers::projection::builder::tests::doc_projection_kind_upgrades_explanation_docs_when_symbol_anchored -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao --lib xiuxian_test_policy_harness::enforce_crate_test_policy_harness -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao --test unit_test enforce_crate_test_policy_harness -- --exact`
  `direnv exec . cargo clippy -p xiuxian-wendao --lib --tests -- -D warnings`
  `direnv exec . git diff --check -- packages/rust/crates/xiuxian-wendao packages/rust/crates/xiuxian-testing/docs/01_core/101_contract_testing_kernel.md docs/GTD/DAILY_2026_04_08.md .cache/agent/execplans/testing-gate-auto-remediation-wave1.md`

## Wendao Test-Policy Follow-Up Slice 10

- continued the same remediation lane and cleared the next parser +
  `memory/julia` cluster exposed by the shared gate
- externalized the inline parser suite in
  `packages/rust/crates/xiuxian-wendao/src/parsers/markdown/links/normalize.rs`
  into the canonical
  `packages/rust/crates/xiuxian-wendao/tests/unit/parsers/markdown/links/normalize.rs`
  target
- rewired
  `packages/rust/crates/xiuxian-wendao/src/memory/julia/mod.rs` to one
  canonical `tests/unit/memory/julia/mod.rs` harness and moved the old
  source-side `test_support.rs` / `tests.rs` scaffolding into
  `tests/unit/memory/julia/support.rs`
- the shared `xiuxian-testing-gate` proof now no longer reports either the
  parser normalize suite or the `memory/julia` test surfaces; the next debt
  has advanced to `src/bin/wendao/execute/*` and other non-analyzer, non-memory
  owners
- focused validation for this slice is:
  `direnv exec . rustfmt --edition 2024 packages/rust/crates/xiuxian-wendao/src/parsers/markdown/links/normalize.rs packages/rust/crates/xiuxian-wendao/src/memory/julia/mod.rs packages/rust/crates/xiuxian-wendao/tests/unit/parsers/markdown/links/normalize.rs packages/rust/crates/xiuxian-wendao/tests/unit/memory/julia/mod.rs packages/rust/crates/xiuxian-wendao/tests/unit/memory/julia/support.rs`
  `direnv exec . cargo test -p xiuxian-wendao --lib parsers::markdown::links::normalize::tests::`
  `direnv exec . cargo test -p xiuxian-wendao --lib memory::julia::tests::`
  `direnv exec . cargo test -p xiuxian-wendao --lib xiuxian_test_policy_harness::enforce_crate_test_policy_harness -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao --test unit_test enforce_crate_test_policy_harness -- --exact`
  `direnv exec . cargo clippy -p xiuxian-wendao --lib --tests -- -D warnings`
  `direnv exec . cargo fmt --all`
  `direnv exec . git diff --check -- packages/rust/crates/xiuxian-wendao packages/rust/crates/xiuxian-testing/docs/01_core/101_contract_testing_kernel.md docs/GTD/DAILY_2026_04_08.md .cache/agent/execplans/testing-gate-auto-remediation-wave1.md`

## Wendao Test-Policy Follow-Up Slice 11

- continued the same remediation lane and cleared the next
  `src/bin/wendao/execute/*` cluster exposed by the shared gate
- externalized the inline suites in
  `packages/rust/crates/xiuxian-wendao/src/bin/wendao/execute/repo.rs`,
  `packages/rust/crates/xiuxian-wendao/src/bin/wendao/execute/gateway/config.rs`,
  and
  `packages/rust/crates/xiuxian-wendao/src/bin/wendao/execute/gateway/command.rs`
  into direct canonical `tests/unit/bin/wendao/execute/...` mounts
- the shared `xiuxian-testing-gate` proof now no longer reports those three
  bin execution owners; the active failure surface has advanced into
  `ingress`, `entity`, `enhancer`, and the larger `repo_index` / `search`
  clusters
- focused validation for this slice is:
  `direnv exec . rustfmt --edition 2024 packages/rust/crates/xiuxian-wendao/src/bin/wendao/execute/repo.rs packages/rust/crates/xiuxian-wendao/src/bin/wendao/execute/gateway/config.rs packages/rust/crates/xiuxian-wendao/src/bin/wendao/execute/gateway/command.rs packages/rust/crates/xiuxian-wendao/tests/unit/bin/wendao/execute/repo.rs packages/rust/crates/xiuxian-wendao/tests/unit/bin/wendao/execute/gateway/config.rs packages/rust/crates/xiuxian-wendao/tests/unit/bin/wendao/execute/gateway/command.rs`
  `direnv exec . cargo test -p xiuxian-wendao --bin wendao execute::repo::tests::repo_overview_query_preserves_repo_id -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao --bin wendao execute::gateway::config::tests::resolve_config_path_prefers_studio_overlay_when_present -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao --bin wendao execute::gateway::command::tests::gateway_runtime_knobs_prefer_toml_over_env -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao --lib xiuxian_test_policy_harness::enforce_crate_test_policy_harness -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao --test unit_test enforce_crate_test_policy_harness -- --exact`
  `direnv exec . cargo clippy -p xiuxian-wendao --lib --tests -- -D warnings`
  `direnv exec . cargo fmt --all`
  `direnv exec . git diff --check -- packages/rust/crates/xiuxian-wendao packages/rust/crates/xiuxian-testing/docs/01_core/101_contract_testing_kernel.md docs/GTD/DAILY_2026_04_08.md .cache/agent/execplans/testing-gate-auto-remediation-wave1.md`

## Wendao Test-Policy Follow-Up Slice 12

- continued the same remediation lane by removing the last unrelated
  `bounded_work_markdown` clippy blockers that had been masking closure on the
  active `xiuxian-wendao --lib --tests` gate
- fixed the doc hygiene in
  `packages/rust/crates/xiuxian-wendao/src/search/queries/sql/bounded_work_markdown/register.rs`
  and converted the
  `packages/rust/crates/xiuxian-wendao/tests/unit/search/queries/sql/bounded_work_markdown.rs`
  suite from `expect()`-driven fixtures to `Result`-returning tests with
  shared fixture setup
- decisive outcome:
  `direnv exec . cargo clippy -p xiuxian-wendao --lib --tests -- -D warnings`
  now passes, so future slices in this lane no longer need to report the old
  `bounded_work_markdown` debt as an unrelated blocker
- focused validation for this slice is:
  `direnv exec . rustfmt --edition 2024 packages/rust/crates/xiuxian-wendao/src/search/queries/sql/bounded_work_markdown/register.rs packages/rust/crates/xiuxian-wendao/tests/unit/search/queries/sql/bounded_work_markdown.rs`
  `direnv exec . cargo test -p xiuxian-wendao --lib search::queries::sql::bounded_work_markdown::tests::registers_bounded_work_markdown_rows_into_sql_surface -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao --lib search::queries::sql::bounded_work_markdown::tests::bootstraps_bounded_work_markdown_query_engine -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao --lib search::queries::sql::bounded_work_markdown::tests::queries_bounded_work_markdown_payload -- --exact`
  `direnv exec . cargo clippy -p xiuxian-wendao --lib --tests -- -D warnings`
  `direnv exec . cargo fmt --all`
  `direnv exec . git diff --check -- packages/rust/crates/xiuxian-wendao packages/rust/crates/xiuxian-testing/docs/01_core/101_contract_testing_kernel.md docs/GTD/DAILY_2026_04_08.md .cache/agent/execplans/testing-gate-auto-remediation-wave1.md`

## Wendao Test-Policy Follow-Up Slice 13

- continued the same remediation lane and cleared the next shared-gate top
  cluster:
  `ingress/spider`,
  `entity`,
  `enhancer/mod.rs`,
  `enhancer/markdown_config/mod.rs`,
  `enhancer/resource_registry/mod.rs`
- rewired those source owners to canonical `tests/unit/.../mod.rs` mounts,
  moved the old flat `tests/unit/entity.rs` and `tests/unit/ingress_spider.rs`
  into the gate-expected canonical paths, and migrated the enhancer
  `src/**/tests.rs` files out of `src/`
- the shared `xiuxian-testing-gate` proof now no longer reports those five
  owners; the active failure surface has advanced to
  `dependency_indexer` and the larger `repo_index` / `search` clusters
- focused validation for this slice is:
  `direnv exec . rustfmt --edition 2024 packages/rust/crates/xiuxian-wendao/src/ingress/spider/mod.rs packages/rust/crates/xiuxian-wendao/src/entity/mod.rs packages/rust/crates/xiuxian-wendao/src/enhancer/mod.rs packages/rust/crates/xiuxian-wendao/src/enhancer/markdown_config/mod.rs packages/rust/crates/xiuxian-wendao/src/enhancer/resource_registry/mod.rs packages/rust/crates/xiuxian-wendao/tests/unit/ingress/spider/mod.rs packages/rust/crates/xiuxian-wendao/tests/unit/entity/mod.rs packages/rust/crates/xiuxian-wendao/tests/unit/enhancer/mod.rs packages/rust/crates/xiuxian-wendao/tests/unit/enhancer/markdown_config/mod.rs packages/rust/crates/xiuxian-wendao/tests/unit/enhancer/resource_registry/mod.rs`
  `direnv exec . cargo test -p xiuxian-wendao --lib ingress::spider::tests::build_document_description_uses_title_and_first_content_line -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao --lib entity::tests::test_relation_type_display -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao --lib enhancer::tests::test_enhance_note_full -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao --lib enhancer::markdown_config::tests::markdown_config_blocks_are_indexed_by_id -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao --lib enhancer::resource_registry::tests::embedded_registry_builds_from_fixture -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao --lib ingress::spider::tests::spider_bridge_for_knowledge_graph_persists_document_entity -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao --lib xiuxian_test_policy_harness::enforce_crate_test_policy_harness -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao --test unit_test enforce_crate_test_policy_harness -- --exact`
  `direnv exec . cargo clippy -p xiuxian-wendao --lib --tests -- -D warnings`
  `direnv exec . cargo fmt --all`
  `direnv exec . git diff --check -- packages/rust/crates/xiuxian-wendao packages/rust/crates/xiuxian-testing/docs/01_core/101_contract_testing_kernel.md docs/GTD/DAILY_2026_04_08.md .cache/agent/execplans/testing-gate-auto-remediation-wave1.md`

## Wendao Test-Policy Follow-Up Slice 14

- continued the same remediation lane by clearing the next shared-gate owner:
  `src/dependency_indexer/symbols/mod.rs`
- rewired that source owner to a direct canonical
  `tests/unit/dependency_indexer/symbols/mod.rs` mount, merged the old
  `src/dependency_indexer/symbols/tests.rs` inline suite and the legacy flat
  `tests/unit/dependency_indexer_symbols.rs` helper into that canonical file,
  and removed the now-redundant source and flat test surfaces
- decisive outcome:
  the live `xiuxian-testing-gate` proof no longer reports
  `src/dependency_indexer/symbols/mod.rs`, so the active top debt has advanced
  into the contiguous `repo_index/state/*` cluster
- focused validation for this slice is:
  `direnv exec . rustfmt --edition 2024 packages/rust/crates/xiuxian-wendao/src/dependency_indexer/symbols/mod.rs packages/rust/crates/xiuxian-wendao/tests/unit/dependency_indexer/symbols/mod.rs`
  `direnv exec . cargo test -p xiuxian-wendao --lib dependency_indexer::symbols::tests::test_extract_rust_symbols -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao --lib dependency_indexer::symbols::tests::test_symbol_index_search -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao --lib dependency_indexer::symbols::tests::`
  `direnv exec . cargo test -p xiuxian-wendao --lib xiuxian_test_policy_harness::enforce_crate_test_policy_harness -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao --test unit_test enforce_crate_test_policy_harness -- --exact`
  `direnv exec . cargo clippy -p xiuxian-wendao --lib --tests -- -D warnings`
  `direnv exec . cargo fmt --all`
  `direnv exec . git diff --check -- packages/rust/crates/xiuxian-wendao packages/rust/crates/xiuxian-testing/docs/01_core/101_contract_testing_kernel.md docs/GTD/DAILY_2026_04_08.md .cache/agent/execplans/testing-gate-auto-remediation-wave1.md`

## Wendao Test-Policy Follow-Up Slice 15

- switched from one-owner follow-ups to a milestone-style bounded cluster and
  cleared the full `repo_index/state` gate front in one pass:
  `src/repo_index/state/mod.rs`
  `src/repo_index/state/tests/*`
  `src/repo_index/state/coordinator/{hydration,lifecycle}.rs`
  `src/repo_index/state/coordinator/runtime/{repository,scheduler}.rs`
  `src/repo_index/state/task/{adaptive,machine,policy,types}.rs`
- moved the whole source-resident `src/repo_index/state/tests/` subtree into
  canonical `tests/unit/repo_index/state/`, rewired every owner to direct
  `#[cfg(test)] #[path = "..."]` mounts, and absorbed the inline
  `coordinator/*` + `task/*` suites into the same canonical tree
- while validating the cluster, fixed two stale `task/adaptive` assertions so
  the tests now match the current EMA-based io-pressure controller semantics
- decisive outcome:
  the live `xiuxian-testing-gate` failure surface no longer reports any
  `repo_index/state` owner; the next top debt has advanced directly to the
  `graph`, `storage`, and `search/*` clusters
- milestone validation for this slice is:
  `direnv exec . cargo fmt --all`
  `direnv exec . cargo test -p xiuxian-wendao --lib repo_index::state::`
  `direnv exec . cargo test -p xiuxian-wendao --lib xiuxian_test_policy_harness::enforce_crate_test_policy_harness -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao --test unit_test enforce_crate_test_policy_harness -- --exact`
  `direnv exec . cargo clippy -p xiuxian-wendao --lib --tests -- -D warnings`
  `direnv exec . git diff --check -- packages/rust/crates/xiuxian-wendao packages/rust/crates/xiuxian-testing/docs/01_core/101_contract_testing_kernel.md docs/GTD/DAILY_2026_04_08.md .cache/agent/execplans/testing-gate-auto-remediation-wave1.md`

## Wendao Test-Policy Follow-Up Slice 16

- continued the same testing-gate remediation lane in
  `packages/rust/crates/xiuxian-wendao` and cleared the full bounded
  `graph + storage + search/cache` front in one milestone pass:
  `src/graph/valkey_persistence.rs`
  `src/storage/crud.rs`
  `src/search/cache/mod.rs`
  `src/search/cache/config.rs`
  `src/search/cache/runtime.rs`
  `src/search/cache/writes.rs`
- moved the old source-resident `src/search/cache/tests.rs` hub into canonical
  `tests/unit/search/cache/mod.rs`, rewired `src/search/cache/mod.rs` to mount
  that tree directly, and externalized the inline `config`, `runtime`, and
  `writes` suites into the same canonical `tests/unit/search/cache/` surface
- rewired `src/graph/valkey_persistence.rs` and `src/storage/crud.rs` to
  direct `#[cfg(test)] #[path = "..."]` mounts, and kept the resulting
  validation bounded to their existing config-resolution and Valkey-runtime
  semantics
- decisive outcome:
  the live `xiuxian-testing-gate` failure surface no longer reports any
  `graph`, `storage`, or `search/cache` owner; the next top debt now starts
  directly inside the broader `search/*` front, beginning at
  `search/reference_occurrence/*`
- milestone validation for this slice is:
  `direnv exec . cargo test -p xiuxian-wendao --lib graph::valkey_persistence::tests::`
  `direnv exec . cargo test -p xiuxian-wendao --lib storage::crud::tests::`
  `direnv exec . cargo test -p xiuxian-wendao --lib search::cache::`
  `direnv exec . cargo test -p xiuxian-wendao --lib xiuxian_test_policy_harness::enforce_crate_test_policy_harness -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao --test unit_test enforce_crate_test_policy_harness -- --exact`
  `direnv exec . cargo clippy -p xiuxian-wendao --lib --tests -- -D warnings`
  `direnv exec . cargo fmt --all`
  `direnv exec . git diff --check -- packages/rust/crates/xiuxian-wendao packages/rust/crates/xiuxian-testing/docs/01_core/101_contract_testing_kernel.md docs/GTD/DAILY_2026_04_08.md .cache/agent/execplans/testing-gate-auto-remediation-wave1.md`

## Wendao Test-Policy Follow-Up Slice 17

- continued the same testing-gate remediation lane in
  `packages/rust/crates/xiuxian-wendao` and cleared one milestone-sized
  bounded `search` corpus cluster in one pass:
  `src/search/reference_occurrence/{build,query}/mod.rs`
  `src/search/attachment/{build,query}/mod.rs`
  `src/search/local_symbol/{build,query}/mod.rs`
  `src/search/repo_content_chunk/{build,query}/mod.rs`
  `src/search/repo_entity/build/mod.rs`
  `src/search/repo_entity/query/mod.rs`
  `src/search/repo_entity/query/hydrate/mod.rs`
  `src/search/repo_entity/schema/mod.rs`
  `src/search/knowledge_section/{build,query}/mod.rs`
- moved the old source-resident `tests.rs` and `tests/` subtrees for those
  six corpus families into canonical `tests/unit/search/...` trees, then
  rewired every owner to direct `#[cfg(test)] #[path = "..."]` mounts
- kept the slice bounded to test-ownership cleanup; no production behavior was
  widened, and the focused validation stayed on the existing corpus build/query
  contracts
- decisive outcome:
  the live `xiuxian-testing-gate` failure surface no longer reports any owner
  from `reference_occurrence`, `attachment`, `local_symbol`,
  `repo_content_chunk`, `repo_entity`, or `knowledge_section`; the next top
  debt now starts at the remaining search-platform core:
  `repo_search`, `coordinator`, `manifest`, `queries`, `tantivy`, `status`,
  `ranking`, and `search/service/core/*`
- milestone validation for this slice is:
  `direnv exec . cargo test -p xiuxian-wendao --lib search::reference_occurrence::`
  `direnv exec . cargo test -p xiuxian-wendao --lib search::attachment::`
  `direnv exec . cargo test -p xiuxian-wendao --lib search::local_symbol::`
  `direnv exec . cargo test -p xiuxian-wendao --lib search::repo_content_chunk::`
  `direnv exec . cargo test -p xiuxian-wendao --lib search::repo_entity::`
  `direnv exec . cargo test -p xiuxian-wendao --lib search::knowledge_section::`
  `direnv exec . cargo test -p xiuxian-wendao --lib xiuxian_test_policy_harness::enforce_crate_test_policy_harness -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao --test unit_test enforce_crate_test_policy_harness -- --exact`
  `direnv exec . cargo clippy -p xiuxian-wendao --lib --tests -- -D warnings`
  `direnv exec . cargo fmt --all`
  `direnv exec . git diff --check -- packages/rust/crates/xiuxian-wendao packages/rust/crates/xiuxian-testing/docs/01_core/101_contract_testing_kernel.md docs/GTD/DAILY_2026_04_08.md .cache/agent/execplans/testing-gate-auto-remediation-wave1.md`

## Wendao Test-Policy Follow-Up Slice 18

- continued the same testing-gate remediation lane in
  `packages/rust/crates/xiuxian-wendao` and cleared the whole
  search-platform-core milestone in one pass:
  `src/search/repo_search/batch.rs`
  `src/search/coordinator/mod.rs`
  `src/search/project_fingerprint.rs`
  `src/search/fuzzy/mod.rs`
  `src/search/manifest/mod.rs`
  `src/search/queries/mod.rs`
  `src/search/queries/{graphql,flightsql,rest,sql}/mod.rs`
  `src/search/tantivy/mod.rs`
  `src/search/status/mod.rs`
  `src/search/status/maintenance.rs`
  `src/search/staged_mutation.rs`
  `src/search/corpus.rs`
  `src/search/ranking.rs`
  `src/search/service/mod.rs`
  `src/search/service/core/{status,repo_runtime,construction,maintenance}/mod.rs`
- moved the remaining source-resident `search` platform test trees into
  canonical `tests/unit/search/...` mounts:
  `src/search/{coordinator,fuzzy,manifest,tantivy,status}/tests.rs`
  `src/search/queries/tests/*`
  `src/search/queries/{graphql,flightsql,rest,sql}/tests/*`
  `src/search/service/tests/*`
  `src/search/service/core/status/tests/*`
  `src/search/service/core/repo_runtime/tests.rs`
  `src/search/service/core/construction/tests.rs`
  `src/search/service/core/maintenance/tests/*`
- externalized the last inline `search` suites into canonical files beside that
  tree:
  `tests/unit/search/repo_search/batch.rs`
  `tests/unit/search/project_fingerprint.rs`
  `tests/unit/search/ranking.rs`
  `tests/unit/search/staged_mutation.rs`
  `tests/unit/search/corpus.rs`
  `tests/unit/search/status/maintenance.rs`
- while validating the cluster, updated the `search::queries::tests::structure`
  contract to assert against the canonical `tests/unit/search/queries/*`
  snapshot tree instead of the old source-resident `src/search/queries/*/tests`
  layout
- decisive outcome:
  the live `xiuxian-testing-gate` failure surface no longer reports any
  `search/*` owner at all; the next top debt is now entirely outside search and
  starts at `pybindings/*`, `query_core`, `unified_symbol`, `skill_vfs/*`, and
  the larger `gateway/studio/*` tree
- milestone validation for this slice is:
  `direnv exec . cargo test -p xiuxian-wendao --lib search::`
  `direnv exec . cargo test -p xiuxian-wendao --lib xiuxian_test_policy_harness::enforce_crate_test_policy_harness -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao --test unit_test enforce_crate_test_policy_harness -- --exact`
  `direnv exec . cargo clippy -p xiuxian-wendao --lib --tests -- -D warnings`
  `direnv exec . cargo fmt --all`
  `direnv exec . git diff --check -- packages/rust/crates/xiuxian-wendao packages/rust/crates/xiuxian-testing/docs/01_core/101_contract_testing_kernel.md docs/GTD/DAILY_2026_04_08.md .cache/agent/execplans/testing-gate-auto-remediation-wave1.md`

## Wendao Test-Policy Follow-Up Slice 19

- continued the same testing-gate remediation lane in
  `packages/rust/crates/xiuxian-wendao` and cleared the next non-search
  milestone in one pass:
  `src/pybindings/unified_symbol_py/mod.rs`
  `src/pybindings/dep_indexer_py/mod.rs`
  `src/query_core/mod.rs`
  `src/unified_symbol/mod.rs`
  `src/skill_vfs/zhixing/resources/mod.rs`
  `src/skill_vfs/internal_manifest/mod.rs`
  `src/skill_vfs/resolver/runtime.rs`
  `src/zhenfa_router/native/semantic_check/docs_governance/mod.rs`
- moved the source-resident test files and trees for that front into canonical
  `tests/unit/...` mounts:
  `src/pybindings/unified_symbol_py/tests.rs`
  `src/pybindings/dep_indexer_py/tests.rs`
  `src/query_core/tests.rs`
  `src/unified_symbol/tests.rs`
  `src/skill_vfs/zhixing/resources/tests.rs`
  `src/skill_vfs/internal_manifest/tests.rs`
  `src/zhenfa_router/native/semantic_check/docs_governance/tests/*`
- externalized the last inline runtime suite from that cluster into
  `tests/unit/skill_vfs/resolver/runtime.rs`, then rewired all affected owners
  to direct canonical `#[path = "..."]` mounts
- validated the feature-gated pybindings sub-cluster explicitly with
  `--features pybindings` instead of treating its absence from the default
  `--lib` list as a policy exception
- decisive outcome:
  the live `xiuxian-testing-gate` failure surface no longer reports any
  `pybindings/*`, `query_core`, `unified_symbol`, `skill_vfs/*`, or
  `docs_governance` owner; the next top debt is now concentrated in the
  broader `gateway/studio/*` and `gateway/openapi/*` tree
- milestone validation for this slice is:
  `direnv exec . cargo test -p xiuxian-wendao --lib query_core::`
  `direnv exec . cargo test -p xiuxian-wendao --lib unified_symbol::`
  `direnv exec . cargo test -p xiuxian-wendao --lib skill_vfs::`
  `direnv exec . cargo test -p xiuxian-wendao --lib zhenfa_router::native::semantic_check::docs_governance::`
  `direnv exec . cargo test -p xiuxian-wendao --lib --features pybindings pybindings::`
  `direnv exec . cargo test -p xiuxian-wendao --lib xiuxian_test_policy_harness::enforce_crate_test_policy_harness -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao --test unit_test enforce_crate_test_policy_harness -- --exact`
  `direnv exec . cargo clippy -p xiuxian-wendao --lib --tests -- -D warnings`
  `direnv exec . cargo fmt --all`
  `direnv exec . git diff --check -- packages/rust/crates/xiuxian-wendao packages/rust/crates/xiuxian-testing/docs/01_core/101_contract_testing_kernel.md docs/GTD/DAILY_2026_04_08.md .cache/agent/execplans/testing-gate-auto-remediation-wave1.md`

## Wendao Test-Policy Follow-Up Slice 20

- continued the same testing-gate remediation lane in
  `packages/rust/crates/xiuxian-wendao` and cleared the first contiguous
  `gateway/studio` front in one pass:
  `src/gateway/studio/vfs/mod.rs`
  `src/gateway/studio/vfs/{flight,flight_content,flight_scan,navigation}.rs`
  `src/gateway/studio/pathing.rs`
  `src/gateway/studio/types/config.rs`
  `src/gateway/studio/types/search_index/mod.rs`
  `src/gateway/studio/analysis/markdown/compile/mod.rs`
  `src/gateway/studio/symbol_index/state/mod.rs`
  `src/gateway/studio/search/project_scope.rs`
  `src/gateway/studio/search/definition/mod.rs`
- moved the source-resident test files and trees for that front into canonical
  `tests/unit/gateway/studio/...` mounts:
  `src/gateway/studio/vfs/tests.rs`
  `src/gateway/studio/types/search_index/tests/*`
  `src/gateway/studio/analysis/markdown/compile/tests.rs`
  `src/gateway/studio/symbol_index/state/tests.rs`
  `src/gateway/studio/search/definition/tests.rs`
- externalized the inline suites from the same cluster into:
  `tests/unit/gateway/studio/vfs/{flight,flight_content,flight_scan,navigation}.rs`
  `tests/unit/gateway/studio/pathing.rs`
  `tests/unit/gateway/studio/types/config.rs`
  `tests/unit/gateway/studio/search/project_scope.rs`
- while normalizing the VFS tree, retired the stale dead surface
  `tests/unit/gateway/studio/vfs.rs` instead of preserving it as a parallel
  compatibility layer; the active canonical mount is now the directory tree
  under `tests/unit/gateway/studio/vfs/`
- decisive outcome:
  the live `xiuxian-testing-gate` failure surface no longer reports any owner
  from this `gateway/studio` front; the next top debt is now concentrated in
  `gateway/studio/search/handlers/*`, `gateway/studio/router/*`,
  `gateway/studio/startup_health/*`, and `gateway/openapi/*`
- milestone validation for this slice is:
  `direnv exec . cargo test -p xiuxian-wendao --lib gateway::studio::vfs::`
  `direnv exec . cargo test -p xiuxian-wendao --lib gateway::studio::pathing::`
  `direnv exec . cargo test -p xiuxian-wendao --lib gateway::studio::types::config::`
  `direnv exec . cargo test -p xiuxian-wendao --lib gateway::studio::types::search_index::`
  `direnv exec . cargo test -p xiuxian-wendao --lib gateway::studio::analysis::markdown::compile::`
  `direnv exec . cargo test -p xiuxian-wendao --lib gateway::studio::symbol_index::state::`
  `direnv exec . cargo test -p xiuxian-wendao --lib gateway::studio::search::project_scope::`
  `direnv exec . cargo test -p xiuxian-wendao --lib gateway::studio::search::definition::`
  `direnv exec . cargo test -p xiuxian-wendao --lib xiuxian_test_policy_harness::enforce_crate_test_policy_harness -- --exact`
  `direnv exec . cargo test -p xiuxian-wendao --test unit_test enforce_crate_test_policy_harness -- --exact`
  `direnv exec . cargo clippy -p xiuxian-wendao --lib --tests -- -D warnings`
  `direnv exec . cargo fmt --all`
  `direnv exec . git diff --check -- packages/rust/crates/xiuxian-wendao packages/rust/crates/xiuxian-testing/docs/01_core/101_contract_testing_kernel.md docs/GTD/DAILY_2026_04_08.md .cache/agent/execplans/testing-gate-auto-remediation-wave1.md`

## Qianji RFC Implementation Coverage Audit

- continued the same Flowhub graph-contract blueprint at
  `.cache/agent/blueprints/qianji_flowhub_planning_source_model.md` and
  recorded this bounded doc slice in
  `.cache/agent/execplans/qianji-rfc-implementation-coverage-audit.md`
- added a canonical coverage-audit document at
  `packages/rust/crates/xiuxian-qianji/docs/rfcs/2026-04-08-qianji-rfc-implementation-coverage-audit.md`
  so the current Flowhub and bounded-work RFC cluster now has one
  code-anchored matrix for `implemented core path`, `partial`, and remaining
  draft-exit gaps
- linked that audit from the package README, the Qianji MOC, and the main
  Flowhub graph-contract RFC without exposing hidden tracking paths in
  canonical documentation
- the audit now makes the current state explicit: the Flowhub graph-contract
  lane is substantially code-backed, while the bounded retrieval RFC cluster
  is partially code-backed and still needs tighter clause-by-clause
  acceptance coverage
- focused validation for this slice is:
  `direnv exec . git diff --check -- packages/rust/crates/xiuxian-qianji/docs/rfcs/2026-04-08-qianji-rfc-implementation-coverage-audit.md packages/rust/crates/xiuxian-qianji/docs/rfcs/2026-04-07-qianji-flowhub-graph-contract-model-rfc.md packages/rust/crates/xiuxian-qianji/README.md packages/rust/crates/xiuxian-qianji/docs/00_mocs/20260315141000-qianji-orchestrator-moc.md .cache/agent/execplans/qianji-rfc-implementation-coverage-audit.md docs/GTD/DAILY_2026_04_08.md`
