# Daily Log - 2026-04-06

## Wendao Memory Julia Compute ABI RFC

- created the governing blueprint at
  `.cache/agent/blueprints/wendao_memory_julia_compute_abi.md`
  and the bounded ExecPlan at
  `.cache/agent/execplans/wendao-memory-julia-compute-abi-rfc.md`
  for the memory-family Julia compute ABI lane
- published the canonical RFC
  `docs/rfcs/2026-04-06-arrow-schema-first-julia-compute-abi-for-wendao-memory-family.md`
  to formalize one Arrow schema-first Julia compute ABI for the Wendao memory
  family
- locked the governing architecture principle into the RFC:
  Julia owns high-performance scientific and mathematical compute only, while
  Rust retains authoritative ownership of host state, lifecycle, registry,
  fallback, audit, and final mutation decisions
- defined the first standardized `memory` family with four profiles:
  `episodic_recall`, `memory_gate_score`, `memory_plan_tuning`, and
  `memory_calibration`, while explicitly constraining the non-retrieval
  profiles to recommendation-only or advice-only behavior
- standardized the three-layer contract model for this lane:
  physical Arrow schema, semantic contract, and capability contract
- added explicit ABI hardening in the RFC for:
  allowed Julia compute scope, host read-only projection batches,
  hidden-writeback prohibition, semantic-vs-physical versioning, family-level
  compat rules, canonical schema fragments, and shadow-compare observability
- synchronized the canonical memory architecture note in
  `docs/01_core/memory/architecture.md` so it now points readers from the
  episodic-memory boundary into the new memory-family Julia compute ABI
- synchronized the WendaoArrow scorer-contract doc in
  `.data/WendaoArrow.jl/docs/01_core/wendao_arrow/architecture/arrow-schema-contract.md`
  with a family-level ABI pointer back to this RFC without widening that doc
  into a second host-ownership specification
- validation for this slice is doc-level only:
  `direnv exec . git diff --check -- docs/rfcs/2026-04-06-arrow-schema-first-julia-compute-abi-for-wendao-memory-family.md docs/01_core/memory/architecture.md docs/GTD/DAILY_2026_04_06.md`
  `git -C .data/WendaoArrow.jl diff --check -- docs/01_core/wendao_arrow/architecture/arrow-schema-contract.md`

## Wendao Memory Julia Public Surface Thinning

- opened the bounded ExecPlan at
  `.cache/agent/execplans/wendao-memory-julia-surface-thinning.md`
  to reduce helper leakage from the product-crate memory Julia bridge
- narrowed `packages/rust/crates/xiuxian-wendao/src/memory/julia/mod.rs`
  so the product crate now re-exports only:
  `ComputeClient`, runtime-resolution helpers, binding-resolution helpers, and
  the minimum typed DTOs required to call the configured client
- removed product-crate re-exports for plugin-owned helper surfaces:
  raw runtime-resolved downcall free functions,
  `build_memory_julia_compute_bindings(...)`,
  `build_memory_gate_score_evidence_row_from_episode(...)`, and
  `build_memory_gate_score_evidence_row_from_store(...)`
- updated the `memory::julia` test surface so it now proves the thin bridge
  around `ComputeClient` and the surviving DTO/runtime exports instead of
  asserting that plugin helpers are visible from `xiuxian-wendao`
- synchronized the package README and the rollout roadmap note so the Wendao
  docs now say the product crate exposes one configured host seam and no
  longer presents plugin-owned helper builders as part of its public surface

## Wendao Memory Julia RFC Structure Sync

- opened the bounded ExecPlan at
  `.cache/agent/execplans/wendao-memory-julia-rfc-structure-sync.md`
  to rewrite the memory-family Julia compute ABI RFC into the newer structured
  outline and keep the canonical boundary docs aligned
- rewrote
  `docs/rfcs/2026-04-06-arrow-schema-first-julia-compute-abi-for-wendao-memory-family.md`
  so the RFC now explicitly carries the structured sections for status,
  authors, date, abstract, motivation, non-goals, governing principle,
  contract model, runtime config, profile intent, schema fragments,
  documentation sync, deferred work, acceptance criteria, and assumptions
- kept the rewritten RFC aligned with current landed host reality in this
  repository, including the runtime-level `memory.julia_compute` surface,
  compute-only Julia ownership, Rust authority, `fallback_mode = "rust"`,
  `shadow_compare = true`, and the current `/memory/calibration` route for
  `memory_calibration`
- synchronized `docs/01_core/memory/architecture.md` so the memory note now
  describes `xiuxian-wendao` as exposing only a thin configured host seam
- synchronized
  `.data/WendaoArrow.jl/docs/01_core/wendao_arrow/architecture/arrow-schema-contract.md`
  so the scorer-contract note now says `WendaoArrow.jl` remains the
  Julia-side transport, contract, and authoring substrate for compute plugins
  only

## Wendao Memory Julia Contract Tightening

- opened the bounded ExecPlan at
  `.cache/agent/execplans/wendao-memory-julia-contract-tightening.md`
  to tighten the remaining memory-family route and recommendation-vocabulary
  drift without widening the lane
- standardized the canonical `memory_calibration` route on
  `/memory/calibration` across runtime config defaults, profile metadata,
  manifest proofs, transport proofs, downcall proofs, thin host tests, and the
  RFC runtime-config example
- standardized the recommendation-only `memory_gate_score` promotion verdict on
  `promote_to_working_knowledge` inside the Julia memory-family contract, and
  updated the calibration artifact sample thresholds to use the same working-
  knowledge promotion vocabulary
- synchronized `packages/rust/crates/xiuxian-wendao-julia/README.md` so the
  package doc now records both the canonical calibration route and the
  narrowed promotion verdict vocabulary for the staged memory-family lane
- validation for this slice is green:
  `direnv exec . cargo fmt -p xiuxian-wendao-runtime -p xiuxian-wendao-julia -p xiuxian-wendao`
  `direnv exec . cargo test -p xiuxian-wendao-runtime --lib memory_julia_compute_runtime --features transport`
  `direnv exec . cargo test -p xiuxian-wendao-julia memory`
  `direnv exec . cargo test -p xiuxian-wendao --lib memory --features julia`
  `direnv exec . cargo clippy -p xiuxian-wendao-runtime --lib --features transport -- -D warnings`
  `direnv exec . cargo clippy -p xiuxian-wendao-julia --all-targets --all-features -- -D warnings`
  `direnv exec . cargo clippy -p xiuxian-wendao --lib --features julia -- -D warnings`
  `direnv exec . cargo check -p xiuxian-wendao --lib --features julia`
  `direnv exec . git diff --check -- packages/rust/crates/xiuxian-wendao-runtime/src/config/memory/julia packages/rust/crates/xiuxian-wendao-julia/src/memory packages/rust/crates/xiuxian-wendao/src/memory/julia packages/rust/crates/xiuxian-wendao-julia/README.md docs/rfcs/2026-04-06-arrow-schema-first-julia-compute-abi-for-wendao-memory-family.md docs/GTD/DAILY_2026_04_06.md .cache/agent/execplans/wendao-memory-julia-contract-tightening.md`

## Wendao Memory Julia Compute Host Read Model

- opened the bounded ExecPlan at
  `.cache/agent/execplans/wendao-memory-julia-compute-host-read-model.md`
  for the first post-RFC implementation slice
- added a runtime-level `memory.julia_compute` host config surface to
  `xiuxian-wendao-runtime` and then renamed the crate namespace from
  `runtime_config` to `config` so the resolved-config layer no longer repeats
  the crate role in its own path
- structured the new memory-family config seam as
  `packages/rust/crates/xiuxian-wendao-runtime/src/config/memory/julia/compute.rs`
  so the path reads as domain -> provider -> capability
- added a read-only host projection seam to `xiuxian-memory-engine` via
  `MemoryProjectionFilter`, `MemoryProjectionRow`, and
  `EpisodeStore::memory_projection_rows(...)`
- kept the projection boundary memory-only:
  the new export carries scope, embeddings, Q/utility state, counters, and
  timestamps, but it does not expose lifecycle mutation or registry authority
- updated `xiuxian-wendao` call sites to the renamed
  `xiuxian_wendao_runtime::config` public path and verified the dependent crate
  still checks under `--features julia`
- validation for this slice:
  `direnv exec . cargo test -p xiuxian-wendao-runtime --lib memory_julia_compute_runtime --features transport`
  `direnv exec . cargo clippy -p xiuxian-wendao-runtime --lib --features transport -- -D warnings`
  `direnv exec . cargo test -p xiuxian-memory-engine projection`
  `direnv exec . cargo clippy -p xiuxian-memory-engine --all-targets --all-features -- -D warnings`
  `direnv exec . cargo check -p xiuxian-wendao --lib --features julia`
- runtime test-scope clippy still has a pre-existing baseline blocker outside
  this slice:
  `packages/rust/crates/xiuxian-wendao-runtime/src/transport/plugin_arrow_exchange/tests.rs`
  still trips `clippy::expect_used` on existing `expect_err(...)` calls

## Wendao Runtime Plugin Arrow Test Clippy Cleanup

- opened the bounded ExecPlan at
  `.cache/agent/execplans/wendao-runtime-plugin-arrow-test-clippy-cleanup.md`
  to close the remaining strict-clippy runtime baseline without widening scope
- replaced the remaining `expect_err(...)` assertions in
  `packages/rust/crates/xiuxian-wendao-runtime/src/transport/plugin_arrow_exchange/tests.rs`
  with explicit error extraction so the tests keep the same failure semantics
  without tripping `clippy::expect_used`
- kept this slice test-only:
  no transport production code changed, and the work stayed inside the
  `plugin_arrow_exchange` test surface
- validation for this slice:
  `direnv exec . cargo test -p xiuxian-wendao-runtime --lib --features transport plugin_arrow`
  `direnv exec . cargo clippy -p xiuxian-wendao-runtime --tests --features transport -- -D warnings`
- result:
  strict runtime test-scope clippy is now green again for
  `xiuxian-wendao-runtime`

## Wendao Memory Julia Thin Compat

- opened the bounded ExecPlan at
  `.cache/agent/execplans/wendao-memory-julia-thin-compat.md`
  for the first `xiuxian-wendao-julia` bridge slice under the memory-family
  Julia compute ABI blueprint
- added a dedicated `packages/rust/crates/xiuxian-wendao-julia/src/memory/`
  feature folder instead of hanging the new lane under `src/plugin/`, so the
  bridge surface now stays runtime-level and domain-first
- added staged memory-family profile metadata and runtime-to-binding helpers
  for `episodic_recall`, `memory_gate_score`, `memory_plan_tuning`, and
  `memory_calibration`, all sourced from
  `xiuxian-wendao-runtime::config::MemoryJuliaComputeRuntimeConfig`
- added the first typed Arrow contract for `episodic_recall`, including
  request-column constants, typed request and score rows, Arrow schemas,
  request validation, response validation, and response decoding
- kept the slice compute-only and thin:
  no live Flight roundtrip, no host mutation hook, no lifecycle transition
  ownership, and no repo-plugin config coupling were introduced
- normalized the new path from the initially over-nested `src/memory/julia/`
  to `src/memory/` so the bridge surface does not repeat the provider name in
  its directory layout
- focused validation for this slice:
  `direnv exec . cargo test -p xiuxian-wendao-julia memory -- --nocapture`
- closed the new compile and lint issues inside
  `packages/rust/crates/xiuxian-wendao-julia/src/memory/episodic_recall.rs`
  and `packages/rust/crates/xiuxian-wendao-julia/src/memory/runtime.rs`, so
  the new `src/memory/` thin compat surface is now green under focused proof:
  `direnv exec . cargo test -p xiuxian-wendao-julia memory`
  `direnv exec . cargo check -p xiuxian-wendao --lib --features julia`
- strict crate-wide clippy is still blocked by pre-existing baseline debt
  outside the new memory slice, especially under
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/`,
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/test_support/`, and
  `packages/rust/crates/xiuxian-wendao-julia/src/compatibility/link_graph/tests.rs`;
  the new `src/memory/` module no longer appears in the reported failures
- archived the completed first-slice ExecPlan at
  `.cache/agent/execplans/archives/wendao-memory-julia-thin-compat.md`
  and opened the next bounded slice at
  `.cache/agent/execplans/wendao-memory-julia-gate-score.md`
- added the next staged profile contract under
  `packages/rust/crates/xiuxian-wendao-julia/src/memory/gate_score.rs`,
  covering recommendation-only `memory_gate_score` request columns,
  response columns, typed request and recommendation rows, Arrow schema
  builders, request and response validators, and response decoding
- aligned the new gate-score semantics with the Rust-owned memory gate surface:
  request rows accept lifecycle-state evidence and gate inputs, while response
  rows validate only recommendation-only verdict strings
  (`retain` / `obsolete` / `promote_to_working_knowledge`) plus confidence,
  utility, TTL, next action, and rationale; no mutation or lifecycle commit
  path was introduced
- focused validation for the gate-score slice is green:
  `direnv exec . cargo test -p xiuxian-wendao-julia memory`
  `direnv exec . cargo check -p xiuxian-wendao --lib --features julia`
  `direnv exec . git diff --check -- packages/rust/crates/xiuxian-wendao-julia docs/GTD/DAILY_2026_04_06.md .cache/agent/execplans/wendao-memory-julia-gate-score.md`
- full `direnv exec . cargo clippy -p xiuxian-wendao-julia --all-targets --all-features -- -D warnings`
  is still blocked by the pre-existing plugin/test baseline under
  `src/plugin/`, `src/plugin/test_support/`, and
  `src/compatibility/link_graph/tests.rs`, but the rerun no longer reports new
  failures in `packages/rust/crates/xiuxian-wendao-julia/src/memory/gate_score.rs`
- archived the completed gate-score ExecPlan at
  `.cache/agent/execplans/archives/wendao-memory-julia-gate-score.md`
  and opened the next bounded slice at
  `.cache/agent/execplans/wendao-memory-julia-plan-tuning.md`
- added the next staged profile contract under
  `packages/rust/crates/xiuxian-wendao-julia/src/memory/plan_tuning.rs`,
  covering advice-only `memory_plan_tuning` request columns, response columns,
  typed request and advice rows, Arrow schema builders, request and response
  validators, and response decoding
- aligned the new plan-tuning semantics with the Rust-owned
  `RecallPlanTuning` surface in `xiuxian-memory-engine/src/recall_feedback.rs`,
  so the Julia Arrow boundary now uses the same `k1` / `k2` / `lambda` /
  `min_score` / `max_context_chars` vocabulary instead of inventing a second
  tuning surface
- focused validation for the plan-tuning slice is green:
  `direnv exec . cargo test -p xiuxian-wendao-julia memory`
  `direnv exec . cargo check -p xiuxian-wendao --lib --features julia`
  `direnv exec . git diff --check -- packages/rust/crates/xiuxian-wendao-julia docs/GTD/DAILY_2026_04_06.md .cache/agent/execplans/wendao-memory-julia-plan-tuning.md`
- archived the completed plan-tuning ExecPlan at
  `.cache/agent/execplans/archives/wendao-memory-julia-plan-tuning.md`
  and opened the next bounded slice at
  `.cache/agent/execplans/wendao-memory-julia-calibration.md`
- added the final staged profile contract under
  `packages/rust/crates/xiuxian-wendao-julia/src/memory/calibration.rs`,
  covering artifact-only `memory_calibration` request columns, response
  columns, typed request and artifact rows, Arrow schema builders, request and
  response validators, and response decoding
- kept the new calibration seam recommendation-only and non-activating:
  artifact refs, summary metrics, thresholds, and weights cross the Arrow
  boundary as typed payload blobs, but no activation authority, lifecycle
  mutation path, or host writeback channel was introduced
- focused validation for the calibration slice is green:
  `direnv exec . cargo fmt -p xiuxian-wendao-julia`
  `direnv exec . cargo test -p xiuxian-wendao-julia memory`
  `direnv exec . cargo check -p xiuxian-wendao --lib --features julia`
  `direnv exec . git diff --check -- packages/rust/crates/xiuxian-wendao-julia docs/GTD/DAILY_2026_04_06.md .cache/agent/execplans/wendao-memory-julia-calibration.md`
- full `direnv exec . cargo clippy -p xiuxian-wendao-julia --all-targets --all-features -- -D warnings`
  remains blocked by the same pre-existing plugin and test baseline under
  `src/plugin/`, `src/plugin/test_support/`, and
  `src/compatibility/link_graph/tests.rs`; the rerun did not report new
  failures in `packages/rust/crates/xiuxian-wendao-julia/src/memory/calibration.rs`
- archived the completed calibration ExecPlan at
  `.cache/agent/execplans/archives/wendao-memory-julia-calibration.md`
- opened the next bounded slice at
  `.cache/agent/execplans/wendao-memory-julia-capability-manifest.md`
  to add one runtime-level manifest-projection surface for the RFC `memory`
  family entry shape
- added `packages/rust/crates/xiuxian-wendao-julia/src/memory/manifest.rs`,
  which projects canonical manifest rows from
  `MemoryJuliaComputeRuntimeConfig` plus staged profile contracts, then
  materializes Arrow response schema, batch-builder, validation, and decode
  helpers for that row shape
- kept this new surface projection-only:
  it does not add live Flight execution, repository-plugin config parsing, or
  host mutation semantics; it only gives the memory-family lane one stable
  typed manifest entry projection under `src/memory/`
- focused validation for the manifest-projection slice is green:
  `direnv exec . cargo fmt -p xiuxian-wendao-julia`
  `direnv exec . cargo test -p xiuxian-wendao-julia memory`
  `direnv exec . cargo check -p xiuxian-wendao --lib --features julia`
- full strict clippy for `xiuxian-wendao-julia` remains blocked by the same
  pre-existing plugin/test baseline outside `src/memory/`; this manifest slice
  is intended to stay on the RFC lane rather than widen into that cleanup, and
  the rerun no longer reports new failures in
  `packages/rust/crates/xiuxian-wendao-julia/src/memory/manifest.rs`
- archived the completed manifest-projection ExecPlan at
  `.cache/agent/execplans/archives/wendao-memory-julia-capability-manifest.md`

## Wendao Memory Julia Clippy Baseline Closure

- opened the bounded ExecPlan at
  `.cache/agent/execplans/wendao-memory-julia-clippy-baseline-closure.md`
  to close the remaining crate-wide strict-clippy blockers after the
  memory-family Julia compute ABI slices landed
- removed the old strict-clippy baseline debt across
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/`,
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/test_support/`, and
  `packages/rust/crates/xiuxian-wendao-julia/src/compatibility/link_graph/tests.rs`
  without adding lint suppressions
- added shared test-support result or option panic helpers plus one float
  equality helper in
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/test_support/common.rs`
  so the graph-structural and compatibility test surfaces no longer depend on
  `expect`, `expect_err`, `unwrap_err`, or direct float equality
- moved the `entry.rs` test module after production items and narrowed the
  graph-structural test helper batch builders to struct-shaped inputs so the
  old `items_after_test_module` and `too_many_arguments` blockers are gone
- aligned the staged graph-structural transport request fixtures with the
  current request contract by carrying explicit candidate edge source and
  destination columns in
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/graph_structural_transport.rs`
- validation for this closure slice:
  `direnv exec . cargo fmt -p xiuxian-wendao-julia`
  `direnv exec . cargo clippy -p xiuxian-wendao-julia --all-targets --all-features -- -D warnings`
  `direnv exec . cargo test -p xiuxian-wendao-julia plugin::graph_structural_transport::tests::validate_graph_structural_request_batches_accepts_staged_shapes -- --exact --nocapture`
  `direnv exec . cargo check -p xiuxian-wendao --lib --features julia`
  `git diff --check -- packages/rust/crates/xiuxian-wendao-julia/src/plugin/test_support/common.rs packages/rust/crates/xiuxian-wendao-julia/src/plugin/entry.rs packages/rust/crates/xiuxian-wendao-julia/src/plugin/graph_structural.rs packages/rust/crates/xiuxian-wendao-julia/src/plugin/graph_structural_exchange_tests.rs packages/rust/crates/xiuxian-wendao-julia/src/plugin/graph_structural_exchange_generic_topology_tests.rs packages/rust/crates/xiuxian-wendao-julia/src/plugin/graph_structural_projection/tests.rs packages/rust/crates/xiuxian-wendao-julia/src/plugin/graph_structural_transport.rs`
- result:
  strict crate-wide clippy is green again for `xiuxian-wendao-julia`, and the
  previously failing staged transport request validation test is green again
- archived the completed ExecPlan at
  `.cache/agent/execplans/archives/wendao-memory-julia-clippy-baseline-closure.md`

## Wendao Memory Julia Live Test Hang Triage

- opened the bounded ExecPlan at
  `.cache/agent/execplans/wendao-memory-julia-live-test-hang-triage.md`
  to isolate the remaining apparent hang in the full
  `xiuxian-wendao-julia` crate test gate after strict clippy returned to green
- isolated the root cause inside the graph-structural live proof surface:
  the crate was not deadlocking, but the serial live suites repeatedly
  re-spawned slow `WendaoSearch.jl --mode solver_demo` services, and libtest
  reported the waiting serial peers as tests "running for over 60 seconds"
- kept the bounded timeout hardening in
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/test_support/official_examples.rs`
  so slow startup or request phases now fail closed with explicit timeout
  context instead of looking like an unbounded hang
- regrouped the slow manifest-discovery proofs in
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/graph_structural_exchange_tests.rs`
  and
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/graph_structural_exchange_generic_topology_tests.rs`
  so each solver-demo `multi_route` live service now serves multiple rerank
  and filter assertions inside one service lifecycle
- validation for this closure slice:
  `direnv exec . cargo test -p xiuxian-wendao-julia plugin::graph_structural_exchange::tests::fetch_graph_structural_solver_demo_rows_for_repository_via_manifest_discovery_against_real_wendaosearch_multi_route_service -- --exact --nocapture`
  `direnv exec . cargo test -p xiuxian-wendao-julia plugin::graph_structural_exchange::generic_topology_tests::fetch_graph_structural_generic_topology_rows_for_repository_via_manifest_discovery_against_real_wendaosearch_solver_demo_multi_route_service -- --exact --nocapture`
  `direnv exec . cargo test -p xiuxian-wendao-julia`
  `direnv exec . cargo clippy -p xiuxian-wendao-julia --all-targets --all-features -- -D warnings`
  `direnv exec . cargo check -p xiuxian-wendao --lib --features julia`
- result:
  the full crate test gate completes again at
  `147 passed; 0 failed; finished in 226.16s`, and the remaining long-tail
  output now reflects bounded serial live-service work rather than a stuck or
  orphaned test process
- archived the completed ExecPlan at
  `.cache/agent/execplans/archives/wendao-memory-julia-live-test-hang-triage.md`

## Wendao Memory Julia Live Service Consolidation

- opened the bounded ExecPlan at
  `.cache/agent/execplans/wendao-memory-julia-live-service-consolidation.md`
  to keep reducing the remaining graph-structural live-test long tail after
  the hang triage slice proved the issue was repeated service startup rather
  than deadlock
- consolidated the remaining graph-structural `demo` and `solver_demo` live
  proofs onto fewer `multi_route` `WendaoSearch.jl` lifecycles in
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/graph_structural_exchange_tests.rs`
  and
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/graph_structural_exchange_generic_topology_tests.rs`
- kept coverage intact while reducing startup count:
  explicit graph-structural transport assertions now target the same live
  `multi_route` services as the manifest-discovery assertions, instead of
  re-spawning separate single-route structural-rerank or constraint-filter
  services for the same mode
- tightened plugin test-support in
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/test_support/official_examples.rs`
  and
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/test_support/common.rs`
  so the live Julia example services now launch through explicit
  `julia --project=...` commands and package-root paths, which removes the
  plugin live-test dependency on repo-level `direnv` evaluation
- during this slice the project-level environment gate regressed outside the
  touched Rust code:
  `direnv exec . true` now fails in `.envrc` evaluation with a
  `config.cachix.push` type error, so the validation fallback for this slice
  used the absolute Nix Rust toolchain path plus direct Julia launches
- validation for this slice:
  `/nix/store/6lyl68mg2a582mqi8vj9zb1y96qvfl4m-rust-stable-1.93.1-1.93.1/bin/rustfmt packages/rust/crates/xiuxian-wendao-julia/src/plugin/test_support/common.rs packages/rust/crates/xiuxian-wendao-julia/src/plugin/test_support/official_examples.rs packages/rust/crates/xiuxian-wendao-julia/src/plugin/graph_structural_exchange_tests.rs packages/rust/crates/xiuxian-wendao-julia/src/plugin/graph_structural_exchange_generic_topology_tests.rs`
  `PATH=/nix/store/6lyl68mg2a582mqi8vj9zb1y96qvfl4m-rust-stable-1.93.1-1.93.1/bin:$PATH /nix/store/6lyl68mg2a582mqi8vj9zb1y96qvfl4m-rust-stable-1.93.1-1.93.1/bin/cargo test -p xiuxian-wendao-julia plugin::graph_structural_exchange::tests::fetch_graph_structural_demo_rerank_rows_for_repository_against_real_wendaosearch_multi_route_service -- --exact --nocapture`
  `PATH=/nix/store/6lyl68mg2a582mqi8vj9zb1y96qvfl4m-rust-stable-1.93.1-1.93.1/bin:$PATH /nix/store/6lyl68mg2a582mqi8vj9zb1y96qvfl4m-rust-stable-1.93.1-1.93.1/bin/cargo test -p xiuxian-wendao-julia plugin::graph_structural_exchange::tests::fetch_graph_structural_solver_demo_rows_for_repository_via_manifest_discovery_against_real_wendaosearch_multi_route_service -- --exact --nocapture`
  `PATH=/nix/store/6lyl68mg2a582mqi8vj9zb1y96qvfl4m-rust-stable-1.93.1-1.93.1/bin:$PATH /nix/store/6lyl68mg2a582mqi8vj9zb1y96qvfl4m-rust-stable-1.93.1-1.93.1/bin/cargo test -p xiuxian-wendao-julia plugin::graph_structural_exchange::generic_topology_tests::fetch_graph_structural_generic_topology_rows_for_repository_via_manifest_discovery_against_real_wendaosearch_solver_demo_multi_route_service -- --exact --nocapture`
  `PATH=/nix/store/6lyl68mg2a582mqi8vj9zb1y96qvfl4m-rust-stable-1.93.1-1.93.1/bin:$PATH /nix/store/6lyl68mg2a582mqi8vj9zb1y96qvfl4m-rust-stable-1.93.1-1.93.1/bin/cargo test -p xiuxian-wendao-julia`
  `PATH=/nix/store/6lyl68mg2a582mqi8vj9zb1y96qvfl4m-rust-stable-1.93.1-1.93.1/bin:$PATH /nix/store/6lyl68mg2a582mqi8vj9zb1y96qvfl4m-rust-stable-1.93.1-1.93.1/bin/cargo clippy -p xiuxian-wendao-julia --all-targets --all-features -- -D warnings`
  `PATH=/nix/store/6lyl68mg2a582mqi8vj9zb1y96qvfl4m-rust-stable-1.93.1-1.93.1/bin:$PATH /nix/store/6lyl68mg2a582mqi8vj9zb1y96qvfl4m-rust-stable-1.93.1-1.93.1/bin/cargo check -p xiuxian-wendao --lib --features julia`
- result:
  the full crate test gate now completes in `109.92s` with
  `143 passed; 0 failed`, down from the earlier `226.16s` long tail while
  preserving the graph-structural live coverage
- follow-up:
  after the project environment fix landed, the canonical repo-environment
  gates were rerun successfully:
  `direnv exec . cargo test -p xiuxian-wendao-julia`
  `direnv exec . cargo clippy -p xiuxian-wendao-julia --all-targets --all-features -- -D warnings`
  `direnv exec . cargo check -p xiuxian-wendao --lib --features julia`
  the current canonical full test pass is `143 passed` in `108.60s`
- archived the completed ExecPlan at
  `.cache/agent/execplans/archives/wendao-memory-julia-live-service-consolidation.md`

## Wendao Memory Julia Capability Manifest Live Consolidation

- opened the bounded ExecPlan at
  `.cache/agent/execplans/wendao-memory-julia-capability-manifest-live-consolidation.md`
  to remove the last repeated `demo` capability-manifest live proofs without
  widening into other plugin lanes
- consolidated the remaining live manifest coverage into one grouped test in
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/capability_manifest.rs`
  so one real `WendaoSearch.jl` `demo` endpoint now covers:
  manifest fetch, manifest preflight validation, graph-structural binding
  discovery, graph-structural transport fallback, and plugin preflight
- removed the redundant same-service live proofs from
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/entry.rs` and
  `packages/rust/crates/xiuxian-wendao-julia/src/plugin/graph_structural_transport.rs`
  and then closed the resulting unused-import warning inside the transport
  test module
- kept the slice test-only:
  no production transport semantics changed, and the live proof still stays
  on the plugin-owned manifest-discovery lane
- validation for this slice:
  `direnv exec . cargo test -p xiuxian-wendao-julia plugin::capability_manifest::tests::demo_capability_manifest_live_proof_covers_fetch_preflight_binding_and_plugin_preflight -- --exact --nocapture`
  `direnv exec . cargo test -p xiuxian-wendao-julia`
  `direnv exec . cargo clippy -p xiuxian-wendao-julia --all-targets --all-features -- -D warnings`
  `direnv exec . cargo check -p xiuxian-wendao --lib --features julia`
  `git diff --check -- packages/rust/crates/xiuxian-wendao-julia/src/plugin/capability_manifest.rs packages/rust/crates/xiuxian-wendao-julia/src/plugin/entry.rs packages/rust/crates/xiuxian-wendao-julia/src/plugin/graph_structural_transport.rs packages/rust/crates/xiuxian-wendao-julia/README.md docs/GTD/DAILY_2026_04_06.md .cache/agent/execplans/wendao-memory-julia-capability-manifest-live-consolidation.md`
- result:
  the grouped live proof is green, crate-wide strict clippy stays green, and
  the current canonical full crate pass is `139 passed; 0 failed` in `136.25s`
- archived the completed ExecPlan at
  `.cache/agent/execplans/archives/wendao-memory-julia-capability-manifest-live-consolidation.md`

## Wendao Memory Julia Episodic Recall Host Staging

- opened the bounded ExecPlan at
  `.cache/agent/execplans/wendao-memory-julia-episodic-recall-host-staging.md`
  to bridge the Rust-owned memory read model into the staged Julia
  `episodic_recall` request contract without moving authority out of Rust
- added a new host-owned feature folder under
  `packages/rust/crates/xiuxian-wendao/src/memory/julia/`
  with `mod.rs` interface modules plus
  `episodic_recall.rs` implementation, so the path now reads as
  domain -> provider -> capability
- added `EpisodicRecallQueryInputs`,
  `build_episodic_recall_request_rows_from_projection(...)`, and
  `build_episodic_recall_request_batch_from_projection(...)`
  in `xiuxian-wendao`, mapping `xiuxian-memory-engine::MemoryProjectionRow`
  into typed `xiuxian-wendao-julia` `episodic_recall` request rows and one
  Arrow request batch
- kept ownership boundaries narrow:
  `xiuxian-memory-engine` still owns the read-only projection model,
  `xiuxian-wendao-julia` still owns the typed Arrow contract, and
  `xiuxian-wendao` now owns only the host staging bridge
- added focused host regression coverage proving:
  projection rows map into request rows,
  projection rows materialize the staged request batch,
  invalid shared query inputs are rejected,
  and a real `EpisodeStore::memory_projection_rows(...)` snapshot can feed the
  new staging seam while respecting scope filtering
- made the host crate dependency boundary explicit:
  `xiuxian-wendao` now directly depends on `xiuxian-memory-engine` and
  `xiuxian-wendao-julia` behind the existing `julia` feature so the host seam
  can reuse the canonical read model and the canonical typed Arrow contract
- validation for this slice:
  `direnv exec . env CARGO_TARGET_DIR=.cache/cargo-target/memory-host-staging cargo test -p xiuxian-wendao --lib memory`
  `direnv exec . env CARGO_TARGET_DIR=.cache/cargo-target/memory-host-staging cargo clippy -p xiuxian-wendao --lib --features julia -- -D warnings`
  `direnv exec . env CARGO_TARGET_DIR=.cache/cargo-target/memory-host-staging cargo test -p xiuxian-wendao-julia memory`
  `direnv exec . env CARGO_TARGET_DIR=.cache/cargo-target/memory-host-staging cargo test -p xiuxian-memory-engine projection`
- result:
  the new host staging seam is green under focused tests and strict clippy,
  while the lower Julia memory contract and memory-engine projection proofs
  remain green in the same isolated validation lane
- archived the completed ExecPlan at
  `.cache/agent/execplans/archives/wendao-memory-julia-episodic-recall-host-staging.md`

## Wendao Memory Julia Gate Score Host Staging

- opened the bounded ExecPlan at
  `.cache/agent/execplans/wendao-memory-julia-gate-score-host-staging.md`
  to bridge Rust-owned gate evidence into the staged Julia
  `memory_gate_score` request contract without moving gate policy or lifecycle
  authority out of Rust
- added `packages/rust/crates/xiuxian-wendao/src/memory/julia/gate_score.rs`
  and wired it through `packages/rust/crates/xiuxian-wendao/src/memory/mod.rs`
  plus `packages/rust/crates/xiuxian-wendao/src/lib.rs`, so the host-owned
  memory bridge now covers both `episodic_recall` and `memory_gate_score`
- added `MemoryGateScoreEvidenceRow`,
  `build_memory_gate_score_request_rows_from_evidence(...)`,
  `build_memory_gate_score_request_batch_from_evidence(...)`,
  `build_memory_gate_score_evidence_row_from_episode(...)`, and
  `build_memory_gate_score_evidence_row_from_store(...)`
  in `xiuxian-wendao`, mapping Rust-owned `MemoryUtilityLedger` plus
  `MemoryLifecycleState` into typed `xiuxian-wendao-julia`
  `memory_gate_score` request rows and one Arrow request batch
- kept ownership boundaries narrow:
  `xiuxian-memory-engine` still owns ledger construction and gate policy
  evaluation, `xiuxian-wendao-julia` still owns the typed Arrow contract, and
  `xiuxian-wendao` owns only the host staging bridge
- added focused host regression coverage proving:
  evidence rows map into request rows,
  evidence rows materialize the staged request batch,
  invalid memory ids are rejected,
  and a real `EpisodeStore` lookup can feed the new gate-score staging seam
- package docs for `xiuxian-wendao` now describe the host-only memory staging
  seam as covering both read-model projection and gate-evidence staging under
  `src/memory/julia/`
- validation for this slice under the default target:
  `direnv exec . cargo test -p xiuxian-wendao --lib memory --features julia`
  `direnv exec . cargo clippy -p xiuxian-wendao --lib --features julia -- -D warnings`
  `direnv exec . cargo test -p xiuxian-wendao-julia memory`
  `direnv exec . cargo check -p xiuxian-wendao --lib --features julia`
  `git diff --check -- packages/rust/crates/xiuxian-wendao/src/lib.rs packages/rust/crates/xiuxian-wendao/src/memory packages/rust/crates/xiuxian-wendao/README.md docs/GTD/DAILY_2026_04_06.md .cache/agent/execplans/wendao-memory-julia-gate-score-host-staging.md`
- result:
  the new host-owned `memory_gate_score` staging seam is green under default
  repo gates, and this slice no longer relies on `CARGO_TARGET_DIR`
- archived the completed ExecPlan at
  `.cache/agent/execplans/archives/wendao-memory-julia-gate-score-host-staging.md`

## Wendao Memory Julia Plan Tuning Host Staging

- opened the bounded ExecPlan at
  `.cache/agent/execplans/wendao-memory-julia-plan-tuning-host-staging.md`
  to bridge Rust-owned recall-tuning inputs into the staged Julia
  `memory_plan_tuning` request contract without moving tuning authority or
  active host config mutation out of Rust
- added `packages/rust/crates/xiuxian-wendao/src/memory/julia/plan_tuning.rs`
  and wired it through `packages/rust/crates/xiuxian-wendao/src/memory/mod.rs`
  plus `packages/rust/crates/xiuxian-wendao/src/lib.rs`, so the host-owned
  memory bridge now covers `episodic_recall`, `memory_gate_score`, and
  `memory_plan_tuning`
- added `MemoryPlanTuningInputs`,
  `build_memory_plan_tuning_request_rows_from_inputs(...)`, and
  `build_memory_plan_tuning_request_batch_from_inputs(...)`
  in `xiuxian-wendao`, mapping canonical
  `xiuxian-memory-engine::RecallPlanTuning` inputs into typed
  `xiuxian-wendao-julia` `memory_plan_tuning` request rows and one Arrow
  request batch
- kept ownership boundaries narrow:
  `xiuxian-memory-engine` still owns recall-tuning semantics and feedback-bias
  normalization, `xiuxian-wendao-julia` still owns the typed Arrow contract,
  and `xiuxian-wendao` owns only the host staging bridge
- added focused host regression coverage proving:
  canonical tuning inputs map into request rows,
  canonical tuning inputs materialize the staged request batch,
  invalid host tuning shapes are rejected,
  and feedback bias is normalized through the host staging seam
- package docs for `xiuxian-wendao` now describe the host-only memory staging
  seam as covering read-model projection, gate evidence staging, and
  plan-tuning input staging under `src/memory/julia/`
- validation for this slice under the default target:
  `direnv exec . cargo fmt -p xiuxian-wendao`
  `direnv exec . cargo test -p xiuxian-wendao --lib memory --features julia`
  `direnv exec . cargo clippy -p xiuxian-wendao --lib --features julia -- -D warnings`
  `direnv exec . cargo test -p xiuxian-wendao-julia memory`
  `direnv exec . cargo check -p xiuxian-wendao --lib --features julia`
  `git diff --check -- packages/rust/crates/xiuxian-wendao/src/lib.rs packages/rust/crates/xiuxian-wendao/src/memory packages/rust/crates/xiuxian-wendao/README.md docs/GTD/DAILY_2026_04_06.md .cache/agent/execplans/wendao-memory-julia-plan-tuning-host-staging.md`
- result:
  the new host-owned `memory_plan_tuning` staging seam is green under default
  repo gates and keeps Rust as the authoritative tuning owner
- archived the completed ExecPlan at
  `.cache/agent/execplans/archives/wendao-memory-julia-plan-tuning-host-staging.md`

## Wendao Memory Julia Calibration Host Staging

- opened the bounded ExecPlan at
  `.cache/agent/execplans/wendao-memory-julia-calibration-host-staging.md`
  to bridge Rust-owned calibration job inputs into the staged Julia
  `memory_calibration` request contract without moving calibration artifact
  adoption or host authority out of Rust
- added `packages/rust/crates/xiuxian-wendao/src/memory/julia/calibration.rs`
  and wired it through `packages/rust/crates/xiuxian-wendao/src/memory/mod.rs`
  plus `packages/rust/crates/xiuxian-wendao/src/lib.rs`, so the host-owned
  memory bridge now covers all four staged RFC profiles:
  `episodic_recall`, `memory_gate_score`, `memory_plan_tuning`, and
  `memory_calibration`
- added `MemoryCalibrationInputs`,
  `build_memory_calibration_request_rows_from_inputs(...)`, and
  `build_memory_calibration_request_batch_from_inputs(...)`
  in `xiuxian-wendao`, mapping Rust-owned calibration job inputs into typed
  `xiuxian-wendao-julia` `memory_calibration` request rows and one Arrow
  request batch
- kept ownership boundaries narrow:
  `xiuxian-wendao-julia` still owns the typed Arrow contract, while
  `xiuxian-wendao` owns only the host staging bridge and Rust still owns any
  calibration artifact adoption or activation decision
- added focused host regression coverage proving:
  canonical calibration inputs map into request rows,
  canonical calibration inputs materialize the staged request batch,
  invalid host inputs are rejected,
  and optional scenario-pack values are trimmed before crossing the Arrow
  boundary
- package docs for `xiuxian-wendao` now describe the host-only memory staging
  seam as covering all four staged memory-family request profiles under
  `src/memory/julia/`
- validation for this slice under the default target:
  `direnv exec . cargo fmt -p xiuxian-wendao`
  `direnv exec . cargo test -p xiuxian-wendao --lib memory --features julia`
  `direnv exec . cargo clippy -p xiuxian-wendao --lib --features julia -- -D warnings`
  `direnv exec . cargo test -p xiuxian-wendao-julia memory`
  `direnv exec . cargo check -p xiuxian-wendao --lib --features julia`
  `git diff --check -- packages/rust/crates/xiuxian-wendao/src/lib.rs packages/rust/crates/xiuxian-wendao/src/memory packages/rust/crates/xiuxian-wendao/README.md docs/GTD/DAILY_2026_04_06.md .cache/agent/execplans/wendao-memory-julia-calibration-host-staging.md`
- result:
  the new host-owned `memory_calibration` staging seam is green under default
  repo gates and keeps calibration artifact adoption on the Rust side
- archived the completed ExecPlan at
  `.cache/agent/execplans/archives/wendao-memory-julia-calibration-host-staging.md`

## Wendao Memory Julia Health Route Propagation

- opened the bounded ExecPlan at
  `.cache/agent/execplans/wendao-memory-julia-health-route-propagation.md`
  to propagate one optional family-level `health_route` from
  `memory.julia_compute` runtime config into memory-family bindings and
  manifest rows
- extended
  `packages/rust/crates/xiuxian-wendao-runtime/src/config/memory/julia/compute.rs`
  so `MemoryJuliaComputeRuntimeConfig` now carries optional `health_route`
  state and resolves it from `memory.julia_compute.health_route`
- updated
  `packages/rust/crates/xiuxian-wendao-julia/src/memory/runtime.rs`
  so staged memory-family `PluginCapabilityBinding` projection now forwards
  the configured `health_route` after normalized Flight-route validation
- updated
  `packages/rust/crates/xiuxian-wendao-julia/src/memory/manifest.rs`
  so staged manifest rows now carry the same family-level `health_route`
  instead of always projecting `None`
- kept this slice family-level and additive:
  no per-profile health-route map, no live execution semantics, and no host
  authority change were introduced
- validation for this slice under the default target:
  `direnv exec . cargo fmt -p xiuxian-wendao-runtime -p xiuxian-wendao-julia`
  `direnv exec . cargo test -p xiuxian-wendao-runtime --lib memory_julia_compute_runtime --features transport`
  `direnv exec . cargo test -p xiuxian-wendao-julia memory`
  `direnv exec . cargo clippy -p xiuxian-wendao-runtime --lib --features transport -- -D warnings`
  `direnv exec . cargo clippy -p xiuxian-wendao-julia --all-targets --all-features -- -D warnings`
- result:
  the memory-family capability contract now carries one optional family-level
  `health_route` from runtime config through binding and manifest projection
  without widening into per-profile transport policy
- archived the completed ExecPlan at
  `.cache/agent/execplans/archives/wendao-memory-julia-health-route-propagation.md`

## Wendao Memory Julia Compat Thinning

- opened the bounded ExecPlan at
  `.cache/agent/execplans/wendao-memory-julia-compat-thinning.md`
  to keep the `xiuxian-wendao` memory-family Julia lane visibly compat-only
  instead of growing a flattened product-crate API surface
- added
  `packages/rust/crates/xiuxian-wendao/src/memory/julia/common.rs`
  so the four host staging profiles now share one internal helper layer for
  required text normalization, optional text trimming, numeric validation,
  embedding validation, and staged host error construction
- updated the four profile modules under
  `packages/rust/crates/xiuxian-wendao/src/memory/julia/`
  to reuse that shared helper layer instead of carrying duplicated local
  `required_text`, `optional_text`, `staging_error`, and validation helpers
- narrowed the public seam by removing the flattened memory-family Julia
  re-exports from `packages/rust/crates/xiuxian-wendao/src/memory/mod.rs` and
  `packages/rust/crates/xiuxian-wendao/src/lib.rs`, so the compat path now
  stays under `xiuxian_wendao::memory::julia::*`
- kept ownership boundaries unchanged:
  `xiuxian-wendao` still owns host staging, `xiuxian-wendao-julia` still owns
  typed Arrow contracts, and no runtime or execution semantics moved in this
  slice
- validation for this slice under the default target:
  `direnv exec . cargo fmt -p xiuxian-wendao`
  `direnv exec . cargo test -p xiuxian-wendao --lib memory --features julia`
  `direnv exec . cargo clippy -p xiuxian-wendao --lib --features julia -- -D warnings`
  `direnv exec . cargo check -p xiuxian-wendao --lib --features julia`
  `git diff --check -- packages/rust/crates/xiuxian-wendao/src/lib.rs packages/rust/crates/xiuxian-wendao/src/memory packages/rust/crates/xiuxian-wendao/README.md docs/GTD/DAILY_2026_04_06.md .cache/agent/execplans/wendao-memory-julia-compat-thinning.md`
- result:
  the host staging seam stays in `xiuxian-wendao`, but it is now thinner:
  shared helper logic moved into one internal module and the public path is
  narrowed to `xiuxian_wendao::memory::julia::*`
- archived the completed ExecPlan at
  `.cache/agent/execplans/archives/wendao-memory-julia-compat-thinning.md`

## Wendao Memory Julia Layering Docs

- opened the bounded ExecPlan at
  `.cache/agent/execplans/wendao-memory-julia-layering-docs.md`
  to make the approved memory-family Julia layering explicit in Wendao package
  docs and reduce the chance of future host-side drift
- updated `packages/rust/crates/xiuxian-wendao/README.md` so the package
  position section and Julia Arrow adapter section both state the same memory
  ownership chain:
  `WendaoMemory.jl` owns compute, `xiuxian-wendao-julia` owns the Julia ABI,
  `xiuxian-wendao` stays a thin host adapter, and Rust retains authoritative
  state and mutation decisions
- updated
  `packages/rust/crates/xiuxian-wendao/docs/06_roadmap/418_julia_plugin_first_rollout.md`
  so the plugin-first rollout doc now carries an explicit memory-family rule:
  new Julia ABI semantics belong in `xiuxian-wendao-julia`, not in
  `xiuxian-wendao`
- updated `docs/01_core/memory/architecture.md` so the canonical memory note
  now names the same package split directly instead of only pointing at the
  RFC
- validation for this slice under the default target:
  `direnv exec . cargo test -p xiuxian-wendao --lib memory --features julia`
  `direnv exec . cargo clippy -p xiuxian-wendao --lib --features julia -- -D warnings`
  `direnv exec . cargo check -p xiuxian-wendao --lib --features julia`
  `git diff --check -- packages/rust/crates/xiuxian-wendao/README.md packages/rust/crates/xiuxian-wendao/docs/06_roadmap/418_julia_plugin_first_rollout.md docs/01_core/memory/architecture.md docs/GTD/DAILY_2026_04_06.md .cache/agent/execplans/wendao-memory-julia-layering-docs.md`
- result:
  Wendao package docs now state the memory-family layering directly, so the
  expected ownership chain is explicit in both the package README and the
  Julia plugin-first rollout doc instead of living only in the RFC
- archived the completed ExecPlan at
  `.cache/agent/execplans/archives/wendao-memory-julia-layering-docs.md`

## Wendao Memory Julia Host Adapter Extraction

- opened the bounded ExecPlan at
  `.cache/agent/execplans/wendao-memory-julia-host-adapter-extraction.md`
  to remove the last local memory Julia helper namespace from
  `xiuxian-wendao` and shift that owner cut into `xiuxian-wendao-julia`
- moved the four memory-family host-staging helpers into the new plugin-owned
  feature folder
  `packages/rust/crates/xiuxian-wendao-julia/src/memory/host/`, so the Julia
  plugin crate now owns both the typed memory ABI and the host-adapter helpers
  over Rust memory-engine projections, gate evidence, tuning inputs, and
  calibration job inputs
- removed `packages/rust/crates/xiuxian-wendao/src/memory/julia/` and the
  product-crate memory seam, and dropped the direct `xiuxian-memory-engine`
  dependency from `packages/rust/crates/xiuxian-wendao/Cargo.toml` because
  the product crate no longer stages Julia memory request batches itself
- updated package docs so the owner chain is explicit:
  `xiuxian-wendao-julia` now owns `memory::host::*`, while `xiuxian-wendao`
  only consumes that seam
- validation for this slice under the default target:
  `direnv exec . cargo fmt -p xiuxian-wendao-julia -p xiuxian-wendao`
  `direnv exec . cargo test -p xiuxian-wendao-julia memory`
  `direnv exec . cargo clippy -p xiuxian-wendao-julia --all-targets --all-features -- -D warnings`
  `direnv exec . cargo check -p xiuxian-wendao --lib --features julia`
  `git diff --check -- packages/rust/crates/xiuxian-wendao/Cargo.toml packages/rust/crates/xiuxian-wendao/src/lib.rs packages/rust/crates/xiuxian-wendao/README.md packages/rust/crates/xiuxian-wendao/docs/06_roadmap/418_julia_plugin_first_rollout.md packages/rust/crates/xiuxian-wendao-julia/Cargo.toml packages/rust/crates/xiuxian-wendao-julia/src/memory packages/rust/crates/xiuxian-wendao-julia/README.md docs/01_core/memory/architecture.md docs/GTD/DAILY_2026_04_06.md .cache/agent/execplans/wendao-memory-julia-host-adapter-extraction.md`
- result:
  the memory-family owner cut is now real in code, not only in docs:
  `xiuxian-wendao-julia` owns both the Julia ABI and the plugin-owned
  `memory::host::*` adapter helpers, while `xiuxian-wendao` no longer keeps a
  local memory Julia seam
- archived the completed ExecPlan at
  `.cache/agent/execplans/archives/wendao-memory-julia-host-adapter-extraction.md`

## Wendao Memory Julia Transport

- opened the bounded ExecPlan at
  `.cache/agent/execplans/wendao-memory-julia-transport.md`
  to add the missing plugin-owned runtime-facing Flight transport seam for the
  memory-family Julia compute lane
- added the new feature folder
  `packages/rust/crates/xiuxian-wendao-julia/src/memory/transport/` with
  `client.rs`, `process.rs`, and `fetch.rs`, so the plugin crate now owns:
  runtime-config-driven Flight client construction,
  staged request or response validation dispatch,
  Flight roundtrip execution, and
  typed fetch helpers for
  `episodic_recall`,
  `memory_gate_score`,
  `memory_plan_tuning`, and
  `memory_calibration`
- kept the slice on the RFC boundary:
  runtime config ownership stays in `xiuxian-wendao-runtime`,
  authoritative memory state stays in `xiuxian-memory-engine`,
  and the new transport helpers do not add host mutation, fallback-policy, or
  lifecycle authority
- added a minimal plugin-local Flight echo service in the transport test
  module so the new fetch helpers prove one real Arrow Flight roundtrip for
  each staged memory profile without incorrectly reusing the runtime's
  rerank-only `WendaoFlightService`
- validation for this slice under the default target:
  `direnv exec . cargo fmt -p xiuxian-wendao-julia`
  `direnv exec . cargo test -p xiuxian-wendao-julia memory`
  `direnv exec . cargo clippy -p xiuxian-wendao-julia --all-targets --all-features -- -D warnings`
  `direnv exec . cargo check -p xiuxian-wendao --lib --features julia`
  `git diff --check -- packages/rust/crates/xiuxian-wendao-julia/Cargo.toml packages/rust/crates/xiuxian-wendao-julia/src/memory packages/rust/crates/xiuxian-wendao-julia/README.md docs/GTD/DAILY_2026_04_06.md .cache/agent/execplans/wendao-memory-julia-transport.md`
- result:
  `xiuxian-wendao-julia` now owns the full memory-family runtime seam
  expected by the RFC:
  typed contracts,
  manifest projection,
  host-adapter inputs,
  and runtime-facing Flight transport helpers all live in the Julia plugin
  crate instead of growing back into `xiuxian-wendao`
- archived the completed ExecPlan at
  `.cache/agent/execplans/archives/wendao-memory-julia-transport.md`

## Wendao Memory Julia Downcall

- opened the bounded ExecPlan at
  `.cache/agent/execplans/wendao-memory-julia-downcall.md`
  to add the missing plugin-owned composition layer above memory host staging
  and memory Flight transport
- added the new feature folder
  `packages/rust/crates/xiuxian-wendao-julia/src/memory/downcall/`
  with one composition helper per staged memory profile:
  `episodic_recall`,
  `memory_gate_score`,
  `memory_plan_tuning`,
  and `memory_calibration`
- kept the ownership boundary narrow:
  `xiuxian-memory-engine` still owns read models and gate or tuning semantics,
  `xiuxian-wendao-runtime` still owns generic Flight config and negotiation,
  and `xiuxian-wendao-julia` now owns the plugin-side composition seam that
  stitches `memory::host::*` together with `memory::transport::*`
- added one internal memory test-support module under
  `packages/rust/crates/xiuxian-wendao-julia/src/memory/test_support.rs`
  so the new downcall tests can prove real per-profile Arrow Flight
  roundtrips without pushing rerank-specific runtime service assumptions into
  the memory lane
- package docs for `xiuxian-wendao-julia` now state the intended thin-host
  layering directly:
  host consumers should prefer `memory::downcall::*` instead of manually
  composing `memory::host::*` and `memory::transport::*` in `xiuxian-wendao`
- validation for this slice under the default target:
  `direnv exec . cargo fmt -p xiuxian-wendao-julia`
  `direnv exec . cargo test -p xiuxian-wendao-julia memory`
  `direnv exec . cargo clippy -p xiuxian-wendao-julia --all-targets --all-features -- -D warnings`
  `direnv exec . cargo check -p xiuxian-wendao --lib --features julia`
  `git diff --check -- packages/rust/crates/xiuxian-wendao-julia/src/memory packages/rust/crates/xiuxian-wendao-julia/README.md docs/GTD/DAILY_2026_04_06.md .cache/agent/execplans/wendao-memory-julia-downcall.md`
- result:
  the memory-family plugin crate now owns the full thin stack expected by the
  RFC:
  typed contracts,
  host-adapter inputs,
  runtime transport,
  and one plugin-owned downcall composition surface all live in
  `xiuxian-wendao-julia`, leaving `xiuxian-wendao` free to stay a thin host
  consumer
- archived the completed ExecPlan at
  `.cache/agent/execplans/archives/wendao-memory-julia-downcall.md`

## Wendao Memory Julia Thin Host Bridge

- opened the bounded ExecPlan at
  `.cache/agent/execplans/wendao-memory-julia-thin-host-bridge.md`
  to add the first formal host-facing `memory::julia` bridge in
  `xiuxian-wendao` without reintroducing host-local memory profile logic
- updated the active blueprint
  `.cache/agent/blueprints/wendao_memory_julia_compute_abi.md`
  so it explicitly governs the thin `xiuxian-wendao` bridge surface in
  addition to `xiuxian-memory-engine`, `xiuxian-wendao-runtime`, and
  `xiuxian-wendao-julia`
- added the new feature-gated files
  `packages/rust/crates/xiuxian-wendao/src/memory/mod.rs` and
  `packages/rust/crates/xiuxian-wendao/src/memory/julia.rs`
  so `xiuxian-wendao` now exposes only one thin namespace:
  `xiuxian_wendao::memory::julia`
- kept the owner cut strict:
  `xiuxian-wendao-julia` still owns the real memory-family host staging,
  typed contracts, transport, and downcall composition, while
  `xiuxian-wendao` now only re-exports the host-facing types and downcall
  entry points that product callers need
- updated `packages/rust/crates/xiuxian-wendao/README.md` and
  `packages/rust/crates/xiuxian-wendao/docs/06_roadmap/418_julia_plugin_first_rollout.md`
  so the docs now say the correct public path is
  `xiuxian_wendao::memory::julia`, not direct product-local host staging
- validation for this slice under the default target:
  `direnv exec . cargo fmt -p xiuxian-wendao -p xiuxian-wendao-julia`
  `direnv exec . cargo test -p xiuxian-wendao --lib memory --features julia`
  `direnv exec . cargo clippy -p xiuxian-wendao --lib --features julia -- -D warnings`
  `direnv exec . cargo test -p xiuxian-wendao-julia memory`
  `direnv exec . cargo check -p xiuxian-wendao --lib --features julia`
  `git diff --check -- packages/rust/crates/xiuxian-wendao/src/lib.rs packages/rust/crates/xiuxian-wendao/src/memory packages/rust/crates/xiuxian-wendao/README.md packages/rust/crates/xiuxian-wendao/docs/06_roadmap/418_julia_plugin_first_rollout.md docs/GTD/DAILY_2026_04_06.md .cache/agent/blueprints/wendao_memory_julia_compute_abi.md .cache/agent/execplans/wendao-memory-julia-thin-host-bridge.md`
- result:
  the product crate now has the explicit host-facing entry the RFC wanted:
  `xiuxian_wendao::memory::julia`
  but all Julia-specific memory ABI meaning still stays below that bridge in
  `xiuxian-wendao-julia`
- archived the completed ExecPlan at
  `.cache/agent/execplans/archives/wendao-memory-julia-thin-host-bridge.md`

## Wendao Memory Julia Runtime Resolution

- opened the bounded ExecPlan at
  `.cache/agent/execplans/wendao-memory-julia-runtime-resolution.md`
  to turn `xiuxian_wendao::memory::julia` from a pure bridge shell into a real
  host runtime-assembly seam
- extracted one crate-private generic settings feature folder at
  `packages/rust/crates/xiuxian-wendao/src/settings/`
  so the merged Wendao settings loader is no longer trapped under the
  link-graph-only namespace
- kept the existing public link-graph override names stable by turning
  `packages/rust/crates/xiuxian-wendao/src/link_graph/runtime_config/settings/mod.rs`
  into thin wrappers over the new crate-private settings seam
- extended
  `packages/rust/crates/xiuxian-wendao/src/memory/julia.rs`
  with:
  `resolve_memory_julia_compute_runtime()`
  and
  `resolve_memory_julia_compute_bindings()`
  so the product crate now resolves merged `memory.julia_compute` host state
  and generic plugin bindings through the thin memory bridge
- added focused host tests proving
  `memory.julia_compute` override config can resolve:
  the current runtime surface and
  the four staged generic capability bindings
- updated Wendao package docs so the boundary is explicit:
  `xiuxian_wendao::memory::julia` is now the host-facing runtime assembly seam,
  while all Julia ABI meaning still lives in `xiuxian-wendao-julia`
- validation for this slice under the default target:
  `direnv exec . cargo fmt -p xiuxian-wendao`
  `direnv exec . cargo test -p xiuxian-wendao --lib memory --features julia`
  `direnv exec . cargo clippy -p xiuxian-wendao --lib --features julia -- -D warnings`
  `direnv exec . cargo test -p xiuxian-wendao-julia memory`
  `direnv exec . cargo check -p xiuxian-wendao --lib --features julia`
  `git diff --check -- packages/rust/crates/xiuxian-wendao/src/lib.rs packages/rust/crates/xiuxian-wendao/src/settings packages/rust/crates/xiuxian-wendao/src/link_graph/runtime_config/settings/mod.rs packages/rust/crates/xiuxian-wendao/src/memory packages/rust/crates/xiuxian-wendao/README.md packages/rust/crates/xiuxian-wendao/docs/06_roadmap/418_julia_plugin_first_rollout.md docs/GTD/DAILY_2026_04_06.md .cache/agent/execplans/wendao-memory-julia-runtime-resolution.md`
- result:
  the memory-family bridge is no longer only a naming shell:
  `xiuxian_wendao::memory::julia` now resolves the current merged runtime and
  materializes the staged generic plugin capability bindings, while
  `xiuxian-wendao-julia` still owns the Julia ABI logic itself
- archived the completed ExecPlan at
  `.cache/agent/execplans/archives/wendao-memory-julia-runtime-resolution.md`

## Wendao Memory Julia Runtime-Resolved Downcalls

- opened the bounded ExecPlan at
  `.cache/agent/execplans/wendao-memory-julia-runtime-downcalls.md`
  to turn the runtime-assembly seam into a directly callable thin host seam
- updated
  `packages/rust/crates/xiuxian-wendao-julia/src/memory/host/mod.rs`
  so the plugin-owned host surface now re-exports the memory-engine carrier
  types that host callers need to construct staged inputs:
  `MemoryProjectionRow`, `MemoryUtilityLedger`,
  `MemoryLifecycleState`, and `RecallPlanTuning`
- replaced the product-crate top-level raw downcall re-exports in
  `packages/rust/crates/xiuxian-wendao/src/memory/julia.rs`
  with runtime-resolved async wrappers that:
  resolve merged `memory.julia_compute` settings,
  reject disabled runtime config early,
  and then delegate directly into plugin-owned `xiuxian-wendao-julia`
  downcalls
- added focused host-seam tests plus local test support under
  `packages/rust/crates/xiuxian-wendao/src/memory/julia/test_support.rs`
  to prove all four staged profiles can roundtrip through
  `xiuxian_wendao::memory::julia::*` using config-backed runtime resolution:
  `episodic_recall`, `memory_gate_score`, `memory_plan_tuning`,
  and `memory_calibration`
- updated the package docs and roadmap note so they now explicitly tell host
  callers to stay on `xiuxian_wendao::memory::julia::*` for configured
  memory-family compute, while raw runtime-param downcalls remain
  plugin-owned in `xiuxian-wendao-julia`
- validation for this slice under the default target:
  `direnv exec . cargo fmt -p xiuxian-wendao -p xiuxian-wendao-julia`
  `direnv exec . cargo test -p xiuxian-wendao --lib memory --features julia`
  `direnv exec . cargo clippy -p xiuxian-wendao --lib --features julia -- -D warnings`
  `direnv exec . cargo test -p xiuxian-wendao-julia memory`
  `direnv exec . cargo check -p xiuxian-wendao --lib --features julia`
  `git diff --check -- packages/rust/crates/xiuxian-wendao/src/memory packages/rust/crates/xiuxian-wendao-julia/src/memory/host/mod.rs packages/rust/crates/xiuxian-wendao/README.md packages/rust/crates/xiuxian-wendao/docs/06_roadmap/418_julia_plugin_first_rollout.md docs/GTD/DAILY_2026_04_06.md .cache/agent/execplans/wendao-memory-julia-runtime-downcalls.md`
- result:
  `xiuxian_wendao::memory::julia` is now the actual thin host entry for
  configured memory-family Julia compute, while all real host staging,
  transport, and decode logic still belongs to `xiuxian-wendao-julia`

## Wendao Memory Julia Host Client

- opened the bounded ExecPlan at
  `.cache/agent/execplans/wendao-memory-julia-host-client.md`
  to keep the RFC lane moving without inventing a fake product-level caller
- converted the product-crate memory Julia seam from one flat
  `packages/rust/crates/xiuxian-wendao/src/memory/julia.rs`
  file into the feature folder
  `packages/rust/crates/xiuxian-wendao/src/memory/julia/`
  with interface-only `mod.rs` plus separated `runtime.rs`, `client.rs`,
  `tests.rs`, and `test_support.rs`
- added `xiuxian_wendao::memory::julia::ComputeClient` as the primary
  configured host entry for memory-family Julia compute; it resolves enabled
  runtime state once and then delegates the four staged profile calls into the
  plugin-owned downcall layer
- kept the RFC boundary intact:
  `xiuxian-wendao` now owns runtime resolution and one configured host client,
  while `xiuxian-wendao-julia` still owns host staging, transport, typed
  contract validation, and response decoding
- added focused host-seam tests that prove `ComputeClient` can resolve
  configured runtime state and roundtrip all four staged profiles:
  `episodic_recall`, `memory_gate_score`, `memory_plan_tuning`,
  and `memory_calibration`
- updated the package docs and rollout note so host callers are now pointed at
  `xiuxian_wendao::memory::julia::ComputeClient` as the primary RFC-aligned
  entry
- validation for this slice under the default target:
  `direnv exec . cargo fmt -p xiuxian-wendao`
  `direnv exec . cargo test -p xiuxian-wendao --lib memory --features julia`
  `direnv exec . cargo clippy -p xiuxian-wendao --lib --features julia -- -D warnings`
  `direnv exec . cargo test -p xiuxian-wendao-julia memory`
  `direnv exec . cargo check -p xiuxian-wendao --lib --features julia`
  `git diff --check -- packages/rust/crates/xiuxian-wendao/src/memory packages/rust/crates/xiuxian-wendao/README.md packages/rust/crates/xiuxian-wendao/docs/06_roadmap/418_julia_plugin_first_rollout.md docs/GTD/DAILY_2026_04_06.md .cache/agent/execplans/wendao-memory-julia-host-client.md`
- result:
  the RFC lane now has a cleaner host-facing object model:
  one configured product-crate client over one plugin-owned memory compute lane

## WendaoSearch Process-Managed Live Service

- opened the bounded ExecPlan at
  `.cache/agent/execplans/wendao-search-process-managed-live-service.md`
  to add one repo-level live smoke surface for `WendaoSearch.jl` without
  moving Search service semantics into `nix`
- added one package-owned TOML live-service descriptor at
  `.data/WendaoSearch.jl/config/live/solver_demo.toml` for the standard
  `solver_demo` multi-route background service
- updated `.data/WendaoSearch.jl/src/service/descriptor.jl` and
  `.data/WendaoSearch.jl/scripts/run_search_service.jl` so
  `run_search_service.jl --config <path>` can load route, mode, host, and
  port from package-owned TOML instead of forcing those semantics into
  process-manager config
- added repo-level launch and readiness scripts at
  `scripts/channel/wendaosearch-launch.sh` and
  `scripts/channel/wendaosearch-healthcheck.sh`; those scripts now consume the
  package-owned TOML config and leave `nix/modules/process.nix` as a thin
  launcher wrapper
- added `.data/WendaoSearch.jl/scripts/probe_search_service.jl` as the
  package-owned client probe for process-managed live smoke against
  `structural_rerank`, `constraint_filter`, or `capability_manifest`
- updated package docs and rollout docs so the boundary is explicit:
  repo-level `devenv` live smoke uses the managed background service, while
  plugin and host crate live tests still self-spawn local Julia services to
  keep isolated ports and cleanup
- moved `xiuxian-wendao-julia` test helpers out of `src/` into
  `packages/rust/crates/xiuxian-wendao-julia/tests/support/` and added
  `packages/rust/crates/xiuxian-wendao-julia/tests/xiuxian-testing-gate.rs`
  backed by `xiuxian-testing`, so the crate now participates in the same test
  structure gate used by the rest of the workspace
- fixed the `wendaosearch-solver-demo` process entry so the launcher and
  readiness probe invoke `$PRJ_ROOT/scripts/channel/...` explicitly instead of
  assuming repo-root cwd; the previous relative-path form failed under
  `process-compose` task execution
- added repo-local stdout and stderr mirroring for the managed
  `wendaosearch-solver-demo` service, so background runs now write
  `.run/logs/wendaosearch-solver-demo.stdout.log` and
  `.run/logs/wendaosearch-solver-demo.stderr.log` in the same style as other
  repo-managed services
- tightened the opt-in Rust process-managed live proof so it now attaches to
  a healthy managed service when one already exists and otherwise starts the
  current `devenv` generation itself, instead of trusting an inherited
  `PC_CONFIG_FILES` value that may point at a stale process-compose graph
- confirmed the current background instance is now running against the updated
  generation again:
  `PC_CONFIG_FILES` resolves to the fresh process-compose YAML,
  `wendaosearch-solver-demo` is healthy on the managed port, and the repo-
  local stdout or stderr logs contain real service output
- validation:
  `direnv exec . julia --project=.data/WendaoSearch.jl .data/WendaoSearch.jl/scripts/probe_search_service.jl --config .data/WendaoSearch.jl/config/live/solver_demo.toml --route-name structural_rerank`
  `direnv exec . julia --project=.data/WendaoSearch.jl .data/WendaoSearch.jl/scripts/probe_search_service.jl --config .data/WendaoSearch.jl/config/live/solver_demo.toml --route-name constraint_filter`
  `direnv exec . julia --project=.data/WendaoSearch.jl .data/WendaoSearch.jl/scripts/probe_search_service.jl --config .data/WendaoSearch.jl/config/live/solver_demo.toml --route-name capability_manifest`
  `direnv exec . cargo test -p xiuxian-wendao-julia --test xiuxian-testing-gate -- --nocapture`
  `direnv exec . env RUN_PROCESS_MANAGED_WENDAOSEARCH_TEST=1 cargo test -p xiuxian-wendao-julia plugin::graph_structural_exchange::tests::fetch_graph_structural_solver_demo_rows_for_repository_against_process_managed_wendaosearch_service -- --exact --nocapture`
  `direnv exec . cargo test -p xiuxian-wendao-julia plugin::graph_structural_exchange::tests::fetch_graph_structural_solver_demo_rows_for_repository_via_manifest_discovery_against_real_wendaosearch_multi_route_service -- --exact --nocapture`
  `direnv exec . bash -lc 'tail -n 5 .run/logs/wendaosearch-solver-demo.stdout.log && tail -n 10 .run/logs/wendaosearch-solver-demo.stderr.log'`

## WendaoSearch Process-Managed Live Regression Audit

- opened the bounded ExecPlan at
  `.cache/agent/execplans/wendao-search-process-managed-live-regression.md`
  to rerun the current process-managed `solver_demo` service through package
  probes and Rust live consumers after the background launcher and log-sink
  fixes landed
- confirmed the already-running managed service is healthy before test
  execution:
  the blueprint path exists, the current background `solver_demo` port answers
  the repo healthcheck, and the process-managed log files already exist under
  `.run/logs/`
- reran the package-owned live probes against the managed service and kept all
  three green:
  `structural_rerank`, `constraint_filter`, and `capability_manifest`
- reran the grouped Rust process-managed proof against the same background
  service and kept it green:
  the opt-in test still covers explicit rerank, explicit filter, manifest-
  discovered rerank, and manifest-discovered filter against one managed
  `solver_demo` multi-route endpoint
- reran the full `xiuxian-wendao-julia` crate test gate with
  `RUN_PROCESS_MANAGED_WENDAOSEARCH_TEST=1` and kept the full pass green,
  proving the process-managed background lane coexists with the existing
  self-spawn live suites without introducing a Julia regression
- result for this slice:
  no Julia code change was required because no live regression reproduced on
  the current managed service generation
- validation:
  `direnv exec . julia --project=.data/WendaoSearch.jl .data/WendaoSearch.jl/scripts/probe_search_service.jl --config .data/WendaoSearch.jl/config/live/solver_demo.toml --route-name structural_rerank`
  `direnv exec . julia --project=.data/WendaoSearch.jl .data/WendaoSearch.jl/scripts/probe_search_service.jl --config .data/WendaoSearch.jl/config/live/solver_demo.toml --route-name constraint_filter`
  `direnv exec . julia --project=.data/WendaoSearch.jl .data/WendaoSearch.jl/scripts/probe_search_service.jl --config .data/WendaoSearch.jl/config/live/solver_demo.toml --route-name capability_manifest`
  `direnv exec . env RUN_PROCESS_MANAGED_WENDAOSEARCH_TEST=1 cargo test -p xiuxian-wendao-julia plugin::graph_structural_exchange::tests::fetch_graph_structural_solver_demo_rows_for_repository_against_process_managed_wendaosearch_service -- --exact --nocapture`
  `direnv exec . cargo test -p xiuxian-wendao-julia --test xiuxian-testing-gate -- --nocapture`
  `direnv exec . env RUN_PROCESS_MANAGED_WENDAOSEARCH_TEST=1 cargo test -p xiuxian-wendao-julia`
