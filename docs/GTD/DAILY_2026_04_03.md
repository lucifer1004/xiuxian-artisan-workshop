# Daily Log - 2026-04-03

## Wendao Package Boundary Clarification

- opened a dedicated execution plan for the package-boundary clarification lane
  at `.cache/agent/execplans/wendao-package-boundary-clarification.md`
- checked the active blueprint at
  `.cache/agent/blueprints/project_anchor_semantic_addressing.md` and recorded that
  this slice is documentation governance aligned to the existing Wendao
  migration program, not a blueprint-overriding architecture change
- reconciled the current roadmap intent from
  `409_core_runtime_plugin_surface_inventory.md`,
  `412_core_runtime_plugin_program.md`,
  `413_m2_core_extraction_package_list.md`, and
  `414_m3_runtime_extraction_package_list.md` into one contributor-facing
  ownership matrix at
  `packages/rust/crates/xiuxian-wendao/docs/06_roadmap/417_wendao_package_boundary_matrix.md`
- clarified the crate entry surfaces by updating:
  `packages/rust/crates/xiuxian-wendao/README.md`
  `packages/rust/crates/xiuxian-wendao-runtime/README.md`
  `packages/rust/crates/xiuxian-wendao-core/README.md`
  `packages/rust/crates/xiuxian-wendao/src/lib.rs`
  `packages/rust/crates/xiuxian-wendao-runtime/src/lib.rs`
  `packages/rust/crates/xiuxian-wendao-core/src/lib.rs`
- fixed the selection rule at the package level:
  `core` owns stable shared contracts,
  `runtime` owns generic host behavior,
  `wendao` owns product semantics plus the remaining migration shell
- queued focused validation on:
  `direnv exec . cargo test -p xiuxian-wendao-core --lib`
  `direnv exec . cargo test -p xiuxian-wendao-runtime --lib`
  `direnv exec . cargo test -p xiuxian-wendao --lib`
  `direnv exec . git diff --check`
- confirmed `direnv exec . git diff --check` is clean and
  `direnv exec . cargo test -p xiuxian-wendao-core --lib` passes
- recorded one existing runtime baseline blocker during focused verification:
  `direnv exec . cargo test -p xiuxian-wendao-runtime --lib` fails because the
  lib-test surface imports rerank symbols that are exported only behind
  `feature = "julia"`

## Wendao Frontend Oxlint Warning Closure

- opened a dedicated execution plan for the `wendao-frontend` Oxlint warning
  closure lane at `.cache/codex/execplans/wendao-frontend-oxlint-warning-closure.md`
- checked `.cache/agent/blueprints/` before execution and recorded that no active
  blueprint directly governs this frontend warning-closure slice
- fixed the current baseline at `377` Oxlint warnings with `0` errors under
  `.data/wendao-frontend/.oxlintrc.json`
- locked the execution order to: mechanical non-`react-perf` buckets, then
  `codeAstAnatomySections`, then the highest-density `react-perf` components
  (`RepoDiagnosticsPage`, `KnowledgePanel`, `GraphView3DStage`,
  `PropertyEditor`, `App`, `StudioBootstrap`)
- queued recurring verification on:
  `direnv exec . oxlint -c .data/wendao-frontend/.oxlintrc.json --tsconfig .data/wendao-frontend/tsconfig.json -f json .data/wendao-frontend/src .data/wendao-frontend/scripts .data/wendao-frontend/rspack.config.ts .data/wendao-frontend/vitest.config.ts`
  `direnv exec . bash -lc 'cd .data/wendao-frontend && npm test -- --run ...'`
  `git -C .data/wendao-frontend diff --check`

## Wendao DataFusion Query Folderization

- opened a dedicated execution plan for the DataFusion query modularization lane at `.cache/agent/execplans/wendao-datafusion-query-folderization.md`
- scoped the current flat-module pressure to `xiuxian-wendao-runtime/src/transport/query_contract.rs` plus the Studio search handler entry seams under `src/gateway/studio/search/handlers/`
- defined this slice to preserve the public Flight/query API through re-exports while moving the implementation to feature folders
- queued focused verification on runtime transport tests plus the Studio SQL/DataFusion query tests after the folderization lands
- landed `xiuxian-wendao-runtime/src/transport/query_contract/tests/` as a
  feature-folder contract suite split by `common/search/sql/vfs/graph/analysis/repo/rerank`
- converted the Studio Flight handler into a thin `flight/mod.rs` interface
  over `flight/provider.rs`, `flight/service.rs`, and `flight/tests/*`
- kept the public transport/query import surface stable while removing the last
  flat test and fixture pressure around the DataFusion/Flight query lane
- verified the new structure with:
  `direnv exec . cargo fmt -p xiuxian-wendao -p xiuxian-wendao-runtime`
  `direnv exec . cargo test -p xiuxian-wendao-runtime query_contract --features julia`
  `direnv exec . cargo test -p xiuxian-wendao build_studio_search_flight_service_snapshots_search_route_contracts --features julia`
  `direnv exec . cargo test -p xiuxian-wendao build_studio_search_flight_service_snapshots_workspace_route_contracts --features julia`
- continued the same folderization lane through the remaining Studio
  search-family handlers by replacing flat `ast.rs`, `attachments.rs`,
  `autocomplete.rs`, `definition.rs`, `references.rs`, and `symbols.rs` with
  dedicated feature folders
- finished the same structural cleanup for the query entry seams by replacing
  the last root barrels `flight.rs`, `sql.rs`, and `queries.rs` with
  `flight/mod.rs`, `sql/mod.rs`, and `queries/mod.rs`, and moved the SQL unit
  suite into `sql/tests.rs`
- kept each new handler `mod.rs` interface-only and pushed batch/provider/
  response/test responsibilities into leaf files instead of rebuilding another
  flat barrel
- re-verified the handler surface with:
  `direnv exec . cargo fmt -p xiuxian-wendao`
  `direnv exec . cargo test -p xiuxian-wendao gateway::studio::search::handlers:: --features julia`
  `direnv exec . git diff --check`
- observed the focused handler gate finish at `102 passed; 0 failed`
- continued the same folderization discipline into the Studio repo gateway
  contract suite by replacing the flat `tests/unit/studio_repo_sync_api.rs`
  entry with `tests/unit/studio_repo_sync_api/mod.rs`
- split the highest-pressure repo gateway query contracts into dedicated test
  modules at `tests/unit/studio_repo_sync_api/gap_reports.rs` and
  `tests/unit/studio_repo_sync_api/planner.rs`, and centralized repeated page
  and node anchoring helpers in the folder root
- re-verified the new gateway-contract structure with:
  `direnv exec . cargo fmt -p xiuxian-wendao`
  `direnv exec . cargo test -p xiuxian-wendao gateway::studio::studio_repo_sync_api_tests:: --features julia`
  `direnv exec . cargo clippy -p xiuxian-wendao --tests --features julia --message-format=short -- -D warnings`
  `direnv exec . git diff --check`
- confirmed the Studio repo gateway folder now clears local clippy pressure;
  the remaining `--tests` blockers have moved to `git/checkout`,
  `analyzers/*`, and `zhenfa_router/*`
- finished the remaining `xiuxian-wendao --tests` lint closure by clearing the
  residual `expect/expect_err` debt in `git/checkout`, `analyzers/config`,
  `analyzers/query/docs/planner`, `analyzers/service/julia_transport`,
  `enhancer/resource_registry`, and `tests/unit/zhenfa_router/*`
- moved the Julia transport Arrow fixture support from the root-level
  `src/analyzers/service/julia_transport_tests.rs` shim into the feature folder
  at `src/analyzers/service/julia_transport/fixtures.rs`, keeping
  `julia_transport/mod.rs` as the local test boundary
- verified the final crate-level test gate with:
  `direnv exec . cargo test -p xiuxian-wendao zhenfa_router:: --features julia`
  `direnv exec . cargo test -p xiuxian-wendao analyzers::service::julia_transport::tests --features julia`
  `direnv exec . cargo test -p xiuxian-wendao enhancer::resource_registry::tests --features julia`
  `direnv exec . cargo clippy -p xiuxian-wendao --tests --features julia --message-format=short -- -D warnings`
  `direnv exec . git diff --check`
- closed `xiuxian-wendao --tests` at green for this workspace slice
- continued the DataFusion SQL lane by replacing the remaining flat
  `handlers/sql/{provider,registration,tests}.rs` shape with dedicated folders
  at `handlers/sql/provider/*`, `handlers/sql/registration/*`, and
  `handlers/sql/tests/*`
- split the runtime SQL query contract one level deeper by replacing
  `transport/query_contract/query/sql.rs` with `query/sql/headers.rs` and
  `query/sql/validation.rs`, keeping `query/sql/mod.rs` interface-only
- fixed the adjacent runtime query-contract test debt exposed by full-clippy
  verification by splitting the overlong `common` route/header assertion and
  removing the remaining `expect_err` in `tests/repo.rs`
- verified the final DataFusion SQL folderization slice with:
  `direnv exec . cargo fmt -p xiuxian-wendao -p xiuxian-wendao-runtime`
  `direnv exec . cargo test -p xiuxian-wendao studio_sql_flight_provider_queries_registered_reference_occurrence_table --features julia`
  `direnv exec . cargo test -p xiuxian-wendao-runtime query_contract --features julia`
  `direnv exec . cargo clippy -p xiuxian-wendao -p xiuxian-wendao-runtime --all-targets --all-features --message-format=short -- -D warnings`
  `direnv exec . git diff --check`
- closed the touched-crate strict gate at green for both `xiuxian-wendao` and
  `xiuxian-wendao-runtime`
- continued the same folder-first query lane inside
  `search_plane/local_symbol/query/` by replacing the flat
  `autocomplete.rs`, `search.rs`, and `tests.rs` siblings with dedicated
  `autocomplete/*`, `search/*`, and `tests/*` folders
- extracted the duplicated local-symbol read-table preflight into
  `search_plane/local_symbol/query/shared/readiness.rs`, so autocomplete and
  search now share one parquet-registration boundary
- verified the local-symbol query slice with:
  `direnv exec . cargo fmt -p xiuxian-wendao`
  `direnv exec . cargo test -p xiuxian-wendao search_plane::local_symbol:: --features julia`
  `direnv exec . cargo clippy -p xiuxian-wendao --all-targets --all-features --message-format=short -- -D warnings`
  `direnv exec . git diff --check`
- continued the same folder-first query lane inside
  `search_plane/reference_occurrence/query/` by replacing the flat
  `candidates.rs`, `decode.rs`, `search.rs`, and `tests.rs` siblings with a
  dedicated `search/*` feature folder plus `tests/*`
- moved the repeated string-column and SQL literal helpers under
  `search_plane/reference_occurrence/query/search/helpers.rs`, so candidate
  collection and hit hydration no longer carry duplicate local utilities
- verified the reference-occurrence query slice with:
  `direnv exec . cargo fmt -p xiuxian-wendao`
  `direnv exec . cargo test -p xiuxian-wendao search_plane::reference_occurrence:: --features julia`
  `direnv exec . cargo clippy -p xiuxian-wendao --all-targets --all-features --message-format=short -- -D warnings`
  `direnv exec . git diff --check`
- continued the same folder-first query lane inside
  `search_plane/knowledge_section/query/` by replacing the flat
  `candidates.rs`, `errors.rs`, `ranking.rs`, `search.rs`, and `tests.rs`
  siblings with a dedicated `search/*` feature folder plus `tests/ranking.rs`
- folded the former ranking helpers into
  `search_plane/knowledge_section/query/search/helpers.rs`, so candidate
  ordering, string-column decoding, and SQL literal formatting now live with the
  search feature instead of in another root flat file
- verified the knowledge-section query slice with:
  `direnv exec . cargo fmt -p xiuxian-wendao`
  `direnv exec . cargo test -p xiuxian-wendao search_plane::knowledge_section:: --features julia`
  `direnv exec . cargo clippy -p xiuxian-wendao --all-targets --all-features --message-format=short -- -D warnings`
  `direnv exec . git diff --check`
- continued the same folder-first query lane inside
  `search_plane/attachment/query/` by replacing the flat
  `scan.rs`, `scoring.rs`, `search.rs`, `types.rs`, and `tests.rs` siblings
  with a dedicated `search/*` feature folder plus `tests/{fixtures,ranking,search}.rs`
- moved the repeated utf8-column and SQL literal helpers under
  `search_plane/attachment/query/search/helpers.rs`, so scan-time candidate
  collection and payload hydration no longer keep parallel local implementations
- verified the attachment query slice with:
  `direnv exec . cargo fmt -p xiuxian-wendao`
  `direnv exec . cargo test -p xiuxian-wendao search_plane::attachment:: --features julia`
  `direnv exec . cargo clippy -p xiuxian-wendao --all-targets --all-features --message-format=short -- -D warnings`
  `direnv exec . git diff --check`
- continued the same folder-first query lane inside
  `search_plane/repo_entity/query/` by replacing the flat
  `execution.rs`, `prepare.rs`, `search.rs`, `types.rs`, and `tests.rs`
  siblings with dedicated `search/*`, `results/*`, and
  `tests/{fixtures,ranking,results}.rs` folders
- separated the generic repo-entity search pipeline from the typed
  module/symbol/example/import query surfaces, so the shared execution path now
  lives under `search/*` while each typed query keeps its own file in
  `results/*`
- verified the repo-entity query slice with:
  `direnv exec . cargo fmt -p xiuxian-wendao`
  `direnv exec . cargo test -p xiuxian-wendao search_plane::repo_entity:: --features julia`
  `direnv exec . cargo clippy -p xiuxian-wendao --all-targets --all-features --message-format=short -- -D warnings`
  `direnv exec . git diff --check`
- continued the same folder-first query lane inside
  `search_plane/repo_content_chunk/query/` by replacing the flat
  `candidates.rs`, `error.rs`, `execution.rs`, `helpers.rs`, `scan.rs`,
  `search.rs`, and `tests.rs` siblings with a dedicated `search/*` feature
  folder plus `tests/ranking.rs`
- moved the repo-content chunk candidate model, scan SQL, engine column
  helpers, and route orchestration under `search/*`, so the root query module
  now only exposes the public seam plus test-only ranking helpers
- verified the repo-content chunk query slice with:
  `direnv exec . cargo fmt -p xiuxian-wendao`
  `direnv exec . cargo test -p xiuxian-wendao search_plane::repo_content_chunk:: --features julia`
  `direnv exec . cargo clippy -p xiuxian-wendao --all-targets --all-features --message-format=short -- -D warnings`
  `direnv exec . git diff --check`
- extended the Studio DataFusion SQL provider from a raw registered-table
  executor into a request-scoped discovery surface with stable
  `wendao_sql_tables` and `wendao_sql_columns` catalogs
- switched SQL execution to a fresh request-scoped `SearchEngineContext`, so
  system catalog registration no longer mutates the shared search-plane SQL
  session
- split SQL catalog registration into
  `handlers/sql/registration/catalog/{tables,columns}.rs` and added structured
  table/column descriptors in `handlers/sql/registration/table.rs`
- normalized catalog column type names so the SQL-visible surface reports
  stable `Utf8` instead of leaking backend-specific `Utf8View`
- expanded the SQL app metadata payload with `column_catalog_table_name` and
  `registered_column_count`
- added focused SQL catalog coverage in
  `handlers/sql/tests/catalog.rs` and shared SQL fixture helpers in
  `handlers/sql/tests/fixtures.rs`
- verified the SQL catalog expansion slice with:
  `direnv exec . cargo fmt -p xiuxian-wendao`
  `direnv exec . cargo test -p xiuxian-wendao gateway::studio::search::handlers::sql::tests --features julia`
  `direnv exec . cargo clippy -p xiuxian-wendao --all-targets --all-features --message-format=short -- -D warnings`
  `direnv exec . git diff --check`
