# Daily Log - 2026-03-24

### [DONE] Feature: Xiuxian LLM local-llm feature topology

- **Objective**: make `xiuxian-llm` default to the LiteLLM provider surface only, and group `mistral` plus DeepSeek OCR local runtime dependencies under a `local-llm` opt-in feature.
- **Update**:
  - `packages/rust/crates/xiuxian-llm/Cargo.toml` now defaults to `provider-litellm` only
  - `local-llm` is now an umbrella over `mistral.rs` and the `vision-dots` local OCR subfeature via `local-llm = ["mistral.rs", "vision-dots"]`
  - `mistral` crate exports and Mistral SDK integration tests are now gated behind `feature = "mistral.rs"`
  - feature smoke tests now pin both the default LiteLLM-only build and the explicit `local-llm` umbrella build
- **Validation Executed**:
  - `CARGO_TARGET_DIR=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/cargo-target/xiuxian-llm-feature-topology direnv exec . cargo check -p xiuxian-llm`
  - `CARGO_TARGET_DIR=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/cargo-target/xiuxian-llm-feature-topology direnv exec . cargo check -p xiuxian-llm --features local-llm`
  - `CARGO_TARGET_DIR=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/cargo-target/xiuxian-llm-feature-topology direnv exec . cargo test -p xiuxian-llm --test feature_default_litellm_only`
  - `CARGO_TARGET_DIR=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/cargo-target/xiuxian-llm-feature-topology direnv exec . cargo test -p xiuxian-llm --features local-llm --test feature_local_llm`
  - `CARGO_TARGET_DIR=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/cargo-target/xiuxian-llm-feature-topology direnv exec . cargo test -p xiuxian-llm --features local-llm --test mistral_runtime`
  - `CARGO_TARGET_DIR=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/cargo-target/xiuxian-llm-feature-topology direnv exec . cargo test -p xiuxian-llm --features local-llm --test mistral_sdk_embedding`
  - `CARGO_TARGET_DIR=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/cargo-target/xiuxian-llm-feature-topology direnv exec . cargo test -p xiuxian-llm --features 'mistral.rs' --test mistral_runtime`

### [DONE] Feature: Wendao Python bindings folder unification

- **Objective**: relocate the optional `pybindings` surface into `packages/rust/crates/xiuxian-wendao/src/pybindings/` and keep the crate root free of scattered Python binding gates.
- **Update**:
  - the Python binding implementation tree has been moved under `src/pybindings/`
  - the central binding entry point now owns the module registration surface
  - the default build and the explicit `--features pybindings` build both pass the targeted `xiuxian-testing-gate` smoke tests

### [IN-PROGRESS] Feature: Repo Index Real-Workspace Stabilization

- **Objective**: validate the Studio repo-index lane against the live SciML workspace and reduce mass remote-sync failure bursts under real GitHub pressure.
- **Update**:
  - bounded real-workspace sampling now runs against `.data/wendao-frontend/wendao.toml`
  - with `XIUXIAN_WENDAO_REPO_INDEX_SYNC_CONCURRENCY=1`, the first live minute sampled `177 total / 81 ready / 5 unsupported / 0 failed`
  - managed remote sync now retries transient transport failures, requeues one retryable batch-level sync failure, and only marks `Syncing` after the sync permit is actually acquired
  - `/api/repo/index/status` now also exposes `syncConcurrencyLimit`, and a fresh live sample against `.data/wendao-frontend/wendao.toml` reported `177 total / 23 ready / 0 unsupported / 0 failed / 4 indexing / syncConcurrencyLimit=1` immediately after enqueue
  - the gateway `OpenAPI` route inventory now covers both `POST /api/repo/index` and `GET /api/repo/index/status`, closing the earlier contract gap where repo-index runtime payloads were live but not tracked by the bundled artifact
  - first-minute A/B sampling is now complete: `sync=1` reached `46 ready / 3 unsupported / 0 failed` at `t+60s`, while `sync=2` reached `45 ready / 3 unsupported / 0 failed`; the earlier GitHub socket-burst failure family did not reproduce in either lane
  - the three live `unsupported` rows remain stable and expected: `StokesDiffEq.jl`, `SundialsBuilder`, and `TensorFlowDiffEq.jl` all report `missing Project.toml`
  - the `177` repo-index total is now explained by config shape, not silent drops: `.data/wendao-frontend/wendao.toml` contains `179` `link_graph.projects.*` entries, but `kernel` and `main` are local link-graph-only projects with `plugins = []`, so repo-index correctly excludes them
  - the frontend status bar now separates adaptive analysis concurrency from the remote-sync semaphore (`targetConcurrency` vs `syncConcurrencyLimit`) and explains the `kernel/main` exclusion as link-graph-only `plugins = []` projects instead of making the repo total look silently short
  - the frontend repo-index snapshot now also groups repeated `unsupported` layout failures into structured reason summaries with repo previews, so the status popover can show high-frequency causes such as `missing Project.toml` together with the affected repo ids without forcing the operator to read the full repo issue preview
  - FileTree now also renders a visible repo-index diagnostics panel for current unsupported and failed repositories, so repo-index problems are no longer discoverable only through the status-bar hover surface
  - failed repo rows inside the new FileTree diagnostics panel now expose a direct retry action wired to `POST /api/repo/index { repo, refresh: true }`, so a single bad repo can be requeued without restarting the whole index lane
  - when multiple failed repos are present, the FileTree diagnostics panel now also exposes `Retry all failed`, which batch-requeues each failed repo through the same existing enqueue route and then refreshes `/api/repo/index/status`
  - unsupported reason rows inside the same diagnostics panel can now expand to show the full repo list plus a concrete fix hint; `missing Project.toml` now explicitly suggests adding `Project.toml` or moving docs-only projects to link-graph-only `plugins=[]`
  - the diagnostics panel now also supports in-place filtering across `All`, `Unsupported`, `Failed`, and individual unsupported reasons, so mixed repo-index problems can be narrowed in the sidebar before taking retry or remediation actions
  - the FileTree diagnostics panel now persists its open state, selected diagnostics filter, selected unsupported reason, and expanded unsupported rows in local storage, so repo-index triage context survives remounts and reloads
  - the same sidebar diagnostics surface now also exposes a full diagnostics drawer for long repo-index result sets, with persisted drawer state and full failed-repo coverage beyond the preview cap
  - the full diagnostics drawer now also groups failed repos by failure reason and supports `Retry filtered failed`, so repo-index remediation can target a single failure family instead of requeueing the whole failed set
  - the same full diagnostics drawer now also exposes an unsupported remediation manifest for the current unsupported filter and can copy that manifest to the clipboard, so structural triage no longer depends on manually transcribing repo ids
  - clicking the repo-index chip in the status bar now opens a dedicated repo diagnostics page with `#repo-diagnostics` hash persistence, and the selected diagnostics filter / reason focus are now also encoded into the hash so operators can share or restore the same triage slice on refresh/back navigation
  - the diagnostics page now also supports repo-focused detail selection; clicking a failed or unsupported repo opens a detail pane and persists that repo focus into the hash via `repo=<id>`, so repo-level triage survives refresh and share links
  - the selected repo detail pane now also exposes page-level remediation actions: failed repos can be retried directly from the diagnostics page, and any selected repo can copy a single-repo diagnostics brief for external handoff
  - the same selected repo detail pane now also supports `Download remediation bundle`, exporting a richer Markdown handoff bundle that includes the current diagnostics brief plus unsupported-repo remediation material when available
  - the diagnostics page header now also supports `Download current triage bundle`, exporting the current filtered failed/unsupported slice as a Markdown handoff bundle with runtime context, link-graph-only exclusions, failure-family grouping, failure-family remediation guidance, and batch `Failure presets` so operators can hand off a whole triage batch without selecting repos one by one
  - the same triage bundle now also includes batch `Unsupported presets`, so grouped layout failures such as `missing Project.toml` export concrete remediation blocks instead of only a grouped manifest
  - unsupported repo detail panes now also expose `Copy fix template`, so common structural failures such as `missing Project.toml` can be handed off as a concrete remediation template
  - `missing Project.toml` detail panes now also expose `Copy link-graph-only preset`, producing a ready-to-edit `link_graph.projects."<repo>"` TOML snippet with `plugins = []`
  - the same `missing Project.toml` path now also exposes `Download config patch` for the selected repo and `Download current config patch` for the current unsupported slice, so link-graph-only remediation can be handed off as raw TOML instead of Markdown only
  - selected failed repo panes now also render failure-family-specific guidance, so transient transport failures and auth/access failures no longer collapse into the same generic retry hint
  - selected failed repo panes now also expose `Copy failure preset`, exporting a structured failure-family handoff with retryability, guidance, and category-specific next steps such as reduced sync concurrency or credential checks
  - failed diagnostics now also expose `Download failure plan` for the selected repo and `Download current failure plan` for the current failed slice, so failure-family remediation can be handed off as raw TOML instead of clipboard-only presets
  - raw TOML exports now also carry workspace-aware headers such as `scope_filter`, current reason focus, `current_repo`, and concurrency context, so offline handoff preserves the operational context in which the export was generated
  - failed-reason grouping is now canonicalized before filtering and export, so repo-specific GitHub transport failures such as `failed to connect to github.com: Can't assign requested address` collapse into one failure family and one failure-plan preset instead of one preset per repo
  - failure-plan and triage exports now also carry stable `reason_key` fields plus bounded `sample_errors`, so automation/ops consumers can key off machine-readable families without losing raw error exemplars
  - failure presets and failure plans now also carry machine-readable action-preset fields (`action_key`, `retry_scope`, `env_overrides`, `follow_up_checks`), so retryable transport failures and manual auth/access checks can be handed off as executable remediation steps instead of guidance text alone
  - selected failed repo panes now also expose `Copy remediation command`, so the repo-level action preset is no longer trapped inside TOML exports and can be copied directly as an operator shell snippet
  - selected failed repo panes now also expose `Download remediation command`, so the same repo-level failure action preset can be handed off as a runnable shell script instead of clipboard-only command text
  - the diagnostics page header now also exposes `Copy current remediation command` for the active failed slice, so a canonical failure family can be handed off as one batch shell snippet instead of one repo at a time
  - the same diagnostics page header now also exposes `Download current remediation command`, so the active failed slice can be handed off as a runnable shell script instead of clipboard-only command text
  - downloaded remediation scripts now also carry workspace-context comment headers such as `scope_filter`, current reason focus, selected repo, and concurrency state, so the shell artifact remains self-describing outside the diagnostics page
  - the same downloaded remediation scripts now also carry `generated_at` plus canonical `reason_key` header fields, so the exported shell artifact can be correlated back to one failure family without reopening the UI
  - downloaded remediation scripts now also summarize canonical `action_key` and `retry_scope` in the top-level header, so operators can see the intended remediation mode before reading the shell body itself
  - downloaded remediation scripts now also summarize `env_overrides` and `follow_up_checks` in the top-level header, so the required environment knobs and post-run validation steps are visible before the shell body itself
  - that same top-level script header now aggregates those fields across the exported scope, so current-slice remediation scripts expose one deduplicated environment-override set and one deduplicated validation checklist before the shell body itself
  - the diagnostics page now also exposes `Download current remediation runbook`, so the current failed slice can be handed off as one Markdown pack containing scope summary, failure plan, and the runnable remediation script instead of separate downloads
  - selected failed repo panes now also expose `Download remediation runbook`, so repo-level handoff has the same single-artifact path instead of separate failure-plan and remediation-script downloads
  - the diagnostics page header now also exposes `Download current diagnostics pack`, so the current triage bundle, current failed-slice remediation runbook, and current unsupported config patch can be handed off together as one Markdown artifact, and that pack now includes a top-level operator summary (`action_keys`, `follow_up_checks`, `suggested_sync_concurrency_limit`, `next_steps`) instead of forcing operators to reconstruct current-scope actions from separate exports
  - that same operator summary is now also rendered directly on the diagnostics page as a current-slice action card, and its failure-family action rows plus unsupported-slice CTA now jump the page directly into the matching failed family or unsupported slice instead of leaving the summary read-only
  - once a failed family or unsupported slice is focused, the diagnostics page now also renders a dedicated focused action bar so retry, remediation-command, runbook, and config-patch actions collapse into one slice-local control surface instead of staying distributed across the full-page header
  - focused failed/unsupported slices now also suppress the duplicate page-header command/download buttons, so the focused action bar is the single slice-local action surface instead of one of two parallel control rows

### [DONE] Feature: Qianji Studio bundle warning closure

- **Objective**: remove the frontend asset-size warnings around the oversized `vendors-async-misc.js` and `vendors.js` bundles without regressing the repo diagnostics workflow.
- **Update**:
  - `CodeSyntaxHighlighter` now uses a curated Shiki fine-grained bundle via `@shikijs/core` + explicit language/theme loaders instead of the full `shiki` entrypoint
  - unsupported languages now fall back directly to plain text instead of attempting to hydrate the full bundled grammar set
  - the Rspack `markdown` cache group now applies to `chunks = "all"`, which pulls the markdown/rendering runtime out of the general initial `vendors.js` baseline
  - the current production build now lands with no asset-size warnings; the resulting chunk sizes are `vendors.js = 988K`, `vendors-async-misc.js = 1.9M`, and `markdown.js = 1.9M`
- **Validation Executed**:
  - `cd .data/wendao-frontend && direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop npm test -- src/components/code-syntax/CodeSyntaxHighlighter.test.tsx src/components/code-syntax/index.test.ts`
  - `cd .data/wendao-frontend && direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop npm run build`
  - repo diagnostics no longer live inside `FileTree`; the sidebar diagnostics panel and preview wrapper are gone, and the status-bar `#repo-diagnostics` page is now the canonical triage surface
  - the diagnostics feature now owns its own `src/components/repoDiagnostics/` boundary for `copy`, `state`, `RepoDiagnosticsDrawer`, `useRepoDiagnostics`, and locale-specific remediation labels instead of continuing to hang under `src/components/panels/FileTree/`
  - the repo diagnostics page and drawer now use the dedicated repo-diagnostics copy contract plus repo-level persisted filter/reason state helpers, so the page no longer depends on FileTree-owned labels or sidebar-only open/drawer state
  - FileTree now starts repo-index polling only when the loaded config contains at least one repo-intelligence-enabled project (`plugins.length > 0`), so link-graph-only workspaces no longer poll `/api/repo/index/status` unnecessarily
  - `FileTree.tsx` has now been split into dedicated `types`, `copy`, `treeModel`, `TreeNode`, `useFileTreeRuntime`, `useFileTreeExpansion`, `useFileTreeStatus`, `FileTreeToolbar`, `FileTreeContent`, and `FileTreeNodes` modules, reducing the main container from `1449` lines to a thin composition layer focused on tree composition instead of diagnostics ownership
  - the earlier diagnostics controller and drawer extraction was retained, but those modules have now been moved out of `panels/FileTree/` into the app-level `repoDiagnostics/` feature folder
  - the fourth modularization slice moved VFS loading, repo-index polling, config push, repo-index refresh, and gateway retry orchestration into `useFileTreeRuntime.ts`; the container now keeps only sidebar store wiring, status sync, diagnostics composition, and tree rendering
  - the fifth modularization slice moved sidebar expansion/bootstrap into `useFileTreeExpansion.ts` and `onStatusChange` emission into `useFileTreeStatus.ts`; the container now keeps only file-selection wiring, diagnostics composition, and tree rendering
  - the sixth modularization slice moved toolbar rendering and content/error framing into `FileTreeToolbar.tsx` and `FileTreeContent.tsx`; the container now keeps only hook composition, diagnostics composition, and `TreeNode` mapping
  - the old `FileTreeDiagnostics.tsx` forwarding layer is gone, and the later `RepoDiagnosticsPanel` / `RepoDiagnosticsPreview` / sidebar-drawer surface is gone as well; diagnostics ownership now sits fully at the app-level page
- **Validation Executed**:
  - `direnv exec . cargo fmt -p xiuxian-wendao`
  - `direnv exec . cargo test -p xiuxian-wendao gateway::studio::repo_index::state::tests:: --lib`
  - `direnv exec . cargo test -p xiuxian-wendao search_plane::service::tests:: --lib`
  - `direnv exec . cargo check -p xiuxian-wendao`
  - `direnv exec . cargo test -p xiuxian-wendao bundled_gateway_openapi_document_covers_declared_route_inventory --lib -- --nocapture`
  - `direnv exec . env XIUXIAN_WENDAO_REPO_INDEX_SYNC_CONCURRENCY=1 target/debug/wendao -c .data/wendao-frontend/wendao.toml gateway start --port 9521`
  - `POST /api/repo/index`
  - `GET /api/repo/index/status`
  - `direnv exec . env XIUXIAN_WENDAO_REPO_INDEX_SYNC_CONCURRENCY=2 target/debug/wendao -c .data/wendao-frontend/wendao.toml gateway start --port 9522`
  - `POST /api/repo/index`
  - `GET /api/repo/index/status`
  - `direnv exec . npx vitest run src/components/panels/FileTree/__tests__/FileTree.test.tsx`
  - `direnv exec . npm test -- src/components/StatusBar.test.tsx src/components/panels/FileTree/repoIndexStatus.test.ts src/components/panels/FileTree/__tests__/FileTree.test.tsx`
  - `direnv exec . npm test -- src/App.test.tsx src/components/StatusBar.test.tsx src/components/panels/FileTree/repoIndexStatus.test.ts src/components/panels/FileTree/__tests__/FileTree.test.tsx`
  - `direnv exec . npm test -- src/components/RepoDiagnosticsPage.test.tsx`
  - `direnv exec . npm test -- src/components/repoDiagnostics/__tests__/state.test.ts src/components/RepoDiagnosticsPage.test.tsx`
  - `direnv exec . npm test -- src/App.test.tsx src/components/RepoDiagnosticsPage.test.tsx src/components/StatusBar.test.tsx src/components/panels/FileTree/repoIndexStatus.test.ts src/components/panels/FileTree/__tests__/FileTree.test.tsx`
  - `direnv exec . npm run build`
  - `direnv exec . git diff --check`
- **Next Bounded Step**:
  - decide whether `targetConcurrency` should remain the adaptive analysis budget or whether Studio should also surface explicit queue-depth / sync-pressure telemetry now that `syncConcurrencyLimit` is explicit
  - decide whether expanded unsupported rows should stop at fix hints or grow into explicit remediation actions / docs links
  - decide whether the new failed-reason remediation lane should stop at `Retry filtered failed` or grow into persisted failure-family presets / exportable retry manifests
  - decide whether the new unsupported remediation manifest should stay clipboard-first or grow into file download / external handoff formats
  - decide whether the new repo-focused remediation lane should stop at `Retry repo` plus remediation bundles, fix templates, and link-graph-only presets, or grow into richer repo-level docs links and remediation presets
  - decide whether the next diagnostics step should prioritize batch export/download or deeper remediation actions before any further FileTree structural work
  - if longer samples stay stable, keep the default sync ceiling at `2` and move optimization effort from transport throttling to richer repo-index observability

### [IN-PROGRESS] Feature: Zen Search Shell Closure

- **Objective**: keep Zen Search as the primary full-screen entrypoint while making canonical-path selection, graph eligibility, and preview behavior explicit in the frontend docs and tests.
- **Update**:
  - `.data/wendao-frontend/docs/01_core/101_studio_surface_protocol.md`, `03_features/205_panel_runtime_map.md`, `03_features/207_panel_handbook.md`, `03_features/208_navigation_examples.md`, and `03_features/206_testing_and_validation.md` now document canonical path normalization, note-only graph eligibility, and the Zen preview flow
  - `.data/wendao-frontend/README.md` now states that workspace-local `.data/wendao-frontend/...` paths are normalized before VFS / graph requests
  - the remaining `SearchBar` mentions in package docs are now restricted to compatibility coverage or historical references
  - Zen mode now keeps the workspace mounted behind the full-screen shell so graph and panel state survive mode switches, and the workspace test suite now pins selection-synced preview fallback explicitly
  - Zen Search is now lazy-loaded from the main app shell so a failure in the Zen subtree cannot block the normal workspace startup path
  - the Studio bootstrap surface now stays on a static blank loading shell until the app is ready or blocked, and the workspace itself stays hidden until the first VFS load completes, instead of painting a visible loading panel during gateway connection
  - the frontend entrypoint now renders `StudioBootstrap` directly instead of wrapping it in `React.StrictMode`, which removes the dev-only double-mount flash during gateway connection
- **Validation Executed**:
  - `direnv exec . npm test -- src/App.test.tsx src/components/ZenSearch/__tests__/useZenSearchPreview.test.tsx src/components/SearchBar/__tests__/searchResultNormalization.test.ts src/utils/__tests__/selectionPath.test.ts`
  - `direnv exec . npm run build`
  - `direnv exec . npm test -- src/App.test.tsx src/components/ZenSearch/__tests__/ZenSearchWindow.test.tsx src/components/ZenSearch/__tests__/ZenSearchLayout.test.tsx src/components/ZenSearch/__tests__/ZenSearchWorkspace.test.tsx`
  - `direnv exec . npm run build`
- **Next Bounded Step**:
  - inspect the remaining graph-entry surfaces only if a future runtime change reintroduces stale “graph lookup” language
  - otherwise treat the Zen Search shell closure as documentation-complete

### [IN-PROGRESS] Feature: Structured Intelligence Dashboard

- **Objective**: evolve the Zen right pane from a linear previewer into a structured, four-layer intelligence dashboard while keeping the existing Zen selection pipeline as the single source of truth.
- **Update**:
  - the current preview payload already exposes enough structured metadata to derive topology, anatomy, fragments, and relational projection without introducing a backend contract change in the first pass
  - the new ExecPlan is scoped to a frontend-only dashboard pass so the right pane can be modularized without duplicating search state
  - the structured dashboard is now wired into Zen Search, renders the four explicit layers, and reuses the existing preview payload as its intermediate representation
  - selection pivot anchors now feed back into the Zen query state instead of escaping to a separate navigation system
  - frontend tests and the bundle pass after the structured-dashboard wiring and the preview-pane null-guard fix
  - the dashboard now tolerates graph-neighbor payloads that omit a center node, keeping the Zen preview path stable under incomplete structured data
  - the saliency excerpt is now surfaced explicitly inside the fragments layer so the highest-signal content appears before the raw reader body
- the dashboard now carries an explicit header so the right pane reads as a structured intelligence surface rather than a generic preview card
  - the first topology layer now includes a clickable local connectome mini-map derived from existing graph neighbors, and that map now supports incoming/outgoing side toggles plus a side-aware focus badge and focused-anchor chip that stay synchronized with the neighboring text-anchor list, compact path trail side badge, and compact two-line top header active-anchor banner with responsive stacking on narrow screens, whose path display is shortened while the full path remains in the tooltip; the mini-map now uses a tighter viewport and node spacing, and the four structured layers now use a balanced two-row grid with slightly tighter padding so the dashboard reads as a symmetric projection surface and exposes each layer as a named landmark region with a compact header layer index
  - code shown in DirectReader, the AST anatomy view, and the structured fragment cards now uses a shared Shiki-backed frontend syntax highlighter instead of the custom TS-family tokenizer
  - The AST anatomy declaration card now also renders structured signature parts with explicit parameter and return lanes, the logic block section now uses explicit validation, execution, and return columns, and the symbol overlay is grouped into local, external, and pivot-anchor sections, so code anatomy is no longer a flat snippet
  - DirectReader source-mode line inspection now uses the same shared Shiki-backed highlighter, so source lines and fenced code blocks share one token palette across supported languages
  - the legacy TS-family tokenizer implementation and compatibility test have now been deleted, so the Shiki-backed renderer is the only live syntax-highlighting path
  - the code-syntax barrel now has a dedicated regression test that pins the absence of any legacy TS-family export, so the old API cannot be reintroduced silently
  - the Shiki renderer now also normalizes common shorthand aliases such as `py`, `rs`, and `jl` to the canonical Python, Rust, and Julia language keys, so those code blocks no longer fall back to plain text when the source language arrives in abbreviated form
  - the Shiki renderer now also falls back to the current file suffix when a code block arrives with the generic `code` label or no explicit language tag, so unlabeled `.py`, `.rs`, and `.jl` content still receives syntax coloring
  - DirectReader now treats code files as source views from the file suffix even without a line target, while keeping markdown-like content in rich mode
  - preview loading indicators now stay suppressed once content is already visible, which reduces the reconnect flash during gateway-linked content reuse
  - MainView content and diagram tabs now show a loading fallback while a selected file is still hydrating, so reconnects no longer flash the empty hint before file content arrives
- **Validation Executed**:
  - `direnv exec . npm test -- src/components/ZenSearch/__tests__/ZenSearchPreviewPane.test.tsx src/components/ZenSearch/StructuredDashboard/__tests__/StructuredIntelligenceDashboard.test.tsx src/components/ZenSearch/__tests__/ZenSearchWindow.test.tsx src/components/ZenSearch/__tests__/ZenSearchLayout.test.tsx src/components/ZenSearch/__tests__/ZenSearchWorkspace.test.tsx src/components/ZenSearch/__tests__/ZenSearchPreviewShell.test.tsx src/components/ZenSearch/__tests__/ZenSearchPreviewEntity.test.tsx src/components/ZenSearch/__tests__/ZenSearchPreviewPlaceholder.test.tsx src/components/ZenSearch/__tests__/ZenSearchPreviewContent.test.tsx src/components/ZenSearch/__tests__/useZenSearchPreview.test.tsx src/App.test.tsx`
  - `direnv exec . npm run build`
- **Next Bounded Step**:
  - if the dashboard later needs richer entity or graph data than the existing preview payload exposes, open a separate backend entity plan rather than extending this first-pass frontend scope

### [IN-PROGRESS] Feature: Wendao Studio Search Plane

- **Objective**: keep tightening the Studio status surface so the new bounded streaming search plane exposes machine-readable query pressure without forcing clients to rescan per-corpus telemetry.
- **Update**:
  - `gateway/studio/types/search_index.rs` is now split into `gateway/studio/types/search_index/` with `definitions.rs`, `conversions.rs`, `status.rs`, and `tests.rs`, while the public search-index DTO façade remains unchanged
  - `search_plane/service/core/status.rs` is now split into `search_plane/service/core/status/` with `helpers.rs`, `runtime.rs`, `compaction.rs`, `repo.rs`, and `tests.rs`, while the public `SearchPlaneService` surface remains unchanged
  - `analyzers/service/projection/planner.rs` is now split into `analyzers/service/projection/planner/` with `api.rs`, `scoring.rs`, `workset.rs`, and `tests.rs`, while the public planner surface remains unchanged
  - the next bounded target is `search_plane/service/core/maintenance.rs`
  - `/api/search/index/status` `queryTelemetrySummary` now groups recent bounded-query telemetry by raw `scope` hint in addition to the existing response-wide totals
  - the new `scopes` buckets preserve per-scope source counts, latest capture time, scan/match/result totals, recall-window ceilings, and trim/drop totals
  - `local_symbol` autocomplete and the broader response-level summary can now be distinguished as opaque lanes such as `search` and `autocomplete` without changing any search response payloads
  - the formal `search_index_status` performance gate now requires repo-scoped warmup to leave a `gateway-sync` scope bucket in `queryTelemetrySummary`, and failure output now includes a compact scope-pressure digest instead of a generic “summary missing” message
  - the same formal `search_index_status` performance gate now also parses `maintenanceSummary`, rejects internally inconsistent aggregate maintenance pressure when it appears, and includes formatted maintenance pressure in diagnostics for missing telemetry or scope failures
  - formal gateway perf support now also wraps budget failures with the latest status diagnostics, so the `search_index_status` gate appends `maintenanceSummary` and scope-pressure context even when the failure comes from the perf budget itself rather than the endpoint assertions
  - the same budget-failure diagnostics now cover the generic formal gateway cases too: warm-cache repo/module/symbol/example/projected-page/code-search gates append `uri`, compact `/api/search/index/status` pressure, and compact `/api/repo/index/status` pressure when a perf budget miss occurs
  - formal gateway perf reports now also persist those diagnostics on success: the saved JSON report metadata carries `gateway_uri`, compact search-index pressure, compact repo-index pressure, and any case-specific extras such as `statusGatePressure`
  - the same metadata path now also covers the ignored real-workspace samples, so successful large-workspace `repo_index_status` and `code_search` reports keep `gateway_uri`, compact search-index pressure, compact repo-index pressure, and sample-specific extras such as `minRepos` or `workspaceQuery`
  - the earlier local `slow` run was traced to the warmup path, not the status endpoint budget itself: the formal status gate now warms repo-scoped telemetry through `GatewayPerfFixture::warm_repo_scope_query(...)`, which hits `SearchPlaneService` directly and keeps the same `gateway-sync` scope bucket without paying for the full HTTP `intent` handler chain
  - `local_symbol` no longer uses a topology-only build fingerprint: its build fingerprint now walks the actual searchable file set and hashes normalized path + file size + mtime, so touching a scanned source/markdown file now forces a new build identity instead of incorrectly reusing the last epoch forever
  - the same metadata-based fingerprint policy now covers `reference_occurrence`, `attachment`, and `knowledge_section` through one shared `search_plane::project_fingerprint` helper, so all local-file corpora now respond to scanned file metadata changes instead of only project topology changes
- **Validation Executed**:
  - `direnv exec . cargo fmt -p xiuxian-wendao`
  - `direnv exec . cargo test -p xiuxian-wendao gateway::studio::types::search_index::tests:: --lib`
  - `direnv exec . cargo check -p xiuxian-wendao --lib --keep-going`
  - `direnv exec . git diff --check`
  - `direnv exec . cargo nextest run -p xiuxian-wendao -E 'test(response_counts_track_phase_and_compaction_state) | test(response_query_telemetry_summary_remains_empty_without_corpus_telemetry) | test(response_query_telemetry_summary_preserves_source_mapping) | test(response_query_telemetry_summary_groups_rows_by_scope_hint) | test(search_index_status_endpoint_returns_idle_corpora_snapshot)'`
  - `direnv exec . cargo check -p xiuxian-wendao`
  - `direnv exec . rustfmt packages/rust/crates/xiuxian-wendao/tests/performance/support/gateway.rs packages/rust/crates/xiuxian-wendao/tests/performance/gateway_search.rs`
  - `direnv exec . cargo nextest run -p xiuxian-wendao --features performance --test xiuxian-testing-gate -E 'test(gateway_perf_budget_profile_maps_runner_labels) | test(gateway_perf_budget_lookup_applies_case_overrides) | test(gateway_perf_budget_lookup_ignores_invalid_values) | test(query_telemetry_scope_reads_specific_scope_bucket) | test(describe_query_telemetry_scopes_formats_scope_pressure) | test(performance::gateway_search::search_index_status_perf_gate_reports_query_telemetry_summary_formal_gate)'`
  - `direnv exec . cargo nextest run -p xiuxian-wendao --features performance --test xiuxian-testing-gate -E 'test(maintenance_summary_reads_aggregate_counts) | test(describe_maintenance_summary_formats_aggregate_pressure) | test(query_telemetry_scope_reads_specific_scope_bucket) | test(describe_query_telemetry_scopes_formats_scope_pressure) | test(performance::gateway_search::search_index_status_perf_gate_reports_query_telemetry_summary_formal_gate)'`
  - `direnv exec . cargo nextest run -p xiuxian-wendao --features performance --test xiuxian-testing-gate -E 'test(assert_gateway_perf_budget_with_diagnostics_appends_context) | test(maintenance_summary_reads_aggregate_counts) | test(describe_maintenance_summary_formats_aggregate_pressure) | test(query_telemetry_scope_reads_specific_scope_bucket) | test(describe_query_telemetry_scopes_formats_scope_pressure) | test(performance::gateway_search::search_index_status_perf_gate_reports_query_telemetry_summary_formal_gate)'`
  - `direnv exec . cargo nextest run -p xiuxian-wendao --features performance --test xiuxian-testing-gate -E 'test(describe_search_index_status_payload_formats_aggregate_surface) | test(describe_repo_index_status_payload_formats_runtime_counts) | test(assert_gateway_perf_budget_with_diagnostics_appends_context) | test(performance::gateway_search::repo_module_search_perf_gate_reports_warm_cache_latency_formal_gate) | test(performance::gateway_search::search_index_status_perf_gate_reports_query_telemetry_summary_formal_gate)'`
  - `direnv exec . cargo nextest run -p xiuxian-wendao --features performance --test xiuxian-testing-gate -E 'test(describe_gateway_perf_case_diagnostics_formats_compact_context) | test(attach_gateway_perf_diagnostics_persists_report_metadata) | test(describe_search_index_status_payload_formats_aggregate_surface) | test(describe_repo_index_status_payload_formats_runtime_counts) | test(assert_gateway_perf_budget_with_diagnostics_appends_context) | test(performance::gateway_search::repo_module_search_perf_gate_reports_warm_cache_latency_formal_gate) | test(performance::gateway_search::search_index_status_perf_gate_reports_query_telemetry_summary_formal_gate)'`
  - `direnv exec . cargo nextest run -p xiuxian-wendao --features performance --test xiuxian-testing-gate -E 'test(collect_gateway_case_diagnostics_with_extra_merges_runtime_pressure) | test(describe_gateway_perf_case_diagnostics_formats_compact_context) | test(attach_gateway_perf_diagnostics_persists_report_metadata) | test(performance::gateway_search::repo_module_search_perf_gate_reports_warm_cache_latency_formal_gate) | test(performance::gateway_search::search_index_status_perf_gate_reports_query_telemetry_summary_formal_gate)'`
  - `direnv exec . cargo check -p xiuxian-wendao --features performance --tests`
  - `direnv exec . rustfmt packages/rust/crates/xiuxian-wendao/src/gateway/studio/perf_support.rs packages/rust/crates/xiuxian-wendao/tests/performance/gateway_search.rs`
  - `direnv exec . cargo nextest run -p xiuxian-wendao --features performance --test xiuxian-testing-gate -E 'test(performance::support::gateway::query_telemetry_scope_reads_specific_scope_bucket) | test(performance::support::gateway::describe_query_telemetry_scopes_formats_scope_pressure) | test(performance::gateway_search::search_index_status_perf_gate_reports_query_telemetry_summary_formal_gate)'`
  - `direnv exec . cargo check -p xiuxian-wendao --features performance --tests`
  - `direnv exec . rustfmt packages/rust/crates/xiuxian-wendao/src/gateway/studio/search/source_index/ast.rs packages/rust/crates/xiuxian-wendao/src/gateway/studio/search/source_index/filters.rs packages/rust/crates/xiuxian-wendao/src/gateway/studio/search/source_index/mod.rs packages/rust/crates/xiuxian-wendao/src/search_plane/local_symbol/build.rs`
  - `direnv exec . cargo nextest run -p xiuxian-wendao fingerprint_projects_changes_when_scanned_file_metadata_changes local_symbol_query_reads_hits_from_published_epoch search_ast`
  - `direnv exec . cargo check -p xiuxian-wendao`
  - `direnv exec . rustfmt packages/rust/crates/xiuxian-wendao/src/search_plane/project_fingerprint.rs packages/rust/crates/xiuxian-wendao/src/search_plane/mod.rs packages/rust/crates/xiuxian-wendao/src/search_plane/local_symbol/build.rs packages/rust/crates/xiuxian-wendao/src/search_plane/reference_occurrence/build.rs packages/rust/crates/xiuxian-wendao/src/search_plane/attachment/build.rs packages/rust/crates/xiuxian-wendao/src/search_plane/knowledge_section/build.rs`
  - `direnv exec . cargo nextest run -p xiuxian-wendao fingerprint_projects_changes_when_scanned_file_metadata_changes fingerprint_source_projects_ignores_skipped_directories fingerprint_note_projects_changes_when_note_metadata_changes search_ast search_references search_attachments search_knowledge`
  - `direnv exec . cargo check -p xiuxian-wendao`
- **Next Bounded Step**:
  - decide whether `scope` should remain a raw opaque hint or be split into explicit lane/repo dimensions before more clients start branching on it
  - continue the new file-fingerprint incremental path into `reference_occurrence`, or deepen `local_symbol` from staging-copy reuse into direct per-file `delete + merge_insert`
  - decide whether the first incremental lane should stay “reuse unchanged rows from the previous active epoch” or immediately push into direct staged row mutation once Lance-side merge ergonomics are ready

### [IN-PROGRESS] Feature: Wendao Studio Search Plane

- **Objective**: replace Studio request-path search hot spots with a background-built Lance/Arrow/Valkey search plane owned by `xiuxian-vector` and orchestrated from `xiuxian-wendao`.
- **Update**:
  - `local_symbol` is now the first local-file corpus with true file-level incremental extraction instead of coarse whole-corpus reparsing
  - `search_plane::project_fingerprint` now exposes scanned-file metadata and `SearchFileFingerprint` generation, so `local_symbol` persists per-file fingerprint state in the search-plane cache
  - local-symbol staging publishes now reuse unchanged rows from the previous active epoch through `xiuxian-vector` async streaming batch scans, while only changed files are reparsed through `build_ast_hits_for_file(...)`
  - changed and deleted files now map to path-scoped row replacement during staging-table assembly, which preserves epoch isolation for readers while removing the old “reparse the whole corpus on any edit” behavior
  - `reference_occurrence` now follows the same file-fingerprint incremental model: changed and deleted source paths are computed per build, only changed files are rescanned for token occurrences, and unchanged rows stream forward from the previous active epoch into the new staging epoch
  - the old `build_ast_index(...) + WalkDir + full replace` path is gone from `reference_occurrence`; file-local AST extraction now supplies the “skip definitions, keep references” rule without forcing a full project AST prepass
  - `knowledge_section` now follows the same note-file incremental model: changed and deleted note paths are computed per build, only changed notes are reparsed into doc/section rows, and unchanged rows stream forward from the previous active epoch into the new staging epoch
  - `knowledge_section` no longer rebuilds from a fresh `WalkDir` + full-table replace on every note edit; the staging table now reuses unchanged published rows and only appends changed-note rows before publish
  - `attachment` now follows the same note-file incremental model: changed and deleted note paths are computed per build, only changed notes are reparsed for attachment targets, and unchanged rows stream forward from the previous active epoch into the new staging epoch
  - the local-file corpus set is now complete on this first incremental architecture: `local_symbol`, `reference_occurrence`, `knowledge_section`, and `attachment` all persist per-file fingerprints and reuse unchanged published rows during staging
  - `xiuxian-vector::VectorStore` now exposes `clone_table(...)`, so a staging epoch can start from the current published Lance table without streaming every unchanged row through Wendao
  - `local_symbol` is now the first corpus on the second incremental stage: staging clones the last published epoch, deletes changed/deleted `path` rows, and `merge_insert`s changed-file batches keyed by `id`
  - unchanged `local_symbol` rows now stay inside Lance-level table cloning instead of being re-scanned and re-appended from Rust, which removes the last application-layer copy step from this corpus's incremental build path
  - `reference_occurrence` now follows the same second-stage write path: staging clones the last published epoch, deletes changed/deleted `path` rows, and `merge_insert`s changed-file batches keyed by `id`
  - unchanged `reference_occurrence` rows therefore also stay inside Lance-level table cloning instead of being streamed through Wendao during epoch assembly
  - `knowledge_section` now follows the same second-stage write path: staging clones the last published epoch, deletes changed/deleted `path` rows, and `merge_insert`s changed-note batches keyed by `id`
  - unchanged `knowledge_section` rows therefore also stay inside Lance-level table cloning instead of being streamed through Wendao during epoch assembly
  - `attachment` now follows the same second-stage write path: staging clones the last published epoch, deletes changed/deleted note `source_path` rows, and `merge_insert`s changed-note batches keyed by `id`
  - unchanged `attachment` rows therefore also stay inside Lance-level table cloning instead of being streamed through Wendao during epoch assembly
  - the local-file direct-mutation migration is now complete: `local_symbol`, `reference_occurrence`, `knowledge_section`, and `attachment` all use staged `clone + delete + merge_insert` instead of application-layer unchanged-row copying
  - `repo_content_chunk` was evaluated as the next direct-mutation frontier, but that move is intentionally deferred: the current repo-index publish contract only hands search-plane a full `documents` slice plus revision, not a stable changed/deleted file set, so true repo-backed file-level mutation would be fake incrementality today
  - instead, the maintenance slice is now deeper for the local direct-mutated corpora: `local_symbol`, `reference_occurrence`, `knowledge_section`, and `attachment` all prewarm their staging epoch before publish by touching a bounded projected scan on the freshly written Lance table
  - prewarm is now observable. Search-plane maintenance state records `last_prewarmed_at` and `last_prewarmed_epoch`, and Studio search-index DTOs map those fields through the existing maintenance surface
  - prewarm is now also a first-class status-reason lane. An unreadable indexing corpus whose current `staging_epoch` has already been prewarmed now reports `statusReason = prewarming`, while unreadable non-prewarmed builds remain `warming_up` and readable refreshes remain `refreshing`
  - `repo_content_chunk` has now crossed into honest repo-backed direct mutation. Repo indexing already publishes the full current file set with per-document `size_bytes` and `modified_unix_ms`, so search-plane can persist repo-scoped `SearchFileFingerprint`s, infer changed and deleted repo paths from the current `documents` slice, and stop rebuilding repo-content tables via full replace on every repo refresh
  - repo-backed staging for `repo_content_chunk` now mirrors the local direct-mutation path: a repo refresh either reuses the published table for revision-only publication updates, replaces into a new versioned table on first publish, or clones the previously published table, deletes changed/deleted `path` rows, and `merge_insert`s changed-document batches keyed by `id`
  - repo-content reads now follow publication metadata instead of a fixed table name. Query paths resolve the active published `table_name` from the combined repo-corpus record first and only fall back to the legacy fixed-name path when no publication record exists
  - `repo_entity` has now crossed into staged direct mutation as well. Search-plane persists repo-scoped `SearchFileFingerprint`s for entity-bearing repo paths, using source-file `size + mtime` when repo documents provide metadata and a row-hash fallback when an analyzer-emitted path is not present in the collected code-document slice
  - repo-backed staging for `repo_entity` now matches `repo_content_chunk`: first publish writes a versioned table, revision-only publication updates reuse the existing published table, and entity changes clone the previous published table, delete changed/deleted `path` rows, and `merge_insert` changed entity rows keyed by `id`
  - the repo-backed direct-mutation set is now complete for Studio search. Both `repo_content_chunk` and `repo_entity` resolve reads through published table metadata instead of fixed table names, which removes the last repo-backed whole-table replace path from request-serving corpora
  - repo-backed prewarm now follows the same contract. `repo_entity` and `repo_content_chunk` both run a bounded projected scan against the freshly written versioned table before publication, persist repo-backed maintenance into combined `SearchRepoCorpusRecord` rows, and preserve that maintenance across the subsequent publication write
  - repo-backed status synthesis now merges per-repo maintenance before deriving lifecycle reason, so unreadable repo-backed corpora can finally emit `statusReason = prewarming` when their current synthetic `staging_epoch` has already been prewarmed
  - repo-backed direct-mutation planning is now partially de-duplicated. A shared `search_plane::repo_staging` helper owns the common staged-mutation action model, revision-only refresh decision, changed/deleted path replacement plan, shared path-delete filter, and versioned table naming, while `repo_entity` and `repo_content_chunk` keep only their corpus-specific fingerprint and payload derivation
- **Validation Executed**:
  - `direnv exec . rustfmt packages/rust/crates/xiuxian-vector/src/ops/columnar.rs packages/rust/crates/xiuxian-vector/tests/test_columnar_tables.rs packages/rust/crates/xiuxian-wendao/src/search_plane/manifest.rs packages/rust/crates/xiuxian-wendao/src/search_plane/cache.rs packages/rust/crates/xiuxian-wendao/src/search_plane/project_fingerprint.rs packages/rust/crates/xiuxian-wendao/src/search_plane/mod.rs packages/rust/crates/xiuxian-wendao/src/search_plane/service/core/mod.rs packages/rust/crates/xiuxian-wendao/src/search_plane/service/core/file_fingerprints.rs packages/rust/crates/xiuxian-wendao/src/gateway/studio/search/source_index/ast.rs packages/rust/crates/xiuxian-wendao/src/gateway/studio/search/source_index/mod.rs packages/rust/crates/xiuxian-wendao/src/search_plane/local_symbol/schema.rs packages/rust/crates/xiuxian-wendao/src/search_plane/local_symbol/build.rs`
  - `direnv exec . cargo nextest run -p xiuxian-vector -E 'test(test_columnar_table_streaming_async_scan_can_append_batches)'`
  - `direnv exec . cargo nextest run -p xiuxian-wendao plan_local_symbol_build_only_reparses_changed_files local_symbol_incremental_refresh_reuses_unchanged_rows local_symbol_query_reads_hits_from_published_epoch local_symbol_autocomplete_reads_suggestions_from_published_epoch search_ast`
  - `direnv exec . cargo check -p xiuxian-vector`
  - `direnv exec . cargo check -p xiuxian-wendao`
  - `direnv exec . rustfmt packages/rust/crates/xiuxian-wendao/src/search_plane/reference_occurrence/build.rs packages/rust/crates/xiuxian-wendao/src/search_plane/reference_occurrence/schema.rs packages/rust/crates/xiuxian-wendao/src/gateway/studio/mod.rs packages/rust/crates/xiuxian-wendao/src/gateway/studio/search/mod.rs packages/rust/crates/xiuxian-wendao/src/gateway/studio/search/handlers/mod.rs packages/rust/crates/xiuxian-wendao/src/gateway/studio/search/handlers/index.rs packages/rust/crates/xiuxian-wendao/src/gateway/studio/search/source_index/ast.rs packages/rust/crates/xiuxian-wendao/src/gateway/studio/search/source_index/mod.rs`
  - `direnv exec . cargo nextest run -p xiuxian-wendao plan_reference_occurrence_build_only_reparses_changed_files reference_occurrence_incremental_refresh_reuses_unchanged_rows reference_occurrence_query_reads_hits_from_published_epoch search_references`
  - `direnv exec . cargo check -p xiuxian-wendao`
  - `direnv exec . rustfmt packages/rust/crates/xiuxian-wendao/src/search_plane/knowledge_section/build.rs packages/rust/crates/xiuxian-wendao/src/search_plane/knowledge_section/schema.rs`
  - `direnv exec . cargo nextest run -p xiuxian-wendao plan_knowledge_section_build_only_reparses_changed_notes knowledge_section_incremental_refresh_reuses_unchanged_rows search_knowledge`
  - `direnv exec . cargo check -p xiuxian-wendao`
  - `direnv exec . rustfmt packages/rust/crates/xiuxian-wendao/src/search_plane/attachment/build.rs packages/rust/crates/xiuxian-wendao/src/search_plane/attachment/schema.rs`
  - `direnv exec . cargo nextest run -p xiuxian-wendao plan_attachment_build_only_reparses_changed_notes attachment_incremental_refresh_reuses_unchanged_rows attachment_query_reads_hits_from_published_epoch search_attachments`
  - `direnv exec . cargo check -p xiuxian-wendao`
  - `direnv exec . rustfmt packages/rust/crates/xiuxian-vector/src/ops/columnar.rs packages/rust/crates/xiuxian-vector/tests/test_columnar_tables.rs packages/rust/crates/xiuxian-wendao/src/search_plane/local_symbol/build.rs`
  - `direnv exec . cargo check -p xiuxian-vector`
  - `direnv exec . cargo check -p xiuxian-wendao`
  - `direnv exec . cargo nextest run -p xiuxian-vector -E 'test(test_columnar_table_clone_delete_and_merge_insert)'`
  - `direnv exec . cargo nextest run -p xiuxian-wendao plan_local_symbol_build_only_reparses_changed_files local_symbol_incremental_refresh_reuses_unchanged_rows local_symbol_query_reads_hits_from_published_epoch search_ast`
  - `direnv exec . rustfmt packages/rust/crates/xiuxian-wendao/src/search_plane/reference_occurrence/build.rs`
  - `direnv exec . cargo nextest run -p xiuxian-wendao plan_reference_occurrence_build_only_reparses_changed_files reference_occurrence_incremental_refresh_reuses_unchanged_rows reference_occurrence_query_reads_hits_from_published_epoch search_references`
  - `direnv exec . rustfmt packages/rust/crates/xiuxian-wendao/src/search_plane/knowledge_section/build.rs`
  - `direnv exec . cargo nextest run -p xiuxian-wendao plan_knowledge_section_build_only_reparses_changed_notes knowledge_section_incremental_refresh_reuses_unchanged_rows search_knowledge`
  - `direnv exec . rustfmt packages/rust/crates/xiuxian-wendao/src/search_plane/attachment/build.rs`
  - `direnv exec . cargo nextest run -p xiuxian-wendao plan_attachment_build_only_reparses_changed_notes attachment_incremental_refresh_reuses_unchanged_rows attachment_query_reads_hits_from_published_epoch search_attachments`
  - `direnv exec . rustfmt packages/rust/crates/xiuxian-wendao/src/search_plane/status.rs packages/rust/crates/xiuxian-wendao/src/search_plane/coordinator.rs packages/rust/crates/xiuxian-wendao/src/search_plane/service/core/maintenance.rs packages/rust/crates/xiuxian-wendao/src/search_plane/local_symbol/build.rs packages/rust/crates/xiuxian-wendao/src/search_plane/reference_occurrence/build.rs packages/rust/crates/xiuxian-wendao/src/search_plane/knowledge_section/build.rs packages/rust/crates/xiuxian-wendao/src/search_plane/attachment/build.rs packages/rust/crates/xiuxian-wendao/src/search_plane/service/tests/maintenance.rs packages/rust/crates/xiuxian-wendao/src/gateway/studio/types/search_index.rs`
  - `direnv exec . cargo check -p xiuxian-wendao`
  - `direnv exec . cargo nextest run -p xiuxian-wendao publish_local_symbol_hits_records_staging_prewarm_metadata response_maps_prewarm_maintenance_metadata local_symbol_query_reads_hits_from_published_epoch reference_occurrence_query_reads_hits_from_published_epoch search_knowledge search_attachments`
  - `direnv exec . rustfmt packages/rust/crates/xiuxian-wendao/src/search_plane/status.rs packages/rust/crates/xiuxian-wendao/src/search_plane/service/helpers.rs packages/rust/crates/xiuxian-wendao/src/search_plane/service/tests/status.rs packages/rust/crates/xiuxian-wendao/src/gateway/studio/types/search_index.rs`
  - `direnv exec . cargo nextest run -p xiuxian-wendao status_marks_indexing_corpus_with_prewarmed_staging_reason response_status_reason_prefers_warming_up_over_prewarming response_maps_prewarm_maintenance_metadata response_counts_track_phase_and_compaction_state`
  - `direnv exec . cargo check -p xiuxian-wendao`
  - `direnv exec . rustfmt packages/rust/crates/xiuxian-wendao/src/gateway/studio/repo_index/types.rs packages/rust/crates/xiuxian-wendao/src/gateway/studio/repo_index/state/collect.rs packages/rust/crates/xiuxian-wendao/src/search_plane/manifest.rs packages/rust/crates/xiuxian-wendao/src/search_plane/cache.rs packages/rust/crates/xiuxian-wendao/src/search_plane/service/core/file_fingerprints.rs packages/rust/crates/xiuxian-wendao/src/search_plane/service/core/publication.rs packages/rust/crates/xiuxian-wendao/src/search_plane/repo_content_chunk/schema.rs packages/rust/crates/xiuxian-wendao/src/search_plane/repo_content_chunk/build.rs packages/rust/crates/xiuxian-wendao/src/search_plane/repo_content_chunk/query.rs packages/rust/crates/xiuxian-wendao/src/search_plane/service/tests/publication.rs packages/rust/crates/xiuxian-wendao/src/search_plane/service/tests/status.rs packages/rust/crates/xiuxian-wendao/src/search_plane/service/tests/runtime.rs packages/rust/crates/xiuxian-wendao/src/gateway/studio/search/handlers/tests.rs packages/rust/crates/xiuxian-wendao/src/gateway/studio/perf_support.rs packages/rust/crates/xiuxian-wendao/src/gateway/studio/repo_index/state/tests.rs packages/rust/crates/xiuxian-wendao/tests/unit/gateway/studio/search.rs`
  - `direnv exec . cargo nextest run -p xiuxian-wendao plan_repo_content_chunk_build_only_rewrites_changed_files plan_repo_content_chunk_build_reuses_table_for_revision_only_refresh repo_content_chunk_incremental_refresh_reuses_unchanged_rows repo_content_search_hits_find_matching_julia_source_lines`
  - `direnv exec . cargo check -p xiuxian-wendao`
  - `direnv exec . rustfmt packages/rust/crates/xiuxian-wendao/src/search_plane/repo_entity/build.rs packages/rust/crates/xiuxian-wendao/src/search_plane/repo_entity/query.rs packages/rust/crates/xiuxian-wendao/src/search_plane/repo_entity/schema.rs packages/rust/crates/xiuxian-wendao/src/search_plane/service/core/publication.rs packages/rust/crates/xiuxian-wendao/src/gateway/studio/repo_index/state/coordinator.rs packages/rust/crates/xiuxian-wendao/src/search_plane/service/tests/support.rs packages/rust/crates/xiuxian-wendao/src/search_plane/service/tests/publication.rs packages/rust/crates/xiuxian-wendao/src/gateway/studio/search/handlers/tests.rs packages/rust/crates/xiuxian-wendao/src/gateway/studio/perf_support.rs packages/rust/crates/xiuxian-wendao/src/gateway/studio/repo_index/state/tests.rs`
  - `direnv exec . cargo nextest run -p xiuxian-wendao plan_repo_entity_build_only_rewrites_changed_files plan_repo_entity_build_reuses_table_for_revision_only_refresh repo_entity_incremental_refresh_reuses_unchanged_rows search_repo_entities_reads_hits_from_published_table build_code_search_response_returns_repo_entity_hits_from_search_plane build_intent_search_response_includes_repo_entity_hits_for_debug_lookup`
  - `direnv exec . cargo check -p xiuxian-wendao`
  - `direnv exec . git diff --check`
  - `direnv exec . rustfmt packages/rust/crates/xiuxian-wendao/src/search_plane/service/core/maintenance.rs packages/rust/crates/xiuxian-wendao/src/search_plane/service/core/status.rs packages/rust/crates/xiuxian-wendao/src/search_plane/manifest.rs packages/rust/crates/xiuxian-wendao/src/search_plane/repo_content_chunk/build.rs packages/rust/crates/xiuxian-wendao/src/search_plane/repo_entity/build.rs`
  - `direnv exec . cargo check -p xiuxian-wendao`
  - `direnv exec . cargo nextest run -p xiuxian-wendao synthesize_repo_status_marks_indexing_corpus_as_prewarming_when_staging_was_prewarmed repo_content_chunk_incremental_refresh_reuses_unchanged_rows repo_entity_incremental_refresh_reuses_unchanged_rows plan_repo_content_chunk_build_only_rewrites_changed_files plan_repo_entity_build_only_rewrites_changed_files repo_content_search_hits_find_matching_julia_source_lines search_repo_entities_reads_hits_from_published_table build_code_search_response_returns_repo_entity_hits_from_search_plane build_intent_search_response_includes_repo_entity_hits_for_debug_lookup`
  - `direnv exec . git diff --check`
  - `direnv exec . rustfmt packages/rust/crates/xiuxian-wendao/src/search_plane/repo_staging.rs packages/rust/crates/xiuxian-wendao/src/search_plane/mod.rs packages/rust/crates/xiuxian-wendao/src/search_plane/repo_content_chunk/build.rs packages/rust/crates/xiuxian-wendao/src/search_plane/repo_entity/build.rs`
  - `direnv exec . cargo check -p xiuxian-wendao`
  - `direnv exec . cargo nextest run -p xiuxian-wendao plan_repo_content_chunk_build_only_rewrites_changed_files plan_repo_content_chunk_build_reuses_table_for_revision_only_refresh repo_content_chunk_incremental_refresh_reuses_unchanged_rows plan_repo_entity_build_only_rewrites_changed_files plan_repo_entity_build_reuses_table_for_revision_only_refresh repo_entity_incremental_refresh_reuses_unchanged_rows synthesize_repo_status_marks_indexing_corpus_as_prewarming_when_staging_was_prewarmed repo_content_search_hits_find_matching_julia_source_lines search_repo_entities_reads_hits_from_published_table build_code_search_response_returns_repo_entity_hits_from_search_plane build_intent_search_response_includes_repo_entity_hits_for_debug_lookup`
  - `direnv exec . git diff --check`
- **Next Bounded Step**:
  - decide whether the remaining repo-backed write paths should reuse `search_plane::repo_staging` more aggressively, or keep the shared helper scoped only to plan/action orchestration
  - decide whether repo-backed compaction and prewarm should move into a dedicated maintenance worker instead of staying inline with publish-time prewarm plus corpus-level compaction scheduling

### [IN-PROGRESS] Feature: Zen code AST anatomy projection

- **Objective**: project code-backed Zen Search results into an AST anatomy view using the existing `/api/analysis/code-ast` contract.
- **Update**:
  - Zen preview state now carries code-AST analysis data in addition to VFS content and graph neighbors
  - the Structured Intelligence Dashboard now branches code-backed results into an AST anatomy surface with declaration identity, logic block decomposition, and symbol overlays
  - code-backed topology is derived from the code-AST graph when the note graph is unavailable
  - focused tests were added for code-AST fetching, AST anatomy rendering, and dashboard integration
- **Validation Planned**:
  - run the focused ZenSearch / Structured Dashboard Vitest set
  - run `npm run build`

### [IN-PROGRESS] Wendao search-plane repo-backed maintenance

- **Objective**: push repo-backed `repo_entity` / `repo_content_chunk` maintenance beyond prewarm-only metadata into a real per-repo compaction task path.
- **Update**:
  - repo publication now computes the next combined-row `SearchMaintenanceStatus`, persists `publish_count_since_compaction` and `last_compacted_row_count`, and schedules per-repo compaction tasks keyed by `(corpus, repo_id, publication_id)`
  - repo compaction completion now writes `compaction_running=false`, `compaction_pending=false`, `publish_count_since_compaction=0`, `last_compacted_at`, and `last_compaction_reason` back into the combined repo-corpus record instead of relying on the corpus-wide coordinator
  - repo-backed status annotation now ORs local compaction-running state onto synthesized maintenance instead of overwriting it, so repo-backed corpora can keep surfacing `compacting`
- **Validation**:
  - `direnv exec . cargo check -p xiuxian-wendao`
  - `direnv exec . cargo nextest run -p xiuxian-wendao annotate_runtime_status_preserves_repo_compaction_running_from_record_maintenance repo_publication_schedules_compaction_and_resets_repo_maintenance synthesize_repo_status_marks_indexing_corpus_as_prewarming_when_staging_was_prewarmed status_with_repo_content_surfaces_ready_repo_tables status_with_repo_content_keeps_published_metadata_while_repo_refreshes search_repo_entities_reads_hits_from_published_table repo_content_search_hits_find_matching_julia_source_lines`
  - `direnv exec . git diff --check`
- **Verification Note**:
  - the narrow repo-maintenance slice is green under `cargo check`, targeted `cargo nextest`, and diff hygiene
  - the temporary `xiuxian-llm` DeepSeek vision build blocker did not reproduce under `cargo check -p xiuxian-llm --tests`; the follow-up targeted `cargo nextest -p xiuxian-wendao ...` lane completed successfully
  - repo-backed prewarm now also shares repo-maintenance task bookkeeping instead of staying as a naked inline helper call. `repo_entity` and `repo_content_chunk` still wait for prewarm completion before publication flips readable, but task claiming/release is now unified with repo compaction and a targeted regression proves the repo maintenance slot is released after publish
  - validation for the repo-prewarm bookkeeping slice is green under `direnv exec . cargo check -p xiuxian-wendao`, `direnv exec . cargo nextest run -p xiuxian-wendao repo_publication_runs_prewarm_via_maintenance_task_and_releases_repo_slot repo_publication_schedules_compaction_and_resets_repo_maintenance synthesize_repo_status_marks_indexing_corpus_as_prewarming_when_staging_was_prewarmed status_with_repo_content_surfaces_ready_repo_tables status_with_repo_content_keeps_published_metadata_while_repo_refreshes search_repo_entities_reads_hits_from_published_table repo_content_search_hits_find_matching_julia_source_lines`, and `direnv exec . git diff --check`
  - repo-backed prewarm also no longer silently skips work when an identical prewarm slot is already claimed. `prewarm_repo_table(...)` now falls back to a direct inline prewarm instead of returning early, so publication semantics remain “prewarm must really finish before readable flip” even under duplicate in-flight callers
  - validation for the slot-occupied prewarm regression is green under `direnv exec . cargo check -p xiuxian-wendao`, `direnv exec . cargo nextest run -p xiuxian-wendao repo_publication_does_not_skip_prewarm_when_slot_is_already_claimed repo_publication_runs_prewarm_via_maintenance_task_and_releases_repo_slot repo_publication_schedules_compaction_and_resets_repo_maintenance synthesize_repo_status_marks_indexing_corpus_as_prewarming_when_staging_was_prewarmed status_with_repo_content_surfaces_ready_repo_tables status_with_repo_content_keeps_published_metadata_while_repo_refreshes search_repo_entities_reads_hits_from_published_table repo_content_search_hits_find_matching_julia_source_lines`, and `direnv exec . git diff --check`
  - repo-backed maintenance now tracks active prewarm directly. `SearchMaintenanceStatus` and the Studio search-index DTO expose `prewarm_running`, repo prewarm toggles it around maintenance-task execution, and unreadable repo-backed indexing corpora can emit `prewarming` as soon as active prewarm begins instead of only after `last_prewarmed_epoch` is recorded
  - validation for the active-prewarm status slice is green under `direnv exec . cargo check -p xiuxian-wendao`, `direnv exec . cargo nextest run -p xiuxian-wendao synthesize_repo_status_marks_indexing_corpus_as_prewarming_when_prewarm_is_running synthesize_repo_status_marks_indexing_corpus_as_prewarming_when_staging_was_prewarmed repo_publication_does_not_skip_prewarm_when_slot_is_already_claimed repo_publication_runs_prewarm_via_maintenance_task_and_releases_repo_slot repo_publication_schedules_compaction_and_resets_repo_maintenance status_with_repo_content_surfaces_ready_repo_tables status_with_repo_content_keeps_published_metadata_while_repo_refreshes response_maps_prewarm_maintenance_metadata`, and `direnv exec . git diff --check`
  - the same active-prewarm lifecycle now applies to local file corpora too. `prewarm_epoch_table(...)` marks coordinator maintenance as `prewarm_running` while the staging-table scan is in flight, clears it on failure, and clears-plus-records `last_prewarmed_epoch` on success, so local first-build indexing can emit `prewarming` before prewarm completion instead of only after the completion marker lands
  - validation for the local active-prewarm symmetry slice is green under `direnv exec . cargo check -p xiuxian-wendao`, `direnv exec . cargo nextest run -p xiuxian-wendao status_marks_indexing_corpus_with_running_prewarm_reason status_marks_indexing_corpus_with_prewarmed_staging_reason synthesize_repo_status_marks_indexing_corpus_as_prewarming_when_prewarm_is_running synthesize_repo_status_marks_indexing_corpus_as_prewarming_when_staging_was_prewarmed response_maps_prewarm_maintenance_metadata`, and `direnv exec . git diff --check`
  - repo-backed prewarm/compaction now also share a single internal `RepoMaintenanceTask` envelope and dispatch path. This does not change external behavior yet, but it removes one branch of duplicated task lifecycle logic and makes the later queue-backed maintenance worker slice smaller and safer
  - validation for the repo-maintenance task-envelope slice is green under `direnv exec . cargo check -p xiuxian-wendao`, `direnv exec . cargo nextest run -p xiuxian-wendao repo_maintenance_task_key_tracks_kind repo_publication_runs_prewarm_via_maintenance_task_and_releases_repo_slot repo_publication_does_not_skip_prewarm_when_slot_is_already_claimed repo_publication_schedules_compaction_and_resets_repo_maintenance synthesize_repo_status_marks_indexing_corpus_as_prewarming_when_prewarm_is_running status_with_repo_content_surfaces_ready_repo_tables`, and `direnv exec . git diff --check`
  - repo-backed maintenance is now actually queue-backed. Search-plane holds a per-process repo-maintenance runtime with `in_flight`, `waiters`, `queue`, and `active_task`; `prewarm_repo_table(...)` now enqueues work, waits on a oneshot completion, and reuses an already queued/running task instead of duplicating work, while compaction enqueues fire-and-forget work on the same drain worker
  - the worker is drain-based rather than permanent: it starts on demand, drains queued repo maintenance tasks, updates waiter completion, and exits once the queue is empty. This avoids introducing a long-lived background loop while still removing the old “spawn one task per repo maintenance action” branching
  - validation for the queue-backed repo-maintenance worker slice is green under `direnv exec . cargo check -p xiuxian-wendao`, `direnv exec . cargo nextest run -p xiuxian-wendao repo_maintenance_task_key_tracks_kind repo_publication_runs_prewarm_via_maintenance_task_and_releases_repo_slot repo_publication_does_not_skip_prewarm_when_slot_is_already_claimed repo_publication_schedules_compaction_and_resets_repo_maintenance synthesize_repo_status_marks_indexing_corpus_as_prewarming_when_prewarm_is_running status_with_repo_content_surfaces_ready_repo_tables`, and `direnv exec . git diff --check`
  - Studio shutdown now also tears down the queue-backed repo maintenance runtime. `SearchPlaneService::stop_repo_maintenance()` rejects new repo maintenance work, clears queued/in-flight waiter state, aborts any active queue worker, and is called from `StudioState::stop_background_services()` alongside `repo_index.stop()` and `symbol_index_coordinator.stop()`
  - two new regressions lock the shutdown semantics: `stop_repo_maintenance_clears_waiters_and_aborts_worker` proves queued waiters are failed and the worker handle is released, while `prewarm_repo_table_rejects_new_tasks_after_shutdown` proves repo prewarm can no longer fall back to inline execution after shutdown has begun
  - validation for the repo-maintenance shutdown slice is green under `direnv exec . cargo check -p xiuxian-wendao`, `direnv exec . cargo nextest run -p xiuxian-wendao repo_maintenance_task_key_tracks_kind stop_repo_maintenance_clears_waiters_and_aborts_worker prewarm_repo_table_rejects_new_tasks_after_shutdown repo_publication_runs_prewarm_via_maintenance_task_and_releases_repo_slot repo_publication_does_not_skip_prewarm_when_slot_is_already_claimed repo_publication_schedules_compaction_and_resets_repo_maintenance synthesize_repo_status_marks_indexing_corpus_as_prewarming_when_prewarm_is_running status_with_repo_content_surfaces_ready_repo_tables`, and `direnv exec . git diff --check`
  - local corpus maintenance now has the same shutdown boundary. `SearchPlaneService::stop_local_maintenance()` marks local maintenance as shutting down, aborts any in-flight local compaction handles, clears the running-compaction runtime, and `stop_background_maintenance()` now shuts down both local and repo maintenance before `StudioState` drops
  - local prewarm now also refuses to continue once shutdown starts. `prewarm_epoch_table(...)` checks the local-maintenance shutdown flag before and during its bounded scan, so shutdown cannot leave a local prewarm silently running behind the stop boundary
  - local compaction is now queue-backed too. Search-plane keeps a local-maintenance runtime with `running_compactions`, `compaction_queue`, `worker_running`, `worker_handle`, and `active_compaction`; scheduled local compactions now drain through a single on-demand worker instead of one detached handle per corpus, while local prewarm remains synchronous and still gates publish
  - local maintenance status now exposes queue telemetry too. `compaction_running` is derived only from `active_compaction`, while `compaction_queue_depth` and `compaction_queue_position` are now surfaced through `SearchMaintenanceStatus` and the Studio search-index DTO so queued local compactions are no longer misreported as already running
  - three new local-maintenance regressions lock the behavior: `stop_local_maintenance_aborts_running_compaction_and_clears_runtime`, `prewarm_epoch_table_rejects_after_local_maintenance_shutdown`, and `publish_ready_and_maintain_skips_local_compaction_after_shutdown`
  - targeted queue-telemetry verification is green under `direnv exec . cargo nextest run -p xiuxian-wendao status_marks_ready_corpus_with_running_compaction_reason status_surfaces_local_compaction_queue_backlog_for_queued_corpus response_maps_local_compaction_queue_metadata response_maps_prewarm_maintenance_metadata response_counts_track_phase_and_compaction_state`
  - validation for the local-maintenance shutdown slice is green under `direnv exec . cargo check -p xiuxian-wendao`, `direnv exec . cargo nextest run -p xiuxian-wendao stop_local_maintenance_aborts_running_compaction_and_clears_runtime prewarm_epoch_table_rejects_after_local_maintenance_shutdown publish_ready_and_maintain_skips_local_compaction_after_shutdown compact_pending_corpus_updates_maintenance_status publish_local_symbol_hits_records_staging_prewarm_metadata status_marks_ready_corpus_with_running_compaction_reason repo_maintenance_task_key_tracks_kind stop_repo_maintenance_clears_waiters_and_aborts_worker prewarm_repo_table_rejects_new_tasks_after_shutdown`, and `direnv exec . git diff --check`

### [IN-PROGRESS] Feature: Wendao LanceDB Valkey hardening

- **Objective**: harden staged direct-mutation deletes in `xiuxian-wendao` and add a real-workspace OpenAPI benchmark entrypoint for the live gateway.
- **Update**:
  - a shared `search_plane::staged_mutation` helper now batches path-scoped staged deletes for `local_symbol`, `reference_occurrence`, `knowledge_section`, `attachment`, `repo_content_chunk`, and `repo_entity`, removing the earlier corpus-local `DELETE ... IN (...)` string builders
  - the staged-delete batch ceiling is now capped at `100` paths per predicate instead of `128`, which is a more conservative ceiling for large branch-switch churn and keeps DataFusion predicate parsing bounded
  - the proposed pure `merge_insert` replacement was evaluated and rejected for the current architecture because deleted-path incrementality still requires reusing the previous published table as a mutation base; the safe shape remains `clone + batched delete + merge_insert`
  - `xiuxian-vector::VectorStore::compact(...)` now performs version cleanup with Lance `retain_n_versions(2)` after file compaction, so version churn is aligned with the active industrial search-plane epoch model instead of relying on a coarse seven-day cleanup window
  - the gateway benchmark is now modularized: `scripts/benchmark_wendao_gateway_openapi.ts` stays as a thin CLI wrapper and the reusable implementation lives in `scripts/wendao_gateway_openapi_benchmark.ts`
  - the benchmark module now validates the full bundled OpenAPI artifact at `packages/rust/crates/xiuxian-wendao/resources/openapi/wendao_gateway.openapi.json`, derives real repo-backed benchmark cases from `.data/wendao-frontend/wendao.toml`, discovers live repo/page/node/gap/VFS seeds automatically, and writes timestamped TOML reports under `.data/wendao-frontend/.benchmark/`
  - OpenAPI execution is now explicit and auditable: safe GET routes run as `smoke` or `benchmark` coverage, mutating routes are recorded as `skip`, and each report persists operation-level pass/fail/skip state together with gateway health snapshots and failure samples
  - the benchmark now also includes sustained high-concurrency pressure suites instead of only one-shot latency sampling. The default profile drives `48` concurrent workers for `30s` against repo-distributed `code_search` traffic plus a mixed user hotset (`code_search`, `autocomplete`, `repo symbol search`, `repo module search`), and records `throughput_rps`, `success_rate`, `p99_ms`, and whether the suite hit the request safety cap
  - the current live stress report is now materialized at `.data/wendao-frontend/.benchmark/wendao_gateway_openapi_2026_03_24T23_00_53_220Z.toml`; it sampled `177` repos, recorded `64` bundled OpenAPI operations as `60 passed / 2 failed / 2 skipped`, and measured `repo_index_status avg=0.85ms`, `repo_sync_status avg=2.25ms`, `code_search avg=2271.59ms p95=2758.94ms non-empty=146`, `openapi_smoke avg=929.73ms p95=4572.32ms`, `stress_code_search throughput=10.25rps p99=8417.95ms success_rate=100%`, and `stress_mixed_user_hotset throughput=81.36rps p99=5782.46ms success_rate=99.96%`
  - the same stress report also captured the remaining live issues instead of hiding them: `/api/analysis/markdown` still returns runtime `MISSING_PATH` despite the bundled OpenAPI contract exposing no required query parameter, `/api/search/definition` returned `NOT_FOUND` for the sampled query, and the only observed stress failure under the mixed hotset was `GET /api/repo/module-search` for `SparseBandedMatrices.jl`, which surfaced `REPO_INTELLIGENCE_FAILED` because the managed checkout lock wait timed out under pressure
  - process-compose Valkey startup now bootstraps `.run/valkey` and `.data/valkey`, writes a pidfile for the owned listener, and only reports healthy when `INFO server` confirms the connected listener's `process_id` matches that pidfile before accepting `PING`, so `wendao-gateway` no longer inherits a false not-ready state from a transient Valkey bootstrap race or an unrelated listener on `127.0.0.1:6379`
  - the standalone Valkey lifecycle scripts now use the same pidfile/process ownership gate, so `just valkey-start`, `just valkey-status`, and `just valkey-stop` no longer treat an unrelated `127.0.0.1:6379` listener as a managed service
  - the Valkey launch surface now also exposes OCI-ready `scripts/channel/valkey-launch.sh` and `scripts/channel/valkey-healthcheck.sh`, so a future image entrypoint can reuse the same ownership gate without depending on `uv` or repository config discovery
  - `scripts/ci_scripts_smoke.sh` now includes the new Valkey launch and healthcheck entrypoints in its syntax gate, so CI catches OCI interface regressions before they reach packaging
  - the packaging step is now concrete too: `oci/valkey/Containerfile` copies the portable launch/healthcheck scripts into a Valkey image, binds `/data/valkey` and `/run/valkey`, and defaults the container to `VALKEY_PROTECTED_MODE=no` and `VALKEY_DAEMONIZE=no`
- **Validation Executed**:
  - `direnv exec . env PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 uv run pytest -c /dev/null scripts/channel/test_valkey_start.py scripts/channel/test_valkey_status.py scripts/channel/test_valkey_stop.py`
  - `direnv exec . bash -n scripts/channel/valkey-start.sh`
  - `direnv exec . bash -n scripts/channel/valkey-status.sh`
  - `direnv exec . bash -n scripts/channel/valkey-stop.sh`
  - `direnv exec . bash -n scripts/channel/valkey-common.sh`
  - `direnv exec . git diff --check -- scripts/channel/valkey-common.sh scripts/channel/valkey-start.sh scripts/channel/valkey-status.sh scripts/channel/valkey-stop.sh scripts/channel/test_valkey_start.py scripts/channel/test_valkey_status.py scripts/channel/test_valkey_stop.py docs/GTD/DAILY_2026_03_24.md`
  - `direnv exec . bash -n scripts/channel/valkey-runtime.sh`
  - `direnv exec . bash -n scripts/channel/valkey-launch.sh`
  - `direnv exec . bash -n scripts/channel/valkey-healthcheck.sh`
  - `direnv exec . bash -n scripts/ci_scripts_smoke.sh`
  - `direnv exec . env PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 uv run pytest -c /dev/null scripts/channel/test_valkey_launch.py scripts/channel/test_valkey_healthcheck.py`
  - `direnv exec . env PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 uv run pytest -c /dev/null tests/test_valkey_oci_containerfile.py`
  - `CARGO_TARGET_DIR=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/cargo-target/wendao-lancedb-valkey-hardening direnv exec . cargo test -p xiuxian-wendao staged_mutation`
  - `CARGO_TARGET_DIR=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/cargo-target/wendao-lancedb-valkey-hardening direnv exec . cargo test -p xiuxian-wendao local_symbol_incremental_refresh_reuses_unchanged_rows`
  - `CARGO_TARGET_DIR=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/cargo-target/wendao-lancedb-valkey-hardening direnv exec . cargo check -p xiuxian-wendao`
  - `CARGO_TARGET_DIR=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/cargo-target/wendao-vector-maintenance direnv exec . cargo test -p xiuxian-vector --test test_maintenance`
  - `CARGO_TARGET_DIR=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/cargo-target/wendao-vector-maintenance direnv exec . cargo test -p xiuxian-wendao staged_mutation`
  - `CARGO_TARGET_DIR=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/cargo-target/wendao-vector-maintenance direnv exec . cargo check -p xiuxian-vector`
  - `direnv exec . node --test --experimental-strip-types scripts/test_benchmark_wendao_gateway_openapi.ts`
  - `direnv exec . node --experimental-strip-types scripts/benchmark_wendao_gateway_openapi.ts --dry-run --repo-limit 5`
  - `direnv exec . node --experimental-strip-types scripts/benchmark_wendao_gateway_openapi.ts --gateway-url http://127.0.0.1:9517 --concurrency 6 --limit 20`
  - `direnv exec . node --experimental-strip-types scripts/benchmark_wendao_gateway_openapi.ts --gateway-url http://127.0.0.1:9517`
  - `direnv exec . git diff --check`
- **Verification Note**:
  - `direnv exec . cargo fmt --all` is still blocked by unrelated missing files under `packages/rust/crates/xiuxian-daochang/tests/telegram_media_support/`
  - the first live gateway report intentionally exited non-zero because the report recorded five real capability failures; that is the expected behavior for a trustworthy benchmark, not a harness crash

### [IN-PROGRESS] Feature: Wendao local corpus partitioned search-plane design

- **Objective**: formalize the next refactor that moves local corpora from single epoch tables toward project- or package-scoped datasets, without destabilizing the already-partitioned repo-backed corpora.
- **Update**:
  - created `.cache/codex/execplans/wendao-local-corpus-partitioned-search-plane.md` to scope the future refactor around local corpora only
  - the plan records the current dependency and code-shape constraints explicitly: `xiuxian-vector` scans are still single-table APIs, and the inspected Lance `3.0.1` surface in this checkout did not expose a usable `replace_where(...)` primitive to treat as the first migration step
  - the migration starting point is therefore `project_id` or `package_id` plus a new unified multi-table scan abstraction in `xiuxian-vector`, not `path_hash`
  - the repository policy blueprint path `.data/blueprints/project_anchor_semantic_addressing.md` is absent in this checkout, and that gap is now recorded in the plan instead of being silently ignored
  - repo-backed corpora remain out of scope for the first migration stage because they already read through per-repo publication records and versioned tables
  - the first implementation slice is now landed in `xiuxian-vector`: `VectorStore` exposes sequential multi-table scan helpers with a shared global limit budget for both sync and async batch consumers
  - `local_symbol` now consumes that new read abstraction already, but keeps the current single published epoch table as a singleton table-list source; this proves the Wendao-side integration before any real local partition publish metadata is introduced
  - a new Wendao regression, `local_symbol_query_can_rerank_across_multiple_tables`, now exercises true two-table fanout and rerank on the local-symbol query path
- **Update**:
  - repo maintenance status now exposes queue telemetry too. For repo-backed corpora, `compaction_running` is no longer set at enqueue time; it only flips once the queue worker promotes a compaction task into `active_task`
  - repo-backed corpus status now overlays `compaction_queue_depth` and `compaction_queue_position` from the shared repo-maintenance runtime, so queued repo compactions are visible in `/api/search/index/status` instead of being flattened into a premature `compacting` state
- **Update**:
  - repo-backed prewarm status now exposes queue telemetry too. `prewarm_running` is no longer treated as an enqueue-time flag; it now flips only when the repo-maintenance worker promotes a prewarm task into `active_task`
  - repo-backed corpus status now overlays `prewarm_queue_depth` and `prewarm_queue_position` from the shared repo-maintenance runtime, and the Studio `/api/search/index/status` DTO maps those fields so queued repo prewarms are visible without being misreported as already running
- **Update**:
  - repo maintenance enqueue order now reflects publication priority too. `prewarm` is inserted before the first queued `compaction`, while compaction remains tail-appended, so publish-gating work cannot sit behind background cleanup
  - because that prioritization happens at enqueue time rather than inside the worker loop, repo `queue_position` stays truthful and continues to match actual execution order
- **Update**:
  - queued repo compaction now coalesces per `(corpus, repo_id)`. When a newer publication schedules compaction for the same repo-backed corpus, the older queued compaction task is dropped before enqueue so stale publication cleanup cannot keep occupying the repo-maintenance queue
  - that replacement happens only for queued compactions; an already active compaction is left alone, which keeps the runtime simple while still removing the most common stale-backlog case
- **Update**:
  - configured project scopes now materialize stable local partition ids, and `search_plane::project_fingerprint` persists those ids into local `SearchFileFingerprint` rows together with project/root metadata for later deleted-path routing and hit enrichment
  - `SearchPlaneService` now enumerates local epoch partition tables with a legacy single-table fallback, and local-symbol query, prewarm, and compaction all consume that discovered table set instead of assuming one published table per epoch
  - `local_symbol` is now the first local corpus on true partitioned publication: one table is written per configured project scope, the first rollout rebuilds from source when the previous active epoch is still legacy-singleton, and subsequent refreshes resume per-partition `clone + delete + merge_insert`
  - local-symbol hits now persist `project_name` and `root_label` directly from the configured scope that produced the file, so partitioned reads preserve Studio navigation metadata without a request-path re-enrichment pass
- **Validation Executed**:
  - `direnv exec . cargo nextest run -p xiuxian-wendao annotate_runtime_status_preserves_repo_compaction_running_from_record_maintenance annotate_runtime_status_marks_repo_compaction_running_from_active_task annotate_runtime_status_surfaces_repo_compaction_queue_backlog status_marks_ready_corpus_with_running_compaction_reason status_surfaces_local_compaction_queue_backlog_for_queued_corpus response_maps_local_compaction_queue_metadata response_maps_prewarm_maintenance_metadata response_counts_track_phase_and_compaction_state`
  - `direnv exec . cargo check -p xiuxian-wendao`
- **Validation Executed**:
  - `direnv exec . cargo nextest run -p xiuxian-wendao synthesize_repo_status_marks_indexing_corpus_as_prewarming_when_prewarm_is_running annotate_runtime_status_marks_repo_prewarm_running_from_active_task annotate_runtime_status_surfaces_repo_prewarm_queue_backlog annotate_runtime_status_preserves_repo_compaction_running_from_record_maintenance annotate_runtime_status_marks_repo_compaction_running_from_active_task annotate_runtime_status_surfaces_repo_compaction_queue_backlog response_maps_prewarm_maintenance_metadata response_maps_local_compaction_queue_metadata response_maps_repo_prewarm_queue_metadata`
  - `direnv exec . cargo check -p xiuxian-wendao`
- **Validation Executed**:
  - `direnv exec . cargo nextest run -p xiuxian-wendao repo_maintenance_task_key_tracks_kind register_repo_maintenance_task_prioritizes_prewarm_before_compaction stop_repo_maintenance_clears_waiters_and_aborts_worker prewarm_repo_table_rejects_new_tasks_after_shutdown repo_publication_runs_prewarm_via_maintenance_task_and_releases_repo_slot repo_publication_does_not_skip_prewarm_when_slot_is_already_claimed repo_publication_schedules_compaction_and_resets_repo_maintenance synthesize_repo_status_marks_indexing_corpus_as_prewarming_when_prewarm_is_running annotate_runtime_status_marks_repo_prewarm_running_from_active_task annotate_runtime_status_surfaces_repo_prewarm_queue_backlog status_with_repo_content_surfaces_ready_repo_tables`
  - `direnv exec . cargo check -p xiuxian-wendao`
  - `direnv exec . git diff --check`
- **Validation Executed**:
  - `direnv exec . cargo nextest run -p xiuxian-wendao repo_maintenance_task_key_tracks_kind register_repo_maintenance_task_prioritizes_prewarm_before_compaction register_repo_maintenance_task_replaces_queued_stale_compaction_for_same_repo stop_repo_maintenance_clears_waiters_and_aborts_worker prewarm_repo_table_rejects_new_tasks_after_shutdown repo_publication_runs_prewarm_via_maintenance_task_and_releases_repo_slot repo_publication_does_not_skip_prewarm_when_slot_is_already_claimed repo_publication_schedules_compaction_and_resets_repo_maintenance status_with_repo_content_surfaces_ready_repo_tables`
  - `direnv exec . cargo check -p xiuxian-wendao`
- **Update**:
  - local queued compaction now coalesces per corpus too. When a newer compaction plan is derived for the same local corpus while the older plan is still queued, the old queued task is replaced instead of being left to compact a stale epoch
  - active local compaction is still left alone; this slice only removes stale queued work, keeping the worker lifecycle and status semantics unchanged
- **Validation Executed**:
  - `direnv exec . cargo nextest run -p xiuxian-wendao enqueue_local_compaction_task_replaces_queued_stale_task_for_same_corpus status_surfaces_local_compaction_queue_backlog_for_queued_corpus compact_pending_corpus_updates_maintenance_status stop_local_maintenance_aborts_running_compaction_and_clears_runtime publish_ready_and_maintain_skips_local_compaction_after_shutdown`
  - `direnv exec . cargo check -p xiuxian-wendao`
- **Update**:
  - local compaction queue now has an explicit ordering rule instead of plain FIFO after coalescing: `PublishThreshold` tasks outrank `RowDeltaRatio`, and within the same reason smaller `row_count` runs first
  - the ordering is applied at enqueue time, so `compaction_queue_position` remains an honest reflection of actual worker order while reducing the chance that a small urgent compact waits behind a large low-urgency one
- **Validation Executed**:
  - `direnv exec . cargo nextest run -p xiuxian-wendao enqueue_local_compaction_task_replaces_queued_stale_task_for_same_corpus enqueue_local_compaction_task_prioritizes_publish_threshold_before_row_delta_ratio enqueue_local_compaction_task_prioritizes_smaller_row_count_within_same_reason status_surfaces_local_compaction_queue_backlog_for_queued_corpus compact_pending_corpus_updates_maintenance_status publish_ready_and_maintain_skips_local_compaction_after_shutdown`
  - `direnv exec . cargo check -p xiuxian-wendao`
- **Update**:
  - repo compaction queue now also has an explicit ordering rule. After same-repo stale replacement, `PublishThreshold` outranks `RowDeltaRatio`, and within the same reason smaller `row_count` is inserted ahead of larger work
  - repo compaction tasks now carry `row_count` in the maintenance runtime, so repo `queue_position` stays truthful while favoring smaller urgent compactions
- **Validation Executed**:
  - `direnv exec . cargo nextest run -p xiuxian-wendao repo_maintenance_task_key_tracks_kind register_repo_maintenance_task_prioritizes_prewarm_before_compaction register_repo_maintenance_task_replaces_queued_stale_compaction_for_same_repo register_repo_maintenance_task_prioritizes_publish_threshold_before_row_delta_ratio register_repo_maintenance_task_prioritizes_smaller_row_count_within_same_reason stop_repo_maintenance_clears_waiters_and_aborts_worker prewarm_repo_table_rejects_new_tasks_after_shutdown repo_publication_runs_prewarm_via_maintenance_task_and_releases_repo_slot repo_publication_does_not_skip_prewarm_when_slot_is_already_claimed repo_publication_schedules_compaction_and_resets_repo_maintenance annotate_runtime_status_surfaces_repo_compaction_queue_backlog status_with_repo_content_surfaces_ready_repo_tables`
  - `direnv exec . cargo check -p xiuxian-wendao`
- **Update**:
  - both local and repo compaction queues now apply a narrow enqueue-time aging guard on top of the existing priority order, so an old `RowDeltaRatio` task eventually stops being pushed behind every new `PublishThreshold` task
  - the guard is queue-local and still keeps `queue_position` truthful because the effective order is computed at enqueue time rather than by worker-side jump-ahead logic
  - `/api/search/index/status` now also exposes whether the queued compaction for a corpus has already crossed that fairness guard through `maintenance.compaction_queue_aged`, so clients can distinguish ordinary backlog from backlog that has already been aged forward
  - the same endpoint now also exposes response-level `maintenanceSummary`, which rolls per-corpus prewarm/compaction running, queue depth, pending count, and aged-queue count into one aggregate surface
- **Validation Executed**:
  - `direnv exec . cargo nextest run -p xiuxian-wendao enqueue_local_compaction_task_replaces_queued_stale_task_for_same_corpus enqueue_local_compaction_task_prioritizes_publish_threshold_before_row_delta_ratio enqueue_local_compaction_task_prioritizes_smaller_row_count_within_same_reason enqueue_local_compaction_task_ages_row_delta_ratio_ahead_of_new_publish_thresholds register_repo_maintenance_task_prioritizes_prewarm_before_compaction register_repo_maintenance_task_replaces_queued_stale_compaction_for_same_repo register_repo_maintenance_task_prioritizes_publish_threshold_before_row_delta_ratio register_repo_maintenance_task_prioritizes_smaller_row_count_within_same_reason register_repo_maintenance_task_ages_row_delta_ratio_ahead_of_new_publish_thresholds status_surfaces_local_compaction_queue_backlog_for_queued_corpus annotate_runtime_status_surfaces_repo_compaction_queue_backlog`
  - `direnv exec . cargo check -p xiuxian-wendao`
  - `direnv exec . cargo nextest run -p xiuxian-wendao status_surfaces_local_compaction_queue_backlog_for_queued_corpus status_surfaces_local_compaction_queue_aging_for_aged_row_delta_task annotate_runtime_status_surfaces_repo_compaction_queue_backlog annotate_runtime_status_surfaces_repo_compaction_queue_aging response_maps_local_compaction_queue_metadata response_maps_repo_prewarm_queue_metadata response_counts_track_phase_and_compaction_state`
  - `direnv exec . cargo nextest run -p xiuxian-wendao response_counts_track_phase_and_compaction_state response_status_reason_prefers_compacting_over_compaction_pending response_maps_local_compaction_queue_metadata response_maps_repo_prewarm_queue_metadata response_maintenance_summary_rolls_up_queue_and_aging_state response_maintenance_summary_stays_empty_without_signals search_index_status_endpoint_returns_idle_corpora_snapshot`
- **Validation Executed**:
  - `CARGO_TARGET_DIR=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/cargo-target/wendao-local-partitioned-search-plane direnv exec . cargo test -p xiuxian-vector --test test_columnar_tables`
  - `CARGO_TARGET_DIR=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/cargo-target/wendao-local-partitioned-search-plane direnv exec . cargo check -p xiuxian-vector`
  - `CARGO_TARGET_DIR=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/cargo-target/wendao-local-partitioned-search-plane direnv exec . cargo check -p xiuxian-wendao`
  - `CARGO_TARGET_DIR=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/cargo-target/wendao-local-partitioned-search-plane direnv exec . cargo nextest run -p xiuxian-wendao -E 'test(local_symbol_query_reads_hits_from_published_epoch) | test(local_symbol_query_can_rerank_across_multiple_tables) | test(local_symbol_autocomplete_reads_suggestions_from_published_epoch) | test(search_ast)'`
  - `direnv exec . git diff --check`
- **Validation Executed**:
  - `CARGO_TARGET_DIR=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/cargo-target/wendao-local-partitioned-search-plane direnv exec . cargo check -p xiuxian-wendao`
  - `CARGO_TARGET_DIR=/Users/guangtao/projects/xiuxian-artisan-workshop/.cache/cargo-target/wendao-local-partitioned-search-plane direnv exec . cargo nextest run -p xiuxian-wendao -E 'test(plan_local_symbol_build_only_reparses_changed_files) | test(local_symbol_incremental_refresh_reuses_unchanged_rows) | test(local_symbol_build_writes_partitioned_epoch_tables_for_multiple_scopes) | test(local_symbol_query_reads_hits_from_published_epoch) | test(local_symbol_query_can_rerank_across_multiple_tables) | test(local_symbol_autocomplete_reads_suggestions_from_published_epoch) | test(publish_local_symbol_hits_records_staging_prewarm_metadata) | test(compact_pending_corpus_updates_maintenance_status)'`

### [IN-PROGRESS] Feature: Wendao pressure follow-ups

- **Objective**: land the smallest high-signal reliability and contract fixes revealed by the live OpenAPI pressure report before attempting a deeper repo-content search rewrite.
- **Update**:
  - managed checkout lock waiting is now pressure-tolerant and configurable through `XIUXIAN_WENDAO_CHECKOUT_LOCK_MAX_WAIT_SECS`, with a new default of `20s` instead of the old fixed `5s`
  - the bundled gateway OpenAPI artifact now declares the required `path` query parameter for `GET /api/analysis/markdown`, and the OpenAPI document test suite now pins both markdown-analysis and code-AST required query params
  - `search_definition` now uses the resolver defaults again, which re-enables fuzzy fallback and Markdown-backed definition resolution instead of forcing exact-only, non-Markdown behavior in the handler
  - the gateway benchmark module now discovers a real local Markdown file path, includes that `markdown_file_path` in TOML discovery metadata, and sends `GET /api/analysis/markdown?path=...` instead of an invalid parameterless smoke probe
  - focused regressions now cover checkout lock wait parsing, markdown contract drift, fuzzy definition fallback, Markdown heading resolution, and the benchmark smoke request plan
- **Validation Executed**:
  - `direnv exec . node --test --experimental-strip-types scripts/test_benchmark_wendao_gateway_openapi.ts`
  - `direnv exec . cargo nextest run -p xiuxian-wendao managed_checkout_lock_times_out_for_active_lockfiles managed_checkout_lock_wait_defaults_to_pressure_tolerant_window managed_checkout_lock_wait_accepts_positive_env_override bundled_gateway_openapi_document_declares_required_markdown_analysis_query_params search_definition_requires_query search_definition_returns_best_payload search_definition_accepts_absolute_source_paths search_definition_uses_markdown_observe_hints search_definition_falls_back_to_fuzzy_symbol_match search_definition_can_resolve_markdown_heading_hits`
  - `direnv exec . cargo check -p xiuxian-wendao`
- **Update**:
  - the benchmark transport no longer uses Node `fetch` or the default `http` loopback binding on this host. The CLI now issues requests through `node:http` / `node:https` and explicitly binds `localAddress` for loopback targets, which avoids the reproducible local `EADDRNOTAVAIL` failure mode seen during gateway benchmarking on `127.0.0.1`
  - a local Node transport regression now covers the new request path with a real loopback test server, and `git diff --check` remains clean after the transport swap
  - reran the full high-pressure gateway benchmark at `96` concurrency for `60s` with a `12000` request cap and wrote the report to `.data/wendao-frontend/.benchmark/wendao_gateway_openapi_2026_03_24T23_35_11_060Z.toml`
  - that run finished with `64 total / 56 passed / 6 failed / 2 skipped` OpenAPI operations, `code_search` `avg 1400.95ms / p95 1693.30ms`, `stress_code_search` `450 req / 6.84 rps / p95 18325.55ms`, and `stress_mixed_user_hotset` `849 req / 11.21 rps / p95 20943.95ms`
  - the old markdown-contract failure is now gone from the live report; the remaining six failures are all `INDEX_NOT_READY` on unpublished local corpora (`local_symbol`, `attachment`, `reference_occurrence`, `knowledge_section`) rather than request-shape drift
- **Update**:
  - gateway perf reports now have a dedicated summary surface. `scripts/render_wendao_gateway_perf_summary.py` scans `.run/reports/xiuxian-wendao/perf-gateway`, keeps only the newest persisted report for each formal gateway case, ignores helper/sample artifacts in the same directory, and writes `gateway_perf_summary.json` plus `gateway_perf_summary.md`
  - `just wendao-gateway-perf-summary` is now the stable local/CI entrypoint for that render step
  - `.github/workflows/xiuxian-wendao-performance-gates.yaml` now renders the summary after the formal gateway lane and appends the markdown output into `GITHUB_STEP_SUMMARY`
- **Validation Executed**:
  - `direnv exec . env PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 uv run pytest -c /dev/null scripts/test_render_wendao_gateway_perf_summary.py scripts/test_render_wendao_gate_status_summary.py`
  - `direnv exec . uv run python scripts/render_wendao_gateway_perf_summary.py --report-dir .run/reports/xiuxian-wendao/perf-gateway --runner-os local --output-json .run/reports/xiuxian-wendao/perf-gateway/gateway_perf_summary.json --output-markdown .run/reports/xiuxian-wendao/perf-gateway/gateway_perf_summary.md`
  - `direnv exec . just wendao-gateway-perf-summary`
  - `direnv exec . git diff --check`
- **Update**:
  - the gateway perf summary is now unified across the formal warm-cache lane and the ignored real-workspace sample lane. The same `render_wendao_gateway_perf_summary.py` surface now reads both `.run/reports/xiuxian-wendao/perf-gateway` and `.run/reports/xiuxian-wendao/perf-gateway-real-workspace`, keeps the newest report per case in each suite, and renders one combined markdown/json summary
  - formal missing cases remain hard requirements for `overall.ok`, while the real-workspace section stays optional/advisory so CI does not fail when the manual large-workspace lane is absent
  - `just wendao-gateway-perf-summary` now passes both report roots explicitly instead of relying on script defaults
- **Validation Executed**:
  - `direnv exec . env PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 uv run pytest -c /dev/null scripts/test_render_wendao_gateway_perf_summary.py scripts/test_render_wendao_gate_status_summary.py`
  - `direnv exec . uv run python scripts/render_wendao_gateway_perf_summary.py --report-dir .run/reports/xiuxian-wendao/perf-gateway --real-workspace-report-dir .run/reports/xiuxian-wendao/perf-gateway-real-workspace --runner-os local --output-json .run/reports/xiuxian-wendao/perf-gateway/gateway_perf_summary.json --output-markdown .run/reports/xiuxian-wendao/perf-gateway/gateway_perf_summary.md`
  - `direnv exec . just wendao-gateway-perf-summary`
- **Update**:
  - the gateway perf summary renderer now supports mirror output. The same unified formal + real-workspace payload can be written into a secondary directory, which lets the large-workspace report root carry `gateway_perf_summary.json` and `gateway_perf_summary.md` without losing the canonical formal copy
  - `just rust-wendao-performance-gateway-real-workspace` now renders that mirrored unified summary automatically after the ignored large-workspace sample run, so the manual lane leaves behind one fresh summary artifact in both `.run/reports/xiuxian-wendao/perf-gateway` and `.run/reports/xiuxian-wendao/perf-gateway-real-workspace`
- **Validation Executed**:
  - `direnv exec . uv run ruff format scripts/render_wendao_gateway_perf_summary.py scripts/test_render_wendao_gateway_perf_summary.py`
  - `direnv exec . env PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 uv run pytest -c /dev/null scripts/test_render_wendao_gateway_perf_summary.py scripts/test_render_wendao_gate_status_summary.py`
  - `direnv exec . just wendao-gateway-perf-summary-real-workspace`
  - `direnv exec . just --show rust-wendao-performance-gateway-real-workspace`
  - `direnv exec . git diff --check`
- **Update**:
  - local corpus cold-start requests no longer fail immediately with `INDEX_NOT_READY` when no epoch has been published yet. `StudioState` now waits for the first successful publish of `local_symbol`, `knowledge_section`, `attachment`, and `reference_occurrence` before the corresponding search handlers continue
  - AST search, autocomplete, definition resolve, knowledge search, attachment search, and reference search handlers now use those readiness waits, so a fresh gateway restart can build the local corpora on demand instead of exposing transient first-request failures
  - targeted cold-start regressions were added for all six affected handler paths without pre-publishing the local corpus in the fixture, which pins the benchmark-facing behavior that was previously missing from the high-pressure report
- **Validation Executed**:
  - `direnv exec . cargo nextest run -p xiuxian-wendao -E 'test(search_knowledge_waits_for_initial_index_publication) | test(search_attachments_waits_for_initial_index_publication) | test(autocomplete_waits_for_initial_index_publication) | test(search_ast_waits_for_initial_index_publication) | test(search_definition_waits_for_initial_index_publication) | test(search_references_waits_for_initial_index_publication) | test(search_knowledge_returns_payload) | test(search_attachments_returns_payload) | test(autocomplete_includes_code_symbols) | test(search_ast_returns_payload) | test(search_definition_returns_best_payload) | test(search_references_returns_payload)'`
  - `direnv exec . cargo check -p xiuxian-wendao`
  - `direnv exec . git diff --check`
- **Update**:
  - repo-intelligence remote sync now has one shared pressure-control surface. `RepoIndexCoordinator` exposes the same remote-sync semaphore to managed-remote request handlers, so repo overview/search/sync traffic can no longer bypass the background repo-index GitHub sync gate
  - the default managed-remote sync ceiling is now `1` instead of `2`, while `XIUXIAN_WENDAO_REPO_INDEX_SYNC_CONCURRENCY` still overrides it for environments that can safely sustain more parallel upstream fetch pressure
  - targeted repo-index tests now pin both the new default and the semaphore behavior, and the existing repo overview/module/sync endpoint smoke tests still pass after the shared-gate integration
- **Validation Executed**:
  - `direnv exec . cargo nextest run -p xiuxian-wendao repo_index_sync_concurrency_limit_defaults_when_env_is_missing repo_index_sync_concurrency_limit_uses_positive_override repo_index_sync_concurrency_limit_ignores_invalid_override retryable_sync_failure_matches_transient_network_transport_errors status_response_counts_each_phase status_response_exposes_active_repos_and_concurrency_metadata sync_permit_blocks_second_remote_sync_until_first_releases repo_overview_endpoint_returns_repo_summary_payload repo_module_search_endpoint_returns_module_payload repo_sync_endpoint_returns_repo_status_payload`
  - `direnv exec . cargo check -p xiuxian-wendao`
  - `direnv exec . git diff --check`
- **Update**:
  - the next bounded pressure slice is now formalized in `.cache/codex/execplans/wendao_repo_content_query_pressure_hardening.md`, scoped only to repo-content query hot-path work instead of a broader search-plane refactor
  - `xiuxian-vector` now exports `string_contains_mask(...)`, a reusable Arrow-backed substring mask helper built on `arrow-string`, so repo-content search can prefilter a batch with one boolean mask instead of probing `line_text_folded.contains(...)` row by row
  - `repo_content_chunk` query filtering now computes that folded-string mask once per batch and only materializes candidates for matching rows; the exact-case `line_text.contains(raw_needle)` check is preserved only for those filtered rows
  - repo content search reads are now bounded by a shared `SearchPlaneService` semaphore with env override `XIUXIAN_WENDAO_REPO_SEARCH_READ_CONCURRENCY`, and the default is derived conservatively from available parallelism rather than leaving request-path scans completely unbounded
  - repo-content FTS eligibility is now widened for common code and path punctuation such as `@`, `.`, `/`, `:`, and `()`, which keeps more real code-search queries on the lighter FTS-first path before scan fallback is considered
  - targeted regressions were added for the new Arrow mask helper and for punctuation-heavy repo-content queries such as `@reexport`
- **Validation Executed**:
  - `direnv exec . cargo nextest run -p xiuxian-vector --test test_string_match`
  - `direnv exec . cargo check -p xiuxian-vector`
  - `direnv exec . rustfmt packages/rust/crates/xiuxian-vector/src/ops/string_match.rs packages/rust/crates/xiuxian-vector/src/ops/mod.rs packages/rust/crates/xiuxian-vector/src/lib.rs packages/rust/crates/xiuxian-vector/tests/test_string_match.rs packages/rust/crates/xiuxian-wendao/src/search_plane/service/core/types.rs packages/rust/crates/xiuxian-wendao/src/search_plane/service/core/construction.rs packages/rust/crates/xiuxian-wendao/src/search_plane/repo_content_chunk/query.rs packages/rust/crates/xiuxian-wendao/src/gateway/studio/search/handlers/tests.rs`
- **Blocked Validation**:
  - `direnv exec . cargo nextest run -p xiuxian-wendao -E 'test(repo_content_search_hits_find_matching_julia_source_lines) | test(repo_content_search_hits_find_matching_code_punctuation_queries) | test(repo_search_read_concurrency_limit_defaults_from_parallelism) | test(repo_search_read_concurrency_limit_accepts_positive_override) | test(repo_search_read_concurrency_limit_ignores_invalid_override) | test(should_use_fts_allows_common_code_punctuation_queries)'`
  - the command is currently blocked by an unrelated worktree conflict in `packages/rust/crates/xiuxian-wendao/src/search_plane/service/core/`: `git status` shows `AD maintenance.rs` plus an untracked `maintenance/` directory, and `cargo` fails before reaching this slice because Rust sees both `service/core/maintenance.rs` and `service/core/maintenance/mod.rs`
  - until that in-progress maintenance modularization is reconciled, full `xiuxian-wendao` validation for this repo-content slice cannot complete without touching someone else's unfinished structural changes
