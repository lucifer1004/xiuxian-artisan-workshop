# Daily Log - 2026-04-07

## Wendao Repo Index Machine-Aware Sync Policy

- continued the active repo-index reliability blueprint at
  `.cache/agent/blueprints/wendao_repo_index_sync_reliability.md` and closed
  the bounded ExecPlan at
  `.cache/agent/execplans/archives/wendao-repo-index-machine-aware-sync-policy.md`
- replaced the remaining frozen repo-index sync defaults with
  machine-aware policy helpers in
  `packages/rust/crates/xiuxian-wendao/src/repo_index/state/task/`, so
  sync concurrency and sync timeout now derive from
  `std::thread::available_parallelism()` while keeping
  `XIUXIAN_WENDAO_REPO_INDEX_SYNC_CONCURRENCY` and
  `XIUXIAN_WENDAO_REPO_INDEX_SYNC_TIMEOUT_SECS` as highest-precedence
  operator overrides
- replaced the fixed `xiuxian-git-repo` remote watchdog default with a
  host-sensitive policy helper in
  `packages/rust/crates/xiuxian-git-repo/src/backend/gix/`, while preserving
  `XIUXIAN_GIT_REPO_REMOTE_OPERATION_TIMEOUT_SECS` override behavior and
  adding focused unit coverage for scaling plus invalid override fallback
- on the current `12`-core development host, the machine-aware policy keeps
  the empirically best repo-index default at `8` concurrent managed-remote
  syncs and lifts the default substrate remote-operation timeout to `66s`
- focused validation for this slice is green:
  `direnv exec . cargo fmt --all`
  `direnv exec . cargo test -p xiuxian-git-repo --lib --tests`
  `direnv exec . cargo test -p xiuxian-wendao repo_index::state::tests:: --lib`
  `direnv exec . cargo clippy -p xiuxian-git-repo --all-targets --all-features -- -D warnings`
  `direnv exec . cargo clippy -p xiuxian-wendao --lib --tests -- -D warnings`

## Wendao Repo Index Machine-Aware Analysis And Retry Policy

- continued the active repo-index reliability blueprint at
  `.cache/agent/blueprints/wendao_repo_index_sync_reliability.md` and closed
  the bounded ExecPlan at
  `.cache/agent/execplans/archives/wendao-repo-index-machine-aware-analysis-retry-policy.md`
- replaced the remaining fixed repo-index policy knobs in
  `packages/rust/crates/xiuxian-wendao/src/repo_index/state/task/`, so
  analysis timeout and retryable sync requeue budget now derive from
  `std::thread::available_parallelism()` instead of staying pinned to `45s`
  and `1`
- kept the operator control surface explicit:
  `XIUXIAN_WENDAO_REPO_INDEX_ANALYSIS_TIMEOUT_SECS` now overrides the
  machine-aware analysis timeout, and
  `XIUXIAN_WENDAO_REPO_INDEX_SYNC_REQUEUE_ATTEMPTS` now overrides the
  machine-aware retry budget
- added a crate-owned diagnostics seam in
  `packages/rust/crates/xiuxian-wendao/src/repo_index/policy.rs` and wired
  the Studio performance fixture/live audit to print the effective
  `analysisTimeout`, `syncTimeout`, and `syncRetryBudget` used by the current
  run without widening product-facing `/api/repo/index/status`
- the fresh full real-workspace proof with the current defaults completed the
  `.data/wendao-frontend` `177`-repo inventory in `66s`, ending at
  `160 ready`, `17 unsupported`, `0 failed`, with live logs showing
  `analysisTimeout=60s`, `syncTimeout=120s`, and `syncRetryBudget=2`
- focused validation for this slice is green:
  `direnv exec . cargo fmt --all`
  `direnv exec . cargo test -p xiuxian-wendao repo_index::state::tests:: --lib`
  `direnv exec . cargo test -p xiuxian-wendao --features performance repo_index_audit::tests:: --test xiuxian-testing-gate -- --nocapture`
  `direnv exec . cargo clippy -p xiuxian-wendao --lib --tests -- -D warnings`
  `direnv exec . cargo clippy -p xiuxian-wendao --features performance --test xiuxian-testing-gate -- -D warnings`
  `direnv exec . env XIUXIAN_WENDAO_GATEWAY_PERF_FULL_AUDIT_TIMEOUT_SECS=1200 XIUXIAN_WENDAO_GATEWAY_PERF_FULL_AUDIT_INTERVAL_SECS=10 cargo test -p xiuxian-wendao --features performance gateway_perf_audits_real_workspace_repo_index_full_run_live --test xiuxian-testing-gate -- --ignored --nocapture`

## Wendao Git Repo Machine-Aware Retry And Open Policy

- continued the active repo-index reliability blueprint at
  `.cache/agent/blueprints/wendao_repo_index_sync_reliability.md` and closed
  the bounded ExecPlan at
  `.cache/agent/execplans/archives/wendao-git-repo-machine-aware-retry-open-policy.md`
- replaced the remaining fixed substrate retry/open defaults in
  `packages/rust/crates/xiuxian-git-repo/src/backend/gix/` and
  `packages/rust/crates/xiuxian-git-repo/src/lock.rs`, so managed remote retry
  attempts, git-open retry attempts, git-open retry delay, and checkout-lock
  retry delay now derive from host parallelism instead of frozen literals
- kept explicit operator overrides for the new substrate policy surface:
  `XIUXIAN_GIT_REPO_MANAGED_REMOTE_RETRY_ATTEMPTS`
  `XIUXIAN_GIT_REPO_MANAGED_GIT_OPEN_RETRY_ATTEMPTS`
  `XIUXIAN_GIT_REPO_MANAGED_GIT_OPEN_RETRY_DELAY_MS`
  `XIUXIAN_GIT_REPO_CHECKOUT_LOCK_RETRY_DELAY_MS`
- the current `12`-core host still resolves to the already-proven substrate
  defaults (`remoteRetries=3`, `gitOpenRetries=5`,
  `gitOpenRetryDelay=100ms`, `checkoutLockRetryDelay=100ms`), but those
  values are now derived instead of hard-coded
- focused validation for this slice is green:
  `direnv exec . cargo fmt --all`
  `direnv exec . cargo test -p xiuxian-git-repo --lib --tests`
  `direnv exec . cargo clippy -p xiuxian-git-repo --all-targets --all-features -- -D warnings`

## Wendao Repo Index Mixed Gateway Load Proof

- continued the active repo-index reliability blueprint at
  `.cache/agent/blueprints/wendao_repo_index_sync_reliability.md` and closed
  the bounded ExecPlan at
  `.cache/agent/execplans/archives/wendao-repo-index-mixed-gateway-load-proof.md`
- split the real-workspace repo-index audit support into a focused mixed-load
  child module under
  `packages/rust/crates/xiuxian-wendao/tests/performance/support/repo_index_audit/`,
  so query-pressure helpers do not keep inflating one monolithic support file
- added a mixed gateway-load proof that runs the real `177`-repo repo-index
  full audit while replaying concurrent repo-backed gateway queries via
  `CancellationToken` + `TaskTracker`, and now logs query totals, latency,
  busiest URI, skipped probes, and last error alongside repo-index progress
- hardened the query mix so repo-backed candidates are preflighted first and
  only stable `200 OK` routes are replayed during the stress run; skipped
  routes are logged explicitly, which exposed that the projected-page route for
  `TestPackage` currently preflights as `409 Conflict`
- the fresh real-workspace mixed-load proof completed in `62s` with
  `177 total`, `160 ready`, `17 unsupported`, `0 failed`, while
  `6` query workers at `6ms` pause issued `291` successful gateway requests
  across the accepted query mix with no query failures
- focused validation for this slice is green:
  `direnv exec . cargo test -p xiuxian-wendao --features performance repo_index_audit::tests:: --test xiuxian-testing-gate -- --nocapture`
  `direnv exec . cargo test -p xiuxian-wendao --features performance mixed_load::tests:: --test xiuxian-testing-gate -- --nocapture`
  `direnv exec . env XIUXIAN_WENDAO_GATEWAY_PERF_FULL_AUDIT_TIMEOUT_SECS=1200 XIUXIAN_WENDAO_GATEWAY_PERF_FULL_AUDIT_INTERVAL_SECS=10 cargo test -p xiuxian-wendao --features performance gateway_perf_audits_real_workspace_repo_index_mixed_gateway_load_live --test xiuxian-testing-gate -- --ignored --nocapture`
  `direnv exec . cargo clippy -p xiuxian-wendao --features performance --test xiuxian-testing-gate -- -D warnings`
  `direnv exec . git diff --check -- packages/rust/crates/xiuxian-wendao/tests/performance/gateway_search.rs packages/rust/crates/xiuxian-wendao/tests/performance/support/mod.rs packages/rust/crates/xiuxian-wendao/tests/performance/support/repo_index_audit.rs packages/rust/crates/xiuxian-wendao/tests/performance/support/repo_index_audit/mixed_load.rs .cache/agent/blueprints/wendao_repo_index_sync_reliability.md packages/rust/crates/xiuxian-wendao/docs/06_roadmap/402_repo_intelligence_mvp.md docs/GTD/DAILY_2026_04_07.md`

## Wendao Search Cache TOML Precedence

- continued the active repo-index reliability blueprint at
  `.cache/agent/blueprints/wendao_repo_index_sync_reliability.md` and opened
  the bounded ExecPlan at
  `.cache/agent/execplans/archives/wendao-search-cache-toml-precedence.md`
- replaced the last env-only search cache construction seam under
  `packages/rust/crates/xiuxian-wendao/src/search/cache/`, so
  `SearchPlaneCache` now resolves `search.cache.*` from merged `wendao.toml`
  before falling back to the existing Valkey env surface
- added focused unit coverage for precedence and invalid-setting fallback,
  proving a valid `search.cache.valkey_url` overrides env while malformed TOML
  values still fall back to a valid env URL for the optional cache runtime
- updated the real `.data/wendao-frontend/wendao.toml` to declare
  `search.cache.valkey_url = "redis://127.0.0.1:6379/0"`, so live gateway
  proofs no longer depend on shell-local env export just to turn cache hits on
- focused validation for this slice is green:
  `direnv exec . cargo test -p xiuxian-wendao search::cache:: --lib`
  `direnv exec . cargo test -p xiuxian-wendao search::service::core::construction::tests:: --lib`
  `direnv exec . cargo fmt --all`
  `direnv exec . cargo clippy -p xiuxian-wendao --lib --tests -- -D warnings`
  `direnv exec . git diff --check -- packages/rust/crates/xiuxian-wendao/src/search/cache packages/rust/crates/xiuxian-wendao/src/search/service/core/construction/runtime.rs packages/rust/crates/xiuxian-wendao/src/valkey_common.rs .data/wendao-frontend/wendao.toml packages/rust/crates/xiuxian-wendao/docs/06_roadmap/402_repo_intelligence_mvp.md docs/GTD/DAILY_2026_04_07.md .cache/agent/blueprints/wendao_repo_index_sync_reliability.md .cache/agent/execplans/archives/wendao-search-cache-toml-precedence.md`
