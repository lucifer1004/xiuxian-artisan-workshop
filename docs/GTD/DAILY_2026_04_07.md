# Daily Log - 2026-04-07

## Qianji Runtime Config Valkey Ownership

- continued the active blueprint at
  `.cache/agent/blueprints/config_core_shared_runtime_resolution.md` and
  closed the bounded ExecPlan at
  `.cache/agent/execplans/archives/qianji-runtime-config-valkey-ownership.md`
- moved Qianji checkpoint persistence runtime ownership under
  `packages/rust/crates/xiuxian-qianji/src/runtime_config/`, so the CLI now
  resolves `checkpoint.valkey_url` through `qianji.toml` first and only falls
  back to `QIANJI_VALKEY_URL`, `VALKEY_URL`, and `REDIS_URL` when no TOML
  value is present
- aligned Qianji config discovery with the documented contract by loading only
  `resources/config/qianji.toml`, user
  `$PRJ_CONFIG_HOME/xiuxian-artisan-workshop/qianji.toml`, and explicit
  `QIANJI_CONFIG_PATH`; legacy user `xiuxian.toml` is no longer part of the
  qianji runtime-config overlay chain
- added focused integration coverage for checkpoint precedence, user-file
  discovery, legacy `xiuxian.toml` ignore behavior, and invalid TOML error
  classification
- focused validation for this slice is green:
  `direnv exec . cargo fmt --all`
  `direnv exec . cargo test -p xiuxian-qianji runtime_config:: --lib --features llm`
  `direnv exec . cargo test -p xiuxian-qianji --test runtime_config --features llm`
  `direnv exec . cargo test -p xiuxian-qianji --bin qianji --features llm`
  `direnv exec . cargo clippy -p xiuxian-qianji --lib --tests --features llm -- -D warnings`

## Config Core Link Graph Cache Runtime Dedup

- continued the active blueprint at
  `.cache/agent/blueprints/config_core_shared_runtime_resolution.md` and
  closed the bounded ExecPlan at
  `.cache/agent/execplans/archives/config-core-link-graph-cache-runtime-dedup.md`
- expanded `packages/rust/crates/xiuxian-config-core/src/resolve/precedence.rs`
  with a shared `toml_first_named_string(...)` helper so runtime-owned TOML
  settings can retain source-aware precedence without each crate cloning the
  same `setting-name vs env-name` glue
- adopted that helper in
  `packages/rust/crates/xiuxian-wendao-runtime/src/config/resolve/cache.rs`,
  so `link_graph.cache.{valkey_url,key_prefix,ttl_seconds}` now follows the
  same TOML-first resolution hygiene as the other Wendao Valkey owners while
  still leaving missing-target policy in the owning runtime crate
- added focused runtime coverage proving three behaviors:
  TOML-backed override files win, env is used only when TOML is absent, and
  explicitly configured invalid TOML values remain authoritative instead of
  being silently replaced by env fallback
- while closing the slice, also repaired the existing
  `xiuxian-wendao-runtime` `transport::query_contract` lib-test feature-gate
  drift so the default `--lib` test and clippy surfaces are self-consistent
  again when `transport` is disabled
- focused validation for this slice is green:
  `direnv exec . cargo fmt --all`
  `direnv exec . cargo test -p xiuxian-config-core`
  `direnv exec . cargo test -p xiuxian-wendao-runtime --features transport config::resolve::cache:: --lib`
  `direnv exec . cargo clippy -p xiuxian-wendao-runtime --lib --features transport -- -D warnings`
  `direnv exec . cargo test -p xiuxian-wendao-runtime --lib`
  `direnv exec . cargo clippy -p xiuxian-wendao-runtime --lib -- -D warnings`

## Config Core Valkey Runtime Resolution Dedup

- continued the active blueprint at
  `.cache/agent/blueprints/config_core_shared_runtime_resolution.md` and
  closed the bounded ExecPlan at
  `.cache/agent/execplans/archives/config-core-valkey-runtime-resolution-dedup.md`
- expanded `packages/rust/crates/xiuxian-config-core/src/resolve/precedence.rs`
  with a shared `first_non_empty_named_lookup(...)` helper and re-exported it
  from `packages/rust/crates/xiuxian-config-core/src/lib.rs`, so precedence
  chains that still need source-aware diagnostics no longer carry crate-local
  copies of the same trim-and-pick logic
- adopted that shared helper in the highest-value Wendao Valkey runtime owners:
  `packages/rust/crates/xiuxian-wendao/src/search/cache/runtime.rs`
  `packages/rust/crates/xiuxian-wendao/src/analyzers/cache/valkey/runtime.rs`
  `packages/rust/crates/xiuxian-wendao/src/graph/valkey_persistence.rs`
  `packages/rust/crates/xiuxian-wendao/src/valkey_common.rs`
- kept the slice bounded on purpose: no new TOML keyspace was invented for the
  analyzer, graph, or knowledge-storage Valkey surfaces in this pass; the
  shared cleanup only retired duplicated resolution hygiene while preserving
  each owner's existing optional-vs-required runtime semantics
- added focused regression coverage for source-aware lookup and Valkey
  resolution fallbacks, including a strict search-cache startup error that
  reports the invalid env source name and an analyzer runtime fallback that
  skips blank primary URLs and blank TTL overrides
- focused validation for this slice is green:
  `direnv exec . cargo fmt --all`
  `direnv exec . cargo test -p xiuxian-config-core`
  `direnv exec . cargo test -p xiuxian-wendao search::cache:: --lib`
  `direnv exec . cargo test -p xiuxian-wendao analyzers::cache::valkey::tests:: --lib`
  `direnv exec . cargo test -p xiuxian-wendao storage::crud::tests:: --lib`
  `direnv exec . cargo clippy -p xiuxian-config-core --all-targets -- -D warnings`
  `direnv exec . cargo clippy -p xiuxian-wendao --bin wendao --lib --tests -- -D warnings`

## Config Core Wendao Valkey Keyspace Unification

- continued the active blueprint at
  `.cache/agent/blueprints/config_core_shared_runtime_resolution.md` and
  closed the bounded ExecPlan at
  `.cache/agent/execplans/archives/config-core-wendao-valkey-keyspace-unification.md`
- expanded `packages/rust/crates/xiuxian-config-core/src/resolve/precedence.rs`
  with a shared `toml_first_named_string(...)` helper so TOML-owned settings
  can retain source-aware diagnostics instead of making each Wendao owner hand
  roll its own `setting-or-env-name` precedence glue
- formalized the remaining non-link-graph Wendao Valkey owner keyspace without
  forcing new embedded defaults:
  `analyzers.cache.{valkey_url,key_prefix,ttl_seconds}`
  `graph.persistence.{valkey_url,key_prefix}`
  `storage.knowledge.valkey_url`
- adopted those keys in the owning runtime surfaces:
  `packages/rust/crates/xiuxian-wendao/src/analyzers/cache/valkey/runtime.rs`
  `packages/rust/crates/xiuxian-wendao/src/graph/valkey_persistence.rs`
  `packages/rust/crates/xiuxian-wendao/src/storage/crud.rs`
  while keeping owner-specific policy explicit:
  analyzer cache stays optional when no setting or env exists, graph
  persistence still requires a configured endpoint, and knowledge storage
  keeps its localhost fallback when nothing is configured
- documented the canonical key names as commented sections in
  `packages/rust/crates/xiuxian-wendao/resources/config/wendao.toml` instead
  of shipping concrete embedded defaults that would silently override env-only
  deployments
- added focused regression coverage proving TOML-first precedence and strict
  explicit-config behavior for analyzer cache, graph persistence, and
  knowledge storage
- focused validation for this slice is green:
  `direnv exec . cargo fmt --all`
  `direnv exec . cargo test -p xiuxian-config-core`
  `direnv exec . cargo test -p xiuxian-wendao analyzers::cache::valkey::tests:: --lib`
  `direnv exec . cargo test -p xiuxian-wendao storage::crud::tests:: --lib`
  `direnv exec . cargo test -p xiuxian-wendao graph::valkey_persistence::tests:: --lib`
  `direnv exec . cargo clippy -p xiuxian-config-core --all-targets -- -D warnings`
  `direnv exec . cargo clippy -p xiuxian-wendao --bin wendao --lib --tests -- -D warnings`

## Config Core Shared Runtime Resolution

- continued the active blueprint at
  `.cache/agent/blueprints/config_core_shared_runtime_resolution.md` and
  closed the bounded ExecPlan at
  `.cache/agent/execplans/archives/config-core-path-resolution-dedup.md`
- added shared project-root-aware path helpers to
  `packages/rust/crates/xiuxian-config-core/src/paths.rs`, so
  `resolve_path_from_value(...)` and
  `resolve_project_root_or_cwd_from_value(...)` now own the low-level
  `PRJ_ROOT` and `PRJ_*` path joining semantics that had drifted across
  Wendao and Qianji
- adopted those helpers in the highest-value duplicated call sites:
  `packages/rust/crates/xiuxian-wendao/src/skill_vfs/resolver/runtime.rs`
  `packages/rust/crates/xiuxian-qianji/src/runtime_config/pathing.rs`
  `packages/rust/crates/xiuxian-qianji/src/scheduler/preflight/wendao_uri.rs`
  `packages/rust/crates/xiuxian-qianji/src/bootcamp/runtime.rs`
  `packages/rust/crates/xiuxian-qianji/src/executors/wendao_refresh/input.rs`
  `packages/rust/crates/xiuxian-qianji/src/bin/qianji.rs`
- tightened two adjacent ownership seams while closing the slice:
  `normalize_relative_path(...)` in Qianji preflight now collapses duplicate
  separators instead of preserving malformed intermediate `//`, and the
  contract-feedback cache default now stays workspace-owned instead of being
  hijacked by a foreign absolute `PRJ_CACHE_HOME`
- cleared the strict-clippy closure blockers encountered along the way in
  touched downstream tests:
  `packages/rust/crates/xiuxian-zhixing/src/heyi/reminders.rs`
  `packages/rust/crates/xiuxian-qianji/tests/planning_source_contracts.rs`
  `packages/rust/crates/xiuxian-qianji/tests/integration/test_audit_scenario_contract.rs`
  `packages/rust/crates/xiuxian-qianji/tests/integration/test_wendao_live_rest_docs_contract_feedback.rs`
- focused validation for this slice is green:
  `direnv exec . cargo fmt --all`
  `direnv exec . cargo test -p xiuxian-config-core`
  `direnv exec . cargo test -p xiuxian-wendao skill_vfs:: --lib`
  `direnv exec . cargo test -p xiuxian-qianji runtime_config:: --lib`
  `direnv exec . cargo test -p xiuxian-qianji scheduler::preflight:: --lib`
  `direnv exec . cargo test -p xiuxian-qianji --test planning_source_contracts`
  `direnv exec . cargo test -p xiuxian-qianji --test audit_scenario_contract`
  `direnv exec . cargo test -p xiuxian-qianji --test wendao_live_rest_docs_contract_feedback`
  `direnv exec . cargo test -p xiuxian-qianji --features llm --bin qianji`
  `direnv exec . cargo test -p xiuxian-zhixing heyi::reminders::tests:: --lib`
  `direnv exec . cargo clippy -p xiuxian-config-core --all-targets -- -D warnings`
  `direnv exec . cargo clippy -p xiuxian-wendao --lib --tests -- -D warnings`
  `direnv exec . cargo clippy -p xiuxian-qianji --lib --tests --features llm -- -D warnings`

## Config Core Numeric And Bool Env Parsing Dedup

- continued the active blueprint at
  `.cache/agent/blueprints/config_core_shared_runtime_resolution.md` and
  closed the bounded ExecPlan at
  `.cache/agent/execplans/archives/config-core-numeric-env-parsing-dedup.md`
- expanded `packages/rust/crates/xiuxian-config-core/src/resolve/precedence.rs`
  with shared scalar parse helpers for trimmed parse, positive numeric parse,
  bool-flag parse, and single-key env lookup parse, then re-exported that
  surface from `packages/rust/crates/xiuxian-config-core/src/lib.rs`
- adopted the shared helpers in the main duplicated runtime-policy owners:
  `packages/rust/crates/xiuxian-wendao/src/repo_index/state/task/policy.rs`
  `packages/rust/crates/xiuxian-wendao/src/search/service/core/construction/concurrency.rs`
  `packages/rust/crates/xiuxian-wendao/src/gateway/studio/perf_support/root.rs`
  `packages/rust/crates/xiuxian-qianji/src/runtime_config/env_vars.rs`
- added focused regression coverage for zero/invalid overrides and bool aliases
  in config-core, Wendao repo-index/search/perf-support, and Qianji
  runtime-config so env parse semantics no longer depend on crate-local
  hand-written `trim().parse()` chains
- focused validation for this slice is green:
  `direnv exec . cargo test -p xiuxian-config-core`
  `direnv exec . cargo test -p xiuxian-wendao repo_index::state::tests:: --lib`
  `direnv exec . cargo test -p xiuxian-wendao search::service::core::construction::tests:: --lib`
  `direnv exec . cargo test -p xiuxian-wendao --features 'performance zhenfa-router' real_workspace_ready_timeout_ --lib`
  `direnv exec . cargo test -p xiuxian-qianji runtime_config:: --lib`
  `direnv exec . cargo clippy -p xiuxian-config-core --all-targets -- -D warnings`
  `direnv exec . cargo clippy -p xiuxian-wendao --features performance --lib --tests -- -D warnings`
  `direnv exec . cargo clippy -p xiuxian-qianji --lib --tests --features llm -- -D warnings`

## Qianhuan Rust-Only Surface

- removed the remaining PyO3 binding surface from
  `packages/rust/crates/xiuxian-qianhuan/` by deleting the crate-local
  `pyo3` dependency, removing the `python_module` export from
  `src/lib.rs`, and deleting `src/python_module.rs`
- updated `packages/rust/crates/xiuxian-qianhuan/README.md` to state the
  crate now exposes a Rust-only orchestration/persona API surface and no
  Python binding contract
- focused validation target for this slice is:
  `direnv exec . cargo check -p xiuxian-qianhuan --lib`
  `direnv exec . cargo test -p xiuxian-qianhuan --lib`
  `direnv exec . git diff --check -- packages/rust/crates/xiuxian-qianhuan/Cargo.toml packages/rust/crates/xiuxian-qianhuan/src/lib.rs packages/rust/crates/xiuxian-qianhuan/src/python_module.rs packages/rust/crates/xiuxian-qianhuan/README.md docs/GTD/DAILY_2026_04_07.md`

## Wendao Repo Index Machine-Aware Sync Policy

- continued the active repo-index reliability blueprint at
  `.cache/agent/blueprints/wendao_repo_index_sync_reliability.md` and closed
  the bounded ExecPlan at
  `.cache/agent/execplans/archives/wendao-repo-index-machine-aware-sync-policy.md`
- replaced the remaining frozen repo-index sync defaults with
  machine-aware policy helpers in
  `packages/rust/crates/xiuxian-wendao/src/repo_index/state/task/`, so
  sync concurrency and sync timeout now derive from
  `std::thread::available_parallelism()` while keeping
  `XIUXIAN_WENDAO_REPO_INDEX_SYNC_CONCURRENCY` and
  `XIUXIAN_WENDAO_REPO_INDEX_SYNC_TIMEOUT_SECS` as highest-precedence
  operator overrides
- replaced the fixed `xiuxian-git-repo` remote watchdog default with a
  host-sensitive policy helper in
  `packages/rust/crates/xiuxian-git-repo/src/backend/gix/`, while preserving
  `XIUXIAN_GIT_REPO_REMOTE_OPERATION_TIMEOUT_SECS` override behavior and
  adding focused unit coverage for scaling plus invalid override fallback
- on the current `12`-core development host, the machine-aware policy keeps
  the empirically best repo-index default at `8` concurrent managed-remote
  syncs and lifts the default substrate remote-operation timeout to `66s`
- focused validation for this slice is green:
  `direnv exec . cargo fmt --all`
  `direnv exec . cargo test -p xiuxian-git-repo --lib --tests`
  `direnv exec . cargo test -p xiuxian-wendao repo_index::state::tests:: --lib`
  `direnv exec . cargo clippy -p xiuxian-git-repo --all-targets --all-features -- -D warnings`
  `direnv exec . cargo clippy -p xiuxian-wendao --lib --tests -- -D warnings`

## Wendao Repo Index Machine-Aware Analysis And Retry Policy

- continued the active repo-index reliability blueprint at
  `.cache/agent/blueprints/wendao_repo_index_sync_reliability.md` and closed
  the bounded ExecPlan at
  `.cache/agent/execplans/archives/wendao-repo-index-machine-aware-analysis-retry-policy.md`
- replaced the remaining fixed repo-index policy knobs in
  `packages/rust/crates/xiuxian-wendao/src/repo_index/state/task/`, so
  analysis timeout and retryable sync requeue budget now derive from
  `std::thread::available_parallelism()` instead of staying pinned to `45s`
  and `1`
- kept the operator control surface explicit:
  `XIUXIAN_WENDAO_REPO_INDEX_ANALYSIS_TIMEOUT_SECS` now overrides the
  machine-aware analysis timeout, and
  `XIUXIAN_WENDAO_REPO_INDEX_SYNC_REQUEUE_ATTEMPTS` now overrides the
  machine-aware retry budget
- added a crate-owned diagnostics seam in
  `packages/rust/crates/xiuxian-wendao/src/repo_index/policy.rs` and wired
  the Studio performance fixture/live audit to print the effective
  `analysisTimeout`, `syncTimeout`, and `syncRetryBudget` used by the current
  run without widening product-facing `/api/repo/index/status`
- the fresh full real-workspace proof with the current defaults completed the
  `.data/wendao-frontend` `177`-repo inventory in `66s`, ending at
  `160 ready`, `17 unsupported`, `0 failed`, with live logs showing
  `analysisTimeout=60s`, `syncTimeout=120s`, and `syncRetryBudget=2`
- focused validation for this slice is green:
  `direnv exec . cargo fmt --all`
  `direnv exec . cargo test -p xiuxian-wendao repo_index::state::tests:: --lib`
  `direnv exec . cargo test -p xiuxian-wendao --features performance repo_index_audit::tests:: --test xiuxian-testing-gate -- --nocapture`
  `direnv exec . cargo clippy -p xiuxian-wendao --lib --tests -- -D warnings`
  `direnv exec . cargo clippy -p xiuxian-wendao --features performance --test xiuxian-testing-gate -- -D warnings`
  `direnv exec . env XIUXIAN_WENDAO_GATEWAY_PERF_FULL_AUDIT_TIMEOUT_SECS=1200 XIUXIAN_WENDAO_GATEWAY_PERF_FULL_AUDIT_INTERVAL_SECS=10 cargo test -p xiuxian-wendao --features performance gateway_perf_audits_real_workspace_repo_index_full_run_live --test xiuxian-testing-gate -- --ignored --nocapture`

## Wendao Git Repo Machine-Aware Retry And Open Policy

- continued the active repo-index reliability blueprint at
  `.cache/agent/blueprints/wendao_repo_index_sync_reliability.md` and closed
  the bounded ExecPlan at
  `.cache/agent/execplans/archives/wendao-git-repo-machine-aware-retry-open-policy.md`
- replaced the remaining fixed substrate retry/open defaults in
  `packages/rust/crates/xiuxian-git-repo/src/backend/gix/` and
  `packages/rust/crates/xiuxian-git-repo/src/lock.rs`, so managed remote retry
  attempts, git-open retry attempts, git-open retry delay, and checkout-lock
  retry delay now derive from host parallelism instead of frozen literals
- kept explicit operator overrides for the new substrate policy surface:
  `XIUXIAN_GIT_REPO_MANAGED_REMOTE_RETRY_ATTEMPTS`
  `XIUXIAN_GIT_REPO_MANAGED_GIT_OPEN_RETRY_ATTEMPTS`
  `XIUXIAN_GIT_REPO_MANAGED_GIT_OPEN_RETRY_DELAY_MS`
  `XIUXIAN_GIT_REPO_CHECKOUT_LOCK_RETRY_DELAY_MS`
- the current `12`-core host still resolves to the already-proven substrate
  defaults (`remoteRetries=3`, `gitOpenRetries=5`,
  `gitOpenRetryDelay=100ms`, `checkoutLockRetryDelay=100ms`), but those
  values are now derived instead of hard-coded
- focused validation for this slice is green:
  `direnv exec . cargo fmt --all`
  `direnv exec . cargo test -p xiuxian-git-repo --lib --tests`
  `direnv exec . cargo clippy -p xiuxian-git-repo --all-targets --all-features -- -D warnings`

## Wendao Repo Index Mixed Gateway Load Proof

- continued the active repo-index reliability blueprint at
  `.cache/agent/blueprints/wendao_repo_index_sync_reliability.md` and closed
  the bounded ExecPlan at
  `.cache/agent/execplans/archives/wendao-repo-index-mixed-gateway-load-proof.md`
- split the real-workspace repo-index audit support into a focused mixed-load
  child module under
  `packages/rust/crates/xiuxian-wendao/tests/performance/support/repo_index_audit/`,
  so query-pressure helpers do not keep inflating one monolithic support file
- added a mixed gateway-load proof that runs the real `177`-repo repo-index
  full audit while replaying concurrent repo-backed gateway queries via
  `CancellationToken` + `TaskTracker`, and now logs query totals, latency,
  busiest URI, skipped probes, and last error alongside repo-index progress
- hardened the query mix so repo-backed candidates are preflighted first and
  only stable `200 OK` routes are replayed during the stress run; skipped
  routes are logged explicitly, which exposed that the projected-page route for
  `TestPackage` currently preflights as `409 Conflict`
- the fresh real-workspace mixed-load proof completed in `62s` with
  `177 total`, `160 ready`, `17 unsupported`, `0 failed`, while
  `6` query workers at `6ms` pause issued `291` successful gateway requests
  across the accepted query mix with no query failures
- focused validation for this slice is green:
  `direnv exec . cargo test -p xiuxian-wendao --features performance repo_index_audit::tests:: --test xiuxian-testing-gate -- --nocapture`
  `direnv exec . cargo test -p xiuxian-wendao --features performance mixed_load::tests:: --test xiuxian-testing-gate -- --nocapture`
  `direnv exec . env XIUXIAN_WENDAO_GATEWAY_PERF_FULL_AUDIT_TIMEOUT_SECS=1200 XIUXIAN_WENDAO_GATEWAY_PERF_FULL_AUDIT_INTERVAL_SECS=10 cargo test -p xiuxian-wendao --features performance gateway_perf_audits_real_workspace_repo_index_mixed_gateway_load_live --test xiuxian-testing-gate -- --ignored --nocapture`
  `direnv exec . cargo clippy -p xiuxian-wendao --features performance --test xiuxian-testing-gate -- -D warnings`
  `direnv exec . git diff --check -- packages/rust/crates/xiuxian-wendao/tests/performance/gateway_search.rs packages/rust/crates/xiuxian-wendao/tests/performance/support/mod.rs packages/rust/crates/xiuxian-wendao/tests/performance/support/repo_index_audit.rs packages/rust/crates/xiuxian-wendao/tests/performance/support/repo_index_audit/mixed_load.rs .cache/agent/blueprints/wendao_repo_index_sync_reliability.md packages/rust/crates/xiuxian-wendao/docs/06_roadmap/402_repo_intelligence_mvp.md docs/GTD/DAILY_2026_04_07.md`

## Wendao Search Cache TOML Precedence

- continued the active repo-index reliability blueprint at
  `.cache/agent/blueprints/wendao_repo_index_sync_reliability.md` and opened
  the bounded ExecPlan at
  `.cache/agent/execplans/archives/wendao-search-cache-toml-precedence.md`
- replaced the last env-only search cache construction seam under
  `packages/rust/crates/xiuxian-wendao/src/search/cache/`, so
  `SearchPlaneCache` now resolves `search.cache.*` from merged `wendao.toml`
  before falling back to the existing Valkey env surface
- added focused unit coverage for precedence and invalid-setting fallback,
  proving a valid `search.cache.valkey_url` overrides env while malformed TOML
  values still fall back to a valid env URL for the optional cache runtime
- updated the real `.data/wendao-frontend/wendao.toml` to declare
  `search.cache.valkey_url = "redis://127.0.0.1:6379/0"`, so live gateway
  proofs no longer depend on shell-local env export just to turn cache hits on
- focused validation for this slice is green:
  `direnv exec . cargo test -p xiuxian-wendao search::cache:: --lib`
  `direnv exec . cargo test -p xiuxian-wendao search::service::core::construction::tests:: --lib`
  `direnv exec . cargo fmt --all`
  `direnv exec . cargo clippy -p xiuxian-wendao --lib --tests -- -D warnings`
  `direnv exec . git diff --check -- packages/rust/crates/xiuxian-wendao/src/search/cache packages/rust/crates/xiuxian-wendao/src/search/service/core/construction/runtime.rs packages/rust/crates/xiuxian-wendao/src/valkey_common.rs .data/wendao-frontend/wendao.toml packages/rust/crates/xiuxian-wendao/docs/06_roadmap/402_repo_intelligence_mvp.md docs/GTD/DAILY_2026_04_07.md .cache/agent/blueprints/wendao_repo_index_sync_reliability.md .cache/agent/execplans/archives/wendao-search-cache-toml-precedence.md`

## Wendao Gateway Startup Health Gate

- opened the bounded ExecPlan at
  `.cache/agent/execplans/archives/wendao-gateway-startup-health-gate.md` under the
  root-config process-loading blueprint and kept the slice inside gateway boot
  health only
- added `gateway/studio/startup_health/` as a library-owned feature folder, so
  gateway startup probes built-in plugin bootstrap plus the TOML-owned
  search-cache and link-graph Valkey surfaces before listener bind
- `wendao gateway start` now logs stable dependency lines such as
  `builtin_plugin_registry=connected ...` and `search_cache_valkey=failed ...`,
  and it aborts startup before worker spawn or socket bind when any required
  dependency fails
- split the search-cache runtime semantics cleanly: the optional cache runtime
  still falls back from malformed TOML to a valid env URL, while the strict
  startup-health connection target reports invalid TOML as a startup-blocking
  failure
- added focused coverage for startup report formatting, startup gate pass/fail
  semantics in the `wendao` binary tests, and strict search-cache connection
  target behavior
- focused validation for this slice is green:
  `direnv exec . cargo test -p xiuxian-wendao search::cache::runtime::tests:: --lib`
  `direnv exec . cargo test -p xiuxian-wendao gateway::studio::startup_health::tests:: --lib`
  `direnv exec . cargo test -p xiuxian-wendao test_gateway_startup_health_ --bin wendao`
  `direnv exec . cargo fmt --all`
  `direnv exec . cargo clippy -p xiuxian-wendao --bin wendao --lib --tests -- -D warnings`

## Wendao Config Core TOML-First Precedence

- continued the root-config process-loading blueprint at
  `.cache/agent/blueprints/wendao_root_config_process_loading.md` and closed
  the bounded ExecPlan at
  `.cache/agent/execplans/archives/wendao-config-core-toml-first-precedence.md`
- added a shared precedence surface to
  `packages/rust/crates/xiuxian-config-core/src/resolve/precedence.rs`,
  including the exported `toml_first_env!` macro plus reusable trimmed-string,
  lookup, and parsed-value helpers so TOML-first resolution stops being
  reimplemented in crate-local runtime code
- adopted that shared helper in
  `packages/rust/crates/xiuxian-wendao/src/search/cache/config.rs` and
  `packages/rust/crates/xiuxian-wendao/src/search/cache/runtime.rs`, keeping
  the intended split intact: the optional cache runtime still falls back from
  malformed TOML to a valid env URL, while the strict gateway startup-health
  connection target still treats malformed TOML as a startup-blocking failure
- added focused unit coverage in both crates for macro/helper precedence,
  invalid TOML fallback, and startup-health probe behavior without widening
  the slice into unrelated config surfaces
- focused validation for this slice is green:
  `direnv exec . cargo test -p xiuxian-config-core`
  `direnv exec . cargo test -p xiuxian-wendao search::cache:: --lib`
  `direnv exec . cargo test -p xiuxian-wendao gateway::studio::startup_health::tests:: --lib`
  `direnv exec . cargo test -p xiuxian-wendao test_gateway_startup_health_ --bin wendao`
  `direnv exec . cargo clippy -p xiuxian-config-core --all-targets -- -D warnings`
  `direnv exec . cargo clippy -p xiuxian-wendao --bin wendao --lib --tests -- -D warnings`
  `direnv exec . cargo fmt --all`

## Qianji Flowhub Planning Source RFC

- created the active blueprint at
  `.cache/agent/blueprints/qianji_flowhub_planning_source_model.md` and the
  bounded ExecPlan at
  `.cache/agent/execplans/qianji-flowhub-planning-source-rfc.md` for the
  Qianji planning-source architecture lane
- published
  `packages/rust/crates/xiuxian-qianji/docs/rfcs/2026-04-07-qianji-flowhub-planning-source-model-rfc.md`
  to formalize a flat `qianji-flowhub/` module registry, module-root
  `[[validation]]` contracts, scenario-root `[template]` composition,
  CLI-first authoring, and validation diagnostics without repair-by-default
- synchronized `packages/rust/crates/xiuxian-qianji/README.md` with current
  TOML and CLI reality, and updated the Qianji MOC at
  `packages/rust/crates/xiuxian-qianji/docs/00_mocs/20260315141000-qianji-orchestrator-moc.md`
  to point at the new RFC
- focused validation for this slice is green:
  `direnv exec . git diff --check -- .cache/agent/blueprints/qianji_flowhub_planning_source_model.md .cache/agent/execplans/qianji-flowhub-planning-source-rfc.md packages/rust/crates/xiuxian-qianji/docs/rfcs/2026-04-07-qianji-flowhub-planning-source-model-rfc.md packages/rust/crates/xiuxian-qianji/README.md packages/rust/crates/xiuxian-qianji/docs/00_mocs/20260315141000-qianji-orchestrator-moc.md docs/GTD/DAILY_2026_04_07.md`

## Qianji Flowhub Contract Grammar

- continued the active planning-source blueprint at
  `.cache/agent/blueprints/qianji_flowhub_planning_source_model.md` and opened
  the bounded ExecPlan at
  `.cache/agent/execplans/qianji-flowhub-contract-grammar.md`
- added a dedicated planning-source contract surface in
  `packages/rust/crates/xiuxian-qianji/src/contracts/planning_source/` plus
  parser/runtime helpers in
  `packages/rust/crates/xiuxian-qianji/src/planning_source/`, keeping the new
  Flowhub module/scenario manifest types separate from the existing execution
  `QianjiManifest`
- implemented current RFC-003-level grammar parsing for:
  `template.use = "<module-name> as <alias>"`
  `template.link = "<alias>::<symbol>"`
  and enforced bounded semantic checks for version gating, non-empty
  `template.use`, path-like identifier rejection, duplicate aliases, and
  unknown `template.link` aliases
- added file-backed planning-source manifest loaders and a bounded
  `resolve_flowhub_scenario_modules(...)` step so scenario `template.use`
  entries now resolve against first-level Flowhub module directories and
  verify root `qianji.toml` name matching before any later materialize work
- refactored the implementation into feature subdirectories
  `src/contracts/planning_source/` and `src/planning_source/` so later
  materialize/validate work can extend the same boundary without rebuilding a
  monolithic parser file
- added focused contract coverage in
  `packages/rust/crates/xiuxian-qianji/tests/planning_source_contracts.rs`,
  including parsing and loading real `qianji-flowhub/*/qianji.toml` seed
  modules plus missing-module and manifest-name-mismatch resolution failures
- synchronized `packages/rust/crates/xiuxian-qianji/README.md` so package docs
  now mention the new parser, loader, and bounded resolution helpers while
  still keeping full materialize-and-validate workflow as a later slice
- focused validation status:
  `direnv exec . git diff --check -- .cache/agent/blueprints/qianji_flowhub_planning_source_model.md .cache/agent/execplans/qianji-flowhub-contract-grammar.md packages/rust/crates/xiuxian-qianji/src/contracts/mod.rs packages/rust/crates/xiuxian-qianji/src/contracts/planning_source/mod.rs packages/rust/crates/xiuxian-qianji/src/contracts/planning_source/validation.rs packages/rust/crates/xiuxian-qianji/src/contracts/planning_source/grammar.rs packages/rust/crates/xiuxian-qianji/src/contracts/planning_source/manifest.rs packages/rust/crates/xiuxian-qianji/src/planning_source/mod.rs packages/rust/crates/xiuxian-qianji/src/planning_source/parse.rs packages/rust/crates/xiuxian-qianji/src/planning_source/load.rs packages/rust/crates/xiuxian-qianji/src/planning_source/resolve.rs packages/rust/crates/xiuxian-qianji/src/planning_source/validate.rs packages/rust/crates/xiuxian-qianji/src/lib.rs packages/rust/crates/xiuxian-qianji/README.md packages/rust/crates/xiuxian-qianji/tests/planning_source_contracts.rs docs/GTD/DAILY_2026_04_07.md`
  `direnv exec . rustfmt --edition 2024 packages/rust/crates/xiuxian-qianji/src/contracts/planning_source/mod.rs packages/rust/crates/xiuxian-qianji/src/contracts/planning_source/validation.rs packages/rust/crates/xiuxian-qianji/src/contracts/planning_source/grammar.rs packages/rust/crates/xiuxian-qianji/src/contracts/planning_source/manifest.rs packages/rust/crates/xiuxian-qianji/src/planning_source/mod.rs packages/rust/crates/xiuxian-qianji/src/planning_source/parse.rs packages/rust/crates/xiuxian-qianji/src/planning_source/load.rs packages/rust/crates/xiuxian-qianji/src/planning_source/resolve.rs packages/rust/crates/xiuxian-qianji/src/planning_source/validate.rs packages/rust/crates/xiuxian-qianji/tests/planning_source_contracts.rs packages/rust/crates/xiuxian-qianji/src/lib.rs packages/rust/crates/xiuxian-qianji/src/contracts/mod.rs`
  `direnv exec . cargo test -p xiuxian-qianji --test planning_source_contracts`
  `direnv exec . cargo check -p xiuxian-qianji --tests`

## Qianji Flowhub Hierarchical Composite Demo

- pivoted the active planning-source blueprint at
  `.cache/agent/blueprints/qianji_flowhub_planning_source_model.md` toward the
  graph-mirrored model and opened the bounded ExecPlan at
  `.cache/agent/execplans/qianji-flowhub-hierarchical-composite-demo.md`
- changed the planning-source grammar and contracts in
  `packages/rust/crates/xiuxian-qianji/src/contracts/planning_source/` so
  `template.use` now accepts hierarchical module refs such as
  `coding/rust as rust`, module-root manifests may own optional `[template]`
  composition, and module-root `template.link` refs may target local symbols
- extended the resolver in
  `packages/rust/crates/xiuxian-qianji/src/planning_source/resolve.rs` so
  scenario manifests can resolve nested module refs and composite modules can
  resolve child modules relative to their own directory
- seeded a concrete hierarchical demo tree under `qianji-flowhub/`:
  `coding/`, `coding/rust/`, `coding/rust/style/`,
  `coding/rust/engineering-requirement/`, `coding/rust/policy/`,
  `blueprint/`, and `plan/`
- updated `qianji-flowhub/README.md` and `qianji-flowhub/scripts/check_layout.sh`
  so Flowhub documentation and the local layout gate now distinguish
  composite modules from leaf modules and walk module manifests recursively
- added a scenario fixture at
  `packages/rust/crates/xiuxian-qianji/tests/fixtures/planning_source/coding_rust_blueprint_plan/qianji.toml`
  plus new contract tests covering hierarchical refs, local module link refs,
  nested scenario resolution, and composite child-module resolution
- synchronized `packages/rust/crates/xiuxian-qianji/README.md` so package docs
  now describe hierarchical module refs and composite module manifests as the
  active parsing surface before full materialization
- focused validation for this slice is green:
  `cd qianji-flowhub && ./scripts/check_layout.sh`
  `direnv exec . cargo test -p xiuxian-qianji --test planning_source_contracts`
  `direnv exec . cargo check -p xiuxian-qianji --tests`
  `direnv exec . git diff --check -- .cache/agent/blueprints/qianji_flowhub_planning_source_model.md .cache/agent/execplans/qianji-flowhub-hierarchical-composite-demo.md packages/rust/crates/xiuxian-qianji/src/contracts packages/rust/crates/xiuxian-qianji/src/planning_source packages/rust/crates/xiuxian-qianji/src/lib.rs packages/rust/crates/xiuxian-qianji/README.md packages/rust/crates/xiuxian-qianji/tests/planning_source_contracts.rs packages/rust/crates/xiuxian-qianji/tests/fixtures/planning_source/coding_rust_blueprint_plan/qianji.toml qianji-flowhub docs/GTD/DAILY_2026_04_07.md`

## Qianji Flowhub RFC Plan Workdir Realignment

- continued the active planning-source blueprint at
  `.cache/agent/blueprints/qianji_flowhub_planning_source_model.md` and opened
  the bounded ExecPlan at
  `.cache/agent/execplans/qianji-flowhub-rfc-plan-workdir-realign.md`
- rewrote the top-level RFC framing in
  `packages/rust/crates/xiuxian-qianji/docs/rfcs/2026-04-07-qianji-flowhub-planning-source-model-rfc.md`
  so the primary operational object is now the normalized Codex plan workdir
  under `$PRJ_CACHE_HOME/codex/<plan_name>/`, generated from the `plan` flow
  and checked by the derived `plan-track` subgraph
- replaced the old scenario-source framing with explicit sections for:
  `Summary`, `Motivation`, `Repository / Runtime Model`, `Flow Library Model`,
  `Working Loop`, `Flowchart Placement`, `Plan / Plan-Track Relation`, and
  `CLI-First Requirement`
- synchronized `packages/rust/crates/xiuxian-qianji/README.md` and the Qianji
  MOC so package docs now describe Flowhub flows, Codex plan workdirs, and
  `plan-track` rather than generic scenario composition
- focused validation for this slice is green:
  `direnv exec . git diff --check -- .cache/agent/blueprints/qianji_flowhub_planning_source_model.md .cache/agent/execplans/qianji-flowhub-rfc-plan-workdir-realign.md packages/rust/crates/xiuxian-qianji/docs/rfcs/2026-04-07-qianji-flowhub-planning-source-model-rfc.md packages/rust/crates/xiuxian-qianji/README.md packages/rust/crates/xiuxian-qianji/docs/00_mocs/20260315141000-qianji-orchestrator-moc.md docs/GTD/DAILY_2026_04_07.md`

## Qianji Flowhub RFC Terminology Work Surface Realignment

- continued the active planning-source blueprint at
  `.cache/agent/blueprints/qianji_flowhub_planning_source_model.md` and opened
  the bounded ExecPlan at
  `.cache/agent/execplans/qianji-flowhub-rfc-terminology-work-surface-realign.md`
- rewrote the top-level RFC framing in
  `packages/rust/crates/xiuxian-qianji/docs/rfcs/2026-04-07-qianji-flowhub-planning-source-model-rfc.md`
  so the canonical model is now explicitly graph-first, centered on a
  materialized plan work surface and Codex operational workdir, and no longer
  treats a hidden cache path as the primary normative object
- replaced the old top-level section stack with explicit terminology,
  directory-semantics, core-model, `plan`, `plan-track`, CLI-first, bounded
  working-loop, flowchart-placement, drift-correction, and next-revision
  sections, then renumbered the lower module and validation contract sections
  to keep the document internally consistent
- synchronized `packages/rust/crates/xiuxian-qianji/README.md`, the Qianji
  MOC, and the governing planning-source blueprint so the package-doc surface
  now describes materialized plan work surfaces and Codex operational
  workdirs instead of path-first plan-workdir wording
- focused validation for this slice is green:
  `direnv exec . git diff --check -- .cache/agent/blueprints/qianji_flowhub_planning_source_model.md .cache/agent/execplans/qianji-flowhub-rfc-terminology-work-surface-realign.md packages/rust/crates/xiuxian-qianji/docs/rfcs/2026-04-07-qianji-flowhub-planning-source-model-rfc.md packages/rust/crates/xiuxian-qianji/README.md packages/rust/crates/xiuxian-qianji/docs/00_mocs/20260315141000-qianji-orchestrator-moc.md docs/GTD/DAILY_2026_04_07.md`

## Qianji Flowhub RFC Query Routing Clarification

- continued the active planning-source blueprint at
  `.cache/agent/blueprints/qianji_flowhub_planning_source_model.md` and opened
  then archived the bounded ExecPlan at
  `.cache/agent/execplans/archives/qianji-flowhub-rfc-query-routing-clarification.md`
- corrected
  `packages/rust/crates/xiuxian-qianji/docs/rfcs/2026-04-07-qianji-flowhub-planning-source-model-rfc.md`
  so `tree` is now explicitly framed as an optional structural probe rather
  than a retrieval mechanism, while exact fragment retrieval is declared as a
  `qianji.toml`-routed Wendao query lane
- rewrote the RFC working loop to make the staging explicit:
  directory and filename inspection first, optional `tree` only for deciding
  whether to go deeper, then `qianji.toml`-declared query routing through
  Wendao for exact fragment retrieval
- corrected the lower RFC contract wording so the active plan-root
  `qianji.toml` no longer claims to have only two responsibilities; it now
  also owns the bounded query-routing contract for Codex execution
- synchronized `packages/rust/crates/xiuxian-qianji/README.md`, the Qianji
  MOC, and the governing planning-source blueprint so the package-doc surface
  now reflects the same structural-inspection vs query-routing split
- focused validation for this slice is green:
  `direnv exec . git diff --check -- .cache/agent/blueprints/qianji_flowhub_planning_source_model.md .cache/agent/execplans/archives/qianji-flowhub-rfc-query-routing-clarification.md packages/rust/crates/xiuxian-qianji/docs/rfcs/2026-04-07-qianji-flowhub-planning-source-model-rfc.md packages/rust/crates/xiuxian-qianji/README.md packages/rust/crates/xiuxian-qianji/docs/00_mocs/20260315141000-qianji-orchestrator-moc.md docs/GTD/DAILY_2026_04_07.md`

## Qianji Flowhub RFC Wendao SQL Retrieval Realignment

- continued the active planning-source blueprint at
  `.cache/agent/blueprints/qianji_flowhub_planning_source_model.md` and opened
  the bounded ExecPlan at
  `.cache/agent/execplans/qianji-flowhub-rfc-wendao-sql-retrieval-realign.md`
- corrected
  `packages/rust/crates/xiuxian-qianji/docs/rfcs/2026-04-07-qianji-flowhub-planning-source-model-rfc.md`
  so exact retrieval is now assigned to Wendao SQL rather than to
  `qianji.toml`-declared query lanes, and the bounded execution loop now uses
  `qianji check --dir <plan-workdir>` as the unified guard command
- removed the remaining plan-root wording that treated `qianji.toml` as a
  retrieval-routing contract; it is now kept on the structural and validation
  side only, while Wendao remains the sole retrieval owner
- synchronized `packages/rust/crates/xiuxian-qianji/README.md`, the Qianji
  MOC, and the governing planning-source blueprint so the package-doc surface
  now points at Wendao SQL retrieval instead of Qianji query lanes
- focused validation for this slice is green:
  `direnv exec . git diff --check -- .cache/agent/blueprints/qianji_flowhub_planning_source_model.md .cache/agent/execplans/qianji-flowhub-rfc-wendao-sql-retrieval-realign.md packages/rust/crates/xiuxian-qianji/docs/rfcs/2026-04-07-qianji-flowhub-planning-source-model-rfc.md packages/rust/crates/xiuxian-qianji/README.md packages/rust/crates/xiuxian-qianji/docs/00_mocs/20260315141000-qianji-orchestrator-moc.md docs/GTD/DAILY_2026_04_07.md`

## Qianji Flowhub RFC 0003 Wendao SQL Minimal Retrieval Surface

- continued the active planning-source blueprint at
  `.cache/agent/blueprints/qianji_flowhub_planning_source_model.md` and opened
  the bounded ExecPlan at
  `.cache/agent/execplans/qianji-flowhub-rfc-wendao-sql-minimal-retrieval-surface.md`
- published the dedicated retrieval draft at
  `packages/rust/crates/xiuxian-qianji/docs/rfcs/2026-04-07-wendao-sql-minimal-retrieval-surface-rfc.md`
  so the bounded plan-work retrieval contract is now described on its own
  canonical surface instead of being carried only as wording inside the larger
  planning-source RFC
- the new RFC fixes the retrieval split to the current repository direction:
  `qianji` owns `show` and `check`, Wendao SQL owns exact retrieval, version
  one exposes only a minimal `markdown` table, and `skeleton` stays a
  structural compression view rather than a summary layer
- synchronized `packages/rust/crates/xiuxian-qianji/README.md`, the Qianji
  MOC, and the governing planning-source blueprint so package-level docs now
  point at the dedicated retrieval RFC alongside the broader planning-source
  RFC
- focused validation for this slice is:
  `direnv exec . git diff --check -- .cache/agent/blueprints/qianji_flowhub_planning_source_model.md .cache/agent/execplans/qianji-flowhub-rfc-wendao-sql-minimal-retrieval-surface.md packages/rust/crates/xiuxian-qianji/docs/rfcs/2026-04-07-wendao-sql-minimal-retrieval-surface-rfc.md packages/rust/crates/xiuxian-qianji/README.md packages/rust/crates/xiuxian-qianji/docs/00_mocs/20260315141000-qianji-orchestrator-moc.md docs/GTD/DAILY_2026_04_07.md`

## WendaoSearch Startup Observability And Gateway Sniff

- continued the active mixed-graph retrieval blueprint at
  `.cache/agent/blueprints/wendao_search_mixed_graph_retrieval.md` and opened
  then archived the bounded ExecPlan at
  `.cache/agent/execplans/archives/wendao-search-startup-observability-gateway-sniff.md`
- extended `SearchServiceDescriptor` with a bounded `gateway_sniff` config
  surface, including CLI and TOML parsing for enable or disable, explicit
  base URL or config path, and probe deadline
- added package-owned startup observability under
  `.data/WendaoSearch.jl/src/service/startup.jl`, so the service now logs one
  stable startup line with route count, warmup state, service mode, and the
  dominant memory-footprint factors instead of looking like a black-box Julia
  worker
- added package-owned gateway probing under
  `.data/WendaoSearch.jl/src/service/gateway_probe.jl`, using a bounded
  `GetFlightInfo` preflight against the Wendao gateway knowledge-search route;
  the service now logs `gateway link established`, `gateway sniff failed`, or
  `gateway sniff skipped`
- fixed the startup order in
  `.data/WendaoSearch.jl/examples/search_service.jl` so the gateway link log is
  emitted only after local gRPC bind succeeds; it no longer claims success
  before the local server is actually up
- kept package tests isolated by forcing child-service regressions to pass
  `--no-gateway-sniff` from
  `.data/WendaoSearch.jl/test/support/live_service.jl`, so `Pkg.test()` stays
  independent of whether a local Wendao gateway happens to be running
- real startup smoke against the current local gateway showed the intended log
  order:
  `WendaoSearch service startup`
  `gRPC server started`
  `WendaoSearch gateway link established`
  with the sniff resolving `http://127.0.0.1:9517` and `/search/knowledge`
- validation for this slice is green:
  `direnv exec . julia --project=.data/WendaoSearch.jl -e 'using WendaoSearch, Test; include(joinpath(pwd(), ".data", "WendaoSearch.jl", "test", "service", "descriptor_and_demo.jl"))'`
  `direnv exec . julia --project=.data/WendaoSearch.jl -e 'using Pkg; Pkg.test()'`

## WendaoSearch Live Gateway Performance Probe

- continued the active mixed-graph retrieval blueprint at
  `.cache/agent/blueprints/wendao_search_mixed_graph_retrieval.md` and opened
  then archived the bounded ExecPlan at
  `.cache/agent/execplans/archives/wendao-search-live-gateway-perf-probe.md`
- added package-owned broad-query perf helpers under
  `.data/WendaoSearch.jl/src/service/live_perf.jl` and a thin manual entry at
  `.data/WendaoSearch.jl/scripts/benchmark_search_service.jl`, so the Julia
  package can benchmark a live `solver_demo` service against real gateway
  search workload without widening into Rust crates
- the new live helper first tries to materialize graph-neighbor fixtures from
  broad knowledge-search hits; when the current gateway does not expose those
  hit paths as graph nodes, it now records the explicit
  `fixture_source=knowledge_path_chain` fallback instead of silently pretending
  it measured graph-neighbor topology
- added an env-gated integration regression in
  `.data/WendaoSearch.jl/test/integration/live_perf.jl` with config helpers in
  `.data/WendaoSearch.jl/test/support/live_perf.jl`, so the package can run a
  real gateway perf check on demand while default `Pkg.test()` behavior stays
  stable
- real broad-query smoke against the current local gateway and local
  `solver_demo` service is green:
  `gateway_query=topology fixture_source=knowledge_path_chain knowledge_rows=10 candidates=3 rows=3 feasible=3 samples=3 median_ms=6.6 min_ms=6.276 max_ms=7.313 median_alloc_bytes=297600 resolved_paths=3 skipped_paths=10`
- validation for this slice is green:
  `direnv exec . julia --project=.data/WendaoSearch.jl .data/WendaoSearch.jl/scripts/benchmark_search_service.jl --host 127.0.0.1 --port 41096 --query topology --knowledge-limit 10 --fixture-limit 3 --graph-limit 20 --samples 3`
  `direnv exec . env WENDAOSEARCH_RUN_LIVE_GATEWAY_PERF=1 WENDAOSEARCH_LIVE_GATEWAY_QUERY=topology WENDAOSEARCH_LIVE_GATEWAY_SEARCH_LIMIT=10 WENDAOSEARCH_LIVE_GATEWAY_FIXTURE_LIMIT=3 WENDAOSEARCH_LIVE_GATEWAY_SAMPLES=3 julia --project=.data/WendaoSearch.jl -e 'using Pkg; Pkg.test()'`

## WendaoSearch Live Performance Report

- continued the active mixed-graph retrieval blueprint at
  `.cache/agent/blueprints/wendao_search_mixed_graph_retrieval.md` and closed
  the bounded ExecPlan at
  `.cache/agent/execplans/archives/wendao-search-live-perf-report.md`
- published the durable report at
  `docs/04_chronicles/research/2026-04-07-live-gateway-performance-report.md`,
  so the live benchmark methodology, measured result, and
  `fixture_source=knowledge_path_chain` interpretation are now recorded on a
  package-local docs surface
- normalized the package README, roadmap, testing notes, and report commands
  to the current `WendaoSearch.jl` git toplevel, removing parent-workspace
  `.data/...` path literals from canonical package docs
- focused validation for this slice is green:
  `direnv exec . git diff --check -- .data/WendaoSearch.jl/README.md .data/WendaoSearch.jl/docs/01_core/wendao_search/roadmap.md .data/WendaoSearch.jl/docs/testing/README.md .data/WendaoSearch.jl/docs/04_chronicles/research/2026-04-07-live-gateway-performance-report.md docs/GTD/DAILY_2026_04_07.md .cache/agent/execplans/wendao-search-live-perf-report.md`
  `git -C .data/WendaoSearch.jl diff --check -- README.md docs/01_core/wendao_search/roadmap.md docs/testing/README.md docs/04_chronicles/research/2026-04-07-live-gateway-performance-report.md`

## Qianji Flowhub RFC Minimal Work Surface Show Check

- continued the active planning-source blueprint at
  `.cache/agent/blueprints/qianji_flowhub_planning_source_model.md` and opened
  the bounded ExecPlan at
  `.cache/agent/execplans/qianji-flowhub-rfc-minimal-work-surface-show-check.md`
- rewrote the bounded-work-surface parts of
  `packages/rust/crates/xiuxian-qianji/docs/rfcs/2026-04-07-qianji-flowhub-planning-source-model-rfc.md`
  so the minimal normative surface is now root `qianji.toml`, root
  `flowchart.mmd`, `blueprint/`, and `plan/`, with `tasks.md` explicitly
  rejected as a duplicate non-surface
- changed the RFC control loop to a unified `qianji show` plus `qianji check`
  model, where `show` exposes `flowchart.mmd,{blueprint,plan}` and `check`
  owns `[[validation]]`, flowchart alignment, boundary, drift, and status
  legality checks instead of introducing a separate `plan-track` command
- synchronized `packages/rust/crates/xiuxian-qianji/README.md`, the Qianji
  MOC, and the governing planning-source blueprint so the package-doc surface
  now points at the thinner work-surface model and the eventual `show` /
  `check` execution loop
- focused validation for this slice is green:
  `direnv exec . git diff --check -- .cache/agent/blueprints/qianji_flowhub_planning_source_model.md .cache/agent/execplans/qianji-flowhub-rfc-minimal-work-surface-show-check.md packages/rust/crates/xiuxian-qianji/docs/rfcs/2026-04-07-qianji-flowhub-planning-source-model-rfc.md packages/rust/crates/xiuxian-qianji/README.md packages/rust/crates/xiuxian-qianji/docs/00_mocs/20260315141000-qianji-orchestrator-moc.md docs/GTD/DAILY_2026_04_07.md`
