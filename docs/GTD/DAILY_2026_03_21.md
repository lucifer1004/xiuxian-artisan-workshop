---
type: knowledge
metadata:
  title: "GTD: Sovereign Nexus (2026-03-21)"
---

# GTD: Sovereign Nexus (2026-03-21)

## Current Delivery

### [IN-PROGRESS] Feature: Repo Intelligence Hot-Path Removal and Visible Progress

- **Objective**: Remove repository-intelligence cold analysis from Studio request paths, move code-search and repo detail reads onto background-prepared snapshots, and expose real repo-index progress separately from VFS state in the frontend.
- **Status**: [Ready for Live Validation] The execution record is tracked in `[[.cache/codex/execplans/wendao-repo-index-hot-path-removal.md]]`. `xiuxian-wendao` now has a dedicated `gateway::studio::repo_index` feature folder with host-derived adaptive bounded concurrency, per-repo phases (`queued/checking/syncing/indexing/ready/unsupported/failed`), snapshot storage, interactive-priority queue promotion, and aggregate status reporting that now also carries active repo IDs plus target/max concurrency. `code_search` no longer performs request-time repository analysis or raw checkout scanning; it now consumes repo-index snapshots and returns explicit `partial/indexingState/pendingRepos/skippedRepos` metadata. Repo-intelligence read handlers now use cached-only analysis lookup so missing snapshots surface as `REPO_INDEX_PENDING` instead of cold analysis. Julia plugins now run a cheap preflight before heavy analysis so unsupported layouts settle into terminal `unsupported` state instead of feeding retry loops. On the frontend, repo-index progress is now independent from `vfsStatus`, `FileTree` polls `/api/repo/index/status` without triggering repository materialization, the bottom status bar shows repo-index counts/current repo separately from VFS connectivity, and SearchBar now treats pending/partial indexing as warning state instead of hard error state.
- **Update**: The same hot-path removal is now extended to Studio local-project symbol search. `gateway::studio::symbol_index` is a separate background coordinator for `search_symbols`, so first-hit local symbol search no longer blocks on request-path index construction. `SymbolSearchResponse` now returns `partial/indexingState/indexError`, and the frontend symbol-mode search path treats warm-up as a warning/pending state instead of a false empty result.
- **Update**: Root-cause diagnosis for the frozen `Current ADTypes.jl` state is now complete. The queue was not blocked by expensive Julia analysis; it was deadlocked in `gateway::studio::repo_index::state` because terminal ready/failed status writes attempted to call `next_attempt_count()` while already holding the same `statuses` `RwLock` write guard. That re-entrant read blocked forever, so the worker never left `indexing`. The fix centralizes terminal status writes through a helper that computes the next attempt count before taking the write lock. A bounded analysis timeout remains in place only as a fail-forward safety net for genuinely stuck analysis work.
- **Update**: Repo indexing is no longer intentionally serialized. The coordinator now derives its hard ceiling from host parallelism and adjusts the live worker window from recent repo-task latency feedback. Interactive repo requests no longer wait behind the cold background backlog; if a repo is already pending, its queued task is promoted to the front instead of being duplicated.
- **Update**: The gateway runtime no longer drops the external Modelica analyzer from the builtin registry. `xiuxian-wendao` now compiles the `xiuxian-wendao-modelica` plugin sources into the core crate behind the builtin `modelica` feature and enables that feature in the default build, closing the live `mcl` failure mode (`repo intelligence plugin \`modelica\` is not registered`). The frontend status bar also now surfaces unsupported/failed repo issue details directly from `/api/repo/index/status`.
- **Validation Plan**:
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo check -p xiuxian-wendao`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo test -p xiuxian-wendao --lib sync_projects_resets_to_idle_when_projects_are_empty`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo test -p xiuxian-wendao --lib ensure_started_marks_non_idle_for_configured_projects`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo test -p xiuxian-wendao --lib sync_repositories_only_enqueues_new_or_changed_repositories`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo test -p xiuxian-wendao --lib status_response_counts_each_phase`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo test -p xiuxian-wendao --lib build_code_search_response_skips_unsupported_repositories_when_searching_all_repos`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo test -p xiuxian-wendao --lib search_symbols_returns_payload`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo test -p xiuxian-wendao --lib search_symbols_returns_pending_payload_while_index_is_warming`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo test -p xiuxian-wendao --lib search_symbols_respects_glob_dir_filters`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo test -p xiuxian-wendao --lib repo_sync_endpoint_returns_repo_status_payload`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo test -p xiuxian-wendao --lib await_analysis_completion_returns_timeout_error_for_stuck_analysis`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo test -p xiuxian-wendao --lib record_repo_status_advances_attempt_count_without_lock_reentrancy`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo test -p xiuxian-wendao --lib gateway::studio::repo_index::state::tests::`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo test -p xiuxian-wendao --bin wendao test_build_plugin_registry_bootstraps_builtin_plugins`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop npm test -- src/components/StatusBar.test.tsx src/components/panels/FileTree/__tests__/FileTree.test.tsx src/components/panels/FileTree/repoIndexStatus.test.ts src/components/SearchBar/__tests__/searchExecution.test.ts`
- **Validation Executed**:
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo check -p xiuxian-wendao`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo test -p xiuxian-wendao --lib sync_projects_resets_to_idle_when_projects_are_empty`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo test -p xiuxian-wendao --lib ensure_started_marks_non_idle_for_configured_projects`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo test -p xiuxian-wendao --lib sync_repositories_only_enqueues_new_or_changed_repositories`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo test -p xiuxian-wendao --lib status_response_counts_each_phase`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo test -p xiuxian-wendao --lib build_code_search_response_skips_unsupported_repositories_when_searching_all_repos`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo test -p xiuxian-wendao --lib search_symbols_returns_payload`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo test -p xiuxian-wendao --lib search_symbols_returns_pending_payload_while_index_is_warming`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo test -p xiuxian-wendao --lib search_symbols_respects_glob_dir_filters`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo test -p xiuxian-wendao --lib repo_sync_endpoint_returns_repo_status_payload`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo test -p xiuxian-wendao --lib await_analysis_completion_returns_timeout_error_for_stuck_analysis`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo test -p xiuxian-wendao --lib record_repo_status_advances_attempt_count_without_lock_reentrancy`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo test -p xiuxian-wendao --lib gateway::studio::repo_index::state::tests::`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo test -p xiuxian-wendao --bin wendao test_build_plugin_registry_bootstraps_builtin_plugins`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop npm test -- src/components/StatusBar.test.tsx src/components/panels/FileTree/repoIndexStatus.test.ts`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop npm test -- src/components/StatusBar.test.tsx src/components/panels/FileTree/__tests__/FileTree.test.tsx src/components/panels/FileTree/repoIndexStatus.test.ts src/components/SearchBar/__tests__/searchExecution.test.ts`

### [IN-PROGRESS] Feature: Wendao Common Fuzzy Search Primitives

- **Objective**: Extract Wendao fuzzy matching from feature-local hardcoded logic into a shared search-primitives layer that can serve lexical scoring and Tantivy-backed fuzzy querying through one bounded crate-local abstraction.
- **Status**: [In Progress] Governance for the slice is tracked in `[[.cache/codex/execplans/wendao-common-fuzzy-search-primitives.md]]`, and the bounded Wendao implementation slice is now landed in eight layers: `xiuxian-wendao` has a shared `search` module with normalized edit-distance scoring, reusable fuzzy options, lexical and Tantivy-backed matchers, and now also a shared Tantivy document schema/index (`SearchDocumentIndex`) reused by `UnifiedSymbolIndex`, projected-page search, and now Repo Intelligence module/symbol/example service search through an `exact Tantivy -> fuzzy Tantivy -> legacy lexical fallback` strategy; projected-page family/navigation/retrieval now also route through the shared projected-page and projected page-index tree builders instead of bypassing them with raw-doc-only helpers; the dedicated projection integration targets now verify those flows with a stable in-memory analysis fixture instead of depending on the currently broken built-in Julia plugin bootstrap path, `build_projected_retrieval_hit(...)` now resolves stable projected `page_id` values through the shared page lookup instead of treating them as raw `doc_id` values, the filtered `repo_projected_` `xiuxian-testing-gate` slice is green again after accepting the deterministic projection snapshot drift, and the `repo_example_search` gate now also passes after moving example retrieval onto the shared search-document index and refreshing the stale CLI snapshot baseline. The repo-overview / repo-sync baseline cleanup is now also landed: analysis orchestration again merges plugin-provided repository metadata, relation enrichment, and skeptic verification output, while managed mirrors/checkouts now preserve read-only `status` semantics, refresh bare-mirror branch heads correctly, and emit drift/freshness metadata that makes the filtered `repo_overview` and `repo_sync` `xiuxian-testing-gate` slices green again. The fuzzy hot path itself now uses a three-row scratch-buffer edit-distance implementation, identifier-aware Tantivy fragment splitting for CamelCase / alpha-digit names, matcher-local char/scratch reuse across candidate loops, thread-local buffer reuse for the public scoring helpers, borrowed-slice Tantivy fragment iteration so the per-document `Vec<String>` fragment materialization is gone from the common rescoring path, a single-pass identifier-boundary state machine instead of `char_indices().collect::<Vec<_>>()`, thread-local buffer reuse inside `LexicalMatcher::search` itself so the shared lexical search loop no longer creates per-call `query_chars` / `candidate_chars` / `scratch` vectors, and now also a single-pass Unicode-aware prefix gate so each candidate is lowercased exactly once while the leading-character requirement is checked with `char::to_lowercase()` equality instead of ASCII-only case folding; both the boundary splitter, the consecutive lexical-search buffer clearing, and the non-ASCII prefix gate now have direct unit coverage. `FuzzySearchOptions` now also exposes a preparatory `camel_case_symbol()` profile for future Julia/Modelica CamelCase-abbreviation tuning. `TopologyIndex` keeps the shared typo-tolerant title fallback, Studio definition resolution keeps the shared lexical fallback, and `fuzzy_suggest` plus graph dedup now reuse the same edit-distance helpers. The slice remains `[IN-PROGRESS]` at the program level because `dependency_indexer::SymbolIndex` and broader cross-crate unification are still follow-up work, and the broad `cargo test -p xiuxian-wendao --lib` gate is not yet globally green due unrelated baseline failures outside this touched scope.
- **Validation Plan**:
  - `cargo fmt -p xiuxian-wendao`
  - `cargo test -p xiuxian-wendao --lib`
  - targeted `cargo test -p xiuxian-wendao --test ...` coverage for touched fuzzy-search surfaces
- **Validation Executed**:
  - `cargo fmt -p xiuxian-wendao`
  - `cargo test -p xiuxian-wendao --lib`:
    completed with unrelated existing failures in `gateway::studio` snapshot/router tests and one `semantic_check` assertion outside the touched scope
  - `cargo test -p xiuxian-wendao search::fuzzy::tests:: --lib`
  - `cargo test -p xiuxian-wendao search::tantivy::tests:: --lib`
  - `cargo test -p xiuxian-wendao link_graph::addressing::topology::tests:: --lib`
  - `cargo test -p xiuxian-wendao unified_symbol::tests:: --lib`
  - `cargo test -p xiuxian-wendao zhenfa_router::native::audit::fuzzy_suggest::tests:: --lib`
  - `cargo test -p xiuxian-wendao gateway::studio::search::definition::tests:: --lib`
  - `cargo test -p xiuxian-wendao analyzers::service::search::tests:: --lib`
  - `cargo test -p xiuxian-wendao --test repo_projected_page_search_test --test repo_projected_page_navigation_search_test --test repo_projected_page_family_search_test --test repo_projected_retrieval_test`
  - `cargo test -p xiuxian-wendao --test xiuxian-testing-gate repo_projected_`
  - `cargo test -p xiuxian-wendao --test xiuxian-testing-gate repo_example_search`
  - `cargo test -p xiuxian-wendao --test xiuxian-testing-gate repo_overview`
  - `cargo test -p xiuxian-wendao --test xiuxian-testing-gate repo_sync`
  - `cargo test -p xiuxian-wendao gateway::studio::search::definition::tests:: --lib`
  - `cargo test -p xiuxian-wendao search::tantivy::tests:: --lib`
  - `cargo test -p xiuxian-wendao analyzers::service::search::tests:: --lib`
  - `cargo test -p xiuxian-wendao search::fuzzy::tests:: --lib`
  - `cargo test -p xiuxian-wendao search::tantivy::tests:: --lib`
  - `cargo test -p xiuxian-wendao analyzers::service::search::tests:: --lib`
  - `cargo test -p xiuxian-wendao projected_page_search_uses_shared_tantivy_fuzzy_index_for_typos --lib`:
    blocked before test execution by unrelated existing compile failures in `gateway::studio::analysis` tests and `docs_governance` tests
  - `direnv exec . cargo test -p xiuxian-wendao analyzers::projection::builder::tests:: --lib`
  - `direnv exec . cargo test -p xiuxian-wendao analyzers::projection::search::tests:: --lib`
  - `direnv exec . cargo test -p xiuxian-wendao gateway::openapi::document::tests::bundled_gateway_openapi_document_covers_declared_route_inventory --lib`
  - `direnv exec . cargo test -p xiuxian-wendao gateway::studio::analysis::tests::analyze_markdown_returns_ir_and_projections --lib`
  - `direnv exec . cargo test -p xiuxian-wendao gateway::studio::studio_repo_sync_api_tests:: --lib`
  - `direnv exec . cargo fmt -p xiuxian-wendao`
  - `direnv exec . cargo test -p xiuxian-wendao --lib`
- **Update**: `cargo test -p xiuxian-wendao --lib` is green again after fixing projected doc-kind inference for standalone `reference` docs, restoring the bundled `/api/analysis/code-ast` OpenAPI entry, and accepting the current studio snapshot baselines for the repo-sync / Markdown-analysis contract shape.
- **Follow-up**: After the common primitives settle inside `xiuxian-wendao`, decide whether the same contract should be lifted into a broader workspace search foundation spanning additional crates and query families.

### [IN-PROGRESS] Refactor: Wendao Git Checkout Structure Closure

- **Objective**: Finish the `git::checkout` modularization so repository transport/materialization is cleanly separated before the next live gateway validation.
- **Status**: [Ready for Live Validation] The checkout subsystem now fully lives under `xiuxian-wendao::git::checkout`, ghq-style managed paths and config-owned remote URL override remain in place, checkout mutation locking is isolated in `lock.rs`, checkout types are isolated in `types.rs`, and `checkout/mod.rs` is now interface-only. A small unrelated `search/tantivy.rs` call/signature drift was also corrected because it blocked the targeted `lib test` gate.
- **Validation Plan**:
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo check -p xiuxian-wendao`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo test -p xiuxian-wendao --lib managed_checkout_lock_`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo test -p xiuxian-wendao --lib resolve_repository_source_overrides_managed_remote_url_from_config`
- **Validation Executed**:
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo check -p xiuxian-wendao`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo test -p xiuxian-wendao --lib managed_checkout_lock_`
  - `direnv exec /Users/guangtao/projects/xiuxian-artisan-workshop cargo test -p xiuxian-wendao --lib resolve_repository_source_overrides_managed_remote_url_from_config`

### [IN-PROGRESS] Refactor: Wendao Large Rust File Modularization

- **Objective**: Build a lossless split plan for every Rust file larger than 400 lines under `xiuxian-wendao`, so later refactors can move one feature folder at a time without changing public module paths or behavior.
- **Status**: [In Progress] Inventory completed across 35 files. The canonical execution plan is `[[.cache/codex/execplans/wendao-large-rust-modularization.md]]`, and the package-level roadmap sync is `[[packages/rust/crates/xiuxian-wendao/docs/06_roadmap/405_large_rust_modularization.md]]`. Three implementation slices are now landed: `gateway/studio/search/handlers.rs` has been converted into `gateway/studio/search/handlers/` with interface-only `mod.rs`, focused leaf modules, preserved public exports, and preserved unit/snapshot fixtures; `gateway/studio/types.rs` has been converted into `gateway/studio/types/` with interface-only `mod.rs`, grouped DTO leaf modules, preserved public type names, and the same `studio_type_collection()` façade; and `analyzers/query.rs` has been converted into `analyzers/query/` with interface-only `mod.rs`, query-family leaf modules, and preserved `crate::analyzers::query::*` re-exports. The governing policy file is `.agent/PLANS.md`, while `.data/blueprints/project_anchor_semantic_addressing.md` is still absent, so the plan anchors on current module topology and stable public API paths. The current worktree now has a green `cargo check -p xiuxian-wendao --lib --keep-going` gate again, and `search/fuzzy.rs` is the next planned bounded slice.
- **Validation Plan**:
  - verify the oversized-file inventory command remains reproducible
  - verify GTD and package docs both reference the same execplan
  - run `cargo fmt -p xiuxian-wendao`
  - run targeted contract and projection validation for the active modularization slice
  - keep the library `cargo check` gate green while structural slices land
- **Validation Executed**:
  - `direnv exec . sh -lc 'rg --files packages/rust/crates/xiuxian-wendao/src -g "*.rs" | xargs wc -l | sort -nr'`
  - package docs synced via `packages/rust/crates/xiuxian-wendao/docs/index.md` and `packages/rust/crates/xiuxian-wendao/docs/06_roadmap/405_large_rust_modularization.md`
  - `direnv exec . cargo fmt -p xiuxian-wendao`
  - `direnv exec . cargo test -p xiuxian-wendao gateway::studio::search:: --lib`
  - `direnv exec . cargo check -p xiuxian-wendao --lib --keep-going`
  - `direnv exec . cargo test -p xiuxian-wendao --test repo_projected_page_search_test --test repo_projected_page_navigation_search_test --test repo_projected_page_family_search_test --test repo_projected_retrieval_test`
  - `direnv exec . cargo test -p xiuxian-wendao search_uses_shared_tantivy_fuzzy_index_for --lib`
  - `direnv exec . cargo test -p xiuxian-wendao repo_sync_endpoint_returns_repo_status_payload --lib`

### [IN-PROGRESS] Refactor: Repo Intelligence Namespace Closure

- **Objective**: Finish the post-rename cleanup after moving the live Repo Intelligence implementation from `src/repo_intelligence/` into `src/analyzers/`, so code, tests, and docs stop drifting across two parallel names.
- **Status**: [In Progress] `cargo check -p xiuxian-wendao -p xiuxian-wendao-modelica`, `cargo test -p xiuxian-wendao --lib`, and `cargo test -p xiuxian-wendao-modelica` are now green again after realigning `xiuxian-wendao-modelica` to the live analyzer record contracts, closing the feature-gate mismatch in `analyzers::service`, restoring the affected gateway repo test fixtures by prewarming repository analysis caches, splitting the brittle `StudioState::new()` bootstrap threshold from the stricter router/API latency threshold, and cleaning the touched warning sources (`RepoIndexSnapshot.revision`, search-support test helpers, local fuzzy-suggest helper, and `execute_search_async` under feature-disabled builds). The remaining cleanup is mostly narrative and debris-oriented: roadmap/docs references still need to describe the live implementation as `xiuxian-wendao::analyzers`, and `.bak` files in the dirty source tree need a separate safe deletion pass because at least one `.bak2` path is still part of a tracked rename.
- **Validation Plan**:
  - `cargo check -p xiuxian-wendao -p xiuxian-wendao-modelica`
  - `cargo test -p xiuxian-wendao search_knowledge_returns_payload --lib`
  - `cargo test -p xiuxian-wendao test_levenshtein_distance_identical --lib`
  - `cargo test -p xiuxian-wendao --test repo_projected_page_navigation_test --test repo_projected_page_family_cluster_test`
- **Validation Executed**:
  - `cargo check -p xiuxian-wendao -p xiuxian-wendao-modelica`
  - `cargo test -p xiuxian-wendao --lib`
  - `cargo test -p xiuxian-wendao-modelica`
  - `cargo test -p xiuxian-wendao search_knowledge_returns_payload --lib`
  - `cargo test -p xiuxian-wendao test_levenshtein_distance_identical --lib`
  - `cargo test -p xiuxian-wendao --test repo_projected_page_navigation_test --test repo_projected_page_family_cluster_test`

## Blueprint Alignment

- Reference: `[[.data/blueprints/project_anchor_semantic_addressing.md]]`
- The blueprint file is still absent in the workspace, so the active execution contract for this slice is `[[packages/rust/crates/xiuxian-wendao/docs/06_roadmap/402_repo_intelligence_mvp.md]]` plus `[[.cache/codex/execplans/wendao-common-fuzzy-search-primitives.md]]`.

### [DONE] Feature: Xiuxian Performance Gate V1 (Wendao-First)

- **Objective**: Land a reusable performance test kernel in `xiuxian-testing` and integrate it into `xiuxian-wendao` through the single test gate entrypoint with `performance` and `performance-stress` feature lanes.
- **Status**: [Done] `xiuxian-testing` now exposes `performance` APIs (`PerfBudget`, `PerfRunConfig`, `PerfReport`, `run_sync_budget`, `run_async_budget`, `assert_perf_budget`) and persists report JSON to `.run/reports/...` under schema `xiuxian-testing.perf-report.v1`; structure validation now allows `tests/performance/`; `xiuxian-wendao` now forwards `performance` features, mounts performance modules in `tests/xiuxian-testing-gate.rs`, includes `tests/performance/{latency,throughput,stress}` gates, adds Criterion bench target `benches/wendao_performance.rs`, and wires new `just`/`nix` CI tasks for quick gate, stress lane, and bench compile lane.
- **Validation Executed**:
  - `direnv exec . cargo test -p xiuxian-testing --features performance`
  - `direnv exec . cargo nextest run -p xiuxian-wendao --features performance --test xiuxian-testing-gate` (pass after performance-only gate mount)
  - `direnv exec . cargo nextest run -p xiuxian-wendao --features "performance performance-stress" --test xiuxian-testing-gate --run-ignored ignored-only`
  - `direnv exec . cargo check -p xiuxian-wendao --features performance --benches`
  - `direnv exec . env CARGO_TARGET_DIR=.cache/cargo-target/xiuxian-wendao-bench cargo bench -p xiuxian-wendao --features performance --bench wendao_performance --no-run` (long-running release+LTO compile lane; non-blocking for PR gate)
