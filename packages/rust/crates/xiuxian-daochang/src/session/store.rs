//! Legacy session store extension slot.
//!
//! The previous shortcut-handling implementation has been superseded by
//! `agent/session_context/window_ops/backup_lifecycle.rs`. This module remains
//! as an empty anchor because `session/mod.rs` still imports `store`.
