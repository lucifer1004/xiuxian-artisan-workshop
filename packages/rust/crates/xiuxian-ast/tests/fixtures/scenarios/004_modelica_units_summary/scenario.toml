# Scenario: Modelica Units Summary
# Description: Parse the Modelica.Units package with nested classes and type definitions

[scenario]
id = "004_modelica_units_summary"
name = "Modelica Units Summary"
description = "Parse the Modelica.Units package with nested classes and type definitions"
category = "modelica_parser"

[input]
type = "modelica_file_source"
paths = ["input"]
